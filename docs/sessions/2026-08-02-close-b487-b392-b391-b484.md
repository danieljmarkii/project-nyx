# Close-out: B-487, B-392, B-391, B-484 (doc hygiene)

**Date:** 2026-08-02

A four-item hygiene close-out — no build-phase change, no app code, no schema. Two items were fully actioned (external + backlog); two were Tier-2 doc-content moves proposed in draft first, then applied after PM approval in the same session. Shipped via **#555** (draft).

## What shipped

- **B-484 — delete `zz-deploy-probe` Edge Function → Done (already gone).** Verified against the live project: `list_edge_functions` returns 8 functions, none `zz-*`, and `get_edge_function(zz-deploy-probe)` returns `Function not found`. It had been deleted via the dashboard between sessions. No action needed beyond confirming and closing the row.
- **B-391 — draft-PR sweep → Done.** Closed the named superseded pair with explanatory comments: **#380** (B-247 PR 2 shared-incident refactor — already on `main`: `_shared/incident-analysis.ts` present, `analyze-vomit` consumes the descriptor, `analyze-stool` live) and **#366** (B-005 archive on the *global* catalog — superseded by B-354's per-account re-scope taking slot 033 and the shipped `035_food_items_archived_at`, which drops the proposed `archived_by_user_id` as per-user-by-construction). Sweep found **23** open drafts (was 13 when filed); the other 21 are parked docs/specs awaiting a PM ruling — triaged in the PR, none closed unilaterally (that's a PM call).
- **B-487 — re-home the 4 orphaned rulings → Done (Tier-2, PM-approved).** See below.
- **B-392 — refresh `nyx-technical-spec` → Done (Tier-2, PM-approved).** See below.

## B-487 — the premise was stale

The four rows were kept inline in `CLAUDE.md` § Open Questions on 2026-07-25 "because CLAUDE.md was the only copy of their ruling." Verification this session showed that is **no longer true** — every one now has a fuller home a session building the surface actually loads:

| Ruling | Fuller home (verified) |
|---|---|
| Ask D2/D7 | `docs/nyx-ask-requirements.md` §2 (D2/D7 rows) + §6 (the full boundary + five mechanisms) |
| PDF-library / HTML-first | `docs/nyx-vet-report-requirements.md` §8.2 + §14 S7 |
| **B-340** red-flag elevation (derive from owner-editable structured fields, never the stale cached `visual_flags`) | `supabase/functions/generate-signal/detection.ts` (B-340 lane, l.353–360 / 4046–4134) + `generate-report` |
| **B-247** PR 3 stool seam (mucus = structured-field-only; pre-vision `repeated_loose_stool`) | `supabase/functions/analyze-stool/index.ts` header (l.26–36 / 74–77) + `_shared/incident-analysis.ts` (l.441–446 / 484) |

Applied: removed the 4 rows from `CLAUDE.md` (+ the "four resolved rows stay below" header-note sentence); appended them **verbatim** to `docs/decisions-archive.md` → new "Re-homed 2026-08-02 (B-487)" section, each with its home pointer (rows pulled byte-exact from `git show HEAD:CLAUDE.md`); updated that file's "What stayed behind" section to past tense. The two *clinical* guardrails were additionally reinforced as **Pattern 9** in the auto-loaded `clinical-guardrails` skill (the read-side complement to the existing Pattern 7 write-back rule) — Pattern 9 text was checked against the shipped code, not authored fresh. Net: ~7 KB off every turn of every session, with the two live build guardrails now discoverable in the skill a session on those surfaces auto-loads.

## B-392 — tech-spec accuracy reconciliation

`docs/nyx-technical-spec-v1_0.md` bumped to header **v1.1 / 2026-08-02** (header carries the version; the `-v1_0.md` filename stays — renaming breaks references, per the living-vs-frozen rule). Applied, verified against live state:

- 3 of 5 Open-Eng questions marked **Resolved** with pointers: PDF-library → HTML-first / B-144; GDPR cascade → hard-delete / B-039; AI-Signal model → Haiku 4.5 architecture B. (Minimum-SDK + push-provider stay Open; the SDK row now records the live Expo SDK 57.)
- Design tokens refreshed off `constants/theme.ts` (the single source of truth): `fontBody 'System'→'Geist'`, `fontDisplay 'TBD'→'Newsreader'`, `colorAccent 'TBD'→'#00C2A8'`, neutrals filled, `colorBrandNight '#13112E'` added (additive brand/night; teal stays the sole accent).
- Migration count `001 … 021` → `001 … 049`; Edge-Functions list 5 → 8 (adds generate-report, analyze-stool, ask); `nyx-schema-v1_0.sql` label corrected from "Canonical schema" to "FROZEN snapshot (canonical = supabase/migrations/)".
- Event-type comment corrected: `weight_check` + `medication` are shipped features, not "post-MVP".
- Version-history row added.

**Deliberately out of scope (not done here):** the tech-spec §7 (Vet Report Export) AC still lists "severity averages" and lacks the must-carry-sections / HTML-first-delivery record — that is the separate vet-report `§13` flagged-edit set tracked as an Open PM Action Item in `STATUS.md` (line ~407), owned by the vet-report track, not B-392.

## Gates / DoD

- **`tsc --noEmit` + `jest --ci`** — green via the pre-push hook (183 suites / 4009 tests). No code changed, so the result is expected; the change is docs/backlog/skill-doc only.
- **Tests** — N/A: no Zustand store, Edge Function, or `lib/` utility touched. The `clinical-guardrails` SKILL.md edit is a decision-record documentation entry, not compiled logic.
- **`adversarial-reviewer`** — N/A: **no clinically- or statistically-load-bearing logic changed.** Pattern 9 documents two rulings (B-340, B-247) that each carried their own adversarial-reviewer pass when the *code* shipped; this session re-homes the decision record and changes no code path. Pattern 9's technical claims were re-verified against `detection.ts` / `analyze-stool` / `_shared/incident-analysis.ts`.
- **Backlog** — all four rows reconciled to `Done`/verified in the same commit; duplicate-ID check (`grep -ohE '^\| B-[0-9]+ ' … | uniq -d`) run after the `main` merge → empty.
- **Persona sign-off** — Product Owner / Backlog Steward ✓ (reconciliation, ID-collision check, draft triage) · Dir. of Engineering ✓ (doc accuracy, no architecture/stack change) · Data Scientist / Dr. Chen ✓ (Pattern 9 restates already-reviewed guardrails verbatim; no new clinical logic) · Designer N/A · QA ✓ (no build-step AC in play; house rules held).

## Outcome

B-484 + B-391 closed; B-487 + B-392 applied after PM approval and closed. All four rows `Done` on the backlog. Shipped via **#555**.
