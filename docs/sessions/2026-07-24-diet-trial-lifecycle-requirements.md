# Diet trial lifecycle — requirements + design session (B-417)

**Date:** 2026-07-24

Backlog search for a "diet trial / elimination diet" feature found **no such row**, and the audit found why it was never filed: `diet_trials` shipped in migration 001 and six surfaces read it (profile card, `getDietTrialProgress`, widget header, vet-report trial section + scope-cascade rung 2, Signal trial-suppressions, Ask), so every later session found the table already there and only ever read from it — **nothing in the app can create a trial**. Consequence: the vet report's own **first** clinical question ("Is this diet trial working?") has never rendered with real data. Second finding: the profile card's **"% compliance" measures logging, not adherence** (`profile.tsx:192`, no filter on the trial food → a chicken-fed pet on a novel-protein trial reads 100%); the server computes the same numerator but labels it honestly (`render.ts:1021`), so the mislabel is client-only. PM ratified **D1** (lifecycle + allowed-set adherence) + **D2** (compliance fix folded into the track); **D3** (allowed-food set vs. B-351 D6) taken recommend-and-proceed and filed as an Open Question. Spec **v0.9 (pre-mock)** + 7-PR plan: `docs/nyx-diet-trial-requirements.md` (§0 carries the readiness gates; deliberately not called build-ready — no mocks yet, and B-351 slice 4 collides on the diet-trial card). Absorbs B-408; unblocks B-094. Docs-only — no app code. Shipped via #431.
