# B-152 part 2 + B-401 — email confirmation ON, honest soft-verify, no raw auth errors.

**Date:** 2026-07-24

The verify screen's "Verify later · continue for now" promised entry an unconfirmed account cannot get (it routed to the login wall); now "I'll do this later" → the Landing. The bigger catch was the line *above* it — "you can do this anytime" framed confirmation as optional the day it became the only way in. Three paths passed Supabase's raw `error.message` into an `Alert`; all now route through the pure `lib/authErrors.ts`, whose property test sweeps 13 failure shapes × 3 contexts asserting no provider string, status code or `undefined` reaches an owner. An unconfirmed sign-in carries a **Resend link** on the alert rather than bouncing back through signup; the 60s per-user SMTP interval maps to a wait, not "For security purposes…". `invalid_credentials` deliberately won't say whether an address has an account (client must not undo enumeration protection). **Infra (PM):** Resend provisioned + `getculprit.app` verified (DKIM/SPF/SPF-MX; DMARC `p=none` bonus), Supabase SMTP configured, sender ratified as `support@` (spec §5.2 amended) — but **zero auth emails sent**, so step 4 is open and the device pass is blocked. **Corrections at merge:** B-401 is `Partial`, not Done — `main`'s row expanded its scope to include the confirm-link deep-link handler, which this PR did not build; and the mid-session B-417 filing collided with `main`'s diet-trial item → **renumbered B-432**. Verified `flowType:'pkce'` is spec'd by B-280 but not yet in `lib/supabase.ts`, so that hazard is future, not live. tsc clean; jest 1724/111. shipped via #436
