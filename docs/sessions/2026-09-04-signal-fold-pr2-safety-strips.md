# Signal fold PR 2 — the safety strips: standing + acute, the ask line, the last-episode date, the FS-3 guard (CUL-785)

**Date:** 2026-09-04
**Outcome:** shipped via #799 (draft) — Home v1, PR 2 of 3. With this and PR 1 (#796) the fold reaches every card class; PR 3 (CUL-788, the motion) is the last rung. Mode: BUILD, plan-gated (the plan and three decision briefs posted to the issue; the session ran unattended, so the draft PR is the redirect point — the PR 1 precedent). Branch `claude/signal-fold-pr2-safety-strips-itej59`.

## What this was

The PM's own screen: a red "recurring vomiting, worth a vet visit" card at Newsreader size, leading Home for six weeks. DF-2 (PM, 2026-09-03) ruled that every safety card folds, the acute class included, on Dr. Chen's conditions — the strip keeps the rail at full opacity, the symptom, the ask verb, the count with its span, and the DATE of the last episode, never a counter; the record re-opens it, never the calendar. PR 1 shipped the primitive with the safety class gated closed. This session flipped the gate and built the safety strip.

## What shipped

- **`lib/signalCopy.ts`** — `stripAskLine` (the card's own vet verb, compressed to ≤ 20 characters so it can never break mid-phrase; `STRIP_ASKS` pins the four signed strings), the four safety name forms (`Recurring {symptom}` · `{Symptom} up this week` · `Eating less than usual` / `Refused the usual food` · `Blood in a {vomit|stool} photo` / `Something unusual in a … photo`, blood leading when both flags are set), the compact count forms (Dr. Chen's own `14 episodes, 5 of 8 weeks · last Aug 26`; worsening on the axis that rose with the zero-prior pair dropped exactly as the face does; intake's day count and meal denominator; the red flag's `AI read of N logged photos · last Sep 1`), `StripContext` (the last-episode instant), `stripDayLocal` (device-zone day off local components, B-514) and `stripDayUTC` (the red flag's day — the same day its sentence and phone script print), `chronicityLastEpisodeFallbackIso` (`expiresAt − 24h − daysSinceLastEpisode`), and `stripA11yLabel` = `{name}. {ask}. {count}, last {Month D}.` with the month spoken in full.
- **`lib/signalFold.ts`** — `canFold` is now true for every card class, with **`intake_decline` held closed behind `INTAKE_DECLINE_FOLDS = false`** (below). The intake row of `MATERIAL_FIELDS` keeps the spec's field with a corrected comment. **After the adversarial pass:** `RecordFacts` + a fifth material kind, `laterInstant` — `record.lastEpisodeIso` on the two standing safety types, the record's own newest episode, re-opening only on a LATER instant (never a deleted episode, never an unanswered read; `keepWitnesses` stops a transient store failure erasing the instant a fold holds); `foldedEntry` / `reconcileFolds` take the record's facts, and `useSignalFold` re-reconciles when the record moves, so a fold re-opens on the episode the owner just logged with no regen at all.
- **`hooks/useLastEpisodeDates.ts`** — the §3.4 read: `MAX(occurred_at)` over the pet's non-deleted events of the finding's `symptomType` (the engine maps `event_type` → `symptomType` one-to-one in `mapSymptomRows`, so one key per query and no symptom list to register — C-11), synchronous inside a `useMemo` so the FIRST paint of a strip carries the record's date (a date that flipped a frame after mount would look like the pet moved), keyed on the pet + the symptom set + `hydrationTick` + `signalTick` + a focus tick (the mount focus skipped — the first render already read). `null` per type when the store did not answer; the strip prints no date rather than a guess. `useSignal` exposes the row's `generatedAt` for chronicity's fallback (`generatedAt − daysSinceLastEpisode` days — late by at most a calendar day, never early; anchored on `generated_at`, not `expires_at − 24h`, after the adversarial pass pointed out the TTL is a migration default with nothing binding the two); worsening has no fallback and prints the count alone.
- **`components/home/InsightCard.tsx`** — `FoldedStrip` gains the ask line (its own `ThemedText`, textSM regular in primary ink — the rail is the only warm mark, S1) and `lastEpisodeIso`; the a11y label comes from `stripA11yLabel`; **a safety strip that cannot say its ask is not drawn** (the FS-3 runtime half), through one exported predicate, `stripRenderable`, that `LiveStack` asks before choosing the strip — after the code review found the host's fallback to the open card unproven, and a host that ignored it would have drawn NOTHING where a safety card should be. The control now renders on safety cards through the same `canFold`.
- **`components/home/SignalZone.tsx`** — reads the dates before the fold and hands them to both (`useSignalFold({ lastEpisodes })`); `LiveStack` threads the dates and `generatedAt`; the record's date wins over the fallback.
- **Tests** — `lib/signalCopy.strip.test.ts` carries the FS-3 build guard (a walk over every `InsightType` — asserted equal to `MATERIAL_FIELDS`' keys — every variant of every safety type must yield a ratified ask, under the cap, spoken in the label; a benign strip must yield none; a new safety type has to be added to the walk with its ask before it can ship), the verbatim safety forms, the FS-11 worst cases (`Recurring skin irritation` · `Check with your vet` · two-digit counts with the widest date), the local/UTC day helpers, the "a date, never a counter" screen; `lib/signalFold.test.ts` the class gate, the intake hold pinned to its constant, improving-then-relapsing through a stood-down marker, the refusing cat, the red flag's nothing-else; `InsightCard.fold.test.tsx` the safety strip's anatomy, styles, one-button C-6 check, spoken label, the intake card without a control, the FS-3 runtime refusal proven by spying the copy layer to null; `SignalZone.fold.test.tsx` the strip at rank 0 above every benign card with no inherited canvas, the record's date before the regen, the fallback, the record-over-fallback precedence, the net-zero re-open vs the window aging, the tier flip, improving-then-relapsing end to end, the all-folded zone with the ask still on screen, the acute red-flag fold and its newer-photo re-open; `hooks/useLastEpisodeDates.test.ts` the SQL shape, dedup, the ticks, the pet switch, the throw. The nothing-folded snapshot changes by exactly one node (the safety card's control, 54 lines).

## Three places the spec's premise and the shipped code disagree (decision briefs on the issue; CUL-796 on `Waiting on PM`)

**A — the intake-decline fold is not record-bounded.** DF-2's bound reads "`daysBelowBaseline` climbs daily, so an acute fold lasts one regen cycle". In `detectIntakeDecline` the loop runs exactly `consecutiveDays` iterations, so the field is a constant 1 (cat) / 2 (dog), and the refusal branch writes 0. A folded not-eating-cat strip would have stayed folded for the whole decline, with DF-5 forbidding the clock. Built: the gate held closed for that one type, the strip copy and tests shipped, the flip one line. The fail-toward-escalation default, reversible, and the ruling was made on a premise that does not hold. CUL-797 files the engine change (count the true run of below-baseline days, or emit an assessed-day marker), noting `detection.ts` is inlined by `generate-report` (CUL-19's ledger).

**B — worsening's three tiers say three things** ("worth booking a vet visit soon" / "worth a word with your vet" / "a word with your vet if it carries on"), where the §4 table gave worsening one form. The strip mirrors the card (the binding rule): firm → `Worth a vet visit`, standard → `Tell your vet`, soft → `Check with your vet`. The soft rung was first built as `Tell your vet`; the `pm-feature-review` caught the conditional hardened into an imperative, and the fix re-entered the same rule.

**C — `Call your vet today` is unreachable.** Both intake sentences end "a word with your vet if it carries on"; the feline floor is a single day, not three. The strip may not out-escalate its card, so both triggers carry `Check with your vet`; the spec's stronger feline verb is Dr. Chen's call on CUL-797.

## Verification

- `npx tsc --noEmit` clean. `npx jest`: 308 suites / 6642 tests green (the full run, every guard included) before the last fixture; the touched suites re-run green after it.
- **Mutation ledger (C-18):**

| Mutation | Red |
|---|---|
| `stripAskLine` → null for chronicity | 17 (the FS-3 walk, the verbatim forms, the strip anatomy, the zone) |
| `INTAKE_DECLINE_FOLDS = true` | 4 (the gate pin, the control, the zone) |
| Drop `FoldedStrip`'s safety-without-ask refusal | 1 (the runtime half) |
| Invert the record / fallback precedence | 0 on the first pass — no fixture had both a record date and an engine anchor — then 1 after `the RECORD wins over the engine fallback when both answer` was added |
| `LiveStack` ignores `stripRenderable` (folds on state alone) | 1 (the end-to-end open-card fallback, added after the code review) |
| The `laterInstant` comparison disabled | 6 (the saturated course, the offline re-open, the kept witness, the walk, the zone) |

The fourth row is the finding worth keeping: three green mutations proved nothing about precedence, and a guard that reds on none of the mutations you try is a fixture missing, not a guard passing. Final: `npx tsc --noEmit` clean; the pre-push hook's full run green on every commit (308 suites, 6642 tests on the first; the later commits add fixtures).

## Reviews — three isolated passes on a scratch copy of the tree, none of them anchored by this conversation

**`adversarial-reviewer` — FAIL on the first cut, both breaks fixed in this PR.** Its attempts, stated as the DoD demands:
1. *The saturated course* — drove the real `detectChronicity` with a dog vomiting once a day for 60 days → **BROKE**: `episodeCount` sits at the window's capacity, `activeWeeks` at 8, `daysSinceLastEpisode` is a floored day count pinned at 0, `tier` is pinned firm, so `materialChange` was a fixed point and the folded `Worth a vet visit` strip never re-opened across ten consecutive new episodes; the §5.3 net-zero rule is inert exactly for the densest, sickest course, and DF-5's whole rationale ("every ongoing course re-opens on its own next episode") was false for it. The transient form (a new episode landing as an old one ages out with `dsle` already 0) is common for any ≥1/day course. **Fix:** the record's newest episode as the witness (above). Re-proven by driving the fixture through `materialChange`, `reconcileFolds` and the zone; q2-day, q3-day and weekly courses still re-open on their next episode via the engine's own fields.
2. *Every strip ask against its card's template* → **BROKE** the soft worsening tier (the same catch the `pm-feature-review` made): "a word with your vet if it carries on" is the intake clause, so its strip says `Check with your vet`, not `Tell your vet`. It also named the structural limit: the FS-3 guard validates the ask against a ratified *set*, never against the card's sentence, so it cannot see this class — recorded as a residual.
3. Verified independently that `daysBelowBaseline` is arithmetically constant → the `INTAKE_DECLINE_FOLDS` hold is correct and necessary; noted it narrows a PM ruling and belongs in a decision brief, which it is (CUL-796).
4. Red flag: a newer photo, a back-dated photo (the cluster count rises), and the owner CLEARING the newest flag so an older photo becomes most recent (the instant moves backwards; any-change re-opens with a literally true reason) → all **held**. Pseudoreplication: no new path.
5. The date: a cough on a vomit card via the adjacency (cannot happen — 1:1 `event_type` → `symptomType`), deleted-then-restored (the `deleted_at` contract) → **held**; a future-dated synced row prints a date the engine never counted (no upper bound on the query) — **accepted**: the alternative is an `occurred_at <= now` bound, which would make a synced future row re-open the fold when the clock reaches it, a clock-driven re-open DF-5 forbids; the row is a clock-skew data error the pickers cannot produce. The strip's day newer than the finding's while offline → **right**, and now the fold re-opens on it too.
6. The fallback: the TTL is a migration default with nothing binding the client's `DAY_MS` to it → **fixed** by anchoring on `generated_at`; the bound (late by up to a day, never early — the escalating direction) is now in the docstring.
7. Dr. Chen's set — the refusing cat (intake fires above a folded chronicity, `SAFETY_TYPE_ORDER`; never touches the other key), the owner who folds and never books (nothing nags, nothing reassures), improving-then-relapsing (release-on-absence; the stand-down marker is a different key `canFold` refuses) → all **held**.
8. Nothing reads the clock to decide a re-open: `materialChange` takes two fingerprints; `reconcileFolds` uses `nowIso` only to stamp a release → **held, structurally**.
Also named, not fails: the two strips on one screen can print a local day and a UTC day for the same instant (each matching its own card — S10; Dr. Chen signs it below); the chronicity strip and its own card's "Most recent: N days ago" can differ by a day (CUL-798).

**`code-reviewer` — fix-before-merge on two test gaps, both closed.** (1) The record-over-fallback precedence was unproven (the reviewer's copy predated the fixture this session had already added after its own mutation pass — the same finding from two directions). (2) The host's fallback to the open card when a safety strip cannot say its ask was proven only for `FoldedStrip` in isolation; through `LiveStack` a regression would have drawn a blank row. Fixed with the one shared predicate + the end-to-end fixture. Nit taken: the `FoldedStrip` refusal reads in one line rather than by implication of the label. Confirmed clean: the synchronous `useMemo` read (reentrancy, the first-focus skip, the pet-switch pairing), the `??` fallback, the FS-3 walk's exhaustiveness (TypeScript's `Record<InsightType, …>` plus the runtime key equality), the snapshot's single hunk, tokens / `ThemedText` / C-8, C-6 tappability.

**`pm-feature-review` — SHIP-SHAPED on the re-open flow, the acute strip and the VoiceOver label; NEEDS-WORK on four flows, each answered:** the strip-vs-card date disagreement → CUL-798 (High); no "it comes back" line on the one-tap face fold, and a control that may not be found → CUL-799 (`Waiting on PM`); the soft-tier ask → fixed; the intake card as the unexplained odd one out → CUL-796. INSUFFICIENT without a device screenshot: the all-folded zone and a safety strip beside a benign strip at default type and at the largest accessibility size (the manual steps). Named and left: `AI read of 2 logged photos` can read as a thin denominator (the face's own sample line says the same; a reword is on CUL-796's list); `Something unusual in a stool photo` is the vaguest string under the strongest ask (spec-verbatim); the date lets an owner do the subtraction the copy refuses to (the spec's own call — a date over a counter).

**Persona sign-off.** Dr. Chen ✓ — the four strings (`Worth a vet visit` / `Tell your vet` / `Check with your vet` / `Call your vet`) each the card's own verb, none stronger, none softer; the date a DATE never a counter; the saturated-course break stated and closed with the record's witness; the intake hold as his dissent asked; signs the local-vs-UTC day split as each strip matching its own card. Designer ✓ / `nyx-voice` ✓ — three short lines, each its own node, the ask in plain primary ink, no `!`, no glyph, no vetoed word (test-pinned per variant); the two flows the product read sent back are filed, not shipped silently. Data Scientist ✓ — one predicate for "has a strip"; the record witness is increase-only (later-only) like every count; DF-4's dissent stands and this is its first vindication (the client table missed the saturated course; a server token would have too — the witness is the record, not the payload). Dir. of Eng ✓ — no engine change, no schema, one AsyncStorage key, the hook's read indexed and synchronous by intent. T&S ✓ — device-local reader state, wiped by name, nothing new leaves the account. QA ✓ — §9 rows below. PO ✓ — CUL-785 In Review, CUL-796 / 797 / 798 / 799 filed, the claim released by the outcome comment.

## DoD

- AC (fold spec §9, PR 2 rows): **pass** on every row but the on-device pass (the PR's manual steps) — with the acute `daysBelowBaseline` row built and pinned but gated (CUL-796).
- Anti-pattern scan: **pass** (three reviews; the two catches fixed).
- Types + lint: **pass**.
- Automated tests: **pass** — every touched module carries tests; six mutations, six reds.
- Secrets: **N/A** — none new.
- Persona sign-off: above.
- Adversarial review: **pass, with the falsification attempts stated above** (FAIL → fixed).
- Future-self: the record witness is the pattern to keep — when the engine's fields can saturate, the record is the only monotone fact the client has, and it costs one indexed aggregate. `INTAKE_DECLINE_FOLDS` is the pattern to watch: a `const` that narrows a ruling is honest only while its brief is open (CUL-796).
- Dev Handoff + QA script: in the Session Summary.
- PM actions: filed (CUL-796, CUL-799 on `Waiting on PM`).

## Residuals

- The intake-decline hold (CUL-796 to ratify; CUL-797 to make the premise true).
- Tier-2 edits to `docs/nyx-signal-fold-requirements.md` §0 DF-2 / §3.4 / §4 / §7 proposed on CUL-796 — not written; the spec header still says v1.2.
- On-device pass on the PM's own record — the seed cannot reproduce six weeks of chronicity (the PR's manual steps).
- The FS-3 guard checks the ask against a ratified set, not against the card's sentence; a guard that asserted the strip's clause against the template would have caught the soft tier before two reviewers did. Worth a fixture in PR 3 or the next strip change.
- The record witness covers the two standing safety types only; the benign symptom types re-open on their own count fields as the spec's table says, and a saturated benign course has no safety cost.
- PR 3 (CUL-788): the motion, which now also has to choreograph a three-line strip.
