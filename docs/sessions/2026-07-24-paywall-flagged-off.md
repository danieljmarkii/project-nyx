# Paywall flagged off for v1 — the live `app_config` flip verified; guide step 6 ✅, B-330 closed, monetization Track 2 complete

**Date:** 2026-07-24

The PM ran `UPDATE app_config SET value='false'::jsonb WHERE key='paywall_enabled'` and asked whether that closed the step. **Verified rather than assumed:** an `UPDATE` prints "Success. No rows returned" whether it matched 1 row or 0, so the live value was read back — `false`, `jsonb_typeof` = **boolean**, `updated_at` 21:22 UTC. The type mattered: a string `"false"` would have failed `lib/appConfig.ts:148`'s `typeof raw === 'boolean'` check and silently resolved to the shipped default, leaving the flip looking done while the paywall stayed reachable. Confirmed the two code halves still hold (primary route gate `pet-age.tsx:68`; defensive deep-link bounce `paywall.tsx:79`) and that migration 030's seed is `ON CONFLICT DO NOTHING`, so a re-applied migration can't clobber the flip. Two residuals recorded, neither blocking: an install whose config cache predates the flip can show the mock once more before its next foreground refresh (fresh installs never can — no cache → fail-closed default), and the guide's step-6 kickoff prompt described a `constants/flags.ts PAYWALL_ENABLED` constant that was never built (T2-5 shipped the server-flippable flag instead) — corrected in the guide so the step-10 artifact check looks in the right place. No app code, no schema, no deploy; tests N/A (docs/config only). — shipped via #432
