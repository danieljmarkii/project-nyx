# History v2 step 2, HV-6: the row's rules, on Home and History

**Date:** 2026-09-25

Shipped via #914 (CUL-1163; fixes CUL-1121 and CUL-1197).

## The ask

HV-6 of History v2, one session and one PR, run beside HV-7, HV-8 and HV-9. Home's day spine (behind `design_v2`) folded meals logged back to back into one line like "3 meals" whatever was eaten, so a refused lunch disappeared inside the line (CUL-1121). The issue gave the one row Home and History share round 3's rules: a run joins only meals of one product eaten normally (rule B), only a run carries a chevron (rule D), every meal is named and a run names its product (rule K), the dose row carries its four chips and names its drug and its meal, the meal a timing line measures from keeps its own row, and the read slot draws the one predicate's states. `lib/dayNodes.ts`'s signature and `components/historyV2/` were off limits (HV-7 renders from the pipeline in parallel). Plan posted; the PM ruled two open points and said go.

## What changed

- **Rule B, one predicate** (`lib/spineCompaction.ts`): a run joins meals of one product key and one kind on one local day, each rated Most or All or unrated, with no photo, note, dose given in it, timing anchor, or found or estimated time. A recorded rating below Most (or one this build does not know) is always its own row; a missing rating never breaks a run, so CUL-1118 cannot change what a run means. Ties sort on the row id (rule H).
- **One product across wet and dry** (`lib/dayEvents.ts`): the owner's library holds "Selected Protein PR, Dry" (dry_kibble) and "Selected Protein PR, Wet" (wet_canned) as two items. A trailing word is dropped only when it repeats the item's own recorded format (`FORMAT_LABEL`) and the row shows that format as its tag; that name, folded by `canonicalizeBrand`, is the product key. A Class A key: convergent, pinned by a cross product property test. Before and after, over every account: 4 of 131 food items carry a format echo (2 accounts), and all 4 now pair into 2 shared keys, covering 341 meals.
- **Rule K** (`lib/spineNode.ts`): a run reads *4 meals · Royal Canin · Selected Protein PR* over *1 wet · 3 dry* (or *all dry*); a single meal reads *Meal · Royal Canin · Selected Protein PR* beside its DRY tag. One `foodLabelOf`, now in `lib/food.ts` (the lane's copy in `lib/patternsTiming.ts` is gone; its evidence label now joins with " · ").
- **The dose row:** the drug from the item, else the course, else only *Medication*; the four shipped chips; *in the 1:00 PM meal* from the stored `paired_event_id` (a clock in prose, no leading zero), with the meal's intake beside it when it was not finished (in the meal chip's ink); *Unconfirmed* only for a dose in doubt (`isComboDoseInDoubt`). The meal says *with Prednisone* (or *with 2 doses of Prednisone*, *with a dose*).
- **The rest of the row:** the intake chip (All and Most teal, Some grey, *Picked at* and Refused rose), with the format tag a shade lighter than a grey chip so *DRY SOME* reads as two facts; FOUND or ESTIMATED under a time the owner did not witness (`SpineRowFrame` gained an optional tag, the column unchanged without one); *Weight · 9.9 lbs*; one accessible label per row in reading order (C-8), speaking commas where the row draws a middle dot. A run's formats inflect a noun (*2 toppers*, *all treats*).
- **The read slot:** the rose word, the grey *Photo not read* beside round 5's hatched mark, the breathing tick, and nothing for a calm read. The read gate keys on what can carry a read, never the symptom tint (`mayCarryRead`, shared by the pipeline and TodayCard's copy read; CUL-1197). TodayCard hands a readable row's photo to the pipeline only once the phone's copy has answered for it, and a failed look (`readAnalysisCopy`, `null`) keeps the card's last answer: no grey flash on first paint, and a rose the card drew stays drawn (CUL-1198 item 1, Home's half).
- **Open in place, the static half:** every member of an opened run is the same full row a single event draws, with the run's rail over the thread.
- **Home's today read** (`lib/todayEventsQuery.ts`, a tested constant read through `readTodayEvents`): it never selected the meal's intake rating, so on a cold open every refusal read as unrated and CUL-1121's fix would have done nothing on Home. It now also carries the dose's stored pair and that meal's intake, the course name (never across pets) and the weight, and it decides the day on parsed instants: a row pulled from another phone at exactly midnight is spelled `+00:00` and a TEXT bound dropped it (C-40).
- **Home's store says what the record says between reads:** a rating written at insert (the *Didn't eat* sheet) rides the optimistic row and re-reads Home; a dose re-rated or re-vehicled after the fact goes through one path (`rateDoseAdherence` / `recordDoseHowGiven`, `lib/medicationDose.ts`) that re-reads Home, on all four screens that do it, guarded by name like CUL-1087's meal path; both optimistic dose rows (the picker and the label capture's first dose) are built by `optimisticDoseRow` from the write's own facts.
- **The timed meal is stable:** the lane's feedings sort on (time, id) before it runs, and now its vomit onsets too, so two bowls or two vomits at one instant always resolve the same way (rule H). A cross-day anchor rides in an optional `timings.timedElsewhere` for History's per-day cards.
- **`guards/dayRowOneWay.test.ts`** (AC 15): Home and History draw a day only through `DayNodeRow` over the pipeline. History's side asserts zero callers until HV-7 lands its list (C-32).
- **Spec v1.5:** the two rulings as ⚠ RULED errata (§3.6, §5.4, AC 18).

## Decisions

- **PM rulings:** an unnamed dose says only *Medication* (never *no medicine named*, which is false while a sync is in flight); a finished `not_enough_to_say` read draws the grey *Photo not read* (nothing was really checked), and its verdict still rides for the Signal gallery's words.
- **Team calls, stated before building and unobjected:** the product key from the item's own format (above); Home's today read widened; CUL-1197 absorbed; *Photo not read* keeps H-4b's words (false only before a fresh install's first pull, or after a failed copy, CUL-1198).
- **The chip reads *Picked at*, as the spec and round 5 draw it.** The first build used the log sheet's *Picked* (B-035, one spelling per rating) without a brief; the PM pass counted three spellings of one rating in one glance on a meal carrying a dose, so the row went back to the spec. The log sheet keeps *Picked*: a chip the owner taps is a choice among five, and changing the capture path is not this issue's.
- **Home does not read the meal's reverse link to its doses.** Home learns a meal's doses from today's own rows, which an Undo edits in place; a count read at load would keep saying *with Prednisone* over an undone dose. A dose on another day than its meal (only reachable by re-timing across midnight) is therefore not a vehicle on Home: CUL-1229.
- **A found or estimated meal is its own row:** a run's time column holds only its range and cannot carry a member's tag.

## Verification

- `tsc --noEmit` clean. Jest: the full suite green at UTC (numbers in the PR), and the touched suites green under Pacific/Kiritimati, Pacific/Chatham and Pacific/Honolulu.
- Every new guard was proven by breaking the source, each red: the intake column dropped from the read, a refusal allowed into a run, the timed-meal break removed, the tint gate restored (CUL-1197), a row internal imported by a surface, the onset sort removed (both same-instant cases), the midnight bound compared as text, the photo handed over before its copy answered (and a meal's photo held back), a failed look treated as an empty answer, the rating dropped from the sheet's mirror, the rated insert's refresh removed, the dose re-rate's refresh removed, the pair dropped from the optimistic dose row, and a screen writing adherence itself.
- Class A (the product key), measured over every account: 4 of 131 food items carry a format echo, in 2 accounts; all 4 pair into 2 shared keys, covering 341 meals.

## Reviews

- **adversarial-reviewer: FAIL, then fixed.** The pure pipeline held every attack, including the three named ones on the cold read (Saturday 8 AM All, noon Refused, 5 PM All is three rows; a picked-at vehicle with a same-minute decoy names the pair and never the decoy; an unnamed refused dose reads *Medication* plus Refused). It broke Home's store: a refusal from the *Didn't eat* sheet folded into a run (B1) and a dose downgraded on its card kept a teal *Given* (B2), both fixed above; same-instant vomits resolved by arrival order (B3, fixed; the lane's own rule is CUL-1230); the midnight text bound (P1, fixed). The stored pair against a later *given directly* (B4) is a PM call, CUL-1231; a time-only meal edit leaving the line stale (P2, predates HV-6) is CUL-1232; the cross-day vehicle (P3) is CUL-1229.
- **pm-feature-review:** the safety core SHIP-SHAPED (refusals never fold, every meal named, the dose names its meal and *Unconfirmed* resolves one tap in); the read slot NEEDS-WORK. Fixed here: the first-paint flash, the chip word, the leading zero, the spoken middle dots, the noun plurals, the tag's ink, the hatched mark. To the PM: how a calm read renders against the Signal gallery (a persona conflict, CUL-1233), the unclear read's words now that the record's side is known (CUL-1234, a revisit of the 2026-09-25 ruling), and Home's count line naming unfinished meals (CUL-1235). The timing line's two spellings of hours is CUL-1236; the kill switch dead end went to CUL-1174, the silent-rose note to CUL-1198, and the device checks (a grazer's day, the rose budget) to CUL-1171.
- **code-reviewer:** no blocking findings; its cleanups applied (the optimistic dose row tested through the real write, the one-way guard's third-surface blind spot stated, the unread mark's geometry named).
- **Personas:** Designer ✓ (principles 1, 3, 5; C-1 inks, C-8 labels) · Engineer ✓ · Data Scientist ✓ (rule B keys on the recorded rating; one product key, convergent) · Dr. Chen ✓ (a refusal is always its own row; *Unconfirmed* never lost to a stale store) · QA ✓ (AC 15 to 19, the resting half of AC 24, CUL-1121's counterexample) · Trust & Safety N/A.
