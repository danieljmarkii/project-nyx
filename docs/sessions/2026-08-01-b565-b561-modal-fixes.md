# Diet-trial start-modal polish + decline-note wording — B-565, B-561

**Date:** 2026-08-01 · **Shipped via #543** · **Branch:** `claude/b565-b561-modal-fixes-vse46v`

Two `pm-feature-review` / `adversarial-reviewer` findings against the diet-trial surfaces, both filed 2026-07-28 on B-533 PR A. Copy/format/layout only — no schema, no clinical logic, no new write path.

## What shipped

**B-565 — five start-modal polish items** (`components/profile/StartTrialModal.tsx`, `lib/dietTrialSetup.ts`):

1. **One date format on the sheet.** The start-date button rendered `toLocaleDateString` ("July 28, 2026") while the derived end date two fields away used `formatTrialEndDate` ("22 September"). The button and its accessibility label now read "28 July" via the same (already-imported) helper. A `startDateDisplay = formatTrialEndDate(startDayKey) ?? startDayKey` local drives both; `startDayKey` always parses back, so the fallback is unreachable and only satisfies the `string | null` return.
2. **`durationHelperLine` moved below the start-date field it describes.** It names the end date the *start date* determines ("From 28 July, that ends 22 September"), so under the indication chips a back-date changed a sentence scrolled off the top. Now below the start-date field; a "moved from here" breadcrumb is left at the old site (the file's existing convention for the same-shape start-date move).
3. **`START_DATE_LABEL` → "First day".** Six wide-tracked micro-caps beside two-word siblings was a parse, not a scan. `startDateHelper` beneath the field already does the teaching.
4. **`startSheetIntro` reworded.** It asserted *"{pet}'s vet has put {pet} on an elimination diet"* (may be false) and named the pet three times in two sentences. It now leads with the instruction and closes on state 0's (`noTrialCard`) payoff line kept verbatim — names the pet once, presumes only the recheck the whole feature is built around, not a specific past directive.
5. **Stale comment fixed.** `durationHelperLine`'s comment said the start date is "editable behind More options" — stale since R3 promoted it to the primary screen.

**B-561 — decline-note tracks its corrected sibling** (`lib/dietTrialCompletion.ts`):
`pushDeclineLines` *itself* was already corrected in #498 ("isn't showing the trial numbers" → "isn't reading these days as a clean run") once the terminal decline branches began rendering the off-diet floor — so the row was stale about **where** the wording survived. The remaining instance was its verbatim-in-register sibling, the completion sheet's `declineNote`, whose own comment requires it to track `pushDeclineLines`. Brought in line to the same "clean run" wording. The completion sheet suppresses the counts when the decline note shows (`TrialCompletionSheet.tsx` renders `declineNote` *instead of* the numbers), so the note was not literally false there as it was on the card — but the corrected clause is true either way and restores the register match.

## Definition of Done

- **Types + lint:** `tsc --noEmit` clean.
- **Automated tests:** full jest green via the pre-push hook — 174 suites / 3903 tests. The four diet-trial suites (`dietTrialSetup`, `dietTrialCompletion`, `dietTrialCard`, `dietTrial`, 729 cases) pass with **no assertion edits** — `durationHelperLine`'s output is unchanged (only its comments and call-site moved), and no test pinned `startSheetIntro`, `START_DATE_LABEL`, or the completion decline string. No new test — no new logic (copy/layout/comment on pure functions and one component).
- **Anti-patterns:** theme-tokens-only (no styles/literals added — reused `styles.help`/`styles.fieldBtnText`); no migration; no RLS/sync change; `nyx-voice` applied to `startSheetIntro` (no exclamation, pet named once, plain language, warm; the B-561 safety escalation sentence preserved verbatim).
- **Persona sign-off:** Designer ✓ (Principle 5 empty-state continuity via the shared payoff line; scannable label; no false assertion; date-format consistency) — Engineer ✓ (unreachable fallback typed cleanly; single date-format source; no logic change) — Data / Dr. Chen N/A (no clinical/statistical logic touched; the decline escalation sentence is unchanged).
- **Adversarial review:** N/A — no clinically- or statistically-load-bearing logic changed. The B-561 edit preserves the escalation ("a cat that stops eating needs a call today…") verbatim and only corrects the withholding-disclosure clause to a safe superset claim.

## Decisions made

- **B-561's real fix site is the completion sibling, not `pushDeclineLines`.** The task/backlog named `pushDeclineLines`, but #498 had already corrected it; leaving `dietTrialCompletion.ts:600` on the old wording would have left the "the numbers" phrasing shipping and violated the string's own "verbatim in register with `pushDeclineLines`" comment. Fixed the surviving copy and left a drift-guard note so a future editor doesn't revert it.
- **`startSheetIntro` (B-565 #4) was in scope even though not explicitly named in the kickoff.** "Close B-565" means all five items; #4 is code-authored (not in the spec), pm-feature-review authorized it, and state 0's conditional phrasing is the given model. Called out here + in the PR as an owner-facing copy change.

## Residuals / PM follow-up

- **Spec §4.1 sync (Tier-2, PM).** §4.1 line 301 locks `START_DATE_LABEL` verbatim as "First day on the trial diet only"; the code now says "First day". The user directed the code change (B-565 #3); the spec needs a matching edit to stay in sync per the LOCKED-copy convention. Flagged, not written unilaterally.
- **On-device QA** (the PR's Manual test steps) is the remaining human gate — the date-format read, the back-date-updates-in-view check, and the reworded intro.
