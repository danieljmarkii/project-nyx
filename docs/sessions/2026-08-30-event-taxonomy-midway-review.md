# 2026-08-30 — Event Taxonomy Expansion: midway product-team check-in (CUL-509)

**Mode:** DISCOVERY (PM-requested). *"We have a beta of new events running, they're working really well… I'm a little stuck… convene the product team and personas, review the current status and the go-forward plan in Linear, make any changes necessary, debate the open questions, and create a go-forward."*
**Outcome:** review posted as the project's first **status update** (health: on track) + per-issue recommendation comments (CUL-684, CUL-685, CUL-686, CUL-689, CUL-663) + a board-fidelity pass (project run order rewritten, D24 encoded as a W2a/W2b issue split, CUL-738 closed, milestones renamed); this record shipped via #773.

## Method — and why it differs from 2026-08-27

The 2026-08-27 review ran **nine isolated lenses** doing falsification against spec v1.3. That work stands, and five adversarial passes since have kept attacking §9a; nothing here re-litigates any of it. Today's ask is a different job: a **sequencing and status judgment with the live decision in view** — which per `docs/personas.md` is persona work, in-context, not another isolated falsification round. No new falsification is claimed by this session; where a lens leans on an attack, it cites one already recorded (§9b, the hard review, the 8-27 review).

## What is true today (the state the review ran against)

- **W1 (cough + sneeze) is live in beta and complete.** All five PRs shipped (CUL-673–677); the enum, capture tiles, client mirrors and engine handshake are in the installed TestFlight build; `generate-signal` is deployed at v33 with the Dr. Chen-ruled cough floors (CUL-687); the §11 swap ran 2026-08-29 — 22 cough + 11 sneeze rows re-keyed from `other` (33 of 34; 1 held by ruling), and ⑦ fires **`firm`** on the live ~9-week cough course. The PM's on-device read ("working really, really well") matches the recorded state.
- **W1's GA is gated only by PM-personal actions** (D12): the CUL-663 on-device QA pass → `log_picker_v2` GA call → `event_types_v2` GA call. The host sheet took ~10 hardening fixes over 28–30 Aug (CUL-678–682, 688, 704, 726, 728…), so it is in the best shape it has ever been — and those fixes are exactly what CUL-729's 11-check device batch verifies.
- **W2 is split (D24) and both halves are gated.** §9a has now failed **five** adversarial passes; today alone the PM ruled nine briefs across two write-attack cycles (v1.4 A/A/A/A → FAIL 22; v1.5 D20–D24 → FAIL 18). §9b(i) holds 18 findings (five silent-on-the-sick) gating W2a; §9b(ii) holds the rule-11 set gating W2b. **Five new briefs sit unruled on CUL-684** (F-C, F-F, F-H, F-J, F14+F17).
- **W3 and W4 are not gated by any of it.** No safety leaf, no §9a dependency; D13/D14 are already ruled; design coverage exists from mock rounds 1–3. They wait only on their own D5 greenlights.
- **The plan surface was stale.** The project description's run order still described W1 as un-started and CUL-684 as "spec v1.4 / six defects / three briefs"; M2.5's milestone name said "Spec v1.4"; CUL-666's title still read "NOT greenlit"; CUL-667 still described the un-split trio with a deleted calibration item (the D17-removed de-dup window). Fixed this session (see Board changes).

**The diagnosis of "stuck," in one sentence:** the ratified order points the project's nose at W2, W2 cannot accept a build until §9b clears, and every currently-actionable item is PM-personal — so build sessions idle while the plan reads as blocked. The unlock is to separate *"what the paper track grinds on"* (W2a, unchanged) from *"what builds next"* (W1's GA, then W3 if greenlit).

## Verdict

**ON TRACK — and the W2 gate is the process working, not the project failing.** Every one of the five FAILs found pre-build silence-on-the-sick defects that no device pass could ever surface (nobody can dogfood an obstructed cat). Unanimous across lenses: do not weaken the gate. What changes is the *shape* of the next stretch: GA what is done, keep W2a as the standing paper track, and put the next build wave to the PM as an explicit choice.

## The lens reads

**Product Owner / Backlog Steward** — Issue-level statuses were honest (the merge→Linear discipline held all week); the **project description** was three states behind reality and has been rewritten. CUL-738 was done in fact (edits merged in #767, recorded as approved in spec §17) and is now closed with its resolving PR. D24 was ruled but not encoded on the board: CUL-667 is retitled to W2a and **CUL-751 (W2b)** now exists, blocked by CUL-684, related to CUL-672 (Q4). Eight `Waiting on PM` items existed with no ordering; the go-forward plan below is that ordering.

**Dir. of Engineering** — W1 shipped under the full discipline (behaviour-neutral per-lane membership first, the symptom-list and membership-walk guards holding, deploy ledger reconciled, client-first release ordering respected). Standing constraint to carry: any future lane-membership change re-crosses the held `generate-report` (CUL-19) per §10.5 — budget the re-ack, every wave. On F-C: endorses option A (client-injected findings into the Signal zone with local cache semantics) as the only shape consistent with HR-27's own reconciliation — the deterministic-client-rule class exists *because* the answer must exist offline at capture, and a network-bound backstop contradicts that premise. It is a real PR in W2a's chain: a client-local overlay store (never a write to `ai_signals`), zone composition order defined, wipe-path membership per the CLAUDE.md rule. Also re-endorses (with the adversarial lens, from 8-27, now sharpened by two more FAILs) moving §9a from prose toward an **executable reference module + property tests** for v1.6 — with the F-I caveat that every new guard is proven by mutation (CUL-613/CUL-621), since v1.5 shipped a property test that could not fail.

**Dr. Chen** — W1 is clinically real: a nine-week cough course the record carried as note text is now machine-readable and correctly flagged `firm`. On W2a: five FAILs are not a reason to abandon — they are the falsification standard doing its job on the highest-stakes rules in the app; every pass has caught defects that would have shipped silence over a sick animal. Supports ruling the five briefs (recommendations endorsed below) and the seam-at-a-time v1.6. **Does not support W3 displacing W2a's paper track** — his original D1 dissent was about exactly this drift — but accepts W3 taking the *build* slot on one concrete condition: **the five CUL-684 rulings land before or with the W3 greenlight**, so the paper track is never parked on an un-ruled queue while a build wave runs. On CUL-686: the acute-dense miss (asthma crisis, early CHF) is clinically better covered by W2a's `labored_breathing` presence leaf than by a cough-density proxy — recommend (a) accept-and-document now and fold the burden-arm question into W2a's greenlight, revisiting only if W2a stays blocked long.

**Sr. Data Scientist** — The swap held the record's integrity (33 re-keyed, notes/timestamps untouched, total invariant enforced in-transaction; the entries-vs-episodes unit discipline held: 22 cough entries → 20 chained episodes in the sliding window). n=1 stands (G6): none of this calibrates floors. On F-C: option A is also the data-minimal answer — under A, `generate-signal` never needs the D14 chip columns at all (the report half rides CUL-19 regardless, per §10.3c), and the strain predicate keeps exactly **one** implementation, client-side — the §5.3/§9a-rule-2 one-predicate rule holding by construction. On CUL-689: records the lean toward span-first (option a) — ⑦'s claim *is* duration, and every added ordering knob is a new calibration to defend — while endorsing (d) unconditionally.

**Sr. Product Designer** — The stuck feeling has a UX reading: every actionable item is on the PM's phone or in the PM's rulings queue, and the plan surface didn't say so. The go-forward is written as lanes with owners for that reason. The GA path is friction-optimized: CUL-663's 12 steps and CUL-729's 11 checks are **one phone sitting** (~30 min, same surface) — merged in the plan and noted on CUL-663. No design change is proposed by this review, so no mock round republishes (mock-what-you-change not triggered). One register note: the PM ruled nine safety briefs today across two failed passes — the five new briefs do **not** need to be ruled today; nothing in Lane 1 or Lane 3 depends on them.

**Jordan (dog owner)** — W1 in the sheet passes the 10-second test (two tiles, confirm unchanged, witnessed-by-construction so no chip pair to think about). GA it: a cough tile behind a beta toggle helps only the household that already has it. On F-J: the brachycephalic-pug case is exactly the owner who deletes the app — three emergency bands a day over normal panting is the nag Principle 4 exists to prevent; endorse damping the *interruption*, never the finding.

**Sam (two-cat owner)** — The W2a multi-cat rules are *the* reason this wave matters to multi-cat homes; endorses F-H option A with the worked case: 2 attributed (Cat A) + 2 attributed (Cat B) + 2 unattributed, `N_U=3` → per-pet counts 2/2 stay below floor, household count 6 crosses → **household band** ("if you can't tell which, take both") — correct under either attribution, and fixture 10 becomes satisfiable. Flags F-R alongside it: the "not sure which cat" affordance must span both W2a leaves (an owner who hears wheezing in the dark must not be force-attributing either). Also backs W3's urinary base — `urine_outside_box` is the FIC/stress lens for cats, and the periuria-vs-spraying capture question (CUL-671's consensus finding, on CUL-509's trail) binds its design: the ambiguous case routes toward the medical read.

**Sr. QA** — GA-chain state is concrete: CUL-662 fixed and merged; CUL-663 not yet run; CUL-729's 11 checks pending; Android spot-check pending. Two script addenda applied to CUL-663 (see Board changes): step 9's flag-off check now has an `event_types_v2` dimension (flag-off grid = the 8-tile pin; beta grid adds Cough/Sneeze under Respiratory — verify both flag states), and the 8-27 follow-up's VoiceOver step on the switcher is added as step 13. On parallelism: W3 build + W2a *paper* do not collide (different artifact types, disjoint files); two build waves at once would double-load the membership lists and guards — builds stay serial, which one-PR-per-session enforces anyway.

**Trust & Safety / Privacy** — The swap ran inside its T&S shape (scoped account, PM-reviewed id list, run-log; two waived gates recorded with reasons — the waiver record *is* the discipline). F-C option A keeps W2a's health inference on-device and out of the LLM/network path entirely — mild preference for A; the injected-finding store joins `wipeLocalSession` with the rule-3 marker. Nothing else new. N/A otherwise.

## The one genuine conflict (Conflict Protocol) — resolved by decision brief, not silently

> **Dr. Chen:** W2 sits second in the ratified order because these leaves have bodies attached (obstruction is fatal in 24–48h). Letting W3 jump the build queue risks the safety trio sliding indefinitely — the exact drift my D1 dissent named.
> **Designer + Jordan + Sam:** W2a cannot accept a build for weeks regardless of anyone's urgency — the gate is Dr. Chen's own standard. Owner value sits ready in W3, and an idle build queue serves nobody, least of all the trio.
> **Dir. Eng / QA:** paper and build don't collide; builds stay serial.
> **PM decision needed:** D8 ratified the wave order "as proposed" (W2 after W1), so re-sequencing the *build* order is yours. The team's recommendation (7 of 8 lenses, Dr. Chen concurring **conditionally**): greenlight W3 as the next build wave, with the condition that the five CUL-684 rulings land before or with that greenlight so the W2a paper track keeps moving. The alternative — hold all builds until §9b(i) passes — is coherent but prices an unknown number of idle weeks on the five-pass history.

## Endorsements — the five CUL-684 briefs (posted to CUL-684)

All five recommendations endorsed as written, with three execution riders: **F-C/A** — the injected-finding path is a client-local overlay (never writes `ai_signals`), joins `wipeLocalSession`, defines zone-composition order, and makes fixture 8's second clause testable; under A the D14 chips stay out of `generate-signal` entirely. **F-F/A** — record in the ruling that post-D23, *attribution is a naming axis, never an evidence-quality guard*: nothing remains that could exercise T's rate-class tier permission, so the two-cat 12-trip case closes by construction, not by class assignment. **F-H/A** — Sam's worked example above; the affordance is per-family, not strain-only (F-R). **F-J/A** — with the guard that a tier rise is never damped and N is Dr. Chen build-gate calibration. **Brief 5 (F14/F17)** — defer to W2b's own activation (post-Q4); pre-registered shapes: F17 resolves as two registers (the sleeping series stays pure; awake-count presence findings reach the report as their own disclosed line — the C5 disclose-beside pattern), F14's reversal guard should fire on genuine decline toward baseline, never on a plateau at an elevated level.

## Rulings recommended (posted to their issues)

- **CUL-686** (cough mute 21 days): **(a) accept + document now; fold (b) the burden arm into W2a's greenlight.** The span floor's exclusion of the self-limiting course is load-bearing; the acute-dense presentation is `labored_breathing`'s job, and a cough-density arm is a proxy for it. Honest first-weeks coverage statement: History/day summary/Trend render and count the rows (rose-tinted); the Signal is quiet — the watching register is vomit-anchored (8-27 finding), so no more than that is claimed. Not (c) — leaning on the twice-gated thing; not (d) — reversing a days-old ruled membership under momentum.
- **CUL-685** (banner single-slot): **(b) disclose multiplicity** ("…and another ongoing sign") — the house pattern (C5: disclose beside, never rank what can't honestly be ranked; B-494: an advertising zone may not go quiet). Longest-span keeps picking which course is *named* (deterministic); never (c) — no invented cross-symptom severity scale.
- **CUL-689** (⑦ recency): **(d) unconditionally** — the cough↔vomit adjacency disclosure rides the most-recent course, independent of card order. On the ordering itself: Dr. Chen prefers **(c)** (recency leads only at a large gap; the bound is his calibration), Data Science leans **(a)** (span-first is what ⑦ claims; fewest knobs) — both reject (b). PM picks (a) vs (c); (d) either way.

## Board changes applied this session (grooming pass)

- **Project description**: run order rewritten to current state (W1 done/beta → Lane 1 GA / Lane 2 W2a paper / Lane 3 next-wave decision); summary updated.
- **CUL-738** → `Done` (edits merged in #767; approval recorded in spec §17).
- **CUL-666** retitled (stale "NOT greenlit" removed; shipped-to-beta date added).
- **CUL-667** retitled to **W2a** + description update block (D24 split; build authority = v1.5 gated on §9b(i); the D17-deleted de-dup-window calibration line superseded).
- **CUL-751** created — the W2b umbrella (blocked by CUL-684; related to CUL-672/CUL-667; milestone M3; Backlog).
- **Milestones**: M2.5 renamed (was "Spec v1.4…") to carry the five-briefs + seam-at-a-time v1.6 state; M3 renamed to note the D24 split.
- **CUL-663** description: CUL-729 merged into the same sitting; step 9 gains the `event_types_v2` flag-state dimension; step 13 (VoiceOver on the switcher) added per the 8-27 follow-up.

## The go-forward plan (also in the project status update)

**Lane 1 — W1 → GA (all PM-personal, ~30–40 min, zero rulings):** one phone sitting (CUL-663 + CUL-729) → Android spot-check → `log_picker_v2` GA call → `event_types_v2` GA call (each GA is a small flag-retirement session on the PM's word, the FR-FLAG-5/B-712 shape). Recommended before GA multiplies exposure — three cheap rulings: CUL-685 (b), CUL-689 (d + a/c pick), CUL-686 (a).
**Lane 2 — W2a paper (standing track):** rule the five CUL-684 briefs (endorsed above) → v1.6 seam-at-a-time (one seam per short session, re-attacked each, trending toward the executable-module shape) → W2a build greenlight only on a PASS. Send the vet sheet (CUL-672 — the phone-readable artifact is on the issue); Q4 slots W2b.
**Lane 3 — next build wave (PM decision):** the sequencing brief above. If W3 is greenlit, its chain is written at greenlight from the §13 checklist (D9) — enum PR (`urine_outside_box`, `urine_change`; `skin_reaction` is dormant-no-migration), capture + D14's first real chip columns (which makes W3 the wave that establishes the new-column sync sub-checklist from 8-27), engine membership deciding **every** lane incl. the must-not-fire set (HR-22), the §10.5 report decision + held-fingerprint re-ack, the fold check → Q7 tracked-events build decision, and the periuria-vs-spraying capture rule. Honest sizing: ~5–7 sessions on W1's precedent.

**Not done by this review, deliberately:** no §9b closures were written (the discipline stands — rulings first, then seam-at-a-time with re-attack); no wave was greenlit (D5 — every greenlight is the PM's); no floor, threshold or ranking was changed.

## DoD (discovery variant)

AC: N/A (no build step advanced — discovery). Tests: N/A — no code touched; docs + Linear only. Anti-pattern scan: no owner-facing copy written into the app; no engine change. Secrets: none. Persona sign-off: **PO ✓ (board fidelity) — Eng ✓ (F-C/A shape, §10.5 constraint) — Dr. Chen ✓ (conditional concurrence recorded, CUL-686/689 recs) — Data ✓ (one-predicate under F-C/A, CUL-689 dissent recorded) — Designer ✓ (lane plan, GA friction) — Jordan ✓ — Sam ✓ (F-H worked case) — QA ✓ (CUL-663 addenda) — T&S ✓ (swap posture verified, F-C/A preference)**. Adversarial review: N/A — no clinical/statistical logic changed; all cited attacks are the recorded ones (§9b, HR, 8-27). Future-self: the lane structure survives because it encodes ownership (PM-personal vs session-runnable), which is the axis the old run order lacked.

— Session 2026-08-30, branch `claude/event-taxonomy-expansion-d1uq7x`. In-context persona convening; per-issue slices posted to the Linear trail; the project's first status update carries the check-in.
