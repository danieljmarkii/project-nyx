# B-351 Phase A PR 2 — extraction emits the ordered `proteins` set; B-048 absorbed; deployed live v15

**Date:** 2026-07-24

`extract-food-from-photo` gains a `proteins` array in the tool schema (panel order, bare animal terms, own confidence field) + a prompt that names the hidden secondary protein as the thing that matters most and forbids guessing from the product name. `primary_protein` is now the DERIVED `proteins[0]`, computed once in `parseToolResult` so the pair can't drift — with the model's primary **hoisted to [0]**: §5 says panel-ordered, §6/D8 says the main is what the food is *sold as*, and they disagree exactly in the case B-351 exists for (a "duck" formula listing chicken first), where panel-order-only would make PR 4 call the trial protein the contaminant. Hoisting is also strictly additive — a hydrolyzed prescription diet with no animal protein on the panel still captures `hydrolyzed soy protein`. **B-048 lands as a WRITE-PATH-ONLY normalizer** in `lib/protein.ts` (`normalizeExtractedProtein`/`deriveProteinSet`: sourcing-descriptor strip · same-species tissue fold `chicken liver`→`chicken` · exact-match alias table `ocean whitefish`→`whitefish`); read-time `canonicalizeProtein` deliberately NOT widened (D3 = map at capture where the owner can correct it, never a retroactive re-merge of stored keys), `hydrolyzed` never stripped, `poultry` never guessed into a species, exact-match so `buffalo`→`bison` can't touch `water buffalo`. Set deduped on the canonical key, prominence-ordered, capped at 8 (Bonferroni-family guard); malformed model output degrades to protein-unknown rather than throwing (a throw would stamp `ai_extraction_status='failed'` on a good row). Every output key proven canonicalize-stable + idempotent in BOTH runtimes. **`adversarial-reviewer` at wrap: FAIL, 7/8 probes broke** — five fixed in-branch + **redeployed as v15** (bare-tissue exposure DROP; `Green Tripe`→`green` garbage key; cap-8 truncating rabbit+venison off a real raw grind; prototype-chain alias returning the `Object` function; vague→specific `white fish` alias) plus the prompt gap that let a model omit `primary_protein` and hand proteins[0] back to panel order. What HELD: 400k-case fuzz found zero write/read re-keys, hydrolyzed-vs-intact and water-buffalo-vs-buffalo stayed apart, no regex pathology. Routed as decisions: **B-412** (owner-typed "Other" bypasses the normalizer — blocking PR 3) · **B-413** (empty set ≠ "clean" — PR 4/5 gate on confidence) · **B-414** (pre-existing canonicalize non-idempotence, 0/56 live rows) · **B-411**. No schema (039 already live). tsc + jest 1628 (107 suites) + 51 deno; **deployed `extract-food-from-photo` v15**, read-back verified + boot/validator smoke-tested. — shipped via #429
