# 2026-08-27 — Event-taxonomy mock round 3, the coverage round (CUL-665)

**Outcome:** the §14 round-3 frame list drawn — seven of ten frames — and republished over round 2.1
at the track's one artifact URL (`https://claude.ai/code/artifact/07e289b5-6b06-4627-b5fb-f96288477848`);
committed HTML (`docs/culprit-event-taxonomy-mockups.html`) remains source of truth. Design session
only — no build wave committed (D5 stands), no spec edit (v1.3 already carries every ruling this round
renders).

## The split, per the CUL-665 thread

The v1.3 session's comment on CUL-665 flagged that §9b (the adversarial pass on §9a's own rewrites,
verdict FAIL, six silence-on-the-sick defects open, closure = CUL-684 at M2.5) sits exactly on three of
the ten frames. Its suggestion — draw the unaffected set now, hold the affected three — was applied:

- **Drawn (frames 1, 2, 5, 6, 7, 9, and 8's family half):** the W1-only grid (the round's headline —
  ten tiles, seven groups, regroup applied, the Itch tile alone under its permanent Skin & coat header
  for two waves); the labored-breathing post-save band (cat, open-mouth, "at rest" anchor per HR-21,
  rule-1 labelled Close with scrim/back inert, rule-13 beat-never-mounts); the read surfaces (History
  day + the report problem line with the HR-7 "38 entries · 14 episodes" precise-units ruling); the
  three change-leaf confirms (noticed-time + optional since/what chips, stored per D14); the Pee-changes
  attribute detail (marked-chips-only render — absence is never "No"); the empty-state pass (Cough +
  the safety-leaf adversarial case); the Q7 reveal as ruled (per-family "· 2 more", fully-hidden
  families keep their header, post-tap tiles land in permanent slots).
- **Held (frames 3, 4, 10 + 8's RRR half), visible as §00 held-cards, never silently absent:** the
  rule-5a ask-band + rule-8 copy (findings 1/4/13, incl. the pass's #1 window-reset finding), the RRR
  sustained state + "Count again" (finding 2), the RRR rising-series surface (finding 3 + the HR-19
  never-ships-without-its-rule binding). Rationale on the page: a mock is design authority, and these
  would be authoritative for rules about to change. They land as **round 3b** at the same URL once
  CUL-684 rules. Nothing W1 depends on is held.

## Batch rulings rendered (mock-what-you-change)

"Digestion" in every grid (never "Tummy"); Lethargy under Energy & behavior; P6 labels stand;
**Itch/Scratch is one tile at every density** (D13 — round 2's "Scratching" relabel retired; §15 Q8
checks wording with the real vets); stool split full-width everywhere (P1); tracked events redrawn to
R6-as-adopted — Profile-homed (segmented pet control removed), safety rows present and **locked
"Always on"** (P2), set-note names the real cost, frequent row marked permanently parked; R2 scale
verdict recorded as confirmed; §07's gate ledger updated (CUL-662 fixed via #724 → CUL-663 is the live
gate).

## Teed to the PM (§10 of the page)

- **1·W1 confirmation** — the headline ask; the shape W1-PR-2 builds. Explicit yes requested, not D8
  silence.
- **T1** — the band's call path ("Call a vet now" with no dial affordance; drawn conservative, option
  teed).
- **T2** — reveal stickiness (per-open drawn; sticky-after-use teed).
- **T3** — confirm the hold plan (round 3b after CUL-684) or ask for provisional draws instead.
- Everything else reacts under D8 silence; new strings listed in the labels ledger.

## Process notes

- The session was launched attached to the wrong repo (`competitive-inteligence`); `project-nyx` was
  attached mid-session and all work landed there on the designated branch name.
- Personas carried in-page: Sam's question opens the change-leaf section; Dr. Chen's HR-21 anchor and
  the precise-units basis are cited at their frames; clinical-guardrails constraints stated on both
  empty states (the safety-leaf empty state never reassures and never claims detection that hasn't
  shipped).

## Next

- PM reads round 3: confirms 1·W1, rules T1–T3, vetoes any label.
- CUL-684 (the §9b rulings) unblocks round 3b — the held frames — and remains the W2 gate either way.
- CUL-663 (pre-GA QA pass) is the host chain's live step; W1 greenlight (Q2) stays its own ask.
