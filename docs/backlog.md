# Project Nyx — Backlog

A running list of items intentionally deferred from the current build sequence. This is the destination for anything that would otherwise be said as "let's log that for the future."

**Access:** PM can type `view backlog`, `show backlog`, or any natural-language equivalent in a session — Claude reads this file and presents it grouped by priority, surfacing anything that blocks the current build phase first.

**Adding items:** Claude adds rows here proactively — do not wait for PM. When raising a "for the future" item mid-session, write it here before continuing. Include a one-line _why_ so future-you can re-evaluate without re-deriving the context.

**IDs:** sequential `B-NNN`. Never reuse. When closing an item, leave the row and mark `Status: Done — <session date>` rather than deleting.

**Priority:**
- **Now** — should be tackled in the current or next session; here only because it's outside the in-flight chunk
- **Next** — tackle within the next 2–3 sessions or the next build phase
- **Later** — real but not time-sensitive; revisit when conditions change (e.g. pre-prod, multi-pet, paid tier)

---

## Open

| ID | Title | Why | Priority | Added | Blocks | Status |
|---|---|---|---|---|---|---|
| B-001 | AI cost & rate-limit strategy | Step 10 (AI Signal) and the food-extraction Edge Function both call Claude. Before real users hit the app, need a per-user/day call cap, caching strategy, and back-of-envelope cost-per-active-user. PM decision May 2026: skip while pre-shipping; revisit before first real-user release. | Later | 2026-05-17 | First real-user release | Open |
| B-002 | Pre-production readiness checklist | Once Step 10 closes, MVP is done — but there's no checklist for first prod build (EAS env populated, app store assets, Sentry/observability, error reporting, privacy policy URL, push notification provider decision). Flesh this out as a `/docs/pre-prod-checklist.md` when prod timing comes into view. | Later | 2026-05-17 | First prod build | Open |
| B-003 | Detail-screen pattern for History events | The food detail screen (Step 6) was praised as a UX motion worth reusing — long-press a row in History → an Event Detail screen with the event's photo, severity, notes, and edit affordances inline. Distinct from the existing `edit-event.tsx` modal, which is form-first. Touches Timeline + edit-event; non-trivial. Punted from the food-library PR. | Next | 2026-05-17 | — | Done — 2026-05-20 (PR #32, shipped together with B-012) |
| B-004 | Food Library as a top-level navigation item | Currently the only way to reach the library is FAB → Meal → picker. The library is gaining prominence and may deserve to be a first-class destination (tab bar entry or drawer). Defer until the rest of the food-library track lands and we see how often owners want to browse without logging. | Later | 2026-05-17 | After food-library track complete | Open |
| B-005 | Smarter library deletes (tombstone instead of cascade) | The Step 6 implementation hard-deletes the food and soft-deletes every meal that ever referenced it ("kills all records" — PM call for the test version). Later we may want to preserve meal history with a tombstoned food reference so correlation/diet-trial data isn't lost when a user re-tidies the library. | Later | 2026-05-17 | Second-user beta | Open |
| B-006 | EXIF extraction on edit-modal photo attach | Today, attaching a photo from the Edit modal does not pull DateTimeOriginal back into `occurred_at` — the event keeps its original time and source. PM flagged this in Step 7 QA as "probably feels right" (a user editing an existing event has already accepted the time); logging as intentional behavior in case we revisit. If we do change it, the edit modal would need the same source-flip dance as `log.tsx`. | Later | 2026-05-17 | — | Open |
| B-007 | FAB experience revamp | PM feedback during Step 7 QA: "the whole FAB experience needs a revamp." Current FAB stacks recent meals, quick symptom chips, photo-log, and "More events" into a tall pop-up that drifts from the three-zone picker pattern we've adopted elsewhere. No specifics yet — needs a fresh design pass before scoping. | Next | 2026-05-17 | — | Open |
| B-008 | Extract `useEditableTimestamp` hook | The flip-on-edit logic for `occurred_at_source` is now duplicated in `app/log.tsx`, `app/food-capture.tsx`, and `app/edit-event.tsx` (three near-identical onChange handlers + seed flow). Pull into a `lib/hooks/useEditableTimestamp.ts` once the behavior settles — premature now, but tag the duplication. | Later | 2026-05-17 | Step 7 follow-up | Open |
| B-009 | UPC collision: merge-to-existing instead of null-drop | `food_items` is globally scoped with a unique constraint on `upc_barcode`. When Claude extracts a UPC that already exists, the Edge Function currently catches the 23505 violation and retries with `upc_barcode: null` — extraction completes but a duplicate food (sans barcode) lands in the library. Proper fix: on collision, look up the existing row, soft-delete or repoint the pending row, and have the client redirect the user to the canonical food. Cleaner library, honors the confirmation-over-entry principle. Bumped to Next once the food-library track stabilizes — duplicates compound quickly once real users scan the same products. | Next | 2026-05-17 | Food-library track close-out | Open |
| B-011 | Food library grouping (treats vs meals, by brand) | Library is currently a flat alphabetical list mixing treats and meals. Owner mental model is "I want to log a treat" or "I want to log Mochi's kibble" — flat list forces a name-scan every time and violates confirmation-over-entry as the library grows. Primary cut: treat vs meal (needs a `food_type` field on `food_items` — either schema migration + Claude classification backfill, or extraction-time tagging going forward). Secondary cut: brand, likely as a filter/sort rather than nested hierarchy. Treat-vs-meal also unblocks correctness downstream — a treat shouldn't count toward diet-trial compliance. Composes with B-004 (library as top-level nav) and B-009 (UPC collision cleanup) — sensible to bundle into the post-Step-7 food-library close-out. **PM decision needed before scoping:** add an explicit `food_type` column (migration + Claude classification backfill) vs infer at render time from ingredient profile. Team recommends the explicit column — also unblocks downstream correctness (treats excluded from diet-trial compliance). | Next | 2026-05-20 | Food-library track close-out | Open |
| B-012 | Larger photos on event detail screens (hero layout) | Detail screens (vomit, meal, stool, edit-event) render attached photos as small thumbnails today. The clinical use case — Jordan in-clinic showing Dr. Chen "here's what it looked like" — wants the photo presented as the primary artifact, not a thumbnail-plus-tap-to-zoom. Dr. Chen's clinical assessment of stool/vomit/lesion appearance also degrades at thumbnail size. Likely a hero-image layout with metadata below; cheap to implement (layout only, signed URL already available). Composes naturally with B-003 (detail-screen pattern for History events) — same surface, same PR if scheduled together. | Next | 2026-05-20 | — | Done — 2026-05-20 (PR #32, bundled with B-003 — `app/event/[id].tsx` 320pt hero photo) |
| B-013 | Per-incident AI analysis — shared infra + per-type rollout (PARENT) | Owner-facing feedback loop after logging an incident (vomit, stool, lethargy, …): the detail screen helps the owner (a) **describe/characterize** the incident and (b) get an **n=1 interpretive read** ("based on this single instance, should I worry?"). Closes the "data goes into a black box" gap that costs week-one retention (Jordan + Designer) while staying clinically honest (Dr. Chen + Data Scientist). **Architecture (decided 2026-05-24):** ONE feature — one Edge Function, one detail-screen component, one guardrail/disclaimer layer — parameterized by `incidentType`, with **per-type prompt templates** and **per-type phased rollout** (ship + clinically validate one type, then light up the next on the same machinery). Do NOT fork the code per type. Backlog shape = this parent (pattern + plumbing + cross-cutting decisions) + one child per incident type (each carries its own prompt + Dr. Chen clinical review + red-flag list). First child: **B-027 Vomit.** Later types (loose stool, lethargy, …) get their own child IDs when scoped — don't pre-allocate. **Scope = two AI jobs, both shipped together per type (Q2):** they fall out of ONE vision call as one JSON payload (description + structured fields + n=1 read), so "together" is *cheaper* than splitting, not a compromise — but v1 carries the clinical guardrail bar from day one (no shipping the easy descriptive half and deferring the hard half). **(1) Descriptive/characterize:** a readable free-text description AND structured fields (per-type — for vomit: colour / content / consistency / blood / bile / foreign-material), both emitted by the same call — NO second "extract structure from prose" pass (avoids a 2nd call, latency, prose↔fields drift). **(2) Interpretive n=1 read:** "based on this single instance, should you worry." **Dr. Chen's non-negotiable asymmetry:** the n=1 read may **escalate on the PRESENCE of a visible red flag** (blood, suspected foreign material, repeated vomiting in a short window) → "worth a call to your vet," never naming a disease; it must **NEVER reassure on the ABSENCE of one** ("nothing visibly alarming in this one" is honest; "Mochi's probably fine" is not — the clear-foam-once-but-not-eaten-36h cat looks benign in the photo and is the feline 48hr hepatic-lipidosis case). Reassurance, if ever, comes only from the cross-incident layer, carefully. **Cross-incident interpretive read = LATER** ("knowing everything about this pet, is the trend something to worry about") — overlaps/shares **Step 10's AI Signal** surface + guardrail plumbing; build once, don't duplicate. **Surfaces (Q4):** per-incident description + n=1 read live on the **incident detail screen** (`app/event/[id].tsx`); **Home is reserved for the cross-incident synthesis** (Principle 3 — Home is Signal/Today/Trend, not a feed; a raw per-incident description on Home would make it a log feed). **Human-override (PM requirement):** description + structured fields are **freely editable**; the n=1 read is **dismissible, NOT editable** (dismiss = soft-hide + reversible per the soft-delete rule, never destroy; editing a clinical read would let owner-authored pseudo-clinical text wear the AI's voice — the editable-facts + dismiss combo is the escape valve when the AI is wrong). **Provenance:** store per-field, render ONE calm "Edited [date]" marker (Designer — visible calm, not per-field confetti); the timestamp answers Dr. Chen's "altered after the fact?" concern; an edited field is the **preferred/more-trusted value on the vet report** (human-reviewed > raw AI). Original AI value retained for free via the cached raw AI payload (same pattern as food's `ai_extraction_confidence`) — "edited" = differs from cached original, no extra history columns. **Re-analysis must NEVER clobber a human-edited field** (mirror the food retry guardrail). **Vet-report scoping (confirmed 2026-05-24):** the n=1 worry read is **owner-facing ONLY, not a vet-report element** — the report is fed by the structured fields + frequency (keeps it clinical-grade, Principle 6; sidesteps "dismissed escalation on the report"). **Cost:** ~2¢/incident (Sonnet 4.6, one photo, one cached call, never re-run on detail-screen open) — same ballpark as the food scanner; cross-incident layer cheaper (no image, schedule/cache cadence). **B-001 rate-limiting does NOT gate the dogfood build** (single user); it gates before real users. **Edge/empty states (QA):** cached-on-log + async pending state; logged-with-no-photo; photo-not-actually-the-type; insight stability across re-opens (caching); a designed "not enough to say yet" uncertain state (Principle 5 — never fabricate to fill space); dismiss control needs 44pt/`hitSlop` (3am test). **Sequencing:** PM decision 2026-05-24 — **consciously queue-jumping ahead of Step 9** (dogfooding: real incidents exist now; the vet report serves a visit not yet booked). Consequence: Step 9 slips, AND the vomit child becomes the FIRST place the non-diagnostic guardrail pattern is set — which Step 10 then inherits (Dr. Chen prefers setting the clinical bar on a concrete case). Persona sentiment: ✓✓ Jordan, ✓✓ Dr. Chen (gated on the asymmetry), ✓ Data Scientist, ✓ Designer (uniform surface + uncertain state), ✓ Engineer (shared infra, per-type rollout). | Now | 2026-05-20 | Step 9 (queue-jumped); cross-incident layer ties to Step 10 AI Signal | Open — approach decided 2026-05-24; first build is B-027 (Vomit) |
| B-014 | Meal intake / pet sentiment capture (WSAVA 5-point ordinal) | Today the meal log conflates *offered* with *consumed*. PM-surfaced use case: cats are notoriously picky, and diet-trial owners (Jordan with Mochi) need to distinguish "she ate it" from "she licked it and walked away" — that's the difference between a successful elimination week and starting over. Capturing owner-reported intake unlocks (a) correlation-engine correctness (correlate *consumed* meals with symptoms, not *offered*), (b) diet-trial compliance calculation that today is not possible, (c) AI Signal lines like "Mochi has eaten 100% of meals on the hydrolyzed protein vs 60% on the kibble", and (d) the cat-owner wedge (hepatic lipidosis 48hr threshold hinges on actual intake — clinically load-bearing). **Scale:** WSAVA Diet History Form 5-point ordinal — `refused / picked / some / most / all` — per Dr. Chen. Validated clinical instrument; do not invent a custom emoji scale. **Schema (v1):** one additive migration adding `meals.intake_rating` (nullable enum/text). Legacy and un-rated rows preserved as `null`. Own PR per CLAUDE.md migration-isolation rule; includes Migration Safety Pre-flight (additive, non-destructive, no backfill required). **Capture surfaces locked for v1 (three, all using the same shared chip-row component):** (1) **Inline on `app/log.tsx`** — optional chip row below the existing save flow. Three Designer conditions are non-negotiable: must be skippable (save works with `intake_rating = null`), must default to null (never pre-select "all"), and must be visually subordinate to the save target with a label that signals "later is fine" (e.g. "Already finished? (optional)"). QA gate: the 10-second test from Step 4 must still pass with intake left unrated — verified on every PR build. (2) **`app/event/[id].tsx` (meal detail screen)** — chip row in both view and edit states, fully editable. The detail screen carrier already ships (PR #32). (3) **`app/(tabs)/history.tsx`** — read-only badge on each meal row once rated; tap the row to navigate to detail and edit. **Gating:** the inline chip (post-log toast) renders for `food_items.food_type` of `'meal'` and `'treat'` — i.e. B-011 is a hard prerequisite. **Treats opted in 2026-05-23** (PM decision, reversing the original "no intake prompts on treats"): treat refusal is itself a clinical signal — a pet declining a treat often precedes declining meals (Dr. Chen). The default-null rule is what makes this safe: we never pre-stamp `'all'` on treats, which the team flagged would bias the intake dataset. `'other'` and unclassified foods stay opted out. The detail-screen and history surfaces are not gated (they render the rating if it exists). **Principle 1 resolution:** the inline surface is permitted because it's an *optional, default-null, visually-subordinate affordance* — not a required decision. The Designer's three conditions enforce this; ship gates on them. **Sequencing:** PM plan is B-011 in a new session, then return here for B-014. **Out of scope for v1 (logged separately):** delayed local notification capture (B-015), diet-trial compliance calculations, AI Signal lines using intake, vet-report rendering of intake. Squad sentiment: ✓✓ Dr. Chen, ✓✓ Data Scientist, ✓ Jordan, ✓ Designer (gated on three conditions), ✓ Engineer (small additive scope — detail-screen carrier already in PR #32). PM endorsed May 2026; inline surface added to scope 2026-05-20. | Next | 2026-05-20 | B-011 food_type distinction (hard prerequisite for inline gating) | **Done — 2026-05-23.** All four v1 capture surfaces shipped: schema (#38), the three v1 surfaces — log toast + detail screen + History badge (#39), treat opt-in + "Treat" label (#40), edit-event form (#41). Out-of-v1 items remain tracked separately: B-015 (delayed notification), and downstream consumers — diet-trial compliance, AI Signal intake lines (Step 10), vet-report intake rendering (Step 9). |
| B-015 | Delayed local notification for intake capture ("did Mochi finish?") | Originally option (b) in B-014's capture-mechanism design space. Cut from B-014 v1 scope because it depends on the still-open push-notification provider question (CLAUDE.md Open Questions). Highest-fidelity capture surface per Jordan ("I'd tap that"), but introduces infra not yet decided. Mechanism: when a meal is logged, schedule a local notification 30–60 minutes out asking "Did Mochi finish?" with the 5-point chips available from the lock screen / notification action. Re-evaluate once the push-notification provider is selected (probably during pre-prod readiness, B-002). Composes with B-014 v1 — same data model, additional capture surface. | Later | 2026-05-20 | Push-notification provider decision (CLAUDE.md Open Questions); after B-014 v1 ships | Open |
| B-016 | App-wide error observability (Sentry-style) | PM-surfaced during B-011 PR2 QA 2026-05-21: hit a silent "Food not found" failure in the food detail screen and asked whether we'd see this in any log. We don't — errors today only land in `console.warn`/`console.error` which the PM can't see from the device. Need a real reporting pipeline (Sentry, Bugsnag, Highlight, etc.) wired into both the React Native app and the Edge Functions so production failures (RLS denials, sync conflicts, extraction errors, orphan-cache loads) reach the PM without a screen-share. Tie into B-002 pre-prod readiness — likely the same PR that adds privacy policy + push-notification provider. Decision needed: which provider; SDK + DSN provisioning; PII redaction rules; sample rate. | Later | 2026-05-21 | First real-user release; B-002 pre-prod readiness | Open |
| B-017 | `food_format` enum overlaps `food_type` ('treat' lives in both) | PM-surfaced during B-011 PR2 QA 2026-05-21: with B-011 shipping a `food_type` column (`meal`/`treat`/`other`), the existing `food_format` ENUM (which includes `'treat'`) overlaps awkwardly. A user adding a food now sees "treat" in both the Format chip row (physical form) and the Type chip row (usage role). Right fix: drop `treat` from `food_format` (rename remaining values to be purely physical: `dry_kibble`/`wet_canned`/`raw`/`freeze_dried`/`fresh_cooked`/`topper`/`other`) — and consider reordering by frequency (`dry_kibble` first since it's the most common case, currently first by accident). Destructive migration — needs a backfill that maps existing `format='treat'` rows to a sensible new value (likely `dry_kibble` or `other`) and concurrently sets `food_type='treat'` if not already set. Own PR per migration-isolation rule; Migration Safety Pre-flight required. Defer until food-library track close-out is otherwise stable — touching this churns the picker code. | Next | 2026-05-21 | Food-library track close-out | Open |
| B-018 | Same-brand+product duplicate foods (no UPC) | B-009 covers the UPC-collision case where Claude extracts a duplicate barcode. But same-brand+product duplicates can also land via manual entry, retried captures, or sync conflicts — `food_items` has no uniqueness constraint on `(brand, product_name)`. The picker masks this today via `GROUP BY LOWER(brand), LOWER(product_name)` in `getLibraryFoods` (lib/db.ts), but the underlying rows persist and the GROUP BY picks an arbitrary winner — including arbitrary `food_type` when one dup has it set and the other doesn't. Proper fix: dedup on insert (look up existing by normalized brand+product before INSERT, fall through to existing row id), plus a one-shot merge script that consolidates current duplicates and updates `meals.food_item_id` to point at the canonical row. Composes with B-009; share the canonicalization helper. | Next | 2026-05-21 | Food-library track close-out | Open |
| B-019 | FAB recent-meals list goes stale (out of sync with picker Recent) | PM-surfaced during B-011 PR2 QA 2026-05-21: the FAB's "Recent meals" quick-log surface and the meal-log picker's "Recent" zone draw from different SQL queries (`components/log/FAB.tsx` has its own `getAllSync` JOIN; the picker calls `lib/db.ts:getRecentFoods`). The FAB version doesn't reflect newly-logged meals reliably — PM observed it as "outdated" relative to the picker. Either consolidate both surfaces onto `getRecentFoods` (same query, same time window, same limit) or refresh the FAB query on every open + on event-store mutation. Engineer recommends consolidation. Composes with B-007 (FAB experience revamp) — likely fixed in that PR. | Next | 2026-05-21 | B-007 FAB revamp | Open |
| B-020 | Picker search bar position mid-screen | PM-surfaced during B-011 PR2 QA 2026-05-21: the search input lives inside the Library section header, which after the Add-new + Recent zones puts it visually centred — owners with a few logged meals have to scroll past Recent to reach search. PM call: "I hate that the search button on the food library is sort of in the middle of the screen." Options: float search above Library (sticky-ish), or promote to a header-level affordance on the picker screen. Compose with B-004 (food library as top-level nav) if that lands first — the standalone library page wants a header search anyway. | Next | 2026-05-21 | B-004 food library top-level nav | Open |
| B-021 | Auth session token >2KB in SecureStore | PM-surfaced during B-011 PR2 QA 2026-05-21: Expo prints `WARN Value being stored in SecureStore is larger than 2048 bytes and it may not be stored successfully. In a future SDK version, this call may throw an error.` Coming from the Supabase session token. Non-blocking today but the SDK warning explicitly says it'll throw in a future SDK. Fix is to swap Supabase's session storage adapter to AsyncStorage for the session-token key (it's not actually secret — RLS still gates everything) or split the token into chunks. Investigate whether Supabase SDK already supports a configurable storage adapter (likely yes via `auth.storage`). Cheap to do; just hasn't been needed yet. | Later | 2026-05-21 | Before Expo SDK upgrade beyond the version where the warning becomes a throw | Open |
| B-022 | Tap-to-expand food photo on food detail screen | PM-surfaced during toast PR QA 2026-05-23: from FAB → Meal → picker → long-press a food → food detail screen, the food photo thumbnails are not tappable. Use case is owner-facing recognition: comparing the library photo to the food currently in hand to confirm it's the same product before logging. At thumbnail size that comparison is unreliable — brand stripe, fill height in the bowl, label colour are all the differentiators and they vanish in a 80–120pt square. Cheap fix: lift the fullscreen `Modal` + zoom viewer pattern already shipped on `app/edit-event.tsx` (lines ~351–380, `photoViewerVisible` state) into a shared `components/ui/PhotoViewer.tsx` and mount it from `app/food/[id].tsx` on photo tap. Composes with B-012 (which established the hero-photo + viewer pattern on event detail) — same UX motion extended to food detail. Considered for the toast PR but out of scope. **Implemented 2026-05-26** — extracted shared `components/ui/PhotoViewer.tsx` (single image or paging gallery, opens at tapped index; Close always, Replace/Remove conditional) and routed **all three** detail surfaces through it: food carousel (now tap-to-expand, Close-only), plus `event/[id].tsx` and `edit-event.tsx` migrated off their duplicated local viewers (PM call: one implementation, not two). Fullscreen-only, no pinch-zoom (deferred → B-036). | Next | 2026-05-23 | — | **Done — 2026-05-26 (PR #59).** Shared `components/ui/PhotoViewer.tsx` extracted; food carousel + `event/[id].tsx` + `edit-event.tsx` all routed through it. (Reconciled 2026-05-31 — row had stayed "Open — PR open (draft)" after the PR merged.) |
| B-024 | Add `jerky` to the `food_format` enum | PM-surfaced 2026-05-23: a treat that is jerky has no honest physical-form value in `food_format` today (closest is `freeze_dried` or `dry_kibble`, neither correct). Add `jerky` as a value so the Format chip can describe the physical form accurately — orthogonal to `food_type='treat'` (a jerky is a treat by *type*, jerky by *format*), which is exactly the separation B-017 establishes. **Composes directly with B-017** — both edit the `food_format` enum, so do them in one migration: B-017 drops `treat` + reshapes the values to be purely physical, and this adds `jerky` to that physical set. Purely additive on its own (new enum value, no backfill), but bundling avoids two churns of the picker code. Migration Safety Pre-flight required as part of the combined PR. **Scope decision 2026-05-26 (PM): ship STANDALONE, not bundled with B-017** — team read was that bundling a one-line additive change into B-017's destructive enum reshape raises blast radius for no schema-side saving (B-017's recreated type carries `jerky` forward regardless). B-017 stays deferred to food-library close-out. Built: migration `014_food_format_jerky.sql` (`ADD VALUE 'jerky' AFTER 'freeze_dried'`), `jerky` chip in `food-capture.tsx` + `food/[id].tsx` (onboarding deliberately keeps its staple-food subset — `jerky` added to its type union only), and the Edge Function learns `jerky` (Claude tool enum + `AI_FORMAT_TO_DB` + test). On-device QA passed (manual pick + save/persist/sync round-trip + edit path); AI-scan + onboarding-regression checks skipped as low-risk by inspection. | Next | 2026-05-23 | Food-library track close-out (bundle with B-017) | **Done — 2026-05-26 (PR #60).** B-017 still deferred to food-library close-out (carries `jerky` forward). |
| B-023 | Home zone for meal/treat preferences (intake-driven analytics) | PM-surfaced 2026-05-23: now that we have `food_type` (B-011) + `intake_rating` (B-014), there's a real owner-facing analytics surface — "what does Mochi actually like and eat?" Pet owners persistently ask "will my cat like this?" / "is she still eating the new kibble?" — today they answer it by gut feel; the data is in the app but invisible. Concrete examples worth surfacing: top 3 most-eaten meals (intake_rating='all' rate), foods Mochi has started refusing (declining intake trend over time, ranked), treats with highest acceptance (cross-pet "Mochi loves these"). This is genuinely new product surface, not a polish — extends the Home from a three-zone snapshot into a lightweight analytics destination, which is a real spec deviation. Open scoping questions: (a) does this slot in as a Zone 4 on Home, or get its own tab/destination — Designer concerned a fourth zone violates Principle 3 (Home is a Signal/Today/Trend intelligence surface, not a dashboard); (b) which queries does the correlation engine need to answer this server-side vs local SQLite aggregates; (c) cold-start UX when there are <5 logged meals per food (empty-state copy required per Principle 5); (d) does this graduate to Premium per Principle 7 — "pets > $" says analytics that inform care quality stay free, but a "compare to similar pets" extension might be premium-shaped. **Persona sentiment:** ✓✓ Jordan (real pain point — "will she like this"), ✓✓ Dr. Chen (declining intake trend is clinically actionable — feeds back to vet consults), ✓ Data Scientist (correlation engine already has the data; this is a query surface, not a schema ask), ✓ Engineer (mostly read-only — should be cheap once B-014 has accumulated data), △ Designer (concerned about Home becoming a dashboard — wants a design pass before scoping, possibly its own destination rather than a Home zone). Compose with B-004 (food library as top-level nav) if it becomes its own destination. Don't scope until B-014 has shipped to real users and we have intake data to validate the queries against — premature analytics on null data is the failure mode. **Update 2026-05-24 (persona discussion, PM-surfaced as a *positioning* question — "be known for more than tracking for sensitive stomachs"):** two new constraints fell out. (1) **Positioning frame:** preferences is the lever that broadens Nyx from the reactive sensitive-stomach/diet-trial wedge to the much larger healthy-pet owner ("the app that knows your pet"), and it's nearly free because the data substrate (`intake_rating` + `food_type`) already exists — zero new logging burden, honors Principles 1/2. Treat as a post-MVP wedge-broadener that must NOT pull resources from Step 9/10. (2) **Clinical-safety constraint (Dr. Chen + Data Scientist, non-negotiable):** intake *decline/refusal* is frequently a disease sign (anorexia is non-specific across CKD/dental/nausea/hyperthyroid per `docs/research/2026-05-feeding-windows-and-partial-eating.md`; feline 48hr hepatic-lipidosis window). The "likes/dislikes" framing is only safe for the **positive, multi-sample** signal ("Mochi loves these"); the **negative** signal must route toward a health flag, never be softened into "Mochi's being picky." Preference = a *rate over N samples*, not a single rating (set a min-sample threshold). Also: a pet that loves treats but refuses meals is a concern, not a preference — `food_type` (B-011) is the guardrail. Architecture note: per-pet "what Mochi likes" is a local SQLite aggregate; cross-pet "popular with similar pets" crosses RLS and is a server-side Edge Function aggregate (different build — don't conflate). **Folded into CLAUDE.md this session:** (i) the clinical-safety constraint is now a durable Data Scientist anti-pattern (governs every `intake_rating` consumer, not just this row); (ii) the **Sam (cat-owner) persona** is now drafted as a stub in the Product Team section — she, not diet-trial Jordan, is the true target user. No decision made; open scoping questions (a)–(d) above still stand. | Later | 2026-05-23 | B-014 intake data accumulated from real users; design pass on Home-vs-destination question | Open |
| B-010 | Event timestamp uncertainty: witnessed vs discovered model | The schema treats `occurred_at` as a single precise timestamp, but PM real-world usage shows owners frequently log events they did not witness — either reasoned point estimates (e.g. vomit found by treats, ~4am inferred) or bounded windows (e.g. vomit found at 4pm, room clean at 2pm). Persona estimates (Jordan + 5 archetypes) average ~35% witnessed / ~65% discovered, inverting the prior 80% assumption; literature corroborates (bilious vomiting overnight, cats vomit privately, vet history-taking explicitly distinguishes witnessed from inferred). Current schema produces false-precision timestamps that propagate into the correlation engine and vet report. Evidence base: [`docs/research/2026-05-event-timestamp-uncertainty.md`](./research/2026-05-event-timestamp-uncertainty.md). Foundational enough to interrupt Step 9 — false-precise timestamps in the vet report would ship a clinical regression. **Schema model DECIDED 2026-05-23 — Option C:** `occurred_at_confidence` enum (`witnessed`/`estimated`/`window`) + nullable `occurred_at_earliest`/`occurred_at_latest`; `occurred_at` kept as derived point. Confidence derived from the affordance touched (never a quiz); witnessed stays one-tap; confidence orthogonal to `occurred_at_source` (don't auto-set from EXIF). Full rationale in CLAUDE.md → Open Questions → Resolved. **Remaining work (in order):** (1) ✓ quick-log UX sketch — `docs/mockups/b010-found-flow.html`. **Direction 2 chosen** (explicit "Saw it / Found it" pills, "Saw it" preset) — optimizes for the ~65% found majority while keeping witnessed one-tap; (2) ✓ schema migration — `012_event_timestamp_confidence.sql` (PR #45, merged). **Backfill = NULL** (PM decision: hand-populate the small legacy set rather than blanket-assert 'witnessed'); column nullable, no default. **Applied to live DB 2026-05-23.** (3) ✓ quick-log capture UI — PR #48 (merged): Direction 2 control on the simple step, 3-mode found selector (all reachable), **windowed `occurred_at` = latest edge** (not midpoint), write + sync the new fields; QA notes 3 & 4 addressed; (4) ✓ PR C — detail-screen confidence display, `edit-event` editable witnessed/found control (non-meal events; meals forced witnessed), History read-only marker (PR #51, merged 2026-05-24; QA notes 1 & 2 closed; on-device QA passed). Added shared `describeOccurredAt` to `lib/utils` as the single source for confidence phrasing (reuse for vet report + AI Signal); `lib/db` TimelineRow/getTimeline/getEventById/updateEvent now carry the window fields, plus new `getEventTimeFields`. Unit tests written, unrunnable pending B-026; (5) vet-report rendering of estimated/window times — **next**, but gated on the PDF-library open question (§6 open: text range vs timeline bar); (6) Step 10 correlation-engine weighting. | Now | 2026-05-19 | Step 9 vet report timestamp rendering; Step 10 AI Signal correlation windows | Open — capture (#45 + #48) + display/edit (PR C, #51) shipped; vet-report rendering (step 5) next, gated on PDF-library decision |
| B-025 | FAB quick-symptom buttons bypass the B-010 timestamp-confidence control | PM-surfaced 2026-05-24 during B-010 QA: the FAB's quick "Vomit" / "Loose stool" chips (`components/log/FAB.tsx` `handleQuickSymptom`) write the event directly with `occurred_at = now` and **never open `app/log.tsx`**, so they skip the new "Saw it happen / Found it" control entirely — and don't set `occurred_at_confidence`, so those events land NULL/unclassified rather than `witnessed`. Net effect: two "Vomit" entry points behave differently (FAB = instant, no control; full flow via FAB → "More events" → Vomit = shows the control), and the fastest/most-prominent logging path can't reach the discovered-event path at all, undercutting the whole point of B-010. PM: "can't have one vomit button launch this flow and the other not — either unify, or be incredibly clear about it." Options: (1) **minimum** — stamp FAB quick-logs `occurred_at_confidence = 'witnessed'` (one-line, keeps the express lane instant, fixes the NULL-tagging); (2) route the chips to `/log?type=vomit` so the control shows (loses the one-tap speed — Designer will object on Principle 1); (3) instant log + a post-log toast "didn't see it? adjust" (mirror the meal "Change time" toast, B-014 pattern). Composes with **B-007** (FAB revamp) and **B-019** (FAB recent-meals staleness) — likely resolved together in the FAB pass. | Next | 2026-05-24 | B-007 FAB revamp | Open |
| B-026 | Wire up an automated test runner (jest) | The Definition of Done references `npm test` + jest, and `.test.ts` files exist (`store/toastStore.test.ts`, `store/attachmentStore.test.ts`, `lib/utils.test.ts`, `supabase/functions/extract-food-from-photo/index.test.ts`), but **there is no `test` npm script and jest is not installed** — so none of them run; the pre-push hook only runs `tsc`. Surfaced 2026-05-24 during B-010 PR #48 DoD: the `deriveOccurredAt` unit tests are written but unrunnable, so the "automated tests pass" DoD line can never be honestly satisfied for store/Edge-Function/utility logic. Fix: add `jest` + `babel-jest`/`ts-jest` + `@testing-library/react-native`, a `test` script, jest config (jest-expo preset), and ideally have the pre-push hook run it. Composes with **B-002** (pre-prod readiness). Low-risk, unblocks honest DoD compliance for every future store/util/Edge-Function PR. | Next | 2026-05-24 | — | **Done — 2026-06-05 (B-054 Phase 0).** Installed `jest-expo` (~54, SDK-aligned), `jest@29`, `@testing-library/react-native`, `react-test-renderer`, `babel-preset-expo`; added `babel.config.js`, `jest.config.js` (ignores the `supabase/functions/**` Deno tests), the `test` script, and wired the pre-push hook to run `npm test`. All 4 RN suites / 45 tests green. Fixed a latent state-leak in `store/toastStore.test.ts` (relied on `hide()` clearing `payload`, which it intentionally does not — fixed in the test, production behavior unchanged). |
| B-027 | Per-incident AI analysis — Vomit (first per-type child of B-013) | First incident type to ship under the B-013 shared-infra pattern — **read B-013 first; it carries every cross-cutting decision.** Build: (a) the shared Edge Function + detail-screen component + guardrail/disclaimer layer, parameterized by `incidentType='vomit'`; (b) the **vomit prompt template** — Dr. Chen-reviewed — with the vomit red-flag list (blood / haematemesis, suspected foreign material, repeated vomiting in a short window, bile-only overnight pattern, concurrent lethargy/anorexia — esp. the feline 48hr hepatic-lipidosis flag) driving the escalate-on-presence path; (c) structured vomit fields (colour, content [food / bile / foam / foreign], consistency, blood y/n) + free-text description, both from one vision call; (d) the n=1 read honoring Dr. Chen's **escalate-on-presence / never-reassure-on-absence** asymmetry; (e) editable fields + dismissible (soft-hide) read + per-field provenance + "Edited [date]" marker, with re-analysis never clobbering a human edit; (f) cached-on-log, async pending state, "not enough to say yet" uncertain state, no-photo handling. **Acceptance gate = Dr. Chen sign-off on the vomit prompt + red-flag list** (clinical judgment isn't unit-testable — QA validates against a fixed set of example photos with expected framing). Schema: new structured fields + AI-payload cache + provenance/dismiss flags likely on the event/attachment record — own migration PR per the migration-isolation rule, Migration Safety Pre-flight required (should be additive/non-destructive). Edge Function = its own PR; detail-screen UI = its own PR. n=1 read is owner-facing only (not on the vet report); structured fields feed the report. **Clinical definition drafted + escalation architecture decided 2026-05-24 (PM): context-assembled escalation floor.** Vision model raises *visual* flags (blood [fresh_red/coffee_ground], suspected foreign material) from the photo; the Edge Function computes *contextual* flags (repeated vomiting, feline reduced intake, concurrent lethargy, bile-overnight pattern) deterministically from `events`+`meals` (per-pet aggregates via `pet_id`, server-side — NOT the correlation engine) and these **force `worth_a_call` regardless of the photo — the model cannot downgrade them** (catches the clear-foam-but-cat-hasn't-eaten-36h hepatic-lipidosis case; also protects photo-less logs). Structured fields (colour/contents/consistency/blood/bile/foreign + per-field confidence, `unsure`-not-guess) feed the vet report; the n=1 `recommendation` enum is `worth_a_call`/`monitor`/`not_enough_to_say` — **ships with NO reassuring value by design** (false-reassurance unrepresentable, same spirit as B-010 CHECKs). Static non-diagnostic disclaimer in UI (not model-generated). Schema preview: dedicated `event_ai_analysis` table (1:1 on event_id, includes `pet_id` + RLS), enums, AI-payload cache + provenance + `dismissed_at` soft-hide, mirroring food's `ai_extraction_*`. **Thresholds set by Dr. Chen 2026-05-24:** repeated-vomiting = **≥2 in 4h OR ≥3 in 24h**; feline reduced-intake = **24h** (lowered from the textbook 48h because the flag fires *alongside an active vomit incident* — vomiting + anorexia compounds risk). `recommendation` enum confirmed with **NO reassuring value** (PM). **Bile-overnight (PM lean = include; Dr. Chen reframe pending PM confirm): capture bile/foam + overnight timing as descriptive structured fields in v1, but do NOT wire it as a `worth_a_call` escalation** — a single bilious episode is usually benign and escalating it would over-alarm (Principle 4 + Dr. Chen); a single instance stays calm `monitor`. The recurring-overnight *pattern* read is cross-incident by nature → deferred to the Step 10 cross-incident layer (B-013). **Data caveat for the Edge Function PR:** the feline-intake flag keys off *absence* of positive-intake logs, conflating "didn't eat" with "didn't log" (`intake_rating` is optional/null) — require a minimum logging baseline and/or prefer an explicit logged decline (`refused`/`picked`/`some`) to limit false-positives. **Acceptance gate CLOSED 2026-05-24** — PM confirmed the bile reframe ("go with Dr. Chen's read"): single bilious episode stays calm `monitor` (descriptive only, no reassurance), recurring-overnight pattern read deferred to the Step 10 cross-incident layer. Schema migration written — `013_event_ai_analysis.sql` (dedicated table, 1:1 on `event_id`, `pet_id` + RLS, six enums incl. `ai_recommendation` with no reassuring value, cached AI payload + per-field confidence + `edited_at` provenance + `dismissed_at` soft-hide; additive/non-destructive; PR #54). **SHIPPED 2026-05-24 — all three PRs merged:** schema #54, `analyze-vomit` Edge Function #55, client trigger + detail-screen UI #56. New-photo path validated on-device. Plumbing fixes en route (migration 003 gap, unchecked-upsert synced bug, Claude media-type, base64 memory, 5MB/undecodable-image graceful degradation, client-side photo compression). Persona QA done → `[Now]` fixes shipped (plain labels, ≥44pt tap targets); follow-ups split out as **B-028** (editable fields + provenance — the fast-follow), **B-029** (design polish), **B-030** (realtime), **B-031** (re-upload/UPDATE policy), **B-032** (B-010 windowed-time precision), **B-033** (intake-rating coverage for the feline floor), **B-034** (the acceptance gate). **Status: shipped; officially Done 2026-05-29 — B-034 read-framing gate cleared via Dr. Chen persona review** (real-photo visual-accuracy confirmation deferred per PM, "good enough for the moment"). | Now | 2026-05-24 | B-013 parent decisions (done); Dr. Chen vomit-prompt review | Done — 2026-05-29 (shipped #54/#55/#56; B-034 gate cleared, provisional — real-photo visual-accuracy check deferred per PM) |
| B-028 | Per-incident AI — editable structured fields + provenance ("Edited [date]") | B-027 shipped the structured vomit observations **read-only**; the editable layer from B-013 (freely-editable fields + dismissible-not-editable read + per-field "edited = differs from cached `ai_raw_payload`" + one calm "Edited [date]" marker, re-analysis never clobbering a human edit) was deferred to keep the first UI PR testable. **Dr. Chen flags this as clinically load-bearing, not polish:** if the AI mis-reads a field (e.g. "Blood: none visible" when the owner saw blood) there's no way to correct it, and that wrong value is what feeds the vet report. QA: the "never-clobber-on-edit" guard is untested until this ships. The `event_ai_analysis` schema already carries `edited_at` + cached `ai_raw_payload` for it. | Next | 2026-05-24 | — | Open |
| B-029 | Per-incident AI detail-screen design polish | Designer feedback from B-027 persona QA: (1) the **"AI READ"** all-caps label reads like a system tag, not Nyx's voice — warm it or drop it; (2) the read **pops in with no arrival moment** — design principles reserve motion for exactly the first-insight transition, so a gentle fade-in is warranted; (3) the **"What's visible" key/value block feels clinical/data-dumpy** on an owner screen — design pass on de-emphasizing it (Designer) vs keeping it as raw material for the editable-fields PR (Dr. Chen); low-stakes unresolved tension. | Later | 2026-05-24 | — | Open |
| B-030 | Per-incident AI — realtime instead of polling | `VomitAnalysisSection` polls `event_ai_analysis` every 3s (≤12 tries) for pending→done, chosen to avoid adding the new table to the `supabase_realtime` publication. The food detail screen already uses realtime (`postgres_changes`); switching here is cleaner + instant + removes the 36s poll-timeout edge (a very slow first analysis can time out the poll and need a manual Re-run). Requires adding `event_ai_analysis` to the realtime publication. | Later | 2026-05-24 | — | Open |
| B-031 | Event-attachment re-upload efficiency + bucket UPDATE policy | `ensureEventAttachmentsSynced` (and foreground sync) re-upload the photo with `upsert:true` even when the file already exists; `nyx-event-attachments` has no UPDATE storage policy, so overwriting an existing object 400s (the harmless "re-upload skipped" warnings). Either skip the re-upload when the row already exists server-side, or add an UPDATE policy to the bucket. Cosmetic/log-noise today; surfaced during B-027 photo debugging. | Later | 2026-05-24 | — | Open |
| B-032 | B-010 × per-incident floor — windowed-time precision | The repeated-vomiting contextual flag counts events within 4h/24h using `occurred_at` (B-010's representative point). For `estimated`/`window` (discovered) events that point is imprecise (latest edge), so the count can be slightly off for found incidents. Acceptable for v1; revisit whether the floor should weight recency by the window bounds. Data Scientist note from B-027 QA. | Later | 2026-05-24 | Step 10 correlation weighting | Open |
| B-033 | Surface/encourage intake rating to feed the feline-reduced-intake flag | B-027's `feline_reduced_intake` floor only fires when the owner has rated meals (≥1 rated meal in 7d, then no most/all in 24h). Sam (cat owner) flagged that fussy-vs-sick is exactly when she might *not* be diligently rating — so the highest-value flag can stay silent precisely when it matters. Consider a gentle prompt to rate intake (composes with B-014 surfaces + B-023). Not a bug; a data-coverage gap that limits the flag's reach. | Later | 2026-05-24 | B-014 / B-023 | Open |
| B-034 | B-027 acceptance gate — validate vomit read framing on real photos | The B-027 gate is "Dr. Chen sign-off on the prompt + red-flag list, validated against a fixed set of example photos with expected framing" — clinical judgment isn't unit-testable. Through build-out we fixed plumbing but never formally collected ~5 real incident reads and checked the framing (escalate-on-presence holds, never-reassure-on-absence, no diagnosis, plain language). Do this before marking B-027 officially Done. **Done — 2026-05-29:** Dr. Chen persona review of all 22 stored `analyze-vomit` reads — framing + structured-observation-logic audit from the `event_ai_analysis` rows, **no photos exfiltrated** (a throwaway Storage-export function was built then torn down unrun; reviewing private health photos to self-validate is both the diagnostic act the product forbids and beyond a persona's authority). Findings: the escalate-on-presence / never-reassure-on-absence asymmetry **holds across every `monitor` read** (honest "on its own / in this photo / this single instance" framing; none say "fine"); escalations fire correctly (repeated-vomiting ×2, suspected foreign material — blue toy balls); human-food and no-photo submissions refuse to fabricate findings; plain language throughout. Two **under-escalation** threshold-tuning notes (not framing failures, both honest about uncertainty) → **B-042**: `f675ea9d` (bile + an unidentifiable non-food piece stayed `monitor`, no foreign-material flag, inconsistent with the blue-ball precedent) and `01b267d6` (`blood`+`foreign_material`=`unsure` stayed `monitor`). Real-photo *visual-accuracy* confirmation + a colleague sanity-check deferred per PM ("good enough for the moment", 2026-05-29). | Now | 2026-05-24 | B-027 officially Done | Done — 2026-05-29 (Dr. Chen persona read-framing review; this session) |
| B-035 | Read-only intake badge: distinct non-chip treatment + differentiate decline from "All" | Surfaced 2026-05-26 while fixing the History intake-pill placement (PR #58); reinforced by Designer review of the post-fix screenshot. Three coupled issues with the read-only intake badge (`components/history/EventRow.tsx` → `IntakeChipRow` read-only path → `FilterChip`). (1) **"All" collision / chip-rhyme (Designer — sharpest reason to act):** the History screen's date filters (`All time`/`Last 7 days`) and type filters (`All`/`Meal`/`Vomit`…) are blue-outlined rounded pills, and the read-only intake badge is *also* a blue-outlined rounded pill that literally reads **"All"** — so the word "All" appears both as an active *filter* at the top and as an *intake value* on every row in near-identical styling. A user can misread the row badge as "matches the All filter" rather than "ate all of it." Aligning the badges into a tidy right rail (PR #58) made this rhyme *more* obvious, not less. The read-only badge needs a distinctly **non-chip** treatment (drop the border; filled tint or a small inline label) so it stops impersonating an interactive filter. (2) **Affordance ambiguity (Designer):** same root cause — the badge reuses `FilterChip` in its `active` state, looks tappable but isn't (`pointerEvents="none"`). The non-chip treatment in (1) resolves this too. (3) **Clinical color semantics (Data Scientist + Dr. Chen):** `Refused`/`Picked` render in the *same calm accent-blue* as `All`/`Most`, so decline — frequently a *disease* signal per the CLAUDE.md intake anti-pattern, not preference — reads as benign as a clean plate, and disappears into a wall of identical blue badges. Decline should get a quieter/attention treatment that routes the eye toward concern without alarm (Principle 4), never the reassuring blue. All three are *separate from* the placement fix (shipped PR #58). Scope: a dedicated read-only badge variant (or new `components/ui/IntakeBadge.tsx`) — non-chip styling + a 2-tier palette (positive multi-sample = calm; decline = attention). Guardrail: a single n=1 rating must not be read as a verdict — but this is a per-event display of the logged rating, so it's honest to show; the *aggregation/preference* reading is B-023's problem, not this badge's. | Next | 2026-05-26 | — | Open |
| B-036 | Pinch-to-zoom in the fullscreen PhotoViewer | Deferred from B-022 (PM chose fullscreen-only). The shared `components/ui/PhotoViewer.tsx` shows photos `resizeMode="contain"` with no pinch/pan — enough for brand/colour/fill comparison, but reading fine print on an ingredients label would benefit from true zoom. Adding it means a gesture/zoom dependency (e.g. `react-native-gesture-handler` + a zoomable image, or `react-native-awesome-gallery`). Low priority — the ingredients photo's text is already captured by the food extraction Edge Function, so zoom is a nice-to-have for owner recognition, not a clinical need. Revisit if owners ask to read labels in-app. | Later | 2026-05-26 | — | Open |
| B-038 | Onboarding food-format options out of sync with the enum | Noticed 2026-05-26 during B-024: `app/onboarding/food.tsx` `FORMAT_OPTIONS` offers only 6 of the `food_format` values — it omits `topper` and `treat` (and now `jerky`), while the other two pickers (`food-capture.tsx`, `food/[id].tsx`) offer the full set. Looks intentional (onboarding captures the pet's *staple* food, so treat-shaped formats are excluded), but it's undocumented and drifts every time the enum grows — B-024 had to decide per-screen whether `jerky` belonged. Decide: is the onboarding subset deliberate (then document it + derive both lists from one source so the staple subset is explicit), or a gap to backfill? Cheap either way; the risk is silent drift, not breakage. | Later | 2026-05-26 | — | Open |
| B-037 | PhotoViewer UX polish from Jordan review | Two owner-facing snags Jordan flagged reviewing B-022 (tap-to-expand). (a) **Discoverability:** nothing signals the photo is tappable on the food carousel or the incident-detail hero (the edit-event thumbnail does say "Tap to view") — Jordan found the expand by accident. Wants a subtle affordance (faint expand-corner icon or one-time hint). (b) **Swipe-down to dismiss:** her thumb expected the iOS Photos gesture — swipe the image down to close. **Tap-image-to-close already shipped in B-022** (covers the simplest version of her dismiss instinct); swipe-down is the remaining gesture and needs `react-native-gesture-handler` (composes with B-036's pinch-zoom — do the gesture work once). Both are polish, not blocking. | Later | 2026-05-26 | — | Open |
| B-040 | Always-available / free-fed food + shared-bowl attribution | PM-surfaced 2026-05-26: our food model logs intake well *in the moment* (meal/treat + B-014's `refused/picked/some/most/all`), but has no representation for food that's **continuously available** — free-choice / ad libitum / grazing. Real scenario (PM's own cats, dogfood): Nyx eats wet food meal-fed in the morning; her sister Theia gets weight-management **dry food that's down all day**; Nyx *also* grazes that dry bowl, and most of that consumption is **unwitnessed**. Three coupled sub-problems: **(1) Modelling standing availability** — free-fed food isn't a point event; it has no `offered_at` and no per-meal `intake_rating`. The event timeline (Option A) assumes discrete events; a 24/7 bowl is a *duration/standing exposure*, not a point. **(2) Multi-pet shared-bowl attribution** — the dry bowl is shared between two cats; even witnessed nibbles often can't be attributed, and absence-of-witnessed ≠ didn't-eat. The research brief is explicit: communal-bowl feeding "defeats individual-intake monitoring" and per-cat attribution is "unsolved without hardware" (RFID feeders) — so self-reported per-cat consumption is low-confidence and must be **marked as such on any vet-facing output**. Touches the multi-pet architecture that's "a sprint away" (bowl is shared → consumption can't be a simple `pet_id` FK). **(3) Diet-trial confound** — continuous access to a non-trial food is exactly the elimination-trial killer (Dr. Chen's binary-compliance rule; "one stolen kibble of a housemate's food confounds the trial"). If Nyx is being monitored for GI issues with 24/7 access to Theia's dry food, any "controlled diet" read on the vet report is silently false unless we surface the uncontrolled exposure. Also breaks the **48hr feline anorexia detection** — you can't see "stopped eating" when the bowl is always down (brief: "a 30–50% drop in 24h intake can occur without an owner registering a single missed-meal event"). Evidence base: `docs/research/2026-05-feeding-windows-and-partial-eating.md` §1 (free-feeding prevalence, multi-cat attribution), §3/§4 (diet-trial integrity, WSAVA feeding-*method* field), §6 (clinical implications). **Likely shape (not decided — kickoff conversation 2026-05-26):** free-feeding modelled as a **low-friction standing fact** set once at food/pet setup ("always available" + optionally "shared between these pets"), NOT a per-nibble log (Principle 1/2); witnessed-eating stays available as a discrete log that *does* feed the correlation engine; absence of witnessed logs is NOT read as "didn't eat"; vet report renders free-choice as a WSAVA feeding-method + flags shared/uncontrolled exposure + marks per-pet attribution low-confidence; diet-trial logic flags the uncontrolled-access confound (framed calm per Principle 4, not alarm). Composes with **B-014** (intake — shares the consumed-not-offered framing), **B-023** (preferences — same intake substrate), **B-010** (timestamp uncertainty — free-feeding is the ultimate "window" case), **multi-pet architecture** (shared bowl), and **Step 9 vet-report honesty** (feeding-method + off-diet-exposure rendering). **Persona sentiment (kickoff):** ✓✓ Sam (her exact reality — the true target user here, not diet-trial Jordan), ✓✓ Dr. Chen (clinical honesty on free-choice + attribution + trial confound is non-negotiable; gated on never reading absence-of-data as wellness), ✓ Data Scientist (must not let low-confidence shared-bowl data masquerade as clean per-pet intake in the correlation engine), △ Designer + Jordan (Principle 1/2 — must NOT become "log every nibble"; standing-fact framing only), ✓ Engineer (additive standing-fact field is cheap; shared-bowl attribution leans on multi-pet, scope that piece separately). **Persona conflict surfaced** (Designer/Jordan friction vs Dr. Chen/Data clinical completeness) — PM decision needed on scope/positioning before this is sized. **Kickoff decisions 2026-05-26 (PM):** (a) **research-only for now** — capture options, no build commitment; (b) **diet-trial-confound treatment PUNTED to implementation time** ("get the need to be strict, but we're not there yet"); (c) **shared-bowl attribution DEFERRED to multi-pet.** Consequence of (c): the *single-pet* slice (represent one pet's free-choice food honestly, e.g. "Theia: free-fed dry, always available") is buildable today; but the clinically sharpest part of the PM's *own* scenario — Nyx getting uncontrolled access to Theia's bowl — IS the multi-pet shared-resource attribution problem, so that piece waits for multi-pet. **Options map drafted in the kickoff (single-pet framing, not decided):** **A (base, always true)** — standing-fact `feeding_arrangement` (pet_id, food_item_id, method='free_choice', active window): set once, zero daily logging, renders as WSAVA feeding-method on the vet report, treated as *context* not events by the correlation engine. Recovers no intake signal on its own. **B** — A + optional refill/topup log → coarse consumption-rate proxy (single-pet only; shared bowl confounds it). **C** — A + keep the witnessed-grazing discrete log as real high-confidence intake (feeds correlation engine); unwitnessed stays silent context; absence ≠ didn't-eat. **D** — A + low-frequency coarse intake check-in ("is Theia eating her dry food normally? normal/less/not touching") → the cat-appropriate substitute for the meal-skip detector the 48hr window needs; depends on push-notification infra (B-015 blocker) + nudge budget (Principle 4). A is the foundation; B/C/D are non-exclusive signal-recovery layers — which to add is the deferred implementation decision. **Update 2026-05-26 (PM reactions — bumped to Now):** (1) PM endorses leaning on the **WSAVA free-choice feeding-method** (established clinical framework — the standing-fact 'A' base). (2) PM endorses **Option C** (optional log of *witnessed* eating events) as a hedge alongside the standing fact. Data Scientist guardrail stands: witnessed logs are real *additive* intake signal but a **biased sample** (only what you see) — they can feed correlation but NEVER be a denominator (no %-eaten, no anorexia detection from them), and absence ≠ didn't-eat. PM also raised a **far-future hardware-bowl integration** (RFID/gravimetric, e.g. SureFeed/Catlink) — clinically the gold standard for per-cat intake per the research brief §1/§4; design implication: model consumption as a **source-agnostic intake event** (source ∈ witnessed / manual / future-hardware, + confidence — mirrors B-010's source/confidence pattern and food's `ai_extraction_confidence`) so hardware slots in later with no rework. (3) **Scope generalization (the big one):** PM observed offered≠consumed is NOT unique to free-feeding — wet food 'given at 7am' isn't eaten at 7am either; cats graze it over hours. Real theme = **offered-vs-consumed fidelity across a spectrum** (fast meal-fed dog → grazed wet → free-fed dry). B-014 (shipped) already captures consumed *amount* (refused/picked/some/most/all); the remaining gap is consumption *timing/distribution* — which matters to the correlation engine (brief §2: ±2h offered-vs-consumed error shifts acute-vomiting differentials) but which owners cannot supply precisely (Designer/Jordan: 'when did she finish?' is theatre). Honest ceiling = a coarse consumption *window* (offered → bowl-removed), reusing B-010's windowing pattern, never false point-precision. **Scope-growth + sequencing flag (Dir of Eng):** folding wet food in moves this from an additive edge feature (standing fact on one food) to touching the **core meal/event model + correlation engine** — bigger blast radius; and 'Now' collides with two live priorities (B-027 not officially Done — B-034 gate open; Step 9 vet report already interrupted once for B-027), so 'Now' for *build* = Step 9 slips again (a PM roadmap call, flagged not blocked; 'Now' for research/design is clean). **Central open scoping question for design phase:** how far to model consumption-distinct-from-offering on the *core* meal log (Data wants it for correlation accuracy; Designer/Jordan cap the friction; Eng flags blast radius) — likely splits into separate build items (free-choice standing fact vs witnessed-event log [Option C] vs consumption-window on meals). **Competitive-landscape learnings 2026-05-27:** the layered R1(standing/ambient context) + R2(discrete annotation) model is the *industry-standard* pattern for unmeasurable intake — independently validated by CGM apps (passive glucose stream + discrete meal annotations, kept separate) and infant feeding trackers (breast = duration/side *proxy* vs bottle = *measured* volume — honest multi-fidelity, never conflated). Shared-bowl attribution is solved ONLY by hardware (SureFeed Microchip Feeder Connect / Petlibro RFID — per-pet via microchip, time + weight to 1g) → confirms deferring N1 to hardware/multi-pet AND positions Nyx as the clinical aggregation layer *above* feeder silos (a feeder only knows its own bowl, not wet food / treats / the other pet's bowl). Human food-symptom diaries (mySymptoms, 1M+ users) validate a *configurable, symptom-class-specific* correlation window (1–72h, default 24h) for Step 10 + "log every exposure" for diet-trial integrity + explicit correlation≠causation honesty copy. TWO new candidate mechanisms surfaced: (i) **photo-based consumption estimate** — the validated plate-waste before/after-photo method (snap bowl down → snap bowl picked up → infer % consumed against reference) as an R3 option that REUSES our existing food-photo extraction pipeline and self-tags confidence (validated in controlled settings; free-living accuracy not guaranteed → confidence-tagged estimate, not ground truth); (ii) make intake **fidelity tiers explicit** in the model (unknown / proxy / owner-estimate / measured-hardware), borrowed from breast-vs-bottle, sharpening D2's source+confidence into named tiers. Competitive lane: no one occupies "frictionless pet food+symptom correlation with clinical-grade vet output" — direct analogs are human + active-logging (mySymptoms/Cara) or pet hardware silos (feeders). `competitive-landscape.md` referenced in CLAUDE.md required-reading but MISSING from repo — flagged to PM. **Strategic value-prop gut-check 2026-05-27 (PM-requested):** team AFFIRMED confidence in the food↔incident value prop and current direction. Key reframe: the two-cat shared-free-fed-bowl difficulty is an *expansion-tier (Sam)* concern, NOT the *core wedge (Jordan — diet-trial/symptom-monitoring)*, where feeding is controlled & attributable by necessity. Multi-cat is common + growing (42% of US cat households, 2024 APPA; single-cat 64%→58% since 2018) so worth honest handling, but the promise holds via "correlate the attributable (directly-fed meals, treats, witnessed eating; incidents are nearly always attributable — you know which cat) + flag the uncontrolled shared bowl as a confounder" = **degraded-but-honest, still a clinical upgrade** (Dr. Chen: *absent* data doesn't confound a diagnosis, *false* data does). Honest dissent (Sam/Data Scientist): for a pure two-cat free-fed healthy-cat case with no controlled food, per-pet food↔incident value is genuinely thin until hardware — don't oversell it. **Risk flagged:** don't let the PM's personal two-cat case distort the roadmap toward software-only attribution (hardware-only per the landscape). Direction: serve the wedge now; honest degraded mode for multi-cat; feeder integration for true per-cat attribution later. CGM analogy corrected (PM): apt mapping is food↔meal-annotations / incidents↔glucose-stream, not two food streams. mySymptoms added to competitive-radar. **Listening-check correction 2026-05-27 (PM):** free-feeding is a first-class need for SINGLE-cat households too — it is **NOT expansion-tier**; only shared-bowl *attribution* (N1) is the multi-pet/hardware-deferred layer. The prior gut-check over-coupled the two. Decouple: **R1 (free-choice standing fact) = core + wedge-relevant** (a single-cat diet-trial patient can be free-fed the *prescription* diet — fully attributable, just grazed, not eaten in one sitting). Two **orthogonal confidence axes** now explicit in the model (sharpens D2 — an intake signal carries both independently): **(1) ATTRIBUTION confidence** — did THIS pet eat it (single-cat free-fed = high/near-certain even when unwitnessed, since no other pet *could* have; multi-cat shared = low, "probably had some" — the PM's "I KNOW she ate this" vs "likely had some" distinction); **(2) QUANTITY fidelity** — `unknown`/`proxy`/`owner-estimate`/`measured-hardware`. Consequence: **single-cat free-fed yields a usable correlation signal with no hardware** — attribution-certain + amount-unknown ("she ate from this bowl today, definitely her; how much/when unknown"); the engine can use attribution-certain/amount-fuzzy data if tagged as such. The hard, hardware-shaped problem is ONLY the attribution axis degrading in multi-cat — not free-feeding itself. | Now | 2026-05-26 | Multi-pet architecture (shared-bowl attribution); composes with B-014 / B-023 / B-010 / Step 9 | **R1 slice scoped + spec'd 2026-06-07** — team reconvened (PM-surfaced dogfooding pain: Theia's free-fed dry food can't be represented at all). **PM decisions:** v1 = **R1 standing fact only** (Option C, wet-food consumption-window, shared-bowl attribution, quantity recovery, diet-trial confound all deferred — each its own item); next step = **requirements doc first** → `docs/nyx-free-feeding-requirements.md` (build-ready). Proposed schema = new additive `feeding_arrangements` pet↔food join table (`is_shared` hook reserved inert for multi-pet). Build vs sequencing-vs-Step-9 remains a PM roadmap call (doc §10). Prior research/design context above retained. |
| B-041 | User data export (GDPR Article 20 portability) | GDPR Art. 20 (with Art. 15 right of access) gives users the right to receive their personal data in a structured, machine-readable format. Likely shape: an "Export my data" button on the profile screen that delivers a CSV of the user's records (pets, events, meals, intake ratings, vet visits, AI analyses, etc.). Like B-039, a client can't read across all its own rows cleanly under RLS, so this is likely an Edge Function with the service role key that reads-and-packages (mirror of B-039's tear-down). **NOT an App Store gate** — confirmed 2026-05-27 that Apple requires in-app account *deletion* (5.1.1(v) → B-039), not export; GDPR is the sole driver here. The legal obligation is the ability to *fulfill* an export request (manual fulfillment is technically sufficient); a self-service in-app button is the cleaner, lower-ops way to satisfy it. Composes with **B-039** (shared Edge-Function + service-role architecture, inverse operation) and the **GDPR-cascade Open Question** (anonymize vs hard-delete). Scope + requirements TBD in a later session. | Later | 2026-05-27 | First real-user release / GDPR compliance; composes with B-039 | Open |
| B-039 | In-app account deletion (App Store hard blocker) | Surfaced 2026-05-26 during App Store submission planning. **Apple Guideline 5.1.1(v): any app that supports account creation MUST let the user initiate account deletion from within the app** — not just deactivation, and a support-email link doesn't satisfy it. Nyx has email signup (and Apple Sign-In is planned), so this is a hard rejection blocker, not polish. **Directly tied to the still-open GDPR-cascade Open Question** (anonymize vs hard-delete on account deletion) — that decision must be made first, since it dictates the implementation. Engineering shape: a client can't cascade-delete its own data through RLS, so this needs an **Edge Function with the service role key** that tears down (or anonymizes) all the user's rows across every child table — `pets`, `events` (soft-deleted but still PII-linked), `meals`, attachments + Storage objects (`nyx-event-attachments`, `nyx-pet-photos`, `nyx-food-photos`), `event_ai_analysis`, `vet_visit*`, `ai_signals`, `user_profiles` — plus the Supabase Auth user itself (`auth.admin.deleteUser`). Note `food_items` is globally scoped (no `user_id`) so those rows stay. Needs: a confirmation UX on the profile screen (destructive, double-confirm, honest copy — no dark patterns, Apple also scrutinizes this), the Edge Function + its own migration if any tombstone columns are needed, and a decision on whether soft-deleted events are hard-purged on account deletion (likely yes — account deletion is the one place the soft-delete-only rule yields to a legal hard-delete). Composes with B-002 pre-prod readiness. | Now | 2026-05-26 | App Store submission / first prod build; GDPR-cascade Open Question (decide first) | Open |
| B-042 | Vomit n=1 escalation floor — treat `unsure`-on-blood and unidentifiable-non-food as soft triggers | Surfaced 2026-05-29 by Dr. Chen's B-034 read-framing review. The framing asymmetry is sound, but two real reads **under-escalated** relative to the floor's own precedent: `f675ea9d` (bile present + a piece "cannot be clearly identified as food" → stayed `monitor`, no `suspected_foreign_material` visual flag, where the blue-toy-ball case `6c5f92c2` *did* flag + escalate on the same kind of suspicion) and `01b267d6` (`blood_present`=`unsure` **and** `foreign_material_present`=`unsure` → stayed `monitor`). Neither is a false-reassurance failure (both honestly state the uncertainty), so this is **threshold tuning, not a guardrail breach**: consider routing (a) `foreign_material_present='unsure'` *with* a model-noted unidentifiable non-food fragment → `suspected_foreign_material` visual flag (soft escalate), and (b) `blood_present='unsure'` → a soft "mention it to your vet" nudge, consistent with escalate-on-*possible*-presence. Touches `supabase/functions/analyze-vomit` prompt/flag logic; re-run the B-034 example set after. Generalizes to sibling analyzers (stool/skin) when they ship. | Next | 2026-05-29 | — | Open |
| B-043 | Delete the leftover `tmp-img-export` Edge Function from Supabase | Surfaced 2026-05-29 during the B-034 read-framing review. A throwaway photo-export helper was deployed mid-session to pull real pet-health photos out of Storage for self-validation, then abandoned unrun (the approach was reversed — it violated our RLS/data-handling standards and reviewing private health photos is the diagnostic act the product forbids). It has been **neutralized** to an inert `410` stub with `verify_jwt=true`, but the function slot still exists in the project. **Remove it entirely** via Supabase dashboard → Edge Functions → `tmp-img-export` → Delete (no MCP/CLI delete tool exists, so this is PM-only). Low effort; flagged high-priority because a dangling export-shaped function — even a neutralized one — is exactly the kind of loose end a security review would catch. | Now | 2026-05-29 | — | Open |
| B-044 | Apply the rest of migration `003_attachments.sql` + audit which migrations are actually applied | Surfaced during B-027 plumbing debug (2026-05-24) and still open. Migration `003_attachments.sql` was only **partially** applied to the live DB — just the `event_attachments` block was run. `vet_visit_attachments` (and `food_items.photo_path`) likely still don't exist server-side, so vet-visit photo sync will fail (now gracefully — staying queued — after the B-027 sync fix, but it still won't succeed). **Two parts:** (1) run the remaining `003_attachments.sql` statements in the Supabase SQL Editor; (2) **audit the full migration history** — 003 slipping through partially means others may have too; reconcile `supabase/migrations/` against what's actually applied to the live DB. Blocks vet-visit attachment sync, which Step 9 (vet report) will lean on. | Now | 2026-05-29 | Step 9 (vet report) — vet-visit attachment sync | Open |
| B-045 | AI Signal (Home Zone 1) — build per `docs/nyx-ai-signal-requirements.md` | Light up the Home screen's "AI-powered insights" placeholder (`components/home/SignalZone.tsx`) — this is Step 10 in the build sequence. Full requirements + team alignment captured in `docs/nyx-ai-signal-requirements.md` (design session 2026-05-30). **Architecture (decided, unanimous):** deterministic detection + LLM phrasing (Option B) — server-side detectors compute & rank a *true* finding; Claude only renders the winning finding into one warm sentence, never decides whether a pattern exists. Reuses the `ai_signals` cache table already shipped in migration `005` (no v1 migration needed); reuses correlation reference query `[2]`. **v1 scope (PM decision 2026-05-30 — promoted the correlation flagship into v1):** ① food/protein→symptom correlation (the "your cat vomits after this food" wedge insight — rigor baked in: min-sample floor, multiple-comparison correction, confidence tiering, associational-copy-only, protein-level before ingredient-level) + ② intake-decline calm health flag (MANDATORY safety net — Dr. Chen's sign-off on shipping correlation first is conditional on the never-reassure flag shipping alongside). **v2 fast-follow:** ③ symptom-trend/diet-trial-progress (engine built v1 to anchor on `diet_trials.started_at`) + ④ positive food/treat preference (B-023's safe half). **Clinical guardrail:** this is the cross-incident multi-sample layer — the ONE place reassurance is permitted (multi-sample + quantified only); inherits & relaxes B-013's n=1 asymmetry; concern findings always outrank reassuring/preference ones in the ranking; absence of a pattern ≠ wellness (building/stale states, never an all-clear). **Principle 3 held:** one ranked sentence, NOT a dashboard (distinct from B-023's analytics-destination question). **Phased build (each step gated, dedicated session, AC pasted per Build Step Kickoff):** Step 1 — pure deterministic detection engine + unit tests (no LLM/UI); Step 2 — `generate-signal` Edge Function: detect→rank→phrase→cache(24h TTL)→templated fallback on LLM failure; Step 3 — wire `SignalZone` to the cache (cache-only read, live/building/stale states, async regen on daily-expiry + debounced-after-log). Composes with B-001 (cost/rate-limit — cache + debounce cap it), B-014 (intake lines), B-023 (preferences — shares substrate), B-033 (intake-rating coverage gates the decline flag). **Open decisions:** tap-to-expand evidence under the sentence (v1 or defer); exact sample-floor/confidence values (lean: start with defaults, tune on real data); correlation copy register (associational + "discuss with your vet" framing — nyx-voice pass at build time). **Persona sign-off:** Dir of Eng ✓, Data Scientist ✓ (conditional — rigor day-one), Dr. Chen ✓ (conditional — safety net ships with correlation), Nutritionist ✓, Biostatistician ✓, Designer ✓ (Principles 3/5), Jordan ✓, Sam ✓. **Rev 2 (PM review 2026-05-30):** home reshaped from a single-winning-sentence into a **curated, prioritized, open-ended set of insight cards** (correlations, preferences, future weight/activity/overfeeding…) — no single finding "wins," safety always leads, set is capped+calm. Adds **visible confidence/evidence tiers** (Early read → Established; never a fake %; weak still suppressed) which double as the speed-vs-quality lever (surface fast at Early-tier, labeled provisional). Adds a **time-to-first-insight model** with per-insight-type thresholds (target: a real insight within ~3–5 days of active logging). Loops in **Jordan + Sam** → home is **context-adaptive** (diet-trial pet leads with trial/correlation; healthy grazer leads with preferences/intake). **Revisits Principle 3** — proposed revised wording in spec §3.1, needs PM confirmation before editing canonical `design-principles.md` + CLAUDE.md (Tier 2/3). Possible small additive migration at build Step 2 to store a finding *set* vs single `signal_text`. **Rev 6 — spec FINALIZED 2026-05-30, all PM open decisions resolved (build-ready):** (a) tap-to-expand evidence under each card → IN v1; (c) visible-card cap → start ~3–4 **with a high-priority override** (a safety/concern insight is never withheld to honor the cap — folded into the revised Principle 3); (d) **Principle 3 revised + canonical docs (CLAUDE.md + `design-principles.md`) updated** — home is now a curated, prioritized, open-ended set of insight cards (sentence/stat/graph per type), not "three zones only"; (b) exact thresholds → **§7 table adopted as v1 defaults**, pressure-tested against the PM's real logging for cat Nyx (tuning loop on live data, not a blocker); (f) per-type card presentation (sentence vs stat vs graph + shared visual language) → design-phase task (Designer + Data Scientist, Dr. Chen + Jordan/Sam consulted); (g) weak *clinical*-correlation opt-in pull surface → **punted to B-046**, but v1 stays open to weak *benign* (positive-preference) insights on the home. | Next | 2026-05-30 | Step 10 (formal build step); Step 9 vet report remains the formal current phase until PM formally interrupts to start this | **Done — 2026-05-31.** All 3 steps built/merged: Step 1 detection engine (PR #72, incl. B-050 case-crossover), migration `015_ai_signals_findings.sql` (PR #73), `generate-signal` Edge Function (PR #74), `SignalZone` wiring (PR #75). Model = Haiku 4.5; cache = ordered findings set (24h TTL). Follow-up insight work continues as **B-051** (reflection detector ③), **B-052** (protein normalization), **B-053** (no-signal diagnostics). (Reconciled 2026-05-31 — row had stayed "Open — deferred to a dedicated session" after all 3 steps shipped.) |
| B-046 | "Patterns we're watching" — opt-in pull view for weak/emerging *clinical* signals (rung ④, copy-first) | Spun out of the B-045 AI Signal design session (decision (g), 2026-05-30). The home/push Signal surface deliberately **suppresses weak below-floor *clinical* correlations** (symptom/intake) — a labeled-"weak" clinical correlation still gets anchored on by an anxious owner and can drive bad elimination decisions or anxiety on noise (multiple-comparisons false positives; Dr. Chen + Data Scientist, decisive in B-045 §6). But the PM's transparency instinct is valid for the *curious owner who pulls* rather than the app that *pushes*. This item = a separate, **off-by-default** "patterns we're watching" explore view where an owner can opt in to see under-threshold emerging clinical patterns, framed "still learning, keep logging," **never concern-framed, never on the home push surface** (Dr. Chen veto on concern-framing stands). NOT v1. Distinct from the benign-preference weak signals, which DO surface on the home in v1 (B-045 §6) and don't need this view. Revisit after B-045 ships and there's real data + owner feedback on whether the pull view is wanted. **Reaffirmed + sharpened 2026-05-30 (speed-vs-rigor follow-up session, B-045 §7.1):** this view is **rung ④ of the value ladder** (early co-occurrence pattern, *not* a claim). The team weighed pulling it into v1 to accelerate adoption and decided **against** — v1 retention is carried by the *no-claims* rungs (① safety / ② reflections / ③ positive preference) + the daily nudge, none of which touch the significance threshold, so rung ⑤'s rigor stays pure and rung ④ doesn't need to ship early. Two reasons it stays here: **(a) confirmation-bias risk** — an early "watch chicken" makes the owner selectively over-log chicken, biasing the very dataset rung ⑤ depends on; **(b) it's unsolved at the *copy* level** — the gap between "worth watching" and "might be the cause" is one verb, and that verb is the whole product. So this is now explicitly a **copy-first** exercise: prove the calibrated, neutral, bidirectional copy contract before agreeing to surface it at all. When it does ship, rung ④ doubles as a statistical accelerant (a focused early hypothesis cuts the multiple-comparison penalty for that food, pulling ⑤ forward). Gate the reconsideration on **B-047** v1 retention data + the copy contract. | Later | 2026-05-30 | B-045 (AI Signal) shipped + real logging/retention data (B-047) + a proven calibrated-copy contract | Open |
| B-047 | Instrument AI Signal: time-to-first-insight + retention | Spun out of the speed-vs-rigor follow-up session (B-045 §7.1, 2026-05-30). The team's resolution of the rigor-vs-adoption tension rests on a *belief* — that the no-claims rungs (① safety, ② reflections, ③ positive preference) + the daily nudge hold an actively-logging owner for the ~6-week wait until a rigorous (rung ⑤) correlation lands, so we can keep ⑤ slow + pure and leave rung ④ (weak clinical early-pattern) punted to B-046. **Don't leave that belief untested.** Instrument from day one of the AI Signal ship: (a) **time-to-first-meaningful-insight** per owner (first non-placeholder card, by rung/insight type); (b) **2 / 4 / 6-week retention** correlated with whether/when an insight surfaced. This turns the rung-④ reconsideration into a data-driven call rather than another round of the same debate (Data Scientist). Composes with B-016 (observability pipeline — likely the same analytics/event sink) and B-001 (cost/rate-limit — same instrumentation seam). Scope: define the events, the sink (Sentry/PostHog/Supabase table — decide with B-016), and PII rules. | Next | 2026-05-30 | B-045 (AI Signal) ship; informs the B-046 rung-④ reconsideration; composes with B-016 observability | Open |
| B-049 | Correlation engine — residual statistical-rigor refinements | Logged 2026-05-30 after fixing the two panel-flagged P0s in the B-045 Step-1 detection engine (PR #72). The gross **pseudoreplication** was fixed (each symptom episode attributed to its single nearest preceding meal; rapid re-logs collapsed into one episode), but two finer, deferrable rigor items remain: **(1) residual within-subject temporal autocorrelation** — a pet fed the same protein daily for weeks produces exposures that aren't fully independent, which Fisher's exact still assumes; a proper treatment is a clustered/mixed-effects or permutation approach, overkill for v1's tens-of-events scale but worth revisiting once datasets grow. **(2) Bonferroni family definition** — the correction divides alpha by the count of (protein × symptom) pairs that *cleared the Early exposure floor*, i.e. a post-filter family; a stricter reading would correct over all pairs evaluated, and Bonferroni itself is conservative vs Benjamini–Hochberg FDR once the family is larger. Both are "make the Established p-value even more defensible" refinements, not correctness blockers — the Established tier is already gated on the corrected p AND the ≥5+5 sample floor AND associational-only copy. **Now scoped against the shipped B-050 case-crossover** (the FDR-vs-Bonferroni + autocorrelation notes still apply). Refinements deferred from the B-050 build (2026-05-31): **(3) 1:M conditional matching** — B-050 ships 1:1 nearest-control matching with a simple exact McNemar; a 1:M conditional-logistic / Mantel-Haenszel design would use more controls per case and recover power; **(4) control reuse** — a single control day can match several cases (acceptable for v1, slightly anticonservative); **(5) dose / proximity weighting** — a 2-cal treat and a full meal are equal binary exposures, and a meal 30 min before == one 7h before; data-limited (B-040 quantity axis), equal-credit for now. Revisit when real dogfood data volume makes a more sophisticated test meaningful (composes with B-032 windowed-time weighting, B-040 quantity axis, B-048 ingredient-level). | Later | 2026-05-30 | Real dogfood data volume; composes with B-032 / B-040 / B-048 / B-050 | Open |
| B-050 | Detector ① rewrite — symptom-anchored case-crossover (replaces nearest-preceding) | Surfaced 2026-05-30 (B-045 design thread). The PM caught a real flaw the expert personas had waved through under ceremonial ✓s: detector ①'s **nearest-preceding-meal attribution** (the fix for the pseudoreplication P0) over-corrected into **winner-take-all** — a symptom gets blamed on the single closest preceding meal, which (a) discards the other in-window exposures and (b) systematically *exonerates the daily staple* (clinically backwards — Dr. Chen). This rewrite replaces it. **Agreed design (case-crossover):** (1) **Unit = the symptom episode** (a "case"), not the meal — each counted once (preserves the pseudoreplication fix). (2) **Multi-implication** — a case implicates **every** protein present in its preceding window, not just the nearest (PM's 9am-wet + 10am-treat → both credited). (3) **Matched controls** — for each case at time *t*, draw control windows at the **same time-of-day on that pet's other days with no symptom of that type**, and **only days with comparable logging** (logging-eligibility guard — else the detector ② logging-gap bug is reintroduced on the control arm: cases are better-logged than controls → attention-bias false positives — Biostatistician's catch). (4) **Matched test** — McNemar / discordant-pairs, NOT pooled Fisher (pooling matched data is biased, not just imprecise — Biostatistician). (5) **Symptom-class-specific windows** — split GI into vomit (~8–12h) vs diarrhea (longer); derm ~72h (Dr. Chen — vomit and diarrhea have different latencies). (6) Bonferroni/FDR + Early/Established tiers carry over; honest framing that **Established will rarely fire early** at these sample sizes (consistent with the §7.1 value ladder). **Attribution-confidence as a first-class input (PM-endorsed direction — model multi-cat as the GENERAL case, single-cat as the high-confidence endpoint):** partition exposures by attribution confidence — high-attribution exposures (hand-fed meals, treats, witnessed eating) feed the correlation cleanly; a shared free-fed bowl is a **low-attribution background exposure** carried as a confounder that **caps the confidence tier** (cannot reach Established while an unattributed shared bowl is in-window) and never produces a clean correlate. The *incident* is almost always attributable (you know which cat was sick) even when the *exposure* is not — so lean on attributable exposures, stay honest about the rest. Single-cat = every exposure high-attribution = the clean special case. Directly ties to **B-040**'s two-axis model (attribution × quantity); the attribution-confidence signal is B-040's to capture, this engine's to *honor*. **Scope decisions locked this session:** detector runs **general-purpose, NOT restricted to stable-diet periods** (PM: must serve the "watchful" pre-trial owner gathering evidence to decide whether to escalate, AND the messy multi-cat household — not just formal trials); the diet-trial reverse-causation confound is covered by associational-only copy + "discuss with your vet" + the future trial-anchored detector ③ for formal trials. **Deferred within scope (data-limited, not algorithm-limited):** dose / proximity weighting (a full meal vs a 2-cal treat; a meal 30 min before vs 7.5h before) — can't capture grazing time/amount honestly yet (B-040 quantity axis); equal-credit-within-window for now. **Process note:** this rewrite is the first to ship under the new DoD adversarial-review rule — each expert states the counterexample they tried (constant staple washes out; multi-cat shared bowl degrades not false-fires; attention-biased control logging). **Blocks B-045 Step 2** — the `generate-signal` Edge Function must not wire detector ① until this lands; the current branch (PR #72) carries the rejected nearest-preceding placeholder behind a KNOWN-LIMITATION comment. **DONE — 2026-05-31 (PR #72):** detector ① rewritten as the symptom-anchored case-crossover exactly per this design — multi-implication, time-of-day-matched 1:1 controls with a logging-eligibility guard, McNemar exact test, split GI windows (vomit 12h / diarrhea 24h / derm 72h), attribution-confidence input (a 'low' shared-bowl exposure caps the finding at Early). A **non-overlap control guard** was added mid-build (a control window must sit ≥ windowHours from the case, else a long 72h window's adjacent control overlaps the case and self-washes — surfaced by the adversarial review). 29 unit tests; `deno check` + `tsc` clean; KNOWN-LIMITATION comment removed; shipped under the new adversarial-review DoD. Residual refinements (1:M conditional matching, control-reuse, within-subject autocorrelation, dose/proximity weighting) live in **B-049**. | Now | 2026-05-30 | B-045 Step 2 (Edge Function) — now unblocked; composes with B-040 (attribution capture), B-049 (residual stats) | Done — 2026-05-31 (PR #72) |
| B-048 | Ingredient-level food→symptom correlation (next detector tier above protein) | PM-surfaced 2026-05-30 reviewing the B-045 detection engine: we capture the **full ingredient list** off the label photo (`extract-food-from-photo` → `ingredients_text`, AAFCO order, verbatim) and store it as free text on the food (`food_items.ingredients_notes`), but the v1 correlation detector (`detection.ts`) keys off **`primaryProtein` only** — the ingredient list is *banked, not analyzed*. Spec §4 says "protein-level **before** ingredient-level"; this row is that deferred next tier. **Why it's deferred, not skipped (ties to §7.1 value-ladder rigor):** (1) **multiple-comparisons blow-up** — 20–40 ingredients/food × symptom types massively inflates the test family, so the Bonferroni penalty skyrockets and the Established rung needs far more data → even slower-to-significance than protein; (2) **collinearity** — "chicken / chicken meal / chicken fat" co-occur within a product and usually can't be statistically separated; protein is the lower-dimension, less-tangled cut; (3) **it's the clinical cut anyway** — elimination diets reason in *protein* (novel/hydrolyzed), Dr. Chen's mental model; (4) **data not structured for it** — free-text ingredients need a **canonicalization pass** ("dehydrated chicken" → `chicken`, handle synonyms/qualifiers/AAFCO naming) before they can be a correlation dimension. **Likely shape:** an ingredient-normalization step (parse `ingredients_notes` → canonical ingredient set per food, probably itself an LLM/lookup pass, stored structured — the "structured ingredient list is post-MVP" the schema already flags) + a new registry detector that runs the same reference-query-[2] windowing + Fisher/correction machinery over canonical ingredients, almost certainly **Established-tier-only with a stricter floor** given the multiplicity, and consider **restricting the candidate set** (e.g. only ingredients the owner is plausibly trialing, or known-allergen classes) to keep the test family small. Composes with B-045 (same engine/registry), B-009/B-018 (food dedup — canonical foods feed canonical ingredients), and the §7.1 instrumentation (B-047). NOT v1; revisit once protein-level has real dogfood data and the value of going finer-grained is demonstrated. | Later | 2026-05-30 | B-045 (AI Signal) protein-level shipped + real data; ingredient-normalization pass | Open |
| B-051 | AI Signal building-state copy misleads a heavily-logging pet (the "reflection" gap) | Surfaced 2026-05-31 dogfooding B-045 Step 3 on cat Nyx (132 meals + 11 vomits over 2 weeks). With v1's two detectors — correlation ① (correctly **washes out** because chicken is a ~3×/day constant staple with no chicken-free control days) and intake-decline ② (correctly **quiet** — Nyx is eating `all`/`most`) — there is genuinely no qualifying finding, so the Signal renders the **building** state. But the building copy ("We're getting to know Nyx — keep logging and the first patterns will surface in a few days") reads as *"not enough data"* to an owner who has logged heavily for two weeks — the exact **"silence churns"** failure §7.1 names (a power-user told to keep logging feels ignored; the filing-cabinet churn Jordan quit two apps over). Root cause: the building state conflates *"too little data yet"* with *"plenty of data, nothing cleared the bar."* The proper fix is the **reflection rung** (§7.1 rung ②: "Nyx vomited 4 times this week — same as last," counts/streaks, no causal claim), which is **detector ③ (v2)**. Two options to consider: (a) bring a minimal reflection card forward into v1 so a data-rich pet always has *something* honest on the Signal (mind Dr. Chen's §7.1 amendment — a *declining* trend routes to safety ②, only flat/improving renders as a neutral reflection); and/or (b) a cheaper interim **copy** fix — when the pet has substantial logs but no findings, say "No clear patterns in Nyx's logs yet — we'll keep watching as you log," distinct from the early "still getting to know" copy. Needs PM call on scope (interim copy now vs wait for detector ③). Composes with B-045 §7.1, B-047 (retention instrumentation will measure exactly this), detector ③. **PM-ELEVATED to Now (2026-05-31):** interim copy shipped (PR #75); the real work — the reflection detector ③ so a data-rich pet always has *some* honest insight on the Signal — is now top priority alongside B-052. This is "surface presence"; B-052 is "make it clean." | Now | 2026-05-31 | B-045 Step 3 shipped; detector ③ (reflections, v2); PM copy-vs-detector scope call | **Detector ③ BUILT 2026-06-07 — draft PR #106** (branch `claude/admiring-bell-XYSJy`). New `reflection` finding type: symptom-episode counts only, week-over-week, flat-or-improving only; **global worsening gate** (any rising symptom by episode-count OR symptom-day-spread blanks the whole layer → yields to the safety lane, Dr. Chen §7.1 amendment #5); never on a zero count (§9 absence≠wellness); ranks below safety AND correlations (band 3); **template-only phrasing** (no LLM → no reassurance drift). PM scope calls: counts-only (no streaks/intake-up), suppress-on-worsening, lowest rank; data-floor (max≥3 episodes, ≥3 logged days/window, `worseningMinEpisodes=2`) deferred to product team, tunable per §7. **No schema** (jsonb `findings` open). `adversarial-reviewer` BROKE 3 load-bearing axes (cross-symptom leak, meal-padding gap, validatePhrasing paraphrase bypass) → all fixed + regression-tested → re-review PASSED. `deno test` 69 + `jest` 114 green. **Done — 2026-06-07 (PR #106 merged).** `generate-signal` deployed via CLI (detector ③ + B-052 `canonicalizeProtein` now live); on-device QA PASSED — Nyx's Signal shows "We've logged 1 episode of vomiting for Nyx this week, down from 4 last week" (cached finding verified: `{type:'reflection', currentCount:1, priorCount:4, direction:'improving'}`). Known interim debt shipped consciously (PM + product-team sign-off): the reflection **duplicates the Trend zone** for symptom counts → tracked as **B-067**. |
| B-052 | Normalize `primary_protein` before correlation grouping (casing + by-product variants) | Surfaced 2026-05-31 dogfooding B-045 Step 3. Real `food_items.primary_protein` values for cat Nyx are dirty/fragmented — `chicken`, `Chicken`, `Chicken By-Product Meal`, `chicken by-product meal` are stored as **four distinct proteins** (case-sensitive, by-product variants, leading/trailing noise). The case-crossover detector keys correlation off `primaryProtein` exactly, so one real protein splits across several keys → fewer discordant matched pairs per key → a real signal is **harder to surface** (compounds the constant-staple washout), and any finding that *did* fire would phrase badly ("meals containing Chicken By-Product Meal"). Fix options: (a) normalize at **write time** in `extract-food-from-photo` (lowercase + trim + map common by-product/qualifier variants to a canonical protein) — preferred, fixes the data at the source; and/or (b) a **defensive normalization** of the protein key inside `detection.ts` before grouping (cheap, immediate, helps existing dirty rows). A backfill pass over existing `food_items` would clean Nyx's historical data. Distinct from but composes with B-009/B-018 (food dedup) and B-048 (ingredient-level canonicalization) — same canonicalization muscle, narrower scope (protein field only). **PM-ELEVATED to Now (2026-05-31):** un-fractures the correlation key so results aren't split across `chicken`/`Chicken`/etc — raises the quality of every finding (won't by itself surface Nyx's chicken, which washes out as a constant staple, but is foundational for any intermittently-fed protein). Prefer (a) write-time normalization in `extract-food-from-photo` + a one-time backfill, with (b) defensive key-normalization in `detection.ts` as the immediate safety net. | Now | 2026-05-31 | B-045 correlation quality; composes with B-009 / B-018 / B-048 | **Partially done — option (b) shipped** on branch `claude/dreamy-feynman-ghXWU` (2026-06-07): `canonicalizeProtein` (new `supabase/functions/generate-signal/protein.ts`, qualifier-strip + junk-drop, no synonym mapping) applied read-time in `detection.ts` before grouping; unit-tested (`protein.test.ts` + new `detection.test.ts` pooling cases). **PM scope call 2026-06-07:** detector-only — keep display text, so (a) write-time normalization in `extract-food-from-photo` AND the one-time `food_items` backfill were intentionally NOT done. **Remaining (Open, suggest re-prioritize to Next):** option (a) + backfill, if/when we want the stored values + vet-report labels cleaned at source. |
| B-054 | Multi-device down-sync (hydration) — a second device sees the shared account's full history | **Verified this session (2026-06-05):** Nyx sync is **push-only** — `lib/sync.ts` flushes local writes *up* to Supabase, but no code path pulls another device's rows *down* into local SQLite (the only cloud→local write is `refreshFoodCache`, the global food library). So two phones on one account are write-only islands: the PM's wife logged into the shared account on a second phone and saw an **empty log** while the PM's writes were visible in the Supabase dashboard. Pets/conditions/diet-trials/AI-Signal cross devices (read live from Supabase); **events / meals / timeline / vet-visits / attachments do not** (read from local SQLite, `lib/db.ts`). This is the missing *down* half of Step 8 (the flush queue is the *up* half). **Reframing (2026-06-05): this is NOT an Expo Go artifact and NOT only a multi-caregiver feature** — it's a single-user **data-durability / device-portability** defect that hits in production on a new phone, a reinstall, an iPad+iPhone, or iOS app-offload (local SQLite starts empty; the app never reads the durable Supabase record back). Perceived loss, not actual loss (data is safe in Supabase), but trust-breaking for a "we remember so you don't have to" app → **likely a pre-prod / App Store launch blocker** (sits with B-039 / B-002), not an optional enhancement. Phase 1 alone fixes the single-user reinstall/new-phone case. Now-vs-pre-prod sequencing is a PM call (spec §1.1 + §9.5). Full requirements + open PM decisions + surfaced persona conflict in **`docs/multi-device-sync-requirements.md`**. Hydration targets: `events`, `meals`, `event_attachments`, `vet_visits`, `vet_visit_attachments` (RLS already permits the read). Key risks captured in the spec: the `set_updated_at` server trigger breaks naive LWW (FR-5); hard-deleted meals can't be observed by a pull and need absence-reconciliation or a tombstone (FR-8, composes with B-005); **logout must wipe local SQLite** once a device can hold another caregiver's data (FR-9, Trust & Safety ship gate); attachment images resolve from Storage not `local_uri` (FR-10). v1 = pull-on-trigger (foreground/reconnect/login) on the single shared login (PM confirmed "same login for now"); **Realtime + household/linked-accounts are explicitly out of v1** (composes with B-030 realtime plumbing). **Phased build plan in spec §12** — four small PRs in risk order (Phase 0 jest/B-026 → Phase 1 read-only cold-start hydration + logout-wipe = the "wife's phone" fix, low risk → Phase 2 conflict-correct LWW, medium risk, adversarial review → Phase 3 incremental watermark + meal-delete reconcile), each with a paste-ready kickoff prompt. **Open: PM sequencing call** — this is new scope outside the MVP build sequence; building it slips Step 9 (vet report) / Step 10 follow-ups. Persona sentiment: Eng ✓, Data ✓ (conditional on FR-5 named), Designer △ (cold-start UX conflict needs a ruling), Trust & Safety ✓ (FR-9 gate), Dr. Chen ✓, Jordan/Sam ✓✓. | Next | 2026-06-05 | Likely pre-prod / App Store launch blocker (device portability — sits with B-039 / B-002); new scope vs Step 9/10 (PM roadmap call); composes with B-005 / B-026 / B-030 / B-039 | **v1 scope complete — all merged to main 2026-06-07.** Phase 0 ✓ (PR #82, jest), Phase 1 ✓ (PR #83 — hydration engine + FR-9 logout-wipe), Phase 2 ✓ (PR #84 — conflict-correct server-time LWW + `meals.updated_at`/B-055; migration `016` verified applied to live DB: 200 rows, 0 nulls), §6 cold-start UI ✓ (PR #85 — block-only-when-empty + reactive refresh-after-hydrate), Phase 3 ✓ (PR #86 — FR-3 incremental watermark + FR-8 absence-reconcile of hard-deleted meals). **Remaining = one physical gate, not code:** AC-6 logout-wipe verified on-device (Trust & Safety; passes at code level). Phase 4 (Realtime) is post-v1/optional, deliberately out of scope. Follow-up hardening tracked as B-057/B-058 + the `expo-image` remote-cache-on-wipe flag. |
| B-055 | Add `meals.updated_at` to make cross-device meal-edit reconciliation correct (the principled FR-6 fix) | **Surfaced 2026-06-05 by the adversarial-reviewer during B-054 Phase 1.** The B-054 requirements §5.2 FR-6 assert meals are "effectively immutable once written" and prescribe `insert-if-absent` hydration — but that premise is **false in the codebase**: `lib/db.ts` `updateMealFood` and `updateMealIntake` mutate a meal in place (`synced=0`), and `intake_rating` is the clinically load-bearing WSAVA 5-point field (intake decline → hepatic-lipidosis routing). Pure insert-if-absent would silently drop a cross-device meal correction and leave device B reassuring on a stale intake reading — an intake-safety-invariant violation. **Phase 1 ships a safe stopgap** (`'refresh-if-synced'` strategy in `lib/hydration.ts`: insert-if-absent, else refresh a converged `synced=1` local meal, never clobber a pending `synced=0` edit) which propagates corrections without a schema change. **Residual bound:** because meals carry no `updated_at` (server or local), two *simultaneous offline* edits to the same meal on two devices resolve by server-arrival order with zero authorship signal (worse than events, which at least compare `updated_at`). The principled fix is a `meals.updated_at` column + `set_updated_at` trigger (own migration PR, Migration Safety Pre-flight, additive/non-destructive) so meals get true LWW like events. Compose with B-054 Phase 2 (FR-4/FR-5 conflict-correctness) — do it in the same conflict-correctness pass. Also revisit the `'refresh-if-synced'` SQL: the `synced=0` protection currently lives only in the pure `reconcileBatch` filter, not as a `WHERE synced=1` backstop on the `ON CONFLICT DO UPDATE` (holds by single-threaded execution today — a defense-in-depth note for Phase 2). | Next | 2026-06-05 | B-054 Phase 2 (conflict-correct LWW); composes with B-005 | **Done — 2026-06-07 (B-054 Phase 2).** Migration `016_meals_updated_at.sql` (additive: nullable→backfill `created_at`→NOT NULL+DEFAULT NOW()→`trg_meals_updated_at`); local schema + meal write-paths (`updateMealFood`/`updateMealIntake` + 3 insert sites) stamp ISO `updated_at`; push sends it; `hydrateMeals` switched to real `'lww'`, retiring `'refresh-if-synced'`. The defense-in-depth `WHERE <table>.synced = 1` backstop was added to the meals/events/vet_visits hydrate `DO UPDATE` (clean int compare, no parseTs trap). Adversarial review BROKE the first cut (a placeholder/param-count mismatch in the meals INSERT would have thrown silently on every hydrate-write → caught, fixed, re-review PASSED). |
| B-056 | Harden `parseTs` timestamp normalization against offset-less / fractional forms | **Surfaced 2026-06-06 by the adversarial-reviewer (DoD pass on the B-054 timestamp fix).** `lib/hydration.ts` `parseTs` normalizes only the *exact* seconds-only SQLite space form (`^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}$`) to UTC before `Date.parse`. Two other offset-less forms would silently mis-parse as **local time** on a non-UTC device and corrupt LWW: a space form **with fractional seconds** (`"... 14:23:05.123"`) and a `T`-separated form **without `Z`** (`"...T14:23:05"`). **Both are unreachable today** — every `updated_at` writer in the repo uses `new Date().toISOString()` (always `Z`) or PostgREST (always an offset), and SQLite's `datetime('now')` is seconds-only — so this is NOT a current defect (review verdict was PASS). But the guard is narrower than its comment implies, and a future writer (a migration using `strftime`, a third-party lib, an SDK change) emitting either form would reconcile wrong with **no test catching it**. Fix: either broaden the regex to "any offset-less datetime → treat as UTC," or assert ISO-with-offset at write time (a single `toISOString()` helper for all event/vet_visit writes). Low priority/effort; compose with B-055 (same Phase-2 LWW hardening pass). | Later | 2026-06-06 | — (compose with B-055 / B-054 Phase 2) | Open |
| B-057 | Guard against `INSERT` column/placeholder/param-count drift in the hydrate SQL | **Surfaced 2026-06-07 by the adversarial-reviewer (B-054 Phase 2).** The reconcile *decision* lives in the pure, unit-tested `lib/hydration.ts`, but the actual `db.runAsync` writes in `lib/sync.ts` (`hydrateEvents`/`hydrateMeals`/`hydrateVetVisits` + the `syncPending*` upserts) are hand-written SQL with parallel column-list / `?`-placeholder / param-array / `excluded.*` clauses that must all agree. Phase 2 shipped a real break here (10 cols named, 9 placeholders, 10 params, `excluded.intake_rating` referencing a non-inserted column) that **threw on every meal hydrate-write, was swallowed by `runHydrationStep`, and was invisible to the green test suite** — caught only by adversarial review. jest has no native SQLite so these statements can't run in-process today. Options: (a) a tiny static assertion test that extracts each hydrate INSERT and asserts `placeholder_count === param_count` (regex/AST over the SQL string — cheap, catches the exact class); (b) an in-memory SQLite (e.g. `better-sqlite3` as a dev-only test dep) to actually execute the hydrate writes against a throwaway schema; (c) a column-list/param builder helper so the two can't drift. Team lean: (a) now (cheapest), (b) if the shell grows. The DoD's "tests required for shared utilities" exemption for these I/O writes ("no native SQLite in jest") is exactly the gap this closes. | Next | 2026-06-07 | — (hardens B-054 sync; do before Phase 3 adds more hydrate SQL) | Open |
| B-058 | Name/enforce the "no write transaction > overlap" assumption behind the FR-3 hydration watermark | **Surfaced 2026-06-07 by the adversarial-reviewer (B-054 Phase 3).** The incremental hydration watermark (FR-3, `lib/hydration.ts` `watermarkQueryFloor` + `HYDRATE_WATERMARK_OVERLAP_MS = 2min`) defends against the Postgres `now()`-is-transaction-START-time skew (a write whose txn began before another device's pull but commits after it lands with an `updated_at` below the pull's observed max) by flooring each incremental `.gte()` pull at `watermark − 2min`, re-pulling a recent overlap window every cycle. **This HOLDS for every write path that exists today** — the only writers to `events`/`meals`/`vet_visits` are the `syncPending*` single-`.upsert()` calls, each a sub-second one-statement transaction, so the 2-min overlap is ~120–600× headroom (adversarial verdict: PASS). **Residual, not a current defect:** the guarantee rests on an *environmental* invariant ("no write transaction on a hydrated table exceeds the overlap"), which the code does not itself enforce. A future **server-side batch `UPDATE`/backfill/migration** that bumps `updated_at` on many rows inside one transaction running >2min would reintroduce the silent lost-row class with no test catching it. Options: (a) document the invariant at the write sites + as a migration-authoring rule ("no >2-min txn touching a hydrated table without bumping the overlap"); (b) raise the overlap if a known long job is introduced; (c) the principled fix — track a server-side logical sequence / `txid`-based watermark instead of wall-clock `updated_at` (larger; only if batch writes become common). Low priority; name it so the assumption is explicit rather than implicit (consistent with how the file already names the FR-5 server-time-LWW bound and B-056). | Later | 2026-06-07 | — (hardens B-054 Phase 3; compose with any future server-side batch write on a hydrated table) | Open |
| B-059 | Consolidate the three meal-insert sites behind one `insertMeal` helper | **Surfaced 2026-06-07** verifying the B-052 deploy: logging a meal via the quick-log FAB produced no AI Signal regen. Root cause = side-effect drift across duplicated insert logic. A meal can be created in **three** places — `app/log.tsx` (full flow), `components/log/FAB.tsx` (quick-log), `app/food-capture.tsx` (photo) — each hand-writing the same `INSERT INTO events`+`INSERT INTO meals`+`food_items_cache` update + sync push, and each responsible for *remembering* to also fire `triggerSignalRegenDebounced` (§2 freshness). Only `log.tsx` did, so the FAB and photo paths left the home Signal stale until the 24h cache expiry. The immediate gap was fixed by adding the trigger to the two missing sites (branch `claude/dreamy-feynman-ghXWU`, 2026-06-07), but that *re-duplicates* the side-effect rather than removing the drift. Proper fix (Dir. of Eng.): one `insertMeal({petId, foodId, occurredAt, ...})` helper in `lib/` that owns the event+meal write, the sync push, AND the signal-regen trigger, so a new entry point physically can't forget a side-effect. Composes with B-057 (same "hand-written SQL drifts" class — a builder/helper closes both). **Done — 2026-06-07 (branch `claude/ecstatic-faraday-diUfn`).** New `lib/meals.ts` `insertMeal()` owns the event+meal write, the `food_items_cache` last_used_at touch, the sync push (events→meals), and `triggerSignalRegenDebounced`; all three call sites (`app/log.tsx` `handleConfirm` meal branch, `components/log/FAB.tsx` `handleQuickMeal`, `app/food-capture.tsx` `commitFoodInner`) now route through it (−34 lines at the sites). Kept store-free by design — callers still own `prependEvent`/toast (UI concerns that differ per surface). Beneficial side-effect of consolidation: meals now always write `occurred_at_confidence='witnessed'` (the FAB + photo paths previously left it NULL; only `log.tsx` set it). `lib/meals.test.ts` asserts all three side-effects fire (the exact B-059 gap) + B-057 placeholder/param-count drift guards; tsc clean, 98/98 jest. | Next | 2026-06-07 | — (prevents recurrence of the §2 freshness drift; compose with B-057) | Done — 2026-06-07 |
| B-060 | `analyze-vomit` n=1 read reassures on the ABSENCE of a red flag ("nothing obviously alarming is visible") | **PM-surfaced 2026-06-07 during PR #99 (palette) on-device QA** — spotted on a real vomit detail screen, NOT in the palette diff (the AI read copy is owned by the `analyze-vomit` Edge Function / `event_ai_analysis`, untouched by the palette swap). The shipped read text reads: _"…and **nothing obviously alarming is visible on its own**. It is worth keeping a close eye…"_ That clause reassures on the *absence* of a visible red flag from a **single sample**, which trips the n=1-never-reassures invariant the whole per-incident-AI track is built on (B-013 parent / B-027 vomit child / `clinical-guardrails` skill): a single-sample read may **escalate on the PRESENCE** of a red flag but must **NEVER reassure on its ABSENCE** (absence ≠ wellness — the clear-foam-once-but-not-eaten-36h cat looks benign in one photo and is the feline 48h hepatic-lipidosis case). The surrounding copy hedges well ("on its own," "keep a close eye," the escalation list), so this is a **phrasing** fix, not a logic rebuild — but "nothing obviously alarming is visible" is exactly the sentence the guardrail forbids; reassurance comes only from the (not-yet-built) cross-incident layer. Fix: run the `clinical-guardrails` + `nyx-voice` lenses over the `analyze-vomit` system prompt / `validate*` layer so the n=1 read states what IS visible and what to watch for, without an absence-of-red-flag all-clear. Check the prompt's few-shot/examples for the same pattern, and whether `validatePhrasing`-equivalent guardrails can mechanically reject "nothing alarming"-class phrasings. Composes with B-027 (same function + asymmetry) and informs Step 10's inherited guardrail. Own PR; copy/prompt only, no schema. **NOT part of PR #99.** | Now | 2026-06-07 | — (clinical safety; composes with B-027, informs Step 10 AI Signal) | Open |
| B-061 | App-wide Geist body-font rollout (the rest of the v1.2 fonts "bland lever") | Surfaced building PR 2 of the design-system rebuild (fonts). That PR wired `expo-google-fonts`, the font-load gate, the resolved tokens (`fontBody`→Geist + `Geist-Medium`/`Geist-SemiBold` weight aliases, `fontDisplay`→Newsreader), and applied the **Newsreader display face to the AI Signal headline only** (the one application §4 names). The migration plan notes Geist body is "likely a *bigger* lever on 'feels bland' than color" — but applying it correctly is **not** a token swap: the app uses raw RN `<Text>` across ~39 files with `fontWeight` (53 `weightMedium`, 3 `weightSemibold`, 3 `weightRegular` sites), and RN does **not** synthesize weights for custom fonts, so a naïve global `fontFamily: 'Geist'` would flatten all 53 medium emphases to regular. Correct rollout = map each weight token to its loaded family (`Geist` / `Geist-Medium` / `Geist-SemiBold`). **Approach is an open decision (see Open Questions):** a `ThemedText` wrapper migrated across the 39 files (clean, no-magic, but churny + on-device-QA-heavy) vs a centralized default-`Text` weight-mapping shim (one file, but trips the "no magic" convention). Geist (400/500/600) is already loaded + tokenized, so this is a pure-application PR once the approach is chosen. Keep each screen internally consistent if phased (never Geist-in-one-card, system-in-the-next on the same screen). **Known trap (flagged in PR 2 review):** every existing `fontWeight: '500'/'600'` becomes a no-op the moment `fontFamily: 'Geist'` is applied to that `Text` — RN won't synthesize weight, so e.g. `InsightCard.expandHint` (`weightMedium`) silently flattens to 400. The rollout must swap `fontWeight` tokens for the matching `fontFamily` (`fontBodyMedium`/`fontBodySemibold`) at every weighted site, not just set `fontBody`. | Next | 2026-06-07 | Design-system PR 2 (fonts) shipped the infra; this consumes `fontBody`. Decide approach (Open Questions) first. | Open |
| B-053 | Explain WHY there's no signal yet (coverage / near-miss diagnostics on the no_pattern state) | PM-surfaced 2026-05-31 dogfooding: an owner who has logged for weeks WILL ask "why is there still no signal?" — and the generic "no clear patterns yet" line (B-051 interim) doesn't answer it. Surfacing the *reason* the engine stayed silent does two things the PM named: (1) **retention** — an honest, specific "here's why" keeps the owner coming back instead of concluding the app is broken/useless (the silence-churn risk again, §7.1); (2) **corrective action** — it tells the owner how to change their *logging* to unlock a signal, which both helps them and improves the dataset. Concrete reasons the engine can already explain (it knows why each detector didn't fire): **(a) constant-staple washout** — "Nyx eats chicken in nearly every meal, so we can't tell whether it's linked to her symptoms — logging some meals without it would help" (exactly Nyx's case); **(b) no symptom-free control days** for the case-crossover; **(c) below the evidence-tier floor** — "a few more logged vomiting episodes and we can start checking for patterns" (show progress toward the §7 threshold); **(d) intake flag dormant because meals aren't rated** (composes with B-033 — "rate a few meals and we can watch for changes in how much Nyx eats"); **(e) sparse / dirty protein data** (composes with B-052). **Design + clinical guardrails (load-bearing):** must NOT nag (Principle 4 — fold into the existing surface, not a daily push), must NOT become a dashboard, and must NEVER read as reassurance — "no signal" is about data coverage, NOT wellness (clinical-guardrails / §9): "we can't see a pattern yet" never becomes "nothing's wrong." Likely shape: the `no_pattern` state's tap-to-expand reveals the top 1–2 coverage reasons + the single most useful corrective nudge, calm and specific (nyx-voice). **Engine component:** detectors emit structured "near-miss / coverage" reasons alongside findings (below-floor counts, staple-coverage ratio, unrated-meal coverage, control-day availability) so the copy cites real numbers and never invents them — same deterministic-detection + phrasing split as the findings themselves. Composes tightly with B-051 (same no_pattern surface), B-052, B-033, B-047 (instrument whether explaining the gap measurably improves retention + corrective logging). | Next | 2026-05-31 | B-045 Step 3 (no_pattern state shipped); composes with B-051 / B-052 / B-033 / B-047 | Open |
| B-062 | Convert the remaining photo-affordance emoji glyphs (📷) to Lucide | Surfaced building design-system PR 3 (event icons). PR 3 replaced all **event-type identity** emoji (🍽 🤢 💩 😴 🐾 ➕) with Lucide via `EventIcon`, and — for visual consistency with surfaces it already touched — swapped the camera glyph in the two **event** photo-attach rows (`app/log.tsx`, `app/edit-event.tsx`) to Lucide `Camera`. Still on emoji, because they're **food-library / photo-pipeline** surfaces outside PR 3's "event rows / FAB / vet report" scope: the event-detail empty-photo hero (`app/event/[id].tsx:348`), the food-capture primary-button camera (`app/food-capture.tsx` ×2), the FoodPicker add-CTA (`components/food/FoodPicker.tsx:101`), and the PhotoCarousel empty state (`components/food/PhotoCarousel.tsx:53`). Low effort/risk — swap each `<Text>📷</Text>` for `<Camera size={…} color={theme.colorTextSecondary} strokeWidth={1.75} />` (size per adjacent copy: 16 inline, 24+ in heroes). Do in one pass so the app has no mixed emoji/Lucide camera glyphs. Pure UI, no schema. | Later | 2026-06-07 | — (cosmetic consistency follow-up to design-system PR 3) | Open |
| B-063 | Completion "moment" → shared, tone-aware overlay fired by every log path | **PM-surfaced 2026-06-07 during design-system PR 4 (#102) on-device QA; PM deferred the design call to the Designer, who decided.** PR 4 added the earned warm-gold glow + spring check to the **only** path that had a completion screen — `app/log.tsx`'s `step==='complete'`. The FAB quick-log paths bypass `/log` entirely (`handleQuickMeal` writes + shows the toast; `handleQuickSymptom` writes and **shows nothing at all** — no moment, no toast, zero confirmation), so the fastest, highest-frequency taps get the *least* feedback. Not a #102 regression (no moment existed anywhere before), but #102 makes the inconsistency obvious. **Designer decision:** the moment fires on **every** log path, but is **tone-aware** — the celebratory warm-gold bloom is for meals / routine / non-symptom logs; symptom logs (vomit, diarrhea, lethargy, itch) get a **calmer, neutral confirm** (no festive gold), because the "earned moment" rewards the *act of tracking*, not the content, and celebrating a worrying symptom is tone-deaf to an anxious owner (Sam) — brushes Principle 4 (warm, not nagging) + the Calm/Oura benchmark. **Shape:** extract the inline completion view out of `app/log.tsx` into a shared store-driven overlay (mirror the `Toast` pattern — a `momentStore` + a `<CompletionMoment/>` mounted at the root) so `handleQuickMeal`, `handleQuickSymptom`, the picker path, and the symptom/simple form all call `showMoment({ tone })`. Fixes the quick-symptom no-feedback gap en route. Store logic + shared component → **tests required**; new pattern → future-self review. Own PR, NOT part of #102. Composes with B-007 (FAB revamp) and B-019 (FAB recent-meals stale). | Now | 2026-06-07 | — (immediate follow-up to design-system PR 4 #102; quick-symptom no-feedback is a live UX gap) | **Done — 2026-06-07 (PR #103).** Shipped as the design-system PR-4 carrier (subsumed & closed the inline #102): shared `store/momentStore.ts` + `components/ui/CompletionMoment.tsx` root overlay, tone-aware (calm symptom / gold routine), fires on every log path, FAB quick-symptom no-feedback gap closed. On-device QA passed (8 steps). |
| B-064 | Fold meal intake capture (+ "Change time") into the completion moment; retire the standalone post-log meal toast | **PM-surfaced 2026-06-07 during PR #102 QA; design call deferred to and made by the Designer.** For meals the gold moment ("Logged") is immediately followed by the post-log `Toast` (Change time + WSAVA how-much-eaten chips) — two surfaces back-to-back that undercut the moment's closure and read as "two steps," making the *optional* follow-up feel required (brushes Principle 1). **Designer decision:** collapse them into ONE surface — the meal moment shows gold + "Logged {brand?}" + an optional inline "How much did {pet} eat?" WSAVA chip row (meals/treats only, per B-014), auto-dismissing if untouched, with "Change time" as a subtle affordance in the same surface. **Safety invariant preserved — non-negotiable:** intake stays captured at peak recall, optional, default-null, never pre-stamped; B-014's three Designer conditions + the "intake is not preference" invariant (Dr. Chen / Data Scientist) carry over unchanged. This changes the *carrier surface*, not the capture. Depends on B-063 (the shared overlay is the carrier). Touches the `Toast`/`IntakeChipRow` plumbing + store → **tests required**. Own PR. | Next | 2026-06-07 | B-063 (shared completion-moment overlay is the carrier) | **Done — 2026-06-07.** Designer call (PM-confirmed): the meal surface is a **warmed bottom card**, not the persisted full-screen moment (a blocking takeover lingering for optional input fights the ≤2s terminal beat + risks making the optional intake read as required). Folded into `store/momentStore.ts` as a discriminated-union payload (`kind: 'beat' \| 'meal'`); new `components/ui/MealCompletionCard.tsx` carries gold check + "Logged · {brand}" + optional WSAVA "How much did {pet} eat?" chip row + "Change time". Both meal paths (`log.tsx` handlePickFood, `FAB` handleQuickMeal) now fire `showMeal`. Retired `Toast.tsx` + `toastStore.ts` entirely (both call sites were meal-only). Intake invariant held: every `showMeal` passes `intakeRating: null`, chips never pre-selected, card non-blocking + auto-dismisses. 18 momentStore tests; tsc clean; 108 jest green. |
| B-065 | `food-capture` meal-log path has no intake / "Change time" surface | Surfaced by `code-reviewer` during B-064 (PR #105). `app/food-capture.tsx`'s confirm flow, when reached from the meal log (`cameFromMealLog`), calls `insertMeal` then shows its own inline `step='complete'` full-screen beat — it never fires `showMeal`, so the WSAVA intake chip row + "Change time" are absent from the photo-capture-and-log path. **This predates B-064 and is outside its named two-path scope** (B-064 changed `log.tsx` + `FAB`); `food-capture` was also never one of B-014's four v1 capture surfaces, and the old toast never fired here either — so it's not a B-064 regression. But it IS a meal-entry path, and the "every meal-entry path routes through showMeal or intake capture vanishes" invariant now applies. Likely fix: after `prependEvent`, fire `showMeal({…, intakeRating: null})` + `router.back()` instead of `setStep('complete')` for the meal-log branch (mirror `log.tsx` handlePickFood) — but `food-capture`'s 'complete' step may carry "you added a new food" affordances worth preserving, so confirm the UX before collapsing it. **PM call:** is intake capture wanted on the add-new-food-then-log path? | Next | 2026-06-07 | — (B-064 follow-up) | Open |
| B-066 | On-dark text colors hardcoded instead of tokenized (house-rules sweep) | `code-reviewer` flagged during B-064 (PR #105): `MealCompletionCard.tsx` uses `'#fff'` / `'rgba(255,255,255,0.7)'` / `rgba(...)` literals on its dark card, violating CLAUDE.md "no hardcoded values — all tokens in `constants/theme.ts`." This is a **codebase-wide convention**, not new to B-064: `components/log/FAB.tsx` (plus glyph, symptom text), the (now-deleted) `Toast.tsx`, and others all use hardcoded white-on-dark. Tokenizing only the new file would create inconsistency, so this is logged as a single consistent sweep: add `colorTextOnDark` / `colorTextOnDarkSecondary` (+ on-dark hairline/overlay) tokens to `theme.ts` and migrate every on-dark surface in one pass. Pure UI, no schema. Compose with any future design-system token work. | Later | 2026-06-07 | — | Open |
| B-067 | AI Signal reflection (③) duplicates the Trend zone for symptom counts | **Surfaced 2026-06-07 on-device, the moment detector ③ (B-051) went live.** The Signal now renders the reflection as the big Newsreader hero ("We've logged 1 episode of vomiting for Nyx this week, down from 4 last week"), and the **Trend zone one card below** (`components/home/TrendZone.tsx` `SymptomChart`, `hooks/useTrend.ts`) shows the same week-over-week count with the same framing (`Vomit · 1 this week · ↓ from 4 last week — improving`) **plus a distribution sparkline** — i.e. Trend carries strictly more. Root cause is a spec overlap, not a build defect: §7.1 rung ② ("reflections: trends, counts, streaks") was effectively already shipped as the Trend zone. **Shipped the duplication consciously** (PM + product-team sign-off, 2026-06-07) to fix the empty-Signal retention gap now and reshape later. **Two coupled things to resolve:** (1) **redundancy** — pick the reconciliation: (a) suppress ③ when the Trend zone already charts that symptom (clean Signal, but a single-symptom pet like Nyx falls back to `no_pattern` → pairs with **B-053** to fill it with something Trend can't say — the leading option), (b) demote ③ out of the hero slot to a quiet footnote, or (c) differentiate ③ to a higher-altitude synthesis (hard for single-symptom pets — the count *is* the insight). (2) **window-consistency risk (Data Scientist, load-bearing):** detector ③ uses rolling-7d windows (`now-7d`/`now-14d`); `useTrend` may use a different window (calendar week?) — today they agree (1/4), but a divergent window would show **contradictory "this week" numbers** in two zones on one screen. Verify/align the window definitions (or share one helper) regardless of which redundancy fix is chosen. **Engineer note:** two independent code paths compute the same week-over-week and will drift; the reconciliation should converge them. Composes with B-051 (the reflection), B-053 (the no_pattern fill), B-047 (retention instrumentation — measure whether the reflection vs Trend matters to retention). Pure UI/detection-gating + possible window-helper share; no schema. | Next | 2026-06-07 | — (B-051 follow-up; composes with B-053 / B-047) | Open |

---

## Done

_(items move here when closed; keep the row, set Status to `Done — <date>`, and note the resolving PR/session)_
