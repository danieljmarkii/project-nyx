{
  "_README": "Edge-Function deploy ledger (B-178 / CUL-135). One entry per deployable function. `fingerprint` = the sha256 of the function's shipping import-closure (index.ts + every local .ts it inlines) last acknowledged for deploy \u2014 computed by guards/edgeFunctionDeploy.test.ts, which FAILS THE BUILD when a function drifts from its recorded fingerprint without a reasoned acknowledgment. Statuses: `deployed` (fingerprint confirmed live), `pending` (a deploy is owed OR the live state is unverified \u2014 see the entry's `reason`), `hold` (deliberately not deployed \u2014 e.g. B-494). `pending`/`hold` require a `reason`. After deploying a function, set its `fingerprint` to the current value (the guard's failure message prints it) with status `deployed`. This ledger records intent; it does NOT prove a deploy happened \u2014 see docs/edge-deploy-runbook.md \u00a7 Deploy ledger.",
  "_seed": "Seeded 2026-08-19 (CUL-135). Fingerprints are the current main closures at ledger creation. Live-vs-main was NOT verified per-function (the live artifact is a bundle, not a source closure, and this guard is token-free by design), so no function is seeded `deployed`: generate-report is the one confirmed `hold` (B-494); every other function is `pending` (baseline). Each converts to `deployed` on its next verified deploy-from-main, or via the live reconciliation follow-on (CUL-541).",
  "functions": {
    "analyze-stool": {
      "status": "pending",
      "fingerprint": "sha256:140150a64cc932147ce71e10e23dfd94563bb1e88a91bf91eea65bc8fae0d803",
      "reason": "RE-FINGERPRINTED 2026-09-05 for CUL-812 (also closes CUL-539): the outer catch's best-effort failure write no longer upserts status:'failed' over a row already holding a worth_a_call \u2014 it records the error alongside and leaves status/recommendation/read_text as the record earned them (buildFailureWrite), the row is re-read AT the write decision rather than reused from before the vision call (the step-3b copy is 10-60s stale and a sibling invocation can write inside that window), a failed read of it fails CLOSED, and existingRealAnalysis now counts a worth_a_call as real WHATEVER its status says \u2014 without that last clause the cap branch writes 'capped' over exactly the rows the client renders as escalations. A benign prior read is deliberately NOT preserved (the failed attempt may have been reading a REPLACED photo, so a stale 'monitor' would be reassurance about an image nothing read). Behaviour change on the failure path only; no model, prompt, cap or floor change. Deploy still rides the held CUL-557 chain (analyze-vomit \u2192 analyze-stool \u2192 ask). \u2014 Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: A8 transform_only redeploy, CUL-258 Anthropic fetch timeouts (via _shared/http.ts), and CUL-542 (#682, foreign-material fragments on the stool read).",
      "ref": "CUL-812",
      "updated": "2026-09-05"
    },
    "analyze-vomit": {
      "status": "pending",
      "fingerprint": "sha256:258cd9b40837bff55470d03953d95a04f47b2e1f5acf6358ddba107d252475f4",
      "reason": "RE-FINGERPRINTED 2026-09-05 for CUL-812 (also closes CUL-539): the outer catch's best-effort failure write no longer upserts status:'failed' over a row already holding a worth_a_call \u2014 it records the error alongside and leaves status/recommendation/read_text as the record earned them (buildFailureWrite), the row is re-read AT the write decision rather than reused from before the vision call (the step-3b copy is 10-60s stale and a sibling invocation can write inside that window), a failed read of it fails CLOSED, and existingRealAnalysis now counts a worth_a_call as real WHATEVER its status says \u2014 without that last clause the cap branch writes 'capped' over exactly the rows the client renders as escalations. A benign prior read is deliberately NOT preserved (the failed attempt may have been reading a REPLACED photo, so a stale 'monitor' would be reassurance about an image nothing read). Behaviour change on the failure path only; no model, prompt, cap or floor change. Deploy still rides the held CUL-557 chain (analyze-vomit \u2192 analyze-stool \u2192 ask). \u2014 Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: A8 transform_only redeploy, CUL-152/B-179 free-text `description` gate (#671, 'one post-merge deploy'), CUL-258 fetch timeouts, and CUL-240 (#681, foreign-material fragments on the vomit read).",
      "ref": "CUL-812",
      "updated": "2026-09-05"
    },
    "ask": {
      "status": "pending",
      "fingerprint": "sha256:0366a0bf02214d528512318083254b37712c040125c1d7b33e9e0eaad7868dc7",
      "reason": "RE-FINGERPRINTED 2026-09-03 for CUL-786: `engineFindings` (tools.ts) now excludes the `stood_down` marker generate-signal writes into ai_signals.findings \u2014 relaying it flipped `hasFindings` true for an otherwise-silent engine and handed the model a sentence about absence; an empty relay must stay 'engine silent', never an all-clear from either side. One filter line + a test; no other behaviour change. Deploy still rides the held CUL-557 chain (analyze-vomit \u2192 analyze-stool \u2192 ask). \u2014 Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: A8 (deploy AFTER analyze-vomit/analyze-stool per the ordering gate) and CUL-258 fetch timeouts. Re-fingerprinted 2026-08-28 for CUL-676 W1-PR-3b: ASK_SYMPTOM_TYPES gained cough + sneeze, mirroring lib/analytics.ts SYMPTOM_EVENT_TYPES (which gained them in PR-3a) \u2014 the G5 Timeline-parity this list exists for, since History and the Patterns grid already show those rows and an Ask count would otherwise disagree with the record the owner is reading. Deploy continues to ride the held CUL-557 ask-redeploy chain, never its own.",
      "ref": "CUL-135",
      "updated": "2026-09-03"
    },
    "delete-account": {
      "status": "hold",
      "fingerprint": "sha256:99ac95836a4ce9ae813959e933e6ba5112995e7e987233a827027c2b1b7e2ccf",
      "reason": "B-119 / CUL-215 re-auth HOLD (client-first deploy ordering): the function now fails closed (reauth_required 401) when a request body carries no password, and the CURRENT shipped client sends none. Deploying this BEFORE a client build carrying the password field is the live/minimum build would 401 every current-client deletion \u2014 an Apple 5.1.1(v) erasure regression. Deploy only AFTER that client build is live, then set `deployed`.",
      "ref": "CUL-215",
      "updated": "2026-08-20"
    },
    "extract-food-from-photo": {
      "status": "pending",
      "fingerprint": "sha256:772c6f590f8a2c6de1dff8f2f16739da7fa654d87e9c5782b746dc4286b90854",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: CUL-258 Anthropic fetch timeouts (via _shared/http.ts).",
      "ref": "CUL-135",
      "updated": "2026-08-19"
    },
    "extract-medication-from-photo": {
      "status": "pending",
      "fingerprint": "sha256:b91616f9c0e685a6f40a0208a57cb353ce2691dc54afeac05a45dbb956170ddc",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: CUL-258 Anthropic fetch timeouts (via _shared/http.ts).",
      "ref": "CUL-135",
      "updated": "2026-08-19"
    },
    "generate-report": {
      "status": "pending",
      "fingerprint": "sha256:7b11c4c1e9e1ce78b3b6297cc10bb9b7ec1d2a7ee812898df2463ed932be6432",
      "reason": "\u26a0 DO NOT DEPLOY THIS ENTRY YET \u2014 the CUL-1038 half is UNDER AN ADVERSARIAL FAIL (2026-09-17). The mandatory pass returned FAIL on the coverage freeze with five executed counterexamples, one of which is a NEW false-reassurance path the freeze introduces: a trial designed at 28 days, extended to 84, logged every one of days 1-28 and then silent, read on day 60, goes from '28 of 60 ... does not support interpreting this trial either way' to '28 of 28 ... supports interpreting it' plus 'All matched the trial diet or a permitted food' \u2014 a clean claim GRANTED where the pre-change code withheld it, over 32 un-logged days that sit INSIDE the window currently in force. Two more: the rendered overrun disclosure is literally false on a since-visit scope opening after the designed window end (no extension required), and ending the trial now moves the claim gate where pre-change it moved nothing. The repair for \u00a75.4 holds; the PR is not shippable as it stands and is awaiting a PM ruling on CUL-1038. The rest of this pending set (the render passes below) is unaffected and would be safe on its own \u2014 if this entry is deployed before CUL-1038 is resolved, REVERT the CUL-1038 commits from the bundle first. \u2014 RE-FINGERPRINTED 2026-09-17 \u2014 CUL-1038 (trial-window PR 1b, #TBD) JOINS the pending set; one deploy still carries all of it. This one is not a render pass: it is a CHANGE TO A SHIPPED CLINICAL GATE'S ARITHMETIC, and it is the only reason this entry is now worth deploying sooner rather than with the next batch. The coverage denominator is FROZEN at `diet_trials.target_duration_days_initial` (migration 068, applied to production 2026-09-17 and backfilled for every live row), so an owner extending an overrun trial can no longer move `belowCoverageFloor`, `mayStateRecordClean` or `interpretability` over a record that did not change (spec \u00a76 D7c, TE-6). EXECUTED on the rendered page, both report scopes: default 10-of-28 'too sparse to read that as a clean elimination' became 32-of-50 'all 32 matched the trial diet or a permitted food' on one tap; since-visit (rung 1, the scope a diet-trial owner actually gets) went 0-of-9 to 'all 22 matched'. Both now hold still. PULL CHANGE: the diet_trials select gains `target_duration_days_initial` \u2014 additive, nullable, no scope or filter change, and the column reads NULL-as-not-recorded with a fallback to the live target, so an unstamped row renders byte-identically to today. RENDER CHANGE: the coverage sentence and the scan-grid tile gain the overrun disclosure B-422's own clip comment has cited since it shipped and nothing rendered (C-38), and `interpretabilityStatement` says WHICH window it covered on an overrun trial. Nothing else in the pending set moved. \u2014 RE-FINGERPRINTED 2026-09-16 for FOUR passes that now ride ONE deploy, three of them already on main and this branch merged on top: R-17 (CUL-999) + R-18 (CUL-1000) the stylesheet pass; R-14 (CUL-291) the trend chart's stop marks; R-7 (CUL-981) the vomit-colour aggregate; and session S3 \u2014 R-3 (CUL-977), R-4 (CUL-978), R-13 (CUL-996)'s nine items and R-15 brief 7(b) (toward CUL-974). NONE changes a pull, a scope, a schema, or any response shape the client reads; all four are render plus pure assembly. STYLE: fourteen informational rules come up off --faint (3.32:1, under the 4.5:1 text floor) to --muted; both no-count chart marks come up off 1.66:1; seventeen type sizes become eight; prose is capped to a 96-character measure; the safety band becomes the only boxed element. R-14: the chart marks intervention STOPS as well as starts, behind one shared predicate. R-7: vomit colour is aggregated over the assessed reads, beside the caveat that needs it. S3: appendix A's LOGGED column gains a date across a day boundary, drops its per-row tag on an all-witnessed column, and states a COUNT of the in-window observations logged as `other` that the eight-leaf allow-list drops from every count and table \u2014 a number, never the notes; appendix B gains a derived Previous-diet row and a Food-used-to-give-medication row, and its supplements and conditions negatives are cross-checked against what the rest of the document already states; appendix C's incompleteness marker is category-specific for home food and its floor disclosure partitions packaged from home-prepared; every \u201citemised in appendix E\u201d promise is replaced by one derived description; a tie in the intake ratings itemises every rating instead of rendering a dash; the removed-photo disclosure names its incidents; an empty appendix D loses its table. PAGE COUNTS, re-measured on the MERGED tree at Letter AND A4 with the MediaBoxes checked distinct (612x792 vs 595x842), so the equal figures are a result and not an ignored @page rule. Merged: clean 10/10, refused 9/9, completed 8 Letter and 7 A4, truncated 8/8, past-window 8/8. Against the other three passes as the base, S3's ONLY page-count effect is clean at A4, 9 to 10 \u2014 at Letter the prose cap had already taken clean to 10, so the extra sheets overlap rather than stacking. That held unchanged across two separate base moves. STAYS PENDING \u2014 merging does not deploy this function; it rides the next Codespace deploy with the rest of the held chain, alongside the CUL-899 VV-1 soft-deleted-visit reader that landed inert, and the vet-report-cold-read re-runs on the regenerated artifact after it.",
      "ref": "CUL-291, CUL-975, CUL-976, CUL-977, CUL-978, CUL-979, CUL-981, CUL-993, CUL-994, CUL-996, CUL-998, CUL-999, CUL-1000",
      "updated": "2026-09-16"
    },
    "generate-signal": {
      "status": "pending",
      "fingerprint": "sha256:9b56985e2fecefc055923cd417f4f145a2483e165601206348676977314b6323",
      "reason": "RE-FINGERPRINTED 2026-09-17 \u2014 BUNDLE-ONLY DRIFT, NO BEHAVIOUR CHANGE, and the gate below is unchanged. CUL-1038 edited `lib/dietTrial.ts`, which this function inlines, so its closure hash moved. What CUL-1038 changed is `computeTrialFacts`'s coverage clip, `interpretabilityStatement` and the new `trialCoverageWindowEndDayIndex`; this function imports exactly ONE symbol from that module \u2014 `isTrialRunning` (index.ts:72, detection.ts:83) \u2014 which resolves through `trialEffectiveEndDayIndex` \u2192 `trialTargetEndDayIndex` on the LIVE `target_duration_days`, untouched by this PR. That is D7(c)'s split working as ruled: an extension still moves BELIEF (is this trial running, which detector suppression reads) and no longer moves a CLAIM ABOUT THE RECORD. Recorded rather than deployed because this entry's own gate has not changed. \u2014 MERGED 2026-09-03: this closure carries BOTH CUL-787 (#797, the counted 4-week compare inside the chronicity card) AND CUL-786 (#798, the labeled stand-down) \u2014 the two v1.1 engine items landed on main within an hour of each other and each re-fingerprinted this entry; one deploy carries both, and both gates apply (CUL-787's on its own terms below; CUL-786's client-before-engine gate is CUL-794). \u2014 [CUL-786] RE-FINGERPRINTED 2026-09-03 after the mandatory adversarial pass on CUL-786 (verdict FAIL, two breaks, both closed in this closure): (1) the carry-forward re-emitted a marker verbatim when the symptom RELAPSED inside the seven days but \u2466 stayed silent (the window slid old onsets out, residual count under the episode floor \u2014 the modal case for cough), so the line said 'No coughing logged in 28 days' on the day the owner logged one \u2014 the carry is now re-anchored to the record (any newer episode than the one it was minted against ends it); (2) the marker took rank 0 over a live safety card (the cat that stops vomiting because it stopped eating) \u2014 mergeStandDowns now splices below every safety finding, and the client binds the lead canvas to the first card. Also: the gap guard no longer counts the last episode's own day, and filters to the fetch union; the stand-down block is fenced so a throw costs the marker, never the regen (code review). Accepted residual, recorded on CUL-786: the gap guard detects abandonment, not attention \u2014 the copy ('logged', 'isn't an all-clear') carries the honesty. \u2014 PENDING 2026-09-03 (CUL-786) \u2014 the labeled stand-down (Signal fold v1.1-a). New module standDown.ts + a shell change in index.ts: before the cache row is replaced, the prior row is read (caller JWT, RLS-scoped) and a `stood_down` marker is minted for a chronicity course that stopped firing on its recency floor (decided by re-running detectChronicity under a counterfactual config with the recency gate open \u2014 no detection.ts change, so generate-report's closure is untouched) with logging held across the gap (Dr. Chen's 4th condition, the span-halves shape); the marker rides the findings array at the card's former rank, carries forward \u22647 days, and is dropped on a re-fire. No detector, floor, lane or ranking change; is_building / signal_text / the summary are computed over the real findings and are byte-identical. DEPLOY GATE (the client-before-engine rule, HR-2): the shipped client renders the unknown type as nothing, so on a marker-ONLY pet an older build shows a blank Signal card for up to 7 days \u2014 redeploy only once a build carrying this PR's SignalZone is the live build (behavioural check: the stand-down line renders on device), then set `deployed`. Previous live: v33, 2026-08-29. \u2014 [CUL-787] RE-FINGERPRINTED 2026-09-03 after the CUL-787 adversarial pass (FAIL \u2192 fixed, same PR): (1) the compare's logged-day denominator now counts the finding's OWN sign on top of the \u2462/\u2463 comparison-gate cell (loggingDaysInWindow gained an optional `alsoCounts` set; byte-identical for every pre-taxonomy type) \u2014 a cough card had printed 'the 4 before: 8 \u00b7 logged on 0 and 0 of those days' and minted a false comparable:true on a 5\u00d7 fall; (2) the prior half is anchored on the whole lookback so an odd windowDays still partitions episodeCount (latent, property-tested). Client-side in the same PR: the withheld density line is falling-only, and the phone-script row speaks each denominator. \u2014 PENDING 2026-09-03 (CUL-787, v1.1-b of the Signal fold track) \u2014 ADDITIVE payload only: a chronicity finding now carries `compare` (the counted 4-week halves of its own 56-day lookback: recentCount / priorCount partitioning episodeCount exactly, plus the logged days per half via the \u2462/\u2463 comparison-gate cell and the shared DENSITY_COMPARABLE_MIN_RATIO verdict), computed post-detection in index.ts's decorate map and attached in medContext.ts#decorateFinding. No detector, floor, lane, ranking or template change: detectChronicity's output is byte-identical (test-pinned), the \u2462 valve that mutes reflections while any symptom is chronic is untouched (test-pinned: an easing chronic course yields no reflection card), and templateChronicity still composes the same sentence \u2014 the client renders the compare in the safety card's EXPAND + phone script only. Owed: redeploy from main after merge (the client half ships in the same PR and is a no-op until this deploys \u2014 an old cache renders the pre-v1.1-b card), then set `deployed`. Still carries the CUL-778 drift below. \u2014 Prior entry retained verbatim: PENDING 2026-09-01 (CUL-778) \u2014 copy drift in phrasing.ts plus a one-line composition fix in detection.ts (discloseCoughVomitAdjacency now marks the leading cough-or-vomit card rather than the leading chronicity card of any sign, so a longer diarrhea course can no longer carry a note that names neither of its counts): the \u00a79 cough\u2194vomit adjacency clause on the chronicity card now carries its own premise ('Coughing is logged too \u2014 a cough can look like retching or end in vomiting. Mention both.', with the other sign named per leader) instead of the bare conclusion 'easily confused' the PM read on device and could not parse. No detector, floor, lane or payload change; the finding shape is unchanged, so the live v33 client renders the old sentence until this redeploys. Owed: redeploy from main after merge, then set `deployed` (previous live: v33, 2026-08-29). \u2014 Prior entry retained verbatim: DEPLOYED 2026-08-29 as version 33 (previous live: v32, 2026-08-21) \u2014 the client gate below is MET and this entry is kept, rather than trimmed to a one-liner, because it is the record of what that gate was and why it held. Verified after deploy: list_edge_functions shows v33 ACTIVE with verify_jwt true, and guards/edgeFunctionDeploy passes against main's closure, so the recorded fingerprint still describes what shipped. GATE CLEARED: a TestFlight build carrying #730's client mirrors is installed and the cough/sneeze types render on device (PM-confirmed 2026-08-29) \u2014 the behavioural check, not a version string, since APP_VERSION and the native APP_BUILD are both binary properties that do not move on an OTA (CUL-690). DOWNSTREAM, SAME DAY: this deploy was the gate on CUL-677's \u00a711 other-row swap, which then RAN \u2014 33 of the owner's 34 `other` rows re-keyed to cough/sneeze (one held: its note names both leaves and an UPDATE cannot split a row), notes/timestamps/confidence untouched, updated_at bumped on all 33 so the re-key propagates. Detector \u2466 now fires 'firm' on the cough course (20 episodes / 50-day span / 6 active weeks / 2 days since last), matching the pre-swap prediction exactly. STILL OPEN, not closed by this deploy: CUL-688 (with recency at 28, R4's span-first ordering can let a 27-days-quiet course LEAD one seen today) and CUL-686 (the density OR-arm for short dense courses, carried as a named defect). \u2014 What follows is the pre-deploy record, retained verbatim. W1-PR-3b session 2 (CUL-676) was the first BEHAVIOUR-CHANGING drift in this chain. What changed: `cough` joined the fetch union and the LANE_SYMPTOM_TYPES.chronicity cell only (\u2466 can now fire for cough, with per-type B-755 floors minEpisodes 4 / firmSpanDays 28); R3 \u2014 cough days now count in the logged-day/density denominators, and those denominators now ENFORCE `set == fetch union` via isFetchedSymptom rather than trusting callers; R4 \u2014 detectChronicity returns EVERY chronic course instead of `[chronic[0]]`, so a longer mild course can no longer delete a denser one (this also changes behaviour for the existing five types, deliberately); \u00a79's cough\u2194vomit adjacency note is attached to the leading card when both courses are chronic; summary.ts's month-summary clause now counts LOGS not 'episodes' (HR-7). GATE, VERIFIED NOT ASSUMED: the ruled precondition is a live build carrying PR-3a (#730). The installed TestFlight build is 1.1.0 (35), 2026-07-25 (docs/dev-handoff-runbook.md \u00a7 Current build state) \u2014 a month older than #730 and older than the enum migration itself (#728), so it does not even know the `cough` event type. Deploying now would put cough findings on a client with no Trend / History / widget / Day-Summary support for them. PR-3a's symptomWord() fallback keeps that from rendering the literal 'undefined', but it is defence-in-depth, not a licence to deploy ahead of the gate. TO CLEAR: cut an A-Native TestFlight build carrying #730 (the SDK-57 fence makes OTA a no-op), confirm it is the live build, then redeploy and set status `deployed`. RE-FINGERPRINTED after the mandatory adversarial pass (2026-08-28), which returned FAIL and whose accepted findings are now in this source: the \u00a79 adjacency clause was 382 chars against validatePhrasing's 320 cap (shortened, and it now names MISATTRIBUTION rather than overlap \u2014 the deflationary reading), and the per-type cough floor had no property gate at all because the \u00a77 #14 noise sweep only ever swept vomit (both sides of the trade are now pinned: 9.22% noise rate at minEpisodes 4, and the once-weekly/fortnightly courses that go silent at 5+). R3 RE-RULED AND CLOSED (PM delegated to the team, 2026-08-28). The ruling stands for COVERAGE \u2014 a cough day is a logged day \u2014 but the two consumers that are COMPARABILITY GATES rather than coverage measures now read the \u2462/\u2463 lane cell (countsTowardComparisonGate): loggingDaysInWindow (\u2462/\u2463 eligibility + SR-4) and trial_response's loggedDaysIn. \u2466's span-halves eligibility keeps full fetch coverage, because its failure direction is escalation, not reassurance \u2014 that is the rule the split encodes. Byte-identical to the pre-cough engine for every existing type (re-verified: the 6,000-case differential still shows 622 losses, all explained, 0 tier softenings), and both reproduced breaks are closed against the LIVE module: the SR-4 case is back to a withheld comparison, and the trial lane is silent before and after. Both client mirrors track the gate set, pinned by guards/loggedDayParity.test.ts in both directions. COUGH FLOORS NOW RULED (Dr. Chen lens, 2026-08-28, CUL-687 \u2014 verdict on the build's own set was NOT DEFENSIBLE AS-IS, three changes applied). Final: minEpisodes 5 with a cat override of 4 (dogs carry a common benign confounder for this key \u2014 reverse sneezing \u2014 that cats do not); ongoingRecencyDays 14 \u2192 28 for BOTH species (a fortnight of silence answers nothing about relapsing-remitting airway disease, and the old value withheld the firm card on a quiescent asthmatic cat while the \u2466\u2192\u2462 valve simultaneously unblanked the reflection layer \u2014 reassurance-by-absence); firmSpanDays 28 kept, 42 rejected for both. This also introduced the first SPECIES-CONDITIONED chronicity floor (chronicityFloorsFor now takes species, mirroring the shipped intake-decline precedent), so \u2466's fire predicate, tier and the \u2462-valve all resolve per-species. One ruled item deliberately NOT taken here: the density OR-arm for short dense courses is a new fire path on a safety detector rather than a calibration, so the ruling's own sanctioned fallback applies \u2014 carried as a named defect on CUL-686. The build gate (a live build carrying #730) is UNCHANGED and still open. Post-ruling verification pass (2026-08-28) returned FAIL on the PRICING, not the numbers: the species split held across a 150-cell exhaustive sweep and every resolver hazard held, but the Q4 honesty argument had been verified against the wrong surface. Fixed in this closure: the floor resolver is now an ALLOWLIST of the five floor keys (a denylist had left `windowDays` overridable at runtime despite two comments claiming it was impossible by construction), and the owner receipt's 'Ongoing since' row became 'First logged' because it sat directly above 'Most recent \u2014 N days ago'. One item routed rather than fixed: with recency at 28, R4's span-first ordering can let a 27-days-quiet course LEAD one seen today \u2014 an ordering call that was never put, now CUL-688.",
      "ref": "CUL-786, CUL-787",
      "updated": "2026-09-03"
    }
  }
}
