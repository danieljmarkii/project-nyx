{
  "_README": "Edge-Function deploy ledger (B-178 / CUL-135). One entry per deployable function. `fingerprint` = the sha256 of the function's shipping import-closure (index.ts + every local .ts it inlines) last acknowledged for deploy — computed by guards/edgeFunctionDeploy.test.ts, which FAILS THE BUILD when a function drifts from its recorded fingerprint without a reasoned acknowledgment. Statuses: `deployed` (fingerprint confirmed live), `pending` (a deploy is owed OR the live state is unverified — see the entry's `reason`), `hold` (deliberately not deployed — e.g. B-494). `pending`/`hold` require a `reason`. After deploying a function, set its `fingerprint` to the current value (the guard's failure message prints it) with status `deployed`. This ledger records intent; it does NOT prove a deploy happened — see docs/edge-deploy-runbook.md § Deploy ledger.",
  "_seed": "Seeded 2026-08-19 (CUL-135). Fingerprints are the current main closures at ledger creation. Live-vs-main was NOT verified per-function (the live artifact is a bundle, not a source closure, and this guard is token-free by design), so no function is seeded `deployed`: generate-report is the one confirmed `hold` (B-494); every other function is `pending` (baseline). Each converts to `deployed` on its next verified deploy-from-main, or via the live reconciliation follow-on (CUL-541).",
  "functions": {
    "analyze-stool": {
      "status": "pending",
      "fingerprint": "sha256:0ccdcdeaebfe137e8a7e0579966583dc61c71040312845e138c429de5a814e70",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: A8 transform_only redeploy, CUL-258 Anthropic fetch timeouts (via _shared/http.ts), and CUL-542 (#682, foreign-material fragments on the stool read).",
      "ref": "CUL-135",
      "updated": "2026-08-19"
    },
    "analyze-vomit": {
      "status": "pending",
      "fingerprint": "sha256:e0415ea99f90ed968db2f283fa837601f20090e642467f9ad1dc096765c6dacc",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: A8 transform_only redeploy, CUL-152/B-179 free-text `description` gate (#671, 'one post-merge deploy'), CUL-258 fetch timeouts, and CUL-240 (#681, foreign-material fragments on the vomit read).",
      "ref": "CUL-135",
      "updated": "2026-08-19"
    },
    "ask": {
      "status": "pending",
      "fingerprint": "sha256:92061b63b1f83a553d0ec4d862812f1ddd0b78ef9fa7a41bce9da533dc565a76",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: A8 (deploy AFTER analyze-vomit/analyze-stool per the ordering gate) and CUL-258 fetch timeouts. Re-fingerprinted 2026-08-28 for CUL-676 W1-PR-3b: ASK_SYMPTOM_TYPES gained cough + sneeze, mirroring lib/analytics.ts SYMPTOM_EVENT_TYPES (which gained them in PR-3a) — the G5 Timeline-parity this list exists for, since History and the Patterns grid already show those rows and an Ask count would otherwise disagree with the record the owner is reading. Deploy continues to ride the held CUL-557 ask-redeploy chain, never its own.",
      "ref": "CUL-135",
      "updated": "2026-08-28"
    },
    "delete-account": {
      "status": "hold",
      "fingerprint": "sha256:99ac95836a4ce9ae813959e933e6ba5112995e7e987233a827027c2b1b7e2ccf",
      "reason": "B-119 / CUL-215 re-auth HOLD (client-first deploy ordering): the function now fails closed (reauth_required 401) when a request body carries no password, and the CURRENT shipped client sends none. Deploying this BEFORE a client build carrying the password field is the live/minimum build would 401 every current-client deletion — an Apple 5.1.1(v) erasure regression. Deploy only AFTER that client build is live, then set `deployed`.",
      "ref": "CUL-215",
      "updated": "2026-08-20"
    },
    "extract-food-from-photo": {
      "status": "pending",
      "fingerprint": "sha256:772c6f590f8a2c6de1dff8f2f16739da7fa654d87e9c5782b746dc4286b90854",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: CUL-258 Anthropic fetch timeouts (via _shared/http.ts).",
      "ref": "CUL-135",
      "updated": "2026-08-19"
    },
    "extract-medication-from-photo": {
      "status": "pending",
      "fingerprint": "sha256:b91616f9c0e685a6f40a0208a57cb353ce2691dc54afeac05a45dbb956170ddc",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: CUL-258 Anthropic fetch timeouts (via _shared/http.ts).",
      "ref": "CUL-135",
      "updated": "2026-08-19"
    },
    "generate-report": {
      "status": "hold",
      "fingerprint": "sha256:137746af9effc9de124475391dcf16cc5ea228aca16ee0319d1c703208fef6b0",
      "reason": "B-494 HOLD UNCHANGED — CUL-19 still gates this function; nothing in W1-PR-3b session 2 relaxes it, and this re-acknowledgment must not be read as progress toward clearing it. Fingerprint re-bumped for CUL-676's report co-work (§10.5 / HR-3 — a lane-membership change IS report work): `cough` + `sneeze` joined REPORT_SYMPTOM_TYPES (cough HAD to, because buildDetectionInput filters on CORRELATION_SYMPTOM_TYPES, so the report now runs ⑦ over cough and would otherwise print a chronicity flag about a sign its own §3.5 table never counts — the B-494 class); render.ts gained proper 'Coughing'/'Sneezing' labels; the report's `chronicity` extract became a LIST so R4's second course is not silently discarded; HR-7 was applied — §3.5 is relabelled 'entries' (it counts minute-deduped rows) while the chronicity flag keeps 'episodes' (3h-chained), with a new legend entry explaining that the two differ on purpose. It also inlines generate-signal/detection.ts, so every engine change above rides this fingerprint. Prior hold notes (CUL-564 §3.8 timing render, CUL-372 episode-collapse re-base) remain in force. Do NOT set `deployed` until B-494 clears. RE-FINGERPRINTED after the adversarial pass (2026-08-28): two further HR-7 residuals fixed (the page-1 tile said 'Episodes' over the trendHalves ENTRIES count, disagreeing with the trend panel's own noun on the same page — the B-532 divergence class; and the days-since tile's noun), plus a genuine unit bug the pass found — `episodeSetMatches` compared minute-deduped ROWS against the engine's 3h-CHAINED episode count, so for every cough flag the local-day reconciliation of symptomDays/daysSinceLastEpisode silently disabled itself and fell back to UTC numbers beside a local-day tile, reinstating the ±1-day disagreement the PR-7 cold read caught on the lead safety line. It now chains the report's rows with the shared lib/symptomEpisodes predicate and compares like for like. B-494/CUL-19 hold UNCHANGED. Re-fingerprinted again for the R3 re-ruling (it inlines detection.ts, so the gate split rides this closure). B-494/CUL-19 hold UNCHANGED. Also carries the CUL-687 verification fix: the chronicity flag's BOLD LEAD said 'has been ongoing 28 days' in the same sentence as 'most recent 27 days ago' — reachable once cough's recency floor widened, and a self-contradiction on the line the legend teaches a vet to scan first. Now 'spans N days'; recency keeps its own clause. B-494/CUL-19 hold UNCHANGED. RE-FINGERPRINTED for CUL-69/B-700 — the chronicity flag's onset date. It printed `firstOnsetIso` as \"(first logged {date})\", but that field is bounded by the DETECTOR's own lookback (a single global 56d for every symptom type — `chronicity.perType` overrides the FLOORS only, never the lookback), while the report's default window is the 90-day fallback — buildDetectionInput's contract states the containment (\"90-day fallback ⊃ 56d chronicity window\") and that containment IS the defect: for any course predating the lookback the onset lands ~34 days inside the window, so the lead safety line dated a cat vomiting since May 20 as \"first logged Jun 13\" while appendix A on the same report printed May 20. Three weeks short, contradicting the report's own log, on the axis a vet reads a chronicity flag FOR — and understating duration is the reassuring direction, which is the one direction this band may not fail in (B-494). B-532's left-censor did not catch it: it tested the onset against the REPORT window edge, the boundary that is not binding here. The flag now carries `firstLoggedIso` (the earliest in-window entry of that symptom, the same rows appendix A prints) beside `firstOnsetIso`, the exposureRange-vs-range split; the rendered date and span anchor to it, the censor floor tests it, and the engine's counts are UNCHANGED but now disclose their own window rather than being re-derived (the C5 disclosure precedent — re-deriving would fork the report off the one statistical source and make this flag disagree with the Signal card for the same pet). RE-FINGERPRINTED after the adversarial pass returned FAIL on the first draft (2026-08-30). The draft also EXTENDED the span by the record preceding the lookback, so the two numbers would agree — and that re-opened §10 #4, the \"two distant data points\" break the engine closes with `loggingEligible` over both halves of the span, `countDistributionWeeks`' B-188 anti-barbell packing, and the minSpan/minEpisodes conjunction. One stale vomit ten months before a real six-week course printed \"spans 335 days · 7 episodes\": the duration ran toward alarm and was false, while the DENSITY a vet triages on ran toward reassurance — one episode every seven weeks over a record holding weekly vomiting, which is the B-494 direction. Cough was the worst case, its 28-day recency floor letting a long-quiet course fire and maximising the gap, and the left-censor then restated the manufactured number as an affirmative clinical minimum (\"348 days is a floor\"). The generalisable rule, now written into render.ts: A RECORD-ANCHORED DATE IS A FACT AND FREE TO STATE; A DURATION IS AN INFERENCE THE ENGINE GUARDS, and any layer that widens it inherits those guards or must not widen it. So the span stays `f.spanDays`, the date comes from `firstLoggedIso`, and where the record reaches back past the counts the two are stated in separate sentences (the \"spans N days (first logged X)\" parenthetical drops, since glued to the span it asserts a duration nobody measured). Two further pass findings fixed with it: the disclosure now names the DATE the counts begin at rather than the lookback LENGTH (which runs back from the window end, so \"the most recent 56 days of that span\" could exceed a 43-day span and read as \"nothing is missing\" — the inverse of the warning), and its gate is INSTANT-granular rather than day-granular (the lookback cuts at an instant, so three morning episodes sat uncounted and undisclosed behind a same-local-day comparison). What the pass confirmed HELD: span-shrink is impossible (clamped, and the anchor ordering is structural), the new censor predicate is implied by the old one so it can only fire more often, the `min` is inert by construction across dedup/3h-chaining/custom windows (0/201 fuzzed flags), the mixed-unit arithmetic does not compound (±1 day, identical to main), and flag-vs-appendix-A disagreement falls from 60 days to 1 across a 219-flag fuzz over seven timezones. RE-FINGERPRINTED AGAIN after a SECOND adversarial pass on the corrected version (2026-08-30), which confirmed the span/date split holds (351 fuzzed flags across 7 timezones: the rendered span equals `f.spanDays` every time, and the flag's date equals appendix A's earliest row of that type every time — 0 days of disagreement, against up to 60 on main) and then broke three more things in the two SENTENCES AROUND it. (1) The left-censor stated the ENGINE span as the clinical floor in a paragraph that had just cited an earlier record anchor, understating the floor by a median 32 days over dense chronic records (max 62) — the reassuring direction, and newly reachable because the censor was effectively dead on the old anchor. Fixed by splitting on which boundary actually binds: the two cases are disjoint by construction (`hasUncountedRecord` entails window > lookback), so where only the window truncates, B-532's sentence is unchanged and correct; where BOTH truncate, no single number is defensible — `f.spanDays` understates and the record's own extent is two logged rows rather than a measured course (that direction is the first pass's finding 1 arriving through the censor) — so the window fact is stated with no duration attached. (2) The instant-granular gate from pass 1 could print one date twice (\"was first logged May 7; these counts begin at May 7\") on ~5% of boundary-edge cough flags, because the gate is an instant while both rendered dates are local days; the same-local-day case now reads \"these counts begin later that day\". (3) The disclosure introduced a second analysis window with no legend entry, against the HR-7 precedent, leaving the reassuring inference (that the engine judged the earlier entries unrelated) available — there is now a gated \"Where the chronicity counts begin\" entry that closes it in as many words, gated like the incident-photos entry so it never explains a line the report does not carry (B-599). The tail copy is also number-agnostic, since this layer does not hold the count it was pluralising over. Both defect-pinning tests were rewritten: the censor test had asserted the floor may never be tighter than the span, and the instant-granular test's own fixture WAS the same-date sentence while asserting only that the disclosure fired. Every guard mutation-proved (CUL-613). RE-FINGERPRINTED a THIRD time after a third adversarial pass (2026-08-30), which re-confirmed the derivation over a 260-flag fuzz (8 timezones x 4 scope bases: the span is always the engine's, the date always equals appendix A's earliest row of that type, no counted row falls in the disclosed gap, the legend gate matches the flag state, and all five prior mutations stay caught) and broke the FORMATTING of the newly-printed record anchor. The important one: the report renders days as \"Mon D\" with no year, which was unambiguous BY CONSTRUCTION while the only date on this line was the detector's onset — structurally within the lookback of the window end. Repointing it at the record anchor removed that construction, because `since_visit` (no clamp in resolveScope) and a stale-active `diet_trial` (the B-594 steady state) give unbounded windows: a 2024-03-10 first log beside a 2026-05-10 count start rendered as \"first logged Mar 10; these counts begin at May 10\", a 27-month sign read as a two-month one, and on a plain 90-day fallback 11% of generation days rendered a REVERSED pair (\"first logged Nov 23; these counts begin at Jan 1\"). On one real record the fix read 26 days MORE reassuring than the bug it replaced. Both dates in that sentence now carry the year whenever it is not the window's, so an ordinary same-year report is unchanged and a printed year always carries signal. Also fixed: the censor's new \"the record's first entry sits at that edge\" asserted an exactness CHRONICITY_LEFT_CENSOR_DAYS (a 7-day tolerance) does not support and contradicted the date in the preceding sentence on 29/156 disclosed flags — it is now B-532's original clause minus the indefensible number, and nothing else; the legend said the flag \"names the date its counts begin\" while the same-local-day branch names none; the same-day tail pluralised over a count this layer does not hold; and the load-bearing §9 cough-vomit caveat now precedes the two window-mechanics sentences rather than trailing them (Principle 6, the row grew). One documented invariant was also FALSIFIED and corrected rather than left standing: \"hasUncountedRecord entails window > lookback\" is false at exactly windowDays === 56 (up to 14h at UTC+14, because the window opens at local midnight while detectionNowFor stamps a past-ending custom window at UTC end-of-day) — no render harm today, but it was load-bearing prose for the next editor of detectionNowFor / CHRONICITY_LEFT_CENSOR_DAYS / windowDays. A new test also asserts LANE_SYMPTOM_TYPES.chronicity subset REPORT_SYMPTOM_TYPES, since the flag's copy now promises a vet the earlier entries are in appendix A and that subset was only prose in the §13a walk. RE-FINGERPRINTED a FOURTH time (2026-08-30). The fourth adversarial pass re-confirmed the derivation at 4000/4000 fuzzed flags and broke the pass-3 date fix: a CONDITIONAL year is worse than none when two dates share a sentence. `firstLoggedIso <= firstOnsetIso` always, so the stamped date is structurally always the FIRST of the pair, and in ordinary English a bare date following a year-stamped one INHERITS that year — \"first logged Nov 23, 2025; these counts begin at May 8\" reads as May 8 2025, six months BEFORE the anchor, which is the reversed pair the year was added to remove, now asserted with more confidence (236/236 of such pairs on a plain 90-day fallback; a 817-day uncounted gap collapsing to 87 on a long window). The year is now decided ONCE for the whole flag row — present whenever the record anchor is in play, absent otherwise, with the censor's window-open date on the same switch so it is not left as the single bare date on an otherwise fully year-stamped page. When it is absent the row is byte-identical to what B-532 and CUL-687 shipped. Also fixed: the disclosure tail \"anything logged before then is in appendix A\" was an unrestricted universal the report cannot honour (appendix A holds in-window rows only, and the default cascade carries no out-of-window disclosure at all), false in the \"you are seeing the whole history\" direction and a flat contradiction of the censor sentence wherever both fire — reachable at a ~57-64 day window, i.e. a vet visit eight weeks ago. Both branches now share one tail that says what appendix A HOLDS rather than where every earlier entry is. And the cross-year guard was rewritten: it had compared indexOf() offsets of two clauses in one sentence, so it asserted word order rather than date order and shipped an instance of the defect as its expected output — it now parses both rendered dates back out and compares them as dates. HELD unchanged under this pass: the windowDays===56 comment (verified accurate in both bound and mechanism by driving resolveScope over lengths 40-80 x 8 timezones), the sentence reorder at every seam, the REPORT_SYMPTOM_TYPES subset test, and the legend copy. FINAL re-fingerprint (2026-08-30). The fifth adversarial pass returned PASS — the review pattern converged, and it could not produce a false, reversed or reassuring chronicity sentence on any record the app can generate. It verified the three closures from passes 2-4 by sweep or mutation rather than by inspection: the censor/tail contradiction is gone across every window length 21-120; the !withYear path is byte-identical to pre-CUL-69 over 120 fixtures x 5 timezones (modulo this branch's own adjacency reorder); day() and fmtDayYear agree on separator, month abbreviation and padding across 12 months x 8 zones; and the rewritten cross-year guard goes red on all five mutations including a deleted sentence, so it cannot pass vacuously. Its four LOW residuals: three are now closed and one is left alone as pre-existing. Closed: (a) the year was decided PER DATE rather than per row, so a key Intl does not zero-pad could split the very pair the switch exists to keep together — unreachable from a timestamptz, but the comment claimed an invariant the code did not enforce, which is the shape that cost this arm four rounds; (b) the switch was ROW-scoped, so a band carrying two chronic courses (one reaching back past its counts, one not) printed the same window-open date two ways two lines apart on a ~57-64 day window — the B-532/HR-7 one-page-two-renders class — and it is now BAND-scoped through `chronicityRecordPrecedesCounts`, ONE predicate now also read by appendix F's legend gate instead of a second copy of the same test (§5.3); (c) the disclosure tail ended on \"including\" where the fact is exclusion, so it now closes on \"they are not in the numbers above\" — B-494's ruling is that the band must stand without the legend. Left alone as PRE-EXISTING and out of this PR's scope: a censored row with no uncounted record can print two bare New-Year-straddling dates on windows <=61d (1.27% of such rows, unchanged since 340ec41) — filed rather than widened. B-494/CUL-19 hold UNCHANGED; this re-acknowledgment is not progress toward clearing it. RE-FINGERPRINTED for CUL-62/B-613 — the out-of-window symptom guard. B-494/CUL-19 HOLD UNCHANGED; this re-acknowledgment is not progress toward clearing it. Two halves, both from vet-report-cold-read rounds 14+15 (top non-blocking item both times). (1) The §6 cherry-pick guard named a count and a date and never the SIGN — the type is read one line above the counter and was discarded — so on a completed elimination whose window closes eleven days early the page could not distinguish 'the trial held to the end' from 'she relapsed in the final week'. (2) The guard was gated on isCustomOverride, i.e. it answered only an owner's question about an owner's choice, while since_visit truncates a long trial BY CONSTRUCTION (it is the second report of every trial, sent at or after a recheck) — there the trial block said '42 trial days fall before it, outside this report's window' and nothing said what was logged in them. That is B-494's own rule one section down: a block that advertises a crop and leaves it unspecified has the shape of a disclosure with the disclosure taken out. New ScopeInfo.trialCropSymptoms counts symptom events inside the TRIAL's elapsed span and outside the window, on any basis, bounded by the trial rather than by all history (outside the trial is outside the block's subject). PRESENT-ONLY by rule, not by default: a zero renders NOTHING, because the cropped days are typically days the owner was not yet logging and 'no symptom events were logged' there converts 'we hold no record' into 'nothing happened' on the span the report has just admitted it cannot see (the diet-trial G2 rule and B-494 in one move). trial.ts now publishes the trial's own elapsed span (elapsedStart/EndDayIndex) from where ctx.startDayIndex lives rather than letting a consumer rebuild it as scope.startDayNum - before (valid only while before > 0 — the seam mistake rounds 2/3/4 paid for three times); it is deliberately NEITHER an evidence nor a coverage bound and no count may be bounded by it. computeLookbackIso gains a third term stretching the event pull to the report trial's start, capped at 400d before the window start, calling selectReportTrial (the one predicate, not a second copy — a divergent copy once anchored a window on an abandoned trial while the block described an active one); ReportInput.eventsSinceIso tells assembly how far the pull reached, and short OR UNKNOWN renders 'at least N' (CUL-708's shape: silence about the floor is not evidence of completeness). Out-of-window dates now carry a year when it is not the window's (CUL-69) — this date is out-of-window by definition and bounded only by a pull that can now reach 400 days back; safe as a conditional stamp only because each clause holds exactly ONE date. RE-FINGERPRINTED AGAIN after the mandatory cold read on the rendered artifact, which ranked the new disclosure BLOCKING for a missing DENOMINATOR — and was right for a sharper reason than an omission: the report's own legend promises 'a count is never read without knowing how long and how completely it was tracked', so a count without one was a contradiction, and being told the rule and then denied it stops a reader looking for the qualifier. The clause now carries the mix and the density ('...and 6 symptom events were logged in those days (5 vomiting, 1 loose stool; most recent: loose stool, May 30). Meals were logged on 42 of those 42 days.'). MEAL-logged days, not any-logged days, on the C5 precedent — 'days with any log' on a real record IS largely the symptom series, so it would circle back on the count it qualifies and read 'well tracked' exactly when symptoms are dense; and the density is WITHHELD when the count is a floor, where both numbers are short over an unknown span and a ratio of two partial numbers is a precision nobody can act on. Also deleted a redundant intersection that read as a safety net: inWindow is day-granular over the same eventDayNumber the loop computes, so the meal-day set provably holds only cropped days — dead code that looks live tells the next reader there is a case it handles, so the boundary it appeared to cover is pinned by test instead. 17 mutations run (CUL-613); THREE did not discriminate and were fixed: the type guard set the field on a snapshot fixture and was blind to assembly dropping it, the pull-widening differential passed vacuously for the weight and dose lanes until the fixture carried weigh-ins and doses, and the tally-order guard's fixture happened to insert in sorted order. That second one established that medicationHistory IS pull-sensitive and is insulated in production only by the separate untrimmed lifetimeDoses array — now asserted rather than assumed, over the whole snapshot, so a new pull-sensitive field fails there instead of riding along. Out-of-scope findings from the same cold read filed rather than folded in: CUL-746, CUL-747, CUL-748. RE-FINGERPRINTED AGAIN (2026-08-30) after BOTH mandated reviews returned blocking findings on the round-16 additions — the core change held under every attack and the material added to satisfy the previous review is what broke, which is the CUL-69 pattern arriving again. (a) The adversarial pass FAILED it on the patient this report exists for: a cat whose 42 cropped trial days hold 42 REFUSALS of the prescribed diet, 42 off-diet feedings and 5 vomits with safetyFlags empty — all of the refusals and feedings invisible on the page, since the block's exposure counts and appendix C are window-scoped and the refusal lane's evidence window is the report's. 'Meals were logged on 42 of those 42 days' beside a symptoms-only enumeration, under a legend saying the section 'names what was logged', told the reader the hole was FILLED; before the change that record admitted a hole, after it it reassured. THE RULE, now in CLAUDE.md: a completeness ratio next to a partial enumeration is a claim about the enumeration, whatever it is a ratio of. The sentence now renders only the UN-logged days ('No meal is logged on 34 of those 42 days.') and nothing when the crop is fully covered, so no completeness ratio can appear and it can only move a reader toward doubt; the legend names its scope outright instead of generalising over it. (b) BOTH reviewers independently broke the same second thing: the crop counted any type==='meal' while computeTrialFacts' coverage excludes treats — lib/dietTrial.ts states in place that 'on live data 82% of feedings are treats, so a days-with-food-logged count is clearable entirely by treat data' — so a cropped head of nothing but dental chews rendered as fully tracked, in the same words, twelve words from a coverage line built the other way, on the larger half of the trial. The predicate now filters treats (§5.3). (c) Per Dr. Chen's follow-up the coverage fact is UNGATED from the symptom count and from countIsFloor: it is a fact about the RECORD rather than the patient, gating it rendered '42 days nobody logged' and '42 days logged and genuinely quiet' identically as nothing, and suppressing a coverage fact is the reassuring direction; a short pull can only overstate un-logged days, which is the safe side. (d) One seam the fixes themselves opened, caught re-reading the diff: the density sentence stopped being gated on the symptom count while the LEGEND gate still keyed on it, so a crop with zero symptoms and 34 dark days carried a line nothing explained — B-599 in both directions, now one shared predicate (trialCropDisclosureRenders). 17 mutations, all red. Residuals NOT taken, filed instead: CUL-749 (the cropped stretch's refusals and off-diet feedings are invisible everywhere — the report is now honest about the gap and still does not fill it; wants a Dr. Chen ruling), plus CUL-746/747/748 from the same round. Also carried as known and unclosed: the recency anchor ('most recent May 24') does not scale with the cropped stretch's coverage the way the count now does, and byType is uncapped. B-494/CUL-19 hold UNCHANGED; this re-acknowledgment is not progress toward clearing it.",
      "ref": "CUL-62",
      "updated": "2026-08-30"
    },
    "generate-signal": {
      "status": "deployed",
      "fingerprint": "sha256:82a0baa2e84e86a931c224c434f8230508c9f6a623a79461f78c347161aa5883",
      "reason": "DEPLOYED 2026-08-29 as version 33 (previous live: v32, 2026-08-21) — the client gate below is MET and this entry is kept, rather than trimmed to a one-liner, because it is the record of what that gate was and why it held. Verified after deploy: list_edge_functions shows v33 ACTIVE with verify_jwt true, and guards/edgeFunctionDeploy passes against main's closure, so the recorded fingerprint still describes what shipped. GATE CLEARED: a TestFlight build carrying #730's client mirrors is installed and the cough/sneeze types render on device (PM-confirmed 2026-08-29) — the behavioural check, not a version string, since APP_VERSION and the native APP_BUILD are both binary properties that do not move on an OTA (CUL-690). DOWNSTREAM, SAME DAY: this deploy was the gate on CUL-677's §11 other-row swap, which then RAN — 33 of the owner's 34 `other` rows re-keyed to cough/sneeze (one held: its note names both leaves and an UPDATE cannot split a row), notes/timestamps/confidence untouched, updated_at bumped on all 33 so the re-key propagates. Detector ⑦ now fires 'firm' on the cough course (20 episodes / 50-day span / 6 active weeks / 2 days since last), matching the pre-swap prediction exactly. STILL OPEN, not closed by this deploy: CUL-688 (with recency at 28, R4's span-first ordering can let a 27-days-quiet course LEAD one seen today) and CUL-686 (the density OR-arm for short dense courses, carried as a named defect). — What follows is the pre-deploy record, retained verbatim. W1-PR-3b session 2 (CUL-676) was the first BEHAVIOUR-CHANGING drift in this chain. What changed: `cough` joined the fetch union and the LANE_SYMPTOM_TYPES.chronicity cell only (⑦ can now fire for cough, with per-type B-755 floors minEpisodes 4 / firmSpanDays 28); R3 — cough days now count in the logged-day/density denominators, and those denominators now ENFORCE `set == fetch union` via isFetchedSymptom rather than trusting callers; R4 — detectChronicity returns EVERY chronic course instead of `[chronic[0]]`, so a longer mild course can no longer delete a denser one (this also changes behaviour for the existing five types, deliberately); §9's cough↔vomit adjacency note is attached to the leading card when both courses are chronic; summary.ts's month-summary clause now counts LOGS not 'episodes' (HR-7). GATE, VERIFIED NOT ASSUMED: the ruled precondition is a live build carrying PR-3a (#730). The installed TestFlight build is 1.1.0 (35), 2026-07-25 (docs/dev-handoff-runbook.md § Current build state) — a month older than #730 and older than the enum migration itself (#728), so it does not even know the `cough` event type. Deploying now would put cough findings on a client with no Trend / History / widget / Day-Summary support for them. PR-3a's symptomWord() fallback keeps that from rendering the literal 'undefined', but it is defence-in-depth, not a licence to deploy ahead of the gate. TO CLEAR: cut an A-Native TestFlight build carrying #730 (the SDK-57 fence makes OTA a no-op), confirm it is the live build, then redeploy and set status `deployed`. RE-FINGERPRINTED after the mandatory adversarial pass (2026-08-28), which returned FAIL and whose accepted findings are now in this source: the §9 adjacency clause was 382 chars against validatePhrasing's 320 cap (shortened, and it now names MISATTRIBUTION rather than overlap — the deflationary reading), and the per-type cough floor had no property gate at all because the §7 #14 noise sweep only ever swept vomit (both sides of the trade are now pinned: 9.22% noise rate at minEpisodes 4, and the once-weekly/fortnightly courses that go silent at 5+). R3 RE-RULED AND CLOSED (PM delegated to the team, 2026-08-28). The ruling stands for COVERAGE — a cough day is a logged day — but the two consumers that are COMPARABILITY GATES rather than coverage measures now read the ③/④ lane cell (countsTowardComparisonGate): loggingDaysInWindow (③/④ eligibility + SR-4) and trial_response's loggedDaysIn. ⑦'s span-halves eligibility keeps full fetch coverage, because its failure direction is escalation, not reassurance — that is the rule the split encodes. Byte-identical to the pre-cough engine for every existing type (re-verified: the 6,000-case differential still shows 622 losses, all explained, 0 tier softenings), and both reproduced breaks are closed against the LIVE module: the SR-4 case is back to a withheld comparison, and the trial lane is silent before and after. Both client mirrors track the gate set, pinned by guards/loggedDayParity.test.ts in both directions. COUGH FLOORS NOW RULED (Dr. Chen lens, 2026-08-28, CUL-687 — verdict on the build's own set was NOT DEFENSIBLE AS-IS, three changes applied). Final: minEpisodes 5 with a cat override of 4 (dogs carry a common benign confounder for this key — reverse sneezing — that cats do not); ongoingRecencyDays 14 → 28 for BOTH species (a fortnight of silence answers nothing about relapsing-remitting airway disease, and the old value withheld the firm card on a quiescent asthmatic cat while the ⑦→③ valve simultaneously unblanked the reflection layer — reassurance-by-absence); firmSpanDays 28 kept, 42 rejected for both. This also introduced the first SPECIES-CONDITIONED chronicity floor (chronicityFloorsFor now takes species, mirroring the shipped intake-decline precedent), so ⑦'s fire predicate, tier and the ③-valve all resolve per-species. One ruled item deliberately NOT taken here: the density OR-arm for short dense courses is a new fire path on a safety detector rather than a calibration, so the ruling's own sanctioned fallback applies — carried as a named defect on CUL-686. The build gate (a live build carrying #730) is UNCHANGED and still open. Post-ruling verification pass (2026-08-28) returned FAIL on the PRICING, not the numbers: the species split held across a 150-cell exhaustive sweep and every resolver hazard held, but the Q4 honesty argument had been verified against the wrong surface. Fixed in this closure: the floor resolver is now an ALLOWLIST of the five floor keys (a denylist had left `windowDays` overridable at runtime despite two comments claiming it was impossible by construction), and the owner receipt's 'Ongoing since' row became 'First logged' because it sat directly above 'Most recent — N days ago'. One item routed rather than fixed: with recency at 28, R4's span-first ordering can let a 27-days-quiet course LEAD one seen today — an ordering call that was never put, now CUL-688.",
      "ref": "CUL-676",
      "updated": "2026-08-29"
    }
  }
}
