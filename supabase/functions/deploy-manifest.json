{
  "_README": "Edge-Function deploy ledger (B-178 / CUL-135). One entry per deployable function. `fingerprint` = the sha256 of the function's shipping import-closure (index.ts + every local .ts it inlines) last acknowledged for deploy — computed by guards/edgeFunctionDeploy.test.ts, which FAILS THE BUILD when a function drifts from its recorded fingerprint without a reasoned acknowledgment. Statuses: `deployed` (fingerprint confirmed live), `pending` (a deploy is owed OR the live state is unverified — see the entry's `reason`), `hold` (deliberately not deployed — e.g. B-494). `pending`/`hold` require a `reason`. After deploying a function, set its `fingerprint` to the current value (the guard's failure message prints it) with status `deployed`. This ledger records intent; it does NOT prove a deploy happened — see docs/edge-deploy-runbook.md § Deploy ledger.",
  "_seed": "Seeded 2026-08-19 (CUL-135). Fingerprints are the current main closures at ledger creation. Live-vs-main was NOT verified per-function (the live artifact is a bundle, not a source closure, and this guard is token-free by design), so no function is seeded `deployed`: generate-report is the one confirmed `hold` (B-494); every other function is `pending` (baseline). Each converts to `deployed` on its next verified deploy-from-main, or via the live reconciliation follow-on (CUL-541).",
  "functions": {
    "analyze-stool": {
      "status": "pending",
      "fingerprint": "sha256:0ccdcdeaebfe137e8a7e0579966583dc61c71040312845e138c429de5a814e70",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: A8 transform_only redeploy, CUL-258 Anthropic fetch timeouts (via _shared/http.ts), and CUL-542 (#682, foreign-material fragments on the stool read).",
      "ref": "CUL-135",
      "updated": "2026-08-19"
    },
    "analyze-vomit": {
      "status": "pending",
      "fingerprint": "sha256:e0415ea99f90ed968db2f283fa837601f20090e642467f9ad1dc096765c6dacc",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: A8 transform_only redeploy, CUL-152/B-179 free-text `description` gate (#671, 'one post-merge deploy'), CUL-258 fetch timeouts, and CUL-240 (#681, foreign-material fragments on the vomit read).",
      "ref": "CUL-135",
      "updated": "2026-08-19"
    },
    "ask": {
      "status": "pending",
      "fingerprint": "sha256:6b808227a6906e76d54acb3d441e1f9552185f21e29f8ecb7cd8105fe97c6795",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: A8 (deploy AFTER analyze-vomit/analyze-stool per the ordering gate) and CUL-258 fetch timeouts.",
      "ref": "CUL-135",
      "updated": "2026-08-19"
    },
    "delete-account": {
      "status": "hold",
      "fingerprint": "sha256:99ac95836a4ce9ae813959e933e6ba5112995e7e987233a827027c2b1b7e2ccf",
      "reason": "B-119 / CUL-215 re-auth HOLD (client-first deploy ordering): the function now fails closed (reauth_required 401) when a request body carries no password, and the CURRENT shipped client sends none. Deploying this BEFORE a client build carrying the password field is the live/minimum build would 401 every current-client deletion — an Apple 5.1.1(v) erasure regression. Deploy only AFTER that client build is live, then set `deployed`.",
      "ref": "CUL-215",
      "updated": "2026-08-20"
    },
    "extract-food-from-photo": {
      "status": "pending",
      "fingerprint": "sha256:772c6f590f8a2c6de1dff8f2f16739da7fa654d87e9c5782b746dc4286b90854",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: CUL-258 Anthropic fetch timeouts (via _shared/http.ts).",
      "ref": "CUL-135",
      "updated": "2026-08-19"
    },
    "extract-medication-from-photo": {
      "status": "pending",
      "fingerprint": "sha256:b91616f9c0e685a6f40a0208a57cb353ce2691dc54afeac05a45dbb956170ddc",
      "reason": "Baseline at ledger creation (CUL-135); live-vs-main not verified. Known undeployed work: CUL-258 Anthropic fetch timeouts (via _shared/http.ts).",
      "ref": "CUL-135",
      "updated": "2026-08-19"
    },
    "generate-report": {
      "status": "hold",
      "fingerprint": "sha256:43f2d056869db06eec8d0e1052a3eedbc7d23f867e1ae4c74f474c7809d8494d",
      "reason": "B-494 HOLD: the report deliberately runs the pre-PR-7 off-diet heuristic until the refusal safety lane ships; the generate-report redeploy is gated on B-494 (see STATUS.md / Open Questions). Do NOT set `deployed` until B-494 clears. Carries CUL-564's REAL behavior change (the report's §3.8 timing line renders timing_story + empty_stomach_timing and drops L2 trial_response / L4 gap_shortening). Fingerprint re-bumped for CUL-372: report.ts inlines generate-signal/detection.ts, whose episode collapse was re-based onto lib/symptomEpisodes (a thin adapter over lib/mealTiming's collapseEpisodes). BEHAVIOR-NEUTRAL, verified by a COMMITTED, re-runnable test — lib/symptomEpisodes.differential.test.ts fuzzes 40,000 inputs against the pre-refactor toEpisodeOnsets/toConfidenceEpisodes bodies transcribed verbatim (asserting >5,000 of them carry duplicate instants and >5,000 split into multiple episodes, so the fuzz is evidence rather than decoration), plus the overnight-chained-bout case. The one divergence is non-finite instants, which no caller can produce (all 7 engine call sites pre-filter Number.isFinite) and which the test pins explicitly. 1369 deno tests green. Adds nothing to what this hold already owes. Also unified the episode-gap constant: SYMPTOM_EPISODE_GAP_HOURS now resolves to DEFAULT_MEAL_TIMING_CONFIG.episodeGapHours (both were an independent literal 3), so the collapse and photoComposition's collapseComposition cannot drift apart. Same value, no behavior change. Fingerprint re-bumped for CUL-319 (B-612): render.ts only, and all three edits are Appendix A rendering fixes for cold-read blockers found on the first artifact ever to exercise these branches — a window bound that falls on a different local day than its row now carries that day (an overnight \"~23:00–07:20\" read backwards and cost the bilious-vomiting timing), a Logged time on a later local day carries its date (a found-next-morning row stated a log 15h before the event's earliest possible time), and the duplicate tag left the `.conf` time-confidence chip style for its own self-glossing one (\"2 logs · same incident, counted once\") so it stops reading as a fifth confidence value or a second episode. No detection, correlation, threshold or count logic touched: the same events, counts and denominators render, with two of them no longer ambiguous and one no longer miscategorised. 441 generate-report cases green, 5 of them new regression tests covering both directions of each rule. Adds nothing to what this hold already owes. Re-bumped again within CUL-319 for the third cold-read blocker (PM ruled option B): the chronicity safety row stated \"has been ongoing 25 days (first logged Jun 5)\" on a page stamped Generated Jul 2 and beside a tile reading 28 d. The 25 came from detection.ts's `spanDays`, a floored INSTANT delta, so it was clock-dependent — one hour later on the same calendar days printed 26 — and it understated, inside a safety flag. The row now states its two anchors (\"has been logged from Jun 5 to Jul 1\") instead of a duration nothing on the page can check; the closing anchor rides the EXISTING local-day recount that already corrects symptomDays/daysSinceLastEpisode, and is null (opening anchor only) when the window does not cover the detector's full episode set. `spanDays` itself is UNCHANGED in detection.ts and still gates minSpanDays/firmSpanDays, so no detector fires differently and the rolling Signal is untouched; no rival span is derived, the number simply stops being rendered. B-532's three intents (log-attribution, never \"first noted\", left-censor disclosure) are preserved and still asserted. 441 generate-report cases green. Final CUL-319 bump: a legend entry for the day-on-a-time convention the two Appendix A fixes introduced. The re-read flagged it as the one convention on that page with no legend row, which is fair — every other one has had a row since B-010. Copy only. Final CUL-319 bump, closing the adversarial pass's finding 7: the chronicity flag's left-censor clause measured only the gap to the report WINDOW's start, which made it structurally unreachable on the 90-day fallback scope — the detector reads a 56-day lookback, so its first onset sits ~34 days inside a window that opened earlier. Reproduced: 30 vomits logged Apr 4 – Jun 30 rendered \"logged from May 7 to Jun 30: 19 episodes\" beside the page's own \"(30 logged)\" and an appendix A opening Apr 4, with no disclosure — a 12-week course understated as 8 on the lead safety line. The clause now also fires at the detector's lookback edge, with its OWN sentence: at the window's edge nothing earlier exists, but at the lookback's edge the earlier episodes are printed in appendix A, so that sentence sends the reader there instead of letting the interval read as the record's extent. No detector change — `windowDays` and `scope.detectionNowIso` were already in hand. 444 generate-report cases green, incl. new tests for both sides of the split and for the null-anchor branch.",
      "ref": "B-494",
      "updated": "2026-08-23"
    },
    "generate-signal": {
      "status": "pending",
      "fingerprint": "sha256:55c2b558e82af97dbae08a2b2f62e24e889b25c6cb749afe62f9f7a87fad355f",
      "reason": "Source drift, behavior-neutral on both counts, so no redeploy is owed for correctness. (1) CUL-564 removed detection.ts's `composeV2` toggle + the legacy pre-v2 composition fork (GA-3 cleanup); generate-signal always called detectSignals with the default v2 composition, so its output matches live v32. (2) CUL-372 re-based detection.ts's episode collapse (toEpisodeOnsets AND toConfidenceEpisodes) onto lib/symptomEpisodes — a thin adapter over lib/mealTiming's existing collapseEpisodes — so the Home Trend card and the Signal engine can no longer count the same events in different units. BEHAVIOR-NEUTRAL, verified by a COMMITTED, re-runnable test — lib/symptomEpisodes.differential.test.ts fuzzes 40,000 inputs against the pre-refactor toEpisodeOnsets/toConfidenceEpisodes bodies transcribed verbatim (asserting >5,000 of them carry duplicate instants and >5,000 split into multiple episodes, so the fuzz is evidence rather than decoration), plus the overnight-chained-bout case. The one divergence is non-finite instants, which no caller can produce (all 7 engine call sites pre-filter Number.isFinite) and which the test pins explicitly. 1369 deno tests green. A source-reconciling redeploy is owed but not behavior-urgent, and can ride the next generate-signal deploy. Was `deployed` as v32 (ACTIVE) — GA-3 (CUL-550), 2026-08-21, which also carried the CUL-258 fetch-timeout work. Also unified the episode-gap constant: SYMPTOM_EPISODE_GAP_HOURS now resolves to DEFAULT_MEAL_TIMING_CONFIG.episodeGapHours (both were an independent literal 3), so the collapse and photoComposition's collapseComposition cannot drift apart. Same value, no behavior change.",
      "ref": "CUL-372",
      "updated": "2026-08-22"
    }
  }
}
