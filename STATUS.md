# Project Nyx — Status

_Canonical answer to "where are we?". High-churn: update inline at session end and any time these change mid-session. CLAUDE.md is the stable operating manual; this file is the volatile state. **Keep it scannable** — prose narrative and build history belong in PR descriptions + git, not here (the file is reconstructable via `git log -p STATUS.md`)._

**Last updated:** 2026-07-16 — **B-191 — `symptom_chronicity` (⑦) added to the cross-pet safety banner allow-list; client-only (no schema/Edge Function/`generate-signal` redeploy), shipped via #370.** Closed the cross-pet escalation gap: a non-active pet whose only safety finding was a weeks-long unresolved chronic course raised no banner on the active pet's Home (safe-direction under-escalation — it still led on that pet's OWN Home, never a false all-clear). `BANNER_SAFETY_PRIORITY` now mirrors the engine's `SAFETY_TYPE_ORDER` exactly (`intake_decline` 0 > `symptom_chronicity` 1 > `symptom_worsening` 2); new onset-month-anchored `bannerRest` teaser ("{pet} has had recurring {symptom} since {month} — worth a look"), descriptive/duration-only + screened by the existing `validateBannerPhrasing`; `useCrossPetSafetyBanner`/`CrossPetSafetyBanner` are generically typed so needed zero changes. **`adversarial-reviewer` PASS** (adversarial `firstOnsetIso` → "since then", never crashes/leaks/reassures; copy clears all four guardrail screens; ranking matches `SAFETY_TYPE_ORDER {0,1,2}`; only adds eligibility — no all-clear/double-count) + **`code-reviewer` ship-ready** (priority mirror byte-for-byte; `onsetMonth` reused, no new KEEP-IN-SYNC surface) + `nyx-voice` ✓. **1302 jest** green, `tsc` clean; no server CI. Backlog **B-191 → Done**. **Residual (NOT a blocker, PM/Dr. Chen):** D2 `minEpisodes` 6-vs-5 chronicity floor still un-ratified — this broadens that detector's surface to a cross-pet banner; safe-error direction unchanged. **Prior (2026-07-16):** **B-180 CLOSED — guarded top-level `Deno.serve` with `import.meta.main` across 5 Edge Functions so their `deno test` suites actually run; shipped via #372.** Importing an Edge Function's `index.ts` for `deno test` ran its module-top `Deno.serve(...)`, which tries to bind a listener in the test runner and **crashes the whole invocation** — so these suites had never executed in-sandbox (the deploy gate silently fell back to `deno check` + Node-verified logic; surfaced during the B-060 v6 deploy). Fix = the standard Deno testability pattern: extract each handler to `const handler = async (req): Promise<Response> => {…}` and guard the listener with `if (import.meta.main) { Deno.serve(handler) }` — true as the deployed entrypoint, false on test import, **behaviour-preserving in prod**. B-180 was scoped to `analyze-vomit`; the sibling check it asked for found the **identical crash-on-import in 4 of the 5 other fns** whose `index.test.ts` imports `./index.ts` (`generate-signal`, `extract-food-from-photo`, `extract-medication-from-photo`, `generate-report`) — all folded in at PM direction (`delete-account` was already safe — imports only `./plan.ts`). **All 5 suites now run green where they previously crashed:** analyze-vomit 46 · generate-signal 266 · extract-food 25 · extract-medication 25 · generate-report 167 (**529 total**); `deno check` clean on all 5; app `tsc` + **1297 jest** green (pre-push hook). **Reviews:** `code-reviewer` **ship-ready** (mechanical, no closure/`this` loss, sound return typing; 2 non-blocking NITs — a shared `_shared/serve-guard.ts` helper if a 6th fn ever joins; and the one prod assumption code-review can't close from a sandbox, that the edge-runtime loads `index.ts` as the isolate main module → `import.meta.main` true → **run the runbook's post-deploy boot smoke-test** whenever these fns are next deployed); `adversarial-reviewer` **N/A** (serve-guard, not analysis logic). No schema; no build-phase change; **no redeploy in this PR** — the guard takes effect on each fn's next natural deploy, and prod behaviour is identical until then. Personas: Engineer ✓ (testability + typing) · Dir. of Eng ✓ (independently-deployed artifacts, duplication acceptable) · Data / Designer / Dr. Chen **N/A**. `tests: the fix IS test-infra — 529 deno tests now execute in-sandbox for the first time`. **Prior (2026-07-16):** **B-204 — `extract-food-from-photo` base64 OOM fixed; shipped via #369 (draft), function redeployed live v12.** The food extractor had the exact rope-string `btoa(Array.from(bytes,…).join(''))` blowup (one JS string per byte, no size cap) that hard-killed `analyze-vomit` with a 546 (WORKER_RESOURCE_LIMIT) on a multi-MB photo (fixed #255) — latent here because food photos are client-compressed, but a large/legacy upload would OOM identically. Ported the analyze-vomit fix **plus the imgproxy downscale** (PM added scope): raw-`blob.size` guard (`MAX_CLAUDE_IMAGE_BYTES` ≈3.93 MB, checked BEFORE encoding) + chunked `bytesToBase64` (32 KB windows) + `fetchUsableImageBlob` on `nyx-food-photos` (oversized re-fetched via Supabase Storage image transformation, 1568px `contain`, B-205 Pro add-on; drops the photo if the transform is unavailable). Handler now drops an un-bringable photo from the extraction set and fails cleanly (`ai_extraction_status='failed'` → the existing retry CTA) only when ALL photos are unusable — food extraction has no clinical floor, so a clean failure is the right degrade (diverges from analyze-vomit's `photoUnreadable`, correctly). Media-type sniffing (a separate latent Claude-400, not an OOM) deliberately OUT of scope; the B-203 partial-drop honesty concern is narrowed (a drop now needs the transform itself to fail) but stays with B-203. **DoD:** 4 new `bytesToBase64` vectors incl. the 32 KB chunk-boundary oracle test vs deno-std → **29/29 deno green**; `deno check` clean; esbuild bundle `node --check` clean. **`code-reviewer` ship-ready** (faithful port — identical constants/arithmetic/chunk size/transform params; correct bucket swap + fail-cleanly divergence; nits all pre-existing/out-of-scope). Deployed via Supabase MCP: **v11→v12 ACTIVE, `verify_jwt=true` preserved, source read-back byte-identical, live endpoint returns a clean 401** (not WORKER_ERROR). Rebased onto `main` (#368) — auto-merged the disjoint `docs/backlog.md` rows. Personas: Engineer ✓ (OOM eliminated, honest degrade, error-handling intact) · Data / Dr. Chen / Designer / Trust & Safety **N/A** (backend infra, no clinical/statistical logic, no data-access boundary change) · adversarial-reviewer **N/A** (pure infra/OOM fix — no detection/AI-read/escalation logic; reviewer concurred). `tests: bytesToBase64 unit-covered; fetchUsableImageBlob is impure/client-touching → integration-verified per convention`. No schema; no client change; parallel-track (Food library Step 3) hardening — no build-phase change. **PM action:** merge #369 (function already live, so merge is repo-tidiness not correctness). **Prior (2026-07-16):** **B-021/B-199 closed — SecureStore >2KB auth-token fix verified already shipped; docs-only, shipped via #371.** The "Supabase auth session token >2048 bytes in SecureStore" item (logged twice: **B-021** general backlog + **B-199** from 2026-07-02 on-device QA) was resolved *incidentally* by the frequent-signin fix chain (#306/#327/#350): `lib/secureStore.ts` `ChunkedSecureStoreAdapter` splits the session across N sub-2048-byte keys, so it never trips the cap — exactly the "chunk it" option both rows proposed. **Verified** via `lib/secureStore.test.ts` (17/17 green; the `>2048-byte round-trip` test drives a mock modelling SecureStore's real ceiling — it *throws* on an over-limit write — so it genuinely exercises the chunking, not a rubber stamp). **Why groomer left them Open:** no PR/commit ever references these IDs, and no commit title carries the "2KB"/"2048"/"chunk" keywords (those live only in code comments) — the linkage is semantic (a "frequent logout" fix that also silenced a SecureStore size warning), not a reference match, so groomer correctly declined to guess; closed here as a human-in-the-loop reconciliation. Closed across `docs/backlog.md` (B-021 + B-199 rows) + `docs/app-store-readiness.md` (Tier-4 register row). Personas: Product Owner ✓ (backlog reconciliation) · Engineer ✓ (adapter genuinely covers >2KB — ceiling-modelling test, not a mock that passes regardless) · Trust & Safety ✓ (session stays encrypted at rest in the OS keystore; no regression) · Data / Dr. Chen / adversarial-reviewer **N/A** (no clinical/statistical logic). `tests: N/A — docs-only` (pre-push hook: tsc + jest green). No schema; no app code; no build-phase change. **Prior (2026-07-16):** **B-354 requirements session (per-account food library); docs-only, shipped via #368 (draft).** PM ratified the direction (per-account now; the shared/standardized catalog returns later as a **separate curated layer**, never by un-scoping user rows — FR-9). Delivered **`docs/nyx-per-account-food-library-requirements.md`** (build-ready): 9 FRs — scope in place on `created_by_user_id` (NOT NULL + `DEFAULT auth.uid()` + FK `SET NULL→CASCADE`); RLS rewrite **dropping 004's permissive policies** (they OR-swallow any new restrictive policy); per-account UPC uniqueness; `nyx-food-photos` ownership-subquery storage policies (paths are `{foodId}/…`, not uid-prefixed — no object moves); `refreshFoodCache` user filter + a **one-time cache truncate** (RLS filtering never deletes already-cached foreign SQLite rows); `extract-food-from-photo` ownership gate (closes **B-343** with FR-2); `delete-account` **preserve→purge flip**; no-degradation invariant (FR-8: signal/report Edge Functions need NO code change — they read via JWT/RLS); + a 5-PR plan w/ gates (`rls-privacy-reviewer` mandatory on PRs 1/3/4). **Grounded on live prod (read-only):** 56 food rows / 1 creator / 0 null-creator / **0 cross-account meal/trial/arrangement refs** (of 624) → backfill = trivial attribution; cheapest this migration will ever be. **B-005 interplay (PM-requested):** per-account makes `archived_at` per-user by construction → B-005 PR 1 simplifies (drop `archived_by_user_id` + any per-user layer); sequence B-354 → B-005 stands; archive deliberately NOT bundled into the re-scoping migration. **CLAUDE.md Tier-1 edits:** hard constraint rewritten (`app_config` = the sole sanctioned global table; no new code may assume the global catalog) + new Open Questions row for the remaining ratifications **D2** (bundle `medication_items` — team unanimous; T&S: same class, worse content) / **D4** (deletion purge flip + terms §3 catalog-license + privacy-policy rewrite + hosted republish — PM action) / **D6** (sequencing). Legal ripple named: terms §3's perpetual cross-user catalog license + privacy's "the one thing that survives" become false the day this ships → rewrite must precede first submission. Personas: T&S ✓ (launch blocker closed by design; legal ripple; med twin) · Dir. of Eng ✓ (in-place scoping = zero client breakage; drop-don't-supplement 004) · Data Scientist ✓ (0-cross-ref verified by query, not assumed; FR-8 fixture-asserted; dedup goes within-account) · Dr. Chen ✓ (AC-5: historical joins byte-equivalent) · Designer/Jordan/Sam ✓ (picker becomes *your* pantry; existing empty states cover new accounts) · Product Owner ✓ (B-343 closure path, B-005/B-009/B-018/B-292 ripples routed) · QA ✓ (two-account AC matrix) · adversarial-reviewer **N/A** (planning only — no clinical/statistical logic authored). `tests: N/A — docs-only` (pre-push hook: tsc + **1297 jest** green). No schema; no app code; no build-phase change. **D2/D4/D6 PM-RATIFIED same day (mid-session):** meds bundle into PR 1 · deletion purge flip + small legal rewrite (hosted republish = PM action at PR 5) · B-354 → B-005 back to back. **PM next:** merge #368; PR 1 (schema migration) is ready to build. **Prior (2026-07-16):** **B-039 in-app account deletion VERIFIED end-to-end & CLOSED — the hardest App Store (5.1.1(v)) blocker is done.** The PM ran the in-app delete flow on a real account (`d.mark@hey.com` / pet "Yeti"); this session verified the erasure against the live DB (project `aigchluqluzuhtbfllgh`): the `auth.users` row is gone, **0 orphaned rows across all 13 cascade tables** (`pets, user_profiles, events, meals, conditions, diet_trials, vet_visits, vet_reports, event_attachments, vet_visit_attachments, ai_signals, event_ai_analysis, feeding_arrangements`), **no residual Storage** objects tied to the deleted user across the three health buckets, and `food_items` SET-NULL attribution intact (AC-3/4/5). Cross-checked that the **deployed** `delete-account` fn == repo (incl. the B-127/B-128 medication-label-photo purge + the FR-2 confused-deputy guard), ACTIVE, `verify_jwt` true; client affordance (Settings → Account → Delete account) + type-to-confirm `DELETE` + offline guard all present (AC-1/2/7/8/11). Marked **Done** across the trackers (`docs/backlog.md`, this file, `docs/app-store-readiness.md`, `docs/app-store-submission-guide.md` step 8). **Riders still open (separate items, NOT gating B-039):** AC-6 **cross-account** logout-wipe (deletion exercises the same `SIGNED_OUT`→`clearLocalData` path, but the "sign into a *different* account → no bleed" assertion wasn't run); a formal `rls-privacy-reviewer` re-run + `deno` suite run for a spotless DoD stamp; B-119/120/121 hardening (B-120 becomes hard the day Apple Sign-In ships). **Incidental finding → B-121 (logged; NOT a B-039 failure):** 14 orphaned `nyx-event-attachments` objects under a deleted pet (`a4b711e3-…`) owned by a *live* account — left by a pet/event soft-delete (which doesn't purge Storage; only account deletion does). Docs-only; no schema, no app code, no build-phase change. `tests: N/A — docs-only` (verification via live-DB SQL + deployed-fn read-back). **Prior (2026-07-16):** **B-005 planning (smarter library deletes → archive-not-cascade) + B-354 logged (per-account food library, PM-parked high-priority); docs/backlog-only, shipped via #365.** Ran the B-005 planning session: PM ratified **archive terminology** (removing a food no longer erases its meal history — that stays a per-event Timeline action), **preserve meal history** via a tombstone/`archived_at` flag, **undo-capable** via an "Archived" library section with Restore + an immediate undo snackbar. The load-bearing invariant: filter `archived_at` ONLY at picker/library reads, NEVER on history/analytics/vet-report joins (else an archived food's past meals lose their name + drop from the correlation exposure set). Surfaced during planning that `food_items` is **globally scoped** (a sanctioned exception) — so a second account sees every account's foods in its picker (`refreshFoodCache` pulls all rows, no user filter). Trust & Safety flags it a **pre-launch privacy blocker** (cross-tenant exposure of health-adjacent diet data + `created_by_user_id`) → **B-354** (de-globalize to per-account; team near-unanimous, PM parked as **high-priority**). B-354 gates B-005: a per-account catalog makes B-005's per-user archive fall out for free, so **B-354 → B-005**. B-005 requirements-doc write deferred until B-354's per-account direction is PM-ratified. No app code; no schema; no build-phase change. Personas: Data Scientist ✓ (preserve-refs invariant; per-account removes cross-user metadata contamination) · Trust & Safety ✓ (global-catalog = launch blocker; `rls-privacy-reviewer` mandatory on the re-scope) · Designer/Jordan/Sam ✓ (archive-not-delete copy; Archived section empty state; cleaner personal picker) · Dr. Chen ✓ (active-diet-trial food must keep tracking when archived) · Engineer ✓ (additive migration; archive = one flag flip, no event/meal mutation) · adversarial-reviewer **N/A** (planning only — no clinical/statistical logic authored). `tests: N/A — docs/backlog-only` (pre-push hook: tsc + **1297 jest** green). **Prior (2026-07-16):** **In-app legal wiring (B-229/B-230/B-270) — hosted-doc links live + onboarding disclaimer acknowledgment; shipped via #361 (schema) + #362 (wiring), both draft.** Hosted docs live at getculprit.app `/privacy` `/terms` `/disclaimer` (verified 200 this session). **#361 (own PR per schema isolation, migration `032_legal_acceptances.sql`, APPLIED LIVE via Supabase MCP):** append-only `legal_acceptances` (user_id, document, version) with a **server-stamped `accepted_at`** — column-level INSERT grant excludes the timestamp so a client can't forge it; insert/read-own RLS; deliberately NO client UPDATE/DELETE path; `ON DELETE CASCADE` folds into B-039. Advisors: zero new findings. **`rls-privacy-reviewer` PASS** (attacks tried + held: cross-user insert, `accepted_at` forgery, owner/other UPDATE-DELETE, cross-user read, upsert-DO-UPDATE [fails safe 42501 — never overwrites the first acceptance], anon; follow-ups honored — client uses a plain `.insert()` + 23505→already-recorded, never a default upsert; never table-level re-GRANT INSERT). **#362 (wiring):** `LEGAL_LINKS_ENABLED` → **true** (the B-283 PR-5 legal flip; gate retained for outage fallback) + `DISCLAIMER_URL`; You-screen gains a **Veterinary disclaimer** row (same gate); signup's "on its way" Alert stubs retired — the "By continuing you agree…" Terms/Privacy links open the live docs; NEW **`app/onboarding/disclaimer.tsx`** between signup and pet setup — appendix copy VERBATIM (`docs/legal/veterinary-disclaimer.md`, nyx-voice-checked at source), the button tap AWAITS `lib/legal.ts recordDisclaimerAcceptance` (write failure → calm retry, never advances unrecorded; re-walk → already-recorded, first acceptance stands); `hooks/usePet.ts` needs-onboarding entry repointed → `/onboarding/disclaimer` so a resumed mid-flow quit still passes the acceptance point; `PrimaryButton` wrapped-label centring. Personas: **Designer ✓** (placement = onboarding, a sanctioned deliberate decision — Principle 1 forbids decisions at moment of EVENT, not setup; one tap; warm terminus untouched) · **Trust & Safety ✓** (server-side dispute-grade record; append-only; rls-privacy PASS) · **Engineer ✓** (plain-insert semantics per the reviewer; no schema in the wiring PR) · **`nyx-voice` ✓** (new strings: "Before you start" / "Read the full disclaimer" / retry alert — calm, no `!`, never reassures; body+button verbatim) · Data / Dr. Chen **N/A** · adversarial-reviewer **N/A** (no clinical/statistical logic — legal wiring). `tsc` clean + **1295 jest** green (+12: `lib/legal` ×5, disclaimer screen ×6, signup legal-links +1). Backlog: **B-229/B-230/B-270 → Done** (in-app halves; the ASC store-listing URL *field* rides guide step 14/B-268); NEW **B-353** — legacy-account acceptance catch-up (existing users never re-traverse onboarding → no record; PM UX call). **PM actions:** run #362's Manual QA on-device (fresh signup → acknowledgment → `legal_acceptances` row; links live from signup + You screen; airplane-mode retry), then merge #361 → #362 (migration already live, so order is tidiness not correctness). **Prior (2026-07-16):** **B-118 closed — leftover `smart-worker` Edge Function verified deleted; shipped via #359 (draft).** PM believed the stock-template `smart-worker` function (zero callers, an `authMode==="secret"` RLS-bypass branch — a security-review loose end, sibling of the closed B-043) had already been dashboard-deleted. Verified it: `list_edge_functions` on the live project (ref `aigchluqluzuhtbfllgh`) returns 8 functions (`extract-food-from-photo`, `analyze-vomit`, `generate-signal`, `delete-account`, `extract-medication-from-photo`, `generate-report`, `view-report`, `zz-deploy-probe`) — **no `smart-worker`**; also absent from the repo. Marked B-118 `Done — 2026-07-16` across the three trackers (`docs/backlog.md`, this file's Cleanup/infra checklist, `docs/app-store-readiness.md`). Docs-only; no schema, no app code, no build-phase change. Personas: Trust & Safety ✓ (the RLS-bypass function is gone) · Product Owner ✓ (backlog reconciliation) · Engineer / Data / Dr. Chen / adversarial-reviewer **N/A** (no clinical/statistical logic — a dashboard delete + status update). `tests: N/A — docs-only` (pre-push hook: tsc + **1283 jest** green). **Adjacent observation (out of scope per PM):** the live list still carries `zz-deploy-probe` (v1, no repo backing) — same class of dangling throwaway as B-043/B-118; and `view-report` runs `verify_jwt:false` (presumably the intended public share-token path). PM ruled both out of scope for this PR; not logged. **Prior (2026-07-16):** **Legal docs (`docs/legal/`) — Step 5 "lawyer pass"; shipped via #358 (new draft PR).** #302 (draft + Step 4 review pass) is **MERGED to `main`** (2026-07-16), so per the git rules this follow-up is a fresh change on a new branch/PR (the kickoff's "ride #302" was stale). Legal-consultant panel diligence over the three docs; three edits applied + a decision memo delivered in-session. **terms §14 — dispute resolution DECIDED:** keep **Nebraska courts** (consumer home-forum carve-out) + add an **informal-resolution-first** step (30-day notice) and an explicit **small-claims carve-out**; **reject arbitration + class-action waiver** (solo/free app → weak class-action upside vs. asymmetric AAA per-case $375 + 2025 mass-arb $8,125 business initiation; "Pets > $" optics) — **PM-ratify**, posture stays provisional pending a licensed-attorney opinion. **terms §12 — Apple minterm-8 gap fixed:** all 10 Apple Minimum Terms checked item-by-item vs. §12; item 8 (developer **name + address**) was email-only → added a `[OPERATOR MAILING ADDRESS]` placeholder (**PM-ratify**: a P.O. box / registered-agent / LLC-office address avoids a home address). **privacy §1/§3 — location/EXIF claim aligned with shipped code (#357):** every photo is metadata-stripped **client-side, at upload, before it leaves the device** (stronger/more accurate than the prior server-strips-at-report-time wording). **New `docs/legal/attorney-brief.md`** — one-page context for a narrow flat-fee Nebraska engagement (formation execution + a §14 enforceability sign-off). **Factual claims re-verified against code:** deletion path (`HomeHeader.tsx:75` → You screen `settings.tsx:117/220`), account-block relocation (`profile.tsx:797`), upload-time EXIF strip (`storage.ts compressForUpload` + `sync.ts`). Personas: Commercial/consumer-contracts ✓ · Apple-platform ✓ (minterms) · Privacy ✓ (EXIF/US-only) · Nebraska formation advisor ✓ (LLC/DBA + publication quirk) · Data/Dr. Chen **N/A** · adversarial-reviewer **N/A** (legal text, no clinical/statistical logic). `tests: N/A — docs-only`. **Residue needing a licensed NE attorney:** (1) LLC + "Culprit" trade-name (DBA) formation **execution** ($100 online each + NE newspaper publication: LLC 3 consecutive weeks + affidavit, DBA one notice + proof within 45 days), (2) an **arbitration-clause enforceability opinion** *only if* the PM later revisits §14. **Recommended LLC trigger: form before the app leaves TestFlight for public release** (converts Apple Seller name off my personal name; caps liability once the audience is public, not TestFlight-scale). B-229/B-230/B-270 stay **In progress**. **PM actions:** ratify §14 posture; provide the mailing address; decide LLC timing; (carried) fill legal name + SMTP + effective dates; verify Supabase backup window. **Prior (2026-07-16):** **Legal docs — legal-consultant review findings + PM decisions applied; rode draft PR #302 (now merged).** Applied the review pass to the three App-Store legal docs (guide step 2). **privacy-policy.md:** §7 (+ terms §8) deletion path corrected to the real shipped **You**-screen-via-Home-avatar route (not the retired "Profile → Account"); §1 discloses per-account/per-feature **AI-usage metering** (migration 031 `ai_usage` + `record_ai_usage`, internal-only, never shared); §4 **US-only** (offered + processed in the US); §2 support-mailto carries app version + OS (`lib/appInfo.ts`); §6 backup residual left **"a short period"** (Supabase retention window unverified — PM to confirm/tighten); §3 Anthropic sentence deliberately untouched. **terms-of-service.md:** §3 catalog license **granted** (not just described) + survives deletion/termination; new **§12 Apple App Store** (Apple's 10 min EULA terms) + **§13 General** (severability / entire-agreement / assignment / no-waiver / notices / force-majeure — **no arbitration clause**); §5 AI usage-limits line; §2 account-sharing **softened** (household caregivers, holder responsible); §14 governing law = **Nebraska / Nebraska courts**. **PM decisions:** US-only · soften sharing · courts/no-arbitration (provisional — lawyer confirms at legal review). **Placeholder fills:** contact `support@getculprit.app` · operator = *an individual* (legal name still a placeholder) · Nebraska venue; SMTP provider + effective dates left as placeholders. **Reconciled the stale draft branch with `main`** (was ~50 commits behind — the "no common ancestor" was a shallow-clone artifact; true merge-base = the recorded PR base `e900b53`; conflicts only in STATUS/backlog/submission-guide → took main's; the 4 legal docs merged clean, so the PR diff is now **purely the legal docs**). No schema; no app code; docs-only. Personas: Trust & Safety ✓ (deletion-path honesty · metering internal-only · US-only · sharing responsibility retained) · Dir. of Eng ✓ (Apple EULA fidelity, assignment/beneficiary) · Designer/`nyx-voice` ✓ (legal register, zero `!`) · Data / Dr. Chen **N/A** · adversarial-reviewer **N/A** (no clinical/statistical logic — legal text). `tests: N/A — docs-only, no store/Edge Function/lib touched`. B-229/B-230/B-270 stay **In progress** (hosting B-273 + in-app wiring remain). **PM actions:** fill `[YOUR FULL LEGAL NAME]` + SMTP provider before hosting; verify Supabase backup window; lawyer pass on the provisional disputes posture. **Prior (2026-07-16):** **Privacy hardening — route all photo uploads through `compressForUpload` to strip EXIF/GPS; shipped via #357 (draft).** Three upload paths (plus the vet-attachment sync re-upload) bypassed `compressForUpload` (`lib/storage.ts` — the `expo-image-manipulator` re-encode that strips EXIF/GPS) and uploaded original camera-roll bytes, so a picked photo's GPS EXIF could reach Supabase Storage: **(1)** vet-visit inline save (`app/vet-visit.tsx:146`, `photoUri` raw) + the sync re-upload (`lib/sync.ts` `syncPendingVetVisits`, `att.local_uri` raw — vet attachments never passed through `prepareAttachmentUpload`); **(2)** pet profile photo (`app/(tabs)/profile.tsx` — `exif:false` only drops EXIF from the picker's JS result, not from the stored file); **(3)** edit-event inline attachment (`app/edit-event.tsx`, `newAttachmentUri` raw). All now re-encode to a stripped JPEG before upload — directly via `compressForUpload`, or via `prepareAttachmentUpload` (wraps it, passes non-images through, forwards `prep.mimeType`) for the vet sync path. **Swept every remaining `uploadPhoto`/`storage.*.upload` callsite** — all others already compressed (food-capture, medication-capture, log, event/[id], food/[id], medication/[id], event-attachment sync/ensure); server-side Edge Functions only download/remove. EXIF **date** reads unaffected (pick-time `asset.exif`, before upload); paths stay `.jpg`/`image/jpeg`; the persisted `local_uri` original (B-104 durable hero) untouched — only the uploaded object is re-encoded. **Makes the privacy-policy claim "we do not collect your device's location, and we do not use photo location metadata" true across all upload paths** (feeds PR #302 policy + B-268 label). **DoD:** `tsc` clean; **1283 jest** green (the `prepareAttachmentUpload` compression/strip guard — now fronting the vet path too — is covered by the 5 existing `lib/sync.test.ts` tests, header updated to note the vet coverage). **Reviews:** `code-reviewer` **ship-ready** (async `.then` chains resolve to a single terminal `.catch` covering the prepended compress step; upsert-then-mark-synced guard intact; mime/extension consistent; noted a *pre-existing, not-introduced* missing width/height on `compressForUpload` at the two new sites → **B-352**); adversarial-reviewer **N/A** (upload plumbing, no clinical/statistical logic). Personas: Trust & Safety ✓ (uploads carry no location metadata) · Engineer ✓ (error-handling + sync-guard invariants held) · Designer ✓ (vet-document legibility at 1600px/q75 — needs the on-device eyeball in the QA script) · Data / Dr. Chen **N/A**. Backlog: **B-352** (thread picker asset dims into `compressForUpload` at the non-dimensioned sites — Later). **Known boundary:** the on-device `local_uri` hero copy still keeps EXIF (app-owned storage, never *collected* — the claim is upload/collection-scoped). **PM action:** none code-side; on-device, eyeball a photographed vet *document* stored at 1600px/q75 for legibility. **Prior (2026-07-15):** **Track-3 monetization T3-A — B-332 manual Primary-protein picker (client UI; no schema); shipped via #355.** `primary_protein` was extraction-only and the flagship diet correlation keys off it, so gating photo extraction (T3-E) without a manual path would silently degrade the FREE tier's flagship care insight (Principle-7 leak). Added a shared `ProteinPicker` (`components/food/ProteinPicker.tsx`) — a wrapping single-select `ChipGroup` (B-146: never an h-scroll row) + an "Other" typed escape, offered set = new `COMMON_PROTEINS` in `lib/protein.ts` (live distinct values + common clinical set, S5) — to BOTH the food-capture manual edit step and the food-detail edit. **Load-bearing invariants:** never null-clobbers an AI-hydrated protein (capture omits the column from its upsert unless the owner touched the picker; detail baseline-diffs + reseeds only when untouched) + one correlation path (owner-picked and AI-extracted both key through the same `canonicalizeProtein()` on read; `COMMON_PROTEINS` are canonicalize-stable, unit-guarded). No schema (`food_items.primary_protein` + `food_items_cache.primary_protein` already exist). **Reviews:** `code-reviewer` caught a real fix-before-merge reseed bug (a stale "Other" field could re-null a just-landed AI value) → fixed + 2 regression tests; `adversarial-reviewer` parity pass **PASS** (junk/whitespace/emoji collapse to null; no new fragmentation; flagged the global-catalog re-key on protein edits as a by-design governance note — consistent with format/food_type). **Unblocks T3-E.** Two non-blocking PM/Designer flags in the PR (re-tap-clears-selection; global-catalog edit). Personas: Designer ✓ (picker UX / B-146) · Engineer ✓ (never-clobber upsert semantics) · Data Scientist / adversarial-reviewer ✓ (correlation parity) · Dr. Chen ✓ (S5 protein set). **PM steer logged (approach A):** the "capture ALL proteins, not just primary" question — clinically strong for elimination trials but a cross-cutting change + a real vet-advisor call — kept OUT of B-332; new CLAUDE.md Open Question + backlog **B-351**. `tests: components/food/ProteinPicker.test.tsx + lib/protein.test.ts` (+17); `tsc` + **1283 jest** green; no server CI (0 checks). Merged `origin/main` into the branch to resolve conflicts in `app/food-capture.tsx` (integrated T2-3/T2-4's `capReached`/`aiRead` cap-state with the protein upsert) + `docs/backlog.md` (kept all of main's B-343–B-350, renumbered mine to B-351). **Prior (2026-07-15):** **Food-picker findability — two-round design exploration (docs-only) shipped via #351; PM ruled same day.** Household dogfood feedback (a food that misses Recent's 5-item/14-day cut is hard to find in the long library list) → **round 1** `docs/food-picker-findability-mockups.html` (baseline diagnosis + Option A tile photos / B pinned search + scope chips / C "rotation" shelf) + **round 2 blue-sky** `docs/food-picker-reimagined-mockups.html` (R1 prediction-first confirm card w/ a surfaced Data-vs-Designer lazy-confirm conflict + Data's four support conditions; R2 barcode/scan identity lane; R3 day-board schedule-as-first-class object; R4 pantry-as-visual-layer; comparison matrix + staged end-state). **PM rulings (Q1–Q4, recorded in both docs' decision panels):** photos (A) **HELD** → B-348 (docs kept as the revisit reference); **build C→B RATIFIED** — **B-346** ("{Pet}'s rotation" shelf: 30d window / cap 12 / wrapped compact grid, recency-factual copy) then **B-347** (pinned search + scope chips; executes B-020) — sequenced, not parallel (same `FoodPicker.tsx`); **B-009 dedup NOT folded in**; round 2 held as documented exploration → **B-349** revisit gate (after C+B + B-288 pilot data; Q5–Q9 unruled) + **B-350** (PM reframe: R3 as *display-only* day organization, no `feeding_schedules` table — Designer/Data pass needed). Personas: Designer ✓ (led both rounds) · Data Scientist ✓ (intake≠preference guardrails; R1 conditions) · Dir. of Eng ✓ (feasibility/costs; R3 schema concern → B-350) · Sam/Jordan ✓ · Dr. Chen ✓ (R2 = ground-truth food identity) · adversarial-reviewer **N/A** (docs-only, no clinical/statistical logic). `tests: N/A — docs-only` (pre-push hook: tsc + 1225 jest green on both pushes). No schema; no build-phase change. **Prior (2026-07-15):** **Track-2 monetization T2-5 — paywall mock gated out of the submission build (B-330); shipped via this PR (draft).** Spec §6.5 + §8 (T2-5). The onboarding paywall (`app/onboarding/paywall.tsx`) is now gated on the server-flippable `paywall_enabled` flag. **Primary route:** `app/onboarding/pet-age.tsx` `finish()` (both Continue + Skip) pushes straight to `/onboarding/done` when the flag is off, so the mocked paywall never mounts (the paywall sits outside the 5-step progress bar → no progress math changes; done is the terminus either way, back-from-done already locked). **Defensive backstop:** `paywall.tsx` bounces a stale-client / deep-link entry to `/onboarding/done` (renders `null` while bouncing → no dead trial CTA flashes), sequenced after the existing no-pet escape hatch. **Render-only** — the paywall key is client-only (§4.2, no server-enforced path; it's a client mock), and it **fails CLOSED**: shipped client default `paywall_enabled=false` (`lib/appConfig.ts` `APP_CONFIG_DEFAULTS`), so a config-unreachable build skips the non-functional CTA (Guideline 2.1/3.1.2 rejection risk) rather than showing a dead one. **Ship-dark:** seeded server value stays `true` (migration `030_app_config.sql:113`, untouched) ⇒ flag-on = today's flow byte-identical **once config has resolved** (the steady state — returning users hydrate the cached flag instantly). **Config-load race (code-reviewer catch, accepted):** a brand-new signup with no cache that reaches pet-age before the async config fetch lands reads the fail-closed default and skips the seeded-`true` paywall — this is the spec AC's *third* clause ("config-unreachable → hidden"), not a break of the byte-identical clause; the failure is soft (a mock CTA shown less often, never a dead screen) and degrades toward the submission goal. Strict byte-identical for that first-run-in-flight window = hardening follow-up **B-345** (PM's call; not blocking). **The `paywall_enabled=false` pre-submission flip is a separate recorded config change** (dashboard/SQL on live `app_config`, noted in STATUS at that time) — NOT part of this PR. **DoD:** `tsc` clean; onboarding suite green (33 tests, +2 pet-age flag-off routing + +1 paywall flag-off bounce; existing paywall/pet-age tests preserved by defaulting the mocked flag ON). No schema; no server change; no deploy. **Reviews:** `code-reviewer` **fix-before-merge (advisory, not blocking)** — gate/guards/tests correct + ship-ready; the one substantive finding (the config-load race above) reconciled as an accepted soft residual + B-345, `STATUS`/comment/PR wording qualified, no logic change (a `configReady` gate would contradict AC clause 3). No adversarial pass needed (pure client routing). Personas: Engineer ✓ (render-only gate, fail-closed default, ship-dark holds) · Designer ✓ (no dead affordance in any state) · Trust & Safety N/A (no data-access boundary — a client route gate) · adversarial-reviewer **N/A** (no clinical/statistical logic — a routing flag). **B-329 → done** (T2-1+T2-3+T2-4+T2-5 all shipped); **B-330 → done on merge** (the recorded flip is the remaining PM action). `tests: N/A exemption not taken — pure-UI screen but the routing gate is tested via the existing screen suites`. **PM action:** perform the pre-submission `paywall_enabled=false` flip on live `app_config` before cutting the submission build; record it in STATUS. **Prior (2026-07-15):** **Track-2 monetization T2-4 — flag-aware client + AI cap states; shipped via #353 (draft). + the four T2-3 AI Edge Functions deployed from merged `main`.** **Deploy** (Supabase MCP, `verify_jwt=true` preserved, sha read-back + clean-4xx boot each): `extract-food-from-photo` v10→v11, `extract-medication-from-photo` v1→v2, `analyze-vomit` v7→v8, `generate-signal` v23→v24 — all ACTIVE (the new `getUser()` 401s on the anon boot test confirm the T2-3 auth path is live). Ship-dark: flags seeded `true`, counters zero ⇒ behavior byte-identical. **T2-4 (client half of B-329):** new `lib/appConfig.ts` + `hooks/useAppConfig` — fetch the server-flippable `app_config` flags (last-known-good AsyncStorage cache; per-key shipped defaults — **AI keys fail-OPEN, `paywall_enabled` fail-CLOSED** per §4.2; **render-only** — the function re-checks every gate, B-252) + a typed decoder for the §4.5 `{cap_reached}`/`{feature_disabled}` 200-bodies. Init = cache-seed only; the single authoritative fetch is auth-driven (`INITIAL_SESSION`/`SIGNED_IN`/`TOKEN_REFRESHED`) + on foreground (code-reviewer's redundant-refresh cleanup applied — was 3 SELECTs on cold start, now 1). **States:** food/med capture — flag-off opens straight on manual entry (no dead camera affordance); `cap_reached` → calm §7.3 band on the edit step (**never** error red), capture never interrupted mid-flow (Principle 1 rider); a capped/flag-off commit records the row **not**-AI-extracted (was falsely `completed`). Vomit read — new `status='capped'` (calm §7.3 runs-tomorrow copy, no retry, no reassurance, routes to vet) + `status='read_disabled'` (renders nothing); escalation still surfaces via the unchanged `completed` path — the function computes contextual flags BEFORE the cap gate, so a fired flag is never a capped row (**never-reassure survives the cap by construction**). D-M6 "free during early access" labels on both extraction surfaces; first B-333 care-first line on the extraction cap states; §7.4 done-screen "Got another pet?" (D-M8-safe). Copy centralized in `constants/monetizationCopy.ts`. **Reviews:** `code-reviewer` **ship-ready** (no correctness/safety blockers; verified the vomit branch-order + response-branching by hand; two follow-ups APPLIED — the `_layout` redundant-refresh cleanup + component-level §11 fixtures); `nyx-voice` ✓; Designer ✓ (all states); `clinical-guardrails` + Dr. Chen ✓ — Dr. Chen falsification: the 11th-vomit-in-a-day and clear-foam-not-eaten-36h cats fire their contextual flag **pre-cap** → written `completed`+`worth_a_call`, the cap can't suppress the escalation; HELD. **DoD:** `tsc` clean; **1263 jest** (+9 this session: 4 vomit-render + 5 hook-wiring; plus the §11 pure-decode + copy-invariant fixtures). **Open flag (PM / `pm-feature-review`):** §7.6-vs-§16 — B-333's "never behind this door" reads slightly oddly on a cap state with no visible paywall (included per §16 + kickoff; trivial to pull). Follow-up **B-344** (food/med capture render fixtures — deferred nicety). B-001 → done (T2-2+T2-3); **B-329 → T2-5 remains** (paywall flag) then the `paywall_enabled=false` pre-submission flip. **Prior (2026-07-15):** **Frequent-logout root cause found + fixed; shipped via #350 (draft).** Read the build-33 on-device auth-diagnostic trail (the #327 probe): the #306 chunking half works (every `sec.get` `path:"ok"`, 2189→2213 chars/5 chunks, survives cold start), but three `sec.get {path:"error"}` fired ~22 min into background (phone locked) and recovered to `path:"ok"` on unlock — the iOS keychain `errSecInteractionNotAllowed` signature. `lib/secureStore.ts` wrote with the default `WHEN_UNLOCKED` accessibility, so `autoRefreshToken`'s ~hourly background refresh on a locked phone can't read the session (→ null → "no session") or persist the refreshed token → intermittent forced logout. **Fix:** every chunk + the pointer commit now write with `keychainAccessible: AFTER_FIRST_UNLOCK` (backup posture unchanged); existing installs re-persist under the new class on next refresh/sign-in. Added a regression test asserting every write carries the option. `tsc` + **1226 jest** green. Engineer ✓ · Trust & Safety ✓ (documented `AFTER_FIRST_UNLOCK_THIS_DEVICE_ONLY` as a non-blocking follow-up) · adversarial-reviewer N/A (no clinical/statistical logic — keychain accessibility flag). Backlog: **B-279** resurface-trigger diagnosed (keychain, not concurrency) + fixed; chunked adapter RETAINED. **B-301** (remove the probe) now gated only on on-device confirmation build 34 holds a session across a locked-phone refresh — keep the probe in build 34, remove after. **PM next:** cut build 34 (fix + probe together), verify the locked-idle AC on-device, then close B-301. **Prior (2026-07-14):** **Track-2 monetization T2-3 — cap + flag enforcement in the four AI Edge Functions (code half; ship-dark).** Spec §4–§5; executes B-329 (code half) + B-001. Each function now, immediately before its Anthropic call: derives a **JWT-verified uid** (`auth.getUser()` — added to extract-food/analyze-vomit/generate-signal; extract-medication already had it; B-252 — the counter never keys on a body value), reads its `app_config` flag, increments+checks the per-user `ai_usage` cap via `record_ai_usage`, and on flag-off/over-cap returns a **typed product state** (`{feature_disabled}` / `{cap_reached, cap, resets_at}` as HTTP 200 — never a bare error). A shared-shape pure gate (`resolveGateState`/`resolveFlagValue`/`resolveCaps`/`computeResetsAt`) copied per-function (spec §15 S6 — no `_shared/` mid-track), Deno-tested per function. Free caps (§4.4): extract-food 15/60, extract-med 10/40, analyze-vomit 10/200 (tier-identical, D-M2), generate-signal 12/240 per-pet (`scope_id=petId`). **`analyze-vomit` reordered** (§5.4): `computeContextualFlags` runs BEFORE the gate, which runs BEFORE the vision call — so a capped/flagged-off incident **still escalates** on context (never-reassure survives the cap by construction; no code path from capped→reassuring). Cap gates the model call only, so a **photo-less log skips the gate entirely** (byte-identical, burns no unit). **generate-signal** keeps no-service-role; its flag forces template phrasing (detection never gated), cap is a per-pet bug-loop backstop (skip regen → client keeps cached signal). Extract-food gained a `validateFoodPhotoPaths` traversal/self-consistency guard (S3). **Ship-dark:** flags seeded `true`, counters at zero ⇒ byte-identical; no client changes (T2-4). **DoD:** **362 deno tests green** (25/25/46/266) + `deno check` clean on all four + app `tsc` clean; `event_ai_analysis.status` gains two values (`capped`/`read_disabled`) into the existing unconstrained TEXT column (not a schema change). **Reviews:** **`adversarial-reviewer` on analyze-vomit = initially FAIL → FIXED → clean** (caught a reachable B-028 clobber: a capped/flag-off contextual-escalation re-analysis of a prior model-completed-but-not-yet-edited row did a full upsert nulling `blood_present`/`foreign_material_present`, silently erasing a red flag from the vet report; `code-reviewer` independently flagged the same — fix routes a prior REAL analysis through update-read-fields-only, regression-tested; escalation still surfaces, structured facts preserved). **`code-reviewer` clean after fixes** (also a NIT applied — food/med pass the real `flagEnabled` to `resolveGateState`, not the literal). **`rls-privacy-reviewer` PASS** (all six attack surfaces held — forged `photo_paths`/`food_item_id`/`petId`/`scope_id` can't charge another user's counter or reach cross-tenant data; the new `getUser()` closes the old anon-key-presence-only gap on extract-food; `validateFoodPhotoPaths` is a traversal guard, the bucket ACL is the cross-tenant boundary; two honesty notes: comment sharpened + pre-existing `food_items` global-overwrite → **B-343**). Personas: Engineer ✓ · Dir. of Engineering ✓ (gate shape, ship-dark, no-service-role preserved on signal) · Data/Dr. Chen ✓ (adversarial: capped escalation holds, never-reassure survives the cap) · Trust & Safety ✓ (rls-privacy PASS). **PM dashboard checks (config, not code):** confirm `verify_jwt=true` on all four functions post-deploy; `nyx-food-photos` bucket is **Private**; migrations 030/031 RLS-enabled in `get_advisors`. **Deploy note:** redeploys all four from merged `main` (per runbook — version bump + ACTIVE + clean 4xx boot smoke). Remaining: **T2-4** (client flag-aware + cap states, D-M6 labels) → **T2-5** (paywall flag) → the `paywall_enabled=false` pre-submission flip. B-001 → **done on merge** (T2-2 + T2-3); B-329 → partial (T2-4 remains). **Prior (2026-07-14):** **Track-2 monetization T2-2 — `ai_usage` throttle table + `record_ai_usage` RPC shipped via #348 (schema half; ship-dark).** Added migration `031_ai_usage.sql`: the per-user AI-call abuse bound (lever #3, spec §4.3) — a `(user_id, function, day, scope_id)` composite-PK counter table (UTC `day`; `scope_id` = pet_id for `generate-signal`'s per-pet cap, sentinel zero-UUID otherwise; `ON DELETE CASCADE` from `auth.users` folds it into the B-039 hard-delete) + the atomic `record_ai_usage(p_function, p_scope_id)` SECURITY DEFINER RPC (`SET search_path = public`; derives the caller from `auth.uid()` and RAISEs on null — the count can never be forged from the request body, B-252; `INSERT … ON CONFLICT DO UPDATE SET count = count+1 RETURNING count` → `day_count`, plus a same-scope UTC-month `SUM` → `month_count`; increment-then-check, attempts counted not successes). RLS on with one `SELECT USING (user_id = auth.uid())` owner-read policy + **no** write policies (default-deny — the RPC is the only write path); `EXECUTE` granted to `authenticated`, explicitly REVOKEd from `anon` (Supabase default-privileges grant it to anon/authenticated/service_role at CREATE, so the PUBLIC revoke alone wasn't enough — verified via live ACL `{postgres,authenticated,service_role}`). Applied live via the Supabase MCP (recorded in history). **Verified live:** atomic increment (2 calls → count 2, no error), per-pet `scope_id` independence (petA=2 / petB=1), correct UTC `day` + `RETURNS TABLE(day_count, month_count)` contract, null-`auth.uid()` → RAISE 42501; test rows cleaned up (0 rows remain). `get_advisors` (security): zero new `ai_usage`/`record_ai_usage` findings beyond the intended `authenticated`-can-execute lint (that IS the grant). **`rls-privacy-reviewer` PASS** — all six attack surfaces held (forged identity via `p_scope_id` → row lands under the caller's own uid; anon bypass → EXECUTE revoked + null-uid RAISE; cross-user read → RLS-filtered; `search_path=public` [no `pg_temp`] + static SQL → shadowing-proof; direct `POST /ai_usage` → RLS default-deny; scope_id cap-evasion → a direct RPC call can only self-inflate, +1 no decrement). B-001 → partial (T2-3 in-function cap enforcement remains). No app code; no client-visible change. Personas: Dir. of Engineering ✓ (schema shape, composite-PK grain, migration isolation) · Trust & Safety ✓ (`rls-privacy-reviewer` PASS; no client write path; JWT-derived identity) · Engineer ✓ · adversarial-reviewer **N/A** (no clinical/statistical logic — a counter, no detection/correlation/AI-read). `tests: N/A — schema-only migration; RPC behavior verified live against prod (atomic increment / scope independence / null-uid raise / return contract)`. **Prior (2026-07-14):** **Intake-timing product discussion (docs/backlog-only) shipped via #345.** PM dogfooding observation: the WSAVA intake chips fire co-located with the meal log (`MealCompletionCard`) — right for **scarfers** (dog kibble, cat treats) but premature for **grazers** (cat wet food), where the owner only learns the amount at the *next* feeding, over the leftover bowl. Ran the two pet-owner personas (Sam ✓✓ / Jordan: don't-break-the-scarfer-flow) + the full product team → the reframe that **intake-timing is a property of the (pet, food) pair, not a fixed step in the flow**, and the mechanic: a grazed food **defers its intake to the next-meal ritual** (answering-beats-initiating), stays honestly **unconfirmed** until then (B-156 G1), models consumed-at as a **window** (B-010). Filed **B-341** (the mechanic) + **B-342** (explain the WSAVA scale in-app). **PM direction:** in-app resolve-at-next-log endorsed as the core; **multi-channel** (in-app + notification) endorsed for conversion *with* cross-channel de-dup + fail-safe silence baked in (the notification half inherits the 2026-07-10 nudge-cap Open Question); **property carrier** (how a food is known to be grazed) left build-time — team lean: inferred default + one-time owner-correctable toggle, sibling to free-choice B-040; WSAVA explainer parked. No build-phase change; no schema; no app code. **Prior (2026-07-14):** **Vet-report send feedback → product decisions (docs-only; no build-phase change, no schema, no app code); shipped via #346.** PM's n=1 from the first real vet-report send: convened the team on 5 reactions. **RATIFIED** (CLAUDE.md Open Q → Resolved): elevate per-incident vomit VISUAL red flags (blood / suspected foreign) to a firm-but-calm Home Signal, led at top — persona conflict (Dr. Chen/Data: surface it vs Designer/owner: 2 false positives already → trust) resolved toward *surface + let the owner clear it cheaply* (the B-028 edit path). **Code-verified build guardrail:** the elevation must derive the flag from the owner-editable structured fields (`foreign_material_present`/`blood_present`), NOT the stale cached `visual_flags` (`lib/analysis.ts:152`) — so an override clears the card by construction, exactly as `generate-report` already does. Confirmed `generate-signal/detection.ts` never reads `event_ai_analysis` (the actual gap). Read the PM's real 38-pg report: the blue-ball foreign flag is correctly ABSENT (his override propagated), but the Jul-9 "stick" (deli-meat seasoning) false positive STILL leads the safety section (uncleared). Logged B-337 (talking-points/cover-note; "first reader is a vet tech, not the DVM" reframe), B-338 (scene-vs-contents prompt hardening, no escalation detune), B-339 (stale-cache residual — the report itself is override-aware), B-340 (the Signal-elevation build — Step 10 evolution, `adversarial-reviewer`-mandatory, deploy-gated on the client renderer). `tests: N/A — docs-only, no store/Edge Function/lib touched`. **Prior (2026-07-14):** **Track-2 monetization T2-1 — `app_config` migration shipped via #347 (schema half; ship-dark).** Added migration `030_app_config.sql`: the server-flippable product lever — a `key TEXT PK / value JSONB / updated_at` table so any AI surface or the onboarding paywall can be switched off with a config change instead of a new build. RLS on with one `SELECT`-to-`authenticated` `USING(true)` policy + **no** write policies (service-role/dashboard flips only, B-252); `set_updated_at` trigger; six seeded keys all matching today's behavior byte-for-byte (`ai_*_enabled=true`, `paywall_enabled=true`, `ai_caps={}`); `ON CONFLICT DO NOTHING` so a re-apply never resets a flipped value. Applied live via the Supabase MCP (recorded in history); security + performance advisors **zero new `app_config` findings** (RLS on; `SELECT USING(true)` is the sanctioned public-read pattern and sidesteps the per-row `auth.uid()` perf lint). `code-reviewer` **ship-ready** (no bug/anti-pattern findings; confirmed the trigger + `ON CONFLICT` are appropriate); one NIT applied — CLAUDE.md hard-constraint wording now names `app_config` as the 2nd sanctioned no-`pet_id`/no-`user_id` table alongside `food_items`. B-329 → partial (T2-3 server enforcement + T2-4 client states remain). No app code; no client-visible change. Personas: Dir. of Engineering ✓ (schema shape, RLS default-deny, migration isolation) · Trust & Safety ✓ (no write path from the app; service-role-only flips) · Engineer ✓ · adversarial-reviewer **N/A** (no clinical/statistical logic — a config table, no detection/correlation/AI-read). `tests: N/A — schema-only migration, no extractable logic`; `tsc` + **1225 jest** green (pre-push hook); no server CI (0 checks). **Prior (2026-07-13):** **Monetization & throttling spec review pass (docs-only) shipped via #344.** PM review of `docs/monetization-and-throttling-requirements.md`; verified the spec's code anchors hold (analyze-vomit step order, `event_ai_analysis.status` = unconstrained TEXT, generate-signal no-service-role, extract-food no-`getUser()`), then recorded five review amendments **without changing any ratified D-M ruling**: §4.4 clinically ratifies the `analyze-vomit` daily cap value (10/day = ER territory; escalation floor fires on #11 regardless) + adds the per-pet-enforcement-vs-per-user-cost note; **§9 T3-E + §16 flag B-047/B-016 as an unowned dependency** gating the gate-flip (both Open/unbuilt, in no PR plan — needs a PM ownership call); §4.2 adds the server-side config-read failure fallback (Dir. of Eng sign-off at T2-3); §4.7 flags the entitlement grace-window duration as an open T3-C product decision (provisional 72h). One review finding parked pending a PM call: the mandated shared client-side decoder for the §4.5 typed 200-body contract. No build-phase change; no schema; no app code. **Prior (2026-07-12):** B-325 direction ① (retroactive med→treat combo linking) shipped via #342 + brand on-device-QA fixes shipped via #343. ① adds a “+ Add a med given with this” entry on the meal/treat DETAIL screen (gated `food_type ∈ {meal,treat}`), reusing the forward `insertMedicationDose(…, pairedEventId)` + PR B3 intake→adherence coupling via a new `comboSource='detail'` param; returns to the treat (no completion-card replay), and a NOT-finished vehicle gets a deliberate confirm sheet (`ComboDoseConfirmSheet`) so the owner records how much of the dose got in rather than landing silently unconfirmed — unconfirmed only via an explicit “Not sure yet”. adversarial-reviewer PASS, code-reviewer clean; residuals B-334/335/336; direction ② (med-first) still held (B-111). Brand QA (#343): removed the CulpritMark glow halo (header) + the teal aurora glow behind the Landing moon (`NightHeroGround` — also the likely source of the stray on-device cream streak), and replaced the dark night pull-to-refresh band with a light strip (day Whorl on `colorSurfaceSubtle`) — fixing the dark-over-light whiplash + unpolished glow + starfield jank. Revises B-284 §3/§5 (Tier-2 doc edits flagged). No schema; no build-phase change.

---

## Current Phase

**Step 10 — AI Signal (`generate-signal`).** B-045 (3-step plan, no Step 4) shipped & merged:

| Step | What | Status |
|---|---|---|
| 1 | Deterministic detection engine (`detection.ts`) — case-crossover (B-050) + intake-decline flag | ✅ #72 (29 tests) |
| 2 | `generate-signal` Edge Function — detect → curate → Haiku-phrase → cache findings set (24h TTL); migration 015 (`findings jsonb`) | ✅ #74 (migration #73) |
| 3 | `SignalZone` wired to cached findings (per-type renderer, confidence tags, tap-to-expand, live/building/stale/`no_pattern`, async regen) | ✅ #75 |

**Architecture:** deterministic detection + LLM phrasing — the server computes & ranks a *true* finding in `detection.ts`; Haiku 4.5 only renders copy, with a deterministic template fallback. `phrasing.ts` = pure phrasing/curation/guardrails (offline unit-tested); `index.ts` = I/O shell. `validatePhrasing` rejects model drift (no `!`; never-reassure / never-"picky" on safety; associational-only on correlation).

**Signal engine — detectors live (deployed `generate-signal` v23):** ① case-crossover, ② intake-decline, ③ reflection (presence/counts; a *declining* trend routes to safety), ④ symptom-worsening lane (B-077), ⑤ post-prandial timing (B-078), ⑥ time-of-day clustering (B-079), ⑦ symptom-chronicity safety lane (B-182 — live v23, confirmed on real data 2026-07-01); + no-signal coverage diagnostics (B-053, incl. **B-070** ≥80%-dominance staple-washout + treats-vs-meals copy register, live v21); + **medication confounders** (B-117 PR 9 #207 — a drug on-board in a symptom window enters `detectCorrelations` as a confounder: case-enriched → suppress, concordant → cap at Early; **+ B-156 PR C1 #234 reads the dose↔vehicle pairing — a drug-in-food combo is attributed to the drug, the vehicle food's protein dropped from the exposure set, + the B-174 in-doubt-dose reconciliation; adversarial-PASS, **deployed v22** read-back-sha-verified + boot smoke-test**). Open follow-ups: **B-052** write-time protein normalization (read-time half shipped #92); **B-067** reflection/Trend dedup; **B-080** diet-structure placement (blocks descriptive Phase 3, PM call).

**On-device QA gap:** empty / `no_pattern` + reflection paths verified on device; the **LIVE safety-card path is now verified** — B-182 chronicity fired on Nyx's real data (2026-07-01 QA), the first live safety card seen on device. The correlation/intake safety lanes still legitimately yield zero on Nyx's healthy data (chicken ~3×/day staple → case-crossover washes it out; intake healthy → flag quiet), so those specific lanes remain unexercised live.

**After Step 10:** Step 9 (vet report) resumes. **Design + spec updated this session (2026-07-02):** discovery (#214) → v1 mock (#216) → **`docs/nyx-vet-report-requirements.md` — now build-ready v2**, folding in the **v3 + cat design mocks** (`vet-report-mock-v3.html` calm diet-trial dog; `vet-report-mock-cat.html` safety-led cat; v1 `vet-report-mock.html` + v2 kept for comparison), a **9-lens synthetic panel review** (Dr. Chen cold-read = CLINIC-READY, fix list applied), and a **real-data dry-run against the pet Nyx** (23 real vomits → the report renders *safety-led*: chronicity + a possible-foreign photo; validated the design end-to-end + surfaced §7.1 data-layer reqs — empty weight state, owner/neuter schema gaps, AI-status denominators, de-dup, free-fed, 90-day fallback). New decisions: **present-only phenotype**, **photos split-by-type** under mandatory §8 privacy controls, **weight (B-186) + empty state**, **stool characteristics**, **medications & supplements**, **branding = keep letterhead / kill footer marketing**. **§12 PR plan REORDERED per PM: Phase 1 full functional report → Phase 2 publish+share+photos → Phase 3 seamless-sharing niceties.** Preconditions **ALL RESOLVED** (B-044 #218 / B-115 #219 / B-028 #220). **Real-vet gate reframed (PM): n=1-first** — PM emails the real Nyx report to their own GP + books a Nyx appointment, in parallel with the build; the first real-vet reaction lands **before Phase 2**. **§8.2 HTML-first RATIFIED (#260).** Phase 1: **PR 1** (`report.ts`) shipped #261, **PR 2** (`render.ts`) shipped #262, **PR 3** (clinical must-carry hardening + B-213 intake-decline duration + traceable meal appendix) shipped #263, **PR 4** (diet/weight/stool + WSAVA hardening — 7 findings fixed across cold-read round-2 CLINIC-READY + adversarial round-3; the GP-0 confound note now names standing/overlapping interventions, free-fed contamination surfaces in Appendix C + B under a trial, off-diet double-counts closed) shipped #264 — all pure, `deno test`-covered, adversarial + cold-read-gated. **Phase 1 (the full functional clinical report) is COMPLETE.** **Phase 2: PR 5** (owner-facing MVP — authenticated `generate-report/index.ts` I/O shell + in-app WebView + on-device PDF share) shipped **#265** (`rls-privacy-reviewer` PASS on the authenticated boundary; `code-reviewer` fix-before-merge all applied; `generate-report` deployed, NEW fn `verify_jwt=true`). **2026-07-03: the FIRST REAL on-device artifact landed + a four-agent panel review + feedback round 1 (bug fixes) shipped** — see the Last-updated entry above + `docs/research/2026-07-vet-report-first-artifact-review.md`. **Phase 2 shipped since (corrected 2026-07-06 — this section had drifted; git is the source of truth):** the §5 PM decision menu was ratified (#267) → **B-221 round-2 design changes** (#268) → **feedback round 2** (severity cut, appendix reorder, wet-meals surfaced, protein-over-time chart, jargon + design pass — #273); **B-222** owner-facing range control (Default + Custom) shipped **#276**; **PR 7 — ALL incident photos in the authenticated report + PDF** shipped **#278** — every photographed in-window incident renders into Appendix E and bakes into the HTML/on-device PDF, safety-flagged (blood/foreign) photos also lead the safety band; `index.ts` fetches each photo ONLY through the EXIF/GPS-stripping + downscaling Supabase image transform (never the raw original), embeds as a `data:` URI (no signed URL persisted), service-role reads only paths from the RLS-scoped enumeration of the verified owner's pet; owner *visibility* = `photo_count` → `app/report.tsx` "Includes N photos". Gates: **`rls-privacy-reviewer` PASS**, **`vet-report-cold-read` CLINIC-READY** (fixed 2 bugs it caught — Appendix-E analysis↔photo divergence disclosed; chronicity `daysSinceLastEpisode` UTC→local, B-246), **`adversarial-reviewer` PASS**; no schema. **Deployed live as `generate-report` v8** (#281, byte-verified; folds in the B-222 zoom fix). Follow-on hardening from PR 7's rls-privacy pass: **B-244** (bind `storage_path` to pet + owner-scope the bucket read) shipped **#282** (CHECK applied to prod 2026-07-05) and **B-218** (drop the `vet_reports_public_share` anon-enumeration policy) shipped **#283**. **Phase 1 + the owner-facing MVP + authenticated photos are COMPLETE and LIVE.** **Genuinely next:** the PM **real-vet R1 email** (emails the regenerated Nyx report to their GP + books the appointment — a PM action; gates the irreversible public path); **B-243** — the interactive **"tap to exclude any photo"** pre-send review (PR 7 shipped owner *visibility* and deferred the interactive deselect; additive, no schema); then **PR 6** — the public token `view-report` route + revocable everyday-photo delivery (split-by-type / token-streaming) + owner link-revocation (B-143), the first unauthenticated path (`rls-privacy-reviewer` MANDATORY; its **B-218 blocker is already cleared** — #283). See `nyx-vet-report-requirements.md` v2.1 (2026-07-04) + §12.

---

## Parallel Tracks

### B-284 Culprit in-app brand alignment — N1 (tokens) + N2 (mark) + N2b (Landing) + N3 (loading system) + N5/N5b (calendar) shipped; N4/N6/N7 next
Spec `docs/culprit-in-app-brand-requirements.md` (v1.0, §10 = PRs N1–N7). Four locked rules (carve / register / one-accent / no-metaphor + motion budget). Night grounds appear **only where the app is working on the pet's behalf** (Landing, loading, the night moment, the Signal-card night variant) — capture & records stay the shipped light system. **Zero new dependencies** (`react-native-svg` + `expo-linear-gradient` already shipped).

| PR | What | Status |
|---|---|---|
| N1 | Night-surface tokens — 10 additive `theme.ts` entries (§2) | ✅ #321 (additive; no repoints; receipts verified; `tsc` + 1079 jest green) |
| N2 | `CulpritMark` (+ HomeHeader / AuthBrandMark) | ✅ draft #325 (carve mask-cutout, live pulse, header tap-to-scroll; `code-reviewer` caught + fixed a pet-switch race + a duplicate a11y node; new `store/signalMarkStore.ts` + `hooks/useReducedMotion.ts`; `tsc` + 1105 jest green) |
| N2b | Landing hero (+ full-bleed starfield) | ✅ #329 (`NightHeroGround` full-bleed starfield + carved moon) |
| N3 | `WhorlSpinner` + skeleton sweep + PTR + night moment | ✅ #333 (native-driver **View-transform** whorl [not `<G>` rotation — renders frozen], sm/md/px, day/night, `tint`; `Skeleton` Tier-1; ~30-site ActivityIndicator sweep; `NightGround`+`NightMoment` at cold-start/report/photo-extract; Home PTR band; `useAppActive`; `colorWhorlRidgeDay`. `code-reviewer` fix-before-merge ×4 applied; D7 scale/opacity + whorl-actually-rotates = on-device QA; CulpritMark same gap → B-322, **fix built this session (View-transform port), on-device confirm pending**) |
| N4 | Signal card (+ `SIGNAL_NIGHT_GROUND` on-device gate, D8) | ⬜ next (ships flag-false) |
| N5 | Calendar v3 — **presentation slice** (count-pips replace the heat-ramp + summary line + a11y) | ✅ #326 (pips + summary) |
| N5b | Calendar plumbing — month paging + drill-in + History day-filter (B-308/B-309) · symptom selector + "Calendar" rebrand + intake "Meals" lens (B-310) | ✅ #330 (paging + drill-in + single-day History filter) + #331 (B-310 selector/rebrand/intake "Meals" lens); coverage-gap (B-312) + med-colour token (B-311) still queued |
| N6 | Completion moment (ping + trial tick) | ⬜ |
| N7 | Home briefing (+ Tier-2 `design-principles.md` §3 edit, D9) | ⬜ merge-gated on §3 sign-off |

**N5 is disjoint from N2–N4** (calendar files vs. brand-mark / loading / signal surfaces) → ran as a separate concurrent session. **N5b is the deferred plumbing:** the mock's `pm-feature-review` (Jordan/Sam) caught that the buckets are symptom-only (no per-day all-event coverage, no multi-month, one auto-picked symptom), so paging + drill-in + coverage + selector each need a query/nav change — split out (B-308/309/310/311) with their designs locked to the PM's A/A/A calls.

### B-283 "You" (profile/settings) screen (app-store readiness) — PRs 1–4 shipped; PR 5 (go-live flips) next (external-gated)
Spec `docs/nyx-settings-requirements.md` (v0.1, §10 = 5-PR plan) + mockups `docs/settings-mockups.html`. Homes the App-Store items that assume an in-app settings surface (B-229/230/231/270) + the support email (B-273), and relocates the *user* actions (owner name / Sign out / Delete account) off the pet-scoped Pet tab. Entry = **avatar in the Home header** ✓PM; name = **"You"** ✓PM. **Zero schema** in every PR.

| PR | What | Status |
|---|---|---|
| 1 | Support + version primitives — `lib/support.ts` (`buildSupportMailto`, `formatAppVersion`) + `constants/links.ts` (`SUPPORT_EMAIL`, legal URLs, `LEGAL_LINKS_ENABLED=false`); pure, unit-tested, no UI | ✅ #315 (`code-reviewer` ship-ready; +17 tests) |
| 2 | The "You" screen (`app/settings.tsx`) + Home-header avatar entry + relocation off `app/(tabs)/profile.tsx` + route registration | ✅ merged #316 (`code-reviewer` fix-before-merge + `pm-feature-review` folded in; +7 owner tests) |
| 3 | Notifications (mocked — honest not-live state, **no armed med reminder** per D7 safety gate) | ✅ merged #317 (`code-reviewer` ship-ready; +shared `ComingSoonLabel`) |
| 4 | App feedback (`mailto` composer reusing PR 1's helper, `[Feedback]` tag) | ✅ merged #318 (`code-reviewer` ship-ready · `nyx-voice` ✓ · `pm-feature-review` SHIP-SHAPED after mail-hand-off fixes; +4 tests; S1 = reuse `support@`+tag) |
| 5 | Go-live flips (`LEGAL_LINKS_ENABLED` on when hosted docs land; real notif wiring; feedback backend) | ◐ **legal flip SHIPPED via #362** (docs live 2026-07-16; Privacy/Terms rows live + new Veterinary-disclaimer row); notif wiring (push-provider decision) + feedback backend remain external-gated |

**All build PRs (1–4) shipped** (PR 3 #317 · PR 4 #318; S1 resolved — reuse `support@` + `[Feedback]` tag). **Only PR 5 (go-live flips) remains** — external-gated (`LEGAL_LINKS_ENABLED` on when hosted docs land; real notif wiring on the push-provider decision; feedback backend if adopted), not a same-session build. Open PM sub-decisions from PR 4's `pm-feature-review` (spec §9): **B-297** (feedback via `expo-mail-composer` vs. raw `mailto`, applies to Contact support too), **B-298** (copyable no-mail fallback), **B-299** (in-composer support-vs-feedback reminder); plus S2–S4.

### B-251 Onboarding experience revamp (app-store readiness) — PRs 1–10 shipped; the flow is COMPLETE end-to-end; only PR 11 (functional social auth) remains
Spec `docs/nyx-onboarding-requirements.md` (v1.3, design-locked; §9 = 11-PR plan) + committed mockup `docs/onboarding-flow-mockups.html` (v3). Burn-down of v0.1 (2-screen name+species) → Signal-led **Landing** (logo + persistent auth + data-rich previews) → account (owner first/last/email; Apple/Google **mocked**, email functional; TOS/privacy mock) → pet setup (type+name **required**; breed/gender/age **skippable**; dual integer-OR-birthday age) → **mocked** paywall → completion + durable `onboarding_completed_at`. **PRs 1–4 shipped (#288 / #287 / #286 / #289):** PR 1 owner-profile schema (migration `027` + `updateOwnerName` helper) · PR 2 age-precision schema (migration `028`, resolves S2) · PR 3 design-system primitives (`TextField` + `PrimaryButton loading`) · **PR 4 onboarding shell** — the durable `onboarding_completed_at` gate (pure, unit-tested `decideOnboarding` §6 seam) replacing the fragile "≥1 pet" inference, `ProgressBar`, and the `_layout` back-nav chrome; `hooks/usePet.ts` rewritten (two-read orchestration + retry/never-false-onboard-on-error/throw guards, hook wiring test); `code-reviewer` fix-before-merge FIXED; no schema. **PR 5 shipped via #290:** the Signal-led **Landing** — `app/(auth)/index.tsx` (logo + a horizontally-paging stack of 3 value previews **leading with the Signal** + tappable ≥44pt page-dots as the a11y swipe-alternative + a pinned Create-account/Log-in) + `components/onboarding/ValuePreview.tsx` ×3 (Signal/log/vet-report, **built from real component styles** — elevated surface, Newsreader headline, `EventIcon` tiles, accent sparkline); `app/_layout.tsx` routes unauthenticated → Landing (B-039 just-deleted → login preserved); `PrimaryButton` gains a teal `accent` variant (PM-ratified over near-black, B-256). Reviews: nyx-voice ✓ / pm-feature-review SHIP-SHAPED / code-reviewer fix-before-merge applied (de-causalised the Signal sample per `generate-signal` `CAUSAL_RE`; page-dot 44pt). No schema; tsc + 942 jest green. Follow-ups B-257/B-258. **PR 6 shipped via #291:** account creation + soft verify — `app/(auth)/signup.tsx` reworked onto `TextField`/`PrimaryButton` (first/last/email/password + calm inline validation; mocked TOS/Privacy; Apple/Google hidden behind `SOCIAL_AUTH_ENABLED`; soft "check your inbox" state w/ resend + edit-email back-escape + proceed→login). `supabase.auth.signUp` → session-present writes first/last + derived `display_name` via `updateOwnerName` → `/onboarding/pet`. New `constants/flags.ts` + pure `lib/authValidation.ts`. Reviews: `rls-privacy-reviewer` PASS / nyx-voice ✓ / code-reviewer fix-before-merge applied. No schema; tsc + 959 jest green. Follow-up: **B-152** (harden the soft-verify path + 2 rls dashboard checks before email confirmation is re-enabled). **PR 7 shipped via #292:** the two REQUIRED pet screens — `app/onboarding/pet-type.tsx` (Cat/Dog tiles, no default → explicit tap → Continue) + `app/onboarding/pet-name.tsx` (`TextField` → inserts `{user_id, name, species}` → Home; PRs 8/9 UPDATE breed/gender/age onto the same row) + shared `components/onboarding/OnboardingHeader.tsx` (`canGoBack()`-aware back + `ProgressBar` steps 1–2 of 5) + new transient `store/onboardingDraftStore.ts` (type+name survive back-then-forward; reset on sign-out via `wipeLocalSession`, FR-9 parity). `usePet.ts` + PR 6's `signup.tsx` redirect repointed `/onboarding/pet` → `/onboarding/pet-type`; v0.1 `pet.tsx` + orphaned `food.tsx` deleted (enacts D3 — no food step). Reviews: nyx-voice ✓ / Designer (P1/P5) ✓ / Jordan + Sam ✓ / code-reviewer fix-before-merge applied (draft-store back-then-forward + null-species escape hatch + `radiogroup`). No schema. **PR 8 shipped via #293:** the two SKIPPABLE optional pet screens — `app/onboarding/pet-breed.tsx` (`BreedPicker` species-filtered + free-text "Other"; Continue UPDATEs `breed`, Skip → null) + `app/onboarding/pet-gender.tsx` (Male/Female `ChipGroup`; Continue UPDATEs `sex`, Skip → `'unknown'`, the insert default); `pet-name` now **pushes** `/onboarding/pet-breed` with an **idempotent** insert-or-update (no duplicate pet on back-nav); `OnboardingHeader` gains an optional prominent Skip (`onSkip`/`skipLabel`/`skipDisabled`); breed field-state seed extracted to `resolveBreedFieldState` (shared with `EditPetModal`). Reviews: nyx-voice ✓ / Designer (P1/P5) ✓ / code-reviewer fix-before-merge applied (Skip-vs-Continue save race) / pm-feature-review (follow-ups B-259/260/261). **Closes B-210**; no schema; tsc + 977 jest. **PR 9 shipped via #294:** the age screen — `app/onboarding/pet-age.tsx` (segmented Age/Birthday toggle; Years/Months numeric fields → approximate DOB anchored `today − duration`; `DateTimePicker` birthday → exact; "Not sure? Skip") + pure `lib/age.ts` (`ageToDob`/`birthdayToDob` + `formatAge`/`formatBirthdayField`, 25 tests). Gender repointed to push `/onboarding/pet-age`. **Honesty gate (S2):** approximate DOB never a witnessed birthday — enforced in `lib/age.ts`, applied on Profile (`~2yr`), `EditPetModal` (honest field + precision-preserving write), and the vet report (`generate-report` drops `(b. YYYY)` for approximate). Data/Dr. Chen honesty ✓; nyx-voice ✓; code-reviewer Ship-ready (fixes applied — `resolveDobPrecisionOnSave` extracted+tested, token fix; B-262 wheel-vs-field deviation). No schema (writes 028's column). tsc + 1011 jest + generate-report deno (160). **`generate-report` deployed v9 (2026-07-06)** from the PR-9 branch — the DOB-precision fix + the pending vet-report render work (B-222) are now live; verified (v9 `ACTIVE`, `verify_jwt` preserved, boot smoke-test clean 404 + 401). **PR 10 shipped via #295 (paywall mock + completion — closes the flow's interim terminus):** `app/onboarding/paywall.tsx` (non-functional dark premium mock — no StoreKit; "Maybe later" + a mocked trial CTA both advance; the trial CTA honestly acknowledges Premium isn't live rather than silently no-op; explicit Principle-7 free-tier line; feature bullets reworked to convenience-only after `pm-feature-review` — the mockup's `Multi-pet profiles` ships FREE / `correlations` = care / `90-day history` truncates free trends+report) + `app/onboarding/done.tsx` ("You're all set" → writes the durable `onboarding_completed_at` on mount via `lib/profile.ts markOnboardingComplete()` → `replace('/(tabs)')`; back-gesture-locked terminus) + dark-surface theme tokens; `pet-age finish()` repointed `replace('/(tabs)')` → `push('/onboarding/paywall')`. Reviews: `pm-feature-review` (done SHIP-SHAPED; paywall NEEDS-WORK findings applied → the two blocking product fixes), `code-reviewer` (ship-ready; both NITs fixed), nyx-voice ✓. No schema (writes migration 027's column). tsc + **1022 jest** green. Follow-ups: **B-263** (reconcile the mock's placeholder bullets to the ratified freemium gate), **B-264/265/266** (StoreKit wiring / placement / escape-hierarchy polish). **The onboarding flow is now complete end-to-end (no dead ends) — a build CAN be cut.** **Only PR 11 (functional Apple/Google)** remains — post-v1, needs PR 6 ✓ + a PM-provisioned Google OAuth client. Moots B-038. Gated PM items: `design-principles.md` §Onboarding ratification (doc-drifted — PR 7 removed the food step; spec §5 breed-skip copy also stale) + the Principle-2 softening (spec §12); freemium-gate decision (unblocks B-263); Google OAuth client for PR 11; **B-152**. **STATUS.md is the one shared-file collision to expect at each PR's wrap.** **2026-07-16 (#362):** the flow gained a post-signup **veterinary-disclaimer acknowledgment** step (B-270) — signup → disclaimer → pet-type, server-recorded in `legal_acceptances` (#361); D10's mocked TOS/privacy links are now LIVE (hosted docs shipped), and `usePet`'s needs-onboarding entry lands on the disclaimer.

### B-271 App Review demo account (app-store readiness) — build-ready spec written + D1–D7 ratified; PR 1 (seed script) next
Spec `docs/nyx-demo-account-requirements.md` (submission-guide step 11). **Why:** Culprit is login-gated, so a reviewer in an *empty* account hits only designed empty states and can't evaluate the Signal / Trend / Patterns / vet report — Apple's "unable to evaluate the app's features" rejection; the demo is also Apple's only window into the wedge. **Story (D1, ratified):** a diet-trial dog "Cooper" — a sneaked-in "contraband" protein genuinely correlates with vomiting (flagship ① correlation, Early tier) + a last-2-days intake dip (② `intake_decline`, safety) as a backstop so the Signal card is never empty; both clinically honest (the trial-food staple washes out; never reassures). **Mechanism:** the Home Signal is a server cache (`ai_signals`) → `generate-signal` must be **run** after seeding; Trend / Patterns / Timeline render on-device from hydrated SQLite (B-054). **Seed:** an idempotent, date-relative, **parameterized** SQL script (`scripts/seed-demo-account.sql`) referencing existing global `food_items` only; credentials only in ASC. **D1–D7 ratified 2026-07-11 (#336).** Build: PR 1 = seed script (`adversarial-reviewer` mandatory — "staple washes out / never reassures") → PR 2 = `docs/app-review-notes.md` → live seed + `generate-signal` + verify (PM-gated: the demo account is created via real signup, which needs email-confirm ON, guide step 9). Multi-account expansion = **B-324**. Depends on **B-152** (email confirm ON) + B-054 (done) + B-272 (ASC record, done).

### B-054 Multi-device down-sync (hydration) — v1 COMPLETE
Spec `docs/multi-device-sync-requirements.md`. Phases 0–3 + §6 cold-start UI all merged (#82–#86); migration 016 (`meals.updated_at`) applied live. Server-time LWW, incremental per-table watermark hydration, meal-ghost absence-reconcile. **Remaining: the physical AC-6 logout-wipe on-device gate** (Trust & Safety; code passes). Phase 4 (Realtime) is post-v1/optional. Residual assumption logged B-058.

### B-117 Medication logging — Phases A–C done; Phase D in flight (PR 9 shipped; PR 10 gated on Step 9)
Spec `docs/nyx-medication-logging-requirements.md` (§12 = 10-PR plan). Model = **regimen + dose-events** (mirrors food: `medication_items`≈`food_items`, `medications`≈`diet_trials`, `medication_administrations`≈`meals`+`intake_rating`). Net-new additive schema (migration 020 applied live; 021 med-photos RLS applied; `nyx-medication-photos` bucket created PRIVATE). Configure regimen once → one-tap dose log. Safety: adherence inherits both invariants (n=1 never reassures; refusal→health flag; missed critical-drug→escalate; AI-extracted dose never silently trusted).

| Phase | PRs | Status |
|---|---|---|
| A — Foundation | 1–3 | ✅ schema (#192) · local mirror+sync (#194) · text-first quick-log (#196) |
| B — Photo + library | 4–6 | ✅ bucket+RLS (#197) · capture + `extract-medication-from-photo` + dose-confirm (#199) · picker detail/edit (#201) |
| C — Regimen + surfaces | 7–8 | ✅ "Current medications" card + compliance % (#202) · timeline + retroactive adherence + double-dose B-135 (#204) |
| D — Clinical consumers | 9–10 | ✅ PR 9 Signal confounder pass (#207 — `detection.ts` `medicationWindows`; case-enriched drug → suppress, concordant → cap at Early; adversarial PASS, fixed a Bonferroni family-shrink; residuals B-138; deployed v19) · ⬜ PR 10 vet-report section (gated on Step 9) |

Carry-forwards: B-122/123 satisfied; B-131 honored; B-128(b) defense-at-rest trigger (own schema PR); open sub-decisions B-132 (library delete), B-133 (`is_critical` owner toggle — PM call); **B-160** (med-details helper chips + copy — ✅ shipped #236).

### B-023 Patterns dashboard — PRs 1–4 merged; PR 5 blocked on Step 9
Spec `docs/nyx-analytics-dashboard-requirements.md`. **No schema** (rides existing tables + `ai_signals` jsonb; migration 018 added an additive `summary`). Build gates resolved: §13 #1 name = "Patterns", §13 #6 colour-as-wellness ruling.

| PR | What | Status |
|---|---|---|
| 1 | Aggregate layer `lib/analytics.ts` + shared `lib/protein.ts` | ✅ #155 |
| 2 | Card components + §13 #6 colour ruling | ✅ #158 |
| 3 | Dashboard screen + Home doorways + `ComingSoonSummary` | ✅ #159 |
| 4 | AI summary — **template-only** (Haiku path built but gated off = B-096); migration 018 | ✅ #160, deployed live |
| 5 | Vet-report bridge ("Share with my vet") | ⬜ blocked on Step 9 |

Open follow-ups: B-094 (decline-watch/diet-trial cards), B-095 (correlations/coverage cards), B-096 (re-enable Haiku summary), B-099 (over-time + dashboard range — reopens §13 #2), B-116 (summary↔card grounding). Shipped polish: #162/#163/#164/#185.

### B-086 Multi-pet v1 — COMPLETE
Spec `docs/nyx-multi-pet-requirements.md`. PRs 1–6 all shipped (#144–#148, #203); **zero schema** (`pet_id`/RLS/`is_active`/`feeding_arrangements` were multi-pet-ready by construction). Single-pet households see no new chrome. PR 6 = cross-pet safety banner + all-active-pets signal freshness (adversarial PASS). Household §7 on-device cases fold into the TestFlight QA pass.

### B-040 Free-feeding R1 — COMPLETE
Spec `docs/nyx-free-feeding-requirements.md`. PRs 1–4 shipped (#119/#121/#122/#123); migration 018 applied live. Schema → capture/view → History rendering (ambient strip + boundary markers + stale-only freshness) → engine ingestion (free-fed protein excluded from candidacy, caps others at Early; adversarial PASS). **Guardrail (every consumer):** free-fed intake is NOT directly observed — never read absence of witnessed intake as "didn't eat"; vet report carries "intake not directly observed" verbatim. Remaining Step 9 consumer: vet-report feeding-method rendering. Open: B-073 (stale-nudge on-device QA), B-074 (exclusion can silence a real finding — awareness).

### Food library / intake
- **Picker findability — ratified build queue (2026-07-15, #351):** **B-346** rotation shelf → **B-347** pinned search + chips (executes B-020); sequenced (same `FoodPicker.tsx`), both no-schema, ready to run. Photos held (B-348); blue-sky directions parked behind B-349/B-350. Design docs: `docs/food-picker-findability-mockups.html` + `docs/food-picker-reimagined-mockups.html` (both carry the PM decision record).
- **B-004 standalone Food Library — COMPLETE** (#176–#182, 7 slices). 4th `Foods` tab, brand grouping, reliable-favorites, row thumbnails; picker stays 2-up tiles (PR 7 decision). + post-QA fixes #183/#184.
- **B-102 `human_food` food_format — in progress** (PRs 1–2, #166/#167). Migration 019 applied live + capture/display parity. Remaining PRs 3–7 (AI-extraction mapping, analytics regression, engine provenance, vet-report line, diet-trial note) — PRs 3/4 are disjoint and parallelizable.
- Open QA findings: B-111 (treats in Top Proteins, clinical call), B-113 (picker↔tab harmonization, #186), B-112/B-114, B-108 (closed #183). Downstream: B-017 (`food_format`/`food_type` overlap, carries B-024/B-102), B-009/B-018 dedup.
- **Food dedup & merge — direction aligned + build-ready DRAFT spec (2026-06-24, #237):** `docs/nyx-food-dedup-requirements.md`. Phase 1 prevention (B-009) first, **no schema**; Phase 2 merge (B-018 + B-005) = repoint-then-tombstone. Survivorship/reversibility/multi-user-auth parked as merge-phase decisions. Awaiting PM ratification → re-prioritizes B-009 to Now.

### B-186 Weight tracking — PRs 1–4 shipped (schema + logging + trend card + History row); v1 = display-only COMPLETE
The 2026-06-25 vet-council deep-dive named a weight **trend** the #1 missing datum (Nyx = zero `weight_check` events on a free-choice *weight-management* diet). **Audit (2026-06-26):** confirmed no existing weight surface — `weight_check` enum + `pets.weight_kg` snapshot exist, but **no value storage** (`events` carries only `severity`/`notes`), no logging UI, no trend surface. **v1 = display-only / descriptive (PM-confirmed): capture + trend, NO loss flag** (a flag is clinically load-bearing → its own spec + mandatory adversarial pass). **Guardrail every consumer inherits:** a weight trend never reassures — stable/rising ≠ wellness (rising can be fluid/edema); loss is the danger signal. Independent of the chronicity-lane work (disjoint files; only STATUS.md collides at wrap).

| PR | What | Status |
|---|---|---|
| 1 | Schema/data layer — migration 024 `weight_checks` (1:1 event child, mirrors `meals`) + local SQLite mirror + `LOCAL_WIPE_TABLES` reg | ✅ merged #244 (applied live, advisors clean) |
| 2 | Logging — `EVENT_TYPES` entry + numeric `weight` log step (pre-filled last weight, lbs) + `insertWeightCheck` + sync up/down + `pets.weight_kg` snapshot to latest reading | ✅ this session — PR #245 (was stacked on PR 1; rebased onto `main` after #244 merged) |
| 3 | Profile weight-trend card (descriptive sparkline + readings + delta; neutral framing, no wellness colour) | ✅ shipped via #248 — `WeightTrendCard` (`getWeightHistory`/`computeWeightTrend` in `lib/weight.ts`; "Log a weigh-in" affordance; empty/single/trend states; guardrail held in colour + copy) |
| 4 | History `weight_check` row + icon | ✅ shipped via #249 — `weight_checks` LEFT JOIN in `getTimeline`/`getEventById` (1:1, soft-delete via parent) + `weight_kg` on `TimelineRow`/`rowToEvent`; `EventRow` renders `${kgToLbs} lbs` as a quiet secondary line (Scale icon already via `EVENT_TYPES`, PR 2); display-only, no verdict |

Fast-follows: **Patterns-dashboard weight card ✓ shipped via #251** — `components/dashboard/WeightCard.tsx`, reading-count-aware placement (populated → safety/health-trajectory cluster, empty nudge → descriptive); shared delta/date copy extracted to `lib/weight.ts`. Remaining: vet-report weight section (Step 9); Home TrendZone weight; weight-loss flag spec (B-190). Card follow-ups: B-192 (empty-state coherence), B-193 (4px token).

### B-247 Stool analysis (per-incident AI read) — spec drafted, awaiting ratification
Spec `docs/nyx-stool-analysis-requirements.md` (§8 = 10-PR plan). Second child of B-013 (sibling of B-027/vomit): no `analyze-stool` exists today despite stool being the primary diet-trial outcome and the highest-volume GI event type; the vet report's "Stool characteristics" section has no AI-read data source. Additive schema only (new `stool_*` enums + columns on `event_ai_analysis` — **no** `stool_normal`/`diarrhea` consolidation, D1). Bristol Stool Scale (Type 1–7) as the consistency taxonomy (D3); Sonnet 4.6 (D4, matches `analyze-vomit`). **Open for ratification before PR 1:** D2 — factor vomit's escalation-floor/image-processing/write-back patterns into a shared `_shared/incident-analysis.ts` module (the `clinical-guardrails` skill's own predicted "second incident type" refactor point) vs. build `analyze-stool` as a standalone copy — Dir. of Engineering call. Draft PR #279 (docs only, not yet merged).

### Design-system v1.2 "Linear Clean" — COMPLETE
Plan `docs/design-system-migration-plan.md`. 4 PRs merged: palette (#99), fonts Geist+Newsreader (#100), Lucide event icons (#101), completion "moment" (#103) + B-064 folded meal card (#105). Open: B-061 app-wide Geist body rollout (open question — wrapper vs shim); B-063 tone-aware-moment design-principles line (PM sign-off). Whole-system aesthetic on-device QA still due.

---

## Blocking Open Questions

- **~~D-M8~~ — multi-pet tiering: RESOLVED 2026-07-12 same day — PM ruled free-to-3, Premium at 4+ (the team rec).** No open monetization decisions remain; next = the build-ready spec session. Original framing: (a) all multi-pet free / (b) free-to-3, Premium 4+ / (c) gate at 2+. Blocks the Track-3 paywall copy + B-263 bullet list + the completion-screen "add pets anytime" line. Full workshop: strategy doc §18. **D-M1–D-M7 RESOLVED 2026-07-12 (PM ratified, with amendments — doc §13).** Track-2 (B-329 flags → B-001 throttles → B-330 mock flag-off) is now ready-to-run; Track-3 additionally gated on B-332 (manual protein capture). Superseded detail — original bullet: Blocks: the freemium gate (D9/B-263), the Premium wiring track (B-331/B-264/B-265/B-266), and the pre-submission Track-2 PRs (B-329 flags, B-001/ai_usage throttles, B-330 paywall-mock flag-off). Decision set + team recommendations: `docs/monetization-and-ai-gating-strategy.md` §8. The one genuinely-contested call is **D-M2** (descriptive vomit-read tier — surfaced persona conflict, doc §7). NOT on the submission critical path itself (B-039/B-229/230/267–273/280 proceed regardless), except B-330 which is a submission blocker once a build is cut.
- **~~PDF rendering library / §8.2 HTML-first~~ — RESOLVED 2026-07-02 (#260).** PM ratified **HTML-first**: canonical server HTML, rendered **in-app via a WebView** (owner sees it in the app — never a downloaded `.html`), handed to the vet as a **PDF via the native share sheet**. The "which PDF library" Q is demoted to the B-144 render spike; PDF-generation *location* (on-device `expo-print` vs server-side) is a build-time sub-decision (spec §14 S7). **No blocking open questions remain for the vet report — Phase 1 (PR 1) is clear to start.**

---

## Open PM Action Items

**Monetization (Track-2 pre-submission)**
- [ ] **Paywall pre-submission flip (T2-5, B-330) — recorded config change, not a deploy.** Before cutting the submission build, set `paywall_enabled=false` in the live `app_config` (Supabase dashboard SQL Editor or MCP: `UPDATE app_config SET value='false'::jsonb WHERE key='paywall_enabled';`) so the non-functional mocked trial CTA (a Guideline 2.1/3.1.2 rejection risk) is never reachable in the submission build. The T2-5 code gate is already merged + ship-dark (seeded `true` ⇒ current flow unchanged until you flip it). **Record the flip here in STATUS when done.** Reversible — set back to `true` to restore the mock (e.g. once T3-D un-mocks the paywall).

**Monetization (Track-3 prerequisites — sequence-block RevenueCat wiring, NOT submission)**
- [ ] **Offline actions 1–4** (`docs/monetization-and-throttling-requirements.md` §10): ① App Store Connect → Agreements, Tax, and Banking (decide the legal/merchant entity first; **highest leverage — external latency, everything paid stacks behind it**) → ② Apple Small Business Program enrollment → ③ RevenueCat account + iOS SDK key + webhook secret (→ Secrets Register at T3-C) → ④ subscription products ($4.99/mo, $39.99/yr + 7-day trial; **checkpoint C2 — the D-M5 final price lock**).
- [ ] **B-047/B-016 ownership decision (surfaced by the #344 review) — gate for T3-E, currently unowned.** Both instrumentation items (B-047 AI-Signal conversion/time-to-first-insight = `Next`; B-016 error observability = `Later`) hard-block the extraction gate flip (§9 T3-E, §12 #6) yet appear in **neither track's PR plan**. Decide: (a) pull the minimum conversion + time-to-first-insight events into Track 3 as an explicit owned PR, or (b) run B-047/B-016 as a named parallel prerequisite track. Left unresolved, T3-E silently slips. *(Not on the submission critical path — only gates the post-launch Premium gate flip.)*
- [ ] **Entitlement grace-window duration — confirm at T3-C build time** (`monetization…requirements.md` §4.7; provisional **72h**). Money on both sides (too long = post-cancel free-riding; too short = a paying owner loses Premium in a dead zone). Not a silent engineer default.
- [ ] **Server-side config-read failure fallback — Dir. of Eng sign-off at T2-3** (§4.2; provisional = mirror the client per-key posture, AI keys fail *open*). Currently unspecified in the ratified strategy; an engineering ratification, not a re-decision.
- [ ] **§4.5 shared response decoder — PM call (parked from the #344 review).** Whether to mandate one `lib/aiResponse.ts` `parseAIResult()` type-guard for the typed 200-body contract so cap/flag discrimination isn't reimplemented per caller. The one remaining *structural* review gap; two-line addition to §4.5/T2-4 if adopted.

**Ship gates / on-device QA**
- [ ] **#361 + #362 (in-app legal wiring) — on-device QA + merge.** Run #362's Manual QA: fresh throwaway signup → "Before you start" acknowledgment → `legal_acceptances` row lands server-stamped (Table Editor); Terms/Privacy open live from the signup acceptance line; You screen's Privacy/Terms/**Veterinary disclaimer** rows all open live pages (no "Coming soon"); airplane-mode acknowledgment shows the calm retry and never advances unrecorded. Merge #361 then #362 (the migration is already applied live, so order is tidiness, not correctness).
- [x] **#276 (B-222 range control) — MERGED + `generate-report` deployed v8.** Client parts (Default/Custom, "Done" toolbar, soft-refresh) verified on-device by the PM this session; the #3 zoom fix is now live post-v8. **One residual device check** (not a blocker): on an **iPhone SE**, confirm "Done" + the "Updating…" pill are reachable without scrolling with the calendar open (the one thing pm-feature-review couldn't settle from code).
- [x] **`generate-report` REDEPLOYED → v8 · ACTIVE (2026-07-05)** — the PM's `npx supabase functions deploy` from current `main` landed the B-222 zoom fix (#276) + the PR-7 incident-photos render (#278) in one deploy (`verify_jwt` preserved; sha `292e3b4f…`). **Remaining device confirm** (not a blocker): regenerate on device → report opens **fit-to-width** + eyeball one PDF's pagination is unchanged.
- [ ] **B-222 follow-up decisions (all low-stakes, backlog-tracked):** **B-240** — which weight surface is right (Profile snapshot vs Patterns tracked-only); first confirm via the Profile card subtext ("From Nyx's profile" = snapshot = expected). **#2 behavior** — keep the smart default cascade (rec) vs force 90d always. **B-238** — add a pre-generation out-of-window hint or leave the disclosure in-report. **B-241** — "Default" vs "Automatic" chip copy.
- [ ] **#257 — on-device QA + squash-merge (this session).** Run the Manual QA in the PR: **B-196** (Current-medications card loads, no `PGRST201`), **B-198** (log while History is open → no duplicate-key error), **B-197** (Edit a weight → value field pre-filled + saves; empty/`0` blocks Save), plus a spot-check of weight tracking + the chronicity card. Merge when green. **No backend action** — client-only fixes; `generate-signal` v23 (with ⑦) already live.
- [ ] **#255 `analyze-vomit` + photo-load fixes — on-device confirm (the one open PR #255 gate):** open the **6/25 and 6/27 vomit events** → each shows a **real AI read** (not stuck "Try analysis"); tapping the photo opens full-screen **fast** (not 5–10 s). If a photo instead reads "I couldn't read this photo," the Pro Image-Transformation add-on isn't active (safe no-op — flag me). Verifies the 546 fix + server-side downscale + viewer perf end-to-end.
- [x] **B-039 — DONE (2026-07-16).** On-device end-to-end deletion **verified** this session against a real account (`d.mark@hey.com` / pet "Yeti"): live-DB QA confirmed auth user gone, **0 rows across all 13 cascade tables**, no residual Storage for the deleted user, `food_items` SET-NULL intact (AC-3/4/5); deployed `delete-account` fn == repo (incl. B-127/B-128), ACTIVE, `verify_jwt` true. **The App Store 5.1.1(v) blocker is closed.** Rider still open: **AC-6 cross-account logout-wipe** (line below) + a formal `rls-privacy-reviewer` re-run / `deno` suite run for a spotless DoD stamp.
- [ ] **Toggle Supabase "Confirm email" OFF** (Auth → Providers → Email) — was needed to create a *throwaway* account for the B-039 QA; that QA is now done (verified against an existing account), so this is **B-152-only now**: re-enable + provision production SMTP before launch. New-account creation silently failed under email-confirmation (fixed in #213 to show "Check your email").
- [ ] **#213 med-QA fixes — on-device spot-check** (Manual QA in the PR): account-creation "Check your email"; strength-gate toggle; "Current medications" counts a logged dose; End-button red; no `meals 23503` on logging an existing food.
- [ ] **AC-6 logout-wipe** — sign out on a populated device → confirm data empties → sign into a *different* account → confirm no prior-pet data shows (Trust & Safety gate; code passes).
- [ ] **App Store readiness — work the 16-step runway in `docs/app-store-submission-guide.md` (#300; register: `docs/app-store-readiness.md` #297+#300).** Start-now PM steps (all parallel, long-lead first): **step 1** verify the ASC record + lock the store name (B-272) · **step 3** pick a host for the support/privacy/terms page (B-273) · **step 4** provision production SMTP (B-152). Ratify the two advisor recommendations riding the runway: **flag the paywall off for v1** (guide step 6 — removes the freemium decision from the submission critical path) and **`supportsTablet: false`** (rides step 5's B-267 config PR). Each guide step has a kickoff prompt (PR steps) or a "Guide step N complete: …" confirmation (PM steps) that keeps the tracker/backlog/STATUS honest.
- [ ] **Culprit rebrand — remaining items** (`docs/culprit-rebrand-execution-plan.md`, #305): PR A (name+strings) shipped #307; **PR B (Moon & Signal icon + splash) shipped #309** — the design-master gate is **cleared**. Remaining: (1) **PR C** (`colorBrandNight` indigo token) — ready to build, no PM prerequisite; (2) **Supabase auth email template** still says "Nyx" (dashboard → Auth → Email Templates) → Culprit; (3) **cut a new TestFlight build from `main` after merging** to make the icon/splash/name visible — they're native/build-time, so an OTA `eas update` won't move them (`eas build --platform ios --profile production --auto-submit`); do the **iOS-18 dark/tinted on-device verification** on that build (unverifiable in the cloud session; icons don't render in Expo Go).
- [ ] **B-267 built-artifact verify (rides the next iOS build-cut; #299)** — the permission-string config fix is merged, but introspect can't check the built plist. On the next `expo prebuild`/EAS build, confirm `ios/*/Info.plist` carries both `NSCameraUsageDescription` + `NSPhotoLibraryUsageDescription` (Culprit copy) and **not** `NSMicrophoneUsageDescription`, **and** that the aggregated `PrivacyInfo.xcprivacy` privacy manifest is present (Expo SDK 54 auto-aggregates — verify, don't assume). Do NOT assume EAS autolinking supplies them. Batches with the Culprit-rebrand build-cut (item above) — same build.
- [ ] **B-053 on-device QA** — Signal regens against v10; the staple-washout diagnostic only shows in `no_pattern` (Nyx may show the reflection finding instead — correct, not a bug).
- [ ] **Confirm AI summary on-device** — open Patterns for Nyx; expect a safety-led, vet-routing summary with every number matching a card.
- [ ] **Whole-system aesthetic on-device QA** — walk the app vs `docs/design-system/_system/.../index.html` for the holistic Calm/Linear/Oura pass (deferred to end-of-system; all 4 design PRs merged).

**Decisions**
- [ ] **B-325 — promote retroactive/med-first combo linking as part of B-156, or fold direction ① (med→existing-treat) in before promotion?** Rides the B-156 promotion call. Investigation + recommended shape logged as B-325 / #338 (retroactive entry on the treat/meal detail screen; run the same PR B3 safety coupling; never mint a phantom vehicle event). Direction ① is the cheap, high-value slice (reuses the forward write path + safety coupling); minting a new treat from the med side is a scoped follow-up gated on the pure-vehicle question.
- [ ] **B-311 medication colour (#332) — ratify the design-system addition + decide B-323.** A new event-category hue (`colorEventMedication #5B7A9E` slate-blue) shipped respecting §1.3 (not teal/indigo/rose; selection stays teal), applied to the drill-in + Today strip. Two low-stakes calls: (1) sign off on the slate value (or redirect it) — adding a category hue is a design-system decision, not a free choice; (2) **B-323** — should History `EventRow` adopt the full symptom/meal/med tint (parity) or stay deliberately symptom-only? On-device: confirm the med glyph reads slate on both the Today circle + the drill-in grey circle, distinct from symptom-rose/meal-teal.
- [ ] **Logging-capture discovery (#319) — 2 ratifications after reading the brief** (`docs/logging-capture-discovery.md` §9; CLAUDE.md Open Questions 2026-07-10 — PM saw all three at wrap, chose to read the brief first): (1) owner-configured scheduled confirmations vs Principle 4's one-nudge cap (Designer dissent on record — channel trust is one bucket; proposed carve-out = opt-in + fail-safe silence + self-pruning) → unblocks **B-288**; (2) household shared-write minimal primitive as capture *infrastructure* (not a social layer) → unblocks **B-292**. (3) native-targets-in-managed-workflow was **sequencing-CONFIRMED same day: decide after B-288 pilot data** — nothing to do until the pilot reports. Also answer when convenient (non-blocking, sharpens the build): your household's meal-time regularity; does your wife carry iPhone + Watch; what Dog Recruit's logging feels like at its best (B-296 teardown seed).
- [ ] **Login consolidation (#311) — one decision + one device check.** (1) Is a **"Forgot password?" flow (B-280)** in scope now, or left as the tracked backlog item? A login-gated app really wants password reset before App Store submission — it's the sharpest returning-owner gap. (2) **Device check** (unverifiable from code): after **account deletion** → on the login screen press **back** → confirm you land on the Landing, not a flashed empty authenticated tab. Also note **B-281** (social-auth block absent from login) must be resolved when PR 11 flips `SOCIAL_AUTH_ENABLED`, or an Apple/Google-signup owner can't log in.
- [ ] **B-251 onboarding — ratify the `design-principles.md` §Onboarding revision (Tier-2 edit, flagged not written)** before/around PR 4–5. The constitution still says "minimum viable / required: name+species / first log in 60s / complete-at-first-log"; the design-locked flow outgrows all of it (owner identity + optional pet richness + store gates; finish line moves to profile-created→warm-home) and Principle 2's "food library pre-populated from onboarding" should soften to "on first meal log" (food capture moved in-app). Also: **Google OAuth client** to provision before **PR 11** (Apple Developer already enrolled) — not blocking PRs 1–10.
- [ ] **B-247 stool analysis spec — ratify §2 decisions (esp. D2 shared-module refactor)** before PR 1 starts. `docs/nyx-stool-analysis-requirements.md`; draft PR #279.
- [x] **B-070 deploy mechanism — RESOLVED 2026-06-21 (PM chose pre-verified MCP deploy).** Deployed `generate-signal` v20 via the B-082 MCP path: authored the 80 KB bundle, proved it byte-identical to `scripts/deploy-edge.sh`'s artifact (sha `a4759d20…`) via a scratch-file round-trip BEFORE deploy, then confirmed the live read-back is byte-identical + a clean boot smoke-test. **B-082-path finding (logged, low urgency):** the inline-content transcription is only safe with the scratch-file sha gate; provisioning a `SUPABASE_ACCESS_TOKEN` cloud-env secret so `supabase functions deploy` reads the file directly is the durable fix for large clinically-load-bearing functions (runbook's "optional future convenience").
- [ ] **B-099** — reopen spec §13 #2 (over-time views + dashboard 7d/30d range) before building B-099.
- [ ] **B-080** — placement of diet-structure observations (Signal band-2 vs B-053 coverage lane); blocks descriptive Phase 3.
- [ ] **B-023 colour** — reconcile Home `TrendZone` (colours a falling symptom "improving" in the accent, `TrendZone.tsx:66/79`) to the §11 #3 ruling, or accept the cross-tier difference.
- [ ] **B-065** — should the food-capture add-then-log path also show the meal completion card (intake chips + "Change time")?
- [x] **B-160** — BUILT #236 on the spec's recommend-and-proceed basis (§6): §3 **strength = no value chips** applied (the §6.5-derived call — no PM gate); **S5** regimen modal included; **S1** Dr. Chen starter-set trim applied ("Gabapentin / Trazodone" → Trazodone, since Gabapentin is already listed). _Optional post-hoc:_ PM may tweak the `COMMON_MEDICATIONS` contents/ordering (`lib/medications.ts`) — purely additive data, no rebuild needed.
- [ ] **B-063** — approve the Tier-2 design-principles line (tone-aware "moment" — calm, never festive on a worrying event); awaiting PM sign-off before writing.
- [ ] **PR #79** — env fail-fast guard + CLAUDE.md doc fix: mark ready/merge or close.
- [x] **Vet-report owner band (§8.1 / R4) — RESOLVED 2026-06-22 (PM): band removed; report is vet-only (Strawman A).** Owner's surface = Patterns dashboard (B-023). §8.1 closed → option (a); formalize in the requirements spec.
- [x] **Vet-report spec gates — RESOLVED 2026-07-02 (#260).** (a) **§8.2 HTML-first ✅ ratified** (in-app WebView render + PDF-via-share-sheet locked; PDF-library Q → B-144); (b) real-vet gate **reframed to n=1-first** — PM emails the Nyx report to their own GP + books an appointment (in parallel; before Phase 2); (c) **cat/safety-led mock ✅ built** (`vet-report-mock-cat.html`) + v3 + the Nyx real-data dry-run. Spec is **build-ready v2**; Phase 1 (PR 1) is clear.
- [ ] **Vet report — PM next actions (2026-07-02):** (1) **email your vet the Nyx report + book the appointment** (the real-vet signal, parallel to the build); (2) **weigh Nyx** before the visit (fills the one empty box + the datum the vet most wants).
- [ ] **Principle 6 doc-drift (Tier-2 edit)** — `nyx-design-principles-v1_0.md` §6 **and** `nyx-technical-spec-v1_0.md` §7 AC both still list "severity averages" as desired report content; the mock + spec §8.4 deliberately use frequency-over-severity (Dr. Chen's stated preference). Both cold reads flagged it. Approve the doc-line update on **both** files (+ the §7 must-carry-section / HTML-first / PDF-library-reshape edits the spec §13 flags — all flagged, not written).
- [ ] **Vet-report mock — synthetic-panel feedback logged** (`docs/vet-report-mock-review.md`). PM follow-ups from it: (a) **B&W test-print** the mock (zebra/band gray on a mono laser); (b) decide the spec-level **report-range cherry-pick guard** (disclose events outside a custom window); (c) greenlight the **second mock — cat/safety-led case** (Sam, feline 48h window) to exercise the safety-leads slot. The real-vet R1/R2/R4 panel is the gate (§5 of that doc is reserved for it).

**Cleanup / infra**
- [x] **B-118** — DONE 2026-07-16: leftover `smart-worker` Edge Function deleted from the dashboard (stock template, `secret`-auth RLS-bypass path). Verified gone via `list_edge_functions` — absent from the live project, not in repo. (Sibling of the closed B-043.)
- [x] **B-044** — DONE 2026-06-22: full live-DB reconciliation — **zero schema drift, nothing to apply.** `vet_visit_attachments` already present (003 gap closed 2026-06-06 by `complete_003_vet_visit_attachments`); `food_items.photo_path` singular is dead (superseded by 007 `photo_paths`; no code reads it). All 22 migrations' objects verified live. History records only 5/22 (001–019 dashboard-pasted, unrecorded) → backfill spun out to **B-162** (latent `db push` re-apply footgun; PM-blessed, low urgency).
- [ ] **B-128(b)** — defense-at-rest `BEFORE INSERT/UPDATE` trigger on `medication_items` (own schema PR; run the backfill pre-check first). Not urgent — the consumer-side guard is live via #200.
- [x] **Deploy `generate-signal` PR C1 (#234) from merged `main`** — DONE (#239): deployed **v22** from merged `main` via the Supabase MCP (`verify_jwt` preserved); live read-back sha byte-identical to the bundle (`0e66e002…`) + clean boot smoke-test. A PM-requested edge-function currency audit also caught + fixed **`analyze-vomit` v4→v5** (B-028 #220 merged but never deployed) and redeployed **`extract-food-from-photo` v9→v10**; `delete-account` v3 / `extract-medication` v1 verified current. Process gap → **B-178**.
- [ ] **Redeploy `generate-signal` to un-gate chronicity detector ⑦ (B-182)** — PRs 1–3 (#246/#247/#250) all merged; the engine registers ⑦ but the **live function predates it**, so chronicity won't surface until a redeploy. Deploy from merged `main` via the Supabase MCP (bundle `scripts/deploy-edge.sh generate-signal` → `deploy_edge_function`, preserve `verify_jwt=true`, byte-gate + boot smoke-test — the B-082 path). The client renderer is live as of #250, so the card renders the moment the cache carries a `symptom_chronicity` finding.
- [ ] **D2 ratification — chronicity `minEpisodes` 6-vs-5 (Dr. Chen)** — the §7 #14 noise gate forced 4→6 (specificity); the PR-1 adversarial review measured a false-negative cost at 6 (a once-weekly-×5 / q2wk-×4 real chronic course is silenced). 6 = credible-surface; 5 = catch-the-once-weekly, defensible because the safe error direction for a safety lane is toward firing. A property test on *real low-count chronic courses* should sit alongside the noise test before the floor locks.
- [x] **Re-deploy `generate-signal` from merged `main`** — DONE 2026-06-21: redeployed **v21** from merged `main` (447af3f) after #211 merged; live read-back byte-identical to the merged-main bundle (sha `a4759d20…`) + clean boot smoke-test. (v20 from-branch had the same bytes; this is the clean-provenance bump.)
- [ ] **Revoke the Supabase personal access token** (`nyx-cli-deploy`, 2026-06-07) — account-level, lives in a session transcript. Now obsolete: B-082 (#208) made backend deploys a token-free MCP path, so nothing uses it.
- [ ] **Supabase CLI dev-dependency** — fold `supabase@^2.102.0` (on branch `claude/epic-volta-H8d6o`) into a PR so it survives merge.
- [ ] _(awareness, no action)_ **B-074** — the free-fed exclusion fails safe but a single stray free-fed day landing on a selected control day can silence a real correlate.
- [ ] _(awareness, before treating vet-report §8.6 as fully closed)_ **B-115 residual → B-163** — the shipped exact-ms treat-collapse (#219) guards the diet-confounder over-count for the **EXIF-collision / future-batch** case only; today's full-precision `new Date()` stamps mean a **rapid per-tap handful (distinct ms) is still over-counted**. Fully closing it needs a real-data-gated near-window widening (under-count tension) or a batch-log path — Phase-2/PR-6 can proceed; just don't read §8.6 as "fully resolved."

---

## Runtime in Use

**TestFlight (real iOS builds) — primary on-device target since 2026-06-07.** Runtime B (Metro `npx expo start --tunnel` + Expo Go) remains available for fast local iteration.

**Cut a new TestFlight build** (verified build 22 from `main`, 2026-06-12) — one command builds the store-signed binary and submits that same artifact:
```
eas build --platform ios --profile production --auto-submit
```
**OTA to the installed build:** `eas update --branch production` (the build's channel is `production`, NOT `preview`).

**Traps (each cost a session — do not repeat):**
- Never build TestFlight with the `preview` profile — it's `distribution: internal` (ad-hoc), never store-submittable. Use `production`.
- Never submit with `eas submit --latest` — it skips internal builds and re-uploads a stale store build (the "build number 8 already used" loop). Use `--auto-submit` (binds the upload to the binary just built).
- "Build number N already used" = EAS counter is behind App Store Connect → `eas build:version:set --platform ios`, then rebuild. (Build numbers live on EAS; `appVersionSource: remote`, `autoIncrement`.)
- All 3 `eas.json` build profiles carry the `EXPO_PUBLIC_SUPABASE_*` `env` block (#90) — EAS cloud builds never see the gitignored `.env.local`; don't strip it (was the first crash-on-launch).
- `app.json` → `ios.infoPlist.ITSAppUsesNonExemptEncryption: false` (#121) auto-answers Apple's encryption questionnaire; keep it committed.

---

## Recent Sessions

_Last ~13 only; older history lives in git (`git log`) + PR descriptions._

- 2026-07-16 — **B-191 — `symptom_chronicity` (⑦) added to the cross-pet safety banner allow-list (Step 10 evolution; client-only, no schema, no Edge Function, no `generate-signal` redeploy)** — B-182 PR 3 made chronicity a renderable SAFETY finding on a pet's own Home Signal, but the cross-pet banner (`selectCrossPetSafetyFinding` / `BANNER_SAFETY_PRIORITY` in `lib/signalCopy.ts`) was a deliberate explicit allow-list (`intake_decline` + `symptom_worsening` only) → a NON-active pet whose only safety finding was a weeks-long unresolved course raised no banner on the active pet's Home. Closed that cross-pet escalation gap (safe-direction: the gap was under-escalation on a secondary surface — the finding still led on that pet's OWN Home, never a false all-clear). Changes: `BANNER_SAFETY_PRIORITY` now mirrors the engine's `SAFETY_TYPE_ORDER` **exactly** (`intake_decline` 0 > `symptom_chronicity` 1 > `symptom_worsening` 2); the union/type-guard widened; a new `bannerRest` chronicity teaser anchored to the onset month ("{pet} has had recurring {symptom} since {month} — worth a look"), descriptive/duration-only (never causal/alarm/resolution) + screened by the existing `validateBannerPhrasing` defense-in-depth. `useCrossPetSafetyBanner`/`CrossPetSafetyBanner` are generically typed over `BannerSafetyFinding`, so hook + component needed **zero** changes. **`adversarial-reviewer` PASS** (adversarial `firstOnsetIso` → degrades to "since then", never crashes/leaks/reassures; the "recurring …" copy clears all four guardrail screens incl. the `recover` stem; within-pet + cross-pet ranking matches `SAFETY_TYPE_ORDER {0,1,2}`; only adds banner eligibility — no new all-clear/double-count). **`code-reviewer` ship-ready** (priority mirror byte-for-byte; `onsetMonth`/`MONTH_NAMES` reused not duplicated — no new KEEP-IN-SYNC surface; comment tightened per its NIT). `nyx-voice` self-check ✓ (P1/P2/P4/P5/P6). Personas: Dr. Chen + Data Scientist ✓ (escalate-on-presence; never-reassure holds) · Designer ✓ · Engineer ✓ · Trust & Safety N/A (client copy/selection, no data boundary). `tests`: `lib/signalCopy.test.ts` +chronicity selection/copy/malformed-ISO/guardrail-sweep (10 chronicity variants); **1302 jest** green, `tsc` clean; no server CI (0 checks). Backlog **B-191 → Done**. **Residual (NOT a blocker, PM/Dr. Chen):** D2 `minEpisodes` 6-vs-5 chronicity floor is still un-ratified — this broadens that detector's surface to a cross-pet banner; safe-error direction (under-escalation, never false reassurance) unchanged. — shipped via #370
- 2026-07-16 — **B-005 planning (smarter library deletes → archive-not-cascade) + B-354 logged (per-account food library); docs/backlog-only, no schema, no app code, no build-phase change** — PM asked for a kickoff on B-005 (tombstone-not-cascade library deletes), which turned into a planning session + a launch-blocker discovery. **Product calls ratified:** (1) **archive terminology** — the delete action becomes "Archive/Remove from library" and **no longer erases meal history** (that stays a per-event Timeline action); (2) **preserve + tombstone** — an `archived_at` flag on `food_items` drops the food from the picker but leaves its meals/`diet_trials` refs intact; (3) **undo-capable** via an "Archived" library section (Restore un-archives → food active/feedable again) + an immediate undo snackbar; (4) **per-user archive**. The load-bearing invariant (Data Scientist + Dr. Chen): filter `archived_at` ONLY at picker/library reads, NEVER on history/analytics/vet-report joins — else an archived food's past meals lose their name and drop from the correlation exposure set (silently re-introducing the data loss B-005 removes). Archive collapses to a single flag flip (no event/meal mutation, unlike today's cascade). **Launch-blocker discovery:** `food_items` is globally scoped (a CLAUDE.md sanctioned exception) → a second account sees every account's foods (`refreshFoodCache` pulls all rows, no user filter). Product-team sanity check came back near-unanimous for per-account, with **Trust & Safety framing the global catalog as a pre-launch privacy blocker** (cross-tenant exposure of health-adjacent diet + `created_by_user_id`) → filed **B-354** (de-globalize to per-account; `rls-privacy-reviewer` mandatory on the re-scope; PM parked as **high-priority**, Now). B-354 gates B-005 (a per-account catalog makes B-005's per-user archive fall out for free) → sequence **B-354 → B-005**. B-005 requirements-doc write (`docs/nyx-food-dedup-requirements.md` §5) deferred until B-354's direction is PM-ratified. Personas: Data Scientist ✓ · Trust & Safety ✓ · Designer/Jordan/Sam ✓ · Dr. Chen ✓ · Engineer ✓ · adversarial-reviewer **N/A** (planning — no clinical/statistical logic authored). `tests: N/A — docs/backlog-only`; `tsc` + **1297 jest** green (pre-push hook); no server CI (0 checks). — shipped via #365
- 2026-07-16 — **B-242 meal→food terminology sweep beyond the FAB (owner-facing copy pass; no schema, no logic, no build-phase change)** — the FAB already says "Recent foods / Log food" (Food › (Meal, Treat) hierarchy, #277); this swept the remaining owner-facing "meal" strings that are actually about *food generically* → "food", across **12 source files / ~14 strings**: the **Top-food + Top-protein** calibration units (`insights/index.tsx`) + the `RankingCard` default (`RankingCard.tsx`) — both cards rank ALL foods incl. treats, so "N more meals to log" was factually wrong; the **delete-food** alert (`food/[id].tsx` — the food may be a treat); the **diet-trial compliance** line (`profile.tsx` — counts all `event_type='meal'` days, treats included); the **food-capture** confirm button (`food-capture.tsx`); two **food-library empty states** (`foods.tsx`, `edit-event.tsx`); two generic **"start logging" prompts** (`DashboardEmptyState.tsx`, `history.tsx`); and — **added after the code-reviewer caught the same data-source bug class** — the Home **TrendZone** "Meals" chart header + "% meal compliance" (`TrendZone.tsx`, same `event_type='meal'` day-count as the profile line), the **MealCompletionCard** "the meal's detail screen" alert (`MealCompletionCard.tsx` — the intake card also fires for treats), and the **FoodTile** `accessibilityHint="Logs a meal"` (`FoodTile.tsx` — renders treat tiles too). **Deliberately LEFT** (checked, meal-only by intent or out of copy-scope): the intake-specific **"Meals finished"** MetricCard + its `calibrationUnit="meal"` (treats & free-fed excluded — "meal" is correct); the **"Meals & treats"** CompositionCard; the food-library **"Meals" section group** + the **Meal/Treat food-type chips**; the RankingCard **"a few more meals"** per-item hint (the finish-rate is rated-*meal*-derived, so "food" would be LESS accurate — reviewer finding declined with rationale); `signalCopy` **"no meals"/"recent meals"** (coverage/intake baseline); and the `eventTypes.ts` **"Meal" category label** (structural quick-log/History category selector, per-row overridden to "Treat" — relabel ripples through every History badge/filter ⇒ **Designer/PM call, not copy-scope**; reviewer concurred). Personas: `nyx-voice` ✓ (warm, no exclamation, forward-looking empties, plain language) · Designer ✓ · Engineer ✓ · **code-reviewer PASS** (fix-before-merge misses folded in) · Data/Dr. Chen/adversarial-reviewer **N/A** (no clinical/statistical logic — pure copy). `tests: N/A — copy-only; RankingCard.test.tsx ×2 + FoodTile.test.tsx updated in lockstep`; `tsc` clean; **1297 jest** green (pre-push hook). Backlog: **B-242 → Done**; the `eventTypes` "Meal" label + Home TrendZone naming logged as the two follow-up/decision notes. — shipped via #363
- 2026-07-16 — **In-app legal wiring — hosted-doc links live + onboarding veterinary-disclaimer acknowledgment with a server-side acceptance record (B-229/B-230/B-270 → Done; pre-submission Guideline 5.1.1(i); closes the B-283 PR-5 legal flip)** — hosted docs verified live (getculprit.app `/privacy` `/terms` `/disclaimer`, all 200). Schema in its own PR per isolation: `legal_acceptances` (migration 032) — append-only `(user_id, document, version)` + server-stamped `accepted_at` (column-grant excludes it; no client UPDATE/DELETE), applied live via Supabase MCP, advisors clean, **`rls-privacy-reviewer` PASS** (cross-user insert / timestamp forgery / update-delete / cross-user read / upsert-DO-UPDATE / anon all held) — shipped via **#361**. Wiring: `LEGAL_LINKS_ENABLED` → true + `DISCLAIMER_URL`; You-screen Veterinary-disclaimer row; signup's "on its way" stubs → real Terms/Privacy links; NEW `app/onboarding/disclaimer.tsx` (signup → disclaimer → pet-type; appendix copy VERBATIM; the tap AWAITS `lib/legal.ts recordDisclaimerAcceptance` — error → calm retry, never advances unrecorded; 23505 → already-recorded); `usePet` needs-onboarding entry → disclaimer. `code-reviewer` **fix-before-merge = sequencing only** (migration-before-code — already applied live; merge #361 → #362) + its two failure-path tests added. Personas: Designer ✓ (Principle-1 placement: onboarding, not mid-log) · T&S ✓ · Engineer ✓ · `nyx-voice` ✓ · Data/Dr. Chen/adversarial-reviewer **N/A** (no clinical/statistical logic). Backlog: B-229/230/270 → **Done**; NEW **B-353** (legacy-account acceptance catch-up). `tsc` + **1297 jest** green (+14). — shipped via #362 (wiring) + #361 (schema)
- 2026-07-16 — **B-118 closed — leftover `smart-worker` Edge Function verified deleted (backlog cleanup / security loose-end; docs-only, no schema, no app code, no build-phase change)** — PM believed the stock-template `smart-worker` function (zero callers, an `authMode==="secret"` RLS-bypass branch; sibling of the closed B-043) was already dashboard-deleted. Confirmed via `list_edge_functions` on the live project (ref `aigchluqluzuhtbfllgh`): 8 functions live, **no `smart-worker`**; also absent from the repo. Marked B-118 `Done — 2026-07-16` across `docs/backlog.md`, this file's Cleanup/infra checklist, and `docs/app-store-readiness.md`. Adjacent (out of scope per PM): `zz-deploy-probe` (v1, no repo backing — same dangling-throwaway class) + `view-report` `verify_jwt:false` (intended public share-token path) both left alone, not logged. Personas: Trust & Safety ✓ · Product Owner ✓ · Engineer/Data/Dr. Chen/adversarial-reviewer **N/A** (no clinical/statistical logic). `tests: N/A — docs-only` (pre-push hook: tsc + **1283 jest** green; no server CI, 0 checks). — shipped via #359
- 2026-07-16 — **Privacy hardening — route all photo uploads through `compressForUpload` to strip EXIF/GPS (pre-App-Store; feeds PR #302 policy + B-268 label; no schema, no build-phase change)** — three upload paths + the vet-attachment sync re-upload bypassed the `expo-image-manipulator` re-encode and uploaded original camera-roll bytes (GPS EXIF could reach Storage): vet-visit inline save + `syncPendingVetVisits` (`att.local_uri` — vet attachments never used `prepareAttachmentUpload`), pet profile photo (`exif:false` only strips the JS result, not the file), edit-event inline attachment. All now re-encode to a stripped JPEG before upload — directly via `compressForUpload`, or via `prepareAttachmentUpload` (forwards `prep.mimeType`) for the vet sync path. Swept every remaining `uploadPhoto`/`storage.upload` callsite (all others already compress; Edge Functions only download/remove). EXIF **date** reads unaffected (pick-time); `.jpg`/`image/jpeg` consistent; `local_uri` durable-hero original untouched. Makes the privacy-policy location claim true across all upload paths. `code-reviewer` **ship-ready** (single terminal `.catch` covers the compress step; upsert→mark-synced guard intact; flagged pre-existing missing width/height → **B-352**); adversarial-reviewer N/A (plumbing, no clinical/statistical logic). Personas: Trust & Safety ✓ · Engineer ✓ · Designer ✓ (vet-doc legibility → on-device eyeball) · Data/Dr. Chen N/A. `tests: prepareAttachmentUpload strip/compress guard covered by lib/sync.test.ts (5), now fronting the vet path — header updated`; `tsc` + **1283 jest** green; no server CI (0 checks). — shipped via #357
- 2026-07-15 — **Track-3 monetization T3-A — B-332 manual Primary-protein picker (client UI; no schema)** — shared `ProteinPicker` (wrapping `ChipGroup` + "Other" escape; `COMMON_PROTEINS` in `lib/protein.ts`, S5) on the food-capture edit step + food-detail edit; never null-clobbers an AI value (touched-tracking upsert + baseline-diff), one correlation path (`canonicalizeProtein` on read, unit-guarded). `code-reviewer` fix-before-merge reseed bug fixed (+2 regression tests); `adversarial-reviewer` parity PASS. Unblocks T3-E. Multi-protein follow-up split to B-351 (PM approach A). `tsc` + 1283 jest green. — shipped via #355
- 2026-07-15 — **Track-2 monetization T2-5 — paywall mock gated out of the submission build (B-330; client-only route gate; ship-dark)** — spec §6.5 + §8 (T2-5). Gated `app/onboarding/paywall.tsx` on the server-flippable `paywall_enabled` flag (built in T2-4's `lib/appConfig.ts`/`hooks/useAppConfig`). **Primary route:** `app/onboarding/pet-age.tsx` `finish()` (both Continue + Skip) pushes straight to `/onboarding/done` when the flag is off — the mocked paywall never mounts; no progress-bar math changes (paywall sits outside the 5-step bar), done is the terminus either way. **Defensive backstop:** `paywall.tsx` bounces a stale-client / deep-link entry to `/onboarding/done` and renders `null` while bouncing (no dead trial CTA flashes), sequenced after the existing no-pet escape hatch. **Render-only** (the paywall key is client-only — §4.2, no server-enforced path; it's a client mock) and **fails CLOSED**: shipped client default `paywall_enabled=false` (`lib/appConfig.ts APP_CONFIG_DEFAULTS`), so a config-unreachable build skips the non-functional CTA (a 2.1/3.1.2 rejection risk) rather than showing a dead one. **Ship-dark:** seeded server value stays `true` (`030_app_config.sql:113`, untouched) ⇒ flag-on = today's flow byte-identical once config resolves. **Config-load race (code-reviewer catch, accepted soft residual → B-345):** a first-run signup reaching pet-age before the async config fetch lands reads the fail-closed default → skips the seeded-`true` mock (the AC's "config-unreachable → hidden" clause, not a byte-identical break; a mock shown less often, never a dead screen). **The `paywall_enabled=false` pre-submission flip is a separate recorded config change** (live `app_config`, noted in STATUS then) — NOT in this PR. No schema; no server change; no deploy. `tsc` clean; onboarding suite green (+3 tests: pet-age flag-off Continue+Skip routing, paywall flag-off bounce; existing paywall/pet-age tests preserved by defaulting the mocked flag ON). **Reviews:** `code-reviewer` **fix-before-merge (advisory)** — gate/guards/tests correct; race reconciled to accepted residual + B-345 (no logic change). Personas: Engineer ✓ (render-only gate, fail-closed default, ship-dark holds) · Designer ✓ (no dead affordance) · Trust & Safety N/A (client route gate, no data boundary) · adversarial-reviewer **N/A** (a routing flag — no clinical/statistical logic). `tests: N/A exemption not taken — routing gate covered via the existing screen suites`. **B-329 → done** (T2-1+T2-3+T2-4+T2-5); **B-330 → done on merge** (the recorded live flip is the remaining PM action). **PM action:** perform the `paywall_enabled=false` flip on live `app_config` before cutting the submission build; record it here. — shipped via this PR
- 2026-07-15 — **Track-2 monetization T2-4 — flag-aware client + AI cap states, + the four T2-3 AI Edge Functions deployed from merged `main`** — deployed `extract-food-from-photo` v11 / `extract-medication-from-photo` v2 / `analyze-vomit` v8 / `generate-signal` v24 via the Supabase MCP (`verify_jwt=true` preserved, sha read-back + clean-4xx boot each; the new `getUser()` 401s on the anon boot test confirm the T2-3 auth path is live; ship-dark, behavior byte-identical). Built the client half of B-329: **`lib/appConfig.ts` + `hooks/useAppConfig`** — server-flippable flags with a last-known-good AsyncStorage cache and per-key shipped defaults (**AI keys fail-OPEN, `paywall_enabled` fail-CLOSED**, §4.2), **render-only** (the function re-checks every gate, B-252), plus a typed decoder for the §4.5 `{cap_reached}`/`{feature_disabled}` 200-bodies; init seeds cache only, the single authoritative fetch is auth-driven (`INITIAL_SESSION`/`SIGNED_IN`/`TOKEN_REFRESHED`) + foreground. **States:** food/med capture flag-off → straight to manual entry (no dead camera affordance); `cap_reached` → calm §7.3 band on the edit step (**never** error red), capture never interrupted mid-flow; a capped/flag-off commit records the row not-AI-extracted. Vomit read `status='capped'` (no retry, no reassurance, routes to vet) + `status='read_disabled'` (renders nothing) — escalation still surfaces via the unchanged `completed` path (the function computes contextual flags **before** the cap gate, so a fired flag is never a capped row → **never-reassure survives the cap by construction**). D-M6 "free during early access" labels on both extraction surfaces; **first B-333 care-first line** on the extraction cap states; §7.4 done-screen "Got another pet?". Copy centralized in `constants/monetizationCopy.ts`. **Reviews:** `code-reviewer` **ship-ready** (no correctness/safety blockers; verified the vomit branch-order + response-branching by hand; both recommended follow-ups APPLIED — the `_layout` redundant-refresh cleanup + component-level §11 fixtures) · `nyx-voice` ✓ · Designer ✓ (all states) · `clinical-guardrails` + Dr. Chen ✓ (falsification: the 11th-vomit-in-a-day & clear-foam-not-eaten-36h cats fire their contextual flag **pre-cap** → written `completed`+`worth_a_call`; the cap can't suppress the escalation; HELD). `tsc` clean · **1263 jest** (+9: 4 vomit-render + 5 hook-wiring; plus §11 pure-decode + copy-invariant fixtures); no server CI (0 checks). **Open flag (PM/`pm-feature-review`):** §7.6-vs-§16 — B-333's "never behind this door" reads slightly oddly on a cap state with no visible paywall. Follow-up **B-344** (food/med capture render fixtures — deferred). B-001 done; B-329 → **T2-5 remains** (paywall flag). Personas: Engineer ✓ · Dir. of Engineering ✓ · Designer ✓ · Dr. Chen/Data ✓ · Trust & Safety ✓ (render-only, B-252). — shipped via #353
- 2026-07-14 — **Track-2 monetization T2-2 — `ai_usage` throttle table + `record_ai_usage` RPC (schema half; ship-dark; no build-phase change to the numbered sequence, no app code)** — built lever #3 of the monetization gating hierarchy from `docs/monetization-and-throttling-requirements.md` §4.3/§8 (B-001's schema half): migration `031_ai_usage.sql`, a `(user_id, function, day, scope_id)` composite-PK counter table + the atomic `record_ai_usage(p_function, p_scope_id)` `SECURITY DEFINER` RPC that every AI Edge Function will call immediately before its Anthropic call (T2-3, later) to compare against the §4.4 caps. UTC `day` (house rule: UTC everywhere); `scope_id` = `pet_id` for `generate-signal`'s per-pet cap, sentinel zero-UUID for the per-user functions (keeps a nullable column out of the PK); `ON DELETE CASCADE` from `auth.users` folds it into the B-039 hard-delete. The RPC derives the caller from `auth.uid()` and RAISEs on null — **the count can never be forged from the request body** (B-252); `INSERT … ON CONFLICT DO UPDATE SET count = count+1 RETURNING count` → `day_count` + a same-scope UTC-month `SUM` → `month_count`; increment-then-check, attempts counted not successes; `SET search_path = public`. RLS on with one `SELECT USING (user_id = auth.uid())` owner-read policy + **no** write policies (default-deny — the RPC is the only write path); `EXECUTE` granted to `authenticated`, explicitly REVOKEd from `anon` (Supabase default-privileges grant it at CREATE, so the `PUBLIC` revoke alone wasn't enough — caught + closed via the live ACL). Applied live via the Supabase MCP `apply_migration` (recorded in history); **verified against prod:** atomic increment (2 calls → 2), per-pet `scope_id` independence (petA=2 / petB=1), correct UTC `day` + `RETURNS TABLE(day_count, month_count)` contract, null-`auth.uid()` → RAISE 42501; test rows cleaned up. `get_advisors` (security) clean for the new objects (only the intended `authenticated`-can-execute lint). **`rls-privacy-reviewer` PASS** (all six attack surfaces held — forged identity, anon bypass, cross-user read, `search_path` shadowing, direct-write RLS bypass, scope_id cap-evasion) · **`code-reviewer` ship-ready** (no bugs; three forward-looking NITs routed to T2-3 — notably back `function`'s unconstrained TEXT with a shared TS string-literal union so the four call sites can't drift). B-001 → **partial** (T2-3 in-function cap enforcement + typed responses + the `analyze-vomit` reorder remain). Personas: Dir. of Engineering ✓ (schema shape / composite-PK grain / migration isolation) · Trust & Safety ✓ (`rls-privacy-reviewer` PASS; no client write path; JWT-derived identity) · Engineer ✓ · Data / Dr. Chen / adversarial-reviewer **N/A** (a counter — no detection/correlation/AI-read logic). `tests: N/A — schema-only migration; RPC behavior verified live against prod`; `tsc` + **1225 jest** green (pre-push hook); no server CI (0 checks). — shipped via #348
- 2026-07-14 — **Intake-timing product discussion — grazers vs scarfers (product/discovery; docs+backlog-only, no build-phase change, no schema, no app code)** — PM ran a dogfooding observation: the WSAVA 5-point intake chips fire co-located with the meal log (`MealCompletionCard`), which is correct for **scarfers** (a dog inhaling kibble, a cat scarfing treats) but **premature for grazers** (a cat's wet food) — the owner only knows how much was eaten at the *next* feeding, standing over the leftover bowl (his own ritual: assess the morning bowl while putting down dinner). Convened the two pet-owner personas (Sam ✓✓ — her exact reality; Jordan: the in-moment timing is *right* for a scarfer, don't add a "check back later" step to my flow) + the full product team. Landed the reframe: **intake-timing is a property of the (pet, food) pair, not a fixed step** (Nyx scarfs treats / grazes wet — same cat, opposite timing) — so the fix removes a premature question and re-homes it to the next-meal ritual ("answering beats initiating", `logging-capture-discovery` §2), stays honestly **unconfirmed** until resolved (B-156 G1 fail-safe generalized), and models consumed-at as a **window** not a point (B-010 confidence + the free-feeding "intake not directly observed" caveat for acute correlation). Separated the novel part (this) from the already-specified threads: free-feeding/shared-bowl attribution = **B-040** (Nyx's wet is attributable since her sister won't touch it; the shared dry bowl is the hardware-deferred case), delayed *notification* = **B-015**. This is explicitly the wet-food offered-vs-consumed timing window the free-feeding R1 fenced off as blast-radius (`nyx-free-feeding-requirements.md` §0) — Eng flag: touches the meal/event model, earns its own spec + adversarial pass on the pending→resolved state machine. Filed **B-341** (context-adaptive intake timing) + **B-342** (in-app WSAVA-scale explainer, pull-to-reveal). **PM direction recorded on B-341:** (1) in-app resolve-at-next-log = core; multi-channel (+ notification) endorsed for higher conversion *with* cross-channel de-dup + fail-safe-silence non-negotiable, notification half inheriting the deferred 2026-07-10 nudge-cap Open Question; (2) property carrier = build-time decision (team lean: infer default + one-time owner-correctable toggle, sibling to free-choice); (3) WSAVA explainer parked ("go deeper later"). Surfaced conflicts (Persona Conflict Protocol): Designer/Jordan (in-app ride ≠ a nudge) vs Data/Dr. Chen (the *push* channel inherits the nudge-cap question); Designer (infer, zero setup) vs Data (don't silently guess a clinical property — infer default, make it correctable). Safety lens held throughout: a grazer leaving wet food is the canonical picky-vs-sick trap — capturing intake when the owner *actually knows* directly strengthens the `feline_reduced_intake` flag (B-027/B-033) for exactly the pet most at risk; resolution copy routes refused/picked toward a health flag, never "picky". Personas: Sam ✓✓ · Jordan ✓ · Sr. Product Designer ✓ (P1/P2/P4) · Sr. Data Scientist ✓ (intake-is-not-preference; fabricated-"Some"-on-dismiss catch) · Dr. Chen ✓ (window-not-point; never-reassure) · Dir. of Engineering ✓ (blast-radius flag) · Sr. QA ✓ (grazed+free-fed / skipped-feeding / two-caregiver / mid-trial-switch edge cases) · Product Owner ✓ (filed, cross-referenced, not duplicated) · adversarial-reviewer **N/A** (no clinical/statistical *logic* authored — discussion + backlog rows only). Discovery-doc write-up (`nyx-intake-timing-requirements.md`) offered, deferred to the roadmap slot per PM. `tests: N/A — docs/backlog-only, no store/Edge Function/lib touched`; `tsc` + jest clean via the pre-push hook; no server CI (0 checks). — shipped via #345
- 2026-07-13 — **Vet-report send feedback → product-team decisions (docs-only; no build-phase change, no schema, no app code)** — PM shared n=1 from the first real vet-report send (report emailed to Completely Cat Clinic). Convened the team on 5 reactions and codified the outcomes. **Ratified (CLAUDE.md Open Q → Resolved): elevate per-incident vomit VISUAL red flags to a firm-but-calm Home Signal, led at top.** The engine gap is real — `generate-signal/detection.ts` reads events/meals/meds but NEVER `event_ai_analysis`, so a per-incident blood/foreign flag lives only on the event detail screen (against Principle 3). Persona conflict — Dr. Chen/Data (surface it) vs Designer/Jordan/Sam (2 false positives this session → crying-wolf trust cost) — resolved toward surface-it because false positives are cheap for the owner to clear (the B-028 edit path). **Load-bearing build guardrail, code-verified:** the client edit writes ONLY structured fields + `edited_at` and does NOT clear the cached `visual_flags`/`recommendation` (`lib/analysis.ts:152`), so the elevation MUST derive the flag from the owner-editable structured fields (`foreign_material_present==='yes'` / `blood_present∈{fresh_red,coffee_ground}`), never the stale `visual_flags` array — an override then clears the card by construction, mirroring `generate-report` (already override-aware; derives safety from those fields, never `SELECT`s `visual_flags`). **Sanity-checked the PM's real 38-page report** (pdfminer text extract): the blue-ball foreign flag is correctly ABSENT (his override propagated), but the Jul-9 "stick-like/toothpick-like object" foreign flag (the deli-meat-seasoning false positive he hasn't cleared) STILL leads the page-1 safety section, honestly hedged `AI READ · UNCONFIRMED`; chronicity (53d/23 episodes) also leads correctly; "no weigh-in recorded" + "neuter not recorded" data gaps shown honestly. Logged **B-337** (vet-report cover-note / talking points + the "first reader is a vet tech / front desk, not the DVM" reframe), **B-338** (`analyze-vomit` scene-vs-contents prompt hardening, scoped so it does NOT detune escalation), **B-339** (refined: the report is override-aware, the residual is the stale `visual_flags`/`recommendation` cache after an edit), **B-340** (the Signal-elevation build item — Step 10 evolution, `adversarial-reviewer`-mandatory, deploy-gated on the `lib/signal.ts`/`InsightCard` renderer per the B-182 lesson). Personas: PM/Product Owner ✓ · Dr. Chen + Data Scientist ✓ (escalate-on-presence permits it; never-reassure holds) · Designer + Jordan/Sam ✓ (the trust dissent, recorded not silenced) · Engineer ✓ (derive-from-structured-fields, deploy sequencing) · adversarial-reviewer **N/A** (no clinical/statistical logic authored — decisions + backlog only; B-340 build will run it). `tests: N/A — docs-only`; no code touched. — shipped via #346
- 2026-07-14 — **Track-2 monetization T2-1 — `app_config` migration (schema half; ship-dark; no build-phase change to the numbered sequence, no app code)** — built the "product lever" from `docs/monetization-and-throttling-requirements.md` §4.2/§8: migration `030_app_config.sql`, a `key TEXT PK / value JSONB NOT NULL / updated_at TIMESTAMPTZ` table read at request time by the AI Edge Functions (T2-3, later) and by the client for UI shaping (T2-4, later), so any AI surface or the onboarding paywall flips on/off via a config change instead of a new build. RLS enabled with exactly one `SELECT`-to-`authenticated` `USING(true)` policy and **no** write policies → RLS default-deny means the app can never write config; only the service role / dashboard flips it (B-252, no client-trusted state). `set_updated_at` trigger (house 016/024 pattern) so a hand-flip stamps `updated_at` honestly. Six seeded keys, every value ship-dark = today's live behavior byte-for-byte (`ai_food_extraction_enabled`/`ai_med_extraction_enabled`/`ai_vomit_read_enabled`/`ai_signal_phrasing_enabled`/`paywall_enabled` = `true`, `ai_caps` = `{}`); `ON CONFLICT (key) DO NOTHING` so re-applying can never reset a deliberately-flipped live value back to the seed. Applied live via the Supabase MCP `apply_migration` (applies + records in history); post-apply SQL verified table/RLS/policy/trigger/6-seeds, and `get_advisors` (security + performance) returned **zero new `app_config` findings** — RLS on, `SELECT USING(true)` is the linter's sanctioned public-read pattern, and the constant qual sidesteps the per-row `auth.uid()` perf lint that flags the pet-scoped tables. `code-reviewer` **ship-ready** — no bug/anti-pattern findings; independently confirmed the `updated_at` trigger and `ON CONFLICT DO NOTHING` are both appropriate (defensive, not scope-creep). Applied its one NIT: CLAUDE.md's engineering hard-constraint line now names `app_config` as the second sanctioned no-`pet_id`/no-`user_id` table (alongside `food_items`), so a future reviewer doesn't flag it as a violation. B-329 → **partial** (T2-3 server enforcement in the four AI functions + T2-4 `lib/appConfig.ts`/`useAppConfig` + client cap/flag states remain). Personas: Dir. of Engineering ✓ (schema shape / RLS default-deny / migration isolation) · Trust & Safety ✓ (no app write path; service-role-only flips) · Engineer ✓ · Data / Dr. Chen / adversarial-reviewer **N/A** (a config table — no detection/correlation/AI-read logic). `tests: N/A — schema-only migration, no extractable logic`; `tsc` + **1225 jest** green (pre-push hook); no server CI (0 checks). — shipped via #347
- 2026-07-13 — **Monetization & throttling spec — PM review pass (docs-only; no build-phase change, no schema, no app code)** — PM asked for a review of `docs/monetization-and-throttling-requirements.md` (working/not-working/improve). Spot-checked the spec's load-bearing code claims against the real code — all held: `analyze-vomit` computes deterministic flags AFTER the vision call today (the §5.4 reorder is valid), `event_ai_analysis.status` is unconstrained `TEXT` (013:167 — the `capped`/`read_disabled` additions really are schema-free), `generate-signal` runs on caller JWT with no service role, `extract-food-from-photo` is JWT-presence-only on service-role with body-supplied paths (the §4.6/S3 IDOR concern is a live gap). Graded the plan **A−** (Track 2 build-ready now; Track 3 A−-planned but decision-gated). Five amendments written into the doc on the PM's accepted reactions, **no ratified D-M ruling changed**: (1) §4.4 records the clinical ratification of the `analyze-vomit` cap *value* (≥10 incidents/day = ER, not at-home logging; the escalation floor fires on #11 regardless) — closes the gap that only the cap *copy*, not the number, had a Dr. Chen route; (2) §4.4 clarifies per-pet *enforcement* vs per-user *cost accounting* (they're different axes, not a contradiction — the PM's #1/#7 confusion) + a build guard for the `scope_id` sentinel-collapse trap; (3) §9 T3-E + §16 flag **B-047/B-016 as an unowned dependency** (both Open/unbuilt, in no PR plan, yet hard-gate the extraction gate flip — PM ownership decision needed); (4) §4.2 adds the authoritative-path config-read failure fallback (mirror client per-key posture, AI fail-open — Dir. of Eng sign-off at T2-3); (5) §4.7 flags the entitlement grace-window duration as an open T3-C product decision (money on both sides; provisional 72h). Parked pending a PM call (noted in the doc header, not actioned): the mandated shared client-side `parseAIResult` decoder for the §4.5 typed 200-body contract — the one remaining *structural* (not process) gap. Personas: Product Owner / PM lens ✓ (the review) · Data Scientist / Dr. Chen ✓ (the vomit-cap-value clinical rationale) · Dir. of Engineering ✓ (server config-read fallback flagged for sign-off) · Engineer ✓ · adversarial-reviewer **N/A** (no clinical/statistical *logic* authored — the doc records the PM's own clinical ratification, not new detection/correlation code). `tests: N/A — docs-only, no store/Edge Function/lib touched`; `tsc` + jest ran clean via the pre-push hook; no server CI (0 checks). — shipped via #344
- 2026-07-12 — **Brand on-device QA fixes — CulpritMark halo, Landing teal glow, pull-to-refresh (Culprit in-app brand / B-284 follow-up to #341; no build-phase change, no schema)** — three PM device-QA findings on the brand surfaces. (1) **CulpritMark glow halo removed** — the soft low-opacity teal circle behind the Signal dot read as an off-brand “glow behind the logo” at the header's 16px (teal is the interactive accent, not a haze, §1.3); resting frame is now a clean dot, the dynamic pulse untouched. (2) **Landing teal glow removed** — the third aurora radial parked on the Signal dot in `NightHeroGround` (a teal `objectBoundingBox` radial) read as a prominent glow blob AND is the likely source of a stray cream streak on device (a stretched radial can paint as a bright streak; the code has no other shape there) → removed the def + ellipse (violet/indigo auroras kept). (3) **Pull-to-refresh lightened** — the dark `NightGround` night band (aurora + starfield + night whorl) over the light Home caused a jarring contrast whiplash, an unpolished glow, and starfield jank → replaced with a light strip (day `WhorlSpinner` on `colorSurfaceSubtle` + hairline). All three revise B-284 (§3 mark static frame, §5 refresh band) — Tier-2 doc edits flagged for PM confirm. Tests updated (CulpritMark reduced-motion/blur → clean dot; NightHeroGround 3→2 radials); adversarial-reviewer N/A (pure UI, no clinical/statistical logic); self code-scan (theme tokens only, no `any`, no hardcoded values). Personas: Designer ✓ (clean mark + light refresh; carve rule/a11y untouched) · Engineer ✓ · Data/Dr. Chen N/A. PM confirmed “everything is looking great” on device. `tsc` + lint clean · **1224 jest** (2 snapshots); no server CI (0 checks). — shipped via #343
- 2026-07-12 — **B-325 direction ① — retroactive med→treat combo linking (B-156 combo parallel track; no build-phase change, no schema)** — closed B-156's false-adherence hole for the owner who misses the ~5s completion card. A new “+ Add a med given with this” entry on the meal/treat DETAIL screen (`app/event/[id].tsx`, gated `food_type ∈ {meal,treat}`, mirrors the card's showIntake gate) reuses the entire forward write path via a new `comboSource='detail'` param — same pre-bound picker, same `insertMedicationDose(…, pairedEventId)`, same `initialComboDoseAdherence` coupling; the forward card path is byte-for-byte unchanged. Design settled across mockup rounds + a Designer/persona call (PM steer): no completion-card replay — returns to the treat (focus-refetch renders the persistent PR B4 cross-link), adherence stays editable on the dose screen (G2); a NOT-finished vehicle gets a deliberate confirm sheet (`components/log/ComboDoseConfirmSheet.tsx` — heads-up + real adherence chips + “Not sure yet”) so the owner inputs how much got in rather than a silent unconfirmed; the dose is written unconfirmed BEFORE the sheet so any dismiss/backdrop/write-fail leaves it unconfirmed, never a false 'given'. New `comboConfirmHeadsUp` copy helper (+tests). **`adversarial-reviewer` PASS** (never a false 'given' across backdrop/skip/write-fail/read-fail; forward path unchanged) · **`code-reviewer`** clean after 2 fixes (theme-token swap on the sheet scrim/grabber; surfaced the write-failure to the owner) · `nyx-voice`/`clinical-guardrails` on copy. Personas: Designer ✓ (Option A + confirm sheet, discoverable + editable-later) · Data/clinical-guardrails ✓ (unconfirmed-by-construction) · Dr. Chen/Sam ✓ (opted-in confirm, not a nudge) · Engineer ✓ (zero schema, reuses the write path). Residuals filed: **B-334** (treat-side Unconfirmed marker), **B-335** (adherence chip tap-target on the safety surface), **B-336** (double-tap dose guard). Direction ② held (B-111). `tsc` + lint clean · **1223 jest** (+ new copy tests); no server CI (0 checks). — shipped via #342
- 2026-07-12 — **B-322 — CulpritMark Signal-dot pulse unfrozen on device (Culprit in-app brand / B-284 N2 follow-up; no build-phase change, no schema)** — PM spotted the Landing hero mark rendering static and asked whether it should pulse (it should) and, correctly, that we design for defaults (Reduce Motion is OFF by default → the frozen mark was hitting ~everyone, not just reduced-motion users). Diagnosed the exact **B-322** gap N3 already documented: the live pulse animated `scale` on react-native-svg's `<G>` with `useNativeDriver:true`, but `RNSVGGroup` exposes only `matrix` natively (the `scale`→`matrix` conversion is JS-side, bypassed by the native driver) → the loop starts, tests pass, nothing reaches the native thread → it degrades to the static frame on a real Fabric build. **Fix = the proven N3 port:** drive the ping ring + breathing dot via native-driver **`Animated.View` transforms**, each wrapper absolutely-positioned + centred on the dot so the scale pivots from the dot's own `cx/cy` (reproducing the old `<G originX/originY>` intent); added `useAppActive` so the loop pauses on app blur (§1.5, matching WhorlSpinner). Reduced-motion AND app-blur both fall back to the same static frame (glow + resting dot, no ring). Tests reworked to the new structure with a **native-driver regression guard** (no `<G>` carries the pulse; ring+dot ride transform-bearing Views) + an app-blur test; 16/16 green, snapshot regenerated. Also corrected WhorlSpinner's mis-numbered "B-320" cross-ref → B-322. **`code-reviewer` ship-ready** (one comment-accuracy nit applied; pivot geometry, no-clip-at-2.1×, and native-driver eligibility all verified). Adversarial-reviewer **N/A** (pure animation-mechanism/UI — no clinical/statistical/correlation logic). Personas: Engineer ✓ (native-driver mechanism, zero new deps) · Designer ✓ (pulse restored; carve rule + a11y untouched) · Data/Dr. Chen N/A. **On-device confirm still outstanding** (a cloud session can't verify the native thread) — flagged in B-322. `tsc` clean · brand/home/onboarding/auth suites green; no server CI (0 checks). — shipped via #341
- 2026-07-12 — **Monetization & throttling — build-ready spec (docs-only; no build-phase change, no schema, no app code) — shipped via #340** — wrote `docs/monetization-and-throttling-requirements.md`, the build contract executing the ratified D-M1–D-M8 strategy. **Track 2 (pre-submission, 5 PRs):** T2-1 `app_config` migration (6 seeded keys, ship-dark values byte-identical to today; the paywall key's client fallback deliberately fail-closed) → T2-2 `ai_usage` migration + `record_ai_usage` SECURITY DEFINER RPC (atomic increment-then-check; per-UTC-day + month; `scope_id` for generate-signal's per-pet cap; no new service-role surface) → T2-3 server enforcement in the four AI functions (flag + cap checks before the Anthropic call; typed 200 `cap_reached`/`feature_disabled`, never error-shaped; `analyze-vomit` rows gain `capped`/`read_disabled` statuses — unconstrained TEXT column, no schema) → T2-4 client states + cap copy + D-M6 early-access labels + first B-333 surface → T2-5 B-330 paywall flag-off. **Track 3 (6 PRs):** T3-A B-332 protein picker (hard-blocks T3-E; independent — ready to run NOW in parallel with everything) → T3-B `entitlements` → T3-C RevenueCat SDK + `revenuecat-webhook` (the one `verify_jwt=false` function; idempotent; rls-privacy-mandatory) → T3-D paywall un-mock (resolves B-263/264/265/266; monthly-forward, runtime StoreKit prices) → T3-E extraction gate flip (checkpoint C1 = the D-M4 revisit; also gated on B-047/B-016 instrumentation) → T3-F D-M8 4+-pet gate built ship-dark. **Two code-verified findings that shaped the spec:** (1) `analyze-vomit` computes its deterministic escalation flags AFTER the vision call today — §5.4 requires the reorder (context+flags → flag/cap check → model) so a capped or flagged-off incident with fired flags still writes an escalating `completed` row (never-reassure survives the cap by construction; `adversarial-reviewer` target); (2) three of four functions never derive a JWT-verified uid (only `extract-medication` does) — §4.6 adds it, and S3 recommends closing `extract-food`'s B-123-class body-supplied-path gap while in there. Caps: ratified daily numbers verbatim + derived monthly ceilings (abuser cost ceiling ≈ $8/user/mo, under §16.2's ~$15 target). §10 numbers the PM offline actions 1–4 (action 1, ASC Agreements/Tax/Banking, has external latency — everything paid stacks behind it). Copy pack §7 drafted (all strings flagged for their review passes). Kickoff-brief ID discrepancy reconciled: B-324–327 in the brief ≡ backlog's B-329–B-332 (B-324 is the demo-account expansion; backlog IDs used throughout). CLAUDE.md read-these row + Open-Question pointers; backlog pointers on B-001/B-329–B-333.
- 2026-07-12 — **VC financial-projections modeling kickoff prompt (fundraising track; docs-only, no build-phase change, no schema, no app code)** — PM asked for a finance-expert take at $4.99/mo premium + free tier and a runnable report prompt. Wrote **`docs/vc-financial-projections-PROMPT.md`** (follows the `vet-report-discovery-PROMPT.md` precedent): bottom-up wedge funnel with the research doc's "triangulated, not investor-grade" caveat carried verbatim; unit economics honoring Pets > $ (free users' real AI COGS modeled; conversion benchmarked BELOW standard freemium); 48-month three-scenario cohort model + sensitivity + the raise ask tied to Series-A milestones; the 5 hardest VC questions with prepared answers; B-263 (paywall contents) flagged as upstream of the conversion assumption and a pre-VC-meeting PM action item. Deliverable of the prompted session: `docs/nyx-financial-model-v1_0.md`. Shipped via #337.
- 2026-07-12 — **B-325 — retroactive + med-first combo-linking gap (investigation; process/meta — no build-phase change, no schema, no app code)** — PM flagged that the med↔treat combo can only be created in-the-moment on the auto-dismissing completion card. Traced the flow: confirmed there is no retroactive "add a med to an already-logged treat" (detail-screen `ComboLinkRow` + History `ComboCrossLink` are read-only) and no med-first "attach a treat" entry at all (`MedicationCompletionCard` has only adherence + the descriptive `how_given` chip; `edit-event.tsx` has zero paired handling). Reframed as adherence-accuracy, not symmetry — the gap reopens B-156's false-adherence hole (PR B3 coupling runs only on the forward path) for the exact target user (Sam) who misses the ~5s window. Split-direction analysis logged: med→existing-treat is cheap (reuses `insertMedicationDose` + `pairedEventId` + the vehicle-intake safety read); minting a new treat from the med side forces the pure-vehicle-inflation question (`how_given` already serves the label-only case). Recommended shape: retroactive "+ Add a med given with this" on the treat/meal DETAIL screen + a med-detail link-to-existing-treat, same B3 coupling, never a phantom event. Backlog **B-325**; draft **PR #338**. PM call open: promote as part of B-156, or fold direction ① in before promotion.
- 2026-07-11 — **Monetization & AI-gating strategy session (strategy/process; no build-phase change, no schema, no app code) — shipped via #335** — PM kicked off the pre-submission monetization + AI-throttling strategy (parameters confirmed up front: no hard date, real-revenue ambition, iOS-only Apple IAP, strategy-first/spec-after). Full product team + guest lenses (App Store submission consultant, two VC advisors) + Jordan/Sam/Dr. Chen feedback; record = `docs/monetization-and-ai-gating-strategy.md`. **Ground truth (code-verified):** none of the four AI surfaces hard-breaks with AI off — Signal detection is deterministic (Haiku phrasing falls back to templates), both extraction flows have manual paths, vomit logging is AI-independent and keeps deterministic escalation — but every fallback reads as an *error* today, so gating needs flag-aware UI (B-329). **Convergent rec:** free/premium line on care/convenience (Premium = food/med photo extraction + future Ask-AI chat B-228 + widgets/themes; Signal/report/alerts/history/multi-pet/export free forever = the D9 answer); drop the coffee tier (3.1.1); submit AI-on-free-server-capped, not flags-off; Culprit Premium sub via RevenueCat → server-side entitlements (B-252-safe) when ratified; throttle architecture (`ai_usage` per-user/day caps, typed `cap_reached`) executes B-001 (promoted Now). **Consultant catches:** the mock paywall's dead trial CTA is a 2.1/3.1.2 rejection risk → B-330 (flag it out of the submission build); monetization never blocks the submission critical path. **Surfaced conflict (PM call, D-M2):** descriptive vomit-read tier — T&S/Designer (escalate-only = care) vs Eng/VC (deterministic escalation stays free both tiers) vs Dr. Chen (not standard of care, but the most reputationally expensive gate); majority rec free-at-launch + data-informed revisit. New B-329/330/331; CLAUDE.md Open Questions row for D-M1–D-M7. Next: PM ratifies D-M1–D-M7 → step-by-step spec session (incl. offline App Store Connect / banking / RevenueCat actions).
- 2026-07-11 — **B-271 App Review demo account — requirements-gathering session (app-store-readiness parallel track; docs-only, no build-phase change, no schema, no app code)** — PM asked to flesh out the seed-style demo account (submission-guide step 11). Wrote the build-ready spec **`docs/nyx-demo-account-requirements.md`**: the 3-layer "why" (Culprit is login-gated → an empty account shows only designed empty states → Apple's "unable to evaluate the app's features" rejection; the demo is Apple's only window into the wedge), and **D1–D7 ratified by the PM**. Story = a **diet-trial dog "Cooper"** whose sneaked-in "contraband" protein genuinely correlates with vomiting (flagship ① correlation, Early tier) + a last-2-days intake dip as a **safety backstop** (② intake_decline) so the Signal card is never empty — both clinically honest (a trigger *found* + a dip *flagged*, never reassurance; the trial-food staple correctly washes out). Mechanism captured: the Home Signal is a **server cache** (`ai_signals`) that needs `generate-signal` **run** after seeding, vs Trend/Patterns/Timeline which render on-device from hydrated SQLite (B-054). Seed = an idempotent, **date-relative, parameterized** SQL script (windows + 24h cache TTL age out otherwise), referencing existing global `food_items` only, credentials only in ASC. **Multi-account seam** per the PM steer → **B-324** (parameterize now, add the picky-cat profile #2 later). Grounded via a codebase research pass (real `detection.ts` thresholds, `lib/sync.ts` hydration list). No detection/AI-read logic changed → **adversarial-reviewer N/A this PR** (the spec pre-commits the falsification target — "staple washes out / never reassures" — as mandatory at PR 1, the seed script). Personas: PM ✓ (deferred narrative, ratified D1–D7) · Data Scientist ✓ (threshold grounding + washout property) · Dr. Chen ✓ (clinical honesty + `vet-report-cold-read` gate) · Trust & Safety ✓ (no-secret-in-repo + global-catalog hygiene) · Designer ✓ (every surface must render) · QA ✓ (§13 acceptance criteria). `tsc` + **1222 jest** green (pre-push hook, unchanged by a docs diff); no server CI (0 checks). — shipped via #336
- 2026-07-11 — **Doc reconciliation — B-231/B-283 stale-status fix (process/meta; no build-phase change, no schema, no app code)** — PM asked for a kickoff prompt on "surface app version in-app" (B-231). Investigation found it was **already built and merged**: `lib/appInfo.ts` + `app/settings.tsx`'s version foot shipped as part of the B-283 "You" screen track back on 2026-07-10 (PR #316), but `STATUS.md`'s PR table still called #316/#317/#318 "draft" (confirmed merged via the GitHub API) and `docs/backlog.md`'s B-231 row still read `Open`. Fixed both: STATUS.md PR-table rows now say "merged", `docs/backlog.md` B-231 → `Done — 2026-07-10 (#316, merged)`, B-283's narrative no longer calls the three PRs drafts. No code touched; confirmed no divergence from `main` at wrap. Personas: Product Owner / Backlog Steward ✓ (the reconciliation itself) · Engineer N/A (no code) · Data/Dr. Chen/adversarial-reviewer N/A (no clinical/statistical logic). `tsc` + **1222 jest** green (pre-push hook, unchanged by this diff); no server CI configured (0 checks). — shipped via #334
- 2026-07-11 — **B-311 — medication category colour token (Culprit in-app brand / B-284 N5b follow-up; no build-phase change, no schema, no app-logic beyond a glyph tint)** — the Calendar v3 drill-in was the first surface to want a per-category tint for a *medication* event, but `theme.ts` had only symptom-rose + meal-teal, so N5b shipped med rows neutral and left the mock's untokenised indigo chip unbuilt (indigo is world/ground-only per §1.3). Decided the med treatment: **`colorEventMedication #5B7A9E`** (muted slate-blue — its own hue family; NOT indigo/violet [reserved], NOT teal [sole interactive accent], NOT rose [safety]; a category identity tint, never a verdict; contrast 4.08–4.45:1) + light-circle companion `colorEventMedicationLight #EAF0F7`. Applied to **`DayEventsSheet`** (now colour-codes symptom/meal/med — replaced the binary `isSymptom` on `DayEventDisplay` with a **theme-free `category` discriminator** in `lib/dayEvents.ts` so the lib stays pure + the mapping is unit-tested; component maps via an exhaustive `Record`) **and** the Home **`TodayZone`** strip (the sibling category-coding surface — `isMedication` already existed but wasn't consulted). Selection stays teal (§1.3, unchanged). **`code-reviewer` fix-before-merge folded in**: it caught the identical med-falls-back-to-neutral gap in TodayZone (fixed) and the three-surface drift; History `EventRow` deliberately left symptom-only (adopting med there implies also meal — a broader History call) → filed **B-323**. Adversarial-reviewer **N/A** (a category glyph tint carries no clinical/statistical verdict). Rebased onto fresh `main` at wrap (N3 #333 / B-310 #331 landed mid-session; conflicts in `theme.ts`/`DayEventsSheet.tsx`/`backlog.md` were parallel-additive — both sides preserved; my new backlog item collided on the `B-319` id → renumbered **B-323**). Personas: Designer ✓ (§1.3 accent rule preserved; identity not verdict) · Engineer ✓ (pure/testable discriminator, exhaustive `Record`, no drift) · Data / Dr. Chen N/A. `tsc` clean · **1222 jest** green; no server CI (0 checks). — shipped via #332
- 2026-07-11 — **B-284 N3 — the Culprit loading system + B-313 Patterns "two months" reconciliation** — one PR, two disjoint tracks (kickoff order). **Mock-first:** `docs/culprit-loading-mockups.html` (theme-faithful, live spinner) reviewed before any RN code; PM chose **B-313 = label the windows**, spinner palette = **teal + lavender** (designer's call), N3 = **build as mocked**. **B-313:** `MetricCard` gains a `Last 30 days` caption (symptom-count only) + `dashboardCards.ts` month window phrases → `the previous 30 days`/`in the last 30 days` (was `last month`/`this month`), so the trailing-30 count never contradicts the calendar-month grid under the same title; both spec-locked windows + the §13 #2 dashboard convention intact. **N3 (spec §5–§6):** new **`WhorlSpinner`** (4 counter-rotating arc ridges 9/14/19/25s + 2.6s breathe; sm/md/px; day/night grounds; `tint` for coloured buttons; reduced-motion static; pause-on-blur) — rotates **RN `Animated.View` transforms**, NOT react-native-svg `<G>` rotation (which exposes only `matrix` natively → frozen under `useNativeDriver`; the review's #1 catch); **`Skeleton`/`SkeletonCard`** Tier-1 on the Patterns load; **~30-site `ActivityIndicator`→`WhorlSpinner` sweep** (grep-gated; `PrimaryButton` the one exception); reusable **`NightGround`** + **`NightMoment`** (§6 full-screen wait, self-measuring so it never traps a screen's back, min-hold + dissolve) at cold-start (`ColdStartOverlay` rebuilt), vet-report build, food/med photo-extraction; **`PullToRefreshSky`** night band on Home (refresh bumps `hydrationTick` so Today/Trend refresh too, not just Signal); new `hooks/useAppActive` + `colorWhorlRidgeDay` token. **`code-reviewer` fix-before-merge → all 4 applied:** (1) native-driver rotation rewrite; (2) night-moment initial-mount-as-visible flash → firstRun guard; (3) report/capture kept-mounted-and-toggled so min-hold/dissolve run; (4) Home refresh `hydrationTick` bump. Adversarial-reviewer **N/A** (nothing clinically/statistically load-bearing — the sweep + copy-framing touch no detection/correlation/AI-read logic). Personas: Designer ✓ (mock, 3 tiers, one-accent + `tint` justified) · Engineer ✓ (native-driver mechanism, zero new deps) · QA ✓ (grep gates + reduced-motion matrix) · Jordan/Sam ✓ (log path untouched) · Data/Dr. Chen N/A. **On-device QA (PM):** whorl actually rotates (review's #1 risk); D7 night-moment scale/opacity lock; no flash on report/capture moment. Backlog: **B-313 Done**, **B-321** (NightGround/NightHeroGround dedup) + **B-322** (CulpritMark same native-driver gap) filed. `tsc` + **1198 jest** green (pre-push hook); no server CI (0 checks). — shipped via #333
- 2026-07-11 — **B-310 — multi-symptom + intake access on the Patterns calendar (Culprit in-app brand / B-284 N5b follow-up; no build-phase change, no schema, no app-logic beyond Patterns)** — replaced the single auto-picked "dominant symptom" grid with ONE "Calendar" card (the `Vomiting → Calendar` rebrand) whose wrapping `ChipGroup` lens selector covers every active symptom (dominant first) — closing the invisible-second-symptom gap for a two-symptom diet trial; switching symptom needs **no refetch** (month buckets already carry every type's `byType`). Added a **"Meals" intake-decline lens** (new `computeIntakeDeclineFrequencyForMonth` / `getIntakeDeclineByMonth`: per-day count of unfinished meals — rated / non-treat / non-free-fed, the exact **inverse** of the "Meals finished" rate; unfloored + descriptive), shown ONLY when the current month carries a real decline, so it never presents a clean month as a reassuring green field (§11 #2). `SymptomCalendar`→**`PatternCalendar`**; `FrequencyCalendarCard` gained back-compat `noun`/`unit`/`selector` props; grid stays symptom-scoped (no meal/med pips — PM A/A/A). **`adversarial-reviewer` PASS** (no false-all-clear on any §11 path: all-unrated month → no lens; free-fed refusal excluded like the rate; failed/loading month → error/retry; A→B pet-switch isolated) + **`code-reviewer` ship-ready**; folded in the unrated-case copy fix (definition names "…or even rated"), a shared `createDayBuckets` helper, and direct card-prop tests. Fixed the stale "darker square" frequency definition (N5 pips). Filed **B-319** (intake lens current-month-gated) + **B-320** (unrated-tail illusion + free-fed blindness → PM/Designer, ties B-312). Personas: Designer ✓ (rebrand + wrapping selector P3/5/6) · Engineer ✓ (no-refetch switch, race guards, DRY) · Data Scientist ✓ (decline = provable inverse of the rate) · Sam ✓ (intake-is-not-preference, never reassures) · Dr. Chen N/A. `tsc` clean · **1203 jest** green; no server CI (0 checks). — shipped via #331
- 2026-07-10 — **B-267 — iOS camera + photo-library permission strings (App-Store hard blocker; config-only, no schema, no app code)** — closed the code half of B-267. Added the `expo-image-picker` config plugin to `app.json` `plugins` with **Culprit**-branded, purpose-stating `cameraPermission`/`photosPermission` copy (states *why* — a photo of a meal, symptom, medication label, or the pet; run through `nyx-voice`); `microphonePermission: false` suppresses the unused `NSMicrophoneUsageDescription`/Android `RECORD_AUDIO` (re-verified every image-picker call site on current `main` is `mediaTypes: ['images']` — no video capture). `expo config --type introspect` confirms both NS keys inject (resolved app name `Culprit`) + the mic suppression. **The session branch was 24 commits stale** (started before the Nyx→Culprit rebrand + PR #299 went conflicted) — reset onto fresh `main` and re-applied the fix + doc reconciliation, which caught the copy that would otherwise have shipped stale "Nyx". Scope-limited to permission strings — does NOT include B-269's `supportsTablet: false` flip. Residuals on the built artifact (introspect can't check): verify both keys on the real `Info.plist` + confirm `PrivacyInfo.xcprivacy` is present. Backlog B-267 + `docs/app-store-readiness.md` reconciled to `Config fixed — #299`. Personas: Dir. of Engineering ✓ (config + introspect verify) · Designer/nyx-voice ✓ (permission copy) · Data/Dr. Chen/adversarial-reviewer N/A (config only). `tests: N/A — config-only app.json change`; `tsc` + **1079 jest** green (pre-push hook); no server CI (0 checks). — shipped via #299
- 2026-07-10 — **Logging-habit discovery kickoff — PM's parallel ChatGPT research pass added (docs-only; no build change, no schema, no app code)** — the PM ran an independent discovery pass on the same logging-retention problem in ChatGPT and asked it committed alongside the in-session discovery doc. Added **`docs/logging-habit-discovery-kickoff.md`**: the attention/recall reframe ("the user fails to think about logging at the moment an event occurs" vs. a friction problem), the shared-caregiving accountability observation, the three problem spaces (reduce friction / increase recall / eliminate logging), Lock Screen surfaces (quick-log widget, Live Activity, status widget), ambient-progress/visibility framing, and predictive/confirmation logging. Real overlap with the already-filed **B-288**/**B-290**/**B-291** tracks from `docs/logging-capture-discovery.md` — landed as a second source input alongside it, no synthesis or backlog changes made. Added a one-line cross-reference between the two docs. No CI checks configured on this repo; `tsc` + **1075 jest** green (pre-push hook). — shipped via #320
- 2026-07-10 — **Logging friction & capture surfaces — PM discovery workshop (process/discovery; no build change, no schema, no app code)** — the full workshop: capture-fidelity + household + friction-is-outside-the-app reframes, the event-class taxonomy, the iOS surface ladder (Expo-today vs CNG-targets vs later, post-WWDC26-checked), the no-gamification call, and the A ⟂ C → B → D/E/F investment sequence with kill criteria. Deliverables: `docs/logging-capture-discovery.md` + backlog **B-288–B-296** + **3 CLAUDE.md Open Questions** (confirmation-vs-nudge-cap w/ Designer dissent · household primitive · native targets) + a PM-decisions item above. Narrows the push-provider OQ (local scheduling needs none). Docs-only; 1075 jest green. — shipped via #319
- 2026-07-10 — **Settings PR 1 — support + version primitives (App-Store-readiness / "You" chunk, B-283 — PR 1 of 5; no build-phase change, no schema, no app UI)** — built the pure, no-UI primitives layer per `docs/nyx-settings-requirements.md` §10, so PR 2 (the "You" screen) and PR 4 (feedback composer) consume settled helpers. **`lib/support.ts`**: `buildSupportMailto(email, {version, build, platform, subject?, body?})` → a **percent-encoded `mailto:`** prefilled with app version + platform (§D6 — triage never starts with "what version are you on?"); `formatAppVersion(version, build)` → "1.0.0 (build 1)", robust to a missing read (→ "unknown", drops the "(build N)" suffix when absent — §4.5, never blank; `build: 0` preserved). **`constants/links.ts`**: `SUPPORT_EMAIL=support@getculprit.app`, canonical `getculprit.app/privacy|/terms`, `LEGAL_LINKS_ENABLED=false` (the §D5 gate — Privacy/Terms render "Coming soon" until the hosted docs are live; App Review *visits* the URL so a 404 is worse than an honest not-yet). Kept **pure**: version/platform injected by the caller (from `expo-constants`, B-231, at the UI layer — no `expo-constants`/`Platform` imports here), so both helpers unit-test trivially and PR 4 reuses `buildSupportMailto` with a `[Feedback]` subject (§D8) with no signature change. Co-located unit tests both files. **`code-reviewer` ship-ready** — adopted its 2 worthwhile nits (an `email`-contract doc note so a future caller doesn't pass a display-name form; a `build: 0` regression test guarding a future "simplify to test `build` directly" refactor), skipped the CRLF-vs-LF mailto-body one (reviewer confirmed `\n` is the de-facto convention across Gmail/Apple Mail/Outlook). Personas: Engineer ✓ (pure/tested, `expo-constants` at the UI boundary) · nyx-voice ✓ (mailto subject + version/platform footer plain, no exclamation) · Designer / Data / Dr. Chen / QA N/A (no UI, no clinical/statistical logic). Adversarial-reviewer **N/A** (nothing clinically/statistically load-bearing). No schema; `tsc` clean · **1068 jest** green (+17 new); no server CI (0 checks). **B-283 → Partial** (PR 1 of 5). — shipped via #315
- 2026-07-10 — **"You" (profile/settings) screen — build-ready spec + 5-PR plan + Notifications/feedback mocks (App-Store-readiness / B-283; process/meta, no build change, no schema, no app code)** — PM asked to operationalize `support@getculprit.app` in-app; the product-team pass found the Pet tab doing double duty (owner name / Sign out / Delete account are *user* actions on a pet-scoped tab) and four App-Store items (B-229/230/231/270) + the support email (B-273) with no in-app home. Produced **`docs/nyx-settings-requirements.md`** (v0.1): D1–D10 (entry = **avatar in the Home header** ✓PM, a doorway not a 5th tab; name = **"You"** [over "Profile", which collides with the pet's profile]; **relocate** Sign out + Delete + owner name off the Pet tab ✓PM; legal links behind `LEGAL_LINKS_ENABLED`; support/feedback via `mailto`), scope expanded per PM to reserve **Notifications** + **App feedback** — both **mocked** in v1, with the notifications mock showing a not-live state with **no armed medication reminder** (clinical-guardrails safety gate). **5-PR plan** (primitives → You screen + entry + relocation → Notifications mocked → App feedback → go-live flips; PR3 ⟂ PR4 parallelizable; **zero schema**). Mockups **`docs/settings-mockups.html`** (theme.ts-faithful). **B-283** filed. Merged `origin/main` **#312** in on wrap (clean). Personas: Designer ✓ · Engineer ✓ · Trust & Safety ✓ · Product Owner ✓ · nyx-voice ✓ · Jordan ✓ · Data/Dr. Chen/QA N/A · Adversarial-reviewer N/A (docs/spec). No app code; no schema; **1046 jest** green; no server CI (0 checks). — shipped via #313


