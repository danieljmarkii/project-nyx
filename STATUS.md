# Project Nyx — Status

_Canonical answer to "where are we?". High-churn: update inline at session end and any time these change mid-session. CLAUDE.md is the stable operating manual; this file is the volatile state. **Keep it scannable** — prose narrative and build history belong in PR descriptions + git, not here (the file is reconstructable via `git log -p STATUS.md`)._

**Last updated:** 2026-07-09 — **Culprit rebrand PR C — `colorBrandNight` indigo token shipped (branding track / B-275 palette half; no build-phase change, no schema, no app-logic change):** tokenised midnight indigo in `constants/theme.ts` as an **additive, world/ground brand colour** — `colorBrandNight #13112E` (the Moon & Signal icon field / marketing heroes / dark brand surfaces) + `colorBrandNightElevated #251F57` (cards/depth on the night). Commented at the token as **world/ground-only, never a tappable accent**: teal (`colorAccent #00C2A8`) stays the **sole interactive accent** (no button / trend-line / live-state repoint), so the design-system "one accent, never decorative" rule survives by construction. **`colorSurfaceDark #101312` reconciliation = KEEP BOTH** (recorded at the token): indigo is the *brand* night identity ground; `colorSurfaceDark` stays the *neutral* near-black photo/premium canvas (paywall, photo viewer) — not repointed (option b would need on-device dark-surface QA and buys nothing; indigo is purely additive). **Pure additive token change — no component adopts these yet, so zero visual regression.** Gates: **Designer / Dir. of Eng ✓** (`colorAccent` unchanged + grep confirms no interactive/tappable repoint + the two dark tokens carry distinct documented roles → one-accent rule holds); **`tsc` clean**; **1038 jest** green (pre-push hook). No schema; tests **N/A** (constants-only file — no store / Edge Function / `lib/` logic). CLAUDE.md indigo Open Question already Resolved (PM adopted 2026-07-08); the `#13112E` value + reconciliation are now locked in-repo. **B-275 → PR C shipped; the palette half is COMPLETE** (icon on-device dark/tinted render still deferred to the next build-cut, per PR B). Shipped via #310. _Prior:_ 2026-07-09 — **Culprit rebrand PR B — Moon & Signal app icon + splash shipped (branding track / B-275; no build-phase change, no schema, no app-logic change):** the design-produced master landed, so the placeholder concentric-circle icon set is replaced with the **Moon & Signal** identity (moonlight crescent + a single teal Signal dot on midnight indigo `#13112E`). Swapped `assets/icon.png` (1024 night master **flattened to RGB — no alpha**), `adaptive-icon.png` (Android safe-zone foreground), `splash-icon.png` (transparent mark), `favicon.png`; **added** `icon-dark.png` + `icon-tinted.png` and wired **`app.json` `ios.icon` = {light,dark,tinted}** (Expo SDK 54 object form, `expo config` accepts it); set `splash.backgroundColor` + `android.adaptiveIcon.backgroundColor` `#ffffff` → the night ground `#13112E`. **Tinted is composited on opaque black** (not shipped transparent) so Expo's `withIosIcons` white-composite (`removeTransparency`+`#fff`) doesn't invert the crescent — verified by reproducing Expo's exact pixel transform + simulating the iOS tint. **Infra held:** bundle ID `com.projectnyx.app`, scheme, slug, owner, projectId all unchanged. Gates: brand colours match the brief exactly (`#13112E`/`#F2EEE4`/`#00C2A8`); **29/40px tray legibility**, **competitor-grid differentiation** (the teal dot is the focal point next to generic moons), **greyscale proof**, and Expo-pipeline **render proxies** (home-squircle / splash / iOS dark / iOS tinted-applied) all pass; `tsc` clean · **1038 jest** green. **iOS dark/tinted on-device render is UNVERIFIED here** (no sharp/simulator/native build in the cloud session, and icons/splash don't render in Expo Go) — must be confirmed on a physical iOS-18 device at the next **build-cut** (preview/TestFlight); if tinted reads wrong, the fix is a hand-tuned dark-ground master (documented). Full design master kit (SVG sources + PNG size-ladder + greyscale proof) preserved at **`docs/brand/culprit-icon/`** (+ README: asset→app mapping + tinted rationale) for ongoing wordmark/Whorl/marketing work. **B-275 → PR B shipped; PR C (`colorBrandNight` token) remaining.** Shipped via #309. _Prior:_ 2026-07-08 — **Culprit rebrand PR A — user-facing `Nyx` → `Culprit` brand strings shipped (branding track / B-274; no build-phase change, no schema, no app-logic change):** flipped `app.json` `name` → `Culprit` (drives the home-screen `CFBundleDisplayName` label) + all 14 user-/vet-facing brand wordmarks across **auth** (landing + login wordmarks, signup verify-email + TOS copy), **home** (`HomeHeader` `Project Nyx` → `Culprit`, codename prefix dropped), **onboarding** (`Culprit Premium`; `ValuePreview` bodies + 3 a11y labels), and the **vet-report** `render.ts` (letterhead, all 4 footers, the "log weigh-ins in Culprit" line, + the `:1669` "Culprit's read" note the naming spec §3.5 missed) — while **holding** every pet-name (`Nyx` the cat) + infra string (`nyx-*` buckets, `nyx.db`, `scheme`, bundle ID `com.projectnyx.app`, `NyxEvent`/`NyxTabBar`, the `nyx-voice` skill). A **QA grep gate proves zero pet/infra collateral**; the dynamic `Patient: ${name}` render was left untouched. Tests updated in lockstep (brand assertions → Culprit; pet-name assertions kept + a new `Patient: Nyx still renders` assertion). Gates: **nyx-voice ✓** (pure brand swaps, no voice drift / no exclamation) · **Designer ✓** (one consistent Culprit wordmark across every surface; no token/layout change) · **vet-report-cold-read PASS on the rebrand question** (regenerated a real Nyx-fixture report → letterhead/footers/weigh-in/appendix-note = **Culprit**; title/name/`Patient:` = **Nyx**; brand-vs-patient split clean, no bug). `tsc` clean · **1038 jest** · **160 deno** (generate-report) green. **Deploy:** `render.ts` is server-rendered → `generate-report` must be **redeployed** (verify_jwt=true preserved) for the report change to reach users; the ~208 KB bundle exceeds a single inline-MCP transfer from an in-progress session, so it was delegated to a fresh-context deploy agent — **PM owns final confirmation** (deploy via the Edge Function dashboard / `scripts/deploy-edge.sh generate-report` if it didn't land). **B-274 → Partial** (PR A shipped; PR B icon gated on the design master, PR C indigo token ready). Shipped via #307. _Prior:_ 2026-07-08 — **Auth session-persistence fix — chunked SecureStore adapter (bug fix on the Step 1 auth foundation; no schema, no build-phase change):** owners were being forced to re-login frequently. Root cause: `lib/supabase.ts` persisted the whole Supabase session (JWT + refresh token + user object, ~2.5–4 KB) through a single-key `expo-secure-store` adapter, but SecureStore caps each value at **2048 bytes** → the write silently failed and the session never durably persisted; `autoRefreshToken`'s ~hourly rewrite made it recur. Fix: new `lib/secureStore.ts` `ChunkedSecureStoreAdapter` — splits the value across ≤512-char keys (each provably under the ceiling), surrogate-pair-safe boundaries, **generation-pointer atomicity** (a crash mid-write keeps the old session intact — closed a corruption path `code-reviewer` caught), and a legacy single-key fallback+migration so existing installs aren't logged out on upgrade. `code-reviewer` **ship-ready** (re-verified after the atomicity fix). 16 new adapter tests; **1038 jest** green, tsc clean. B-279 filed for two non-blocking residuals (no concurrency mutex — relies on supabase-js serialization; unbounded gen counter). Shipped via #306. Not tied to a build step — orthogonal to Step 9 / B-251. _Prior:_ 2026-07-08 — **Culprit rebrand execution plan (process/meta; no build change, no schema, no app code):** turned the two Culprit specs (naming #304 + icon #303) into a build-ready **PR-by-PR plan** — `docs/culprit-rebrand-execution-plan.md` (via #305): **PR A** name + in-app/vet-report brand strings (ready now, no dependency; deploys `generate-report`), **PR B** icon + splash (gated on the design-produced Moon & Signal master), **PR C** `colorBrandNight` indigo token (own theme PR). PM calls this session (via AskUserQuestion): **wait for a design master** for the icon; **adopt the indigo token now** (resolves the CLAUDE.md indigo Open Question → additive & background-only, teal stays the sole *interactive* accent); **plan-only** (no app code touched). QA catch: a re-grep found a 5th vet-report brand string the naming spec's §3.5 missed — `render.ts:1669` ("Nyx's read"), folded into PR A. **PR A ⟂ PR C** disjoint (brand strings/`render.ts` vs `theme.ts`) → parallelizable; **PR B is the long pole** (external design). Backlog **B-274/B-275** reconciled; CLAUDE.md indigo Open Question marked Resolved. _Prior:_ 2026-07-08 — **Culprit name-rebrand plan (process/meta; no build change, no schema, no app code):** researched every `Nyx` occurrence and split **brand vs. pet-name vs. infra**; wrote the build-ready naming spec **`docs/culprit-rename-requirements.md`** — ~14 user-facing brand strings across 8 files + 3 test files become "Culprit" (`app.json` `name`, the auth/home/onboarding wordmarks + paywall, and the vet-report letterhead/footer), while **explicitly holding** bundle ID `com.projectnyx.app`, the `nyx-*` Storage buckets, `nyx.db`, the `nyx-voice` skill, and every "Nyx"-the-pet fixture (the PM's cat). Pairs with the **B-275 "Moon & Signal"** icon direction for a combined **"name + icon"** pass in a later session. Updated **B-274** to point at the spec + resolve its scope question; filed **B-276** (internal `Nyx*` identifiers), **B-277** (docs codename sweep), **B-278** (deep-link scheme) as deferred. See Recent Sessions. _Prior:_ 2026-07-08 — **Culprit brand & icon direction (process/meta; no build change, no schema, no app code):** product-team review of the 24-option *Culprit Logo Explorations* (4 turns); PM delegated the pick to the team → aligned on **"Moon & Signal"** (moonlight crescent + a single teal Signal-dot). Teal stays the one interactive accent; **midnight indigo** proposed as an additive *background* brand colour (`colorBrandNight` — new CLAUDE.md Open Question); crescent-C + teal-tittle wordmark (3f); Whorl motif (3d); pet-warmth secondary; red kept off-brand. New: `docs/culprit-icon-brand-direction.md` + `docs/brand/culprit-icon-moon-signal.svg`; **B-275** filed; rendered pitch shipped as a Claude artifact. See Recent Sessions. _Prior:_ 2026-07-08 — **App Store submission — guide step 1 DONE (PM dashboard action; no build change, no schema, no app code):** PM verified the App Store Connect record (Apple ID `6777691423`, Bundle ID `com.projectnyx.app`, status "1.0 Prepare for Submission") and **locked the store name**. App is rebranded **Culprit**: Name = **"Culprit — Pet Health Tracker"** (28/30), Subtitle = "Track symptoms, find triggers" (29/30), Category = Lifestyle (primary) / Health & Fitness (secondary). Tracker in `docs/app-store-submission-guide.md` → step 1 ✅; **B-272 closed**. New follow-up **B-274** (align on-device app name + repo branding Nyx → "Culprit"; do the `app.json` `name` flip before the step-10 build; bundle ID stays `com.projectnyx.app`). Next long-lead items (all independent, startable now): step 2 (legal docs — highest leverage, feeds the step-14 privacy label), step 3 (web presence), step 4 (production SMTP). _Prior:_ 2026-07-07 — **App Store readiness, round 2 — expert advisory pass + step-by-step submission guide (process/meta; no build change, no schema, no app code):** PM asked for an expert submission-advisory review of the #297 register; findings applied as doc updates, not a memo. Register updated (demo account **B-271**, ASC-record verification **B-272**, live web presence **B-273**, privacy-manifest check riding B-267, a concrete **flag-the-paywall-off-for-v1** recommendation that takes the freemium decision off the submission critical path, `supportsTablet: false` + 6.9" screenshot guidance); backlog rows B-271–273 added, B-267/B-269 sharpened; **new `docs/app-store-submission-guide.md`** — the sequenced 16-step runway with per-step links/tips, kickoff prompts for every PR-shaped step, a PM confirmation protocol, and a live progress tracker. Shipped via #300. _Prior:_ 2026-07-07 — **App Store readiness pass (process/meta; no build change, no schema):** groomed `docs/backlog.md` for App-Store readiness (reconciled B-039 draft→merged; added hard-blocker **B-267** iOS permission strings + B-268/B-269/B-270) and added the consolidated register **`docs/app-store-readiness.md`** — shipped via #297 (merged), this wrap entry in a small follow-up PR. See Recent Sessions. _Prior:_ 2026-07-06 — **Doc correction (process/meta; no build change):** the Step 9 vet-report block below ("After Step 10 …") had drifted stale — it framed **PR 7 (incident photos)** as upcoming, but git shows PR 7 **shipped #278**, **deployed live as `generate-report` v8 #281**, and was hardened by **B-244 #282** + **B-218 #283**. Corrected the block to actual state; the open Step 9 work is now the PM **real-vet R1 email**, **B-243** (interactive tap-to-exclude photo review), then **PR 6** (public link, B-218 blocker cleared). Backlog verified already-current for every touched B-ID. No code, no schema. Prior substantive session: onboarding B-251 PR 10 (#295) — see Recent Sessions.

---

## Current Phase

**Step 10 — AI Signal (`generate-signal`).** B-045 (3-step plan, no Step 4) shipped & merged:

| Step | What | Status |
|---|---|---|
| 1 | Deterministic detection engine (`detection.ts`) — case-crossover (B-050) + intake-decline flag | ✅ #72 (29 tests) |
| 2 | `generate-signal` Edge Function — detect → curate → Haiku-phrase → cache findings set (24h TTL); migration 015 (`findings jsonb`) | ✅ #74 (migration #73) |
| 3 | `SignalZone` wired to cached findings (per-type renderer, confidence tags, tap-to-expand, live/building/stale/`no_pattern`, async regen) | ✅ #75 |

**Architecture:** deterministic detection + LLM phrasing — the server computes & ranks a *true* finding in `detection.ts`; Haiku 4.5 only renders copy, with a deterministic template fallback. `phrasing.ts` = pure phrasing/curation/guardrails (offline unit-tested); `index.ts` = I/O shell. `validatePhrasing` rejects model drift (no `!`; never-reassure / never-"picky" on safety; associational-only on correlation).

**Signal engine — detectors live (deployed `generate-signal` v23):** ① case-crossover, ② intake-decline, ③ reflection (presence/counts; a *declining* trend routes to safety), ④ symptom-worsening lane (B-077), ⑤ post-prandial timing (B-078), ⑥ time-of-day clustering (B-079), ⑦ symptom-chronicity safety lane (B-182 — live v23, confirmed on real data 2026-07-01); + no-signal coverage diagnostics (B-053, incl. **B-070** ≥80%-dominance staple-washout + treats-vs-meals copy register, live v21); + **medication confounders** (B-117 PR 9 #207 — a drug on-board in a symptom window enters `detectCorrelations` as a confounder: case-enriched → suppress, concordant → cap at Early; **+ B-156 PR C1 #234 reads the dose↔vehicle pairing — a drug-in-food combo is attributed to the drug, the vehicle food's protein dropped from the exposure set, + the B-174 in-doubt-dose reconciliation; adversarial-PASS, **deployed v22** read-back-sha-verified + boot smoke-test**). Open follow-ups: **B-052** write-time protein normalization (read-time half shipped #92); **B-067** reflection/Trend dedup; **B-080** diet-structure placement (blocks descriptive Phase 3, PM call).

**On-device QA gap:** empty / `no_pattern` + reflection paths verified on device; the **LIVE safety-card path is now verified** — B-182 chronicity fired on Nyx's real data (2026-07-01 QA), the first live safety card seen on device. The correlation/intake safety lanes still legitimately yield zero on Nyx's healthy data (chicken ~3×/day staple → case-crossover washes it out; intake healthy → flag quiet), so those specific lanes remain unexercised live.

**After Step 10:** Step 9 (vet report) resumes. **Design + spec updated this session (2026-07-02):** discovery (#214) → v1 mock (#216) → **`docs/nyx-vet-report-requirements.md` — now build-ready v2**, folding in the **v3 + cat design mocks** (`vet-report-mock-v3.html` calm diet-trial dog; `vet-report-mock-cat.html` safety-led cat; v1 `vet-report-mock.html` + v2 kept for comparison), a **9-lens synthetic panel review** (Dr. Chen cold-read = CLINIC-READY, fix list applied), and a **real-data dry-run against the pet Nyx** (23 real vomits → the report renders *safety-led*: chronicity + a possible-foreign photo; validated the design end-to-end + surfaced §7.1 data-layer reqs — empty weight state, owner/neuter schema gaps, AI-status denominators, de-dup, free-fed, 90-day fallback). New decisions: **present-only phenotype**, **photos split-by-type** under mandatory §8 privacy controls, **weight (B-186) + empty state**, **stool characteristics**, **medications & supplements**, **branding = keep letterhead / kill footer marketing**. **§12 PR plan REORDERED per PM: Phase 1 full functional report → Phase 2 publish+share+photos → Phase 3 seamless-sharing niceties.** Preconditions **ALL RESOLVED** (B-044 #218 / B-115 #219 / B-028 #220). **Real-vet gate reframed (PM): n=1-first** — PM emails the real Nyx report to their own GP + books a Nyx appointment, in parallel with the build; the first real-vet reaction lands **before Phase 2**. **§8.2 HTML-first RATIFIED (#260).** Phase 1: **PR 1** (`report.ts`) shipped #261, **PR 2** (`render.ts`) shipped #262, **PR 3** (clinical must-carry hardening + B-213 intake-decline duration + traceable meal appendix) shipped #263, **PR 4** (diet/weight/stool + WSAVA hardening — 7 findings fixed across cold-read round-2 CLINIC-READY + adversarial round-3; the GP-0 confound note now names standing/overlapping interventions, free-fed contamination surfaces in Appendix C + B under a trial, off-diet double-counts closed) shipped #264 — all pure, `deno test`-covered, adversarial + cold-read-gated. **Phase 1 (the full functional clinical report) is COMPLETE.** **Phase 2: PR 5** (owner-facing MVP — authenticated `generate-report/index.ts` I/O shell + in-app WebView + on-device PDF share) shipped **#265** (`rls-privacy-reviewer` PASS on the authenticated boundary; `code-reviewer` fix-before-merge all applied; `generate-report` deployed, NEW fn `verify_jwt=true`). **2026-07-03: the FIRST REAL on-device artifact landed + a four-agent panel review + feedback round 1 (bug fixes) shipped** — see the Last-updated entry above + `docs/research/2026-07-vet-report-first-artifact-review.md`. **Phase 2 shipped since (corrected 2026-07-06 — this section had drifted; git is the source of truth):** the §5 PM decision menu was ratified (#267) → **B-221 round-2 design changes** (#268) → **feedback round 2** (severity cut, appendix reorder, wet-meals surfaced, protein-over-time chart, jargon + design pass — #273); **B-222** owner-facing range control (Default + Custom) shipped **#276**; **PR 7 — ALL incident photos in the authenticated report + PDF** shipped **#278** — every photographed in-window incident renders into Appendix E and bakes into the HTML/on-device PDF, safety-flagged (blood/foreign) photos also lead the safety band; `index.ts` fetches each photo ONLY through the EXIF/GPS-stripping + downscaling Supabase image transform (never the raw original), embeds as a `data:` URI (no signed URL persisted), service-role reads only paths from the RLS-scoped enumeration of the verified owner's pet; owner *visibility* = `photo_count` → `app/report.tsx` "Includes N photos". Gates: **`rls-privacy-reviewer` PASS**, **`vet-report-cold-read` CLINIC-READY** (fixed 2 bugs it caught — Appendix-E analysis↔photo divergence disclosed; chronicity `daysSinceLastEpisode` UTC→local, B-246), **`adversarial-reviewer` PASS**; no schema. **Deployed live as `generate-report` v8** (#281, byte-verified; folds in the B-222 zoom fix). Follow-on hardening from PR 7's rls-privacy pass: **B-244** (bind `storage_path` to pet + owner-scope the bucket read) shipped **#282** (CHECK applied to prod 2026-07-05) and **B-218** (drop the `vet_reports_public_share` anon-enumeration policy) shipped **#283**. **Phase 1 + the owner-facing MVP + authenticated photos are COMPLETE and LIVE.** **Genuinely next:** the PM **real-vet R1 email** (emails the regenerated Nyx report to their GP + books the appointment — a PM action; gates the irreversible public path); **B-243** — the interactive **"tap to exclude any photo"** pre-send review (PR 7 shipped owner *visibility* and deferred the interactive deselect; additive, no schema); then **PR 6** — the public token `view-report` route + revocable everyday-photo delivery (split-by-type / token-streaming) + owner link-revocation (B-143), the first unauthenticated path (`rls-privacy-reviewer` MANDATORY; its **B-218 blocker is already cleared** — #283). See `nyx-vet-report-requirements.md` v2.1 (2026-07-04) + §12.

---

## Parallel Tracks

### B-251 Onboarding experience revamp (app-store readiness) — PRs 1–10 shipped; the flow is COMPLETE end-to-end; only PR 11 (functional social auth) remains
Spec `docs/nyx-onboarding-requirements.md` (v1.3, design-locked; §9 = 11-PR plan) + committed mockup `docs/onboarding-flow-mockups.html` (v3). Burn-down of v0.1 (2-screen name+species) → Signal-led **Landing** (logo + persistent auth + data-rich previews) → account (owner first/last/email; Apple/Google **mocked**, email functional; TOS/privacy mock) → pet setup (type+name **required**; breed/gender/age **skippable**; dual integer-OR-birthday age) → **mocked** paywall → completion + durable `onboarding_completed_at`. **PRs 1–4 shipped (#288 / #287 / #286 / #289):** PR 1 owner-profile schema (migration `027` + `updateOwnerName` helper) · PR 2 age-precision schema (migration `028`, resolves S2) · PR 3 design-system primitives (`TextField` + `PrimaryButton loading`) · **PR 4 onboarding shell** — the durable `onboarding_completed_at` gate (pure, unit-tested `decideOnboarding` §6 seam) replacing the fragile "≥1 pet" inference, `ProgressBar`, and the `_layout` back-nav chrome; `hooks/usePet.ts` rewritten (two-read orchestration + retry/never-false-onboard-on-error/throw guards, hook wiring test); `code-reviewer` fix-before-merge FIXED; no schema. **PR 5 shipped via #290:** the Signal-led **Landing** — `app/(auth)/index.tsx` (logo + a horizontally-paging stack of 3 value previews **leading with the Signal** + tappable ≥44pt page-dots as the a11y swipe-alternative + a pinned Create-account/Log-in) + `components/onboarding/ValuePreview.tsx` ×3 (Signal/log/vet-report, **built from real component styles** — elevated surface, Newsreader headline, `EventIcon` tiles, accent sparkline); `app/_layout.tsx` routes unauthenticated → Landing (B-039 just-deleted → login preserved); `PrimaryButton` gains a teal `accent` variant (PM-ratified over near-black, B-256). Reviews: nyx-voice ✓ / pm-feature-review SHIP-SHAPED / code-reviewer fix-before-merge applied (de-causalised the Signal sample per `generate-signal` `CAUSAL_RE`; page-dot 44pt). No schema; tsc + 942 jest green. Follow-ups B-257/B-258. **PR 6 shipped via #291:** account creation + soft verify — `app/(auth)/signup.tsx` reworked onto `TextField`/`PrimaryButton` (first/last/email/password + calm inline validation; mocked TOS/Privacy; Apple/Google hidden behind `SOCIAL_AUTH_ENABLED`; soft "check your inbox" state w/ resend + edit-email back-escape + proceed→login). `supabase.auth.signUp` → session-present writes first/last + derived `display_name` via `updateOwnerName` → `/onboarding/pet`. New `constants/flags.ts` + pure `lib/authValidation.ts`. Reviews: `rls-privacy-reviewer` PASS / nyx-voice ✓ / code-reviewer fix-before-merge applied. No schema; tsc + 959 jest green. Follow-up: **B-152** (harden the soft-verify path + 2 rls dashboard checks before email confirmation is re-enabled). **PR 7 shipped via #292:** the two REQUIRED pet screens — `app/onboarding/pet-type.tsx` (Cat/Dog tiles, no default → explicit tap → Continue) + `app/onboarding/pet-name.tsx` (`TextField` → inserts `{user_id, name, species}` → Home; PRs 8/9 UPDATE breed/gender/age onto the same row) + shared `components/onboarding/OnboardingHeader.tsx` (`canGoBack()`-aware back + `ProgressBar` steps 1–2 of 5) + new transient `store/onboardingDraftStore.ts` (type+name survive back-then-forward; reset on sign-out via `wipeLocalSession`, FR-9 parity). `usePet.ts` + PR 6's `signup.tsx` redirect repointed `/onboarding/pet` → `/onboarding/pet-type`; v0.1 `pet.tsx` + orphaned `food.tsx` deleted (enacts D3 — no food step). Reviews: nyx-voice ✓ / Designer (P1/P5) ✓ / Jordan + Sam ✓ / code-reviewer fix-before-merge applied (draft-store back-then-forward + null-species escape hatch + `radiogroup`). No schema. **PR 8 shipped via #293:** the two SKIPPABLE optional pet screens — `app/onboarding/pet-breed.tsx` (`BreedPicker` species-filtered + free-text "Other"; Continue UPDATEs `breed`, Skip → null) + `app/onboarding/pet-gender.tsx` (Male/Female `ChipGroup`; Continue UPDATEs `sex`, Skip → `'unknown'`, the insert default); `pet-name` now **pushes** `/onboarding/pet-breed` with an **idempotent** insert-or-update (no duplicate pet on back-nav); `OnboardingHeader` gains an optional prominent Skip (`onSkip`/`skipLabel`/`skipDisabled`); breed field-state seed extracted to `resolveBreedFieldState` (shared with `EditPetModal`). Reviews: nyx-voice ✓ / Designer (P1/P5) ✓ / code-reviewer fix-before-merge applied (Skip-vs-Continue save race) / pm-feature-review (follow-ups B-259/260/261). **Closes B-210**; no schema; tsc + 977 jest. **PR 9 shipped via #294:** the age screen — `app/onboarding/pet-age.tsx` (segmented Age/Birthday toggle; Years/Months numeric fields → approximate DOB anchored `today − duration`; `DateTimePicker` birthday → exact; "Not sure? Skip") + pure `lib/age.ts` (`ageToDob`/`birthdayToDob` + `formatAge`/`formatBirthdayField`, 25 tests). Gender repointed to push `/onboarding/pet-age`. **Honesty gate (S2):** approximate DOB never a witnessed birthday — enforced in `lib/age.ts`, applied on Profile (`~2yr`), `EditPetModal` (honest field + precision-preserving write), and the vet report (`generate-report` drops `(b. YYYY)` for approximate). Data/Dr. Chen honesty ✓; nyx-voice ✓; code-reviewer Ship-ready (fixes applied — `resolveDobPrecisionOnSave` extracted+tested, token fix; B-262 wheel-vs-field deviation). No schema (writes 028's column). tsc + 1011 jest + generate-report deno (160). **`generate-report` deployed v9 (2026-07-06)** from the PR-9 branch — the DOB-precision fix + the pending vet-report render work (B-222) are now live; verified (v9 `ACTIVE`, `verify_jwt` preserved, boot smoke-test clean 404 + 401). **PR 10 shipped via #295 (paywall mock + completion — closes the flow's interim terminus):** `app/onboarding/paywall.tsx` (non-functional dark premium mock — no StoreKit; "Maybe later" + a mocked trial CTA both advance; the trial CTA honestly acknowledges Premium isn't live rather than silently no-op; explicit Principle-7 free-tier line; feature bullets reworked to convenience-only after `pm-feature-review` — the mockup's `Multi-pet profiles` ships FREE / `correlations` = care / `90-day history` truncates free trends+report) + `app/onboarding/done.tsx` ("You're all set" → writes the durable `onboarding_completed_at` on mount via `lib/profile.ts markOnboardingComplete()` → `replace('/(tabs)')`; back-gesture-locked terminus) + dark-surface theme tokens; `pet-age finish()` repointed `replace('/(tabs)')` → `push('/onboarding/paywall')`. Reviews: `pm-feature-review` (done SHIP-SHAPED; paywall NEEDS-WORK findings applied → the two blocking product fixes), `code-reviewer` (ship-ready; both NITs fixed), nyx-voice ✓. No schema (writes migration 027's column). tsc + **1022 jest** green. Follow-ups: **B-263** (reconcile the mock's placeholder bullets to the ratified freemium gate), **B-264/265/266** (StoreKit wiring / placement / escape-hierarchy polish). **The onboarding flow is now complete end-to-end (no dead ends) — a build CAN be cut.** **Only PR 11 (functional Apple/Google)** remains — post-v1, needs PR 6 ✓ + a PM-provisioned Google OAuth client. Moots B-038. Gated PM items: `design-principles.md` §Onboarding ratification (doc-drifted — PR 7 removed the food step; spec §5 breed-skip copy also stale) + the Principle-2 softening (spec §12); freemium-gate decision (unblocks B-263); Google OAuth client for PR 11; **B-152**. **STATUS.md is the one shared-file collision to expect at each PR's wrap.**

### B-054 Multi-device down-sync (hydration) — v1 COMPLETE
Spec `docs/multi-device-sync-requirements.md`. Phases 0–3 + §6 cold-start UI all merged (#82–#86); migration 016 (`meals.updated_at`) applied live. Server-time LWW, incremental per-table watermark hydration, meal-ghost absence-reconcile. **Remaining: the physical AC-6 logout-wipe on-device gate** (Trust & Safety; code passes). Phase 4 (Realtime) is post-v1/optional. Residual assumption logged B-058.

### B-117 Medication logging — Phases A–C done; Phase D in flight (PR 9 shipped; PR 10 gated on Step 9)
Spec `docs/nyx-medication-logging-requirements.md` (§12 = 10-PR plan). Model = **regimen + dose-events** (mirrors food: `medication_items`≈`food_items`, `medications`≈`diet_trials`, `medication_administrations`≈`meals`+`intake_rating`). Net-new additive schema (migration 020 applied live; 021 med-photos RLS applied; `nyx-medication-photos` bucket created PRIVATE). Configure regimen once → one-tap dose log. Safety: adherence inherits both invariants (n=1 never reassures; refusal→health flag; missed critical-drug→escalate; AI-extracted dose never silently trusted).

| Phase | PRs | Status |
|---|---|---|
| A — Foundation | 1–3 | ✅ schema (#192) · local mirror+sync (#194) · text-first quick-log (#196) |
| B — Photo + library | 4–6 | ✅ bucket+RLS (#197) · capture + `extract-medication-from-photo` + dose-confirm (#199) · picker detail/edit (#201) |
| C — Regimen + surfaces | 7–8 | ✅ "Current medications" card + compliance % (#202) · timeline + retroactive adherence + double-dose B-135 (#204) |
| D — Clinical consumers | 9–10 | ✅ PR 9 Signal confounder pass (#207 — `detection.ts` `medicationWindows`; case-enriched drug → suppress, concordant → cap at Early; adversarial PASS, fixed a Bonferroni family-shrink; residuals B-138; deployed v19) · ⬜ PR 10 vet-report section (gated on Step 9) |

Carry-forwards: B-122/123 satisfied; B-131 honored; B-128(b) defense-at-rest trigger (own schema PR); open sub-decisions B-132 (library delete), B-133 (`is_critical` owner toggle — PM call); **B-160** (med-details helper chips + copy — ✅ shipped #236).

### B-023 Patterns dashboard — PRs 1–4 merged; PR 5 blocked on Step 9
Spec `docs/nyx-analytics-dashboard-requirements.md`. **No schema** (rides existing tables + `ai_signals` jsonb; migration 018 added an additive `summary`). Build gates resolved: §13 #1 name = "Patterns", §13 #6 colour-as-wellness ruling.

| PR | What | Status |
|---|---|---|
| 1 | Aggregate layer `lib/analytics.ts` + shared `lib/protein.ts` | ✅ #155 |
| 2 | Card components + §13 #6 colour ruling | ✅ #158 |
| 3 | Dashboard screen + Home doorways + `ComingSoonSummary` | ✅ #159 |
| 4 | AI summary — **template-only** (Haiku path built but gated off = B-096); migration 018 | ✅ #160, deployed live |
| 5 | Vet-report bridge ("Share with my vet") | ⬜ blocked on Step 9 |

Open follow-ups: B-094 (decline-watch/diet-trial cards), B-095 (correlations/coverage cards), B-096 (re-enable Haiku summary), B-099 (over-time + dashboard range — reopens §13 #2), B-116 (summary↔card grounding). Shipped polish: #162/#163/#164/#185.

### B-086 Multi-pet v1 — COMPLETE
Spec `docs/nyx-multi-pet-requirements.md`. PRs 1–6 all shipped (#144–#148, #203); **zero schema** (`pet_id`/RLS/`is_active`/`feeding_arrangements` were multi-pet-ready by construction). Single-pet households see no new chrome. PR 6 = cross-pet safety banner + all-active-pets signal freshness (adversarial PASS). Household §7 on-device cases fold into the TestFlight QA pass.

### B-040 Free-feeding R1 — COMPLETE
Spec `docs/nyx-free-feeding-requirements.md`. PRs 1–4 shipped (#119/#121/#122/#123); migration 018 applied live. Schema → capture/view → History rendering (ambient strip + boundary markers + stale-only freshness) → engine ingestion (free-fed protein excluded from candidacy, caps others at Early; adversarial PASS). **Guardrail (every consumer):** free-fed intake is NOT directly observed — never read absence of witnessed intake as "didn't eat"; vet report carries "intake not directly observed" verbatim. Remaining Step 9 consumer: vet-report feeding-method rendering. Open: B-073 (stale-nudge on-device QA), B-074 (exclusion can silence a real finding — awareness).

### Food library / intake
- **B-004 standalone Food Library — COMPLETE** (#176–#182, 7 slices). 4th `Foods` tab, brand grouping, reliable-favorites, row thumbnails; picker stays 2-up tiles (PR 7 decision). + post-QA fixes #183/#184.
- **B-102 `human_food` food_format — in progress** (PRs 1–2, #166/#167). Migration 019 applied live + capture/display parity. Remaining PRs 3–7 (AI-extraction mapping, analytics regression, engine provenance, vet-report line, diet-trial note) — PRs 3/4 are disjoint and parallelizable.
- Open QA findings: B-111 (treats in Top Proteins, clinical call), B-113 (picker↔tab harmonization, #186), B-112/B-114, B-108 (closed #183). Downstream: B-017 (`food_format`/`food_type` overlap, carries B-024/B-102), B-009/B-018 dedup.
- **Food dedup & merge — direction aligned + build-ready DRAFT spec (2026-06-24, #237):** `docs/nyx-food-dedup-requirements.md`. Phase 1 prevention (B-009) first, **no schema**; Phase 2 merge (B-018 + B-005) = repoint-then-tombstone. Survivorship/reversibility/multi-user-auth parked as merge-phase decisions. Awaiting PM ratification → re-prioritizes B-009 to Now.

### B-186 Weight tracking — PRs 1–4 shipped (schema + logging + trend card + History row); v1 = display-only COMPLETE
The 2026-06-25 vet-council deep-dive named a weight **trend** the #1 missing datum (Nyx = zero `weight_check` events on a free-choice *weight-management* diet). **Audit (2026-06-26):** confirmed no existing weight surface — `weight_check` enum + `pets.weight_kg` snapshot exist, but **no value storage** (`events` carries only `severity`/`notes`), no logging UI, no trend surface. **v1 = display-only / descriptive (PM-confirmed): capture + trend, NO loss flag** (a flag is clinically load-bearing → its own spec + mandatory adversarial pass). **Guardrail every consumer inherits:** a weight trend never reassures — stable/rising ≠ wellness (rising can be fluid/edema); loss is the danger signal. Independent of the chronicity-lane work (disjoint files; only STATUS.md collides at wrap).

| PR | What | Status |
|---|---|---|
| 1 | Schema/data layer — migration 024 `weight_checks` (1:1 event child, mirrors `meals`) + local SQLite mirror + `LOCAL_WIPE_TABLES` reg | ✅ merged #244 (applied live, advisors clean) |
| 2 | Logging — `EVENT_TYPES` entry + numeric `weight` log step (pre-filled last weight, lbs) + `insertWeightCheck` + sync up/down + `pets.weight_kg` snapshot to latest reading | ✅ this session — PR #245 (was stacked on PR 1; rebased onto `main` after #244 merged) |
| 3 | Profile weight-trend card (descriptive sparkline + readings + delta; neutral framing, no wellness colour) | ✅ shipped via #248 — `WeightTrendCard` (`getWeightHistory`/`computeWeightTrend` in `lib/weight.ts`; "Log a weigh-in" affordance; empty/single/trend states; guardrail held in colour + copy) |
| 4 | History `weight_check` row + icon | ✅ shipped via #249 — `weight_checks` LEFT JOIN in `getTimeline`/`getEventById` (1:1, soft-delete via parent) + `weight_kg` on `TimelineRow`/`rowToEvent`; `EventRow` renders `${kgToLbs} lbs` as a quiet secondary line (Scale icon already via `EVENT_TYPES`, PR 2); display-only, no verdict |

Fast-follows: **Patterns-dashboard weight card ✓ shipped via #251** — `components/dashboard/WeightCard.tsx`, reading-count-aware placement (populated → safety/health-trajectory cluster, empty nudge → descriptive); shared delta/date copy extracted to `lib/weight.ts`. Remaining: vet-report weight section (Step 9); Home TrendZone weight; weight-loss flag spec (B-190). Card follow-ups: B-192 (empty-state coherence), B-193 (4px token).

### B-247 Stool analysis (per-incident AI read) — spec drafted, awaiting ratification
Spec `docs/nyx-stool-analysis-requirements.md` (§8 = 10-PR plan). Second child of B-013 (sibling of B-027/vomit): no `analyze-stool` exists today despite stool being the primary diet-trial outcome and the highest-volume GI event type; the vet report's "Stool characteristics" section has no AI-read data source. Additive schema only (new `stool_*` enums + columns on `event_ai_analysis` — **no** `stool_normal`/`diarrhea` consolidation, D1). Bristol Stool Scale (Type 1–7) as the consistency taxonomy (D3); Sonnet 4.6 (D4, matches `analyze-vomit`). **Open for ratification before PR 1:** D2 — factor vomit's escalation-floor/image-processing/write-back patterns into a shared `_shared/incident-analysis.ts` module (the `clinical-guardrails` skill's own predicted "second incident type" refactor point) vs. build `analyze-stool` as a standalone copy — Dir. of Engineering call. Draft PR #279 (docs only, not yet merged).

### Design-system v1.2 "Linear Clean" — COMPLETE
Plan `docs/design-system-migration-plan.md`. 4 PRs merged: palette (#99), fonts Geist+Newsreader (#100), Lucide event icons (#101), completion "moment" (#103) + B-064 folded meal card (#105). Open: B-061 app-wide Geist body rollout (open question — wrapper vs shim); B-063 tone-aware-moment design-principles line (PM sign-off). Whole-system aesthetic on-device QA still due.

---

## Blocking Open Questions

- **~~PDF rendering library / §8.2 HTML-first~~ — RESOLVED 2026-07-02 (#260).** PM ratified **HTML-first**: canonical server HTML, rendered **in-app via a WebView** (owner sees it in the app — never a downloaded `.html`), handed to the vet as a **PDF via the native share sheet**. The "which PDF library" Q is demoted to the B-144 render spike; PDF-generation *location* (on-device `expo-print` vs server-side) is a build-time sub-decision (spec §14 S7). **No blocking open questions remain for the vet report — Phase 1 (PR 1) is clear to start.**

---

## Open PM Action Items

**Ship gates / on-device QA**
- [x] **#276 (B-222 range control) — MERGED + `generate-report` deployed v8.** Client parts (Default/Custom, "Done" toolbar, soft-refresh) verified on-device by the PM this session; the #3 zoom fix is now live post-v8. **One residual device check** (not a blocker): on an **iPhone SE**, confirm "Done" + the "Updating…" pill are reachable without scrolling with the calendar open (the one thing pm-feature-review couldn't settle from code).
- [x] **`generate-report` REDEPLOYED → v8 · ACTIVE (2026-07-05)** — the PM's `npx supabase functions deploy` from current `main` landed the B-222 zoom fix (#276) + the PR-7 incident-photos render (#278) in one deploy (`verify_jwt` preserved; sha `292e3b4f…`). **Remaining device confirm** (not a blocker): regenerate on device → report opens **fit-to-width** + eyeball one PDF's pagination is unchanged.
- [ ] **B-222 follow-up decisions (all low-stakes, backlog-tracked):** **B-240** — which weight surface is right (Profile snapshot vs Patterns tracked-only); first confirm via the Profile card subtext ("From Nyx's profile" = snapshot = expected). **#2 behavior** — keep the smart default cascade (rec) vs force 90d always. **B-238** — add a pre-generation out-of-window hint or leave the disclosure in-report. **B-241** — "Default" vs "Automatic" chip copy.
- [ ] **#257 — on-device QA + squash-merge (this session).** Run the Manual QA in the PR: **B-196** (Current-medications card loads, no `PGRST201`), **B-198** (log while History is open → no duplicate-key error), **B-197** (Edit a weight → value field pre-filled + saves; empty/`0` blocks Save), plus a spot-check of weight tracking + the chronicity card. Merge when green. **No backend action** — client-only fixes; `generate-signal` v23 (with ⑦) already live.
- [ ] **#255 `analyze-vomit` + photo-load fixes — on-device confirm (the one open PR #255 gate):** open the **6/25 and 6/27 vomit events** → each shows a **real AI read** (not stuck "Try analysis"); tapping the photo opens full-screen **fast** (not 5–10 s). If a photo instead reads "I couldn't read this photo," the Pro Image-Transformation add-on isn't active (safe no-op — flag me). Verifies the 546 fix + server-side downscale + viewer perf end-to-end.
- [ ] **B-039 PR 2** (#193, merged) — on-device end-to-end deletion QA on a **throwaway** account; closes the App Store 5.1.1(v) blocker. Code + 527 tests pass.
- [ ] **Toggle Supabase "Confirm email" OFF** (Auth → Providers → Email) to unblock the B-039 throwaway-account QA above — new-account creation silently failed under email-confirmation (fixed in #213 to show "Check your email"; re-enable before launch — B-152).
- [ ] **#213 med-QA fixes — on-device spot-check** (Manual QA in the PR): account-creation "Check your email"; strength-gate toggle; "Current medications" counts a logged dose; End-button red; no `meals 23503` on logging an existing food.
- [ ] **AC-6 logout-wipe** — sign out on a populated device → confirm data empties → sign into a *different* account → confirm no prior-pet data shows (Trust & Safety gate; code passes).
- [ ] **App Store readiness — work the 16-step runway in `docs/app-store-submission-guide.md` (#300; register: `docs/app-store-readiness.md` #297+#300).** Start-now PM steps (all parallel, long-lead first): **step 1** verify the ASC record + lock the store name (B-272) · **step 3** pick a host for the support/privacy/terms page (B-273) · **step 4** provision production SMTP (B-152). Ratify the two advisor recommendations riding the runway: **flag the paywall off for v1** (guide step 6 — removes the freemium decision from the submission critical path) and **`supportsTablet: false`** (rides step 5's B-267 config PR). Each guide step has a kickoff prompt (PR steps) or a "Guide step N complete: …" confirmation (PM steps) that keeps the tracker/backlog/STATUS honest.
- [ ] **Culprit rebrand — remaining items** (`docs/culprit-rebrand-execution-plan.md`, #305): PR A (name+strings) shipped #307; **PR B (Moon & Signal icon + splash) shipped #309** — the design-master gate is **cleared**. Remaining: (1) **PR C** (`colorBrandNight` indigo token) — ready to build, no PM prerequisite; (2) **Supabase auth email template** still says "Nyx" (dashboard → Auth → Email Templates) → Culprit; (3) **cut a new TestFlight build from `main` after merging** to make the icon/splash/name visible — they're native/build-time, so an OTA `eas update` won't move them (`eas build --platform ios --profile production --auto-submit`); do the **iOS-18 dark/tinted on-device verification** on that build (unverifiable in the cloud session; icons don't render in Expo Go).
- [ ] **B-053 on-device QA** — Signal regens against v10; the staple-washout diagnostic only shows in `no_pattern` (Nyx may show the reflection finding instead — correct, not a bug).
- [ ] **Confirm AI summary on-device** — open Patterns for Nyx; expect a safety-led, vet-routing summary with every number matching a card.
- [ ] **Whole-system aesthetic on-device QA** — walk the app vs `docs/design-system/_system/.../index.html` for the holistic Calm/Linear/Oura pass (deferred to end-of-system; all 4 design PRs merged).

**Decisions**
- [ ] **B-251 onboarding — ratify the `design-principles.md` §Onboarding revision (Tier-2 edit, flagged not written)** before/around PR 4–5. The constitution still says "minimum viable / required: name+species / first log in 60s / complete-at-first-log"; the design-locked flow outgrows all of it (owner identity + optional pet richness + store gates; finish line moves to profile-created→warm-home) and Principle 2's "food library pre-populated from onboarding" should soften to "on first meal log" (food capture moved in-app). Also: **Google OAuth client** to provision before **PR 11** (Apple Developer already enrolled) — not blocking PRs 1–10.
- [ ] **B-247 stool analysis spec — ratify §2 decisions (esp. D2 shared-module refactor)** before PR 1 starts. `docs/nyx-stool-analysis-requirements.md`; draft PR #279.
- [x] **B-070 deploy mechanism — RESOLVED 2026-06-21 (PM chose pre-verified MCP deploy).** Deployed `generate-signal` v20 via the B-082 MCP path: authored the 80 KB bundle, proved it byte-identical to `scripts/deploy-edge.sh`'s artifact (sha `a4759d20…`) via a scratch-file round-trip BEFORE deploy, then confirmed the live read-back is byte-identical + a clean boot smoke-test. **B-082-path finding (logged, low urgency):** the inline-content transcription is only safe with the scratch-file sha gate; provisioning a `SUPABASE_ACCESS_TOKEN` cloud-env secret so `supabase functions deploy` reads the file directly is the durable fix for large clinically-load-bearing functions (runbook's "optional future convenience").
- [ ] **B-099** — reopen spec §13 #2 (over-time views + dashboard 7d/30d range) before building B-099.
- [ ] **B-080** — placement of diet-structure observations (Signal band-2 vs B-053 coverage lane); blocks descriptive Phase 3.
- [ ] **B-023 colour** — reconcile Home `TrendZone` (colours a falling symptom "improving" in the accent, `TrendZone.tsx:66/79`) to the §11 #3 ruling, or accept the cross-tier difference.
- [ ] **B-065** — should the food-capture add-then-log path also show the meal completion card (intake chips + "Change time")?
- [x] **B-160** — BUILT #236 on the spec's recommend-and-proceed basis (§6): §3 **strength = no value chips** applied (the §6.5-derived call — no PM gate); **S5** regimen modal included; **S1** Dr. Chen starter-set trim applied ("Gabapentin / Trazodone" → Trazodone, since Gabapentin is already listed). _Optional post-hoc:_ PM may tweak the `COMMON_MEDICATIONS` contents/ordering (`lib/medications.ts`) — purely additive data, no rebuild needed.
- [ ] **B-063** — approve the Tier-2 design-principles line (tone-aware "moment" — calm, never festive on a worrying event); awaiting PM sign-off before writing.
- [ ] **PR #79** — env fail-fast guard + CLAUDE.md doc fix: mark ready/merge or close.
- [x] **Vet-report owner band (§8.1 / R4) — RESOLVED 2026-06-22 (PM): band removed; report is vet-only (Strawman A).** Owner's surface = Patterns dashboard (B-023). §8.1 closed → option (a); formalize in the requirements spec.
- [x] **Vet-report spec gates — RESOLVED 2026-07-02 (#260).** (a) **§8.2 HTML-first ✅ ratified** (in-app WebView render + PDF-via-share-sheet locked; PDF-library Q → B-144); (b) real-vet gate **reframed to n=1-first** — PM emails the Nyx report to their own GP + books an appointment (in parallel; before Phase 2); (c) **cat/safety-led mock ✅ built** (`vet-report-mock-cat.html`) + v3 + the Nyx real-data dry-run. Spec is **build-ready v2**; Phase 1 (PR 1) is clear.
- [ ] **Vet report — PM next actions (2026-07-02):** (1) **email your vet the Nyx report + book the appointment** (the real-vet signal, parallel to the build); (2) **weigh Nyx** before the visit (fills the one empty box + the datum the vet most wants).
- [ ] **Principle 6 doc-drift (Tier-2 edit)** — `nyx-design-principles-v1_0.md` §6 **and** `nyx-technical-spec-v1_0.md` §7 AC both still list "severity averages" as desired report content; the mock + spec §8.4 deliberately use frequency-over-severity (Dr. Chen's stated preference). Both cold reads flagged it. Approve the doc-line update on **both** files (+ the §7 must-carry-section / HTML-first / PDF-library-reshape edits the spec §13 flags — all flagged, not written).
- [ ] **Vet-report mock — synthetic-panel feedback logged** (`docs/vet-report-mock-review.md`). PM follow-ups from it: (a) **B&W test-print** the mock (zebra/band gray on a mono laser); (b) decide the spec-level **report-range cherry-pick guard** (disclose events outside a custom window); (c) greenlight the **second mock — cat/safety-led case** (Sam, feline 48h window) to exercise the safety-leads slot. The real-vet R1/R2/R4 panel is the gate (§5 of that doc is reserved for it).

**Cleanup / infra**
- [ ] **B-118** — delete the leftover `smart-worker` Edge Function from the dashboard (stock template, no callers, carries a `secret`-auth path that bypasses RLS).
- [x] **B-044** — DONE 2026-06-22: full live-DB reconciliation — **zero schema drift, nothing to apply.** `vet_visit_attachments` already present (003 gap closed 2026-06-06 by `complete_003_vet_visit_attachments`); `food_items.photo_path` singular is dead (superseded by 007 `photo_paths`; no code reads it). All 22 migrations' objects verified live. History records only 5/22 (001–019 dashboard-pasted, unrecorded) → backfill spun out to **B-162** (latent `db push` re-apply footgun; PM-blessed, low urgency).
- [ ] **B-128(b)** — defense-at-rest `BEFORE INSERT/UPDATE` trigger on `medication_items` (own schema PR; run the backfill pre-check first). Not urgent — the consumer-side guard is live via #200.
- [x] **Deploy `generate-signal` PR C1 (#234) from merged `main`** — DONE (#239): deployed **v22** from merged `main` via the Supabase MCP (`verify_jwt` preserved); live read-back sha byte-identical to the bundle (`0e66e002…`) + clean boot smoke-test. A PM-requested edge-function currency audit also caught + fixed **`analyze-vomit` v4→v5** (B-028 #220 merged but never deployed) and redeployed **`extract-food-from-photo` v9→v10**; `delete-account` v3 / `extract-medication` v1 verified current. Process gap → **B-178**.
- [ ] **Redeploy `generate-signal` to un-gate chronicity detector ⑦ (B-182)** — PRs 1–3 (#246/#247/#250) all merged; the engine registers ⑦ but the **live function predates it**, so chronicity won't surface until a redeploy. Deploy from merged `main` via the Supabase MCP (bundle `scripts/deploy-edge.sh generate-signal` → `deploy_edge_function`, preserve `verify_jwt=true`, byte-gate + boot smoke-test — the B-082 path). The client renderer is live as of #250, so the card renders the moment the cache carries a `symptom_chronicity` finding.
- [ ] **D2 ratification — chronicity `minEpisodes` 6-vs-5 (Dr. Chen)** — the §7 #14 noise gate forced 4→6 (specificity); the PR-1 adversarial review measured a false-negative cost at 6 (a once-weekly-×5 / q2wk-×4 real chronic course is silenced). 6 = credible-surface; 5 = catch-the-once-weekly, defensible because the safe error direction for a safety lane is toward firing. A property test on *real low-count chronic courses* should sit alongside the noise test before the floor locks.
- [x] **Re-deploy `generate-signal` from merged `main`** — DONE 2026-06-21: redeployed **v21** from merged `main` (447af3f) after #211 merged; live read-back byte-identical to the merged-main bundle (sha `a4759d20…`) + clean boot smoke-test. (v20 from-branch had the same bytes; this is the clean-provenance bump.)
- [ ] **Revoke the Supabase personal access token** (`nyx-cli-deploy`, 2026-06-07) — account-level, lives in a session transcript. Now obsolete: B-082 (#208) made backend deploys a token-free MCP path, so nothing uses it.
- [ ] **Supabase CLI dev-dependency** — fold `supabase@^2.102.0` (on branch `claude/epic-volta-H8d6o`) into a PR so it survives merge.
- [ ] _(awareness, no action)_ **B-074** — the free-fed exclusion fails safe but a single stray free-fed day landing on a selected control day can silence a real correlate.
- [ ] _(awareness, before treating vet-report §8.6 as fully closed)_ **B-115 residual → B-163** — the shipped exact-ms treat-collapse (#219) guards the diet-confounder over-count for the **EXIF-collision / future-batch** case only; today's full-precision `new Date()` stamps mean a **rapid per-tap handful (distinct ms) is still over-counted**. Fully closing it needs a real-data-gated near-window widening (under-count tension) or a batch-log path — Phase-2/PR-6 can proceed; just don't read §8.6 as "fully resolved."

---

## Runtime in Use

**TestFlight (real iOS builds) — primary on-device target since 2026-06-07.** Runtime B (Metro `npx expo start --tunnel` + Expo Go) remains available for fast local iteration.

**Cut a new TestFlight build** (verified build 22 from `main`, 2026-06-12) — one command builds the store-signed binary and submits that same artifact:
```
eas build --platform ios --profile production --auto-submit
```
**OTA to the installed build:** `eas update --branch production` (the build's channel is `production`, NOT `preview`).

**Traps (each cost a session — do not repeat):**
- Never build TestFlight with the `preview` profile — it's `distribution: internal` (ad-hoc), never store-submittable. Use `production`.
- Never submit with `eas submit --latest` — it skips internal builds and re-uploads a stale store build (the "build number 8 already used" loop). Use `--auto-submit` (binds the upload to the binary just built).
- "Build number N already used" = EAS counter is behind App Store Connect → `eas build:version:set --platform ios`, then rebuild. (Build numbers live on EAS; `appVersionSource: remote`, `autoIncrement`.)
- All 3 `eas.json` build profiles carry the `EXPO_PUBLIC_SUPABASE_*` `env` block (#90) — EAS cloud builds never see the gitignored `.env.local`; don't strip it (was the first crash-on-launch).
- `app.json` → `ios.infoPlist.ITSAppUsesNonExemptEncryption: false` (#121) auto-answers Apple's encryption questionnaire; keep it committed.

---

## Recent Sessions

_Last ~13 only; older history lives in git (`git log`) + PR descriptions._

- 2026-07-09 — **Culprit rebrand PR B — Moon & Signal app icon + splash (branding track / B-275; no build-phase change, no schema, no app-logic change)** — the design-produced master landed (verified genuine — full Moon & Signal export set + SVG sources, vs. the placeholder concentric-circle grid still in `assets/`), so executed PR B of `docs/culprit-rebrand-execution-plan.md` §4. Swapped the placeholder set for the Moon & Signal identity (moonlight crescent + a single teal Signal dot on midnight indigo): `assets/icon.png` (1024 night master **flattened to RGB — no alpha**, the App-Store master requirement; delivered PNG was all-opaque RGBA), `adaptive-icon.png` (Android safe-zone foreground), `splash-icon.png` (transparent mark), `favicon.png`; **added** `icon-dark.png` + `icon-tinted.png`. `app.json`: `splash.backgroundColor` + `android.adaptiveIcon.backgroundColor` `#ffffff` → the night `#13112E`; wired `ios.icon` = {light,dark,tinted} (Expo SDK 54 object form; `expo config` accepts it). **Engineering call:** the tinted variant ships as a greyscale luminance map on **opaque black** (not transparent) so Expo's `withIosIcons` white-composite (`removeTransparency`+`#fff`) doesn't invert the crescent — proven by reproducing Expo's exact pixel transform + simulating the iOS tint (renders a clean crescent + quieter dot, not a filled tile). **Infra held:** bundle ID `com.projectnyx.app`, scheme, slug, owner, projectId unchanged. Gates: brand colours match the brief exactly (`#13112E`/`#F2EEE4`/`#00C2A8`); **29/40px tray legibility · competitor-grid differentiation** (the teal dot is the focal point next to generic moons) **· greyscale proof · Expo-pipeline render proxies** (home-squircle / splash / iOS-dark / iOS-tinted-applied) all pass. **Honest gap:** iOS dark/tinted + true on-device icon/splash render are UNVERIFIABLE in the cloud session (no sharp/simulator/native build; icons don't render in Expo Go) → confirm on a physical iOS-18 device at the next TestFlight **build-cut**; if tinted reads wrong, the fix is a hand-tuned dark-ground master (documented). Master kit (SVG sources + PNG size-ladder + greyscale proof) preserved at **`docs/brand/culprit-icon/`** (+ README: asset→app mapping + tinted rationale); a self-contained **Moon & Signal brand-reference Claude artifact** published for reuse. Personas: **Designer ✓** (three grounds, tray legibility, differentiation, greyscale — reviewed on the *rendered images*) · **Engineer ✓** (no-alpha master, tinted-pipeline correctness, `ios.icon` schema, infra held) · **PM-lens ✓** (wedge/brand fit on the rendered marks — the `pm-feature-review` subagent reads screen *code* and can't see a PNG, so the higher-fidelity product review is the rendered-image one) · Trust & Safety N/A (no data path). Adversarial-reviewer **N/A** (assets/config — no clinically/statistically load-bearing logic). No schema; `tsc` clean · **1038 jest** green; no server CI (0 checks). **B-275 → PR B shipped; PR C (`colorBrandNight` token) remaining.** — shipped via #309
- 2026-07-08 — **Culprit rebrand PR A — user-facing `Nyx` → `Culprit` brand strings (branding track / B-274; no build-phase change, no schema, no app-logic change)** — executed PR A of `docs/culprit-rebrand-execution-plan.md` §3 against the authoritative inventory in `docs/culprit-rename-requirements.md` (re-grepped every line first — they'd drifted). Flipped `app.json` `name` → **Culprit** (the home-screen `CFBundleDisplayName` label, the single most important change) + all 14 user-/vet-facing brand wordmarks: **auth** (landing + login wordmarks, signup verify-email + TOS copy), **home** (`HomeHeader` `Project Nyx` → `Culprit`, codename prefix dropped, stale-placeholder comment fixed), **onboarding** (paywall `Culprit Premium`; `ValuePreview` 2 bodies + 3 VoiceOver a11y labels + comment), and the **vet-report** `render.ts` (letterhead `:515`, all 4 footers `:1581`, the "log weigh-ins in Culprit" body line `:805`, and the `:1669` "photo findings are **Culprit's** read" note that the naming spec §3.5 missed — caught by re-grep). **Held** (the guardrail): the dynamic `Patient: ${name}` render, every pet-name (`Nyx` the cat) fixture, and all infra (`nyx-*` buckets, `nyx.db`, `scheme`, bundle ID `com.projectnyx.app`, `NyxEvent`/`NyxTabBar`, the `nyx-voice` skill) — a `git diff` grep gate proved **zero pet/infra collateral**. Also updated the two `render.ts` collision comments (brand no longer clashes with the patient name) and corrected the stale `app-store-submission-guide.md:49` on-device-name line. 3 test files updated in lockstep — brand assertions → Culprit, **pet-name assertions kept** + a new explicit `Patient: Nyx still renders` assertion so "brand changed, pet didn't" is locked. Gates: **nyx-voice ✓** (pure brand substitutions — no exclamation, no first/second-person drift) · **Designer ✓** (one consistent Culprit wordmark across auth/home/onboarding/report; pure string swap, no token/layout regression) · **`vet-report-cold-read` PASS on the rebrand question** — rendered a real Nyx-fixture report and the agent verified the brand-vs-patient split is exactly correct (Culprit = masthead/footers/weigh-in line/Appendix-A photo note; Nyx = title/signalment/`Patient:` footer/diet-history prose), no misplaced string; it separately flagged that the *hand-built demo fixture's* clinical counts don't reconcile, correctly diagnosed as a fixture artifact, **not** a render-code or rebrand defect. `tsc` clean · **1038 jest** · **160 deno** (generate-report) green (deno provisioned on-demand to run the offline `render.test.ts`/`report.test.ts`/`index.test.ts` suites). **Deploy:** the vet report is server-rendered, so `generate-report` needs a **redeploy** (verify_jwt=true preserved) to reach users — the ~208 KB esbuild bundle exceeds a single inline-MCP `deploy_edge_function` transfer from this (context-heavy) session, so it was delegated to a fresh-context deploy agent; **PM owns final confirmation** and will deploy via the Edge Function dashboard / `scripts/deploy-edge.sh generate-report` if it didn't land. Personas: Designer ✓ · nyx-voice ✓ · Dr. Chen / vet-report-cold-read ✓ · QA ✓ (grep gate). Adversarial-reviewer **N/A** (no clinically/statistically load-bearing logic changed — copy/config only; the render *engine* is untouched). No schema; no server CI (0 checks; pre-push hook green). **B-274 → Partial** (PR A done; PR B icon gated on design master, PR C indigo token ready — both parallelizable). — shipped via #307
- 2026-07-08 — **Auth session-persistence fix — chunked SecureStore adapter (stops frequent forced re-logins)** — PM reported having to sign in far more often since "the create/login flow update." Investigation traced it NOT to the B-251 onboarding work (owner-name writes land in `user_profiles`, not the session token) but to `lib/supabase.ts`: the auth client persisted the entire Supabase session (JWT + refresh token + user object, ~2.5–4 KB) through a **single-key `expo-secure-store` adapter**, and SecureStore enforces a hard **2048-byte-per-value limit** → the write silently failed, the session never durably persisted, and `autoRefreshToken`'s ~hourly rewrite made it recur (cold start → no session → login screen). **Fix:** new `lib/secureStore.ts` **`ChunkedSecureStoreAdapter`** (swapped into `supabase.ts`, zero new deps, chosen over AES-in-AsyncStorage to keep encryption-at-rest) — (1) splits the value across ≤512-char SecureStore keys, each provably under the 2048-byte ceiling; (2) **surrogate-pair-safe** chunk boundaries (a lone UTF-16 half is corrupted to U+FFFD by native UTF-8 storage — e.g. an emoji in `display_name`; caught + proved pre-review); (3) **generation-pointer atomicity** — each write lands under a fresh generation of chunk keys the reader isn't following, committed by a single atomic pointer write, so a crash mid-refresh keeps the OLD session intact instead of reconstructing a corrupt new-chunk-0+stale-chunk-1 hybrid; (4) legacy single-key fallback + migration so existing installs keep their session on upgrade. **`code-reviewer` fix-before-merge → ship-ready:** caught a real in-place-overwrite torn-write **corruption** path (worse than the original bug) — fixed with the generation-pointer scheme + 2 atomicity regression tests, then **independently re-verified** (its repro now returns the intact old session, not the `"2222"+"1111"` hybrid); both nits also closed (strict `/^(\d+):(\d+)$/` pointer parse; cleanup moved out of the write try/catch), plus a follow-up commit logging the pre-write pointer-read catch (no-silent-failures). Personas: Engineer ✓ (storage-atomicity) · Trust & Safety ✓ (session material encrypted at rest + fully cleared on sign-out) · Data/Dr. Chen/Designer/QA N/A (no clinical/statistical logic, no UI, no schema). Adversarial-reviewer **N/A** (storage atomicity, not clinically/statistically load-bearing — the `code-reviewer` atomicity pass is the right lens and ran). **B-279 filed** (2 non-blocking residuals: no concurrency mutex — relies on supabase-js's internal storage serialization; unbounded generation counter — orphans only on sustained cleanup failure, never read). No schema; 16 new adapter tests, **1038 jest** green, tsc clean; no server CI (0 checks). — shipped via #306
- 2026-07-08 — **Culprit rebrand execution plan — PR-by-PR sequence (process/meta; no build change, no schema, no app code)** — PM asked for a plan to operationalize all the naming + branding work (icon direction #303 + naming spec #304) — "maybe one PR, maybe several." **Verified both specs against the live tree first** (re-grepped every brand-candidate `Nyx`: the naming spec's ~14-site inventory holds; the current `assets/icon.png` is a placeholder concentric-circle grid — no Nyx mark embedded, so PR B is a clean replace) and **caught one omission** — a 5th vet-report brand string the naming spec's §3.5 missed, `render.ts:1669` ("Photo findings are **Nyx's** read…" — brand-as-actor copy), folded into PR A. Delivered **`docs/culprit-rebrand-execution-plan.md`** — the orchestration layer over the two specs: **PR A** name + in-app + vet-report brand strings (build-ready now, deploys `generate-report`) · **PR B** Moon & Signal icon + splash (gated on the design master) · **PR C** `colorBrandNight` indigo token (own theme PR) — with sequencing (A ⟂ C parallelizable, B the long pole), per-PR review gates, rollback, and a combined DoD. **Three PM decisions via AskUserQuestion:** icon = **wait for a design master**; indigo token = **adopt now** (resolves the CLAUDE.md indigo Open Question → additive/background-only); this session = **plan-only** (no app code). Deferrals preserved (B-276 identifiers / B-277 docs-codename / B-278 deep-link scheme); PM/non-repo items named (design-master delivery; Supabase auth email template still "Nyx"). Backlog **B-274/B-275** reconciled + indigo Open Question resolved in the same PR. Personas: Product Owner ✓ (3 PRs mapped, deferrals kept) · Engineer ✓ (bundle ID/slug/buckets/DB/scheme held; deploy step named; token additive) · Designer ✓ (Moon & Signal + indigo world colour; one interactive accent survives) · Dr. Chen / vet-report-cold-read ✓ (brand-not-actor attribution + patient name intact) · QA ✓ (brand-vs-pet-vs-infra grep gate; caught the :1669 omission) · Trust & Safety ✓ (no data-path change). Adversarial-reviewer **N/A** (planning doc — no clinically/statistically load-bearing logic). No app code; no schema; **1022 jest** green (pre-push hook); no server CI (0 checks). — shipped via #305
- 2026-07-08 — **Culprit name-rebrand plan — build-ready naming spec (process/meta; no build change, no schema, no app code)** — PM asked to write up the Nyx → Culprit *naming* plan now so a later session can combine it with the PR #303 icon/design work into a first "name + icon" brand pass. Researched all ~1,560 `Nyx` occurrences and sorted them into three hats: **① brand** (read on screen → "Culprit"), **② pet** (the PM's cat Nyx, in sample/test data → never touch), **③ infra** (bundle ID, `nyx-*` Storage buckets, `nyx.db`, `nyx.activePetId`, Expo slug, deep-link scheme, `NyxEvent`/`NyxTabBar` types → do not change — user-invisible and destructive/breaking). Deliverable **`docs/culprit-rename-requirements.md`**: exact file·line inventory of the ~14 user-facing strings + 3 test files, the OUT-OF-SCOPE table with rationale, the tricky **vet-report** surface (letterhead/footer wordmark → Culprit, but `render.ts` prints the pet name "Nyx" too — leave the dynamic `Patient:`/`<title>` renders; keep the mechanism-not-brand AI attribution), AC + on-device QA for the future execution PR, and the combined-pass sequencing (design's Moon & Signal master is the long pole). Found + flagged: no central `APP_NAME` constant (brand is inline everywhere); the stale `app-store-submission-guide.md:49` line that still says the on-device name "stays as is"; and the PM/dashboard-only items (icon/splash assets = B-275, Supabase auth email templates, ASC). Fast-forwarded the branch to pick up #303 first. Backlog: **B-274** repointed at the spec + scope resolved; **B-276/277/278** filed (deferred internal-identifier / docs-codename / deep-link-scheme work); CLAUDE.md Read-These index + this file updated. Personas: Product Owner ✓ (scope + deferrals filed) · Engineer ✓ (infra held, deploy step named) · Designer ✓ (pairs with locked icon direction) · Dr. Chen / vet-report-cold-read ✓ (patient-name intact, brand-not-actor attribution preserved) · QA ✓ (brand-vs-pet-vs-infra split is the guardrail) · Trust & Safety ✓ (no data-path change). Adversarial-reviewer **N/A** (planning doc — no clinically/statistically load-bearing logic). No app code; no schema. — shipped via #304
- 2026-07-08 — **Culprit brand & icon direction — product-team review of the 24 logo explorations (process/meta; no build change, no schema, no app code)** — PM shared `Culprit_Logo_Explorations.pdf` (4 turns × 6 = 24 icon directions, Turn 1 most-constrained → Turn 4 fully-unconstrained) and asked the product team + an App-Store lens to discuss, surface improvement opportunities, and align on favourites; PM **delegated the pick to the team** with the north star *"a great design that leads to great marketing and branding."* **Team resolved to "Moon & Signal"** — a moonlight crescent cradling a single teal Signal dot (crescent = night/Nyx equity; dot = the found culprit **and** the AI Signal, our differentiator). It bridges the moon-vs-name fork, keeps **teal as the one interactive accent**, adds **midnight indigo as an additive *background* brand colour**, adopts the **crescent-C + teal i-tittle wordmark** (Turn-3f) + the **Whorl fingerprint** (Turn-3d) as the marketing motif; pet-warmth stays secondary (Dr. Chen/Sam "not a pet brand"). Held in reserve: **2a Teal Badge** (safe fallback) + **4f The Verdict** (strategic alt if we ever lead with the name). Guardrail recorded: **red stays off the brand** (`#F43F5E` = symptom, `#DC2626` = destructive). New docs: direction doc `docs/culprit-icon-brand-direction.md` (verdict on all 24 + open decisions), the self-contained **rendered pitch** `docs/brand/culprit-direction.html` (also a Claude artifact), a **Claude Design handoff brief** `docs/brand/culprit-icon-design-brief.md`, + indicative master `docs/brand/culprit-icon-moon-signal.svg`; **B-275** filed; **CLAUDE.md Open Question** added (adopt `colorBrandNight` indigo token?). Flagged: the **App-Store Consultant** is not yet a formal persona (`docs/personas.md`) — proposed to formalise. Personas: Designer ✓ · Data ✓ · Dr. Chen ✓ · Jordan+Sam ✓ · App-Store lens ✓ · Dir. of Eng ✓ · Product Owner ✓ (B-275) · QA/T&S N/A. Adversarial-reviewer **N/A** (no clinically/statistically load-bearing logic). No schema; no app code. — shipped via #303
- 2026-07-08 — **App Store submission — guide step 1 DONE + app rebranded Culprit (PM dashboard action; no build change, no schema, no app code)** — PM ran guide step 1 (`docs/app-store-submission-guide.md`): verified the existing App Store Connect record (screenshot-confirmed — Apple ID `6777691423`, Bundle ID `com.projectnyx.app`, status "1.0 Prepare for Submission") and **locked the store name**, rebranding the app **Nyx → Culprit**. Claude first advised on the name/subtitle (PM chose **Culprit** as the brand → Claude gave the ASO-optimized split: **Name "Culprit — Pet Health Tracker"** 28/30 carries the broad high-volume search term + clears the globally-unique-name collision; **Subtitle "Track symptoms, find triggers"** 29/30 carries the reactive-owner wedge with non-overlapping keywords), reviewed the ASC screenshot (all fields within limits; the "2"/"1" counters = chars remaining; caught the unsaved state → flagged Save; noted Content Rights unset = a step-15 "No"), then recorded completion. **Category** Lifestyle (primary) / Health & Fitness (secondary) — deliberately not Medical (strictest review lens; Culprit is a tracker, not diagnostic). Docs: guide tracker step 1 → ✅; **B-272 closed**; **B-274 added** (align on-device `app.json` `name` + repo branding Nyx → Culprit before the step-10 build — bundle ID stays `com.projectnyx.app` to preserve the record; a config-only flip, not a submission blocker); STATUS.md. Personas: PM ✓ (brand decision + positioning) · Sr. Product Designer ✓ (ASO/voice on name+subtitle — specific-over-generic, no exclamation) · Product Owner ✓ (B-272 closed, B-274 filed, row contract kept) · Trust & Safety / Data / Dr. Chen / QA N/A (no code, no data). Adversarial-reviewer **N/A** (docs only — no clinically/statistically load-bearing logic). No schema; **1022 jest** green (pre-push hook); no server CI (0 checks). Next long-lead items all independent & startable now: step 2 (legal docs — highest leverage, feeds step 14), step 3 (web presence), step 4 (SMTP). — shipped via #301
- 2026-07-07 — **App Store readiness, round 2 — expert submission-advisory pass + the step-by-step submission guide (process/meta; no build change, no schema, no app code)** — PM asked Claude to act as an App Store submission expert, review the #297 register, and apply the accepted findings + produce a PM-facing runway. Every advisory claim was **verified against the repo first** (`app.json` — no `NS*UsageDescription`, plugin absent, `ITSAppUsesNonExemptEncryption` already set, `supportsTablet: true`; `eas.json` versioning sane; `paywall.tsx` trial CTA admits Premium isn't live; `signup.tsx` social buttons properly hidden; real 1024² icon). **Register (`docs/app-store-readiness.md`):** four untracked App-Review gaps added — **B-271** demo account + reviewer notes (login-gated app; empty account = "unable to evaluate" rejection), **B-272** verify the existing ASC record + lock the store name (TestFlight ⇒ record exists; name is the long-lead risk), **B-273** live web presence (App Review *visits* the support URL; one static page hosts support + B-229 + B-230), privacy-manifest verification riding B-267's built-artifact check; **Tier 3 reworked to a recommendation — feature-flag the paywall OFF for v1** (mock trial CTA ≈ certain Guideline 2.1 rejection; StoreKit path also drags in the Paid Apps Agreement) which **removes the freemium decision from the submission critical path**; B-267 sharpened (mic string, purpose-copy, Nyx-voice), B-269 sharpened (`supportsTablet: false` rec, one 6.9" set, 2025 age tiers); already-de-risked gains export compliance/icon/bundleID/versioning. **Backlog:** B-271/272/273 added (row contract kept); B-267/B-269 amended. **New `docs/app-store-submission-guide.md`:** the sequenced **16-step runway** (6 phases, long-lead first — ASC record/legal docs/web presence/SMTP now; 3 parallelizable config PRs; B-039 device QA → email-confirm flip; build + built-artifact checklist; demo seed/screenshots/copy/nutrition label; submit + rejection playbook) with per-step URLs/tips, **kickoff prompts for every PR-shaped step**, a "Guide step N complete" confirmation protocol, and a live progress tracker. With the paywall flagged off, **nothing on the runway waits on a PM product decision**. Personas: PM ✓ (requested + will ratify the 2 recommendations) · Dir. of Engineering ✓ (repo-verified claims) · Trust & Safety ✓ (B-271/273 are its lens) · Product Owner ✓ (3 rows, contract kept) · QA ✓ (step 8/10 gates preserved). Adversarial-reviewer **N/A** (docs only). No schema; **1022 jest** green (pre-push hook); no server CI (0 checks). — shipped via #300
- 2026-07-07 — **App Store readiness — backlog grooming pass + consolidated readiness register (process/meta; no build change, no schema, no app code)** — PM asked the `backlog-groomer` + product-team personas (Product Owner, Trust & Safety, Dir. of Engineering, PM, QA) to sweep the backlog and surface everything needed to submit to the App Store. **Grooming (`docs/backlog.md`):** reconciled **B-039**'s stale head (#193 is *merged*, not draft — only the on-device 5.1.1(v) deletion QA gate remains) + added **four proactive rows** for genuinely-untracked work (no scope invented): **B-267** iOS camera/photo permission usage strings missing from `app.json` (a **NEW hard blocker** found by direct repo read — `expo-image-picker` requests camera/library but no `NS*UsageDescription` is set and the config plugin isn't listed → iOS crashes / App Review rejects), **B-268** App-Privacy nutrition label, **B-269** store listing assets, **B-270** app-level medical disclaimer. **New synthesis doc `docs/app-store-readiness.md`** — a tiered register keyed to backlog IDs (Tier 1 hard blockers → Tier 5 hardening + already-de-risked), realizing the checklist B-002 anticipated; surfaces two existing PM decisions onto the critical path (freemium gate; v1 social-auth scope). Bottom line: the hardest blocker (B-039) is code-complete + merged; the gap to submittable is the permission-string config, the legal/privacy artifacts, the store listing, the email-confirm flip, and the paywall decision. Personas: Product Owner ✓ (grooming + 4 rows, structural flag: B-253–266 sit below the `## Done` header → next slim pass) · Trust & Safety ✓ · Dir. of Engineering ✓ (B-267 found + verified against `app.json`/`package.json`) · PM ✓ · QA ✓. Adversarial-reviewer **N/A** (docs only — no clinically/statistically load-bearing logic). No schema; **1022 jest** green (pre-push hook); no server CI (0 checks). — shipped via #297 (merged); STATUS.md wrap entry rides a small follow-up PR (work PR merged mid-session).

- 2026-07-06 — **Doc correction — STATUS.md Step 9 vet-report section had drifted stale (process/meta; no build change)** — a "build PR 7" session that discovered PR 7 was *already shipped*. The vet-report block ("After Step 10 …") still framed **PR 7 (incident photos)** as upcoming, but git shows it **shipped #278**, **deployed live as `generate-report` v8 #281**, and was hardened by **B-244 #282** (storage-path CHECK + owner-scoped bucket RLS, applied to prod 2026-07-05) + **B-218 #283** (dropped the `vet_reports_public_share` anon-enumeration policy); the parallel onboarding track (#284–#295) had only refreshed its own section + the top line, never this block. **Did NOT rebuild PR 7** — surfaced the contradiction (branch was at `main` HEAD, 0 unique commits; #278's own code + gates confirmed complete: `rls-privacy-reviewer` PASS · `vet-report-cold-read` CLINIC-READY · `adversarial-reviewer` PASS); PM chose doc-only. Corrected the block to actual state (also folding in the round-2 design **B-221 #268/#273** + range control **B-222 #276** that had shipped since the snapshot) + trimmed the Last-updated line. Backlog verified **already-current** for every touched B-ID — B-218/221/222/244 `Done` · B-243 `Open` · B-246 `Partial` — nothing to reconcile. Genuinely-open Step 9 work: PM **real-vet R1 email** · **B-243** (interactive tap-to-exclude photo review; additive, no schema) · then **PR 6** (public link, B-218 blocker cleared). Root cause worth noting: the branch `claude/step-9-vet-report-pr7-kytv5e` was auto-named for PR-7 work that no longer existed → the generating automation was reading the same stale status. No code, no schema; doc-only. **1022 jest** green (pre-push hook); no server CI (0 checks). — shipped via #296
- 2026-07-06 — **Onboarding revamp (B-251) PR 10 — paywall mock + completion (closes the flow's interim terminus)** — the last two screens (§3.7/§3.8/§9 PR 10, mockups 11–12), so the flow is now **complete end-to-end (no dead ends) — a build CAN be cut**. New `app/onboarding/paywall.tsx` (a non-functional dark premium mock — no StoreKit/purchase/selection; "Maybe later" + a mocked "Start 7-day free trial" both advance; the trial CTA **honestly acknowledges Premium isn't live yet** rather than silently no-op; explicit **Principle-7 free-tier line**; per-screen status bar flips light-on-focus / restores-`auto`-on-blur via `useFocusEffect`) + `app/onboarding/done.tsx` ("You're all set — say hi to {name}" → `Go to {name}'s home` → `replace('/(tabs)')`; **back-gesture-locked** true terminus; the durable **`onboarding_completed_at` is written on mount** so a quit here still counts). New `lib/profile.ts markOnboardingComplete()` (single-column upsert — never wipes name/tz; **non-fatal**: a failed write is backstopped by the §6 legacy has-pet rule, so no re-onboard bounce) + unit tests; 4 dark-surface theme tokens; `pet-age finish()` repointed `replace('/(tabs)')` → `push('/onboarding/paywall')` (tests updated). **`pm-feature-review`: done SHIP-SHAPED, paywall NEEDS-WORK → both blocking product fixes applied** — (1) the silent trial CTA → honest acknowledgment; (2) the design-locked mockup's premium bullets **gated care** (`Multi-pet profiles` ships FREE via B-086 + contradicts the very next screen; `Advanced correlation views` = care per Principle 3; `90-day history` truncates the free trend view + vet report), violating the AC "no gated care" → reworked to **convenience-only** placeholders; the real gate is the PM's freemium-gate call → **B-263**. `code-reviewer` **ship-ready** (both NITs fixed: a shared escape-guard ref in `done.tsx`; a completion-write reject-branch test). Adversarial-reviewer **N/A** (§11 — not clinically/statistically load-bearing). Personas: Designer ✓ (P5/P7) · Engineer ✓ · nyx-voice ✓ · Jordan/Sam ✓ (pm-review) · Product Owner ✓ (B-263/264/265/266) · Data/Dr. Chen N/A. **No schema** (writes migration 027's column). Only **PR 11** (functional Apple/Google) remains — post-v1, gated on a PM Google OAuth client. tsc + **1022 jest** green; no server CI (0 checks). — shipped via #295
- 2026-07-06 — **Onboarding revamp (B-251) PR 9 — pet setup: age (dual input + DOB precision) + the cross-surface honesty gate** — the last skippable pet-setup step (§3.6/§9 PR 9, mockup 10). New `app/onboarding/pet-age.tsx` (step 5/5 — segmented **Age/Birthday** toggle via `ChipGroup` `allowDeselect={false}`; **Age** = Years/Months numeric fields → an approximate DOB anchored `today − duration` (S6); **Birthday** = `DateTimePicker` → an exact DOB; "Not sure? Skip" leaves `date_of_birth` null) + pure, unit-tested **`lib/age.ts`** (`ageToDob`/`birthdayToDob` write transforms + `formatAge`/`formatBirthdayField`/`resolveDobPrecisionOnSave` honest-display/merge helpers; 29 tests). `pet-gender`'s interim `router.replace('/(tabs)')` **repointed** to push `/onboarding/pet-age` — age now owns the interim terminus to Home until PR 10. **Clinical-honesty gate (S2) — an approximate DOB (a computed anchor) is never rendered as a witnessed birthday** — the rule lives in one tested home (`lib/age.ts`) and is applied on all **3** DOB surfaces: Profile age chip (`~2yr`), `EditPetModal` (honest "About July 2024" field + precision-preserving write via `resolveDobPrecisionOnSave` — a real calendar pick flips it to `exact`, editing weight never promotes it; also fixed a pre-existing `toISOString` UTC-day-shift on save → `dateToYmd`), and the **vet report** (`generate-report` `index/report/render.ts` thread `date_of_birth_precision`; `render.ts` drops the `(b. YYYY)` birth-year for approximate → `~N yr`). **Data Scientist / Dr. Chen honesty sign-off (not a bare ✓):** Data tried "'2 years' entered in July" → `~2yr` / "About July 2024" / report `~2 yr`, never "b. 2024" (precision set by input MODE, never inferred from the date) → held; Dr. Chen tried "approximate pet, owner edits only weight" → precision preserved → held. `code-reviewer` **Ship-ready, fixes applied** (extracted `resolveDobPrecisionOnSave` pure+tested; `dobEstimateNote` `theme.textSM`; numeric-fields-vs-spec's-"two wheels" → **B-262** for Designer/PM ratification). Adversarial-reviewer **N/A** (§11 — not clinical/statistical *logic*; the honesty rule gets the Data/Dr. Chen gate instead). Personas: Data ✓ · Dr. Chen ✓ · Designer ✓ (P1/P5) · Engineer ✓ · Jordan/Sam ✓ · Product Owner ✓ (B-262). **No schema** (writes migration 028's column; every pet-load path selects it). **`generate-report` DEPLOYED v9 (2026-07-06)** — PM deployed from this branch via `npx supabase functions deploy` (script 265 kB), shipping the DOB fix + the vet-report track's pending B-222 render work; verified (v9 ACTIVE, `verify_jwt` preserved, boot smoke-test 404 + no-auth 401). tsc + **1011 jest** + generate-report **deno (160)** green; no server CI (0 checks). — shipped via #294
- 2026-07-06 — **Onboarding revamp (B-251) PR 8 — pet setup: breed + gender (the two skippable optional steps)** — the two optional pet-setup steps (§3.4/§3.5/§9 PR 8, mockup 08–09), the **first steps that UPDATE the PR-7 pet row**. New `app/onboarding/pet-breed.tsx` (step 3/5 — reuses `components/pet/BreedPicker.tsx` species-filtered + free-text "Other"; Continue UPDATEs `pets.breed`, prominent teal Skip leaves it null) + `app/onboarding/pet-gender.tsx` (step 4/5 — Male/Female via `ChipGroup` per B-146; Continue UPDATEs `pets.sex`, Skip leaves `'unknown'` = the insert default). `pet-name` now **pushes** `/onboarding/pet-breed` (was `replace('/(tabs)')`) so back-nav returns step-by-step, and its pet write is now **idempotent** (insert-or-update via the store's `activePet` — onboarding starts at zero pets, so an active pet here can only be the one this flow created) so re-entering the required steps never creates a second pet; gender's `replace('/(tabs)')` is the **interim terminus** until PR 9 (age) repoints it. `OnboardingHeader` gains an optional prominent-teal Skip (`onSkip`/`skipLabel`/`skipDisabled`). Breed field-state seed extracted to **`resolveBreedFieldState`** (`constants/breeds.ts`), replacing `EditPetModal`'s private copy (DRY, unit-tested). **`code-reviewer` fix-before-merge → applied:** a Skip-vs-Continue **save race** (a Skip tap mid-save double-navigated on pet-breed) → `handleSkip` guards `saving` + `OnboardingHeader.skipDisabled`; regression-tested (new `pet-breed`/`pet-gender` screen tests). `nyx-voice` ✓ (fixed a Pattern-8 raw-error-string leak across all 3 screens); Designer ✓ (Principles 1/5; fixed a sub-44pt link); `pm-feature-review` (breed SHIP-SHAPED; gender reads sparse → **B-259**, "Not sure? Skip" → **B-260**, "Other" reachability → **B-261**; **⚠ do NOT cut a standalone build before PR 9/10** — step-4-of-5 dead-ends to Home + `onboarding_completed_at` unwritten). Adversarial-reviewer **N/A** (§11 — not clinically/statistically load-bearing). Personas: Designer ✓ · Engineer ✓ (idempotent-insert invariant verified) · Jordan + Sam ✓ · nyx-voice ✓ · Data/Dr. Chen N/A · Product Owner ✓ (B-259/260/261). No schema. Doc-drift: spec §5 breed-skip copy stale vs mockup. tsc + **977 jest** green; no server CI (0 checks). **Closes B-210.** — shipped via #293
- 2026-07-06 — **Onboarding revamp (B-251) PR 7 — pet setup: type + name (the two required pet screens)** — the two hard-required pet-setup steps (§3/§9 PR 7, mockup 06–07). New `app/onboarding/pet-type.tsx` (two large **Cat · Dog** tiles, **no default** — an explicit tap selects, Continue advances; the **multi-pet reassurance** line so a multi-pet owner isn't stalled; drops the enum's `other`) + `app/onboarding/pet-name.tsx` (single autofocused `TextField`, Continue gated on a non-empty name → inserts the pet `{user_id, name, species}` via the `add-pet` insert path → Home; PRs 8/9 UPDATE breed/gender/age onto the same row). New shared `components/onboarding/OnboardingHeader.tsx` (`canGoBack()`-aware back — hidden on the entry step, shown once pushed + `ProgressBar` steps 1–2 of 5 + an a11y-hidden "Step N of 5" label). `hooks/usePet.ts` + PR 6's `app/(auth)/signup.tsx` redirect repointed `/onboarding/pet` → `/onboarding/pet-type`; retired v0.1 screens `pet.tsx` + the now-orphaned `food.tsx` **deleted** (enacts D3 — no food step; closes **B-038**). **`code-reviewer` fix-before-merge → applied:** the "back preserves values" AC was one-directional (backing up to change the type re-mounted a fresh name screen and dropped the typed name) → type+name now live in a new transient **`store/onboardingDraftStore.ts`** (single source of truth; survives back-then-forward; **reset on sign-out** via `wipeLocalSession`, FR-9 parity, so a name can't bleed across accounts); + a null-species **escape hatch** (stray deep-link → back to type) + a `radiogroup` a11y wrapper. Personas: Designer ✓ (Principles 1 no-default / 5 designed states) · Engineer ✓ (transient draft-store pattern, sync-safe) · Jordan ✓ (10-sec) · Sam ✓ (multi-pet reassurance) · nyx-voice ✓ · Data/Dr. Chen N/A. Adversarial-reviewer **N/A** (§11 — not clinically/statistically load-bearing). Merged `main`'s **PR 6 (#291)** in on wrap (STATUS.md + `signup.tsx` collisions resolved — signup repointed to pet-type, its 2 redirect tests updated). No schema. tsc + **965 jest** green; no server CI (0 checks). — shipped via #292
- 2026-07-06 — **Onboarding revamp (B-251) PR 6 — account creation + soft email verify** — reworked `app/(auth)/signup.tsx` onto the shared `TextField`+`PrimaryButton` (PR 3): owner **first/last/email/password** with calm inline validation (errors surface only after a submit attempt, then clear live), a mocked TOS/Privacy line (B-230/B-229), **Apple/Google behind `SOCIAL_AUTH_ENABLED`** (off → hidden, S7), and a soft **"check your inbox"** verify state (resend + an **edit-email back-escape** + "verify later · continue for now"). On submit: `supabase.auth.signUp` → **session-present** writes first/last + a derived **`display_name="First Last"`** via the PR 1 `updateOwnerName` helper (so `generate-report`'s existing read is untouched), routes to `/onboarding/pet`; **no-session** → soft verify; **already-registered** → login. New **`constants/flags.ts`** (`SOCIAL_AUTH_ENABLED` — the project's first feature flag; PR 11 flips it) + pure **`lib/authValidation.ts`** (email/password/name, unit-tested). **No schema** (reads PR 1's `user_profiles` columns). **Reviews:** `rls-privacy-reviewer` **PASS** (auth path — RLS `WITH CHECK` fully contains the name write; enumeration signal not widened; dormant social path unreachable; **2 PM dashboard config checks → folded into B-152**) · `nyx-voice` ✓ applied (dropped the Pattern-3-forbidden "Coming soon" mock copy; warmed the error title) · `code-reviewer` **fix-before-merge → applied** (edit-email back-escape closing the §10 "no dead ends" gap; `Divider` reuse; login-link ≥44pt; owner-name-write failure-path test; the deeper "continue → login raw-error once confirmation is re-enabled" hardening → **B-152**). Adversarial-reviewer **N/A** (§11 — not clinically/statistically load-bearing). Personas: Designer ✓ (Principles 1/4/5) · Engineer ✓ · Trust & Safety ✓ (rls-privacy PASS) · Data/Dr. Chen N/A. Independent of PR 7/10 (disjoint files); **PR 11 now unblocked** (gated on the PM's Google OAuth client). STATUS.md the one shared-file collision. tsc + **959 jest** green; no server CI (0 checks). — shipped via #291
- 2026-07-06 — **Onboarding revamp (B-251) PR 4 — shell: routing + progress + durable completion gate** — the navigation frame the flow's screens slot into, and the durable gate (D12) that replaces the fragile "≥1 pet ⇒ onboarded" inference (which silently treated a mid-flow quit as done). New pure **`lib/onboarding.ts`** `decideOnboarding({onboardingCompletedAt, petCount})` encodes the §6 matrix — flag set → complete (authoritative even with 0 active pets); null flag + a pet → **legacy complete, never re-onboarded**; null flag + no pet → onboard — fully unit-tested. **`hooks/usePet.ts`** rewritten to read `user_profiles.onboarding_completed_at` (migration 027, **confirmed applied live** before wiring) alongside the pet list and drive the gate through the seam, preserving the cold-start retry + never-false-onboard guards; a profile-read error only blocks the gate in the petless case, so existing accounts (all legacy: a pet + null flag) are unaffected. **`components/onboarding/ProgressBar.tsx`** = calm segmented step indicator (accent fills, a11y `progressbar` role); **`app/onboarding/_layout.tsx`** = shared stack chrome (header hidden, back-gesture on so back preserves entered values, consistent slide/canvas). **No schema** (reads PR 1's column). Routing entry stays `/onboarding/pet` until PR 7 repoints it to `/onboarding/pet-type`; no account has the flag set until PR 10's completion write, so every existing account routes via the legacy rule today (no behaviour change). **`code-reviewer` fix-before-merge (soft):** the two-read `Promise.all` had no try/catch — a *thrown* read (zero-connectivity cold start) would reject unhandled and skip the retry machinery; **FIXED** (fold a throw into the same retry-once/never-onboard path) + added `hooks/usePet.test.ts` locking the read → gate → route wiring (legacy pet, completed-flag-no-pet, fresh redirect, throw + pets-error guards). Adversarial-reviewer N/A (routing, not clinical/statistical — §11). Personas: Engineer ✓ (gate seam + no false-onboarding path incl. throw) · Designer ✓ (ProgressBar restraint, Principle 6) · QA ✓ (§6 routing matrix unit-tested) · Data/Dr. Chen N/A. Reviewer nit: spec §9 lists `store/petStore.ts`/`app/_layout.tsx` as PR-4 files — neither needed a change (redirect already lives in `usePet`; `setOnboarded` already exists); flagged for PM/QA confirm as intentional descope. Independent of the vet-report track — `STATUS.md` was the one shared-file collision (merged `main`'s #285 cleanly). tsc + **933 jest** green; no server CI (0 checks). — shipped via #289
- 2026-07-06 — **Onboarding revamp (B-251) PR 2 — pet DOB-precision schema (schema-isolated; first build PR of the swimlane)** — added the clinical-honesty marker so onboarding's dual integer-OR-birthday age input (D6) never stores a *computed* DOB as a witnessed birthday. New `028_pet_dob_precision.sql`: `dob_precision` ENUM (`exact`/`approximate`) + `pets.date_of_birth_precision NOT NULL DEFAULT 'exact'`. Default is the truthful backfill — the ONLY current `date_of_birth` writer is `EditPetModal`'s date picker (every existing DOB is an owner-selected calendar date; `add-pet`/`onboarding/pet` never write a DOB), verified live (2/2 rows read `exact`). Applied via Supabase MCP `apply_migration`; `get_advisors` (security+perf) surfaced only pre-existing warnings — no new advisory (ENUM has no RLS surface; no FK added; `pets` RLS untouched). Extended the client `Pet` type + the honesty contract in-comment (`'approximate'` renders as "~2 years old", never a date; a future computed-DOB writer MUST set `'approximate'` explicitly) + mechanical test-fixture updates for the new non-null field. **No UI** — the age input + `age→DOB+precision` logic land in PR 9 (which writes this column, with its own Dr. Chen honesty gate + unit tests). Resolves sub-decision **S2**. **Data Scientist / Dr. Chen honesty sign-off (not a bare ✓):** Data Scientist tried "is there a legacy computed-DOB writer the `exact` default would mislabel?" → none exists (grep + live check) → held; Dr. Chen tried "could a no-birthday pet read as having an exact one?" → precision is inert without a DOB (documented contract) → held; residual = a *future* writer forgetting `'approximate'`, routed to PR 9's gate. Adversarial-reviewer subagent N/A (schema/type only, no clinical/statistical *logic* shipped). Independent of PR 1 (owner-profile 027) + PR 3 (design-system primitives) — STATUS.md is the one shared-file collision to expect at merge. tsc + **892 jest** green (pre-push hook). No server CI configured (0 checks). — shipped via #287
- 2026-07-06 — **Onboarding PR 1 (B-251) — owner-profile schema + name write helper (schema-isolated build)** — first of the 11-PR onboarding revamp. Migration `027_user_profile_names.sql` adds `first_name` / `last_name` / `onboarding_completed_at` to `user_profiles` (additive, nullable, no default) — **applied live via the Supabase MCP `apply_migration`**; `get_advisors` (security + perf) shows **no new finding from 027** and no `user_profiles` RLS gap (the sole `user_profiles_owner` `FOR ALL USING auth.uid()=id` policy — migration 001 — covers the new columns for read AND write; live `pg_policies` confirms exactly one unchanged policy). `lib/profile.ts` gains a real `UserProfile` type + `updateOwnerName()` beside `updateDisplayName`: writes first/last + a derived `display_name = "First Last"` so `generate-report`'s existing `display_name` read is untouched (no report change); pure `deriveDisplayName()` exported + unit-tested (9 new tests). **Reviews:** `rls-privacy-reviewer` (light) **PASS** (forged-`id` upsert blocked by the owner WITH-CHECK default, cross-user SELECT row-filtered, 027 adds no policy/grant/trigger); `supabase-sync` **PASS** (Pattern 1 error-checked + Pattern 5 LWW `onConflict:'id'`); adversarial **N/A** (name capture — not clinically/statistically load-bearing, §11). Two reviewer notes routed: **B-252** (`onboarding_completed_at` owner-forgeable → PR 10 must not gate entitlements/security on it); `display_name`→vet-report "Owner:" line already `h()`-escaped in `generate-report/render.ts` (confirmed, no action). **tsc clean; 900 jest green.** NO app/UI code, NO Edge Function change. PR 2 (age-precision schema → migration 028) + PR 3 (design-system primitives) are independent + parallelizable next; PR 4 (shell) needs this PR's column. Personas: Data ✓ (schema shape + back-compat derive) · Engineer ✓ (additive/nullable, upsert matches sibling helpers) · Trust & Safety ✓ (rls-privacy light PASS) · Designer/Dr. Chen N/A · Product Owner ✓ (B-252 logged). — shipped via #288
- 2026-07-05 — **Onboarding experience revamp — build-ready spec + design-locked v3 mockup + 11-PR plan (planning/design session; no build code, no schema)** — PM opened the **app-store-readiness swimlane** to burn down v0.1 onboarding (2 screens: name+species + a skippable food form) and rebuild. Product team convened, surfaced two conflicts (funnel-vs-clinical on required fields → PM: **type+name required, rest skippable**; paywall-mock-vs-Principle-7 → **mock it, don't wire it**) and set the shape (D1–D13): email-only build now with **Apple/Google mocked + specced as PR 11**, a **3-card value story**, TOS/privacy acceptance mock (B-230/B-229), **no notification prompt** (no nudge system yet), durable `onboarding_completed_at` replacing the fragile "≥1 pet" gate. `docs/nyx-onboarding-requirements.md` (build-ready; §4 = two additive schema migrations 027/028; §9 = **11-PR plan** with dependency graph, per-PR files/pre-flights/AC/tests/review gates). **Design iterated v1→v2→v3 over three PM review rounds + a Jordan/Sam persona interview:** v2 collapsed the carousel into one **Landing** (logo + persistent auth + **data-rich Nyx previews** — log/Signal/vet-report, not abstract art, borrowing the competitor's tangibility but scoped to our wedge); v3 **led with the Signal** (the differentiator), reworked the logging copy ("A couple of taps today. A clearer picture tomorrow."), kept Phase C at 5 screens. **PM signed off ("Love it")** → committed the final mockup `docs/onboarding-flow-mockups.html`. Backlog **B-251** (subsumes B-210 breed-in-onboarding, moots B-038 food-format-subset). Renumbered B-248→B-251 + migrations 025/026→027/028 (main #282/#283 took them). Personas: Designer ✓ (became the in-house onboarding expert — Pawfolio model / CompanAIn anti-pattern / funnel-vs-calm; Principles 1/5/7) · PM ✓ (all decisions Q1–Q4 + copy/order/length) · Dir. of Eng ✓ (schema-mostly-exists, Apple-forces-Google constraint, design-system primitives) · Data Scientist + Dr. Chen ✓ (age-precision honesty gate — never render a computed DOB as a witnessed birthday; deferred to PR 9 adversarial gate) · Trust & Safety ✓ (store gates: Apple AC, verification, TOS) · Jordan + Sam ✓ (interviewed — entry-confusion + data-rich-not-overwhelming + rescue-age skip + multi-pet reassurance) · Product Owner ✓ (B-251 + reconciliation). **Adversarial review N/A this session** — no clinical/statistical logic *shipped* (it's a plan); the age-precision honesty + any escalation logic are adversarial/expert-gated at their build PRs (PR 9). tsc + **892 jest** green (pure docs, unaffected). Proposed Tier-2 `design-principles.md` §Onboarding revision flagged for PM ratification (not written). — shipped via #284
- 2026-07-05 — **Stool analysis (GI-symptom AI read) — build-ready requirements spec + backlog item B-247 (planning session; no build code, no schema)** — PM asked for a status survey of the stool side of the app (parallel to the shipped vomit per-incident AI pipeline), then a build-ready project plan. Survey: stool logging UX already exists (Normal/Loose sub-step, FAB quick-tap) and rides the generic Signal correlation engine, but there is no `analyze-stool` sibling to `analyze-vomit` — the highest-volume GI event type has zero per-incident clinical read, and the vet report's "Stool characteristics" section has no AI-read data source. New `docs/nyx-stool-analysis-requirements.md` (modeled on `nyx-medication-logging-requirements.md`'s D-table/PR-plan structure): **D1** build on the current schema split (no consolidation — stays a separate CLAUDE.md Open Question), **D3** Bristol Stool Scale (Type 1–7), **D4** Sonnet 4.6, **D5** escalation floor (blood / repeated Type 7 / concurrent vomiting-lethargy), **D6** feeds the existing vet-report stool section; **D2** (shared `_shared/incident-analysis.ts` module, per the `clinical-guardrails` skill's own predicted "second incident type" refactor) proposed and flagged for Dir. of Engineering ratification rather than decided unilaterally. 10-PR plan (§8), PR 1 = additive schema only. Backlog **B-247** added (second child of B-013). Personas: PM ✓ (initiated + directed D1/D3/deliverable-format) · Dir. of Engineering — D2 flagged, not yet ratified · Data Scientist — schema proposed, pending ratification · Dr. Chen — Bristol Scale chosen for clinical-trust fit, no formal review pass run yet · Product Owner/Backlog Steward ✓ (B-247 added per protocol, row format verified). Adversarial review N/A (no clinical/statistical code shipped this session — mandatory at PR 3 once `analyze-stool` itself is built). tsc + **891 jest** green (unaffected — no app code touched, pure docs). — shipped via #279
- 2026-07-05 — **FAB quick-log: food terminology + symptom photo step** — three small PM-requested changes to the quick-log menu (`components/log/FAB.tsx`), no schema, no store-logic. (1) **Terminology follows the Food › (Meal, Treat) hierarchy:** "Recent meals"→**"Recent foods"**, "New meal"→**"Log food"** (verb, to not read as "add to the `Foods` library tab"), "No meals logged yet"→"No foods logged yet" — accurate because the recent shelf is `food_type`-agnostic (includes treats). (2) **Removed the "Log with photo" row** — it landed on the same type grid as "More events", just with a photo pre-attached (a redundant 2nd pathway; the type grid keeps its own "Attach photo" entry, so photo-first logging is unchanged). (3) **Vomit / Loose stool no longer silently auto-log** — they route into the existing `/log?type=vomit|diarrhea` `simple` step, which already carries an **optional photo** (a vomit photo auto-triggers the `analyze-vomit` AI read) + the **B-010 "Saw it / Found it"** time affordance these discovery-prone GI events need; resolved the standing `handleQuickSymptom` TODO and deleted the dead `handleQuickSymptom`/`handlePhotoLog`/`launchPicker` helpers + imports. Every behavior the silent path had (sync push, AI-Signal regen, calm completion moment, write-time multi-pet identity) is reproduced by `/log`'s `handleConfirm` — net a strict improvement (adds the photo + AI read). **PM confirmed both design calls** ("food" wording over first-word-only; reuse the log screen over instant-log-then-offer-a-photo). **code-reviewer ship-ready** (2 non-actionable NITs: symptom buttons now match the guard-less `Log food`/`More events` nav pattern; the `RankingCard` "No meals logged yet" is a different surface → **B-242**); adversarial-reviewer N/A (no clinical/statistical logic). tsc + **891 jest** green (no `FAB.test.tsx` before or after — pure UI/routing, `tests: N/A`). Personas: Designer ✓ (Principles 1 & 6 — photo optional so capture stays fast, a vomit photo is clinical signal now captured) · Engineer ✓ (reuses the tested flow, deletes dead code) · QA ✓ (routing verified, suite green) · Data/Dr. Chen N/A. — shipped via #277
- 2026-07-04 — **Vet report (Step 9) — PR 7 photo scope DECISION + spec → v2.1 (planning/decision session; no build code, no schema)** — PM reviewed a two-surface photo mock (in-app vs PDF) and **reversed the 2026-07-02 "split by type" call for the authenticated surface: ALL incident photos now go in the report + PDF** (the complete artifact the vet reviews; safety photos still lead the safety band; EXIF/GPS stripped + downscaled at render; optional pre-send "tap to exclude" review). Rationale: the split's only justification was *revocability*, a property of the public link (PR 6), not a PDF the owner deliberately hands their own vet — excluding everyday photos just means the vet never gets them, which is worse, not more private. The split-by-type + token-streaming + link-revocation (B-143) machinery **relocated to PR 6**; **PR 7 now builds BEFORE PR 6 and is decoupled from it** (PR numbers kept stable for cross-refs). Updated `nyx-vet-report-requirements.md` (v2.1 header + §2/§3/§4/§8/§12/AC-7) + STATUS pointer. Personas: Dr. Chen ✓ (an incomplete report erodes trust; the vet needs the images) · Pet Owner ✓ (complete = usable ahead of / during the appt) · Designer ✓ (Principle 6, no placeholder noise) · Trust & Safety ✓ (dissent surfaced → converted to EXIF-strip + pre-send review, not exclusion) · Engineer ✓ (drops dual-surface render, simpler PR 7). — shipped via #275
- 2026-07-04 — **Vet report (Step 9) round-3 — feedback round 2 (10 items) + charts + B-233 marker fix** — PM's 2nd real-artifact review folded into the pure render/report layer (NO schema, no app code): severity column cut (#2), appendices reordered B=diet-history/C=off-diet (#3), wet-food meals surfaced via new `DietSummary.mealItems` + a new **Appendix E — Meals & intake** (#7/#8), jargon killed (#5), "intervention"→"change" (#6), AI-extraction sentence (#10), Designer pass + single-column diet block + inline safety chip (#1/#4), and **#9 charts BUILT** — a `ProteinTimeline` protein-over-time stacked bar (muted hue + SVG texture, greyscale-proofed; §5.8 colour carve-out **PM-ratified**) + week-over-week vomit-chart labels. **B-233** (was B-227): free-fed = standing context, not a dated "change" (adversarial-reviewer PASS). Gates: cold-read **CLINIC-READY** · code-reviewer fix-before-merge **FIXED** · adversarial **PASS**. Merged `main` + renumbered colliding B-IDs (226/227/228/229 → 232/233/234/235). **131 deno + 886 jest + tsc green.** `generate-report` **NOT yet redeployed** — bundle built (sha `25ed1c76`); PM deploys via dashboard post-merge. — shipped via #273
- 2026-07-04 — **Backlog logging: B-226 Patterns calendar improvements** — PM feedback session (no build-step work) dogfooding the Patterns tab's `FrequencyCalendarCard`: (1) a tapped day should show ALL incidents logged that day, not just the card's scoped symptom; (2) wants a true paginated month calendar instead of a fixed window; (3) the multi-shade red heat scale has never read as legible to the PM despite the existing B-097 Fewer→More legend. Checked the backlog first — no existing item covered this combination — so logged fresh as **B-226** (Priority: Later; PM flagged it as not urgent right now). Docs-only, no code touched. Personas: Product Owner/Backlog Steward ✓ (verified no duplicate, correct row format/ID sequencing) · PM ✓ (raised the request). — shipped via #270
- 2026-07-03 — **Vet report (Step 9) Phase 1 PR 4 — diet/weight/stool + WSAVA hardening** — an adversarial + cold-read hardening pass on the diet/feeding/weight/stool sections + the WSAVA-style Appendix C (already rendered from PR 2; no net-new sections). Built a render harness driving the real `assembleReport→renderReport` over two stress cases — a *contaminated diet-trial dog* (active hydrolyzed trial + a concurrent free-fed duck bowl + a standing pre-window steroid + a mid-window metronidazole course + a supplement + a treat×human-food cheese cube + falling weight) and a *safety-led monitoring cat* (no trial, refused-normal-food intake decline, possible-foreign vomit, chronicity, no weigh-ins) — and ran both mandatory gates cold. **Round 1: both reports NOT READY → 7 findings fixed.** `report.ts`: **(A1)** `buildConcurrentChanges` includes every window-**overlapping** intervention (not just window-starting), tagging pre-window ones `ongoing` — a standing steroid was dropped from the GP-0 "Reading the trend" note, letting the diet take its credit (§4/B-117, highest-consequence); **(A3)** human_food precedence so a treat×human-food feeding counts once; **(A5)** near-simultaneous weigh-in collapse (LWW). `render.ts`: **(A2)** Appendix C primary-diet + Appendix B antigen line now surface a concurrent free-fed competing protein under an active trial (was page-1-only); **(A4)** trial-aware framing (no "diet-trial confounder" on a no-trial report); **(A6)** `distinctLabels` (no "Ground beef ×4"); **(A7)** honest stool traceability + Appendix F clause. **`vet-report-cold-read` round 2 = both CLINIC-READY.** **`adversarial-reviewer`: all 5 load-bearing fixes held → found 3 more (round 3, all fixed):** `format==='treat'` treat dropped from Appendix B + tally (confounder predicate now mirrors the treat predicate); null-`activeFrom` free-fed escaping the trend note (guard dropped; `consider` nullable start); mid-window-ended confounder mislabelled "ongoing since" (`ConcurrentChange.endInWindow` → "until <date>"). Two cold-read NITs left safe-direction/unfixed: chronicity `activeWeeks`-vs-span (**B-216**), and a free-fed caveat on the intake fast-line (adding it would *soften* an intake flag — `clinical-guardrails` forbids; current wording errs safe). NO schema; `generate-signal` NOT redeployed (pure Phase-1 layer). +11 targeted deno tests → 72 report/render + 869 jest + tsc green. **Phase 1 COMPLETE.** Backlog **B-216** / **B-217** (capture WSAVA previous-diet). Personas: Dr. Chen ✓ (cold-read ×2 + adversarial ×3 gates) · Data Scientist ✓ (confounder-set completeness, weight-dedup LWW, never-reassure held) · Designer ✓ (Principle 6 non-colour, honest timing copy) · Engineer ✓ (pure layer, no schema, predicate/union parity) · nyx-voice/clinical-guardrails ✓ (intake never-soften upheld over a cold-read NIT). — shipped via #264
- 2026-07-03 — **Vet report (Step 9) Phase 1 PR 3 — clinical must-carry hardening + B-213 intake-decline duration** — the reduced-intake safety flag now answers "how long off food?". `detection.ts` `IntakeDeclineFinding.lastFullMealIso` = the most recent *fully-eaten* meal (strictest `all`, never a nibble → the derived gap can never *under-state* time-since-a-real-meal, the reassuring direction a never-reassure lane must avoid; null ⇒ "no full meal recorded", itself an escalating fact). `report.ts` threads it onto the flag as `hoursSinceLastFullMeal` (measured from the window end / the detector's own `now`) + a new `provenance.intakeLog` (rated meals, most-recent-first, capped at 40 with the remainder disclosed, and the last-full-meal **anchor pinned back past the cap** so the page-1 number is always a *visible, tagged* appendix row — the adversarial fix). `render.ts` = the "about N h/days without a full meal" line keyed to the feline 48–72h window + a decline-**slope** trajectory (ate all → some → picked → refused, so the gap can't be misread as marked anorexia — the cold-read fix) + a "Recent meals & intake" appendix. Escalate-only / never-"picky" / never-reassure held throughout. **`adversarial-reviewer` FAIL→fixed→re-verified** (the page-1 anchor could fall outside the capped appendix → cited-but-invisible in a chronic-inappetence cat; closed by anchor-pinning + a report-computed tag). **`vet-report-cold-read` = CLINIC-READY** (both copy fixes verified traceable line-for-line; 3 non-blocking NITs, none in-scope). NO schema; `generate-signal` NOT redeployed (pure Phase-1 layer; the new jsonb field is a structural no-op for the client Signal card). 61 report/render + 261 signal deno + 869 jest + tsc green. Backlog B-213 → Done; low-sev follow-ups **B-214** (UTC-vs-local hours skew on a custom past window) / **B-215** (latent meal-time B-010 guard). Personas: Dr. Chen ✓ (adversarial + cold-read gates) · Data Scientist ✓ (strict-`all` never-under-state; window-consistent `now`) · Designer ✓ (Principle 6 non-colour, slope legibility) · Engineer ✓ (pure layer, no schema, mirrors detection/report split). — shipped via #263
- 2026-07-02 — **Cat & dog breed lists revamped + searchable `BreedPicker`** — competitive research (CompanAIn / compainain.ai) flagged the v0.1 breed lists as thin (15 cats / 25 dogs, hardcoded in `EditPetModal`). Expanded to **cats 15→71** (union of TICA championship + CFA recognized breeds, cross-checked vs Wikipedia's *List of cat breeds*; show-ring coat-length divisions folded into their parent, genuinely-distinct breeds kept; `Domestic Shorthair/Mediumhair/Longhair` + `Mixed breed` kept & pinned — the entry CompanAIn drops) and **dogs 25→310** (full AKC directory + the ubiquitous non-AKC crosses owners actually have — Goldendoodle/Labradoodle/Pit Bull Terrier; owner-friendly names where AKC show naming diverges, e.g. German Shepherd/Poodle) in a new `constants/breeds.ts` (pure `filterBreeds`/`breedsForSpecies`, `readonly` lists). Extracted `components/pet/BreedPicker.tsx` — search box + 80-row render cap with a visible "keep typing" cue (B-146 no-hidden-overflow) + always-reachable "Other / not listed" free-text hatch (carries the typed query) + 44pt rows; **popular breeds pinned above the alphabetical mass** so the common answer shows without scrolling. **Every legacy breed string preserved** (back-compat test → no existing pet silently drops to "Other"). NO schema (`pets.breed` stays a single string); breed capture still lives only in `EditPetModal` (onboarding/add-pet unchanged). **Reviews:** code-reviewer ship-ready (folded: popular-pinning, diacritic-insensitive search, `readonly`, 44pt) + pm-feature-review SHIP-SHAPED (caught the dog-owner alphabetical-wall-past-the-cap → bail-to-Other duplicate risk → fixed by pinning; + carry-query-into-Other); adversarial-reviewer **N/A** (no clinical/statistical logic). tsc + **849 jest** green (869 after merging `main`). Merged `origin/main` mid-session (#255/#257 had moved it) — only `docs/backlog.md` conflicted: #257 claimed B-196/197/198 for unrelated items, so this session's rows renumbered to **B-209** (multi-breed selection) / **B-210** (breed in onboarding) / **B-211** (list refresh cadence). Personas: Designer ✓ (Principles 1/3/5, searchable long list + empty state) · Engineer ✓ (extraction, `readonly`, no-magic) · Dr. Chen/Data ✓ (breed→disease-risk clinical value; no clinical logic) · QA ✓ (back-compat + functional AC). — shipped via #259
- 2026-07-02 — **`analyze-vomit` 546 crash fix + Pro image-transform downscale + full-screen photo-load perf (B-207)** — QA found the vomit AI read stuck on "Not enough to say / Try analysis" for a 6.47 MB photo. Diagnosed from logs: `analyze-vomit` returning **HTTP 546 (WORKER_RESOURCE_LIMIT)** at ~1.3 s with zero error-log lines and no `event_ai_analysis` row — the char-by-char base64 built a ~250 MB cons-string rope on the multi-MB image and the 250 MB isolate memory limit *hard-killed* the worker before the `try/catch` could write a failure row. Root cause of the oversized photo: `lib/sync.ts`'s `ensureEventAttachmentsSynced` force-re-uploaded the uncompressed original `local_uri` (`upsert:true`) over the inline-compressed object on every analysis trigger. **Three fixes:** (1) Edge — guard on raw `blob.size` BEFORE encoding (`MAX_CLAUDE_IMAGE_BYTES = floor(cap/4)*3`) + replace the rope encoder with a chunked `bytesToBase64` (btoa over 32 KB windows); (2) Edge — `fetchUsableImageBlob` re-fetches an oversized photo via Supabase Storage image transformations (imgproxy, Pro — server-side 1568px `contain`, zero isolate memory) so it gets a real read, every transform-failure path funnelling to the honest `photoUnreadable` degrade (no new failure mode); (3) client — `prepareAttachmentUpload` compresses image attachments before (re)upload in the two event-attachment sync paths (skips non-images/remote sentinels; falls back to the original on compress error). Deployed `analyze-vomit` **v6→v7** via the B-082 MCP path (`verify_jwt` preserved, read-back matched, JWT'd boot smoke-test clean 4xx); a zero-auth render-endpoint probe confirmed **image transformations are enabled**. **Then** the separate photo-load QA report: tapping a vomit photo full-screen took 5–10 s (downloaded the raw 4.48–6.47 MB original) → `getSignedUrl` gained an optional imgproxy `transform`; `app/event/[id].tsx` serves a 1600px `contain` transform for the hero+viewer (shared cached URI → instant tap) with a raw-URL fallback on image error so a disabled add-on degrades to the old behaviour, never a blank photo; derivation extracted to a pure unit-tested `lib/eventPhoto.ts`. **Reviews:** adversarial-reviewer **PASS ×2** (crash-fix + downscale — every counterexample held; downscaling can only upgrade an oversized photo from no-read to a read via the same never-reassure pipeline); code-reviewer **PASS ×3** (fixed: `ensureEventAttachmentsSynced` mime parity; two viewer-fallback bugs — a removed photo lingering + a mid-fallback "Add photo" flash over an existing photo). tsc · **838 jest** (new: `bytesToBase64` ×4, `prepareAttachmentUpload` ×5, `getSignedUrl` ×3, `resolveEventPhotoDisplay` ×8) · **deno 39**. No schema. Backlog: B-203 (multi-photo partial-drop), B-204 (`extract-food-from-photo` same latent base64 OOM), B-205 (downscale — **Done**), B-206 (portrait longest-edge uncapped), B-207 (**Done**), B-208 (refocus empty-hero flicker). Personas: Dr. Chen/Data Scientist ✓ (adversarial ×2) · Engineer ✓ (sync discipline, MCP deploy) · Designer ✓ (viewer perf, 1600px = app bar) · Trust & Safety ✓ (no new access path). — shipped via #255
- 2026-06-26 — **B-186 weight fast-follow: Patterns-dashboard weight card** — added the descriptive weight card to the Patterns dashboard, the dashboard sibling of the Profile `WeightTrendCard`. New `components/dashboard/WeightCard.tsx` is **prop-driven** (the screen owns the load + multi-pet stale-guard, unlike the self-loading Profile card): `app/insights/index.tsx` reads `getWeightHistory`→`computeWeightTrend` in the same `Promise.all`, `lib/dashboardScreen.ts` carries a `weightTrend` descriptor through `buildDashboardCards`. The two cards now share `describeWeightDelta` + `formatWeightDate` (extracted to `lib/weight.ts`, unit-tested) so the **never-reassure** delta copy can't drift; the Profile card was refactored onto them (rendered strings unchanged). **Placement was the PM's "confirm first" gate → PM deferred to the product team → resolved reading-count-aware:** a *populated* trend leads the **health-trajectory/safety cluster** (spec §6 group A — "Is {pet} okay?"; the vet-council #1 missing datum; group A already houses a neutral card, diet-trial progress), while an *empty* card is only a logging nudge so it heads the **descriptive** cluster (pm-feature-review: an empty nudge shouldn't out-rank live safety/intake answers on a pull surface). Neutral in every state — `priority` is ORDERING ONLY (the dashboard is uncapped; never a verdict colour). `selectDashboardState` now counts weight readings so a weight-only pet isn't shown the cold-start empty state. Empty state shows a forward-looking nudge (no `pets.weight_kg` snapshot — a trend surface shouldn't render a static profile number as a tracked reading); single-reading state gained a date anchor so a lone number can't read as "today's". **Reviews:** code-reviewer SHIP-READY (one fix folded: explicit `renderCard` exhaustiveness guard); pm-feature-review — multi-reading SHIP-SHAPED, **safety bar PASS across all three states**, empty/single polish folded in. adversarial-reviewer N/A (display-only, no statistical/clinical logic; the loss flag remains a separate adversarial-mandatory spec, B-190). NO schema (reads live migration 024). tsc + **823 jest** green (+ new `describeWeightDelta`/`formatWeightDate` + dashboard placement/`selectDashboardState` tests). Personas: Dr. Chen ✓ (vital sign leads when real, never reassures) · Designer ✓ (Principle 3/5, neutral) · Data Scientist ✓ (`priority` ordering-only, never-verdict held) · Engineer ✓ (prop-driven, type-only `WeightTrend` import keeps `dashboardScreen` runtime-pure). Backlog: B-192 (empty-state coherence), B-193 (4px token); B-189 scope extended to cover this card's tap-through. — shipped via #251
- 2026-06-26 — **B-186 PR 4: History `weight_check` row + value — v1 weight display track COMPLETE** — surfaced the measured weight on the History timeline, the last surface of B-186's display-only v1. The Scale icon already rendered via `EVENT_TYPES` (PR 2); this PR adds the value: `getTimeline`/`getEventById` gain a `LEFT JOIN weight_checks wc ON wc.event_id = e.id` selecting `wc.weight_kg` (strictly 1:1 via the UNIQUE `event_id`, so no row fan-out — the `meals` join twin; soft-deletes already excluded through the parent `events.deleted_at IS NULL` filter, since `weight_checks` carries no `deleted_at`), `weight_kg` added to `TimelineRow` + threaded through `rowToEvent`, and `EventRow` renders `${kgToLbs(weight_kg)} lbs` as a quiet secondary line — the weight analog of how a meal row shows its food / a dose its drug — reusing `lib/weight.kgToLbs` (the one shared 0.1-lb rounding rule) so the row can't drift from the log step or the trend card. **Never-reassure honoured:** display-only — a number, no verdict/colour/direction. NO schema (reads live migration 024). **code-reviewer run** — one fix-before-merge applied (`fontSize: 13` → `theme.textSM`) + the `numberOfLines={1}` nit; adversarial-reviewer N/A (no statistical/clinical logic). tsc + **805 jest** green (no new tests — pure SQL join + passthrough mapping + display-only render; `kgToLbs` already covered by `lib/weight.test.ts`). No CI configured on the repo (0 checks). Personas: Engineer ✓ (1:1 join, soft-delete via parent) · Designer ✓ (quiet secondary line, matches meal/dose register) · Data/Dr. Chen ✓ (display-only, guardrail held). **B-186 v1 (PRs 1–4) complete; fast-follows B-189/B-190 + STATUS list remain.** — shipped via #249
- 2026-06-26 — **B-182 chronicity detector ⑦, PR 3: owner copy + client renderer — the build chain (PRs 1–3) is COMPLETE** — the deploy-gate PR that makes `symptom_chronicity` a renderable safety card. **Server (`generate-signal/phrasing.ts`):** finalized `templateChronicity` (was a PR-1 placeholder) — names duration + recurrence + count over the honest active-weeks-of-lookback denominator, anchors the first onset ("since {month}"), tiered vet ask, "a read of your logs, not a diagnosis"; added a `symptom_chronicity` branch to `validatePhrasing` (CAUSAL/MECHANISM/FOOD screens; reassurance + "picky" already barred by the safety branch). **Client:** `lib/signal.ts` mirrors the cached payload (`symptom_chronicity` in `InsightType`, new `ChronicityTier` + `SymptomChronicityFinding`, into `SignalFinding`; `associationalOnly` omitted per the ⑤/⑥ mirror convention); `lib/signalCopy.ts` gains `recencyPhrase` + `onsetMonth` helpers and chronicity branches in `sampleLine` (honest "N episodes across A of the last W weeks") and `evidenceText` (onset month + duration + recurrence + still-ongoing recency, tiered ask, never "resolved"); `InsightCard` registers `symptom_chronicity: SentenceBody` (safety rail, no confidence tag). NO schema. **Guardrails:** never causal/mechanism/food/reassurance/resolution — the recency clause ("the most recent today/yesterday/N days ago") is the honest face of the recency floor; guardrail sweeps over every tier×symptom×recency on both server templates and client copy. **code-reviewer fix-before-merge → resolved:** evidence dropped the "since {month}" anchor the card sentence carries (added, with a client `onsetMonth` mirror) + `sampleLine` denominator wording diverged from the evidence ("of 8" → "of the last 8") — both fixed + tests updated. Adversarial review N/A per spec §8 (copy/render only; the clinical logic was adversarial-reviewed in PRs 1–2). Personas: Designer ✓ (Principles 3/6) · Dr. Chen ✓ (vet-ask both tiers, never reassures/causal) · Engineer ✓ (mirror matches payload, InsightType exhaustive) · nyx-voice ✓. 259 deno + 802 jest + tsc green. **DEPLOY un-gated — `generate-signal` redeploy is now a PM Action Item; D2 minEpisodes 6-vs-5 still the live Dr. Chen ratification.** Backlog: B-182 → Partial (build complete, redeploy + D2 remain); follow-up B-191 (chronicity → cross-pet banner; renumbered off a B-189 collision with #248's weight items). Merged `origin/main` mid-wrap (#248 had moved it) — backlog `B-189` ID collision resolved by renumbering this session's item to B-191; code files disjoint. — shipped via #250
- 2026-06-26 — **B-182 chronicity detector ⑦, PR 2: composition, ranking & the B-188 fix** — wired the load-bearing composition couplings on top of PR 1's pure detector (#246), engine-only in `generate-signal/detection.ts`. **(1) B-188 fixed** (the PR-1 adversarial finding): replaced the now-anchored `activeWeeks` bucket measure (`floor((now−onset)/7d)`) with a phase-stable greedy ≥7-day-gap packing `countDistributionWeeks` — counts distinct ~weekly periods from the onset data ALONE, so a two-cluster "barbell" (3 stale + 3 recent, long quiet gap) counts as 2 distribution-weeks (never straddles a bucket edge into a false 3) and the measure is calendar-phase-invariant. It's activity-selection-optimal (the max pairwise-≥7d-apart set), so it never under-counts a genuinely distributed course — every clinical fixture's `activeWeeks` is preserved except the q2-day golden, which correctly becomes 6 (a 42-day span packs into 6 disjoint 7-day reaches, not 7 fixed bins). **(2) ③ valve (§4.4):** `detectReflections`' global gate now also returns `[]` when any symptom `isChronic(s) && s.loggingEligible` — the EXACT predicate `detectChronicity` fires on, so "③ silent ⟺ ⑦ speaks" holds by construction → the improving-tail ("down from 4") + cross-symptom ("itch is improving" next to chronic vomiting) false-reassurance is closed. **(3) ④ suppression + firm inheritance (§4.5, D1):** new composition fn `suppressWorseningWhenChronic` (sibling of `suppressTimeOfDayWhenPostprandial`, runs in `detectSignals` before ranking) drops a same-symptom worsening ④ when ⑦ fires for it and lifts ⑦ to `firm` for any symptom that was also worsening (urgency never lost; inheritance set captured BEFORE the ④ filter); different-symptom worsening is kept (two-signal gestalt). **(4) Ranking:** `SAFETY_TYPE_ORDER` = intake_decline → symptom_chronicity → symptom_worsening (council Consensus #3; all three stay band 0). NO schema, no migration, no caller-query change. Fixtures 11–13 + a B-188 7-phase barbell regression; full generate-signal deno suite green (253) + pre-push tsc/jest. **adversarial-reviewer PASS** — every load-bearing failure mode (barbell straddle, false-negative under-count of a q5/q6-day course, valve drift, urgency-loss, gestalt-drop, rank order) survived a named counterexample; 2 pre-existing safe-direction residuals (R1 multi-axis single-card collapse, R2 base-rate-dependent noise gate) routed to Dr. Chen, neither a PR-2 regression. **STILL DEPLOY-GATED on PR 3** (copy/client renderer) — do NOT redeploy generate-signal. Personas: Data Scientist ✓ (B-188 measure + valve-by-construction) · Dr. Chen ✓ (never-reassure asymmetry) · Engineer ✓ (composition-layer purity). D1 built per recommendation; **D2 (minEpisodes 6-vs-5) still the live PM/Dr. Chen ratification**. — shipped via #247
- 2026-06-26 — **B-186 PR 2: weight logging (stacked on PR 1)** — gave the long-dormant `weight_check` event type a real quick-log path: an `EVENT_TYPES.weight_check` entry (Scale icon) + a numeric `weight` sub-step in `app/log.tsx` (pre-filled with the pet's last reading in lbs — weight is the one event where the value IS the entry, so confirm-don't-enter can't apply; minimise friction instead, Principle 1/Jordan). New `lib/weight.ts` owns the write side-effects (`insertWeightCheck` = event + `weight_checks` child in one transaction + FK-ordered fire-and-forget push, the `insertMeal`/`insertMedicationDose` twin), the kg↔lbs conversions (extracted from `EditPetModal` so the log step + profile edit share one rounding rule), `parseWeightLbsToKg` (rejects empty/non-numeric/≤0/implausible — the client half of the DB `CHECK (weight_kg > 0)` + a guard against wedging the sync queue past `NUMERIC(5,2)`), and `getLatestWeightKg`. Sync up/down mirrors meals exactly: `syncPendingWeightChecks` (gates on parent `events.synced=1` for the FK) + `hydrateWeightChecks` (server-time LWW on `updated_at`, no absence pass — a weight check is only ever soft-deleted via its parent event), both wired into `syncNow`/`hydrateFromCloud`. After each log the `pets.weight_kg` snapshot is repointed at the **latest reading by occurred_at** (a back-dated entry never overwrites a newer reading; Supabase + petStore, best-effort, never blocks the log). The owner note lands on `events.notes` (where every reader already shows it), not the child. **Never-reassure honoured:** the completion beat is calm (no festive gold over a clinical number) and renders **no weight verdict** — v1 is display-only, the loss flag is a later spec. NO schema in this PR (rides #244's migration 024). **code-reviewer fix-before-merge → resolved** (upper-bound cap, theme-token sizes, note-on-event, placeholder, sync-abort test); adversarial-reviewer N/A (no AI/correlation/escalation logic). tsc + **795 jest** green (new `lib/weight.test.ts`; EventIcon test updated — `weight_check` graduated from UI-unexposed to a real glyph). — shipped via #245
- 2026-06-26 — **B-186 weight tracking, PR 1: `weight_checks` schema/data layer — the missing vital sign** — the vet-council's #1 missing datum (zero `weight_check` events on a free-choice *weight-management* cat). **Mandatory audit corrected the kickoff brief's "likely no schema change":** `weight_check` (enum) + `pets.weight_kg` (current-snapshot) exist, but the `events` table carries only `severity`/`notes` — **no column to store a measured weight value** (every other typed event stores its value in a 1:1 child table; weight had none), no logging UI, no trend surface. So B-186 **does** need a migration. Added `024_weight_checks` (1:1 event child mirroring `meals`/`medication_administrations`: `event_id` UNIQUE→events, `pet_id`, `weight_kg NUMERIC(5,2) NOT NULL CHECK >0` — the value IS the event, so NOT NULL unlike `meals.intake_rating`; pet-scoped RLS `weight_checks_owner`, covering index so neither FK trips the unindexed-FK lint, `set_updated_at` LWW trigger) + local SQLite mirror (`lib/db.ts`) + `LOCAL_WIPE_TABLES` reg (children-first, before `events`; AC-6 logout-wipe) + hydration tests. **Applied live** via MCP `apply_migration`; `get_advisors` clean (the only `weight_checks` notices are the project-wide `auth_rls_initplan` pattern + an expected pre-consumer `unused_index`). **v1 scope PM-confirmed: display-only / descriptive — capture + trend, NO weight-loss flag** (a flag needs a defensible threshold + is clinically load-bearing → own spec + mandatory adversarial pass). **Guardrail carried to every consumer:** a weight trend never reassures — stable/rising ≠ wellness (rising can be fluid/edema), loss is the danger signal. Schema-isolated per the migration rule (UI is PRs 2–4). Merged `main` in mid-wrap (#243 B-182 spec, docs-only — the predicted STATUS/backlog collision, git auto-resolved clean, no conflict). Persona sign-off: Data Scientist ✓ (value-gap caught; NOT NULL + `>0`; kg-canonical) · Engineer ✓ (established child-table pattern) · Trust & Safety ✓ (RLS + wipe) · Designer/Dr. Chen N/A (no UI/copy this PR). tsc + **782 jest** green (pre-push hook). Next: PR 2 logging (`EVENT_TYPES` + numeric `weight` log step + insert + sync). — shipped via #244
- 2026-06-26 — **B-182 chronicity/persistence Signal lane (detector ⑦) — build-ready spec (doc-only)** — wrote `docs/nyx-chronicity-signal-requirements.md` off the vet-council deep-dive (§9 #1, Findings 2/3): a deterministic `symptom_chronicity` **safety** lane that fires on **span + sustained-burden + still-ongoing** (orthogonal to ④'s week-over-week **delta** axis), so the council's strongest-but-unstated signal — "ongoing ~q2-day for 6 weeks, unresolved" — finally reaches the owner instead of arriving as ④'s "3 up from 2" or ③'s calm "same as last week." Modeled on detector ④ (`detectWorsening`/`isWorsening`/`resolveWorseningTier`): own long-span helper (`computeChronicityStats`), a shared **`isChronic`** predicate consumed by BOTH ⑦'s firing AND ③'s suppression gate — so the **⑦→③ valve** ("③ silent ⟺ ⑦ speaks") is closed by construction and the **"improving tail of a chronic course"** false-reassurance (4→3→1/wk reads as "improving — down from 4") is killed. Floors (all must pass): span ≥21d / ≥4 episodes / ≥3 active weeks (distribution, the fake-rise analog) / last episode ≤14d (the "ongoing" gate — older ⇒ **SILENT, never "resolved"**). Symptom-agnostic (all correlation types, not vomit-only), timezone-independent, template-only (no LLM). **§10 pre-stages 9 falsification attempts** (flat-relentless, improving-tail, recently-resolved, log-gap-two-endpoints, noise-sparse, …) for the mandatory build-time `adversarial-reviewer` pass; 3 gated PRs (detector → composition/valve → copy). Routed to PM (spec §9 / CLAUDE.md Open Q): **D1** ④↔⑦ same-symptom interaction (recommend ⑦-suppresses-④ + firm-tier inheritance), **D2** floor ratification (noise property test is the empirical gate), **D3** build greenlight. Docs-only; no code/schema/build-phase change. — shipped via #243
- 2026-06-25 — **Vet-council specialist-panel deep-dive on Nyx (new method) — shipped via #241 (draft)** — third dogfood deep-dive, but instead of one frontier model's gestalt (the Opus/Fable PoCs) we convened the formalized **specialist veterinary panel** (`docs/vet-specialist-panel.md`, written this session to formalize the deferred vet-report-discovery §8.8) as **five INDEPENDENT, isolated subagent reads** (GI internist · nutritionist · criticalist · behaviorist · skeptical GP) on the **same computed evidence pack** — model held constant so specialty is the only variable; each ran its own falsification pass; a chair reconciled. **All five independently broke the benign "over-treated cat who eats too fast" story** on three facts it can't explain — 6-week unresolved chronicity, the empty-stomach bilious minority (4 yellow/bile), and refusals of two complete diets (Fancy Feast, Hill's) time-locked to vomit clusters. Reconciled clinical gestalt: **≥2 overlapping processes** (chronic enteropathy + bilious/motility overnight + treat-bolt regurgitation) with the 05-18 foreign-body/pica flag as the separate highest-consequence rule-out. **Net-new over the single-model briefs:** the phenotype decomposition (bile-timing ≠ overnight-timing → several processes), the **vomit-vs-regurgitation** capture gap, the **weight-management-base contradiction** (free-choice Royal Canin Weight kibble undermined by an 86%-treat load; "intake not observed" is load-bearing), and a **built-in adversary** (the skeptic deflated two of the engine's own live cards from inside the panel). **Data:** Nyx now ~6 wk / ~20 vomit episodes / 42 days, ongoing (one today); 336 feedings 86% treats, chicken 67%, Temptations 96; 43 soft-deleted vs 21 live vomits (reframes **B-071** as clinically load-bearing — under-counting). Live engine now shows ④ worsening + ⑥ 3–7am (both built since Fable) but is silent on **chronicity** — the strongest signal, deterministically computable today. **Routed (the brief routes decisions OUT per the research-folder contract):** backlog **B-182** (chronicity lane) / **B-183** (meal-finished-rate lane) / **B-184** (vomit-vs-regurg quick-log field) / **B-185** (re-surfacing `worth_a_call` flag) / **B-186** (weight-tracking) / **B-187** (diet-load); CLAUDE.md Open Questions: AI-Signals-card **bounded-gestalt-reviewer** scope + **surface-report-to-owners** north-star + emerging-signals council input. **Methodology finding:** specialty diversity surfaced what single-model capability didn't — structured disagreement was a lever the single model couldn't pull. Docs only; no code/schema/build-phase change; tsc + 782 jest green (pre-push hook). — shipped via #241
- 2026-06-25 — **B-181 jotted: food-specific analytics on the food library (doc-only)** — captured a PM idea (screenshot of the food detail screen) to surface per-food analytics on `app/food/[id].tsx` (logged frequency, finish/intake history for that food, last eaten, meals-vs-treats split, later any established food→symptom link). Recorded as the per-FOOD cut, **distinct from B-023** (per-pet preferences → Patterns dashboard), reusing the same `food_type`×`intake_rating` aggregates rather than a new pipeline; **carries the intake-not-preference invariant** (a decline routes to a health flag, never "picky"/reassurance). Priority Later, gated on real multi-sample data; Designer/Data-Scientist passes noted (placement vs the clinical ingredient block / small-N honesty). No build-phase change; backlog-only edit. tsc + 782 jest green via pre-push hook. — shipped via #242
- 2026-06-24 — **Pre-TestFlight polish (B-161/B-136/B-062/B-137/B-166 + B-158 + B-060) — shipped via #238 (draft)** — seven items before cutting a new TestFlight build (the first since build 22 / 6/12); five low-risk pure-UI + a med-setup reframe + a clinical safety fix. **B-161:** drug name distinguishes Home "Today" medication rows — shared `formatDrugLabel` (EventRow de-duped onto it) + `TodayZone` subline + `loadTodayEvents` joins `medication_administrations`/`_items_cache` so the name shows on a **cold** load (was live-prepend-path-only); `UNIQUE(event_id)` rules out join fan-out; combo `paired_*` stays Today-absent (B-176). **B-136:** shared `cardActionTouch` (44pt) on all 7 Conditions/Current-medications card actions. **B-062:** remaining 📷/🖼 → Lucide `Camera`/`Images` across FoodPicker CTA, PhotoCarousel, `event/[id]`, food-capture, medication-capture; dead icon styles removed; FoodPicker CTA now matches the MedicationPicker twin. **B-137:** `FreeFeedingStrip` gated to the All/Meal History lenses (a food standing fact, like the §6a markers). **B-166:** right-edge `LinearGradient` "there's more →" fade on both pickers' Recent shelves. NO schema; no clinical/statistical logic. tsc + **772 jest** (+4 `formatDrugLabel`) green; **code-reviewer PASS** — fixed the one should-fix (`try/catch` on the widened today query) + a nit (dropped a redundant `position:'relative'`). On-device QA folds into the TestFlight pass (44pt action-row height + the fade cue). **B-158:** regimen "Course length" → explicit **Ongoing / Set an end** chip toggle (days input shows only for "Set an end"; chronic meds save `target_duration_days=null`, no fake course; matches the modal's Frequency/Route chips — whole-modal ChipGroup stays B-167); code-reviewer ship-ready. **B-060 (clinical):** `analyze-vomit` no longer reassures on the ABSENCE of a red flag — a first regex-denylist was **adversarial-FAILED** (can't enumerate "model asserted wellness"; missed ~86%), replaced with a **STRUCTURAL** pure `selectReadText` that surfaces the model's read ONLY on `worth_a_call` (escalate-on-presence), deterministic `buildMonitorReadText` on the `monitor`/no-flag path → reassurance-on-absence closed by construction (adversarial sweep: monitor surfaces the model read 0/48 floor inputs); prompt/`read_text` schema reworded off the absence framing; **adversarial-reviewer PASS** (rework); residual `description`-field vector → **B-178**. ✅ **B-060 DEPLOYED same session — `analyze-vomit` v6 live** (MCP `deploy_edge_function`, `verify_jwt=true` preserved; pre-deploy byte-gate sha `0231be5a` + read-back match + clean 401 boot smoke-test; `deno check` ✓, deno *test runner* blocked in-sandbox by the pre-existing `Deno.serve`-on-import → logic Node-verified). Live on the current build too (server-side). — shipped via #238
- 2026-06-24 — **B-160: medication-details name-chip helper + strength format copy** — the manual medication-details path (`medication-capture.tsx` `step='confirm'`/`'edit'`) + the regimen modal (`AddMedicationModal.tsx`) now offer a horizontal, scrollable **tap-to-fill drug-NAME chip row** to cut the misspellings that fracture the organically-built `medication_items` library and weaken the PR-9 Signal key. New `components/medication/MedicationNameChips.tsx` (built on `FilterChip`, **deliberately distinct from the single-select `ChipGroup`** — a name chip FILLS the field and the row vanishes; it is not a persistent selection) with a right-edge fade (the B-146 "never hide options with no peek" cue); shown **empty-state only** so the AI-confirm happy path shows none. Fed by curated `COMMON_MEDICATIONS` (≤16; **S1 Dr. Chen trim** — "Gabapentin / Trazodone" → the real, separately-bottled Trazodone, since Gabapentin is already listed) + pure `commonMedicationsForSpecies` (a cat leads with methimazole/mirtazapine, a dog with carprofen/pimobendan; a **UNION**, never a filter — never hides a `both` drug) in `lib/medications.ts`. Plus **always-on strength FORMAT helper copy** ("copy it exactly, with the unit"). **The load-bearing §3 clinical catch is honored: NAME chips only — STRENGTH gets format copy, NEVER value chips** (a tappable strength value would erode the §6.5 dose-confirm gate). A name-chip tap only sets the name — `strengthConfirmed`/`StrengthGate`/`canSaveMedicationCapture` are **untouched** (verified) — and fills the text field ONLY (no `medication_items` row / id / link), so the library stays organically built; in the modal it routes through `onChangeDrugName` so a free-text name clears any stale link (`buildRegimenPayload` invariant). `critical` flag defined for forward PR-9 `is_critical` reuse (§7), not rendered. **adversarial-reviewer N/A** (no statistical/clinical *logic* added — the §3 call is a Dr. Chen judgment, reviewed in-context, not a falsifiable engine); **code-reviewer PASS** after one folded theme-token fix (`lineHeight` → `theme.lineHeightSM`). NO schema, NO migration, NO Edge Function. +11 tests (curated-list contract + ordering/union); tsc + **778 jest** green. — shipped via #236
- 2026-06-24 — **`generate-signal` deployed v22 (PR C1) + edge-function currency audit (all 5 reconciled)** — deployed PR C1's `generate-signal` **v22** from merged `main` via the Supabase MCP (byte-verified bundle → scratch-cmp gate → deploy → live read-back sha `0e66e002…` == bundle → boot smoke-test 404/401; `verify_jwt=true` preserved). On a PM request to "ensure all edge functions are up to date," reconciled every live function against on-disk source via live read-back: **`analyze-vomit` was genuinely STALE → redeployed v5** (live v4 from 2026-05-25 was missing **B-028 (#220, merged 2026-06-22)** — it still ran the old inline write-back without the tested `buildAnalysisWriteBack` never-clobber guard; a real merge-without-deploy gap); **`extract-food-from-photo` redeployed v10** (its git "stale" signal was a shallow-clone artifact — `67fafe2`/#183 is the clone's graft boundary; redeployed to be certain); **`delete-account` v3 + `extract-medication-from-photo` v1 verified current** (deployed from their own PR branches before squash-merge; read-backs carry the final B-128/B-123 logic; no later commits). Every redeploy: scratch-cmp byte-verified pre-deploy + boot smoke-test + `verify_jwt=true` preserved. Process finding (merges don't auto-deploy Edge Functions → silent drift) → **B-178**. Operational + STATUS only (no code/schema). — shipped via #239
- 2026-06-24 — **Food dedup & merge: product-team direction session → build-ready DRAFT spec** — convened the team on duplicate `food_items`. Aligned **prevention-first**: **Phase 1 = prevention (B-009)** — tiered client-side match-and-steer on the "Add new" path only (hot re-log path untouched, Principle 1) + reactive UPC-collision resolution in `extract-food-from-photo`; **no schema**. **Phase 2 = merge (B-018 + B-005)** — service-role `merge-foods` Edge Function repoints `meals`/`diet_trials`/`feeding_arrangements` then **tombstones** the loser (`merged_into`, never delete; historical feedings read as the survivor); rides existing hydration rails (`updated_at` + B-054). **Survivorship/reversibility/multi-user-auth parked as merge-phase decisions — not Phase-1 blockers** (PM call). Key insight: the picker already `GROUP BY`s exact dupes, so prevention targets textual variants while merge fixes the latent arbitrary-`food_type` integrity bug. ID reconciliation: the work is B-009/B-018/B-005 — *not* "B-142" (→ B-158) / "B-162" (migration backfill). Spec `docs/nyx-food-dedup-requirements.md`; backlog rows reconciled. Docs-only; tsc + 768 jest green via hook. Next: PM ratifies → build Phase 1 PR P1 (`canonicalizeProduct` + `findDuplicateCandidate`; no schema/UI). — shipped via #237
- 2026-06-23 — **B-156 Phase C, PR C1: Signal engine reads the dose↔vehicle pairing (combo confounder) — combo build chain COMPLETE** — the AI-Signal correlation engine now attributes a medication-in-food combo to the **drug, not the food** (the §1 "antibiotic-not-chicken" harm, now closed for the combo case). (1) **Core:** a meal flagged `isMedicationVehicle` (its event id is a live dose's `paired_event_id`) has its **protein dropped** from the case/control exposure set in `detectCorrelations`→`windowExposures` (it still counts toward `mealCount`, so the window stays logging-eligible; the drug rides separately as a `MedicationWindow`). A **per-exposure** drop, deliberately NOT a candidacy-wide free-fed exclusion — the same food without a pill on another day stays a clean exposure. (2) **B-174 RESOLVED:** `doseToMedicationWindow` returns null (drug not on board) for an in-doubt combo dose (`adherence null` AND vehicle refused/picked — carrier not eaten → pill not delivered); standalone `null` (§5.1=given, the dominant B-135 signal) + an explicit owner `given`/`partial` are untouched. (3) **I/O shell:** both threaded off **one** `paired_event_id` join (`pairedEventIds` marks vehicles, `mealIntakeById` resolves each combo dose's vehicle intake); no combos ⇒ byte-identical. Composes with B-117 PR 9 `medicationWindows`. **adversarial-reviewer PASS** — executed counterexamples on all 7 invariants: never-reassure held under the suppressed-combo flare + the every-safety-meal-flagged case; ②/③/④ byte-identical under the flag (`deepEqual`); Bonferroni family **demote-only, never inflates**; multi-pet/soft-delete/out-of-lookback/N-dose plumbing safe. Its one non-blocking copy residual → **B-177** (staple-washout names a vehicle-attributed protein with an inaccurate "nothing to compare" reason — honest-uncertainty direction, NOT reassurance). NO schema (reads live migration 023); +6 deno tests (**150 deno + 768 jest** green); tsc + `deno check` clean. ⚠️ draft, NOT yet deployed — deploy `generate-signal` from merged `main` after #234 (inert until real combo data flows). Only **promotion** (PM) remains on B-156. — shipped via #234
- 2026-06-23 — **B-156 Phase B, PR B4: combo-edit cross-link (dose ↔ vehicle) — Phase B build chain COMPLETE** — makes a combo (a dose given inside a meal/treat) legible as ONE act across its two independent History events (the G2 model) **without merging them**: a visible, tappable cross-link **on both History rows + both detail screens**. Dose → **"Given with {food}"** taps to the meal/treat; meal/treat → **"Given with a {drug} dose" / "Given with N doses"** taps to the dose, fed by a new reverse-lookup `PAIRED_DOSE_REVERSE_JOIN` (aggregated GROUP BY → N doses in one bowl never multiply the meal's timeline row) added to `getTimeline`/`getEventById`. **Both AC met: soft-delete drops each link cleanly** (forward join nulls `paired_food_name`; reverse join filters `pade.deleted_at IS NULL`) and **the link survives an edit** (the intake/adherence/vehicle UPDATEs are column-narrow, never touch `paired_event_id`; the two events stay independently editable). Copy **symmetric "Given with …"** on both sides — reworked off the Designer's `+`-reads-as-create collision (vs B2b's "+ Add a med…"). New pure helpers `pairedVehicleLinkLabel`/`pairedDoseLinkLabel` (return null → no dangling affordance) + a shared `ComboCrossLink`/`ComboLinkRow`. Reviews: **pm-feature-review** (Jordan+Sam) — dose→meal SHIP-SHAPED, meal→dose copy reworked; **code-reviewer** — reverse-join correctness / soft-delete / multi-pet / edit-survival all HOLD, fixed the 44pt tap-target floor. Backlog → **B-175** (N-dose-in-one-vehicle nav), **B-176** (Today/Home link parity). NO schema (only a local SQLite index mirroring migration 023); reverse SQL + label helpers tested against in-memory SQLite; tsc + **768 jest** green (+12). Only **Phase C** (engine confounder, gated) + **promotion** (PM) remain. — shipped via #233
- 2026-06-23 — **B-156 Phase B, PR B3: the intake→adherence SAFETY COUPLING (Flow B) — adversarial-reviewer PASS** — the clinical payoff of the whole combo. A combo dose whose linked vehicle is marked **refused/picked** lands **UNCONFIRMED** (adherence `null`, **never an auto `given`**); the auto-dismissing card **sharpens** its prompt to **"Did {pet} still get it?"** (no pre-lit chip + a faint "{pet} didn't finish the food." reason); the dose **resurfaces calmly** — a rose **"Unconfirmed"** tag on the History row + a soft-rose note on the dose-detail screen (the adherence chips above resolve it). **Architecture = couple-at-creation + derive-at-read-time:** pure `initialComboDoseAdherence` (refused/picked→null, else `given`) sets the starting adherence in `handlePickMedication` (threaded to the dose write, the optimistic prepend, and the card payload via new `MedicationPayload.vehicleIntake`); the in-doubt RESURFACE is a pure read-time derivation `isComboDoseInDoubt` (combo + vehicle refused/picked + adherence null) fed by a LIVE join in `getTimeline`/`getEventById` (`paired_vehicle_intake` + `paired_food_name`, routed through `events pe … AND pe.deleted_at IS NULL` so a soft-deleted vehicle drops the flag cleanly) — **no new enum, no new column, no stored flag, no auto-flip**; `null` already counts un-given toward compliance (`bucketAdherence`→`unrated`). An EXPLICIT owner answer (INCLUDING `given` — they may have pilled directly after the refusal) clears the doubt and is never re-nagged. On a vehicle-read failure the dose falls back to `null` (never asserts `given` under uncertainty). The never-reassure / never-"fussy" / no-`!` invariant is a **TEST** over every emitted string (`comboAdherencePrompt`/`comboInDoubtReason`/`doseInDoubtNote`/`DOSE_IN_DOUBT_TAG`), clinical-guardrails Pattern 8 — the medication analog of `analyze-vomit`'s escalation floor (Pattern 2: no reachable reassuring verdict by construction). **adversarial-reviewer PASS** — every load-bearing failure mode (silent/auto `given`, compliance over-read, over-nag an answered dose, creation↔read-time divergence, multi-pet leak, soft-deleted vehicle, refusal-softened copy) survived a concrete counterexample; its one fixable finding (soft-deleted-vehicle legibility) closed in-code via the `pe.deleted_at` join filter. **code-reviewer fix-before-merge** addressed: `doseInDoubtNote` grammar reflowed + pinned by an EXACT-string test; `paddingVertical:2`→`theme.spaceMicro`; two clarifying comments. **G4 RESOLVED → document-as-known-limit** (the coupling keys off the vehicle, can't see "ate-around-the-pill"/the `some` edge → B-173). Residuals named: scenario-2 provenance (auto-`given`-then-refused needs an `adherence_source` column, deferred), detection-`null` reconciliation (→ B-174, the PR-9 confounder gate). NO schema; tsc + **756 jest** green (+18). Next: **PR B4** (combo-edit cross-link) + Phase C (engine, gated). — shipped via #232
- 2026-06-23 — **B-156 Phase B, PR B2b: combo entry UI — "+ Add a med given with this" → linked dose** — the UI half of the combo (stacked on B2 #230's data layer). A quiet, **meal/treat-only** opt-in line on the auto-dismissing meal completion card (the **3rd affordance on the narrowest surface in the app** — spent deliberately; the standing "stop before a 3rd affordance" guard honored + documented in the card header) opens the medication picker **pre-bound to that meal** (context banner naming the pet + food), and the picked dose lands **LINKED** to the meal event (`paired_event_id`) with `how_given` **inferred from the food type** (meal→`in_food`, treat→`in_treat`), confirmed via the dose card reframed **"Logged together · {drug} · with {food}"** so the one-act link is legible. **Principle 1 holds — the ~99% no-med path stays exactly one tap** (the line adds zero taps; renders only for foods you'd hide a pill in). **Multi-pet wrong-pet guard:** the dose binds to the **meal's** pet (`pairedPetId`, captured on the meal payload at log time via the new `MealPayload.petId` — also fed by the FAB quick-meal path), never a re-read active pet → `dose.pet_id === paired_event.pet_id` **by construction**; the migration-023 same-pet trigger backstops. `handlePickMedication` now serves standalone AND combo off the **shared** `insertMedicationDose` path; `handleConfirm` returns `petId`; combo-back closes the modal. New pure `inferDoseVehicleFromFoodType` (`lib/medications.ts`); `MealPayload.petId` + `MedicationPayload.pairedFoodName` (momentStore). **Scope = entry + linked-dose write ONLY** — the intake→adherence safety coupling (not-finished vehicle → dose-in-doubt) is the gated, **adversarial-mandatory PR B3**, deliberately not folded in. **Reviews:** pm-feature-review (Jordan+Sam) — banner + multi-pet + link legibility **SHIP-SHAPED**; entry copy reworked off the twice-flagged (§9 + this review) tense-ambiguity ("+ Gave a med with this" → "+ Add a med given with this"). code-reviewer — multi-pet write-path safety **HOLDS** (no pet-id divergence path); fixed the picker mount guard (gate on `pickerPetId`, not `activePet`) + an off-scale `fontSize` → `theme.textSM`. Deferrals → **B-171** (owner-facing card names generic "cetirizine" not brand "Zyrtec" — PM call, spans the med surface), **B-172** (combo card "Did {pet} take it?" reads redundant against pre-lit chips — couples with B3). No schema; +6 tests (4 inference + 2 momentStore); tsc + **738 jest** green. Next: **PR B3** (safety coupling, adversarial-mandatory). — shipped via #231
- 2026-06-23 — **B-156 G2 gate resolved + Phase B PR B2: `paired_event_id` mirror + sync + write-path plumbing** — **G2 (combo edit model) RESOLVED → two independent, cross-linked History instances** (not a merged unit; PM-ratified 2026-06-23 via the #229/#230 merge): a combo IS two events (single-event-timeline; Option D rejected) and History already shows two rows, so each is edited on its own existing detail screen (the meal-intake edit + the A3 dose adherence/`how_given` edit on `event/[id].tsx`) — **zero** new coordinated-write surface; adherence stays independently/explicitly editable and is **never auto-recomputed** from an intake edit (never-auto-flip / n=1-never-reassures); the link is shown **visible + tappable on both rows** so "one act" stays legible without merging. **Un-gates Phase B** (G1/G2/G3 all resolved; only build-time G4 left); rationale + plan reslice in `docs/medication-food-combo-investigation.md` §9 R2 / §10. **PR B2 (stacked on B1 #229)** — the A2 analog for the combo link: `paired_event_id` added to the `medication_administrations` SQLite mirror (`MEDICATION_SCHEMA_SQL` in `lib/medications.ts` + an upgrade ALTER in `lib/db.ts` for pre-existing installs), to the push mapper `administrationRowToRemote` + the hydration pull (`lib/sync.ts` fetch-list + INSERT/`DO UPDATE`/params in lock-step vs the **B-057** drift trap), and as an optional `pairedEventId` param on `insertMedicationDose` (defaults NULL; the caller must pass a same-pet event — the migration-023 trigger is the server-side backstop). **No UI** — the combo entry "+ Gave a med with this?" line is **PR B2b** (the PM re-sliced the original single PR B2 data-layer-first, mirroring A2→A3). Round-trips device↔Supabase↔device; an unlinked dose reads a clean NULL. **+4 tests** (mapper forwards-as-is + null-clean; local-DDL round-trip + null-default; the B-057 key-set drift guard extended to `paired_event_id`); tsc + **732 jest** green; `supabase-sync` skill applied; code-reviewer pending. **B1 (#229) still a DRAFT** — its only blocker was G2 (now resolved); ready to mark ready/merge, after which this B2 PR retargets `main`. — shipped via #230
- 2026-06-23 — **B-156 Phase B, PR B1: schema — dose↔meal `paired_event_id` link + same-pet guard** — the foundational schema for the combo (a dose given inside a meal/treat). Additive migration `023_dose_paired_event.sql` on `medication_administrations`: nullable `paired_event_id` FK→`events(id)` `ON DELETE SET NULL` (a hard-deleted vehicle clears the link, never cascade-kills the historical dose; **no `UNIQUE`** — N doses may share one bowl), a partial reverse-lookup index `WHERE paired_event_id IS NOT NULL`, and a `BEFORE INSERT OR UPDATE` trigger `enforce_dose_paired_event_same_pet` rejecting any cross-pet/cross-owner link the bare FK can't catch (defense-at-rest against the multi-pet wrong-pet-adherence hazard). Hardened the fn: `SECURITY INVOKER` + `SET search_path=''` + schema-qualified `public.events` (closes `function_search_path_mutable`). Applied live (`aigchluqluzuhtbfllgh`) + verified four ways: structure (nullable, FK delete=SET NULL, index, trigger), an **isolated rolled-back empirical test** (cross-pet→`check_violation`, same-pet→accepted; `total_admins` unchanged, nothing persisted), `get_advisors` security clean (`prosecdef=false`, `proconfig={search_path=}`), FK-covered/no new perf finding. **rls-privacy-reviewer PASS — BOUNDARY HOLDS** (cross-pet/cross-owner INSERT, NULL→cross-pet UPDATE, SET-NULL orphan, RLS/error-oracle each given a concrete attack and held) — its one latent finding (the guard fires on `medication_administrations`, not `events`; safe only while `events.pet_id` is immutable in-app, grep-verified) → **B-169**; pre-existing fn-hardening debt → **B-170**. Schema-only/migration-isolated — no app code (mirror+sync = PR B2; coupling = PR B3 adversarial-gated; edit = PR B4). **GATE: G3 satisfied (#228); G2 (edit model) STILL OPEN — schema is G2-agnostic, ships as draft #229 for the PM's ahead-of-gate call** (the migration header's premature "G2 ruling" line corrected to match Open Questions/backlog). Tests N/A (schema-only; verified via the live-DB trigger test); 728 jest green at push. — shipped via #229
- 2026-06-23 — **B-153 + B-154: one-tap dose → active-regimen linking + "Log a dose" card affordance** — closes the two halves of the §5.1 gap surfaced by the `pm-feature-review` med dogfood (#213). **B-153 (write-path):** `handlePickMedication` now resolves the drug's most-recently-started active regimen via new `getActiveRegimenForDrug` (`ACTIVE_REGIMEN_FOR_DRUG_QUERY`, read from the locally-hydrated `medications` table so linking works offline) and carries `medication_id` + inherits `dose_amount`; lookup failure degrades to an honest ad-hoc dose (logging is never gated on the enrichment). `attributeDosesToRegimens` reworked to a **two-pass precedence** — an explicit `medication_id` link is authoritative (never re-matched by drug/window; a link to an out-of-set/ended regimen counts toward nothing, never silently reassigned), else the legacy item+window fallback. **B-154 (entry point):** a "Log a dose" action on each Current-medications regimen row (`handleLogDose`) writes via the **shared** `insertMedicationDose` with `medication_id = reg.id` + inherited dose, shows the same warmed completion card (adherence downgrade reachable), optimistic `given++`. `insertMedicationDose.medicationItemId` relaxed to `string | null` so a **free-text** regimen (no library item) is loggable for the first time — its only path to accumulate doses. `loadMedications` now fetches doses via `.or(medication_id.in,medication_item_id.in)` (fail-closed UUID guard) and maps `medication_id`. **§5.1 Open Question resolved → LINK** (recommend-and-proceed, not load-bearing; awaiting PM ratification). **Unblocks B-156 Phase B G3.** No schema. **adversarial-reviewer PASS** (double-count / wrong-regimen / compliance over-read / refusal-softening / `.or` injection all held) — its one over-reassurance finding (optimistic `given++` going stale after an in-session downgrade to refused/missed) closed by moving the card to a `useFocusEffect` ground-truth reconcile; **code-reviewer fix-before-merge** (the `.or()` string interpolation) resolved with a fail-closed UUID guard + precise invariant comment. +12 tests (7 link-precedence + 4 active-regimen SQL + factory); tsc + **728 jest** green. — shipped via #228
- 2026-06-23 — **B-156 Phase A, PR A3: `how_given` capture + display UI** — completes Slice B of the combo-log plan (`docs/medication-food-combo-investigation.md` §10), stacked on #226 (A2). First applied/verified migration `022` to live via the Supabase MCP (it was already applied in the A2 session — verified rather than re-applied: enum + nullable column present, `get_advisors` clean) and un-drafted #226. Then built the vehicle UI: (1) an optional, skippable, default-null "How was it given?" chip row on `MedicationCompletionCard` (subordinate to the adherence row; never blocks the auto-dismiss); (2) a retroactive edit on the dose **event-detail** screen `app/event/[id].tsx` — **reconciled the doc's "edit on `medication/[id].tsx`" to the architecture**: that screen edits the drug *catalog* item, but `how_given` is a per-**dose** fact, so the edit belongs on the dose event; (3) a quiet read line in History (`EventRow`). New `components/log/VehicleChipRow.tsx` (mirrors the optional/clearable `IntakeChipRow`, not the always-stated `AdherenceChipRow`); `updateDoseHowGiven` (the descriptive twin of `updateDoseAdherence`); `how_given` threaded through `getDoseForEvent`/`getTimeline`/`getEventById`/`TimelineRow`/`NyxEvent`/`rowToEvent`; canonical `DoseVehicle` + `MEDICATION_VEHICLE_OPTIONS` + `vehicleLabel` + `asDoseVehicle` in `lib/medications.ts` (single source — the inline union in `insertMedicationDose` now aliases it); `patchHowGiven` on the moment store. **Descriptive only** — carries no adherence/safety meaning (intake→adherence coupling is the gated Phase B), so no adversarial review needed (mirrors A2). nyx-voice ✓; code-reviewer **fix-before-merge → resolved** (eventStore inlined the vehicle union → now imports `DoseVehicle`; a `fontSize: 12` magic number → `theme.textXS`; centralized the last read-coercion in `asDoseVehicle`). +8 tests (vehicle options drift-guard + label + `asDoseVehicle` + `patchHowGiven`); tsc + **717 jest** green. Open follow-up: `022_dose_how_given.sql` isn't committed on-disk (applied via MCP; SQL in #226 body) → schema-isolated PR (PM Action Item). — shipped via #227
- 2026-06-23 — **B-156 Phase A, PR A2: `how_given` dose-vehicle local mirror + sync + write** — client-only Slice B of the combo-log plan (`docs/medication-food-combo-investigation.md` §10). Adds `how_given` (direct/in_food/in_treat/in_pill_pocket/other) to the `medication_administrations` SQLite mirror (+ an upgrade ALTER for installs from B-117 PR 2 #194), an optional `howGiven` param on `insertMedicationDose` (closed literal-union type, like `adherence`), and the hydration pull + push mapper — so the vehicle round-trips device↔Supabase↔device; `how_given` joins the `DO UPDATE SET` so a cross-device vehicle edit propagates. Descriptive fact only: unset = clean NULL, **never coerced** (the same never-default rule `adherence` uses; carries no adherence/safety meaning — coupling to adherence is the gated Phase B). **G3 verified at kickoff: B-153/B-154 do NOT block A2** — they gate Phase B's combo *linked-dose* write, not Phase A (§10 lists Phase A as ungated, concurrent with B-153/B-154; only a trivial `insertMedicationDose`/`lib/sync.ts` merge to expect). **Prereq before it ships: apply migration `022_dose_how_given.sql` (PR A1, additive enum + nullable column — SQL + pre-flight in #226) to live**, else the dose-push upsert rejects on the missing column and ALL dose sync queues. +4 tests (wire round-trip + null-clean, local-DDL round-trip + null-default, B-057 key-set drift guard); tsc + 707 jest green; code-reviewer PASS. A3 (capture/display chip) next. — shipped via #226
- 2026-06-23 — **B-160 spec: medication-details helper copy + popular-name chips** — build-ready DRAFT `docs/medication-details-helper-requirements.md` + HTML mock `docs/medication-details-helper-mock.html` (PR #222). The manual "Medication details" screen (`medication-capture.tsx` edit/confirm) + (recommended S5) the regimen modal get a tappable drug-NAME example-chip row (curated `COMMON_MEDICATIONS` in `lib/medications.ts`, species-ordered) + richer per-field helper copy. **The load-bearing decision (§3): example chips on the NAME only — STRENGTH gets format helper copy, never value chips, since a tappable strength value would erode the §6.5 dose-confirm gate.** Chips fill the text field only (no `medication_items` row / no id / no strength) → never a back-door catalog (parent §D2/S5) nor a B-122 PII surface; the `critical` flag is a forward reuse candidate for S2/PR-9 `is_critical`. Reconciled mid-wrap to the B-146 `ChipGroup` refactor on main (name row is a NEW horizontal tap-to-fill row on `FilterChip`, distinct from single-select `ChipGroup`). Personas: Dr. Chen/`clinical-guardrails` (§3 catch), Designer, Data Scientist (name-key/B-052), Engineer, Product Owner, Trust & Safety. 1-PR plan; 5 sub-decisions (S1 list trim, S5 modal). Docs-only — no code/schema/build-phase change; tsc + 698 jest green via hook. — shipped via #222
- 2026-06-22 — **B-146: ChipGroup — wrapping option pickers replace silent h-scroll** — medication Form (10) / Route (8) + food Format (10) rendered chips in a hidden-overflow horizontal `ScrollView` (no scrollbar/arrow/fade), so 5–6 options sat off-screen → owners picked from only what they could see (discoverability + clinical-field correctness). New accessible `components/ui/ChipGroup` (radio-group a11y, 44pt, pre-selects the AI/most-likely value, `rowGap`>`columnGap` to clear FilterChip's hitSlop) renders every option; `allowDeselect` preserves each site (med form/route clear-on-re-tap; food format required, never blank). Consolidates 3 hand-rolled copies (ChipScroll/ChipRow/formatRow) → 1 primitive, net −54 LoC; matches `AddMedicationModal`'s existing wrap. History filter left as-is (already edge-faded); Recent shelves → B-166; AddMedicationModal chips → B-167; pre-existing FilterChip a11y/`#fff` debt → B-168. Convention added to CLAUDE.md. Designer/Engineer/QA/T&S sign-off; code-reviewer ship-ready. 5 ChipGroup tests; tsc + 703 jest green. No schema. — shipped via #224
- 2026-06-22 — **B-156 (combo-log: med given with food/treat): product-team investigation → build deferred (PM).** Convened the team on whether to piggyback medication capture onto the meal/treat flow (Zyrtec-in-a-Delectable). PM chose **keep treats + meds separate for now** (not "bad idea," "not now"). Decision record `docs/medication-food-combo-investigation.md`: (1) the recent-treats trap is solved by construction — the food↔med link is per-**event**, never on `food_items` (the B-010 saw-it/found-it shape), so Recent re-adds the bare food; (2) the real value is **adherence accuracy** — a pill in a not-finished vehicle is a *dose in doubt*, and two unlinked logs record a false-adherence "given" the med n=1-never-reassures rule forbids; (3) the one real cost is the deliberately-narrow meal completion card. Recommended phased shape for the future build: slice B (`how_given` enum on the dose) → slice C (piggyback combo, entry = treat/meal card, intake→adherence coupling, adversarial-gated) → §8 engine reads the pairing. Also added a concept HTML workflow mock (`docs/medication-food-combo-mock.html`) + a **pet-owner review** (Jordan + Sam via `pm-feature-review`): Flow C (`how_given`) & Flow D (trap) WORKS-FOR-ME; Flow A/B NEEDS-WORK — the safety prompt can't live on the 5s/1.5s auto-dismiss card (couples with the live B-117 "med card unrated-until-touched" open question; CLAUDE.md cross-linked), edit-a-combo model undecided, + the cat-ate-around-the-pill false-adherence gap (all captured in doc §9). Then, on PM request, wrote a **gated PR-by-PR build plan** (doc §10): team raised no objection to planning, only 3 "keep-it-honest" flags (gate the combo behind a timing spike + 2 decisions; combo composes w/ B-153/B-154's dose-write; promotion is a PM call). Plan = Phase 0 spike + Phase A `how_given` (A1–A3, **ungated, buildable now**) + Phase B combo (B1–B4) + Phase C engine (gated, adversarial). **G1 RESOLVED (PM): the safety prompt's card auto-dismisses — with the non-negotiable `clinical-guardrails` fail-safe that an *unanswered* prompt records `unconfirmed`, never `given` (no path to a reassuring verdict by construction), and resurfaces calmly.** So Phase B is now gated on G2 (edit model) + G3 (B-153/B-154 order) only; PR 0 downgraded gate→de-risk. CLAUDE.md med-card open question narrowed to critical-drug escalation. B-156 Later→**Next**. NO schema, NO app code, NO build-phase change yet. — shipped via #221
- 2026-06-22 — **B-147: empty-state copy sweep (Principle 5)** — warmed the genuinely-plain owner-facing empty states on the older top-level screens (History filtered + true-empty, Foods title, Profile Conditions + Medications, edit-event food picker) up to the bar the Home/dashboard surfaces already set; pet-named + forward-looking per `nyx-voice` Pattern 3, **copy-only** (no style/logic), every rewrite a prompt-to-log never a wellness verdict (§11 #2 / Pattern 6). Left by design: `VomitAnalysisSection` n=1 read, event-not-found / no-pet guards, the 📷 hero (→ B-062), archived-pets. Designer + Engineer lenses (per PM scope); Data-Scientist one-line safety check clean. Engineer de-dup follow-up (shared `EmptyState` primitive + copy-lint) → **B-165**. No build-phase / precondition change. tsc clean; 698 jest green. — shipped via #223
- 2026-06-22 — **B-028: editable per-incident AI structured fields + "Edited [date]" provenance** — the read-only vomit AI fields are now owner-editable on the event detail screen (`VomitFieldsEditor` + shared `vomitFields` vocab); per-field "edited" marker (derived: live field vs cached `ai_raw_payload`) + one calm "Edited [date]" line; n=1 read stays dismissible-not-editable + owner-facing-only (never on the report). **The never-clobber-on-re-analysis guard — untested until now — is extracted to pure fns and tested both halves:** `buildAnalysisWriteBack` (server; edited_at set ⟹ refresh read/flags only, never a structured field or `ai_raw_payload`; 3 deno tests) + `buildVomitEditWrite`/`deriveEditedFields` (client; write only editable fields + edited_at, never a read column; jest). **adversarial-reviewer PASS** (re-read-clobber, escalation-floor-bypass, revert-to-AI provenance, read↔field divergence all held; one cosmetic `contents`-dedup finding fixed + tested). code-reviewer fix-before-merge addressed (hitSlop→16; broken entity; tokens). **Clears the last Step 9 precondition (PR-7 owner-confirmed fields).** Per-tap-failure-hides-edits UX residual → B-164. No schema (migration 013). jest 690 · deno 28 · tsc clean. — shipped via #220
- 2026-06-22 — **B-115: exact-timestamp treat re-log collapse** — new pure `collapseTreatRelogs` (`lib/analytics.ts`) dedups `(foodItemId, ms)`-identical TREAT rows to one EXPOSURE before `computeTopProteins`/`computeTopFoods` ranking, so a multi-piece handful logged per-piece can't inflate a treat's count/share/rank/floor on the vet-report-bound diet-confounder line (spec §8.6 / §11). Treat-only + exact-ms + non-null-id = narrowest SAFE scope (never over-collapses a genuine giving → never hides a confounder; meals untouched → §11 #1 finished-rate + decline lane unaffected **by construction**; B-111 + §11 #5 floor preserved). Decision settled Data-Scientist-lens (dedup, not raw-count). 8 new tests. Adversarial-reviewer PASS. Clears the Step 9 Phase-2/PR-6 precondition **for the EXIF-collision/future-batch case**; rapid per-tap residual (distinct ms) → B-163. No schema; jest 679. — shipped via #219
- 2026-06-22 — **B-044 migration-drift audit closed** — full repo-migrations-vs-live-DB reconciliation: **zero schema drift, nothing to apply.** `vet_visit_attachments` (table+index+RLS) already present (003 partial-apply gap closed 2026-06-06 by `complete_003_vet_visit_attachments`); `food_items.photo_path` singular is **dead** (superseded by 007 `photo_paths` TEXT[]; `refreshFoodCache` reads `photo_paths`, derives `[0]` into the *local* cache only). All 22 on-disk migrations' objects verified live (22 enums · ALTERed cols · RLS + 13 storage policies · `on_auth_user_created` + `updated_at` triggers · 5 buckets). Root cause: `schema_migrations` records only 5/22 (001–019 dashboard-pasted, unrecorded) → live introspection is the only trustworthy drift signal. Clears the Step 9 B-044 precondition; history-backfill residual → B-162. Docs-only. — shipped via #218

- 2026-06-22 — Vet-report (Step 9): **build-ready requirements spec + 10-PR plan** — `docs/nyx-vet-report-requirements.md` (DRAFT): the section contract + the server-side `generate-report` architecture (pure `report.ts`/`render.ts` + I/O shell, mirrors `generate-signal`; **deterministic, no LLM on clinical content**) + a 4-phase / ~10-PR plan. Folds **§8.1 vet-only** + **§8.3 scope-cascade/cherry-pick-guard** + the must-carry consumers as owned PRs (B-117 PR 10 / B-040 / B-102 PR 6 / B-010 / B-023 PR 5 / B-143 / B-144); §8.4/§8.5 ratified one-line. **NO new schema/app code; locks after the real-vet R1/R2 pass; preconditions B-044/B-115/B-028 flagged.** Backlog rows reconciled (B-010/B-028/B-115/B-143/B-144). — shipped via #217
