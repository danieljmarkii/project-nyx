_Canonical answer to "where are we?". High-churn: update inline at session end and any time these change mid-session. CLAUDE.md is the stable operating manual; this file is the volatile state. **Keep it scannable** — prose narrative and build history belong in `docs/sessions/` + PR descriptions + git, not here (the file is reconstructable via `git log -p STATUS.md`)._

_**Size budget (enforced at `/wrap`, added 2026-07-19 workflow retro; tightened 2026-07-25):** target under ~200 lines. **Completed PM action items are deleted, not left checked** — `git log` is the archive. If a section is growing unbounded, that's the signal to prune, not to keep appending._

_**No "Last updated" line, and no session list — deliberately (2026-07-25).** Both were single-line, every-session rewrites, so every pair of parallel sessions conflicted on them by construction; the overnight of 2026-07-24 shipped **two** contradictory "Last updated" lines to `main` from a botched resolution. The per-session record now lives one-file-per-session in **[`docs/sessions/`](docs/sessions/)** (see its README), which cannot collide. For "what shipped last", read `ls docs/sessions/ | sort -r | head`; for "when was this file last true", read `git log -1 STATUS.md`._

---

## Current Phase

**Step 10 — AI Signal (`generate-signal`).** B-045 (3-step plan, no Step 4) shipped & merged:

| Step | What | Status |
|---|---|---|
| 1 | Deterministic detection engine (`detection.ts`) — case-crossover (B-050) + intake-decline flag | ✅ #72 (29 tests) |
| 2 | `generate-signal` Edge Function — detect → curate → Haiku-phrase → cache findings set (24h TTL); migration 015 (`findings jsonb`) | ✅ #74 (migration #73) |
| 3 | `SignalZone` wired to cached findings (per-type renderer, confidence tags, tap-to-expand, live/building/stale/`no_pattern`, async regen) | ✅ #75 |

**Architecture:** deterministic detection + LLM phrasing — the server computes & ranks a *true* finding in `detection.ts`; Haiku 4.5 only renders copy, with a deterministic template fallback. `phrasing.ts` = pure phrasing/curation/guardrails (offline unit-tested); `index.ts` = I/O shell. `validatePhrasing` rejects model drift (no `!`; never-reassure / never-"picky" on safety; associational-only on correlation).

**Signal engine — detectors live (deployed `generate-signal` v25):** ① case-crossover, ② intake-decline, ③ reflection (presence/counts; a *declining* trend routes to safety), ④ symptom-worsening lane (B-077), ⑤ post-prandial timing (B-078), ⑥ time-of-day clustering (B-079), ⑦ symptom-chronicity safety lane (B-182 — live v23, confirmed on real data 2026-07-01); + no-signal coverage diagnostics (B-053, incl. **B-070** ≥80%-dominance staple-washout + treats-vs-meals copy register, live v21); + **medication confounders** (B-117 PR 9 #207 — a drug on-board in a symptom window enters `detectCorrelations` as a confounder: case-enriched → suppress, concordant → cap at Early; **+ B-156 PR C1 #234 reads the dose↔vehicle pairing — a drug-in-food combo is attributed to the drug, the vehicle food's protein dropped from the exposure set, + the B-174 in-doubt-dose reconciliation; adversarial-PASS, **deployed v22** read-back-sha-verified + boot smoke-test**). Open follow-ups: **B-052** write-time protein normalization (read-time half shipped #92); **B-067** reflection/Trend dedup; **B-080** diet-structure placement (blocks descriptive Phase 3, PM call). **⑧ `incident_red_flag` (B-340) LIVE at v25** — engine (#395) + client renderer (#396) shipped, and `generate-signal` **redeployed to v25** (deploy gate lifted: #396 client on-device + `!inner` query smoke-tested). Ranked at the top of the safety band; reads `event_ai_analysis` via the repo's first `!inner` join, derives the flag from owner-editable structured fields (override-aware), escalate-on-presence/never-reassure. The authenticated end-to-end render is verified on-device (PM QA, pending).

**On-device QA gap:** empty / `no_pattern` + reflection paths verified on device; the **LIVE safety-card path is now verified** — B-182 chronicity fired on Nyx's real data (2026-07-01 QA), the first live safety card seen on device. The correlation/intake safety lanes still legitimately yield zero on Nyx's healthy data (chicken ~3×/day staple → case-crossover washes it out; intake healthy → flag quiet), so those specific lanes remain unexercised live.

**After Step 10:** Step 9 (vet report) resumes. **Design + spec updated this session (2026-07-02):** discovery (#214) → v1 mock (#216) → **`docs/nyx-vet-report-requirements.md` — now build-ready v2**, folding in the **v3 + cat design mocks** (`vet-report-mock-v3.html` calm diet-trial dog; `vet-report-mock-cat.html` safety-led cat; v1 `vet-report-mock.html` + v2 kept for comparison), a **9-lens synthetic panel review** (Dr. Chen cold-read = CLINIC-READY, fix list applied), and a **real-data dry-run against the pet Nyx** (23 real vomits → the report renders *safety-led*: chronicity + a possible-foreign photo; validated the design end-to-end + surfaced §7.1 data-layer reqs — empty weight state, owner/neuter schema gaps, AI-status denominators, de-dup, free-fed, 90-day fallback). New decisions: **present-only phenotype**, **photos split-by-type** under mandatory §8 privacy controls, **weight (B-186) + empty state**, **stool characteristics**, **medications & supplements**, **branding = keep letterhead / kill footer marketing**. **§12 PR plan REORDERED per PM: Phase 1 full functional report → Phase 2 publish+share+photos → Phase 3 seamless-sharing niceties.** Preconditions **ALL RESOLVED** (B-044 #218 / B-115 #219 / B-028 #220). **Real-vet gate reframed (PM): n=1-first** — PM emails the real Nyx report to their own GP + books a Nyx appointment, in parallel with the build; the first real-vet reaction lands **before Phase 2**. **§8.2 HTML-first RATIFIED (#260).** Phase 1: **PR 1** (`report.ts`) shipped #261, **PR 2** (`render.ts`) shipped #262, **PR 3** (clinical must-carry hardening + B-213 intake-decline duration + traceable meal appendix) shipped #263, **PR 4** (diet/weight/stool + WSAVA hardening — 7 findings fixed across cold-read round-2 CLINIC-READY + adversarial round-3; the GP-0 confound note now names standing/overlapping interventions, free-fed contamination surfaces in Appendix C + B under a trial, off-diet double-counts closed) shipped #264 — all pure, `deno test`-covered, adversarial + cold-read-gated. **Phase 1 (the full functional clinical report) is COMPLETE.** **Phase 2: PR 5** (owner-facing MVP — authenticated `generate-report/index.ts` I/O shell + in-app WebView + on-device PDF share) shipped **#265** (`rls-privacy-reviewer` PASS on the authenticated boundary; `code-reviewer` fix-before-merge all applied; `generate-report` deployed, NEW fn `verify_jwt=true`). **2026-07-03: the FIRST REAL on-device artifact landed + a four-agent panel review + feedback round 1 (bug fixes) shipped** — see the Last-updated entry above + `docs/research/2026-07-vet-report-first-artifact-review.md`. **Phase 2 shipped since (corrected 2026-07-06 — this section had drifted; git is the source of truth):** the §5 PM decision menu was ratified (#267) → **B-221 round-2 design changes** (#268) → **feedback round 2** (severity cut, appendix reorder, wet-meals surfaced, protein-over-time chart, jargon + design pass — #273); **B-222** owner-facing range control (Default + Custom) shipped **#276**; **PR 7 — ALL incident photos in the authenticated report + PDF** shipped **#278** — every photographed in-window incident renders into Appendix E and bakes into the HTML/on-device PDF, safety-flagged (blood/foreign) photos also lead the safety band; `index.ts` fetches each photo ONLY through the EXIF/GPS-stripping + downscaling Supabase image transform (never the raw original), embeds as a `data:` URI (no signed URL persisted), service-role reads only paths from the RLS-scoped enumeration of the verified owner's pet; owner *visibility* = `photo_count` → `app/report.tsx` "Includes N photos". Gates: **`rls-privacy-reviewer` PASS**, **`vet-report-cold-read` CLINIC-READY** (fixed 2 bugs it caught — Appendix-E analysis↔photo divergence disclosed; chronicity `daysSinceLastEpisode` UTC→local, B-246), **`adversarial-reviewer` PASS**; no schema. **Deployed live as `generate-report` v8** (#281, byte-verified; folds in the B-222 zoom fix). Follow-on hardening from PR 7's rls-privacy pass: **B-244** (bind `storage_path` to pet + owner-scope the bucket read) shipped **#282** (CHECK applied to prod 2026-07-05) and **B-218** (drop the `vet_reports_public_share` anon-enumeration policy) shipped **#283**. **Phase 1 + the owner-facing MVP + authenticated photos are COMPLETE and LIVE.** **Genuinely next:** the PM **real-vet R1 email** (emails the regenerated Nyx report to their GP + books the appointment — a PM action; gates the irreversible public path); **B-243** — the interactive **"tap to exclude any photo"** pre-send review (PR 7 shipped owner *visibility* and deferred the interactive deselect; additive, no schema); then **PR 6** — the public token `view-report` route + revocable everyday-photo delivery (split-by-type / token-streaming) + owner link-revocation (B-143), the first unauthenticated path (`rls-privacy-reviewer` MANDATORY; its **B-218 blocker is already cleared** — #283). See `nyx-vet-report-requirements.md` v2.1 (2026-07-04) + §12.

---

## Parallel Tracks

### Password recovery (B-280) — spec v1.2; **PR 1 shipped**; PR 2 gated only on the §9.3 device checks
Spec `docs/nyx-password-recovery-requirements.md` **v1.2** + design-locked mocks `docs/culprit-password-recovery-mockups.html`. Closes the app's sharpest returning-owner dead end and a submission gap (hardening audit §B2). Flow: login link → request → neutral Sent → same-phone deep link → set password → **in** (no credential re-entry).

| PR | What | Status |
|---|---|---|
| 1 | Foundations — `flowType: 'pkce'`, pure `lib/passwordRecovery.ts` (two-stage FR-4 classification), **persisted** recovery gate, request marker, FR-17 log redaction, flag off | ✅ **shipped via #444** (79 new tests; no user-visible change) |
| 2 | The whole flow (FR-1→FR-20) — deliberately not split further; a request without a handler ships a dead end, which is the defect this track removes | ⬜ **all rulings landed**; gated only on the §9.3 device checks. `rls-privacy-reviewer` is a **merge gate** |
| 3 | Settings → change password + the FR-19 eviction option | ⬜ **D4 ruled** — buildable after PR 1 |
| 4 | Enablement — flip `PASSWORD_RECOVERY_ENABLED`, on-device end-to-end | 🔓 **SMTP gate LIFTED** (#445 — production SMTP verified by a live send). Now gated on **PR 2 existing** (§8's sequence needs a real reset sent end-to-end before the flip) + three §9.2 dashboard items: the `nyx:///reset-password` **redirect allowlist** (without it every link dead-ends), enumeration protection, and the recovery email template that still says "Nyx" |

**Prerequisite closed — B-428 (PR #488):** the password reveal toggle is on the shared `components/ui/TextField` primitive, so PR 2's reset screen and PR 3's change-password screen inherit it (plus the re-mask-on-background behaviour) by passing `secureTextEntry` — do **not** hand-roll a masked input, and don't add a confirm field without revisiting B-428's per-screen tap-cost reasoning.

**PM rulings 2026-07-25 — all three landed. D1c ACCEPTED** (custom scheme ships; https interstitial → B-426, post-submission) · **D4 YES** + **email change scoped in as D9** (spec'd, build slot open → PR 5; corrected a wrong v1 claim — the vet report reads `display_name` first, the account email only as a *fallback*) · **D6b YES — the best-practice session workflow is adopted and scoped (spec §7.2).** App Review has no guideline here and can't test it, so it was a product/security call; but the standard control terminates other sessions on reset, and the original "no" rec never checked it. Now: **reset evicts unconditionally** (after a successful write, never before); **settings change offers it**, default off. Detection needs no new code — a revoked refresh token is already a non-retryable failure → `SIGNED_OUT` → the shipped teardown. **The constraint that shaped it: the evicted device cannot know *why* it was signed out** (revocation is indistinguishable from deletion or any other non-retryable failure), so the banner names the likely cause without asserting it — the same trap as the original "That link has expired". Involuntary-vs-deliberate reuses B-039's one-shot banner shape. Residual (a device offline across the whole access-token window loses unsynced rows) → **B-430**.

**The finding worth carrying to other tracks:** `wipeLocalSession()` was written for the `SIGNED_OUT` transition, where the session goes **null** and every session-keyed producer unmounts. A recovery swap is non-null → non-null, so `useWidgetSnapshots`/`useSync` **re-arm** and re-publish the previous owner's pets to the App Group + Home Screen widget *after* the wipe. Fix: force a real `signOut()` before the exchange rather than maintaining a second teardown. **When a new flow reuses an existing teardown, check the transition *shape* it was built for, not just what it clears.** Files B-426/419/420/421; annotates B-401 (pkce arms a landmine on the B-152 flip).

### ⭐ Ask (B-228) — THE NEXT MAIN PROJECT (PM 2026-07-18) — server chain A1→A4 shipped; A6 rundown shipped; A5 (client) next
Spec `docs/nyx-ask-requirements.md` **v2.2** (build-ready — **no PM blockers**: D2 ratified expanded, D7 = "Ask" placeholder by choice, **D8/D9 conversation-grain fixes 2026-07-18**) + design-locked mock `docs/ask-mockups.html` + evidence `docs/research/2026-07-ask-ai-ux-landscape.md`. Owner-initiated **answer-card** Q&A over the pet's own record (not a chatbot): Sonnet plans over a **closed, read-only tool set** → deterministic compute (floors + denominators, `deleted_at` contract, G5 Timeline-parity — extended to reads) → validated phrasing (`validateAnswer`: never reassure/diagnose, numbers-⊆-tool-results) → typed 200s. Escalation is **relay-only** (leads the answer when the engine fired; never minted). **Boundary (D2 ratified):** the full record scoped to the question — notes + photos in, via **scoped retrieval** (no bulk tool) + **transform-only** photo access + the **one-read-path** (live reads reuse `analyze-vomit` + its cap). Flags: `ask_enabled`/`ask_general_enabled` via the new zero-schema **allowlist convention** (PM-only dogfood first); caps ride shipped `ai_usage` (`ask_conversation` monthly / `ask_message` daily); born-Premium wiring at Track-3.

| PR | What | Status |
|---|---|---|
| A1 | Config-seed migration (2 flag keys, allowlist shape; own PR) | ✅ shipped via #398 (draft) — applied live |
| A2 | Allowlist flag primitive (client decode + server resolve; bool back-compat) | ✅ shipped via #401 (draft) — `code-reviewer` ship-ready; deno tests un-run (no deno in env) |
| A3 | `ask/tools.ts` deterministic tool layer (D2 whitelist enforced in types) | ✅ shipped via #402 (draft) — code-reviewer + adversarial-reviewer both run/addressed; 40 deno tests |
| A4 | `ask` Edge Function (plan-loop + validator + deflections + caps) — **adversarial + rls-privacy MANDATORY** | ✅ code shipped via this PR (draft) — both gates PASS; 102 deno tests; **DEPLOY pending** (verified 69 KB bundle, new fn `verify_jwt=true`, ship-dark) |
| A5 | Client surface (`app/ask.tsx`, header pill, answer-card renderer) | ⬜ |
| A6 | Vet-visit rundown (deterministic, client-only, works capped/offline) — **parallel-safe with A3/A4** | ✅ shipped via #406 (draft) — `lib/rundown.ts` + `app/rundown.tsx` (`/rundown`) + `RundownTileRow`; clinical-guardrails/pm-feature-review/code-reviewer all run/addressed; 36 tests; jest 1442. Entry lands with A5 (S5). |
| A7 | Copy/safety hardening + the general-mode flip-on review package | ⬜ |
| A8 | Live photo reads via the `analyze-vomit` machinery (transform-only; shared cap; clinical-guardrails) — adversarial + rls-privacy MANDATORY | ✅ shipped via #409 (draft) — both gates PASS (adversarial: 4 rounds to CONFIRMED-CLOSED; rls-privacy: PASS, residual closed); deno 129 (ask+shared) + 89 vomit/stool regression unmodified; jest 1491. **DEPLOY-GATED:** redeploy `analyze-vomit` + `analyze-stool` FIRST (honor `transform_only`, default-off = behavior-neutral), THEN `ask`. |

### Vet Files (B-478) — **VF-0 → VF-6 SHIPPED**; the track is complete. The four mandatory reviews have now run, and their fallout is filed
**G4 — treated as PROMOTED by the VF-2 build instruction (2026-07-26), pending an explicit PM word.** The spec and this file both still read `Later`; the build was requested directly, which is the only channel a promotion has. Flagged rather than assumed silently — if G4 was *not* meant to open, VF-2 is merged-but-dark (see the entry gate below) and costs nothing to leave parked.
Spec **`docs/nyx-vet-files-requirements.md`** v1.0 (all decisions closed 2026-07-26: G1–G3 + D11–D14) + design-locked mock `docs/culprit-vet-files-mockups.html` (round 2.1). Central per-pet vet records library (email screenshots, lab PDFs, vaccine certs); v1 = store/browse/share, **Phase-2 AI over the corpus is D8-gated** (D2-class PM+T&S ruling, Ask §6 mirror). Ruled: new `vet_documents` table + `nyx-vet-documents` bucket · PDFs in v1 store-and-view · pet-profile entry · chips out / search out (B-479) / multi-pet via duplicate-on-add / report paperclip out (B-480, NOT D8-gated). Carries **D7**: an uploaded document never mints/re-dates a `vet_visits` row (protects the report window). Persona reviews (Jordan+Sam, 2 rounds) in `docs/sessions/2026-07-26-vet-files-discovery-spec.md`. Spec shipped via #463.

| PR | What | Status |
|---|---|---|
| VF-0 | B-248 + B-466 hardening — owner-scope `nyx-vet-attachments`, `storage_path` CHECK on `vet_visit_attachments` (migration 043) | ✅ #466, applied |
| VF-1 | **The substrate** — migrations 044 + 045 (`vet_documents`, owner-scoped `nyx-vet-documents` policies, the same-pet/immutability trigger); local mirror, push sync, hydration, wipe; `delete-account` purge coverage | ✅ **this session** (both applied live; `delete-account` deployed v6) |
| VF-2 | **The library** — `app/vet-files.tsx` (L-real rows, E1-r2 empty state, kind `ScopeMenu`), the profile Records card (A1-r2 + A1z), the D11 recovery sheets (Name / Add type), the short-lived signed-URL read path | ✅ **this session** (32 new tests; jest 2674) |
| VF-3 | **Capture** — D1-r2 add sheet (camera / Photos multi-select / Files PDF), instant save with defaults, D2-r2 saved moment (offline line, "Name it", D13 also-add), `document_group_id` grouping, `VET_FILES_ENTRY_ENABLED` flipped | ✅ **this session** (43 new tests; jest 2747) |
| VF-4 | **Detail** — `app/vet-document/[id].tsx` (hero + PhotoViewer page swipe, PDF WebView, metadata card, conditional visit-link row w/ the D7 invariant test, Share, ⋯ Rename/Delete), soft delete + the 30-day Recently deleted sheet, and the AC-12 cache-on-open | ✅ **this session** (44 new tests; jest 2791) |
| VF-6 | **The finish pass** — `nyx-voice` over every string, the three owed `rls-privacy-reviewer` passes, `pm-feature-review`, §8 AC verification, on-device QA script | ✅ **this session** (8 new tests; jest 2850) |

**VF-1 notes that bind VF-2/VF-3.** The PM's dashboard bucket action is **done** (private, 15 MB, jpeg/png/heic/pdf). Object keys are `{pet_id}/{document_id}.{ext}` and come **only** from `buildVetDocumentPath` (`lib/vetDocuments.ts`) — the pet prefix is enforced three ways (table CHECK, Storage policies, `scopeVetDocumentPaths` in `delete-account`, which validates the **whole shape**, not the leading segment). **VF-3 must call `resolveVetDocumentMime` before building the row**, because every image is re-encoded to JPEG and the row's `mime_type` drives the viewer branch. **`prepareVetDocumentUpload` deliberately THROWS instead of falling back to the original on a failed re-encode** (§6.2 no-original-fallback) — a failed upload leaves `synced = 0` and retries. Table grants **SELECT/INSERT/UPDATE only**: a delete is an UPDATE setting `deleted_at`, verified live (owner hard-DELETE affects 0 rows). **`storage_path` is immutable server-side** (045) — relocating a document is a new row + new object, never an UPDATE. Signed-URL read path is still VF-2's.

**VF-2 notes that bind VF-4.** `VET_FILES_ENTRY_ENABLED` (`lib/vetFilesEntry.ts`) is now **`true`** — flipped as VF-3's last change, so the profile Records card renders and `/vet-files` is reachable the way an owner reaches it. It stays a build-time constant, not an `app_config` flag (a flag would need its own migration to answer a question settled by which PRs have merged). One `pendingScreen()` no-op remains, the detail route — grep it in VF-4. Three things carry into the next PRs: **(1)** the list groups by `document_group_id` and `MIN(page_index)` is load-bearing, not decoration — it pins SQLite's bare-column rule to the cover row so a 4-page thread can't render page 3's thumbnail beside page 1's title; **(2)** `local_uri` always wins over a signed URL when resolving a thumbnail, which is the free half of **AC 12** (a document captured on this device reads with no network) — and the honest-failure half is that the tile *rests on its glyph* and **never spins**; **(3)** signed URLs are **15 min**, not the Foods tab's 24 h — a signed URL is a bearer token and these are clinical records, so nothing long-lived leaks into a log or a crash report, and nothing is persisted. **AC-12 caching feasibility pass (owed by VF-2, §8): recommend persisting the bytes on first successful full-size open in VF-4** to the durable `persistCapture` path and setting `local_uri` — no new column, no new cache layer, and the list thumbnail starts rendering offline as a side effect.

**VF-3 notes that bind VF-4/VF-6.** Capture is `lib/vetDocumentCapture.ts` (pure: screen → build → copy strings) + `app/vet-files.tsx` (the pickers and the alerts); the sheet chrome moved to `components/vetfiles/SheetShell.tsx` on its third caller. Five things bind the next PRs. **(1) A NEW NATIVE MODULE shipped — `expo-document-picker` (~57.0.1)**, the only way to pick a PDF; no config plugin is needed (its iOS plugin only fires on `ios.usesIcloudStorage`, which stays off — a user-selected file needs no iCloud entitlement). **Neither existing binary contains it** — not TestFlight build 35, and not the custom dev client (Expo Go, which carries the whole SDK, is retired for SDK 57). So the module is **required lazily inside `pickPdfs`**, not imported at the top of the file: its entry point calls `requireNativeModule()` at IMPORT time and throws, which on a static import would take down the whole Vet Files **route** on the binary people are testing with rather than one row. As shipped, camera and Photos work on today's builds and the Files row shows *"PDFs need an app update"* until a fresh native build exists. Do not "tidy" that require to a static import without cutting the build first. **(2) Grouping is decided at the source, not by the owner**: camera and Photos produce ONE document with N pages; **Files produces one document PER PDF**, because two portal PDFs are two records and fusing them would make Phase-2 attribution wrong invisibly. **(3) The camera's multi-page path is a deliberate addition to the mock** — `expo-image-picker` shoots once per launch, so the saved moment carries "Add another page" (appends to the same `document_group_id`, inherits its `document_date`, never starts a second document). The alternative was asking "another page?" *before* the save the sheet promises is instant. Worth a PM/Designer word at VF-6. **(4) The 15 MB bucket limit is mirrored client-side** (`VET_DOCUMENT_MAX_BYTES`) and enforced on **PDFs only** — an image is re-encoded before upload, so its picked size says nothing about what lands; an oversize PDF would otherwise sit `synced = 0` retrying forever with nobody told. **(5) D13's copy is independent at every layer** — new row ids, a new group id per group, the other pet's key prefix, its own durable local file, and **`vet_visit_id` dropped** (the server's same-pet trigger rejects an inherited link).

**VF-4 notes that bind VF-6.** The last `pendingScreen()` is gone; `/vet-document/[id]` is keyed on the **document group**, not the cover row's id. Six things carry forward. **(1) D7 is now a regression test, not a comment** — `lib/vetDocumentDetail.test.ts` runs every write this feature can make (rename / kind / notes / date / link / adopt-local-uri / soft-delete / restore) against a real `node:sqlite` and compares `vet_visits` byte-for-byte before and after, including the case where the linked visit id matches no row (the local mirror declares no FK, so "never mints" has to be *checked*, not inferred from a constraint). A future setter that re-dates a visit to match its document fails there rather than in a vet report six weeks later. **(2) `updateVetDocumentGroup`'s column list is CLOSED** — `title | kind | notes | document_date | vet_visit_id`. `storage_path` is deliberately absent because 045 makes it immutable server-side: a local UPDATE would succeed on the phone and be rejected forever on push, wedging the row at `synced = 0`. **(3) AC 12 is closed, by the shape VF-2 recommended** — the first full-size *open* of a page downloads it (`persistRemoteObject` → cache → `persistCapture`) and adopts the durable path as `local_uri`, so the document and its library thumbnail both read offline afterwards. The adoption write deliberately **does not touch `updated_at` or `synced`**: `local_uri` is local-only, and bumping `updated_at` would mean merely *looking* at a document on one device beats a real rename made on another under LWW. Caching runs alongside the viewer, never in front of it, and every failure path returns null silently. **(4) The 30-day promise now has a surface behind it** — the ⋯ menu's "Kept for 30 days" is backed by the Recently deleted sheet on the library, whose entry point renders **only when something is in it** and sits **outside** the empty/populated branch (deleting your only document lands you on the empty state, which is exactly when the undo has to still be reachable). The countdown is counted in **calendar days**, so it reads "30 days left" an hour after the delete rather than an off-by-one-looking 29, and the final day says "Last day to restore" rather than "0 days left" next to a working button. **(5) The PDF viewer is a WebView, on purpose** — WKWebView renders PDFs natively, so this needs no new native module and is testable on today's binaries (unlike VF-3's `expo-document-picker`). It latches its URI at open so the AC-12 download can't tear down a rendered document mid-read, and `javaScriptEnabled={false}` because this is third-party clinical HTML-adjacent content from an unknown PIMS. **(6) Share stages a named copy first** (`stageForShare`, extracted from `shareReportPdf`'s inline version on its second caller) — `Sharing.shareAsync` hands over the file at its path and our paths are UUIDs, so without it the vet receives `a3f9c1e2-….pdf`. **One deviation from the mock, deliberate:** the mock's nav shows a text back-label "‹ Vet Files"; the app's shared `Header` takes an icon back plus a centred title, so this ships as `‹` + "Vet Files". Worth a Designer word at VF-6.

**All three owed `rls-privacy-reviewer` passes RAN in VF-6 (2026-07-27) — VF-2 PASS-with-findings, VF-3 FAIL, VF-4 FAIL.** Every *access-control* boundary held under executed attack (the VF-3 reviewer replayed migrations 044+045 onto a live Postgres and ran 21 hostile writes; cross-tenant row/object insert, path-prefix bypass, upsert-hijack, inherited `vet_visit_id` and `document_group_id` cross-account, `storage_path` re-point, Storage `move()` into another prefix and hard delete all denied). **What broke was retention, egress and one silent data loss, and all of it is fixed in VF-6:** the Share handed the vet the **un-stripped ORIGINAL** (the EXIF/GPS strip lived only inside `prepareVetDocumentUpload`, i.e. only on the path to Storage — "GPS never travels" was true of the bucket and false of the share sheet); a **soft delete during an in-flight push was lost permanently** (`synced = 1` set unconditionally by id after an upload that can take tens of seconds, so the delete was marked pushed while the stale snapshot landed — and nothing could correct it, since the row read synced, LWW kept the newer local `updated_at`, and `softDeleteVetDocument` is guarded on `deleted_at IS NULL`); **staged share copies and download temps were unwipeable** (the sign-out file wipe is row-driven and both writers produce files no row names — they survived sign-out AND account deletion); and the **PDF WebView leaked its signed URL via `Referer`** (`originWhitelist={['*']}`, no navigation handler). Plus `cacheControl: '0'` on every upload, which §6.2 required and nothing implemented. **One finding did NOT fit VF-6 and is filed: B-545** (filed as B-529, renumbered — #491 took that block on `main` first) — a terminally-rejected upload retries forever with no owner-visible state and 20 of them wedge the queue; it needs a `sync_error` column, so it is a schema PR. **VF-1's pass is closed** — FAIL on the first cut, all six findings fixed in #479 (migration 045); the two that generalise past this feature are filed as **B-519** (local-file wipe fails open) and **B-520** (023/041 triggers are RLS-blind), and the full write-up is in `docs/sessions/2026-07-26-vet-files-vf1-substrate.md`.

### Home Screen Widget (B-289/B-290/B-291) — W1–W5 built; **W6 cut as build 35 and the widget is UNUSABLE on-device (B-481)**
Spec **`docs/nyx-widget-requirements.md`** v1.0 (D1–D9 ratified) + design-locked mock `docs/culprit-widget-mockups.html`. Jobs: top-of-mind + when-in-doubt-app-it-out. v1 scope = meals+treats, free, no AI/med capture. Plan: **W1 ✅** `logged_via` migration (038, #422, applied live) → **W2 ✅** SDK 57 upgrade (#423; **on-device regression = open PM QA**) → **W3 ✅** B-290 App-Group write path (#425; rls-privacy PASS, spike answers in the PR) → **W4 ✅ shipped via #426 (draft)** — App Intents (`logMealIntent`/`logTreatIntent`/`topUpBowlIntent`, inbox-first + owner-JWT REST leg) + the pure resolution lib (learned slots — no declared slot model exists, B-040 is free-choice-only; slot→named-food w/ stability floors; trial override; treat shortlist; trial day) + the read-only extension session reader (`lib/widgetSession.ts` over `secureStoreTiers.ts`) + `bowl_topup` (arrangement re-attest, never intake). **D5 SETTLED (Dir.-of-Eng recommend-and-proceed): option (a)** — build-time "Pet slot" enum (6) resolved through the app-published sticky/tombstoned `pets-index.json` (B-086 anti-silent-switch); option (b) = the named-picker upgrade path if slot labels confuse at W6 → **W5 ✅ shipped via #428 (draft)** → **W6** TestFlight cut. Parallel to B-288 and the Ask track. Note: the first dev-client/TestFlight build from post-W3 `main` picks up the App Group entitlement — verify app + (later) extension resolve the same container on-device.

**W5 — the architecture finding that reshaped §4 (needs a PM read).** `expo-widgets` does not run app JS in the extension: a layout is a **single self-contained function**, stringified by babel's `'widget'` directive and evaluated in a bare JavaScriptCore context (only `@expo/ui/swift-ui` + modifiers + a JSX shim — no imports, no filesystem, no network). Two consequences: **(1)** the picker flip is entirely **extension-local** — the interaction intent re-evaluates the layout, merges the returned props patch into the stored timeline entry, and reloads; the app is not in the loop (§4.1 Q2 answered from the module's own source; the *latency measurement* is still the on-device gate). **(2)** W4's App Intents **cannot execute in the extension**, so a tap is captured into an **outbox** in the widget's own props and the app replays it through those same shipped intents (`lib/widgetBridge.ts`) carrying the widget's tap-time ids — the id-keyed idempotency and honest `occurred_at` survive the hop. **Product consequence to weigh:** a widget capture is durable at tap time but becomes a *row* when the app next runs (instantly if it's alive — the interaction event drives a drain; otherwise on next foreground). §5.1's "no app launch" holds for the *tap*; the record's freshness now depends on the app running, which is the same shape as the offline path (§5.6) but is a §4 wording change worth ratifying. Undo is **state-based, not a 60s timer** (a widget gets no guaranteed timed re-render; a "tap to undo" that silently stopped working would be worse) and is honored on both sides of the drain — post-drain it soft-deletes.

### B-351 Multi-protein capture — **Phase A + Phase B COMPLETE** (slices 0–6 shipped); the track's remaining work is deploy + dogfood
Spec `docs/nyx-multi-protein-requirements.md` **v1.1** (D1–D8 ratified 2026-07-19; D3a/D9/D10 added 2026-07-24) + mocks `docs/nyx-multi-protein-mockups.html`.

| Slice | What | Status |
|---|---|---|
| 0 | B-414 Class-A convergence fix + cross-product convergence property test (~5.4k variants) | ✅ #433 |
| 1 | Schema — `food_items.proteins TEXT[]` (migration 039, **applied live**) + cache mirror + `refreshFoodCache` | ✅ #424 |
| 2 | Extraction — ordered `proteins` set; `primary_protein` = derived `proteins[0]` (model primary hoisted); B-048 absorbed as a write-path normalizer. **Deployed v15** | ✅ #429 |
| 3 | Manual capture — the two-line D8 picker (auto-demote, never-in-both, D9 typed escape, the §11 seed rule); both screens write both columns | ✅ shipped via #446 |
| 4 | Tier-1 disclosure + the Tier-2 trial-contaminant flag (add-time soft confirm · log-time completion card · trial-card note); the D10 completeness gate | ✅ shipped via #447 |
| 5 | Vet-report render — the full set per food, primary emphasised, off-trial `*`, provenance line, set-membership tally/chart, all gated on D10 | ✅ shipped via #448 |
| 6 | **Phase B** — set-membership correlation + collinearity clustering + the Home linked pair | ✅ **shipped via #458** (`adversarial-reviewer` run; deploy-gated — see below) |

**Slice 3 needs a PM read on two things** (detail: `docs/sessions/2026-07-25-b351-d8-protein-picker.md`). **(1) `primary_protein === proteins[0]` now has one deliberate exception** — a *cleared* main writes NULL alongside a non-empty `proteins`, because writing the demoted `proteins[0]` republished the designation the owner had just cleared and the reseed put it straight back. Honest, but it deviates from D4 / migration 039 and wants a spec nod. **Slice 6 closed the consequence half of this** — `detection.ts` and `analytics.ts` now read the set through `readProteinSet`, which skips a null primary and keeps the demoted tail, so a cleared-main food is no longer protein-unknown to the detector (verified: `readProteinSet(['duck','chicken'], null)` → `['duck','chicken']`). What still wants the nod is the *spec* deviation itself, and the standing rule that no reader may assume a non-null head. **(2) → B-436** — a protein typed into "Also contains" and never blurred before Save may be dropped entirely (local draft state, emits only on commit); fix direction is a save-time commit. On-device QA carries the step meanwhile.

**Slice 5 — the read path had the wrong keying function, and the reviews caught it** (detail: `docs/sessions/2026-07-25-vet-report-protein-render.md`). The first implementation derived the report's set with `deriveProteinSet`, which is a **write-path** function applying D3a's Class-B rules, while the trial target resolved through `canonicalizeProtein` (Class A) — so the two sides of the off-trial comparison were keyed differently and a protein could fail to match **itself**. An `ocean whitefish` trial food (spec §11 records **3 such rows live**) rendered *"The trial food's own label also lists Whitefish"* in bold on page 1; same for `Buffalo`, `Deer`, `Deboned Chicken`, `Chicken Liver`, `Egg Whites`. `lib/protein.ts:202` had banned exactly this in writing, and it re-created the client/report split that `lib/trialProtein.ts` was extracted *in the same session* to prevent. Fixed by **`readProteinSet`** — the same hoist+dedupe shape keyed Class-A only. The rule to carry into slice 6: **a read path uses one keying function**; semantic re-keying of stored rows is B-416's job, on the write path. **The `vet-report-cold-read` returned NOT READY on the first artifact**, and its best finding was not a bug: page 1 told the *wrong story about the case* — reading it alone, Dr. Chen concluded "contaminated trial food, re-run", when a free-fed chicken/turkey bowl meant the elimination diet **was never run**, three pages away. That ad-lib exposure now sits on the trial line, and Appendix E (the table a vet checks for what the patient actually ate) no longer renders the contaminated trial food as clean `duck`. **Deferred, filed not fixed: B-488** (a stale `ai_extraction_confidence` can manufacture "nothing else on the label" after an owner edit — unsafe direction, mirror of B-437; the only available proxy over-triggers, so the precise fix is on the write path), plus **B-489/B-490** (the cold read's two pre-existing blockers) and **B-444/B-445**. Second cold read fixed three of its own loose ends on page 1 (the chart contradicted the diet line about a standing exposure; the promoted claim left its provenance in appendix B; `(duck)` contradicted "also lists Chicken") and took its top clinical risk — **the headline now qualifies "day N of M"** when off-trial protein reached the pet, because a top-down scan that stopped early yielded "scope her" where the honest reading was "this was never an elimination trial". Also filed: **B-446/B-447/B-448**, and **B-449** — merging `main` showed the report's `daysElapsed` is a **fifth** day-counter implementation sitting outside B-421's brand-new one-counter guard, whose consumer list is client-only.

**Slice 4 needs a PM read on one thing** (detail: `docs/sessions/2026-07-25-b351-slice-4-disclosure-and-contaminant.md`). The add-time soft confirm says **"trial diet"** where the PM-reviewed mock said **"elimination trial"** — `diet_trials` carries no indication column (D6 deferred it), so the app cannot know a trial's *type*, and asserting one is a fabricated clinical detail on copy an owner may repeat to their vet. Ratify the substitution, or add the column and say the specific thing. Two rulings from elsewhere bound the design and are worth knowing here: **B-417's C2** (the trial diet's own contamination is a trial-level standing fact, never a per-feeding verdict — so the trial food is excluded from the completion-card path by construction, and a non-trial food flags only the *first* time it is fed inside the trial window), and **D10** (presence-only — an empty or unread protein set yields silence, never an all-clear; there is no negative form of any string in the module). Slice 3's flagged hazard is handled: the trial target reads the owner-designated `primary_protein`, never `proteins[0]`, because a cleared main writes NULL there and reading the demoted head would **invert** the check.

**The `adversarial-reviewer` FAILED slice 4 and was right to** — six breaks, every one in the *assembly* layer or in an interaction between two individually-correct rules, while the pure predicate + copy layer withstood all sixteen attacks it named. The two that mattered most: **(1)** rule 3 was implemented as "first MEAL of this food in the window", so one offline log — or the normal vet-directed setup where `started_at` is back-dated behind meals already fed — **permanently muted that food for the whole trial**; it now counts heads-ups *given*, via a small persisted per-trial ledger, which also deleted the module's only SQL. **(2)** the library row could render **"No animal proteins on the label"** over a chicken-and-salmon food, because `proteins` is *optional* in the extractor's tool schema while `confidence.proteins` is *required* — a partial tool call yields an empty set with a high-confidence panel. The string is deleted; `mayClaimCompleteProteinSet` now states the rule for slice 5 to inherit. Also fixed: a soft confirm that **gated the meal write** on `?fromLog=1` and silently discarded the meal on "Not now" (Principle 1); a stale unresolved context memoized for 5 minutes; C2's exclusion missing a re-photographed bag of the trial diet; and an unknown target disabling every check with nothing saying so. **B-440 accepted, not fixed:** the trial target is an ungated AI read, so a wrong `primary_protein` inverts the advice — gating on the confidence field would disable the feature for the majority (manual foods, null confidence), so slice 4 discloses the assumption on the trial card instead and the real fix belongs in B-417 PR 3's start-a-trial modal. **Still INSUFFICIENT:** `loadTrialProteinContext`'s assembly has no automated test *and* no runnable on-device path, because **no `diet_trials` write path exists anywhere in the app** — Tier 2 end-to-end needs a hand-inserted row until B-417 PR 1 lands.

**The `adversarial-reviewer` failed slice 3 twice** before it passed — five defects, all at the `ProteinPicker`↔`ProteinSetPicker` seam, the worst writing fabricated keys (`["bison","biso","bis","bi","b"]`) into the correlation column from one typed word, and one re-opening B-412 *inside a single row*. Its sharpest finding was that all 85 cases were green before **and** after the fixes: nothing exercised the typed escape *inside* the composite. Now locked by a **property test** (no interaction sequence may invent a key; a no-op is a save→reseed fixpoint), mutation-verified. Same example-vs-property gap as B-414 — worth carrying into slices 4/5, which inherit this picker.
**Slice 6 — Phase B, the correlation half (#458).** Every feeding now contributes its **whole** protein set to the case-crossover, so the hidden secondary that B-416 just surfaced in 34 live rows finally reaches the engine — a `Tiki Cat after DARK Rabbit & Chicken Liver` bowl is a chicken exposure, not clean rabbit. Free-fed bowls exclude **every** protein they carry; a medication vehicle drops its **whole** set; a row with no captured set degrades to `[primary_protein]`, proved byte-identical by test rather than asserted. Attribution stays honest via **collinearity clustering**: proteins whose exposure indicator vectors over a symptom's matched set are *exactly identical* become one joint candidate, named together and credited to none. **That answers §13's open "100% or a fraction?" with exact identity** — every statistic ① computes is a pure function of that vector, so identical vectors are bit-identically untestable and no tolerance parameter is defensible; a fraction would merge proteins the data *can* separate. It self-resolves with nothing to tune: one window where the diet varies splits the cluster, which is exactly the action the card asks for. Family sizing follows for free — a 4-protein bag costs **one** comparison, not four — while never-exposed (zero-vector) proteins deliberately stay singletons, because merging *those* would shrink the family and loosen `correctedAlpha` for unrelated findings (the B-117 PR 9 tier-inflation direction). A cluster inherits its **weakest** member's attribution, so it can never be certified Established on the clean member's behalf. `CorrelationFinding.protein` became the joint **label** ("chicken and duck") rather than a representative member — deliberate safe degradation: the worst an un-updated reader can do is name both without the caveat, where a representative would have silently credited one and **exonerated its twin by omission**. Joint findings are **template-only** (`validatePhrasing` can catch a causal verb; it cannot notice that a paraphrase dropped a member). The vet report's lead line now states the association *cannot be attributed to either protein individually*. **Behaviour change the spec did not anticipate → B-468:** the guardrail also fires for two always-co-fed *single-protein* foods (the 9am wet + 10am treat routine), so the pre-existing multi-implication fixture flipped from two cards to one joint candidate — defensible on the merits, but §7 #6 predicted a *rare* card and a fixed feeding routine is not rare; watch the frequency in dogfood. **Left out on purpose → B-467:** two further descriptive protein rankings (`ask/tools.ts` `topProteins`, `summary.ts` `topMealProtein`) still key on `primary_protein` alone and now under-count exposure; `topMealProtein` feeds a clinically-reviewed AI-summary clause whose own header forbids silent alignment, so it needs a nod rather than a mechanical edit. **The `adversarial-reviewer` FAILED it, six times, and every finding was a reproduced program output** (detail in the session record). Highest severity: **the joint card told diet-trial owners to break their trial** — `detectCorrelations` had no `dietTrialActive` gate while `priorityBand` *promotes* correlations to the lead slot for trial pets, so "feed one without the other" led Home for exactly the population §7 #6 says joint candidates are most likely for; fixed with an engine-resolved `jointGuidance` that swaps the action for a route to the vet without ever suppressing the finding. Then **two paths that promoted an *unrelated* finding Early → Established** with byte-identical statistics (a collinearity merge shrinking the family 4→3; a free-fed bowl declaring a second protein, which is sharper because the family is shared across symptom lanes while the `standingConfounder` cap is not) — fixed with a family **floor**: never smaller than a primary-only engine would have made it, which keeps §7 #2 (a 4-protein bag still costs one comparison) while making it impossible for photographing one bag to strengthen a claim about another protein. **Provisional — wants a PM/Data nod.** Also: **joint candidates could reach `established`**, contradicting §7 #4 (collinearity now caps at Early, so a joint candidate never reaches the vet report); **staple-washout named one of several tied staples**, implying the unnamed one was assessed and cleared; the attribution floor ignored the control arm; and **the "it self-resolves" argument was false as stated** — 1:1 matching means most days never enter the matched set, so an owner can feed the proteins apart and see nothing (copy softened to "would *start* to separate them"; → **B-469**, real fix is B-049's 1:M matching). Note this did *not* weaken the exact-identity decision, which rests on the test statistic being a pure function of the vector. New residual **B-470**: set membership is binary so protein *dose* is invisible, and slice 6 makes that configuration the norm (adding chicken to the novel-protein bag is what raises staple dose on trial days) — direction of error is toward the novel protein. All six fixes **mutation-verified**.

**DEPLOY — MERGED, NOT YET DEPLOYED (the one open item from this session).** #458 is on `main`, so the gate is lifted and the redeploy is now *required*: the engine and its client renderer are both in `main` while production still runs **v25**, which predates set membership. Nothing is broken meanwhile — v25 keeps emitting single-protein findings and the new client renders them exactly as before (the `proteins`/`jointGuidance` fields are optional on the client mirror precisely for this) — but **no multi-protein exposure reaches any owner until `generate-signal` is redeployed.** The bundle is built and verified: `scripts/deploy-edge.sh generate-signal` → `.edge-build/generate-signal/index.ts`, **sha256 `b66d5a58…`**, 108,268 bytes, syntax-checked, all slice-6 symbols present, `verify_jwt` stays `true`.

**Why it did not ship in-session, and what to change.** The MCP `deploy_edge_function` tool takes the bundle as **inline text**, so the only path is for the agent to carry all 108 KB through its own context and re-emit it verbatim. That is fine for a small function and genuinely unsafe for this one: at 2,457 lines it does not fit in a single contiguous read, so it arrives across six tool results, and a silent truncation during re-emission would deploy a broken clinical Edge Function. The failure *is* detectable (the runbook's sha read-back + boot smoke test) and recoverable, but the honest reading is that a 108 KB build artifact should not be retyped by a language model at all. **The fix is a one-time PM action → B-471:** provision a Supabase PAT as a *cloud-env secret* (never in the repo — the Secrets Register already contemplates exactly this: "only needed if the PM later wants `supabase functions deploy` straight from disk, in which case set it as a cloud-env secret"). With it, `npx supabase functions deploy generate-signal --project-ref aigchluqluzuhtbfllgh` bundles and pushes from disk in one command, with no context hop and no retyping — and every future Edge-Function deploy stops being the riskiest step in the pipeline. `generate-report` (~similar size) and `ask` have the same exposure.

**Previous gate text, for the record:** `generate-signal` must NOT be redeployed until #458 merges — the client renderer ships in the same PR, so the gate is merge, not a PR chain.

**Standing rule from D3a, binding on every slice:** merges split on *does justifying this require knowing anything about animals?* **Class A** (orthographic/artifact) is permitted always, on read, retroactively; **Class B** (semantic) is write-path-only, never retroactive. Slice 3's serializer is Class-A only for exactly this reason — re-keying a *seeded* `ocean whitefish` because the owner edited the brand field would be a retroactive Class-B merge.

**B-416 — DONE 2026-07-26, shipped via #452** (detail: `docs/sessions/2026-07-26-protein-rederivation-backfill.md`). The library is no longer single-protein: **62 rows scanned, 37 changed, multi-protein rows 1 → 34**, incl. `Tiki Cat after DARK Rabbit & Chicken Liver → ["rabbit","duck","chicken"]` (a rabbit novel-protein food that had been keyed as clean rabbit and passed the slice-4 contaminant check), `Blue Buffalo Wilderness Duck → ["duck","chicken","turkey"]` and `Weruva Lamb Burger-ini → ["lamb","fish","tuna"]`. **The open PM sub-decision was put and RATIFIED** — yes, re-key the primaries; blast radius was exactly 3 rows (`ocean whitefish` → `whitefish`). What decided it: that mapping is *already* shipped on the write path, so declining would have guaranteed a **widening** split rather than holding the status quo. Built as a pure jest-tested module (`lib/proteinBackfill.ts`, 33 tests, mutation-verified) + a generator that emits reviewable SQL/rollback/report (`scripts/b416-protein-backfill/`) rather than a re-run of the vision model — a backfill over clinical data has to be auditable before it is applied. Both slice-5 guards held on live data: **0** rows where `canonicalize(primary_protein) ≠ proteins[0]`, **0** duplicate keys, **0** stored keys lost, and the applied state hashes identical to the independently computed expected state. **Deliberately did NOT write `ai_extraction_confidence`** — a keyword scan is a presence detector, not a completeness attestation, so these rows still read "ingredient list not read" rather than manufacturing "nothing else on the label" (D10; real fix is B-437). Residuals → **B-471** (D9 disclosure gap on the 3 re-keys) and **B-472** (lexicon coverage; re-extraction is the complete fix). Also open: **B-411** (`poultry` / `chicken fat` deliberate under-claims — read before writing slice 4's trial-conflict predicate).

### B-284 Culprit in-app brand alignment — N1 (tokens) + N2 (mark) + N2b (Landing) + N3 (loading system) + N5/N5b (calendar) shipped; N4/N6/N7 next
Spec `docs/culprit-in-app-brand-requirements.md` (v1.0, §10 = PRs N1–N7). Four locked rules (carve / register / one-accent / no-metaphor + motion budget). Night grounds appear **only where the app is working on the pet's behalf** (Landing, loading, the night moment, the Signal-card night variant) — capture & records stay the shipped light system. **Zero new dependencies** (`react-native-svg` + `expo-linear-gradient` already shipped).

| PR | What | Status |
|---|---|---|
| N1 | Night-surface tokens — 10 additive `theme.ts` entries (§2) | ✅ #321 (additive; no repoints; receipts verified; `tsc` + 1079 jest green) |
| N2 | `CulpritMark` (+ HomeHeader / AuthBrandMark) | ✅ draft #325 (carve mask-cutout, live pulse, header tap-to-scroll; `code-reviewer` caught + fixed a pet-switch race + a duplicate a11y node; new `store/signalMarkStore.ts` + `hooks/useReducedMotion.ts`; `tsc` + 1105 jest green) |
| N2b | Landing hero (+ full-bleed starfield) | ✅ #329 (`NightHeroGround` full-bleed starfield + carved moon) · **recomposed via #478 (2026-07-26):** static **whorl ground** in (the site watermark, indigo ridges, Option B of `docs/culprit-landing-hero-mockups.html`), the ring-train **ping OUT** (semantically empty pre-auth + never played clean on device); Landing now carries zero ambient loops; whorl scale/opacity = on-device tuning AC |
| N3 | `WhorlSpinner` + skeleton sweep + PTR + night moment | ✅ #333 (native-driver **View-transform** whorl [not `<G>` rotation — renders frozen], sm/md/px, day/night, `tint`; `Skeleton` Tier-1; ~30-site ActivityIndicator sweep; `NightGround`+`NightMoment` at cold-start/report/photo-extract; Home PTR band; `useAppActive`; `colorWhorlRidgeDay`. `code-reviewer` fix-before-merge ×4 applied; D7 scale/opacity + whorl-actually-rotates = on-device QA; CulpritMark same gap → B-322, **on-device confirm DONE 2026-07-26: it animates but the ring train reads rough — Landing retired the ping (#478); the HomeHeader placement is the open call** (CLAUDE.md Open Questions, 2026-07-26)) |
| N4 | Signal card (+ `SIGNAL_NIGHT_GROUND` on-device gate, D8) | ⬜ next (ships flag-false) |
| N5 | Calendar v3 — **presentation slice** (count-pips replace the heat-ramp + summary line + a11y) | ✅ #326 (pips + summary) |
| N5b | Calendar plumbing — month paging + drill-in + History day-filter (B-308/B-309) · symptom selector + "Calendar" rebrand + intake "Meals" lens (B-310) | ✅ #330 (paging + drill-in + single-day History filter) + #331 (B-310 selector/rebrand/intake "Meals" lens); coverage-gap (B-312) + med-colour token (B-311) still queued |
| N6 | Completion moment (ping + trial tick) | ⬜ |
| N7 | Home briefing (+ Tier-2 `design-principles.md` §3 edit, D9) | ⬜ merge-gated on §3 sign-off |

**N5 is disjoint from N2–N4** (calendar files vs. brand-mark / loading / signal surfaces) → ran as a separate concurrent session. **N5b is the deferred plumbing:** the mock's `pm-feature-review` (Jordan/Sam) caught that the buckets are symptom-only (no per-day all-event coverage, no multi-month, one auto-picked symptom), so paging + drill-in + coverage + selector each need a query/nav change — split out (B-308/309/310/311) with their designs locked to the PM's A/A/A calls.

### B-283 "You" (profile/settings) screen (app-store readiness) — PRs 1–4 shipped; PR 5 (go-live flips) next (external-gated)
Spec `docs/nyx-settings-requirements.md` (v0.1, §10 = 5-PR plan) + mockups `docs/settings-mockups.html`. Homes the App-Store items that assume an in-app settings surface (B-229/230/231/270) + the support email (B-273), and relocates the *user* actions (owner name / Sign out / Delete account) off the pet-scoped Pet tab. Entry = **avatar in the Home header** ✓PM; name = **"You"** ✓PM. **Zero schema** in every PR.

| PR | What | Status |
|---|---|---|
| 1 | Support + version primitives — `lib/support.ts` (`buildSupportMailto`, `formatAppVersion`) + `constants/links.ts` (`SUPPORT_EMAIL`, legal URLs, `LEGAL_LINKS_ENABLED=false`); pure, unit-tested, no UI | ✅ #315 (`code-reviewer` ship-ready; +17 tests) |
| 2 | The "You" screen (`app/settings.tsx`) + Home-header avatar entry + relocation off `app/(tabs)/profile.tsx` + route registration | ✅ merged #316 (`code-reviewer` fix-before-merge + `pm-feature-review` folded in; +7 owner tests) |
| 3 | Notifications (mocked — honest not-live state, **no armed med reminder** per D7 safety gate) | ✅ merged #317 (`code-reviewer` ship-ready; +shared `ComingSoonLabel`) |
| 4 | App feedback (`mailto` composer reusing PR 1's helper, `[Feedback]` tag) | ✅ merged #318 (`code-reviewer` ship-ready · `nyx-voice` ✓ · `pm-feature-review` SHIP-SHAPED after mail-hand-off fixes; +4 tests; S1 = reuse `support@`+tag) |
| 5 | Go-live flips (`LEGAL_LINKS_ENABLED` on when hosted docs land; real notif wiring; feedback backend) | ◐ **legal flip SHIPPED via #362** (docs live 2026-07-16; Privacy/Terms rows live + new Veterinary-disclaimer row); notif wiring (push-provider decision) + feedback backend remain external-gated |

**All build PRs (1–4) shipped** (PR 3 #317 · PR 4 #318; S1 resolved — reuse `support@` + `[Feedback]` tag). **Only PR 5 (go-live flips) remains** — external-gated (`LEGAL_LINKS_ENABLED` on when hosted docs land; real notif wiring on the push-provider decision; feedback backend if adopted), not a same-session build. **B-298 (copyable no-mail fallback) shipped 2026-07-26 via #487** — §4.5's *copyable*-address promise now actually holds on both support paths, via one shared `lib/supportFallback.ts`; adds `expo-clipboard` (first native module since the widget track, so the dev client needs one rebuild). Open PM sub-decisions from PR 4's `pm-feature-review` (spec §9): **B-297** (feedback via `expo-mail-composer` vs. raw `mailto`, applies to Contact support too), **B-299** (in-composer support-vs-feedback reminder); plus S2–S4.

### B-251 Onboarding experience revamp (app-store readiness) — PRs 1–10 shipped; the flow is COMPLETE end-to-end; only PR 11 (functional social auth) remains
Spec `docs/nyx-onboarding-requirements.md` (v1.3, design-locked; §9 = 11-PR plan) + committed mockup `docs/onboarding-flow-mockups.html` (v3). Burn-down of v0.1 (2-screen name+species) → Signal-led **Landing** (logo + persistent auth + data-rich previews) → account (owner first/last/email; Apple/Google **mocked**, email functional; TOS/privacy mock) → pet setup (type+name **required**; breed/gender/age **skippable**; dual integer-OR-birthday age) → **mocked** paywall → completion + durable `onboarding_completed_at`. **PRs 1–4 shipped (#288 / #287 / #286 / #289):** PR 1 owner-profile schema (migration `027` + `updateOwnerName` helper) · PR 2 age-precision schema (migration `028`, resolves S2) · PR 3 design-system primitives (`TextField` + `PrimaryButton loading`) · **PR 4 onboarding shell** — the durable `onboarding_completed_at` gate (pure, unit-tested `decideOnboarding` §6 seam) replacing the fragile "≥1 pet" inference, `ProgressBar`, and the `_layout` back-nav chrome; `hooks/usePet.ts` rewritten (two-read orchestration + retry/never-false-onboard-on-error/throw guards, hook wiring test); `code-reviewer` fix-before-merge FIXED; no schema. **PR 5 shipped via #290:** the Signal-led **Landing** — `app/(auth)/index.tsx` (logo + a horizontally-paging stack of 3 value previews **leading with the Signal** + tappable ≥44pt page-dots as the a11y swipe-alternative + a pinned Create-account/Log-in) + `components/onboarding/ValuePreview.tsx` ×3 (Signal/log/vet-report, **built from real component styles** — elevated surface, Newsreader headline, `EventIcon` tiles, accent sparkline); `app/_layout.tsx` routes unauthenticated → Landing (B-039 just-deleted → login preserved); `PrimaryButton` gains a teal `accent` variant (PM-ratified over near-black, B-256). Reviews: nyx-voice ✓ / pm-feature-review SHIP-SHAPED / code-reviewer fix-before-merge applied (de-causalised the Signal sample per `generate-signal` `CAUSAL_RE`; page-dot 44pt). No schema; tsc + 942 jest green. Follow-ups B-257/B-258. **PR 6 shipped via #291:** account creation + soft verify — `app/(auth)/signup.tsx` reworked onto `TextField`/`PrimaryButton` (first/last/email/password + calm inline validation; mocked TOS/Privacy; Apple/Google hidden behind `SOCIAL_AUTH_ENABLED`; soft "check your inbox" state w/ resend + edit-email back-escape + proceed→login). `supabase.auth.signUp` → session-present writes first/last + derived `display_name` via `updateOwnerName` → `/onboarding/pet`. New `constants/flags.ts` + pure `lib/authValidation.ts`. Reviews: `rls-privacy-reviewer` PASS / nyx-voice ✓ / code-reviewer fix-before-merge applied. No schema; tsc + 959 jest green. Follow-up: **B-152** (harden the soft-verify path + 2 rls dashboard checks before email confirmation is re-enabled). **PR 7 shipped via #292:** the two REQUIRED pet screens — `app/onboarding/pet-type.tsx` (Cat/Dog tiles, no default → explicit tap → Continue) + `app/onboarding/pet-name.tsx` (`TextField` → inserts `{user_id, name, species}` → Home; PRs 8/9 UPDATE breed/gender/age onto the same row) + shared `components/onboarding/OnboardingHeader.tsx` (`canGoBack()`-aware back + `ProgressBar` steps 1–2 of 5) + new transient `store/onboardingDraftStore.ts` (type+name survive back-then-forward; reset on sign-out via `wipeLocalSession`, FR-9 parity). `usePet.ts` + PR 6's `signup.tsx` redirect repointed `/onboarding/pet` → `/onboarding/pet-type`; v0.1 `pet.tsx` + orphaned `food.tsx` deleted (enacts D3 — no food step). Reviews: nyx-voice ✓ / Designer (P1/P5) ✓ / Jordan + Sam ✓ / code-reviewer fix-before-merge applied (draft-store back-then-forward + null-species escape hatch + `radiogroup`). No schema. **PR 8 shipped via #293:** the two SKIPPABLE optional pet screens — `app/onboarding/pet-breed.tsx` (`BreedPicker` species-filtered + free-text "Other"; Continue UPDATEs `breed`, Skip → null) + `app/onboarding/pet-gender.tsx` (Male/Female `ChipGroup`; Continue UPDATEs `sex`, Skip → `'unknown'`, the insert default); `pet-name` now **pushes** `/onboarding/pet-breed` with an **idempotent** insert-or-update (no duplicate pet on back-nav); `OnboardingHeader` gains an optional prominent Skip (`onSkip`/`skipLabel`/`skipDisabled`); breed field-state seed extracted to `resolveBreedFieldState` (shared with `EditPetModal`). Reviews: nyx-voice ✓ / Designer (P1/P5) ✓ / code-reviewer fix-before-merge applied (Skip-vs-Continue save race) / pm-feature-review (follow-ups B-259/260/261). **Closes B-210**; no schema; tsc + 977 jest. **PR 9 shipped via #294:** the age screen — `app/onboarding/pet-age.tsx` (segmented Age/Birthday toggle; Years/Months numeric fields → approximate DOB anchored `today − duration`; `DateTimePicker` birthday → exact; "Not sure? Skip") + pure `lib/age.ts` (`ageToDob`/`birthdayToDob` + `formatAge`/`formatBirthdayField`, 25 tests). Gender repointed to push `/onboarding/pet-age`. **Honesty gate (S2):** approximate DOB never a witnessed birthday — enforced in `lib/age.ts`, applied on Profile (`~2yr`), `EditPetModal` (honest field + precision-preserving write), and the vet report (`generate-report` drops `(b. YYYY)` for approximate). Data/Dr. Chen honesty ✓; nyx-voice ✓; code-reviewer Ship-ready (fixes applied — `resolveDobPrecisionOnSave` extracted+tested, token fix; B-262 wheel-vs-field deviation). No schema (writes 028's column). tsc + 1011 jest + generate-report deno (160). **`generate-report` deployed v9 (2026-07-06)** from the PR-9 branch — the DOB-precision fix + the pending vet-report render work (B-222) are now live; verified (v9 `ACTIVE`, `verify_jwt` preserved, boot smoke-test clean 404 + 401). **PR 10 shipped via #295 (paywall mock + completion — closes the flow's interim terminus):** `app/onboarding/paywall.tsx` (non-functional dark premium mock — no StoreKit; "Maybe later" + a mocked trial CTA both advance; the trial CTA honestly acknowledges Premium isn't live rather than silently no-op; explicit Principle-7 free-tier line; feature bullets reworked to convenience-only after `pm-feature-review` — the mockup's `Multi-pet profiles` ships FREE / `correlations` = care / `90-day history` truncates free trends+report) + `app/onboarding/done.tsx` ("You're all set" → writes the durable `onboarding_completed_at` on mount via `lib/profile.ts markOnboardingComplete()` → `replace('/(tabs)')`; back-gesture-locked terminus) + dark-surface theme tokens; `pet-age finish()` repointed `replace('/(tabs)')` → `push('/onboarding/paywall')`. Reviews: `pm-feature-review` (done SHIP-SHAPED; paywall NEEDS-WORK findings applied → the two blocking product fixes), `code-reviewer` (ship-ready; both NITs fixed), nyx-voice ✓. No schema (writes migration 027's column). tsc + **1022 jest** green. Follow-ups: **B-263** (reconcile the mock's placeholder bullets to the ratified freemium gate), **B-264/265/266** (StoreKit wiring / placement / escape-hierarchy polish). **The onboarding flow is now complete end-to-end (no dead ends) — a build CAN be cut.** **Only PR 11 (functional Apple/Google)** remains — post-v1, needs PR 6 ✓ + a PM-provisioned Google OAuth client. Moots B-038. Gated PM items: `design-principles.md` §Onboarding ratification (doc-drifted — PR 7 removed the food step; spec §5 breed-skip copy also stale) + the Principle-2 softening (spec §12); freemium-gate decision (unblocks B-263); Google OAuth client for PR 11; **B-152**. **STATUS.md is the one shared-file collision to expect at each PR's wrap.** **2026-07-16 (#362):** the flow gained a post-signup **veterinary-disclaimer acknowledgment** step (B-270) — signup → disclaimer → pet-type, server-recorded in `legal_acceptances` (#361); D10's mocked TOS/privacy links are now LIVE (hosted docs shipped), and `usePet`'s needs-onboarding entry lands on the disclaimer.

### B-271 App Review demo account (app-store readiness) — build-ready spec written + D1–D7 ratified; PR 1 (seed script) next
Spec `docs/nyx-demo-account-requirements.md` (submission-guide step 11). **Why:** Culprit is login-gated, so a reviewer in an *empty* account hits only designed empty states and can't evaluate the Signal / Trend / Patterns / vet report — Apple's "unable to evaluate the app's features" rejection; the demo is also Apple's only window into the wedge. **Story (D1, ratified):** a diet-trial dog "Cooper" — a sneaked-in "contraband" protein genuinely correlates with vomiting (flagship ① correlation, Early tier) + a last-2-days intake dip (② `intake_decline`, safety) as a backstop so the Signal card is never empty; both clinically honest (the trial-food staple washes out; never reassures). **Mechanism:** the Home Signal is a server cache (`ai_signals`) → `generate-signal` must be **run** after seeding; Trend / Patterns / Timeline render on-device from hydrated SQLite (B-054). **Seed:** an idempotent, date-relative, **parameterized** SQL script (`scripts/seed-demo-account.sql`) referencing existing global `food_items` only; credentials only in ASC. **D1–D7 ratified 2026-07-11 (#336).** Build: PR 1 = seed script (`adversarial-reviewer` mandatory — "staple washes out / never reassures") → PR 2 = `docs/app-review-notes.md` → live seed + `generate-signal` + verify (PM-gated: the demo account is created via real signup, which needs email-confirm ON, guide step 9). Multi-account expansion = **B-324**. Depends on **B-152** (email confirm ON) + B-054 (done) + B-272 (ASC record, done).

### B-117 Medication logging — Phases A–C done; Phase D in flight (PR 9 shipped; PR 10 gated on Step 9)
Spec `docs/nyx-medication-logging-requirements.md` (§12 = 10-PR plan). Model = **regimen + dose-events** (mirrors food: `medication_items`≈`food_items`, `medications`≈`diet_trials`, `medication_administrations`≈`meals`+`intake_rating`). Net-new additive schema (migration 020 applied live; 021 med-photos RLS applied; `nyx-medication-photos` bucket created PRIVATE). Configure regimen once → one-tap dose log. Safety: adherence inherits both invariants (n=1 never reassures; refusal→health flag; missed critical-drug→escalate; AI-extracted dose never silently trusted).

| Phase | PRs | Status |
|---|---|---|
| A — Foundation | 1–3 | ✅ schema (#192) · local mirror+sync (#194) · text-first quick-log (#196) |
| B — Photo + library | 4–6 | ✅ bucket+RLS (#197) · capture + `extract-medication-from-photo` + dose-confirm (#199) · picker detail/edit (#201) |
| C — Regimen + surfaces | 7–8 | ✅ "Current medications" card + compliance % (#202) · timeline + retroactive adherence + double-dose B-135 (#204) |
| D — Clinical consumers | 9–10 | ✅ PR 9 Signal confounder pass (#207 — `detection.ts` `medicationWindows`; case-enriched drug → suppress, concordant → cap at Early; adversarial PASS, fixed a Bonferroni family-shrink; residuals B-138; deployed v19) · ⬜ PR 10 vet-report section (gated on Step 9) |

Carry-forwards: B-122/123 satisfied; B-131 honored; B-128(b) defense-at-rest trigger (own schema PR); open sub-decisions B-132 (library delete), B-133 (`is_critical` owner toggle — PM call); **B-160** (med-details helper chips + copy — ✅ shipped #236); **B-171 + B-172** (dose-card copy — ✅ shipped #485: `drugDisplayName` gives the completion card the owner's word for the drug (brand-preferred, PM ruling (b)); `doseAdherencePrompt` restates a pre-lit adherence state with the correction named instead of re-asking, while in-doubt / unset / **unrated-vehicle combo** still ask. No default or threshold changed. `adversarial-reviewer` FAILed the first cut on two real breaks — both fixed: the serve-time combo assumed-`given` now keeps the question, and the brand-first reorder of the ROW surfaces was **reverted** to **B-522**, which needs a Dr. Chen call because the shared leading generic is the only cue for a duplicate active ingredient that `getDoubleDoseFlag` can't catch).

### B-023 Patterns dashboard — PRs 1–4 merged; PR 5 blocked on Step 9
Spec `docs/nyx-analytics-dashboard-requirements.md`. **No schema** (rides existing tables + `ai_signals` jsonb; migration 018 added an additive `summary`). Build gates resolved: §13 #1 name = "Patterns", §13 #6 colour-as-wellness ruling.

| PR | What | Status |
|---|---|---|
| 1 | Aggregate layer `lib/analytics.ts` + shared `lib/protein.ts` | ✅ #155 |
| 2 | Card components + §13 #6 colour ruling | ✅ #158 |
| 3 | Dashboard screen + Home doorways + `ComingSoonSummary` | ✅ #159 |
| 4 | AI summary — **template-only** (Haiku path built but gated off = B-096); migration 018 | ✅ #160, deployed live |
| 5 | Vet-report bridge ("Share with my vet") | ⬜ blocked on Step 9 |

Open follow-ups: B-094 (decline-watch/diet-trial cards), B-095 (correlations/coverage cards), B-096 (re-enable Haiku summary), B-099 (over-time + dashboard range — reopens §13 #2), B-116 (summary↔card grounding). Shipped polish: #162/#163/#164/#185.

### Food library / intake
- **B-568 wet/dry variant on the event surfaces — shipped via #500 (2026-07-28).** Two forms of one product (Royal Canin Selected Protein PR, 4 wet + 4 dry) rendered as the same string on Home/Today, History, the calendar drill-in, the completion card and the vet report; the library surfaces had always shown `BRAND · FORMAT`. No migration. **Two facts bind later work:** the label map is now dependency-free `lib/foodFormat.ts`, shared with the Deno functions (a second copy is the B-103 drift class); and `app/(tabs)/history.tsx`'s explicit row mapper drops any new *optional* `TimelineRow` field silently — `tsc` will not catch it. **`generate-report` is CODE ONLY — rides the existing B-494 deploy hold.** Detail: `docs/sessions/2026-07-28-food-variant-display.md`.
- **Picker findability — ratified build queue (2026-07-15, #351):** **B-346** rotation shelf → **B-347** pinned search + chips (executes B-020); sequenced (same `FoodPicker.tsx`), both no-schema, ready to run. Photos held (B-348); blue-sky directions parked behind B-349/B-350. Design docs: `docs/food-picker-findability-mockups.html` + `docs/food-picker-reimagined-mockups.html` (both carry the PM decision record).
- **B-004 standalone Food Library — COMPLETE** (#176–#182, 7 slices). 4th `Foods` tab, brand grouping, reliable-favorites, row thumbnails; picker stays 2-up tiles (PR 7 decision). + post-QA fixes #183/#184.
- **B-102 `human_food` food_format — in progress** (PRs 1–2, #166/#167). Migration 019 applied live + capture/display parity. Remaining PRs 3–7 (AI-extraction mapping, analytics regression, engine provenance, vet-report line, diet-trial note) — PRs 3/4 are disjoint and parallelizable.
- Open QA findings: B-111 (treats in Top Proteins, clinical call), B-113 (picker↔tab harmonization, #186), B-112/B-114, B-108 (closed #183). Downstream: B-017 (`food_format`/`food_type` overlap, carries B-024/B-102), B-009/B-018 dedup.
- **Food dedup & merge — direction aligned + build-ready DRAFT spec (2026-06-24, #237):** `docs/nyx-food-dedup-requirements.md`. Phase 1 prevention (B-009) first, **no schema**; Phase 2 merge (B-018 + B-005) = repoint-then-tombstone. Survivorship/reversibility/multi-user-auth parked as merge-phase decisions. Awaiting PM ratification → re-prioritizes B-009 to Now.

### B-186 Weight tracking — PRs 1–4 shipped (schema + logging + trend card + History row); v1 = display-only COMPLETE
The 2026-06-25 vet-council deep-dive named a weight **trend** the #1 missing datum (Nyx = zero `weight_check` events on a free-choice *weight-management* diet). **Audit (2026-06-26):** confirmed no existing weight surface — `weight_check` enum + `pets.weight_kg` snapshot exist, but **no value storage** (`events` carries only `severity`/`notes`), no logging UI, no trend surface. **v1 = display-only / descriptive (PM-confirmed): capture + trend, NO loss flag** (a flag is clinically load-bearing → its own spec + mandatory adversarial pass). **Guardrail every consumer inherits:** a weight trend never reassures — stable/rising ≠ wellness (rising can be fluid/edema); loss is the danger signal. Independent of the chronicity-lane work (disjoint files; only STATUS.md collides at wrap).

| PR | What | Status |
|---|---|---|
| 1 | Schema/data layer — migration 024 `weight_checks` (1:1 event child, mirrors `meals`) + local SQLite mirror + `LOCAL_WIPE_TABLES` reg | ✅ merged #244 (applied live, advisors clean) |
| 2 | Logging — `EVENT_TYPES` entry + numeric `weight` log step (pre-filled last weight, lbs) + `insertWeightCheck` + sync up/down + `pets.weight_kg` snapshot to latest reading | ✅ this session — PR #245 (was stacked on PR 1; rebased onto `main` after #244 merged) |
| 3 | Profile weight-trend card (descriptive sparkline + readings + delta; neutral framing, no wellness colour) | ✅ shipped via #248 — `WeightTrendCard` (`getWeightHistory`/`computeWeightTrend` in `lib/weight.ts`; "Log a weigh-in" affordance; empty/single/trend states; guardrail held in colour + copy) |
| 4 | History `weight_check` row + icon | ✅ shipped via #249 — `weight_checks` LEFT JOIN in `getTimeline`/`getEventById` (1:1, soft-delete via parent) + `weight_kg` on `TimelineRow`/`rowToEvent`; `EventRow` renders `${kgToLbs} lbs` as a quiet secondary line (Scale icon already via `EVENT_TYPES`, PR 2); display-only, no verdict |

Fast-follows: **Patterns-dashboard weight card ✓ shipped via #251** — `components/dashboard/WeightCard.tsx`, reading-count-aware placement (populated → safety/health-trajectory cluster, empty nudge → descriptive); shared delta/date copy extracted to `lib/weight.ts`. Remaining: vet-report weight section (Step 9); Home TrendZone weight; weight-loss flag spec (B-190). Card follow-ups: B-192 (empty-state coherence), B-193 (4px token).

### B-247 Stool analysis (per-incident AI read) — PR 1 (schema) in review; engine PRs unblocked
Spec `docs/nyx-stool-analysis-requirements.md` (§8 = 10-PR plan). Second child of B-013 (sibling of B-027/vomit): no `analyze-stool` exists today despite stool being the primary diet-trial outcome and the highest-volume GI event type; the vet report's "Stool characteristics" section has no AI-read data source. Additive schema only (new `stool_*` enums + columns on `event_ai_analysis` — **no** `stool_normal`/`diarrhea` consolidation, D1). Bristol Stool Scale (Type 1–7) as the consistency taxonomy (D3); Sonnet 4.6 (D4, matches `analyze-vomit`). **PR 1 (schema) shipped via #379** (draft) — migration `034_stool_analysis_columns.sql`, additive-only; rls-privacy-reviewer PASS + Data Scientist ✓. PM action: `apply_migration` when ready. **D2 RESOLVED 2026-07-16** (PM delegated to team → ratified): build the shared `_shared/incident-analysis.ts` as a **descriptor-driven pipeline** (`runIncidentAnalysis(descriptor, req)` owns auth→gate→context→image→vision→floor→write-back; each type = a small descriptor), so the Nth photo-analysis type (PM's coat-quality riff) = descriptor + migration + one monetization row, no handler copy. Validated by the two real types; coat-quality is the seam's acceptance test, not built now. Per-type escalation floor + mandatory `adversarial-reviewer` + reassurance-regex preserved; PR 2 AC = vomit's tests pass unmodified. Unblocks PR 2 (shared module + vomit refactor). Old draft PR #279 (docs) superseded by this branch.

## Blocking Open Questions

**Diet trial (B-417) — ✅ ALL SEVEN PRs SHIPPED (2026-07-26).** #450 (migration 040) · #453 (local mirror, closing B-408) · #456 (start-a-trial modal — the first write path `diet_trials` has ever had) · #454 (card v2 + Home strip, executing D2) · #459 (`lib/dietTrial.ts`, the ONE off-diet predicate) · #467 (vet-report render) · **#481 (the completion milestone)**. The wedge is end-to-end for the first time: an owner can start a trial, live with it, extend it, complete it with their own read, or stop it early with a structured reason — and every one of those reaches the vet report. Spec `docs/nyx-diet-trial-requirements.md` v1.0 + design-locked mocks; per-PR narratives in `docs/sessions/` (seven files, `…-b417-…`).

**⛔ THE ONE THING STILL GATING VALUE: `generate-report` IS NOT DEPLOYED, and the hold is a decision.** PR 7's render is on `main`; the Edge Function still runs **v13 (Jul 18)**, held behind **B-494** (the refused-cat safety band — the product team ruled it blocking for the *deploy*, not the merge, 2026-07-26). So the report an owner generates keeps the pre-PR-7 off-diet heuristic (~530 exposures across 645 feedings on live data) until that lane ships. **This also bounds PR 6:** merging it is safe, but an app build carrying it must not reach users before the redeploy, or completing a trial deletes it from the report — the exact harm §11's PR-6-after-PR-7 rule exists to prevent. When the gate clears: `scripts/deploy-edge.sh generate-report` → Supabase MCP `deploy_edge_function`, preserving `verify_jwt=true`. No migration, no new secret.

**Open residuals, each with a row:** **B-474 — CLOSED 2026-07-28 (#498).** The card renders exposure facts at last: `dietTrialFacts.ts` routes through `computeTrialFacts`, states 3/4 and record-and-continue are reachable, and the affirmative claim is gated by the composite `mayStateRecordClean` the vet report has always asked and the card never did. Shipped as the **wiring half of a split** — see the Diet-trial line under Open PM Action Items. **B-494/B-495/B-496** (cold-read, PR 7) · **B-475** (§12's *"every flag is tappable to its reason"* is UNMET — `explainVerdict` has zero callers; list screen is B-458) · **B-476**/**B-473** (Later) · **B-493** (no Designer/Dr. Chen pass on the §7 block as shipped) · **B-497**–**B-501** (report-wide, all pre-existing) · **B-452** (`sync_error` still has no reader) · **B-456** (Dr. Chen: `allowed_from` back-dating biases the exposure floor down; both directions carry a real clinical cost, and the ruling changes the WRITE) · **B-420** (the "20–30% adherence" figure traces to no published source) · **B-566** (the active card states a bare coverage ratio over a whole-range refusal — round 9 tried to fix it inside #498 and BOTH review lenses broke the fix: `rangeRefusal`’s floors fire on day 2 of 56 for a dog rated some/all/some, because they were derived for a claim gate whose own justification reads "silence is cheap". The fix is #499’s register, not a patch off the wrong predicate) · **B-567** (`allowedSetUnavailable` + a partial match deletes the app’s own uncertainty from the card — needs new copy, so Designer).

**From PR 6's two mandatory reviews — both FAILED the first cut and both were right.** Seven findings fixed in-PR; the worst was a **UTC/local inversion** in the outcome read that ran the before-window a day long while the during-window lost TODAY, both biasing toward *"improved"* on the screen that ends a diet (B-421's fourth day-math path — now pinned at UTC−7/+13/+5:30). Eight rows filed, not fixed: **B-508** ("Stopped early" is a category error at day 56, and the completed path captures no reason — PM) · **B-509** (`Save` ends a medical intervention with no confirm, no undo, no edit; the escape hierarchy is inverted — PM) · **B-510** (GI gets **five** exposures of the stop button before ACVIM's 12 weeks vs skin's two, *and* the smaller extension — Dr. Chen, pairs with P-1) · **B-511** (the refusal "health lane" fires no detector, and `detectIntakeDecline` needs rated meals an owner with an untouched bowl never produces — B-494's neighbourhood) · **B-512** (the milestone is invisible on Home + widget, and demotes to a link after 24h) · **B-513** (a decline-flagged trial has no *designed* decision surface) · **B-514** (three pre-existing fixtures fail outside UTC, so CI's UTC-only run under-tests the exact axis B-421 defends) · **B-515** (the outcome counts are not pseudoreplication-safe — named by the reviewer as NOT falsified, so nobody logs it as reviewed). **A second adversarial pass then ran against the fixes themselves** — the DoD line is not satisfiable on the strength of one's own repairs — and cleared all seven (52,608 instants × 9 zones on the day-key round trip, 0 failures) while catching **two more**, both fixed: the SQL lower bound was never padded under a comment saying it was, so east of ~UTC+3 the sheet rendered a **false** *"Nothing was logged in the N weeks before the trial started"* (the same defect sat in `readCoverage`, fixed as a pattern); and the continuation sentence reached the outcome sheet but **not** the terminal card, so a GI owner ended the trial and lived with a card that said nothing about continuing. Three more residuals filed: **B-516** (the sparse threshold leaks just above its trigger) · **B-517** (the day-math guard's `CONSUMERS` list still doesn't cover this file, so nothing stops a fifth path) · **B-518** (two similar-sounding day ratios with different numerators).

**Pre-ship review + rulings R1–R8 (2026-07-27).** Five isolated chairs ran the merged feature as ONE artifact — `pm-feature-review` (4 of 5 flows NEEDS-WORK), `adversarial-reviewer` (FAIL: six counterexamples EXECUTED against `main`, all green-on-CI), `code-reviewer` (two fix-before-ship seam bugs), `rls-privacy-reviewer` (**PASS** — attacks executed as real JWT roles on a migrations replica), and a `vet-report-cold-read` of two real-pipeline artifacts (**both NOT READY**; the Miso artifact's false trial-diet self-contamination inverted the clinical conclusion). Verdict: the clinical core held every attack; the breaks concentrate in **food-identity resolution feeding the predicate** and **computation-to-surface wiring**. Record + full gate list: **`docs/diet-trial-preship-review-2026-07.md`**; rows **B-529–B-544** filed; **B-508 closed by R4**. The PM ruled all eight decisions in one sitting: **R1** — the refusing-pet card gates the TestFlight cut (build now holds on B-533/B-474 + B-534–B-538, not only the redeploy), plus the PM's own intake-rating teach line; **R2** — G2 = trial reports; the no-trial section drops "off-diet" vocabulary; **R3** — the start date ("first day on the trial diet ONLY") moves to the default path; **R4** — the qualitative outcome approach is paused, the data leads (question optional everywhere; §6.1 untouched); **R5** — grace windows: report anchor 90d, card 30d; **R6** — widget punted to a full revamp (B-542); **R7** — the hydrolyzed↔intact derived-from relation, team-delegated, robust, own PR (B-529); **R8** — the vet sitting handed to the team. **Mock round 5 drawn same session** (refusal state, teach line, free-fed all-claim deleted, start date promoted, outcome-optional). The redeploy gate is now **B-494 + B-529–B-532 + a fresh cold read**.


Two **provisional** decisions ride at the PR that consumes them, flagged for Dr. Chen, blocking nothing:
- **P-1 — duration defaults, all four cells:** dog·skin 56d, dog·gut 28d, cat·skin 56d, **cat·gut 42d** (the only new number; cats reach ~50% remission at 4wk vs dogs >90% at 5). Consumed at **PR 3**. Lookup constant, no schema.
- **P-2 — no `paused` state** (PM: "a trial is either active or not"). Consumed at **PR 1** by omission. Adding the enum value later is a migration; the accepted cost is that a vet-directed hold becomes two rows.

**One residual with no number yet:** the **coverage floor**. Three defensible definitions of coverage read 100% / 84% / 19% over the same live 70 days, so **PR 5 pins the metric, then sets the floor**. It gates only §7.2's interpretability sentence — not the counts, not the card, and no alarm in either direction.


Nearest live PM gates, none of which block code already in flight:
- **B-182 chronicity D1/D2** — Dr. Chen's `minEpisodes` 6-vs-5 sensitivity call, and the B-247 stool ruling-(a) formal clinical sign-off. Both gate *ratification*, not the shipped code.

---

## Open PM Action Items

**Vet Files (B-478) — the track is complete; these are decisions and one build, not blockers on code**
- [ ] **Confirm G4 (priority) explicitly.** VF-2 and VF-3 were both built on a direct build instruction read as the promotion, but the spec and the backlog still say `Later` and no promotion is recorded anywhere. Say the word either way — the backlog Priority column is deliberately untouched pending it. **This is no longer free to leave open:** VF-3 flipped `VET_FILES_ENTRY_ENABLED`, so the feature is now live on the pet profile rather than merged-but-dark. If G4 was *not* meant to open, the one-line revert is the whole rollback.
- [ ] **Cut a native build before testing the Files/PDF row on ANY runtime.** VF-3 adds **`expo-document-picker`** (the only way to pick a PDF), and neither TestFlight build 35 nor the custom dev client contains it — Expo Go, which would have, is retired for SDK 57. This is handled rather than fatal: the module is required lazily, so camera and Photos work on today's builds and Files says *"PDFs need an app update"*. Everything else in VF-3 is testable right now on the dev client; only that one row waits for `eas build`.
- [ ] **Four mock deviations now want a single Designer/PM word, not four.** VF-3's **"Add another page"** on the saved moment — `expo-image-picker` shoots one frame per launch while D1-r2's copy promises "Snap each page — they stay together", and appending *after* the save keeps the instant-save contract that asking beforehand would break (detail in `docs/sessions/2026-07-27-vet-files-vf3-capture.md`) · VF-4's icon-back + centred title instead of the mock's text back-label "‹ Vet Files" · VF-6's **"Doc date" → "Date"** ("Doc" reads as *doctor* in a vet app, and the row directly below it is "Vet visit", so both can scan as "when was the appointment") · VF-6's **"Send page 2 of 3" / "Send this document"** replacing the mock's bare **"Share"** (which also fixes a real defect — a bare Share over a 3-page discharge sheet let an owner believe they had handed over the document when they had handed over its cover). `pm-feature-review` judged the first two ACCEPT and the Files-row one NEEDS-WORK (→ B-548); the last two are new this session.
- [ ] **Four product decisions routed to the backlog by `pm-feature-review`, each self-contained:** **B-546** (adopt a picked PDF's filename as identity — untitled PDF rows are indistinguishable, and PDFs are the ER record class) · **B-547** (D13's copy-to-another-pet is unreachable after the saved moment — the **spec says "and the detail ⋯ menu"** and the mock and build say otherwise, so this is a spec-vs-mock authority call) · **B-551** (does the "not included in the vet report" line also go on the report screen, where the assumption actually bites) · **B-554** (is the 8th-card position on the Pet tab the intended home for the parking-lot moment, and does that promote §11's share-sheet import).
- [ ] **Verify four things the repo cannot verify, all from the privacy passes.** Bucket `nyx-vet-documents` is **private** with the 15 MB limit and the four mime types (every policy is decoration if it is public) · migration 045 is live (`select prosecdef from pg_proc where proname='enforce_vet_document_pet_scope';` → `t`) · a cross-tenant probe with two real accounts returns a **uniform** not-found on rows AND storage objects (AC 7's server half) · `exiftool` on a bucket object confirms `compressForUpload` actually strips GPS (native behaviour, unprovable from source).

**Diet trial (B-417) — post-review state (2026-07-27): two holds, one sitting, one open call**
- [ ] **Do NOT cut a TestFlight build carrying B-417 PR 6.** The hold gates on BOTH tracks (ruling R1): the `generate-report` redeploy (**B-494 + B-529–B-532**, then a fresh `vet-report-cold-read`) AND the refusal register — **now #499, not #498**. B-533 was **split 2026-07-28**: the wiring + claim gate shipped via **#498**, the R1 register is held in **#499**. So R1 is unsatisfied and the cut still waits, on #499 + B-534–B-538. Deployed function is still **v13 (Jul 18)**.
- [ ] **#499 is blocked on two things only you and Dr. Chen can give:** (a) **the stand-down semantics** — when may a fired safety register be stood down? Silence must not do it (round 4: an owner documented 42 refusals, stopped rating, the register vanished over a cat still refusing), and round 5 found the repair counted *ratings* not *finished* feedings so **more logged refusals cancelled it**. The current answer is defensible and unratified. (b) **the feline register says "needs a call today"**, raised from "soon" because this lane is the only watcher on the 48h window for a diet refused from day 1.
- [ ] **A mock round for four disclosure lines that exist in code and in no mock** — `pushUnmatchedCaveat`, `pushPastBowlCaveat`, `pushUntrackedHead`, the refusal card's off-diet floor line. Round 5 draws the register but none of these; they were forced into existence by the review rounds, which is the "inventing them inside a build PR" failure the loader's own header warned about. Gates #499.
- [ ] **Run the batched vet sitting (R8, team to schedule/run):** P-1/P-3 duration cells (the cat placeholders) · the GI extension arithmetic + **B-510** (PM lean recorded: extensions come from vets and clinical sources, not the end user) · **B-456** back-dating (team rec to pressure-test: allow, cap at trial start, disclose the edit on the report) · the R1 refusal-lane semantics · the cold read's clinical asks (left-censoring disclosure, unlogged-medication caveat, the delta render). One sitting closes five items.
- [ ] **B-509 — still open, deliberately unruled:** `Save` ends a medical intervention with no confirm, no undo, no edit. R4 ruled the *question* (optional), not the *irreversibility*. Fold into B-533's design pass or rule separately. _(B-508 closed by R4 — outcome/reason optionality is by design; the day-≥-target label error folds into B-533's copy pass.)_


**Migration history (2026-07-26) — two non-urgent decisions, both `Later`**
- [ ] **Pick an option for B-507** — the backfill closed less than B-162 assumed. History holds 26 timestamp-style versions that match no on-disk filename, so `supabase db push` would still re-run 26 files. Options (a) re-key those rows to the filename prefix, (b) add a second row per file, (c) rename the files, (d) accept it and never run `db push` (the documented deploy path is the MCP anyway). (d) is defensible — it just needs to be a decision rather than an accident, plus a runbook warning. Full trade-offs on the B-507 row.
- [ ] **B-506 — renumber one of the two `018_` migrations.** `018_ai_signals_summary.sql` and `018_feeding_arrangements.sql` collide on one version namespace; both are applied, neither could be recorded without guessing which owns `018`. Needs a free, correctly-sorting prefix. _(Not blocking anything until B-507 (d) is reversed.)_
- _Not a PM item: applying migration 036 (B-505, `Now`) runs from a cloud session via the MCP — queued as the next session's first task._

**Settings (B-283) — one item, from B-298 (#487)**
- [ ] **Rebuild the dev client once before the next Runtime B session** — `eas build --platform ios --profile development`. B-298 adds `expo-clipboard`, a native module the installed dev client doesn't have linked, so Metro will start and then the app errors at load ("when the dev client itself goes stale", `docs/dev-handoff-runbook.md`). One-time; unrelated to the TestFlight cut.

**Event-time confidence (B-448, #492) — one expert call**
- [ ] **Designer + Dr. Chen: is `Saw it` the honest default for an EXIF-seeded symptom photo? (B-524)** Five live `vomit` rows are `witnessed` on a time that came from the photo's metadata. Four were shot seconds before logging, so the tag is true. One was shot **5h39m** before (01:38 → logged 07:17) — the canonical B-010 *discovery* case, printed on the vet report as `seen`, the strongest claim the column can make. Not a code leak: the owner had the Saw-it/Found-it control on screen and left it on the seed, and Principle 1 is exactly why a seed exists. Options on the B-524 row (leave it · seed `found` past an N-minute gap · surface the gap in the attribution line the confirm screen already renders).

**Build 35 TestFlight QA (2026-07-26) — three PM-side items**
- [ ] **Provision `SUPABASE_ACCESS_TOKEN`** (B-485) — mint at dashboard → Account → Access Tokens, add as a **Codespace secret** scoped to `project-nyx`, then `bash scripts/deploy-edge.sh <name> --deploy`. Its first real run is its verification: the guard path is tested, the upload path has never executed. Revoke the old `nyx-cli-deploy` PAT while you're there, and add the new one to the Secrets Register.
- [ ] **Deploy `generate-report`** — still **v13 (Jul 18)**, so #448's protein render is not live and the vet report's Appendix B "Proteins in the diet" block cannot appear no matter which app build is installed. `ask` is one version behind too (#449's timezone fix; flag-gated, not urgent). `generate-signal` needs **no** deploy — #448's change to it is additive re-exports only `generate-report` consumes.
- [ ] **Set the Supabase Site URL** (B-483) — Authentication → URL Configuration. The confirm-email click loop was completed this session: it lands on a `localhost` page. It works (confirmation is server-side before the redirect) but it is visibly broken at the highest-stakes moment in the funnel and is a submission risk. One field; the deep-link version stays B-432.


**B-351 slice 4 (#447) — one blocker, one copy call, one QA limitation**
- [ ] **Tier 2 is now QA-able on device — no SQL needed.** B-417 **PR 3 shipped the start-a-trial modal** (#456), so a trial can be created from the Pet tab and the trial-contaminant half is finally exercisable end-to-end. One caveat that is itself the thing to check: start a **single-food** trial for this pass. PR 3's multi-food guard makes `loadTrialProteinContext` go **silent** on any trial with 2+ `primary_diet` foods (B-453), so a wet+dry trial correctly renders no standing note and would read as a false negative.

**B-416 protein re-derivation (#452) — one Tier-2 edit, one design call, one device pass**
- [ ] **Confirm the Tier-2 spec edit** (`docs/nyx-multi-protein-requirements.md` §11, the Phase-A fast-follow paragraph): mark B-416 **done** and record that the Class-B sub-decision was put and ratified 2026-07-26. Flagged, not written — Tier-2 protocol. One word ("confirmed") unblocks it.
- [ ] **Rule B-462 — fats/oils and plant proteins.** The lexicon was widened hard on your "err on surfacing for the vet" steer, but these two were left out because `food_items.proteins` feeds the **shipped owner-facing contaminant flag** as well as the vet report, so every key becomes owner copy (*"This one has X…"*). Recorded persona conflict — Dr. Chen: disclose both, it's my judgement to make; Designer/Jordan: *"should skip chicken fat"* is advice a vet would contradict. The likely answer is a **second disclosure channel** that never reaches the contaminant check, not a wider lexicon — which needs a design round. Dairy is the precedent: included, and it cost **zero** new alarms.
- [ ] **On-device pass on the backfilled sets** — the one unchecked DoD box. Script in the handoff; the load-bearing check is that a food now listing more proteins still says *"ingredient list not read"* rather than *"nothing else on the label"* (D10 — confirms the pass did not overreach).

**CI ruleset polish (B-433) — optional, ~15s**
- [ ] **Set the `main` ruleset's Allowed merge methods to Squash only.** It currently permits Merge/Squash/Rebase while the Git Workflow rule is squash-only, so the written convention is enforceable but not enforced. Ruleset `main` → *Require a pull request before merging* → Allowed merge methods. (Makes `Require linear history` redundant — don't tick both.) Not urgent; the B-390 gate itself is live and binding.

**Email confirmation (guide step 9 — B-152 part 2; part 1 ✅ 2026-07-25)**
- [ ] **The second RLS dashboard check:** "Confirm email" ✅ (verified live by the send), but confirm **email-enumeration protection** is enabled and the auth email rate limits are sane. #436's `invalid_credentials` copy deliberately won't reveal whether an address has an account — that's only half the protection if the server setting is off.
- [ ] **Auth email templates still say "Nyx"** (hardening audit §B7) — Authentication → Emails → Templates. Visible in the email received 2026-07-25; a reviewer creating an account would see it too.
- [ ] **In-app device pass** — needs a dev-client build (`eas build --profile development --platform ios`); Expo Go stopped working once the widget native targets landed. Script in #436.
- [ ] **Delete the `+smtp1` test user** once the click loop is done, so it doesn't join the two pre-existing unconfirmed orphans ahead of B-271.

**Storage hardening — B-431 and B-248/B-466 both closed 2026-07-26. Every bucket is now owner-scoped.**
- [ ] **One on-device vet-visit photo check, when convenient — not a blocker.** Nothing reads these objects from Storage today (the app renders vet attachments from the local file), and the bucket holds 0 objects, so there is no regression surface to guard. But a vet-visit photo capture is the cheapest confirmation that the new INSERT policy accepts a legitimate upload — and it exercises the same upsert-needs-SELECT question as the B-431 pet-photo check below, on a bucket that (unlike `nyx-pet-photos`) has always had a SELECT policy. Do it alongside (a) below and you get both answers from one session.
- [ ] **B-431 is fixed and live (migration 042, PR #460) — two follow-ups need you.** **(a) One on-device pet-photo upload + a replacement upload.** This is the merge gate, and it also settles a long-standing Open Question: the "`nyx-pet-photos` uploads fail 42501" question blames the SQL-created-bucket `owner = null` landmine, but **all six buckets have `owner = null`** and the other five accept uploads — the real discriminator was almost certainly that this bucket had **no SELECT policy** (the upsert path needs one; `RETURNING` is SELECT-gated), which 042 adds. The bucket holds 0 objects and 0 pets carry a `photo_path`, so pet photos have **never once worked in production**. If the upload now succeeds, mark that Open Question resolved. **(b) Confirm "Allow anonymous sign-ins" is OFF** (Dashboard → Authentication → Providers) — an anonymous session carries the `authenticated` role, which would make 042's `TO authenticated` a low bar. Nothing uses it today (0 anonymous of 6 users); this is a config confirm, not a suspicion.

**Auth / iOS logout (#412)**
- [ ] **Verify the frequent-logout fix on-device** (the only remaining DoD item — code-confirmed, not yet on-device-confirmed). Get #412 onto the device (Runtime B, or `eas update --branch preview` to your TestFlight build since that's where you repro'd it), then: force-quit → Airplane Mode → reopen → **you stay on Home with local data, NOT the login screen** (old build 34 bounced here). Confirm normal cold start still restores, and Sign out still routes to the Landing. Note the on-device diagnostic trail is **no longer available** — B-301 removed the probe (2026-07-26) after build 34 confirmed #350's locked-phone fix, so a recurrence here needs a fresh repro report rather than a shared trail.

**Widget W5 (on-device + spec decisions)**
- [ ] **Answer the §4.1 flip-latency question on a real device** — the one gate the spec put in front of committing to the flip design. It could not be answered in a cloud session (no device, and widget UI is not OTA-able). Cut a dev-client or TestFlight build from this branch, place the widget, tap **Meal**: the picker should appear in well under a second **with the app force-quit**. The code path is extension-local by construction (the interaction intent re-evaluates the layout in-process and reloads the timeline), so this is a confirmation, not a discovery — but if it drags with the app dead, the fallback is precomputing both picker states into timeline entries (recorded in the PR).
- [ ] **Ratify the §4 wording change the outbox forces** — a widget tap is *captured* with no app launch, but becomes a *row* when the app next runs (instantly while it's alive). §5.1 as written says "logged in ≤2 taps with no app launch". Confirm the reading (the tap is the capture; the record follows, same as the offline path §5.6), or tell us to chase a background-write path.
- [ ] **Confirm the undo mechanic** — state-based ("Undo" stays until the app takes the capture off the widget's hands) rather than the mock's "~60s", because a widget gets no guaranteed timed re-render. Rationale + the residual (a drained bowl top-up can't be reverted, unlike a meal/treat) are in the PR.
- [ ] **Check the pet-slot labels with two pets on one Home Screen** (D5 option (a)): Edit Widget shows "Pet 1…Pet 6", and the widget itself shows the pet's real name. If that reads as guesswork at W6, option (b) — the named picker — is the recorded upgrade path.

**Widget (2026-07-24 ratification — Tier-2 doc edits awaiting your confirm)**
- [ ] **Confirm the D-M1 amendment edits** (widget ruled FREE — `docs/nyx-widget-requirements.md` §10): (1) `docs/monetization-and-throttling-requirements.md` — remove/reframe the "Home-screen widgets" Premium bullet (Premium may wrap widget *styles*, never the capture surface); (2) the B-263 paywall bullet swap picks this up when next touched. Per Tier-2 protocol these aren't written until you confirm; one word ("confirmed") unblocks both.

**Monetization (Track-3 prerequisites — sequence-block RevenueCat wiring, NOT submission)**
- [ ] **Offline actions 1–4** (`docs/monetization-and-throttling-requirements.md` §10): ① App Store Connect → Agreements, Tax, and Banking (decide the legal/merchant entity first; **highest leverage — external latency, everything paid stacks behind it**) → ② Apple Small Business Program enrollment → ③ RevenueCat account + iOS SDK key + webhook secret (→ Secrets Register at T3-C) → ④ subscription products ($4.99/mo, $39.99/yr + 7-day trial; **checkpoint C2 — the D-M5 final price lock**).
- [ ] **B-047/B-016 ownership decision (surfaced by the #344 review) — gate for T3-E, currently unowned.** Both instrumentation items (B-047 AI-Signal conversion/time-to-first-insight = `Next`; B-016 error observability = `Later`) hard-block the extraction gate flip (§9 T3-E, §12 #6) yet appear in **neither track's PR plan**. Decide: (a) pull the minimum conversion + time-to-first-insight events into Track 3 as an explicit owned PR, or (b) run B-047/B-016 as a named parallel prerequisite track. Left unresolved, T3-E silently slips. *(Not on the submission critical path — only gates the post-launch Premium gate flip.)*
- [ ] **Entitlement grace-window duration — confirm at T3-C build time** (`monetization…requirements.md` §4.7; provisional **72h**). Money on both sides (too long = post-cancel free-riding; too short = a paying owner loses Premium in a dead zone). Not a silent engineer default.
- [ ] **Server-side config-read failure fallback — Dir. of Eng sign-off at T2-3** (§4.2; provisional = mirror the client per-key posture, AI keys fail *open*). Currently unspecified in the ratified strategy; an engineering ratification, not a re-decision.
- [ ] **§4.5 shared response decoder — PM call (parked from the #344 review).** Whether to mandate one `lib/aiResponse.ts` `parseAIResult()` type-guard for the typed 200-body contract so cap/flag discrimination isn't reimplemented per caller. The one remaining *structural* review gap; two-line addition to §4.5/T2-4 if adopted.

**Ask (B-228)**
- [ ] **A8 (#409) redeploy — ORDER MATTERS, three functions.** A8 changed `_shared/incident-analysis.ts` (the `transform_only` opt-in, inlined into every incident-analysis bundle) and `ask` (the `read_photo` tool + the A7 guardrail hardening still pending from #408). Redeploy **`analyze-vomit` then `analyze-stool` FIRST** (they only *learn to honor* `transform_only`; default-off ⇒ detail-screen behavior byte-identical, so this is safe/behavior-neutral), **THEN `ask`** (which starts sending `transform_only:true`). If `ask` deploys before the analyze-* pair, a live read hits the old analyze-* and sends a raw (un-stripped) original — the AC-13 violation window. Each via `scripts/deploy-edge.sh <fn>` → Supabase MCP `deploy_edge_function` (preserve `verify_jwt=true`); verify version bump + `ACTIVE` + a JWT'd bogus-`event_id`/`pet_id` boot smoke-test (clean 4xx, not `WORKER_ERROR`). Record here when done. **Dashboard checks (rls-privacy):** `nyx-event-attachments` bucket **PRIVATE**; the Storage image-transform (imgproxy) add-on **enabled** (the sole EXIF/GPS-strip for Ask reads — if off, reads degrade to "couldn't read the photo", never raw bytes). **On-device QA after deploy:** ask "what did her last vomit look like?" on a photographed-but-unread vomit → the answer's read line recounts it + the tap-through opens the event (read now on the detail screen); a no-flag photo never reads "looks fine/clear".
- [ ] **Deploy `ask` v1 (A4, #403) — the one A4 follow-up.** Verified bundle `.edge-build/ask/index.ts` (sha256 `e098f736…`, 102 deno tests green, `node --check` valid). New function, **`verify_jwt=true`**, ship-DARK (`ask_enabled`/`ask_general_enabled` stay off, so it's reachable by no one until the PM allowlists their uid post-A5). Deploy via the runbook (`scripts/deploy-edge.sh ask` → Supabase MCP `deploy_edge_function`; 69 KB, under the B-225 ceiling — inline from a fresh cloud session, or `npx supabase functions deploy ask`). **Then:** `list_edge_functions` shows `ask` ACTIVE + `verify_jwt:true`; a JWT'd bogus-`pet_id` boot smoke-test returns a clean 404 (not `WORKER_ERROR`); `get_advisors` (security) clean (the rls-privacy-reviewer PM checks). Record the deploy here when done.

**Widget (W3/W4 — B-290)**
- [ ] **W3 on-device seams (needs a fresh dev-client build from the W3 branch/`main` — the entitlement rides the binary):** (1) session survives relaunch + a >1h background (the shared-tier migration didn't log you out; no repeated `[secureStore] shared-tier write failed` in the console — on a pre-entitlement binary that warning once per refresh is the *designed* fallback, not a failure); (2) sign out → back in → clean re-hydrate (App Group wipe). Full script in the W3 PR.
- [ ] **T&S call (raised by the rls-privacy pass, now that the session ALSO lives in the shared keychain group):** keep `AFTER_FIRST_UNLOCK` (iCloud-keychain/backup-migratable) or move session tokens to `AFTER_FIRST_UNLOCK_THIS_DEVICE_ONLY`. Pre-existing posture (`lib/secureStoreTiers.ts` header), sharper now; not W3-blocking.
- [ ] **W4 rls-privacy LOW findings — two accept-or-harden calls (PASS otherwise; neither merge-blocking, both within-tenant):** (1) a departed pet's **name** persists as a `pets-index.json` tombstone past the snapshot prune (deliberate — it's the D5/B-086 anti-silent-switch guarantee; name-only, sign-out-wiped; FR-9-vs-D5 tension for T&S); (2) no server-side constraint forces a direct-REST write to `logged_via ∈ {widget,intent}` — a modified extension binary could claim `'app'` provenance into its own account (analytics-level; harden = a small CHECK/trigger migration if wanted).
- [ ] **W4 dashboard check (rls-privacy):** when the extension target exists (W5), confirm its entitlements grant ONLY `group.com.projectnyx.app` and the App Group is owned solely by this team — the load-bearing assumption behind the shared-keychain session read.

**Password recovery (B-280)**
- [ ] **Three device checks (spec §9.3)** — none answerable from code, all gate PR 2's design. **Q1:** open a reset link on desktop, then the *same* link on the phone — if the phone shows "That link no longer works", the desktop hop **burns the token** and D1c's warning must move into the email template. **Q2:** request on phone A, open on phone B (both with the app) — is the verifier-absent error distinguishable from a real expiry? **If not, this reopens D1a (PKCE).** **Q3:** does `message://` reach a Gmail-only owner's inbox?
- [ ] **Read the project's JWT/access-token expiry** (Supabase → Auth). D6b's whole cost argument (§7.2) assumes an evicted device stays live ~1h and flushes its queue first; if it's much shorter, B-430's data-loss residual is wider than priced.
- [ ] **Supabase dashboard, before the flow goes live (spec §9.2):** add `nyx:///reset-password` to the **Redirect URL allowlist** (without it every link dead-ends) · confirm **email enumeration protection** state *before* flipping it (it changes shipped signup/login behaviour) · turn on **"Secure password change"** (or PR 3's current-password field is decorative) · turn on the **password-changed notification email** (D6b's compensating control) · rewrite the **recovery email template** off "Nyx" → Culprit.
- [ ] **Read the recovery-link lifetime** (Supabase → Auth → OTP expiry) — now needed for **two** reasons, not one: §5.5's "they don't last long" is an unverified assertion, **and** PR 1 ships a 24h `RECOVERY_PROVENANCE_WINDOW_MS` (FR-14) that must stay **≥** that lifetime, or a legitimate late tap on a still-valid link is refused.

**Ship gates / on-device QA**
- [ ] **#423 (W2 SDK 57) post-merge regression QA** — run the 13-step script in the PR on-device (Runtime B; Expo Go still works until W3). Merged ahead of the pass at PM request; a regression found now is upgrade-caused by construction. Also: the **first TestFlight cut from post-W2 `main` must be a native `eas build`** (OTA fenced by `version` 1.1.0).
- [ ] **B-358 (#392) — apply migration `036` live ONLY AFTER the reordered client reaches devices.** The tightened `nyx-food-photos` Storage INSERT (owner subquery) is safe only once every installed client runs the insert-then-upload reorder; applying it while an old (upload-then-insert) bundle is live would `42501` every food-photo upload on that device (the B-182 deploy-gate lesson). So this is NOT a merge-time `apply_migration` — cut/OTA the client build first, confirm it's live for testers, then `apply_migration` 036 via the Supabase MCP + `get_advisors`. **Record the apply here in STATUS when done.**
- [ ] *(Optional, non-blocking)* one-time audit of `nyx-food-photos` for objects planted under the old permissive INSERT (now unreadable/un-overwritable cross-tenant, but worth a sweep before launch). *(The bucket-PRIVATE confirm is DONE — verified live 2026-07-20 audit: all buckets private except `nyx-pet-photos`, whose public posture is now the audit §A3 decision.)*
- [ ] **#361 + #362 (in-app legal wiring) — on-device QA + merge.** Run #362's Manual QA: fresh throwaway signup → "Before you start" acknowledgment → `legal_acceptances` row lands server-stamped (Table Editor); Terms/Privacy open live from the signup acceptance line; You screen's Privacy/Terms/**Veterinary disclaimer** rows all open live pages (no "Coming soon"); airplane-mode acknowledgment shows the calm retry and never advances unrecorded. Merge #361 then #362 (the migration is already applied live, so order is tidiness, not correctness).
- [ ] **B-222 follow-up decisions (all low-stakes, backlog-tracked):** **B-240** — which weight surface is right (Profile snapshot vs Patterns tracked-only); first confirm via the Profile card subtext ("From Nyx's profile" = snapshot = expected). **#2 behavior** — keep the smart default cascade (rec) vs force 90d always. **B-238** — add a pre-generation out-of-window hint or leave the disclosure in-report. **B-241** — "Default" vs "Automatic" chip copy.
- [ ] **#257 — on-device QA + squash-merge (this session).** Run the Manual QA in the PR: **B-196** (Current-medications card loads, no `PGRST201`), **B-198** (log while History is open → no duplicate-key error), **B-197** (Edit a weight → value field pre-filled + saves; empty/`0` blocks Save), plus a spot-check of weight tracking + the chronicity card. Merge when green. **No backend action** — client-only fixes; `generate-signal` v23 (with ⑦) already live.
- [ ] **#255 `analyze-vomit` + photo-load fixes — on-device confirm (the one open PR #255 gate):** open the **6/25 and 6/27 vomit events** → each shows a **real AI read** (not stuck "Try analysis"); tapping the photo opens full-screen **fast** (not 5–10 s). If a photo instead reads "I couldn't read this photo," the Pro Image-Transformation add-on isn't active (safe no-op — flag me). Verifies the 546 fix + server-side downscale + viewer perf end-to-end.
- [ ] **Toggle Supabase "Confirm email" OFF** (Auth → Providers → Email) — was needed to create a *throwaway* account for the B-039 QA; that QA is now done (verified against an existing account), so this is **B-152-only now**: re-enable + provision production SMTP before launch. New-account creation silently failed under email-confirmation (fixed in #213 to show "Check your email").
- [ ] **#213 med-QA fixes — on-device spot-check** (Manual QA in the PR): account-creation "Check your email"; strength-gate toggle; "Current medications" counts a logged dose; End-button red; no `meals 23503` on logging an existing food.
- [ ] **AC-6 logout-wipe** — sign out on a populated device → confirm data empties → sign into a *different* account → confirm no prior-pet data shows (Trust & Safety gate; code passes).
- [ ] **App Store readiness — work the 16-step runway in `docs/app-store-submission-guide.md` (#300; register: `docs/app-store-readiness.md` #297+#300).** Start-now PM steps (all parallel, long-lead first): **step 1** verify the ASC record + lock the store name (B-272) · **step 3** pick a host for the support/privacy/terms page (B-273) · **step 4** provision production SMTP (B-152). ~~Ratify + flag the paywall off for v1~~ — **done 2026-07-24**, guide step 6 now ✅ (PM ratified + flipped `app_config.paywall_enabled=false`, verified live; B-330 closed, B-263–266 deferred out of the submission path). ~~`supportsTablet: false`~~ — **done 2026-07-24**, guide step 5 now ✅ (app target prebuild-verified iPhone-only; effective at the next native build-cut, not OTA; widget-extension device-family residual → B-415). **No PM *product decision* remains on the runway** — what's left is operational (SMTP, screenshots, listing copy, the build-cut). Each guide step has a kickoff prompt (PR steps) or a "Guide step N complete: …" confirmation (PM steps) that keeps the tracker/backlog/STATUS honest.
- [ ] **Culprit rebrand — remaining items** (`docs/culprit-rebrand-execution-plan.md`, #305): PR A (name+strings) shipped #307; **PR B (Moon & Signal icon + splash) shipped #309** — the design-master gate is **cleared**. Remaining: (1) **PR C** (`colorBrandNight` indigo token) — ready to build, no PM prerequisite; (2) **Supabase auth email template** still says "Nyx" (dashboard → Auth → Email Templates) → Culprit; (3) **cut a new TestFlight build from `main` after merging** to make the icon/splash/name visible — they're native/build-time, so an OTA `eas update` won't move them (`eas build --platform ios --profile production --auto-submit`); do the **iOS-18 dark/tinted on-device verification** on that build (unverifiable in the cloud session; icons don't render in Expo Go).
- [ ] **B-267 built-artifact verify (rides the next iOS build-cut; #299)** — the permission-string config fix is merged, but introspect can't check the built plist. On the next `expo prebuild`/EAS build, confirm `ios/*/Info.plist` carries both `NSCameraUsageDescription` + `NSPhotoLibraryUsageDescription` (Culprit copy) and **not** `NSMicrophoneUsageDescription`, **and** that the aggregated `PrivacyInfo.xcprivacy` privacy manifest is present (Expo SDK 54 auto-aggregates — verify, don't assume). Do NOT assume EAS autolinking supplies them. Batches with the Culprit-rebrand build-cut (item above) — same build.
- [ ] **B-053 on-device QA** — Signal regens against v10; the staple-washout diagnostic only shows in `no_pattern` (Nyx may show the reflection finding instead — correct, not a bug).
- [ ] **Confirm AI summary on-device** — open Patterns for Nyx; expect a safety-led, vet-routing summary with every number matching a card.
- [ ] **Whole-system aesthetic on-device QA** — walk the app vs `docs/design-system/_system/.../index.html` for the holistic Calm/Linear/Oura pass (deferred to end-of-system; all 4 design PRs merged).

**Decisions**
- [ ] **B-325 — promote retroactive/med-first combo linking as part of B-156, or fold direction ① (med→existing-treat) in before promotion?** Rides the B-156 promotion call. Investigation + recommended shape logged as B-325 / #338 (retroactive entry on the treat/meal detail screen; run the same PR B3 safety coupling; never mint a phantom vehicle event). Direction ① is the cheap, high-value slice (reuses the forward write path + safety coupling); minting a new treat from the med side is a scoped follow-up gated on the pure-vehicle question.
- [ ] **B-311 medication colour (#332) — ratify the design-system addition + decide B-323.** A new event-category hue (`colorEventMedication #5B7A9E` slate-blue) shipped respecting §1.3 (not teal/indigo/rose; selection stays teal), applied to the drill-in + Today strip. Two low-stakes calls: (1) sign off on the slate value (or redirect it) — adding a category hue is a design-system decision, not a free choice; (2) **B-323** — should History `EventRow` adopt the full symptom/meal/med tint (parity) or stay deliberately symptom-only? On-device: confirm the med glyph reads slate on both the Today circle + the drill-in grey circle, distinct from symptom-rose/meal-teal.
- [ ] **Logging-capture discovery (#319) — 2 ratifications after reading the brief** (`docs/logging-capture-discovery.md` §9; CLAUDE.md Open Questions 2026-07-10 — PM saw all three at wrap, chose to read the brief first): (1) owner-configured scheduled confirmations vs Principle 4's one-nudge cap (Designer dissent on record — channel trust is one bucket; proposed carve-out = opt-in + fail-safe silence + self-pruning) → unblocks **B-288**; (2) household shared-write minimal primitive as capture *infrastructure* (not a social layer) → unblocks **B-292**. (3) native-targets-in-managed-workflow was **sequencing-CONFIRMED same day: decide after B-288 pilot data** — nothing to do until the pilot reports. Also answer when convenient (non-blocking, sharpens the build): your household's meal-time regularity; does your wife carry iPhone + Watch; what Dog Recruit's logging feels like at its best (B-296 teardown seed).
- [ ] **Login consolidation (#311) — one device check.** (The B-280 scope question that used to lead this item is answered: recovery is in flight, PR 1 shipped.) **Device check** (unverifiable from code): after **account deletion** → on the login screen press **back** → confirm you land on the Landing, not a flashed empty authenticated tab. Also note **B-281** (social-auth block absent from login) must be resolved when PR 11 flips `SOCIAL_AUTH_ENABLED`, or an Apple/Google-signup owner can't log in.
- [ ] **B-251 onboarding — ratify the `design-principles.md` §Onboarding revision (Tier-2 edit, flagged not written)** before/around PR 4–5. The constitution still says "minimum viable / required: name+species / first log in 60s / complete-at-first-log"; the design-locked flow outgrows all of it (owner identity + optional pet richness + store gates; finish line moves to profile-created→warm-home) and Principle 2's "food library pre-populated from onboarding" should soften to "on first meal log" (food capture moved in-app). Also: **Google OAuth client** to provision before **PR 11** (Apple Developer already enrolled) — not blocking PRs 1–10.
- [ ] **Work the 2026-07-20 hardening-audit top actions** (`docs/app-store-hardening-audit-2026-07.md`) — **two left:** ① pet-photos-public posture call (§A3); ② B-403 Supabase config flips (~15 min). *(Numbering keeps the audit's own order for what remains. Closed so far: **§A1 — the B-248 vet-attachments owner-scoping migration — DONE**, shipped + applied 2026-07-26 as migration 043/PR #466, which also ran **§A4**'s live `pg_policies` verify (every policy on `storage.objects` names a specific bucket; none grant `anon`/`public`). **B-397 DONE 2026-07-26** — both orphaned functions deleted by the PM + verified from-session, `list_edge_functions` 10 → 8 and now an exact match for `supabase/functions/`; `view-report` was the project's ONLY `verify_jwt=false` function, so that exposure class is now empty (closed via #465). Migration 034 is confirmed APPLIED live — the old apply-034 action is done; `analyze-stool` PR-3 prerequisite cleared.)*
- [ ] **B-099** — reopen spec §13 #2 (over-time views + dashboard 7d/30d range) before building B-099.
- [ ] **B-080** — placement of diet-structure observations (Signal band-2 vs B-053 coverage lane); blocks descriptive Phase 3.
- [ ] **B-023 colour** — reconcile Home `TrendZone` (colours a falling symptom "improving" in the accent, `TrendZone.tsx:66/79`) to the §11 #3 ruling, or accept the cross-tier difference.
- [ ] **B-065** — should the food-capture add-then-log path also show the meal completion card (intake chips + "Change time")?
- [ ] **B-063** — approve the Tier-2 design-principles line (tone-aware "moment" — calm, never festive on a worrying event); awaiting PM sign-off before writing.
- [ ] **PR #79** — env fail-fast guard + CLAUDE.md doc fix: mark ready/merge or close.
- [ ] **Vet report — PM next actions (2026-07-02):** (1) **email your vet the Nyx report + book the appointment** (the real-vet signal, parallel to the build); (2) **weigh Nyx** before the visit (fills the one empty box + the datum the vet most wants).
- [ ] **Principle 6 doc-drift (Tier-2 edit)** — `nyx-design-principles-v1_0.md` §6 **and** `nyx-technical-spec-v1_0.md` §7 AC both still list "severity averages" as desired report content; the mock + spec §8.4 deliberately use frequency-over-severity (Dr. Chen's stated preference). Both cold reads flagged it. Approve the doc-line update on **both** files (+ the §7 must-carry-section / HTML-first / PDF-library-reshape edits the spec §13 flags — all flagged, not written).
- [ ] **Vet-report mock — synthetic-panel feedback logged** (`docs/vet-report-mock-review.md`). PM follow-ups from it: (a) **B&W test-print** the mock (zebra/band gray on a mono laser); (b) decide the spec-level **report-range cherry-pick guard** (disclose events outside a custom window); (c) greenlight the **second mock — cat/safety-led case** (Sam, feline 48h window) to exercise the safety-leads slot. The real-vet R1/R2/R4 panel is the gate (§5 of that doc is reserved for it).

**B-036 pinch-to-zoom (#461) — one product call on device, one irreversible decision still open**
- [ ] **Decision #4 — the zoom floor, wants your eyes on a real device.** `MIN_MAX_ZOOM_SCALE = 2.5` (`lib/photoZoom.ts`) lets a low-detail photo zoom **past** its real-pixel ceiling into interpolation. Reasoning: a dead pinch reads as broken, and magnification helps you *point at* a region in an exam room even when it adds no detail. **Dr. Chen dissents and it is recorded, not resolved** — magnified JPEG artifacts on a suspected-blood / foreign-material photo can read as clinical texture. To check: open an **older** event photo (the remote 1600px path, not one taken today) and pinch. Does the softness at the top end feel honest, or should it stop at 1:1? Strict answer = set the constant to `1`; it's one line, documented at the flip point.
- [ ] **Option C — raise the 1600px capture cap for text-bearing photos?** The only **irreversible** piece: `MAX_EDGE_PX = 1600` / q75 means every photo already stored is capped forever, so a change only helps future captures. This is why #461 does **not** fix the ingredient-list case (food photos are 1600px *and* signed-URL-only, so also network-required in a clinic). Changing it alters what `analyze-vomit` / `analyze-stool` / both extractors see → `adversarial-reviewer` item, not a gesture change.
- [ ] **Ten seconds: does the vet report already pinch-zoom?** Its viewport is `width=794` with no `user-scalable=no`/`maximum-scale` and Android defaults `setBuiltInZoomControls` true, so it likely already works. It has the most headroom of any surface (1000px q72 rendered ≤340px tall, ~3×) and is the surface Dr. Chen actually reads. Unverifiable in a cloud session; if it *doesn't* work, the fix is a `render.ts` viewport tweak riding the next `generate-report` deploy.

**Cleanup / infra**
- [ ] **B-128(b)** — defense-at-rest `BEFORE INSERT/UPDATE` trigger on `medication_items` (own schema PR; run the backfill pre-check first). Not urgent — the consumer-side guard is live via #200.
- [ ] **Redeploy `generate-signal` to un-gate chronicity detector ⑦ (B-182)** — PRs 1–3 (#246/#247/#250) all merged; the engine registers ⑦ but the **live function predates it**, so chronicity won't surface until a redeploy. Deploy from merged `main` via the Supabase MCP (bundle `scripts/deploy-edge.sh generate-signal` → `deploy_edge_function`, preserve `verify_jwt=true`, byte-gate + boot smoke-test — the B-082 path). The client renderer is live as of #250, so the card renders the moment the cache carries a `symptom_chronicity` finding.
- [ ] **D2 ratification — chronicity `minEpisodes` 6-vs-5 (Dr. Chen)** — the §7 #14 noise gate forced 4→6 (specificity); the PR-1 adversarial review measured a false-negative cost at 6 (a once-weekly-×5 / q2wk-×4 real chronic course is silenced). 6 = credible-surface; 5 = catch-the-once-weekly, defensible because the safe error direction for a safety lane is toward firing. A property test on *real low-count chronic courses* should sit alongside the noise test before the floor locks.
- [ ] **Revoke the Supabase personal access token** (`nyx-cli-deploy`, 2026-06-07) — account-level, lives in a session transcript. Now obsolete: B-082 (#208) made backend deploys a token-free MCP path, so nothing uses it.
- [ ] **Supabase CLI dev-dependency** — fold `supabase@^2.102.0` (on branch `claude/epic-volta-H8d6o`) into a PR so it survives merge.
- [ ] _(awareness, no action)_ **B-074** — the free-fed exclusion fails safe but a single stray free-fed day landing on a selected control day can silence a real correlate.
- [ ] _(awareness, before treating vet-report §8.6 as fully closed)_ **B-115 residual → B-163** — the shipped exact-ms treat-collapse (#219) guards the diet-confounder over-count for the **EXIF-collision / future-batch** case only; today's full-precision `new Date()` stamps mean a **rapid per-tap handful (distinct ms) is still over-counted**. Fully closing it needs a real-data-gated near-window widening (under-count tension) or a batch-log path — Phase-2/PR-6 can proceed; just don't read §8.6 as "fully resolved."

---

## Runtime in Use

**TestFlight (real iOS builds) — primary on-device target since 2026-06-07.** Runtime B (Metro `npx expo start --tunnel` + the **custom dev client** — Expo Go retired as of W2/SDK 57, transition window until W3; see `docs/dev-handoff-runbook.md`) remains available for fast local iteration.

**Installed TestFlight build: 1.1.0 (35), 2026-07-25** — the first binary since SDK 57, the first carrying the widget + App Group, the first iPhone-only, min iOS 16.4. Previous: 1.0.0 (34), 2026-07-18.

**Cut a new TestFlight build** (verified build 22 from `main`, 2026-06-12) — one command builds the store-signed binary and submits that same artifact:
```
eas build --platform ios --profile production --auto-submit
```
**OTA to the installed build:** `eas update --branch production` (the build's channel is `production`, NOT `preview`).

**Traps (each cost a session — do not repeat):**
- **SDK 57 OTA fence (W2, 2026-07-24):** `version` bumped 1.0.0 → 1.1.0 so SDK-57 bundles never reach the installed SDK-54 TestFlight build (`runtimeVersion.policy: appVersion`). Until a fresh binary is cut, `eas update --branch production` is a harmless no-op against that build; the **first post-W2 TestFlight cut must be a native `eas build`**, never OTA. Never revert the version to "fix" a non-arriving OTA — an SDK-57 bundle on the SDK-54 binary is a crash-on-launch. Full note: `docs/dev-handoff-runbook.md`.
- Never build TestFlight with the `preview` profile — it's `distribution: internal` (ad-hoc), never store-submittable. Use `production`.
- Never submit with `eas submit --latest` — it skips internal builds and re-uploads a stale store build (the "build number 8 already used" loop). Use `--auto-submit` (binds the upload to the binary just built).
- "Build number N already used" = EAS counter is behind App Store Connect → `eas build:version:set --platform ios`, then rebuild. (Build numbers live on EAS; `appVersionSource: remote`, `autoIncrement`.)
- All 3 `eas.json` build profiles carry the `EXPO_PUBLIC_SUPABASE_*` `env` block (#90) — EAS cloud builds never see the gitignored `.env.local`; don't strip it (was the first crash-on-launch).
- `app.json` → `ios.infoPlist.ITSAppUsesNonExemptEncryption: false` (#121) auto-answers Apple's encryption questionnaire; keep it committed.

---

## Recent Sessions

**Moved to [`docs/sessions/`](docs/sessions/) — one file per session (2026-07-25).** Kept out of this file because a shared, prepend-ordered list is a guaranteed merge conflict between parallel sessions; a new file per session is a guaranteed non-conflict. Nothing was discarded — every entry that was here became a file in that directory, including the three that landed on `main` while this change was in flight.

```bash
ls docs/sessions/ | sort -r | head -10
```
