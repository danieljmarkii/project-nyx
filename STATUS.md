# Project Nyx — Status

_Canonical answer to "where are we?". High-churn: update inline at session end and any time these change mid-session. CLAUDE.md is the stable operating manual; this file is the volatile state. **Keep it scannable** — prose narrative and build history belong in PR descriptions + git, not here (the file is reconstructable via `git log -p STATUS.md`)._

_**Size budget (enforced at `/wrap`, added 2026-07-19 workflow retro):** target under ~200 lines. Recent Sessions ≤ ~10 one-line entries; **no "Previous:" narrative archive** at the top (it duplicates Recent Sessions); **completed PM action items are deleted, not left checked** — `git log` is the archive. If a section is growing unbounded, that's the signal to prune, not to keep appending._

**Last updated:** 2026-07-24 — **Widget PR W5 — the widget itself.** `expo-widgets` + the `systemMedium` CulpritWidget: round-3 states 1–4, the in-place flip, undo, deep links, and the D5 pet-slot binding through `pets-index.json`. The architecture finding that shaped it: the extension evaluates a **stringified, self-contained** layout in a bare JavaScriptCore context (no imports, no filesystem, no network), so W4's intents cannot run there — a tap lands in an **outbox** in the widget's own props and the app drains it back through those same intents with the widget's own tap-time ids. Flip latency is therefore extension-local (§4.1 Q2 answered from the module's source; the on-device measurement is still the PM gate). Client-only, no schema, no deploy — but **not OTA-able**: needs a dev-client/TestFlight build. _(Previous: 2026-07-24 History filter UX — see Recent Sessions.)_

---

## Current Phase

**Step 10 — AI Signal (`generate-signal`).** B-045 (3-step plan, no Step 4) shipped & merged:

| Step | What | Status |
|---|---|---|
| 1 | Deterministic detection engine (`detection.ts`) — case-crossover (B-050) + intake-decline flag | ✅ #72 (29 tests) |
| 2 | `generate-signal` Edge Function — detect → curate → Haiku-phrase → cache findings set (24h TTL); migration 015 (`findings jsonb`) | ✅ #74 (migration #73) |
| 3 | `SignalZone` wired to cached findings (per-type renderer, confidence tags, tap-to-expand, live/building/stale/`no_pattern`, async regen) | ✅ #75 |

**Architecture:** deterministic detection + LLM phrasing — the server computes & ranks a *true* finding in `detection.ts`; Haiku 4.5 only renders copy, with a deterministic template fallback. `phrasing.ts` = pure phrasing/curation/guardrails (offline unit-tested); `index.ts` = I/O shell. `validatePhrasing` rejects model drift (no `!`; never-reassure / never-"picky" on safety; associational-only on correlation).

**Signal engine — detectors live (deployed `generate-signal` v25):** ① case-crossover, ② intake-decline, ③ reflection (presence/counts; a *declining* trend routes to safety), ④ symptom-worsening lane (B-077), ⑤ post-prandial timing (B-078), ⑥ time-of-day clustering (B-079), ⑦ symptom-chronicity safety lane (B-182 — live v23, confirmed on real data 2026-07-01); + no-signal coverage diagnostics (B-053, incl. **B-070** ≥80%-dominance staple-washout + treats-vs-meals copy register, live v21); + **medication confounders** (B-117 PR 9 #207 — a drug on-board in a symptom window enters `detectCorrelations` as a confounder: case-enriched → suppress, concordant → cap at Early; **+ B-156 PR C1 #234 reads the dose↔vehicle pairing — a drug-in-food combo is attributed to the drug, the vehicle food's protein dropped from the exposure set, + the B-174 in-doubt-dose reconciliation; adversarial-PASS, **deployed v22** read-back-sha-verified + boot smoke-test**). Open follow-ups: **B-052** write-time protein normalization (read-time half shipped #92); **B-067** reflection/Trend dedup; **B-080** diet-structure placement (blocks descriptive Phase 3, PM call). **⑧ `incident_red_flag` (B-340) LIVE at v25** — engine (#395) + client renderer (#396) shipped, and `generate-signal` **redeployed to v25** (deploy gate lifted: #396 client on-device + `!inner` query smoke-tested). Ranked at the top of the safety band; reads `event_ai_analysis` via the repo's first `!inner` join, derives the flag from owner-editable structured fields (override-aware), escalate-on-presence/never-reassure. The authenticated end-to-end render is verified on-device (PM QA, pending).

**On-device QA gap:** empty / `no_pattern` + reflection paths verified on device; the **LIVE safety-card path is now verified** — B-182 chronicity fired on Nyx's real data (2026-07-01 QA), the first live safety card seen on device. The correlation/intake safety lanes still legitimately yield zero on Nyx's healthy data (chicken ~3×/day staple → case-crossover washes it out; intake healthy → flag quiet), so those specific lanes remain unexercised live.

**After Step 10:** Step 9 (vet report) resumes. **Design + spec updated this session (2026-07-02):** discovery (#214) → v1 mock (#216) → **`docs/nyx-vet-report-requirements.md` — now build-ready v2**, folding in the **v3 + cat design mocks** (`vet-report-mock-v3.html` calm diet-trial dog; `vet-report-mock-cat.html` safety-led cat; v1 `vet-report-mock.html` + v2 kept for comparison), a **9-lens synthetic panel review** (Dr. Chen cold-read = CLINIC-READY, fix list applied), and a **real-data dry-run against the pet Nyx** (23 real vomits → the report renders *safety-led*: chronicity + a possible-foreign photo; validated the design end-to-end + surfaced §7.1 data-layer reqs — empty weight state, owner/neuter schema gaps, AI-status denominators, de-dup, free-fed, 90-day fallback). New decisions: **present-only phenotype**, **photos split-by-type** under mandatory §8 privacy controls, **weight (B-186) + empty state**, **stool characteristics**, **medications & supplements**, **branding = keep letterhead / kill footer marketing**. **§12 PR plan REORDERED per PM: Phase 1 full functional report → Phase 2 publish+share+photos → Phase 3 seamless-sharing niceties.** Preconditions **ALL RESOLVED** (B-044 #218 / B-115 #219 / B-028 #220). **Real-vet gate reframed (PM): n=1-first** — PM emails the real Nyx report to their own GP + books a Nyx appointment, in parallel with the build; the first real-vet reaction lands **before Phase 2**. **§8.2 HTML-first RATIFIED (#260).** Phase 1: **PR 1** (`report.ts`) shipped #261, **PR 2** (`render.ts`) shipped #262, **PR 3** (clinical must-carry hardening + B-213 intake-decline duration + traceable meal appendix) shipped #263, **PR 4** (diet/weight/stool + WSAVA hardening — 7 findings fixed across cold-read round-2 CLINIC-READY + adversarial round-3; the GP-0 confound note now names standing/overlapping interventions, free-fed contamination surfaces in Appendix C + B under a trial, off-diet double-counts closed) shipped #264 — all pure, `deno test`-covered, adversarial + cold-read-gated. **Phase 1 (the full functional clinical report) is COMPLETE.** **Phase 2: PR 5** (owner-facing MVP — authenticated `generate-report/index.ts` I/O shell + in-app WebView + on-device PDF share) shipped **#265** (`rls-privacy-reviewer` PASS on the authenticated boundary; `code-reviewer` fix-before-merge all applied; `generate-report` deployed, NEW fn `verify_jwt=true`). **2026-07-03: the FIRST REAL on-device artifact landed + a four-agent panel review + feedback round 1 (bug fixes) shipped** — see the Last-updated entry above + `docs/research/2026-07-vet-report-first-artifact-review.md`. **Phase 2 shipped since (corrected 2026-07-06 — this section had drifted; git is the source of truth):** the §5 PM decision menu was ratified (#267) → **B-221 round-2 design changes** (#268) → **feedback round 2** (severity cut, appendix reorder, wet-meals surfaced, protein-over-time chart, jargon + design pass — #273); **B-222** owner-facing range control (Default + Custom) shipped **#276**; **PR 7 — ALL incident photos in the authenticated report + PDF** shipped **#278** — every photographed in-window incident renders into Appendix E and bakes into the HTML/on-device PDF, safety-flagged (blood/foreign) photos also lead the safety band; `index.ts` fetches each photo ONLY through the EXIF/GPS-stripping + downscaling Supabase image transform (never the raw original), embeds as a `data:` URI (no signed URL persisted), service-role reads only paths from the RLS-scoped enumeration of the verified owner's pet; owner *visibility* = `photo_count` → `app/report.tsx` "Includes N photos". Gates: **`rls-privacy-reviewer` PASS**, **`vet-report-cold-read` CLINIC-READY** (fixed 2 bugs it caught — Appendix-E analysis↔photo divergence disclosed; chronicity `daysSinceLastEpisode` UTC→local, B-246), **`adversarial-reviewer` PASS**; no schema. **Deployed live as `generate-report` v8** (#281, byte-verified; folds in the B-222 zoom fix). Follow-on hardening from PR 7's rls-privacy pass: **B-244** (bind `storage_path` to pet + owner-scope the bucket read) shipped **#282** (CHECK applied to prod 2026-07-05) and **B-218** (drop the `vet_reports_public_share` anon-enumeration policy) shipped **#283**. **Phase 1 + the owner-facing MVP + authenticated photos are COMPLETE and LIVE.** **Genuinely next:** the PM **real-vet R1 email** (emails the regenerated Nyx report to their GP + books the appointment — a PM action; gates the irreversible public path); **B-243** — the interactive **"tap to exclude any photo"** pre-send review (PR 7 shipped owner *visibility* and deferred the interactive deselect; additive, no schema); then **PR 6** — the public token `view-report` route + revocable everyday-photo delivery (split-by-type / token-streaming) + owner link-revocation (B-143), the first unauthenticated path (`rls-privacy-reviewer` MANDATORY; its **B-218 blocker is already cleared** — #283). See `nyx-vet-report-requirements.md` v2.1 (2026-07-04) + §12.

---

## Parallel Tracks

### ⭐ Ask (B-228) — THE NEXT MAIN PROJECT (PM 2026-07-18) — server chain A1→A4 shipped; A6 rundown shipped; A5 (client) next
Spec `docs/nyx-ask-requirements.md` **v2.2** (build-ready — **no PM blockers**: D2 ratified expanded, D7 = "Ask" placeholder by choice, **D8/D9 conversation-grain fixes 2026-07-18**) + design-locked mock `docs/ask-mockups.html` + evidence `docs/research/2026-07-ask-ai-ux-landscape.md`. Owner-initiated **answer-card** Q&A over the pet's own record (not a chatbot): Sonnet plans over a **closed, read-only tool set** → deterministic compute (floors + denominators, `deleted_at` contract, G5 Timeline-parity — extended to reads) → validated phrasing (`validateAnswer`: never reassure/diagnose, numbers-⊆-tool-results) → typed 200s. Escalation is **relay-only** (leads the answer when the engine fired; never minted). **Boundary (D2 ratified):** the full record scoped to the question — notes + photos in, via **scoped retrieval** (no bulk tool) + **transform-only** photo access + the **one-read-path** (live reads reuse `analyze-vomit` + its cap). Flags: `ask_enabled`/`ask_general_enabled` via the new zero-schema **allowlist convention** (PM-only dogfood first); caps ride shipped `ai_usage` (`ask_conversation` monthly / `ask_message` daily); born-Premium wiring at Track-3.

| PR | What | Status |
|---|---|---|
| A1 | Config-seed migration (2 flag keys, allowlist shape; own PR) | ✅ shipped via #398 (draft) — applied live |
| A2 | Allowlist flag primitive (client decode + server resolve; bool back-compat) | ✅ shipped via #401 (draft) — `code-reviewer` ship-ready; deno tests un-run (no deno in env) |
| A3 | `ask/tools.ts` deterministic tool layer (D2 whitelist enforced in types) | ✅ shipped via #402 (draft) — code-reviewer + adversarial-reviewer both run/addressed; 40 deno tests |
| A4 | `ask` Edge Function (plan-loop + validator + deflections + caps) — **adversarial + rls-privacy MANDATORY** | ✅ code shipped via this PR (draft) — both gates PASS; 102 deno tests; **DEPLOY pending** (verified 69 KB bundle, new fn `verify_jwt=true`, ship-dark) |
| A5 | Client surface (`app/ask.tsx`, header pill, answer-card renderer) | ⬜ |
| A6 | Vet-visit rundown (deterministic, client-only, works capped/offline) — **parallel-safe with A3/A4** | ✅ shipped via #406 (draft) — `lib/rundown.ts` + `app/rundown.tsx` (`/rundown`) + `RundownTileRow`; clinical-guardrails/pm-feature-review/code-reviewer all run/addressed; 36 tests; jest 1442. Entry lands with A5 (S5). |
| A7 | Copy/safety hardening + the general-mode flip-on review package | ⬜ |
| A8 | Live photo reads via the `analyze-vomit` machinery (transform-only; shared cap; clinical-guardrails) — adversarial + rls-privacy MANDATORY | ✅ shipped via #409 (draft) — both gates PASS (adversarial: 4 rounds to CONFIRMED-CLOSED; rls-privacy: PASS, residual closed); deno 129 (ask+shared) + 89 vomit/stool regression unmodified; jest 1491. **DEPLOY-GATED:** redeploy `analyze-vomit` + `analyze-stool` FIRST (honor `transform_only`, default-off = behavior-neutral), THEN `ask`. |

### Home Screen Widget (B-289/B-290/B-291) — W1–W5 built; W6 (TestFlight cut) next
Spec **`docs/nyx-widget-requirements.md`** v1.0 (D1–D9 ratified) + design-locked mock `docs/culprit-widget-mockups.html`. Jobs: top-of-mind + when-in-doubt-app-it-out. v1 scope = meals+treats, free, no AI/med capture. Plan: **W1 ✅** `logged_via` migration (038, #422, applied live) → **W2 ✅** SDK 57 upgrade (#423; **on-device regression = open PM QA**) → **W3 ✅** B-290 App-Group write path (#425; rls-privacy PASS, spike answers in the PR) → **W4 ✅ shipped via #426 (draft)** — App Intents (`logMealIntent`/`logTreatIntent`/`topUpBowlIntent`, inbox-first + owner-JWT REST leg) + the pure resolution lib (learned slots — no declared slot model exists, B-040 is free-choice-only; slot→named-food w/ stability floors; trial override; treat shortlist; trial day) + the read-only extension session reader (`lib/widgetSession.ts` over `secureStoreTiers.ts`) + `bowl_topup` (arrangement re-attest, never intake). **D5 SETTLED (Dir.-of-Eng recommend-and-proceed): option (a)** — build-time "Pet slot" enum (6) resolved through the app-published sticky/tombstoned `pets-index.json` (B-086 anti-silent-switch); option (b) = the named-picker upgrade path if slot labels confuse at W6 → **W5 ✅ shipped via #428 (draft)** → **W6** TestFlight cut. Parallel to B-288 and the Ask track. Note: the first dev-client/TestFlight build from post-W3 `main` picks up the App Group entitlement — verify app + (later) extension resolve the same container on-device.

**W5 — the architecture finding that reshaped §4 (needs a PM read).** `expo-widgets` does not run app JS in the extension: a layout is a **single self-contained function**, stringified by babel's `'widget'` directive and evaluated in a bare JavaScriptCore context (only `@expo/ui/swift-ui` + modifiers + a JSX shim — no imports, no filesystem, no network). Two consequences: **(1)** the picker flip is entirely **extension-local** — the interaction intent re-evaluates the layout, merges the returned props patch into the stored timeline entry, and reloads; the app is not in the loop (§4.1 Q2 answered from the module's own source; the *latency measurement* is still the on-device gate). **(2)** W4's App Intents **cannot execute in the extension**, so a tap is captured into an **outbox** in the widget's own props and the app replays it through those same shipped intents (`lib/widgetBridge.ts`) carrying the widget's tap-time ids — the id-keyed idempotency and honest `occurred_at` survive the hop. **Product consequence to weigh:** a widget capture is durable at tap time but becomes a *row* when the app next runs (instantly if it's alive — the interaction event drives a drain; otherwise on next foreground). §5.1's "no app launch" holds for the *tap*; the record's freshness now depends on the app running, which is the same shape as the offline path (§5.6) but is a §4 wording change worth ratifying. Undo is **state-based, not a 60s timer** (a widget gets no guaranteed timed re-render; a "tap to undo" that silently stopped working would be worse) and is honored on both sides of the drain — post-drain it soft-deletes.

### B-351 Multi-protein capture (Phase A) — PR 1 (schema) shipped; slices 2–5 now parallelizable
Spec `docs/nyx-multi-protein-requirements.md` v1.0 (D1–D8 ratified 2026-07-19) + mocks `docs/nyx-multi-protein-mockups.html`. **PR 1 ✅ shipped via #424 (draft)** — `food_items.proteins TEXT[]` (migration 039, **applied live**, backfill canonical-parity verified on all 16 distinct values; advisors clean) + `food_items_cache` mirror + `refreshFoodCache` + both pre-sync push carriages + the `lib/protein.ts` encoding helpers. Remaining Phase A (§11, disjoint files, can fan out): **2** extraction emits ordered `proteins` (absorbs B-048) · **3** two-line D8 picker · **4** Tier-1 disclosure + Tier-2 trial-contaminant flag · **5** vet-report render (`vet-report-cold-read` gate). Phase B (slice 6, correlation engine) is sequential + `adversarial-reviewer`-mandatory + deploy-gated.

### B-284 Culprit in-app brand alignment — N1 (tokens) + N2 (mark) + N2b (Landing) + N3 (loading system) + N5/N5b (calendar) shipped; N4/N6/N7 next
Spec `docs/culprit-in-app-brand-requirements.md` (v1.0, §10 = PRs N1–N7). Four locked rules (carve / register / one-accent / no-metaphor + motion budget). Night grounds appear **only where the app is working on the pet's behalf** (Landing, loading, the night moment, the Signal-card night variant) — capture & records stay the shipped light system. **Zero new dependencies** (`react-native-svg` + `expo-linear-gradient` already shipped).

| PR | What | Status |
|---|---|---|
| N1 | Night-surface tokens — 10 additive `theme.ts` entries (§2) | ✅ #321 (additive; no repoints; receipts verified; `tsc` + 1079 jest green) |
| N2 | `CulpritMark` (+ HomeHeader / AuthBrandMark) | ✅ draft #325 (carve mask-cutout, live pulse, header tap-to-scroll; `code-reviewer` caught + fixed a pet-switch race + a duplicate a11y node; new `store/signalMarkStore.ts` + `hooks/useReducedMotion.ts`; `tsc` + 1105 jest green) |
| N2b | Landing hero (+ full-bleed starfield) | ✅ #329 (`NightHeroGround` full-bleed starfield + carved moon) |
| N3 | `WhorlSpinner` + skeleton sweep + PTR + night moment | ✅ #333 (native-driver **View-transform** whorl [not `<G>` rotation — renders frozen], sm/md/px, day/night, `tint`; `Skeleton` Tier-1; ~30-site ActivityIndicator sweep; `NightGround`+`NightMoment` at cold-start/report/photo-extract; Home PTR band; `useAppActive`; `colorWhorlRidgeDay`. `code-reviewer` fix-before-merge ×4 applied; D7 scale/opacity + whorl-actually-rotates = on-device QA; CulpritMark same gap → B-322, **fix built this session (View-transform port), on-device confirm pending**) |
| N4 | Signal card (+ `SIGNAL_NIGHT_GROUND` on-device gate, D8) | ⬜ next (ships flag-false) |
| N5 | Calendar v3 — **presentation slice** (count-pips replace the heat-ramp + summary line + a11y) | ✅ #326 (pips + summary) |
| N5b | Calendar plumbing — month paging + drill-in + History day-filter (B-308/B-309) · symptom selector + "Calendar" rebrand + intake "Meals" lens (B-310) | ✅ #330 (paging + drill-in + single-day History filter) + #331 (B-310 selector/rebrand/intake "Meals" lens); coverage-gap (B-312) + med-colour token (B-311) still queued |
| N6 | Completion moment (ping + trial tick) | ⬜ |
| N7 | Home briefing (+ Tier-2 `design-principles.md` §3 edit, D9) | ⬜ merge-gated on §3 sign-off |

**N5 is disjoint from N2–N4** (calendar files vs. brand-mark / loading / signal surfaces) → ran as a separate concurrent session. **N5b is the deferred plumbing:** the mock's `pm-feature-review` (Jordan/Sam) caught that the buckets are symptom-only (no per-day all-event coverage, no multi-month, one auto-picked symptom), so paging + drill-in + coverage + selector each need a query/nav change — split out (B-308/309/310/311) with their designs locked to the PM's A/A/A calls.

### B-283 "You" (profile/settings) screen (app-store readiness) — PRs 1–4 shipped; PR 5 (go-live flips) next (external-gated)
Spec `docs/nyx-settings-requirements.md` (v0.1, §10 = 5-PR plan) + mockups `docs/settings-mockups.html`. Homes the App-Store items that assume an in-app settings surface (B-229/230/231/270) + the support email (B-273), and relocates the *user* actions (owner name / Sign out / Delete account) off the pet-scoped Pet tab. Entry = **avatar in the Home header** ✓PM; name = **"You"** ✓PM. **Zero schema** in every PR.

| PR | What | Status |
|---|---|---|
| 1 | Support + version primitives — `lib/support.ts` (`buildSupportMailto`, `formatAppVersion`) + `constants/links.ts` (`SUPPORT_EMAIL`, legal URLs, `LEGAL_LINKS_ENABLED=false`); pure, unit-tested, no UI | ✅ #315 (`code-reviewer` ship-ready; +17 tests) |
| 2 | The "You" screen (`app/settings.tsx`) + Home-header avatar entry + relocation off `app/(tabs)/profile.tsx` + route registration | ✅ merged #316 (`code-reviewer` fix-before-merge + `pm-feature-review` folded in; +7 owner tests) |
| 3 | Notifications (mocked — honest not-live state, **no armed med reminder** per D7 safety gate) | ✅ merged #317 (`code-reviewer` ship-ready; +shared `ComingSoonLabel`) |
| 4 | App feedback (`mailto` composer reusing PR 1's helper, `[Feedback]` tag) | ✅ merged #318 (`code-reviewer` ship-ready · `nyx-voice` ✓ · `pm-feature-review` SHIP-SHAPED after mail-hand-off fixes; +4 tests; S1 = reuse `support@`+tag) |
| 5 | Go-live flips (`LEGAL_LINKS_ENABLED` on when hosted docs land; real notif wiring; feedback backend) | ◐ **legal flip SHIPPED via #362** (docs live 2026-07-16; Privacy/Terms rows live + new Veterinary-disclaimer row); notif wiring (push-provider decision) + feedback backend remain external-gated |

**All build PRs (1–4) shipped** (PR 3 #317 · PR 4 #318; S1 resolved — reuse `support@` + `[Feedback]` tag). **Only PR 5 (go-live flips) remains** — external-gated (`LEGAL_LINKS_ENABLED` on when hosted docs land; real notif wiring on the push-provider decision; feedback backend if adopted), not a same-session build. Open PM sub-decisions from PR 4's `pm-feature-review` (spec §9): **B-297** (feedback via `expo-mail-composer` vs. raw `mailto`, applies to Contact support too), **B-298** (copyable no-mail fallback), **B-299** (in-composer support-vs-feedback reminder); plus S2–S4.

### B-251 Onboarding experience revamp (app-store readiness) — PRs 1–10 shipped; the flow is COMPLETE end-to-end; only PR 11 (functional social auth) remains
Spec `docs/nyx-onboarding-requirements.md` (v1.3, design-locked; §9 = 11-PR plan) + committed mockup `docs/onboarding-flow-mockups.html` (v3). Burn-down of v0.1 (2-screen name+species) → Signal-led **Landing** (logo + persistent auth + data-rich previews) → account (owner first/last/email; Apple/Google **mocked**, email functional; TOS/privacy mock) → pet setup (type+name **required**; breed/gender/age **skippable**; dual integer-OR-birthday age) → **mocked** paywall → completion + durable `onboarding_completed_at`. **PRs 1–4 shipped (#288 / #287 / #286 / #289):** PR 1 owner-profile schema (migration `027` + `updateOwnerName` helper) · PR 2 age-precision schema (migration `028`, resolves S2) · PR 3 design-system primitives (`TextField` + `PrimaryButton loading`) · **PR 4 onboarding shell** — the durable `onboarding_completed_at` gate (pure, unit-tested `decideOnboarding` §6 seam) replacing the fragile "≥1 pet" inference, `ProgressBar`, and the `_layout` back-nav chrome; `hooks/usePet.ts` rewritten (two-read orchestration + retry/never-false-onboard-on-error/throw guards, hook wiring test); `code-reviewer` fix-before-merge FIXED; no schema. **PR 5 shipped via #290:** the Signal-led **Landing** — `app/(auth)/index.tsx` (logo + a horizontally-paging stack of 3 value previews **leading with the Signal** + tappable ≥44pt page-dots as the a11y swipe-alternative + a pinned Create-account/Log-in) + `components/onboarding/ValuePreview.tsx` ×3 (Signal/log/vet-report, **built from real component styles** — elevated surface, Newsreader headline, `EventIcon` tiles, accent sparkline); `app/_layout.tsx` routes unauthenticated → Landing (B-039 just-deleted → login preserved); `PrimaryButton` gains a teal `accent` variant (PM-ratified over near-black, B-256). Reviews: nyx-voice ✓ / pm-feature-review SHIP-SHAPED / code-reviewer fix-before-merge applied (de-causalised the Signal sample per `generate-signal` `CAUSAL_RE`; page-dot 44pt). No schema; tsc + 942 jest green. Follow-ups B-257/B-258. **PR 6 shipped via #291:** account creation + soft verify — `app/(auth)/signup.tsx` reworked onto `TextField`/`PrimaryButton` (first/last/email/password + calm inline validation; mocked TOS/Privacy; Apple/Google hidden behind `SOCIAL_AUTH_ENABLED`; soft "check your inbox" state w/ resend + edit-email back-escape + proceed→login). `supabase.auth.signUp` → session-present writes first/last + derived `display_name` via `updateOwnerName` → `/onboarding/pet`. New `constants/flags.ts` + pure `lib/authValidation.ts`. Reviews: `rls-privacy-reviewer` PASS / nyx-voice ✓ / code-reviewer fix-before-merge applied. No schema; tsc + 959 jest green. Follow-up: **B-152** (harden the soft-verify path + 2 rls dashboard checks before email confirmation is re-enabled). **PR 7 shipped via #292:** the two REQUIRED pet screens — `app/onboarding/pet-type.tsx` (Cat/Dog tiles, no default → explicit tap → Continue) + `app/onboarding/pet-name.tsx` (`TextField` → inserts `{user_id, name, species}` → Home; PRs 8/9 UPDATE breed/gender/age onto the same row) + shared `components/onboarding/OnboardingHeader.tsx` (`canGoBack()`-aware back + `ProgressBar` steps 1–2 of 5) + new transient `store/onboardingDraftStore.ts` (type+name survive back-then-forward; reset on sign-out via `wipeLocalSession`, FR-9 parity). `usePet.ts` + PR 6's `signup.tsx` redirect repointed `/onboarding/pet` → `/onboarding/pet-type`; v0.1 `pet.tsx` + orphaned `food.tsx` deleted (enacts D3 — no food step). Reviews: nyx-voice ✓ / Designer (P1/P5) ✓ / Jordan + Sam ✓ / code-reviewer fix-before-merge applied (draft-store back-then-forward + null-species escape hatch + `radiogroup`). No schema. **PR 8 shipped via #293:** the two SKIPPABLE optional pet screens — `app/onboarding/pet-breed.tsx` (`BreedPicker` species-filtered + free-text "Other"; Continue UPDATEs `breed`, Skip → null) + `app/onboarding/pet-gender.tsx` (Male/Female `ChipGroup`; Continue UPDATEs `sex`, Skip → `'unknown'`, the insert default); `pet-name` now **pushes** `/onboarding/pet-breed` with an **idempotent** insert-or-update (no duplicate pet on back-nav); `OnboardingHeader` gains an optional prominent Skip (`onSkip`/`skipLabel`/`skipDisabled`); breed field-state seed extracted to `resolveBreedFieldState` (shared with `EditPetModal`). Reviews: nyx-voice ✓ / Designer (P1/P5) ✓ / code-reviewer fix-before-merge applied (Skip-vs-Continue save race) / pm-feature-review (follow-ups B-259/260/261). **Closes B-210**; no schema; tsc + 977 jest. **PR 9 shipped via #294:** the age screen — `app/onboarding/pet-age.tsx` (segmented Age/Birthday toggle; Years/Months numeric fields → approximate DOB anchored `today − duration`; `DateTimePicker` birthday → exact; "Not sure? Skip") + pure `lib/age.ts` (`ageToDob`/`birthdayToDob` + `formatAge`/`formatBirthdayField`, 25 tests). Gender repointed to push `/onboarding/pet-age`. **Honesty gate (S2):** approximate DOB never a witnessed birthday — enforced in `lib/age.ts`, applied on Profile (`~2yr`), `EditPetModal` (honest field + precision-preserving write), and the vet report (`generate-report` drops `(b. YYYY)` for approximate). Data/Dr. Chen honesty ✓; nyx-voice ✓; code-reviewer Ship-ready (fixes applied — `resolveDobPrecisionOnSave` extracted+tested, token fix; B-262 wheel-vs-field deviation). No schema (writes 028's column). tsc + 1011 jest + generate-report deno (160). **`generate-report` deployed v9 (2026-07-06)** from the PR-9 branch — the DOB-precision fix + the pending vet-report render work (B-222) are now live; verified (v9 `ACTIVE`, `verify_jwt` preserved, boot smoke-test clean 404 + 401). **PR 10 shipped via #295 (paywall mock + completion — closes the flow's interim terminus):** `app/onboarding/paywall.tsx` (non-functional dark premium mock — no StoreKit; "Maybe later" + a mocked trial CTA both advance; the trial CTA honestly acknowledges Premium isn't live rather than silently no-op; explicit Principle-7 free-tier line; feature bullets reworked to convenience-only after `pm-feature-review` — the mockup's `Multi-pet profiles` ships FREE / `correlations` = care / `90-day history` truncates free trends+report) + `app/onboarding/done.tsx` ("You're all set" → writes the durable `onboarding_completed_at` on mount via `lib/profile.ts markOnboardingComplete()` → `replace('/(tabs)')`; back-gesture-locked terminus) + dark-surface theme tokens; `pet-age finish()` repointed `replace('/(tabs)')` → `push('/onboarding/paywall')`. Reviews: `pm-feature-review` (done SHIP-SHAPED; paywall NEEDS-WORK findings applied → the two blocking product fixes), `code-reviewer` (ship-ready; both NITs fixed), nyx-voice ✓. No schema (writes migration 027's column). tsc + **1022 jest** green. Follow-ups: **B-263** (reconcile the mock's placeholder bullets to the ratified freemium gate), **B-264/265/266** (StoreKit wiring / placement / escape-hierarchy polish). **The onboarding flow is now complete end-to-end (no dead ends) — a build CAN be cut.** **Only PR 11 (functional Apple/Google)** remains — post-v1, needs PR 6 ✓ + a PM-provisioned Google OAuth client. Moots B-038. Gated PM items: `design-principles.md` §Onboarding ratification (doc-drifted — PR 7 removed the food step; spec §5 breed-skip copy also stale) + the Principle-2 softening (spec §12); freemium-gate decision (unblocks B-263); Google OAuth client for PR 11; **B-152**. **STATUS.md is the one shared-file collision to expect at each PR's wrap.** **2026-07-16 (#362):** the flow gained a post-signup **veterinary-disclaimer acknowledgment** step (B-270) — signup → disclaimer → pet-type, server-recorded in `legal_acceptances` (#361); D10's mocked TOS/privacy links are now LIVE (hosted docs shipped), and `usePet`'s needs-onboarding entry lands on the disclaimer.

### B-271 App Review demo account (app-store readiness) — build-ready spec written + D1–D7 ratified; PR 1 (seed script) next
Spec `docs/nyx-demo-account-requirements.md` (submission-guide step 11). **Why:** Culprit is login-gated, so a reviewer in an *empty* account hits only designed empty states and can't evaluate the Signal / Trend / Patterns / vet report — Apple's "unable to evaluate the app's features" rejection; the demo is also Apple's only window into the wedge. **Story (D1, ratified):** a diet-trial dog "Cooper" — a sneaked-in "contraband" protein genuinely correlates with vomiting (flagship ① correlation, Early tier) + a last-2-days intake dip (② `intake_decline`, safety) as a backstop so the Signal card is never empty; both clinically honest (the trial-food staple washes out; never reassures). **Mechanism:** the Home Signal is a server cache (`ai_signals`) → `generate-signal` must be **run** after seeding; Trend / Patterns / Timeline render on-device from hydrated SQLite (B-054). **Seed:** an idempotent, date-relative, **parameterized** SQL script (`scripts/seed-demo-account.sql`) referencing existing global `food_items` only; credentials only in ASC. **D1–D7 ratified 2026-07-11 (#336).** Build: PR 1 = seed script (`adversarial-reviewer` mandatory — "staple washes out / never reassures") → PR 2 = `docs/app-review-notes.md` → live seed + `generate-signal` + verify (PM-gated: the demo account is created via real signup, which needs email-confirm ON, guide step 9). Multi-account expansion = **B-324**. Depends on **B-152** (email confirm ON) + B-054 (done) + B-272 (ASC record, done).

### B-054 Multi-device down-sync (hydration) — v1 COMPLETE
Spec `docs/multi-device-sync-requirements.md`. Phases 0–3 + §6 cold-start UI all merged (#82–#86); migration 016 (`meals.updated_at`) applied live. Server-time LWW, incremental per-table watermark hydration, meal-ghost absence-reconcile. **Remaining: the physical AC-6 logout-wipe on-device gate** (Trust & Safety; code passes). Phase 4 (Realtime) is post-v1/optional. Residual assumption logged B-058.

### B-117 Medication logging — Phases A–C done; Phase D in flight (PR 9 shipped; PR 10 gated on Step 9)
Spec `docs/nyx-medication-logging-requirements.md` (§12 = 10-PR plan). Model = **regimen + dose-events** (mirrors food: `medication_items`≈`food_items`, `medications`≈`diet_trials`, `medication_administrations`≈`meals`+`intake_rating`). Net-new additive schema (migration 020 applied live; 021 med-photos RLS applied; `nyx-medication-photos` bucket created PRIVATE). Configure regimen once → one-tap dose log. Safety: adherence inherits both invariants (n=1 never reassures; refusal→health flag; missed critical-drug→escalate; AI-extracted dose never silently trusted).

| Phase | PRs | Status |
|---|---|---|
| A — Foundation | 1–3 | ✅ schema (#192) · local mirror+sync (#194) · text-first quick-log (#196) |
| B — Photo + library | 4–6 | ✅ bucket+RLS (#197) · capture + `extract-medication-from-photo` + dose-confirm (#199) · picker detail/edit (#201) |
| C — Regimen + surfaces | 7–8 | ✅ "Current medications" card + compliance % (#202) · timeline + retroactive adherence + double-dose B-135 (#204) |
| D — Clinical consumers | 9–10 | ✅ PR 9 Signal confounder pass (#207 — `detection.ts` `medicationWindows`; case-enriched drug → suppress, concordant → cap at Early; adversarial PASS, fixed a Bonferroni family-shrink; residuals B-138; deployed v19) · ⬜ PR 10 vet-report section (gated on Step 9) |

Carry-forwards: B-122/123 satisfied; B-131 honored; B-128(b) defense-at-rest trigger (own schema PR); open sub-decisions B-132 (library delete), B-133 (`is_critical` owner toggle — PM call); **B-160** (med-details helper chips + copy — ✅ shipped #236).

### B-023 Patterns dashboard — PRs 1–4 merged; PR 5 blocked on Step 9
Spec `docs/nyx-analytics-dashboard-requirements.md`. **No schema** (rides existing tables + `ai_signals` jsonb; migration 018 added an additive `summary`). Build gates resolved: §13 #1 name = "Patterns", §13 #6 colour-as-wellness ruling.

| PR | What | Status |
|---|---|---|
| 1 | Aggregate layer `lib/analytics.ts` + shared `lib/protein.ts` | ✅ #155 |
| 2 | Card components + §13 #6 colour ruling | ✅ #158 |
| 3 | Dashboard screen + Home doorways + `ComingSoonSummary` | ✅ #159 |
| 4 | AI summary — **template-only** (Haiku path built but gated off = B-096); migration 018 | ✅ #160, deployed live |
| 5 | Vet-report bridge ("Share with my vet") | ⬜ blocked on Step 9 |

Open follow-ups: B-094 (decline-watch/diet-trial cards), B-095 (correlations/coverage cards), B-096 (re-enable Haiku summary), B-099 (over-time + dashboard range — reopens §13 #2), B-116 (summary↔card grounding). Shipped polish: #162/#163/#164/#185.

### B-086 Multi-pet v1 — COMPLETE
Spec `docs/nyx-multi-pet-requirements.md`. PRs 1–6 all shipped (#144–#148, #203); **zero schema** (`pet_id`/RLS/`is_active`/`feeding_arrangements` were multi-pet-ready by construction). Single-pet households see no new chrome. PR 6 = cross-pet safety banner + all-active-pets signal freshness (adversarial PASS). Household §7 on-device cases fold into the TestFlight QA pass.

### B-040 Free-feeding R1 — COMPLETE
Spec `docs/nyx-free-feeding-requirements.md`. PRs 1–4 shipped (#119/#121/#122/#123); migration 018 applied live. Schema → capture/view → History rendering (ambient strip + boundary markers + stale-only freshness) → engine ingestion (free-fed protein excluded from candidacy, caps others at Early; adversarial PASS). **Guardrail (every consumer):** free-fed intake is NOT directly observed — never read absence of witnessed intake as "didn't eat"; vet report carries "intake not directly observed" verbatim. Remaining Step 9 consumer: vet-report feeding-method rendering. Open: B-073 (stale-nudge on-device QA), B-074 (exclusion can silence a real finding — awareness).

### Food library / intake
- **Picker findability — ratified build queue (2026-07-15, #351):** **B-346** rotation shelf → **B-347** pinned search + chips (executes B-020); sequenced (same `FoodPicker.tsx`), both no-schema, ready to run. Photos held (B-348); blue-sky directions parked behind B-349/B-350. Design docs: `docs/food-picker-findability-mockups.html` + `docs/food-picker-reimagined-mockups.html` (both carry the PM decision record).
- **B-004 standalone Food Library — COMPLETE** (#176–#182, 7 slices). 4th `Foods` tab, brand grouping, reliable-favorites, row thumbnails; picker stays 2-up tiles (PR 7 decision). + post-QA fixes #183/#184.
- **B-102 `human_food` food_format — in progress** (PRs 1–2, #166/#167). Migration 019 applied live + capture/display parity. Remaining PRs 3–7 (AI-extraction mapping, analytics regression, engine provenance, vet-report line, diet-trial note) — PRs 3/4 are disjoint and parallelizable.
- Open QA findings: B-111 (treats in Top Proteins, clinical call), B-113 (picker↔tab harmonization, #186), B-112/B-114, B-108 (closed #183). Downstream: B-017 (`food_format`/`food_type` overlap, carries B-024/B-102), B-009/B-018 dedup.
- **Food dedup & merge — direction aligned + build-ready DRAFT spec (2026-06-24, #237):** `docs/nyx-food-dedup-requirements.md`. Phase 1 prevention (B-009) first, **no schema**; Phase 2 merge (B-018 + B-005) = repoint-then-tombstone. Survivorship/reversibility/multi-user-auth parked as merge-phase decisions. Awaiting PM ratification → re-prioritizes B-009 to Now.

### B-186 Weight tracking — PRs 1–4 shipped (schema + logging + trend card + History row); v1 = display-only COMPLETE
The 2026-06-25 vet-council deep-dive named a weight **trend** the #1 missing datum (Nyx = zero `weight_check` events on a free-choice *weight-management* diet). **Audit (2026-06-26):** confirmed no existing weight surface — `weight_check` enum + `pets.weight_kg` snapshot exist, but **no value storage** (`events` carries only `severity`/`notes`), no logging UI, no trend surface. **v1 = display-only / descriptive (PM-confirmed): capture + trend, NO loss flag** (a flag is clinically load-bearing → its own spec + mandatory adversarial pass). **Guardrail every consumer inherits:** a weight trend never reassures — stable/rising ≠ wellness (rising can be fluid/edema); loss is the danger signal. Independent of the chronicity-lane work (disjoint files; only STATUS.md collides at wrap).

| PR | What | Status |
|---|---|---|
| 1 | Schema/data layer — migration 024 `weight_checks` (1:1 event child, mirrors `meals`) + local SQLite mirror + `LOCAL_WIPE_TABLES` reg | ✅ merged #244 (applied live, advisors clean) |
| 2 | Logging — `EVENT_TYPES` entry + numeric `weight` log step (pre-filled last weight, lbs) + `insertWeightCheck` + sync up/down + `pets.weight_kg` snapshot to latest reading | ✅ this session — PR #245 (was stacked on PR 1; rebased onto `main` after #244 merged) |
| 3 | Profile weight-trend card (descriptive sparkline + readings + delta; neutral framing, no wellness colour) | ✅ shipped via #248 — `WeightTrendCard` (`getWeightHistory`/`computeWeightTrend` in `lib/weight.ts`; "Log a weigh-in" affordance; empty/single/trend states; guardrail held in colour + copy) |
| 4 | History `weight_check` row + icon | ✅ shipped via #249 — `weight_checks` LEFT JOIN in `getTimeline`/`getEventById` (1:1, soft-delete via parent) + `weight_kg` on `TimelineRow`/`rowToEvent`; `EventRow` renders `${kgToLbs} lbs` as a quiet secondary line (Scale icon already via `EVENT_TYPES`, PR 2); display-only, no verdict |

Fast-follows: **Patterns-dashboard weight card ✓ shipped via #251** — `components/dashboard/WeightCard.tsx`, reading-count-aware placement (populated → safety/health-trajectory cluster, empty nudge → descriptive); shared delta/date copy extracted to `lib/weight.ts`. Remaining: vet-report weight section (Step 9); Home TrendZone weight; weight-loss flag spec (B-190). Card follow-ups: B-192 (empty-state coherence), B-193 (4px token).

### B-247 Stool analysis (per-incident AI read) — PR 1 (schema) in review; engine PRs unblocked
Spec `docs/nyx-stool-analysis-requirements.md` (§8 = 10-PR plan). Second child of B-013 (sibling of B-027/vomit): no `analyze-stool` exists today despite stool being the primary diet-trial outcome and the highest-volume GI event type; the vet report's "Stool characteristics" section has no AI-read data source. Additive schema only (new `stool_*` enums + columns on `event_ai_analysis` — **no** `stool_normal`/`diarrhea` consolidation, D1). Bristol Stool Scale (Type 1–7) as the consistency taxonomy (D3); Sonnet 4.6 (D4, matches `analyze-vomit`). **PR 1 (schema) shipped via #379** (draft) — migration `034_stool_analysis_columns.sql`, additive-only; rls-privacy-reviewer PASS + Data Scientist ✓. PM action: `apply_migration` when ready. **D2 RESOLVED 2026-07-16** (PM delegated to team → ratified): build the shared `_shared/incident-analysis.ts` as a **descriptor-driven pipeline** (`runIncidentAnalysis(descriptor, req)` owns auth→gate→context→image→vision→floor→write-back; each type = a small descriptor), so the Nth photo-analysis type (PM's coat-quality riff) = descriptor + migration + one monetization row, no handler copy. Validated by the two real types; coat-quality is the seam's acceptance test, not built now. Per-type escalation floor + mandatory `adversarial-reviewer` + reassurance-regex preserved; PR 2 AC = vomit's tests pass unmodified. Unblocks PR 2 (shared module + vomit refactor). Old draft PR #279 (docs) superseded by this branch.

### Design-system v1.2 "Linear Clean" — COMPLETE
Plan `docs/design-system-migration-plan.md`. 4 PRs merged: palette (#99), fonts Geist+Newsreader (#100), Lucide event icons (#101), completion "moment" (#103) + B-064 folded meal card (#105). Open: B-061 app-wide Geist body rollout (open question — wrapper vs shim); B-063 tone-aware-moment design-principles line (PM sign-off). Whole-system aesthetic on-device QA still due.

---

## Blocking Open Questions

- **~~D-M8~~ — multi-pet tiering: RESOLVED 2026-07-12 same day — PM ruled free-to-3, Premium at 4+ (the team rec).** No open monetization decisions remain; next = the build-ready spec session. Original framing: (a) all multi-pet free / (b) free-to-3, Premium 4+ / (c) gate at 2+. Blocks the Track-3 paywall copy + B-263 bullet list + the completion-screen "add pets anytime" line. Full workshop: strategy doc §18. **D-M1–D-M7 RESOLVED 2026-07-12 (PM ratified, with amendments — doc §13).** Track-2 (B-329 flags → B-001 throttles → B-330 mock flag-off) is now ready-to-run; Track-3 additionally gated on B-332 (manual protein capture). Superseded detail — original bullet: Blocks: the freemium gate (D9/B-263), the Premium wiring track (B-331/B-264/B-265/B-266), and the pre-submission Track-2 PRs (B-329 flags, B-001/ai_usage throttles, B-330 paywall-mock flag-off). Decision set + team recommendations: `docs/monetization-and-ai-gating-strategy.md` §8. The one genuinely-contested call is **D-M2** (descriptive vomit-read tier — surfaced persona conflict, doc §7). NOT on the submission critical path itself (B-039/B-229/230/267–273/280 proceed regardless), except B-330 which is a submission blocker once a build is cut.
- **~~PDF rendering library / §8.2 HTML-first~~ — RESOLVED 2026-07-02 (#260).** PM ratified **HTML-first**: canonical server HTML, rendered **in-app via a WebView** (owner sees it in the app — never a downloaded `.html`), handed to the vet as a **PDF via the native share sheet**. The "which PDF library" Q is demoted to the B-144 render spike; PDF-generation *location* (on-device `expo-print` vs server-side) is a build-time sub-decision (spec §14 S7). **No blocking open questions remain for the vet report — Phase 1 (PR 1) is clear to start.**

---

## Open PM Action Items

**Auth / iOS logout (#412)**
- [ ] **Verify the frequent-logout fix on-device** (the only remaining DoD item — code-confirmed, not yet on-device-confirmed). Get #412 onto the device (Runtime B, or `eas update --branch preview` to your TestFlight build since that's where you repro'd it), then: force-quit → Airplane Mode → reopen → **you stay on Home with local data, NOT the login screen** (old build 34 bounced here). Confirm normal cold start still restores, and Sign out still routes to the Landing. If a logout still recurs, share the Auth-diagnostics trail — a real one now shows `sec.remove {kind:session}` / `SIGNED_OUT` with cause. Once confirmed, the temporary auth-diagnostic scaffolding (`lib/authDebug.ts`, `app/settings/diagnostics.tsx`) can be removed.

**Widget W5 (on-device + spec decisions)**
- [ ] **Answer the §4.1 flip-latency question on a real device** — the one gate the spec put in front of committing to the flip design. It could not be answered in a cloud session (no device, and widget UI is not OTA-able). Cut a dev-client or TestFlight build from this branch, place the widget, tap **Meal**: the picker should appear in well under a second **with the app force-quit**. The code path is extension-local by construction (the interaction intent re-evaluates the layout in-process and reloads the timeline), so this is a confirmation, not a discovery — but if it drags with the app dead, the fallback is precomputing both picker states into timeline entries (recorded in the PR).
- [ ] **Ratify the §4 wording change the outbox forces** — a widget tap is *captured* with no app launch, but becomes a *row* when the app next runs (instantly while it's alive). §5.1 as written says "logged in ≤2 taps with no app launch". Confirm the reading (the tap is the capture; the record follows, same as the offline path §5.6), or tell us to chase a background-write path.
- [ ] **Confirm the undo mechanic** — state-based ("Undo" stays until the app takes the capture off the widget's hands) rather than the mock's "~60s", because a widget gets no guaranteed timed re-render. Rationale + the residual (a drained bowl top-up can't be reverted, unlike a meal/treat) are in the PR.
- [ ] **Check the pet-slot labels with two pets on one Home Screen** (D5 option (a)): Edit Widget shows "Pet 1…Pet 6", and the widget itself shows the pet's real name. If that reads as guesswork at W6, option (b) — the named picker — is the recorded upgrade path.

**Widget (2026-07-24 ratification — Tier-2 doc edits awaiting your confirm)**
- [ ] **Confirm the D-M1 amendment edits** (widget ruled FREE — `docs/nyx-widget-requirements.md` §10): (1) `docs/monetization-and-throttling-requirements.md` — remove/reframe the "Home-screen widgets" Premium bullet (Premium may wrap widget *styles*, never the capture surface); (2) the B-263 paywall bullet swap picks this up when next touched. Per Tier-2 protocol these aren't written until you confirm; one word ("confirmed") unblocks both.

**Monetization (Track-2 pre-submission)**
- [ ] **Paywall pre-submission flip (T2-5, B-330) — recorded config change, not a deploy.** Before cutting the submission build, set `paywall_enabled=false` in the live `app_config` (Supabase dashboard SQL Editor or MCP: `UPDATE app_config SET value='false'::jsonb WHERE key='paywall_enabled';`) so the non-functional mocked trial CTA (a Guideline 2.1/3.1.2 rejection risk) is never reachable in the submission build. The T2-5 code gate is already merged + ship-dark (seeded `true` ⇒ current flow unchanged until you flip it). **Record the flip here in STATUS when done.** Reversible — set back to `true` to restore the mock (e.g. once T3-D un-mocks the paywall).

**Monetization (Track-3 prerequisites — sequence-block RevenueCat wiring, NOT submission)**
- [ ] **Offline actions 1–4** (`docs/monetization-and-throttling-requirements.md` §10): ① App Store Connect → Agreements, Tax, and Banking (decide the legal/merchant entity first; **highest leverage — external latency, everything paid stacks behind it**) → ② Apple Small Business Program enrollment → ③ RevenueCat account + iOS SDK key + webhook secret (→ Secrets Register at T3-C) → ④ subscription products ($4.99/mo, $39.99/yr + 7-day trial; **checkpoint C2 — the D-M5 final price lock**).
- [ ] **B-047/B-016 ownership decision (surfaced by the #344 review) — gate for T3-E, currently unowned.** Both instrumentation items (B-047 AI-Signal conversion/time-to-first-insight = `Next`; B-016 error observability = `Later`) hard-block the extraction gate flip (§9 T3-E, §12 #6) yet appear in **neither track's PR plan**. Decide: (a) pull the minimum conversion + time-to-first-insight events into Track 3 as an explicit owned PR, or (b) run B-047/B-016 as a named parallel prerequisite track. Left unresolved, T3-E silently slips. *(Not on the submission critical path — only gates the post-launch Premium gate flip.)*
- [ ] **Entitlement grace-window duration — confirm at T3-C build time** (`monetization…requirements.md` §4.7; provisional **72h**). Money on both sides (too long = post-cancel free-riding; too short = a paying owner loses Premium in a dead zone). Not a silent engineer default.
- [ ] **Server-side config-read failure fallback — Dir. of Eng sign-off at T2-3** (§4.2; provisional = mirror the client per-key posture, AI keys fail *open*). Currently unspecified in the ratified strategy; an engineering ratification, not a re-decision.
- [ ] **§4.5 shared response decoder — PM call (parked from the #344 review).** Whether to mandate one `lib/aiResponse.ts` `parseAIResult()` type-guard for the typed 200-body contract so cap/flag discrimination isn't reimplemented per caller. The one remaining *structural* review gap; two-line addition to §4.5/T2-4 if adopted.

**Ask (B-228)**
- [ ] **A8 (#409) redeploy — ORDER MATTERS, three functions.** A8 changed `_shared/incident-analysis.ts` (the `transform_only` opt-in, inlined into every incident-analysis bundle) and `ask` (the `read_photo` tool + the A7 guardrail hardening still pending from #408). Redeploy **`analyze-vomit` then `analyze-stool` FIRST** (they only *learn to honor* `transform_only`; default-off ⇒ detail-screen behavior byte-identical, so this is safe/behavior-neutral), **THEN `ask`** (which starts sending `transform_only:true`). If `ask` deploys before the analyze-* pair, a live read hits the old analyze-* and sends a raw (un-stripped) original — the AC-13 violation window. Each via `scripts/deploy-edge.sh <fn>` → Supabase MCP `deploy_edge_function` (preserve `verify_jwt=true`); verify version bump + `ACTIVE` + a JWT'd bogus-`event_id`/`pet_id` boot smoke-test (clean 4xx, not `WORKER_ERROR`). Record here when done. **Dashboard checks (rls-privacy):** `nyx-event-attachments` bucket **PRIVATE**; the Storage image-transform (imgproxy) add-on **enabled** (the sole EXIF/GPS-strip for Ask reads — if off, reads degrade to "couldn't read the photo", never raw bytes). **On-device QA after deploy:** ask "what did her last vomit look like?" on a photographed-but-unread vomit → the answer's read line recounts it + the tap-through opens the event (read now on the detail screen); a no-flag photo never reads "looks fine/clear".
- [ ] **Deploy `ask` v1 (A4, #403) — the one A4 follow-up.** Verified bundle `.edge-build/ask/index.ts` (sha256 `e098f736…`, 102 deno tests green, `node --check` valid). New function, **`verify_jwt=true`**, ship-DARK (`ask_enabled`/`ask_general_enabled` stay off, so it's reachable by no one until the PM allowlists their uid post-A5). Deploy via the runbook (`scripts/deploy-edge.sh ask` → Supabase MCP `deploy_edge_function`; 69 KB, under the B-225 ceiling — inline from a fresh cloud session, or `npx supabase functions deploy ask`). **Then:** `list_edge_functions` shows `ask` ACTIVE + `verify_jwt:true`; a JWT'd bogus-`pet_id` boot smoke-test returns a clean 404 (not `WORKER_ERROR`); `get_advisors` (security) clean (the rls-privacy-reviewer PM checks). Record the deploy here when done.

**Widget (W3/W4 — B-290)**
- [ ] **W3 on-device seams (needs a fresh dev-client build from the W3 branch/`main` — the entitlement rides the binary):** (1) session survives relaunch + a >1h background (the shared-tier migration didn't log you out; no repeated `[secureStore] shared-tier write failed` in the console — on a pre-entitlement binary that warning once per refresh is the *designed* fallback, not a failure); (2) sign out → back in → clean re-hydrate (App Group wipe). Full script in the W3 PR.
- [ ] **T&S call (raised by the rls-privacy pass, now that the session ALSO lives in the shared keychain group):** keep `AFTER_FIRST_UNLOCK` (iCloud-keychain/backup-migratable) or move session tokens to `AFTER_FIRST_UNLOCK_THIS_DEVICE_ONLY`. Pre-existing posture (`lib/secureStoreTiers.ts` header), sharper now; not W3-blocking.
- [ ] **W4 rls-privacy LOW findings — two accept-or-harden calls (PASS otherwise; neither merge-blocking, both within-tenant):** (1) a departed pet's **name** persists as a `pets-index.json` tombstone past the snapshot prune (deliberate — it's the D5/B-086 anti-silent-switch guarantee; name-only, sign-out-wiped; FR-9-vs-D5 tension for T&S); (2) no server-side constraint forces a direct-REST write to `logged_via ∈ {widget,intent}` — a modified extension binary could claim `'app'` provenance into its own account (analytics-level; harden = a small CHECK/trigger migration if wanted).
- [ ] **W4 dashboard check (rls-privacy):** when the extension target exists (W5), confirm its entitlements grant ONLY `group.com.projectnyx.app` and the App Group is owned solely by this team — the load-bearing assumption behind the shared-keychain session read.

**Ship gates / on-device QA**
- [ ] **#423 (W2 SDK 57) post-merge regression QA** — run the 13-step script in the PR on-device (Runtime B; Expo Go still works until W3). Merged ahead of the pass at PM request; a regression found now is upgrade-caused by construction. Also: the **first TestFlight cut from post-W2 `main` must be a native `eas build`** (OTA fenced by `version` 1.1.0).
- [ ] **B-358 (#392) — apply migration `036` live ONLY AFTER the reordered client reaches devices.** The tightened `nyx-food-photos` Storage INSERT (owner subquery) is safe only once every installed client runs the insert-then-upload reorder; applying it while an old (upload-then-insert) bundle is live would `42501` every food-photo upload on that device (the B-182 deploy-gate lesson). So this is NOT a merge-time `apply_migration` — cut/OTA the client build first, confirm it's live for testers, then `apply_migration` 036 via the Supabase MCP + `get_advisors`. **Record the apply here in STATUS when done.**
- [ ] *(Optional, non-blocking)* one-time audit of `nyx-food-photos` for objects planted under the old permissive INSERT (now unreadable/un-overwritable cross-tenant, but worth a sweep before launch). *(The bucket-PRIVATE confirm is DONE — verified live 2026-07-20 audit: all buckets private except `nyx-pet-photos`, whose public posture is now the audit §A3 decision.)*
- [ ] **#361 + #362 (in-app legal wiring) — on-device QA + merge.** Run #362's Manual QA: fresh throwaway signup → "Before you start" acknowledgment → `legal_acceptances` row lands server-stamped (Table Editor); Terms/Privacy open live from the signup acceptance line; You screen's Privacy/Terms/**Veterinary disclaimer** rows all open live pages (no "Coming soon"); airplane-mode acknowledgment shows the calm retry and never advances unrecorded. Merge #361 then #362 (the migration is already applied live, so order is tidiness, not correctness).
- [ ] **B-222 follow-up decisions (all low-stakes, backlog-tracked):** **B-240** — which weight surface is right (Profile snapshot vs Patterns tracked-only); first confirm via the Profile card subtext ("From Nyx's profile" = snapshot = expected). **#2 behavior** — keep the smart default cascade (rec) vs force 90d always. **B-238** — add a pre-generation out-of-window hint or leave the disclosure in-report. **B-241** — "Default" vs "Automatic" chip copy.
- [ ] **#257 — on-device QA + squash-merge (this session).** Run the Manual QA in the PR: **B-196** (Current-medications card loads, no `PGRST201`), **B-198** (log while History is open → no duplicate-key error), **B-197** (Edit a weight → value field pre-filled + saves; empty/`0` blocks Save), plus a spot-check of weight tracking + the chronicity card. Merge when green. **No backend action** — client-only fixes; `generate-signal` v23 (with ⑦) already live.
- [ ] **#255 `analyze-vomit` + photo-load fixes — on-device confirm (the one open PR #255 gate):** open the **6/25 and 6/27 vomit events** → each shows a **real AI read** (not stuck "Try analysis"); tapping the photo opens full-screen **fast** (not 5–10 s). If a photo instead reads "I couldn't read this photo," the Pro Image-Transformation add-on isn't active (safe no-op — flag me). Verifies the 546 fix + server-side downscale + viewer perf end-to-end.
- [ ] **Toggle Supabase "Confirm email" OFF** (Auth → Providers → Email) — was needed to create a *throwaway* account for the B-039 QA; that QA is now done (verified against an existing account), so this is **B-152-only now**: re-enable + provision production SMTP before launch. New-account creation silently failed under email-confirmation (fixed in #213 to show "Check your email").
- [ ] **#213 med-QA fixes — on-device spot-check** (Manual QA in the PR): account-creation "Check your email"; strength-gate toggle; "Current medications" counts a logged dose; End-button red; no `meals 23503` on logging an existing food.
- [ ] **AC-6 logout-wipe** — sign out on a populated device → confirm data empties → sign into a *different* account → confirm no prior-pet data shows (Trust & Safety gate; code passes).
- [ ] **App Store readiness — work the 16-step runway in `docs/app-store-submission-guide.md` (#300; register: `docs/app-store-readiness.md` #297+#300).** Start-now PM steps (all parallel, long-lead first): **step 1** verify the ASC record + lock the store name (B-272) · **step 3** pick a host for the support/privacy/terms page (B-273) · **step 4** provision production SMTP (B-152). Ratify the two advisor recommendations riding the runway: **flag the paywall off for v1** (guide step 6 — removes the freemium decision from the submission critical path) and **`supportsTablet: false`** (rides step 5's B-267 config PR). Each guide step has a kickoff prompt (PR steps) or a "Guide step N complete: …" confirmation (PM steps) that keeps the tracker/backlog/STATUS honest.
- [ ] **Culprit rebrand — remaining items** (`docs/culprit-rebrand-execution-plan.md`, #305): PR A (name+strings) shipped #307; **PR B (Moon & Signal icon + splash) shipped #309** — the design-master gate is **cleared**. Remaining: (1) **PR C** (`colorBrandNight` indigo token) — ready to build, no PM prerequisite; (2) **Supabase auth email template** still says "Nyx" (dashboard → Auth → Email Templates) → Culprit; (3) **cut a new TestFlight build from `main` after merging** to make the icon/splash/name visible — they're native/build-time, so an OTA `eas update` won't move them (`eas build --platform ios --profile production --auto-submit`); do the **iOS-18 dark/tinted on-device verification** on that build (unverifiable in the cloud session; icons don't render in Expo Go).
- [ ] **B-267 built-artifact verify (rides the next iOS build-cut; #299)** — the permission-string config fix is merged, but introspect can't check the built plist. On the next `expo prebuild`/EAS build, confirm `ios/*/Info.plist` carries both `NSCameraUsageDescription` + `NSPhotoLibraryUsageDescription` (Culprit copy) and **not** `NSMicrophoneUsageDescription`, **and** that the aggregated `PrivacyInfo.xcprivacy` privacy manifest is present (Expo SDK 54 auto-aggregates — verify, don't assume). Do NOT assume EAS autolinking supplies them. Batches with the Culprit-rebrand build-cut (item above) — same build.
- [ ] **B-053 on-device QA** — Signal regens against v10; the staple-washout diagnostic only shows in `no_pattern` (Nyx may show the reflection finding instead — correct, not a bug).
- [ ] **Confirm AI summary on-device** — open Patterns for Nyx; expect a safety-led, vet-routing summary with every number matching a card.
- [ ] **Whole-system aesthetic on-device QA** — walk the app vs `docs/design-system/_system/.../index.html` for the holistic Calm/Linear/Oura pass (deferred to end-of-system; all 4 design PRs merged).

**Decisions**
- [ ] **B-325 — promote retroactive/med-first combo linking as part of B-156, or fold direction ① (med→existing-treat) in before promotion?** Rides the B-156 promotion call. Investigation + recommended shape logged as B-325 / #338 (retroactive entry on the treat/meal detail screen; run the same PR B3 safety coupling; never mint a phantom vehicle event). Direction ① is the cheap, high-value slice (reuses the forward write path + safety coupling); minting a new treat from the med side is a scoped follow-up gated on the pure-vehicle question.
- [ ] **B-311 medication colour (#332) — ratify the design-system addition + decide B-323.** A new event-category hue (`colorEventMedication #5B7A9E` slate-blue) shipped respecting §1.3 (not teal/indigo/rose; selection stays teal), applied to the drill-in + Today strip. Two low-stakes calls: (1) sign off on the slate value (or redirect it) — adding a category hue is a design-system decision, not a free choice; (2) **B-323** — should History `EventRow` adopt the full symptom/meal/med tint (parity) or stay deliberately symptom-only? On-device: confirm the med glyph reads slate on both the Today circle + the drill-in grey circle, distinct from symptom-rose/meal-teal.
- [ ] **Logging-capture discovery (#319) — 2 ratifications after reading the brief** (`docs/logging-capture-discovery.md` §9; CLAUDE.md Open Questions 2026-07-10 — PM saw all three at wrap, chose to read the brief first): (1) owner-configured scheduled confirmations vs Principle 4's one-nudge cap (Designer dissent on record — channel trust is one bucket; proposed carve-out = opt-in + fail-safe silence + self-pruning) → unblocks **B-288**; (2) household shared-write minimal primitive as capture *infrastructure* (not a social layer) → unblocks **B-292**. (3) native-targets-in-managed-workflow was **sequencing-CONFIRMED same day: decide after B-288 pilot data** — nothing to do until the pilot reports. Also answer when convenient (non-blocking, sharpens the build): your household's meal-time regularity; does your wife carry iPhone + Watch; what Dog Recruit's logging feels like at its best (B-296 teardown seed).
- [ ] **Login consolidation (#311) — one decision + one device check.** (1) Is a **"Forgot password?" flow (B-280)** in scope now, or left as the tracked backlog item? A login-gated app really wants password reset before App Store submission — it's the sharpest returning-owner gap. (2) **Device check** (unverifiable from code): after **account deletion** → on the login screen press **back** → confirm you land on the Landing, not a flashed empty authenticated tab. Also note **B-281** (social-auth block absent from login) must be resolved when PR 11 flips `SOCIAL_AUTH_ENABLED`, or an Apple/Google-signup owner can't log in.
- [ ] **B-251 onboarding — ratify the `design-principles.md` §Onboarding revision (Tier-2 edit, flagged not written)** before/around PR 4–5. The constitution still says "minimum viable / required: name+species / first log in 60s / complete-at-first-log"; the design-locked flow outgrows all of it (owner identity + optional pet richness + store gates; finish line moves to profile-created→warm-home) and Principle 2's "food library pre-populated from onboarding" should soften to "on first meal log" (food capture moved in-app). Also: **Google OAuth client** to provision before **PR 11** (Apple Developer already enrolled) — not blocking PRs 1–10.
- [ ] **Work the 2026-07-20 hardening-audit top actions** (`docs/app-store-hardening-audit-2026-07.md`): ① delete the orphaned `view-report` + `zz-deploy-probe` functions (B-397, dashboard, 2 min); ② greenlight the B-248 vet-attachments owner-scoping migration PR (§A1 — the one pre-multi-user blocker); ③ pet-photos-public posture call (§A3); ④ the 30-second live `pg_policies` verify (§A4); ⑤ B-403 Supabase config flips (~15 min). *(Migration 034 is confirmed APPLIED live — the old apply-034 action is done; `analyze-stool` PR-3 prerequisite cleared.)*
- [ ] **B-099** — reopen spec §13 #2 (over-time views + dashboard 7d/30d range) before building B-099.
- [ ] **B-080** — placement of diet-structure observations (Signal band-2 vs B-053 coverage lane); blocks descriptive Phase 3.
- [ ] **B-023 colour** — reconcile Home `TrendZone` (colours a falling symptom "improving" in the accent, `TrendZone.tsx:66/79`) to the §11 #3 ruling, or accept the cross-tier difference.
- [ ] **B-065** — should the food-capture add-then-log path also show the meal completion card (intake chips + "Change time")?
- [ ] **B-063** — approve the Tier-2 design-principles line (tone-aware "moment" — calm, never festive on a worrying event); awaiting PM sign-off before writing.
- [ ] **PR #79** — env fail-fast guard + CLAUDE.md doc fix: mark ready/merge or close.
- [ ] **Vet report — PM next actions (2026-07-02):** (1) **email your vet the Nyx report + book the appointment** (the real-vet signal, parallel to the build); (2) **weigh Nyx** before the visit (fills the one empty box + the datum the vet most wants).
- [ ] **Principle 6 doc-drift (Tier-2 edit)** — `nyx-design-principles-v1_0.md` §6 **and** `nyx-technical-spec-v1_0.md` §7 AC both still list "severity averages" as desired report content; the mock + spec §8.4 deliberately use frequency-over-severity (Dr. Chen's stated preference). Both cold reads flagged it. Approve the doc-line update on **both** files (+ the §7 must-carry-section / HTML-first / PDF-library-reshape edits the spec §13 flags — all flagged, not written).
- [ ] **Vet-report mock — synthetic-panel feedback logged** (`docs/vet-report-mock-review.md`). PM follow-ups from it: (a) **B&W test-print** the mock (zebra/band gray on a mono laser); (b) decide the spec-level **report-range cherry-pick guard** (disclose events outside a custom window); (c) greenlight the **second mock — cat/safety-led case** (Sam, feline 48h window) to exercise the safety-leads slot. The real-vet R1/R2/R4 panel is the gate (§5 of that doc is reserved for it).

**Cleanup / infra**
- [ ] **B-128(b)** — defense-at-rest `BEFORE INSERT/UPDATE` trigger on `medication_items` (own schema PR; run the backfill pre-check first). Not urgent — the consumer-side guard is live via #200.
- [ ] **Redeploy `generate-signal` to un-gate chronicity detector ⑦ (B-182)** — PRs 1–3 (#246/#247/#250) all merged; the engine registers ⑦ but the **live function predates it**, so chronicity won't surface until a redeploy. Deploy from merged `main` via the Supabase MCP (bundle `scripts/deploy-edge.sh generate-signal` → `deploy_edge_function`, preserve `verify_jwt=true`, byte-gate + boot smoke-test — the B-082 path). The client renderer is live as of #250, so the card renders the moment the cache carries a `symptom_chronicity` finding.
- [ ] **D2 ratification — chronicity `minEpisodes` 6-vs-5 (Dr. Chen)** — the §7 #14 noise gate forced 4→6 (specificity); the PR-1 adversarial review measured a false-negative cost at 6 (a once-weekly-×5 / q2wk-×4 real chronic course is silenced). 6 = credible-surface; 5 = catch-the-once-weekly, defensible because the safe error direction for a safety lane is toward firing. A property test on *real low-count chronic courses* should sit alongside the noise test before the floor locks.
- [ ] **Revoke the Supabase personal access token** (`nyx-cli-deploy`, 2026-06-07) — account-level, lives in a session transcript. Now obsolete: B-082 (#208) made backend deploys a token-free MCP path, so nothing uses it.
- [ ] **Supabase CLI dev-dependency** — fold `supabase@^2.102.0` (on branch `claude/epic-volta-H8d6o`) into a PR so it survives merge.
- [ ] _(awareness, no action)_ **B-074** — the free-fed exclusion fails safe but a single stray free-fed day landing on a selected control day can silence a real correlate.
- [ ] _(awareness, before treating vet-report §8.6 as fully closed)_ **B-115 residual → B-163** — the shipped exact-ms treat-collapse (#219) guards the diet-confounder over-count for the **EXIF-collision / future-batch** case only; today's full-precision `new Date()` stamps mean a **rapid per-tap handful (distinct ms) is still over-counted**. Fully closing it needs a real-data-gated near-window widening (under-count tension) or a batch-log path — Phase-2/PR-6 can proceed; just don't read §8.6 as "fully resolved."

---

## Runtime in Use

**TestFlight (real iOS builds) — primary on-device target since 2026-06-07.** Runtime B (Metro `npx expo start --tunnel` + the **custom dev client** — Expo Go retired as of W2/SDK 57, transition window until W3; see `docs/dev-handoff-runbook.md`) remains available for fast local iteration.

**Cut a new TestFlight build** (verified build 22 from `main`, 2026-06-12) — one command builds the store-signed binary and submits that same artifact:
```
eas build --platform ios --profile production --auto-submit
```
**OTA to the installed build:** `eas update --branch production` (the build's channel is `production`, NOT `preview`).

**Traps (each cost a session — do not repeat):**
- **SDK 57 OTA fence (W2, 2026-07-24):** `version` bumped 1.0.0 → 1.1.0 so SDK-57 bundles never reach the installed SDK-54 TestFlight build (`runtimeVersion.policy: appVersion`). Until a fresh binary is cut, `eas update --branch production` is a harmless no-op against that build; the **first post-W2 TestFlight cut must be a native `eas build`**, never OTA. Never revert the version to "fix" a non-arriving OTA — an SDK-57 bundle on the SDK-54 binary is a crash-on-launch. Full note: `docs/dev-handoff-runbook.md`.
- Never build TestFlight with the `preview` profile — it's `distribution: internal` (ad-hoc), never store-submittable. Use `production`.
- Never submit with `eas submit --latest` — it skips internal builds and re-uploads a stale store build (the "build number 8 already used" loop). Use `--auto-submit` (binds the upload to the binary just built).
- "Build number N already used" = EAS counter is behind App Store Connect → `eas build:version:set --platform ios`, then rebuild. (Build numbers live on EAS; `appVersionSource: remote`, `autoIncrement`.)
- All 3 `eas.json` build profiles carry the `EXPO_PUBLIC_SUPABASE_*` `env` block (#90) — EAS cloud builds never see the gitignored `.env.local`; don't strip it (was the first crash-on-launch).
- `app.json` → `ios.infoPlist.ITSAppUsesNonExemptEncryption: false` (#121) auto-answers Apple's encryption questionnaire; keep it committed.

---

## Recent Sessions

_Last ~10 only; older history lives in git (`git log`) + PR descriptions._

- 2026-07-24 — **Widget PR W5 — the Culprit Home Screen widget** — `expo-widgets` + the `systemMedium` `CulpritWidget` (app.json plugin config: App Group, `systemMedium`, the D5 `petSlot` enum of 6 slots) rendering the design-locked round-3 states: resting (status column + whole-tile Meal/Treat targets, CulpritMark geometry rebuilt from SwiftUI primitives), the in-place flip to named choices (trial-diet lead row, hybrid `Top up bowl` labelled "not a meal", the dashed `Something else…` app door always last), and the just-logged/undo strip — plus three doors (signed out / unbound slot / tombstoned pet, D5's anti-silent-switch state). **Architecture finding:** the layout is a stringified self-contained function evaluated in the extension's JavaScriptCore (no imports, no I/O), so the flip is extension-local (§4.1 Q2) and W4's intents can't run there — taps land in an **outbox** in the widget's props and `lib/widgetBridge.ts` replays them through the shipped intents with the widget's tap-time ids (undo = one path, pre- AND post-drain, the W3 §4.1 Q5 recommendation). Drain-before-publish ordering; sign-out wipes the timeline (UserDefaults, which `clearWidgetData`'s directory delete never touched). New: `lib/widgetProps.ts` (pure props/timeline builders + outbox collection), `hooks/useWidgetPetLink.ts` (deep links carry `?pet=` so the app opens on the widget's pet), `plugins/withoutPushEntitlement.js` (expo-widgets 57.0.6 adds `aps-environment` unconditionally — stripped; ordering verified via `expo config --type introspect`). `widgets/CulpritWidget.test.ts` evaluates the REAL emitted string in a faithful stand-in of the extension's global context, so a leaked module reference fails in CI, not on a Home Screen. Snapshot gains `bowlConfirmedAt` (bowl ✓ only when re-attested today). nyx-voice fixes applied (empty state names the pet; "logged 7:42a" replaces a "just now" that would go stale). tsc + jest 110 suites / 1666. Client-only, no schema, no deploy; **widget UI is not OTA-able** — pairs with a dev-client/TestFlight build. — shipped via #428
- 2026-07-24 — **History filter UX — the last h-scroll chip rail retired (PM report: the Medication filter was undiscoverable — 8th of 9 chips past the fold; B-146's edge-fade carve-out for this row failed in practice)** — the event-type filter becomes the same quiet pill + bottom-sheet as the date scope, via a new shared generic `components/ui/ScopeMenu` (every option a full-width sheet row — none can hide; rows scroll INSIDE the sheet past 75% height; the pill tints teal when any non-default scope is active so a filtered list reads as filtered from the header; radio-style a11y, 44pt floors) + `components/history/TypeScopeControl` (EVENT_TYPES-driven labels+glyphs; **adds the missing `weight_check` filter** — loggable since B-186, unfilterable until now) + `DateScopeControl` re-based as a thin wrapper (API + B-308 day-drill-in override preserved, test-covered). Header = `History · [All types ▾][All time ▾]`; the chip FlatList + LinearGradient fade deleted (−44pt header chrome). ChipGroup-vs-ScopeMenu split documented (options-as-content wrap in-line; options-as-lens go behind a pill). Same session (PM-directed): before/after mock `docs/history-filter-mockups.html` (artifact-published) + **`docs/nyx-filter-ux-requirements.md`** — the app-wide filter/scope pattern language (four sanctioned shapes + six rules), grounded in a full inventory whose verdict is that the hidden-h-scroll class is **already dead app-wide** (History was the last rail); only two borderline surfaces, both kept as-is recommend-and-proceed (D1 FoodPicker scope, D2 Calendar lens w/ a ≥5-lens conversion tripwire → **B-405**); conditional PRs F1/F2 pre-scoped, F3 = the Tier-2 `design-principles.md` edit (PM-gated). CLAUDE.md Code Conventions gained the lens-shapes paragraph. Client-only; no schema, no deploy. `tsc` + jest 1513 (101 suites; +5 `ScopeMenu` tests) green. — shipped via #421
- 2026-07-24 — **Widget PR W4 — App Intents + resolution logic** — `lib/widgetResolution.ts` (pure, tested: learned 14d meal slots [no declared slot model exists — B-040 is free-choice-only; recorded design decision], slot→usual-food w/ 60%/≥3 stability floors, trial-diet override [no-slots-only fallback — no overfeed nudge], 90d treat shortlist w/ brand+product pooling, trial day via the shared B-084 math + additive `trialTargetDays`); the three intents in `lib/widgetCapture.ts` (inbox-file-first — failed write = surfaced failed tap; best-effort owner-JWT PostgREST leg, column parity w/ sync.ts; `bowl_topup` = arrangement re-attest, parsed-ms guarded, no REST leg, no regen); `lib/widgetSession.ts` read-only SHARED_TIER session reader (never refreshes / never holds the refresh token) over the new dependency-free `secureStoreTiers.ts` + `captureRecord.ts` (extension-bundle purity, pinned by a static import-graph guard test); **D5 SETTLED = option (a)** pet-slot enum + sticky tombstoned `pets-index.json` (B-086 anti-silent-switch; (b) = named-picker upgrade path). `rls-privacy-reviewer` PASS (2 LOW PM accept-or-harden items recorded); `code-reviewer` fix-before-merge all fixed in-branch (incl. a real B-055 lexical-SQL catch). tsc + jest 106 suites / 1617. W5 unblocked. — shipped via #426
- 2026-07-24 — **B-351 Phase A PR 1 — `food_items.proteins TEXT[]` migration + cache mirror shipped + applied live** — additive migration 039 (prominence-ordered canonical keys, `DEFAULT '{}'`; pre-flight clean, rollback = `DROP COLUMN`) with a one-shot in-SQL `canonicalizeProtein` mirror backfill (explicitly one-shot-only; `lib/protein.ts` stays the source of truth) — applied via Supabase MCP + parity-verified on all 56 rows / 16 distinct `primary_protein` values ("Chicken By-Product Meal"→`{chicken}`, literal "null"→`{}`, "ocean whitefish" un-mapped per the B-048 scope note); advisors clean. Client: `food_items_cache.proteins` (JSON-text, #385 ALTER pattern), `refreshFoodCache` pull (ON-CONFLICT `last_used_at` footgun regression-tested), both Pattern-6 pre-sync paths carry the set up (offline-captured foods never flatten to the server default), new `proteinsToCacheText`/`proteinsFromCacheText` helpers (decode degrades to protein-unknown, never throws). `primary_protein` untouched = derived `proteins[0]` from PRs 2/3. No index (no server-side predicate; Phase B computes in TS). tsc + jest 100 suites / 1514 green. Unblocks Phase A slices 2–5 (parallelizable). — shipped via #424
- 2026-07-24 — **Widget PR W3 — B-290 App-Group write path** — every app-side bridge W4/W5 stand on: `lib/appGroup.ts` (container `group.com.projectnyx.app` + entitlement via CNG + FR-9 sign-out wipe) · `lib/captureInbox.ts` (file-per-capture inbox; pure classification trust boundary — no-garbage D2 structural, provenance narrowed to `widget`/`intent`, UTC normalized at the boundary; idempotent `INSERT OR IGNORE` ingest wired pre-push in `useSync`; FIFO oldest-first under an **apply** cap so deferrals never starve a tap; 45-day age-out) · `lib/secureStore.ts` shared-keychain tier (App Group id as `kSecAttrAccessGroup`; passive local→shared migration; distinct per-tier key namespaces vs. the cross-group `SecItemDelete` trap; fall-back-down writes so an entitlement-less binary degrades to pre-W3 behavior; **generation retention** so the W4 cross-process reader can't torn-read an ordinary refresh) · `lib/widgetSnapshot.ts` + `useWidgetSnapshots` (per-pet snapshots, D9-unexpressible contract key-set-pinned, debounced publish on event/pet/hydration change, stale-pet prune, B-055-safe ms-window) · `logged_via` client mirror + push/hydration round-trip (migration 038's deferred half). §4.1 spike answered from the pulled `expo-widgets@57.0.6` package (configurable widgets YES but params **static** → D5 pet-picker = W5 decision; flip latency = on-device gate). `rls-privacy-reviewer` PASS (1 LOW within-tenant residual recorded); `code-reviewer` fix-before-merge findings all fixed + regression-tested. tsc + jest 102/1546. — shipped via #425
- 2026-07-24 — **Widget PR W2 — Expo SDK 54→57 upgrade (no features)** — the riskiest-blast-radius PR of the widget chain, run solo per spec §7. SDK 57 chosen over 56 (current stable; `expo-widgets` 57.0.6 = latest there). RN 0.81.5→0.86.0 · React 19.1→19.2.3 · TS 5.9→6.0 · unified 57.x `expo-*` · jest-expo 57 (+ its new `@react-native/jest-preset` peer) · `expo-dev-client` added · `@expo/config-plugins@57.0.6` top-level (unhoisted peer of the datetimepicker plugin; expo-doctor-documented exemption). `app.json`: **`version` 1.0.0→1.1.0 = the OTA fence** (SDK-57 bundle on the SDK-54 binary would crash; A-OTA now a no-op until the first post-W2 **native** build) + SDK-57 schema cleanup (newArch/edge-to-edge always-on; splash → plugin, same values). Mechanical code deltas only: `absoluteFillObject`→`absoluteFill` (removed in RN 0.86, 12 sites), explicit jest types under TS 6, 2 test-utility fixes, CulpritMark snapshot re-recorded (React 19.2 `useId` format — ids only, geometry unchanged). Runbook rewritten for the Runtime-B dev-client switch (Expo Go = stopgap until W3). tsc + jest 100 suites/1508 green. **Merge gate: PM full-app on-device regression** (script in the PR). — shipped via #423
- 2026-07-24 — **Widget PR W1 — B-289 `logged_via` migration (038) shipped + applied live** — new `logged_via` enum (`app`·`notification`·`reconciled`·`widget`·`intent`·`watch`·`device`) + `NOT NULL DEFAULT 'app'` column on `events`/`meals`/`medication_administrations`; additive, own schema PR, pre-flight clean (destructive n; backfill = the default — a true claim while the app is the only capture surface ever to have written a row, the whole reason W1 leads the chain); distinct-from-`events.source` + all-three-tables rationale in the migration header; RLS unchanged by construction. Applied via Supabase MCP `apply_migration` + verified (3 columns live; 854/709/32 rows read `'app'`; advisors clean of new findings). No client code touches the column (rides W3). Unblocks W3–W5 + B-288 provenance; W2 independent. — shipped via #422
- 2026-07-24 — **B-351 multi-protein capture — design session; PM ratified D1–D8, spec v1.0 build-ready** — resolved the gating Open Question (sensitivity vs. attribution = false binary: capture wins *exposure*; collinearity-aware joint candidates keep *culprit-ID* honest, "let the vet piece it together"). Wrote `docs/nyx-multi-protein-requirements.md` v1.0 + `docs/nyx-multi-protein-mockups.html` (4 surfaces: two-line Main/Also-contains picker · contaminant catch at **add+log** [log-time non-blocking, Principle 1] · joint-candidate linked-pair Signal · vet-report primary+secondary, Dr. Chen-ratified). Rulings: D1 capture-all/phased · D2 flag-at-add+log · D3 absorb B-048 · D4 `proteins TEXT[]` · D5 joint compute-always/Home-above-floor/report-is-synthesis · D6 no trial excluded-list v1 · D7 disclosure(all)-vs-escalation(trial) · D8 two-line picker · VR report Phase-A fast-follow. **6-PR plan** (Phase A capture+contaminant+report; Phase B correlation, `adversarial-reviewer`-mandatory). Docs-only, no code/schema; B-351 `Later`, build not started. — shipped via #416
- 2026-07-24 — **Home Screen Widget ideation → ratified requirements (docs-only)** — PM-initiated exploration built on the 2026-07-10 capture discovery; platform re-verify caught `expo-widgets` going stable in SDK 56 (JS-authored, no Swift — reshaped OQ3); 3 mock rounds w/ PM reactions (garbage-data kill → the logs-only-what-it-can-name rule; jobs-to-be-done spine; B's simplicity imported into A; hybrid feeding; per-widget pet binding) → `docs/culprit-widget-mockups.html` (design-locked) + `docs/nyx-widget-requirements.md` v1.0 (D1–D9, PRs W1–W6). OQ3 resolved (CLAUDE.md); widget FREE (amends D-M1, Tier-2 edits flagged); B-289/290/291 → Now. — shipped via #419
- 2026-07-24 — **Landing-hero Signal pulse re-choreography (PM + spouse feedback: "abrasively prominent… rate isn't even symmetrical")** — two-round design session over a live artifact mock: round 1 softened (4.4s breathe-and-release) → PM compared the frame-locked mock of the ORIGINAL values against the device and ruled the radar format wins — the roughness is `Animated.loop`'s per-cycle JS-thread restart stalling (worst in Expo Go dev mode), not the design → 1/2/3-ring trains mocked → **three rings ratified**. `CulpritMark` now emits a staggered 3-ring concentric train at the original values (2.6s/ring, 0.66×→2.1×, opacity 0.9→0, dot 1.12×), dot + each ring on an INDEPENDENT loop so a stalled restart lands while that layer is invisible/at rest (round-1 softening reverted in-branch). B-322 native-driver contract + reduced-motion/blur static frame untouched; pulse-contract tests pin the train. Reanimated fallback if a production build still stutters → B-404 (Later). tsc + jest 100 suites / 1508 green. Client-only; no schema, no deploy. — shipped via #420


